- [x] [Settings · slide 2] Fix the "Upload logo" button (POST /files returns 500)
  - [x] Reproduce the 500 on `front-end/islands/SettingsPage.tsx` logo upload, tracing the request to `backend/src/files/entrypoints` (the `/files` controller) and `backend/src/files/domain` (root cause: FileStore.create wrote meta+index+ALL pages in ONE kv.atomic() — Deno KV caps an atomic op at 819,200 bytes, so uploads ≥ ~768KB 500'd)
  - [x] Fix the upload handler so PNG/JPG logo uploads succeed and the returned URL persists to the user's business profile (pages now commit in batches with meta last; 8MB max returns a clean 413; logo renders on public quote/contract/invoice docs via the new capability-gated GET /public-logo/:kind/:id)
  - [x] Surface a friendly error in the UI instead of the raw `POST /files failed: 500` text currently shown at the bottom of the form
  - [x] Verify: upload a PNG and a JPG from Settings → logo persists and shows on quotes & invoices; no 500 in backend logs (verified: 1.9MB PNG + 625KB JPG via the UI, byte-perfect round-trip, logo visible on the public quote page)
- [x] [Settings · slide 10] Make remaining business settings editable
  - [x] In `front-end/islands/SettingsPage.tsx` add editable fields for Mailing Address, plus uploads for Insurance and Tax W-9
  - [x] Extend the user/business DTO and controllers to store mailing address + insurance file ref + W-9 file ref (fields live in sibling DTOs: business-address.ts, business-insurance.ts, tax-identity.ts — all routed through the same /files flow)
  - [x] Verify: edit mailing address, upload an insurance doc and a W-9 → values persist across reload and appear where business details are read
- [x] [Nav · slide 12] Make the logo / "Paperwork Monster" wordmark link to Dashboard
  - [x] In `front-end/islands/DashSidebar.tsx` (and `front-end/components/ui/Brand.tsx` if it renders the wordmark) wrap the logo + "Paperwork Monster" text in a link to `/dashboard`
  - [x] Verify: clicking the logo or the wordmark navigates to the dashboard
- [x] [Nav · slide 12] Capitalize "My assistant" → "My Assistant"
  - [x] Fix the label in `front-end/islands/DashSidebar.tsx` (and any i18n string under `lang/` / `backend/src/core/i18n`) so the "A" is capitalized everywhere it renders
  - [x] Verify: sidebar reads "My Assistant" in EN and the Spanish string is consistent
- [x] [Nav · slide 12] Fix sidebar item set + order to Dashboard, Clients, Quotes, Contracts, Invoices, Payments
  - [x] In `front-end/islands/DashSidebar.tsx` order the nav items exactly: Dashboard, Clients, Quotes, Contracts, Invoices, Payments
  - [x] Remove the standalone "Settings" nav item — it duplicates the "Hans Pedersen" (account) entry; reach Settings via the account menu instead
  - [x] Verify: sidebar shows the six items in order with no separate Settings link; the account/profile entry still reaches Settings
- [x] [Nav · slide 12] QuickBooks-style collapse control for the sidebar
  - [x] In `front-end/islands/DashSidebar.tsx` replace the current arrow between the account row (HANS LLC) and Settings with a hamburger + arrow affordance that collapses/expands the rail (QuickBooks pattern)
  - [x] Reuse the same collapse concept for the PM Assistant conversations panel (`front-end/islands/AsstThreads.tsx` / `AsstChat.tsx`)
  - [x] Verify: clicking the control collapses the sidebar to icons and expands it back; the same control collapses the assistant conversation list
- [x] [Nav · slide 10] Fix the PM Assistant hamburger menu icon (currently does nothing)
  - [x] Trace the broken handler in `front-end/islands/AsstChat.tsx` / `front-end/islands/AsstThreads.tsx` (or `ChatHeaderLive.tsx`) and wire the hamburger to toggle the conversation/menu panel (AsstThreads toggle collapses the conversations panel; the topbar hamburger drives the rail/drawer via the pm:sb-toggle event)
  - [x] Verify: tapping the hamburger in the PM Assistant view opens and closes the menu on desktop and mobile
- [x] [Dashboard · slide 10] Surface "My Assistant" at the top of the dashboard (mobile)
  - [x] In `front-end/islands/DashboardPage.tsx` add a prominent "My Assistant" entry at the top so mobile users don't need the hamburger to find it (hero CTA row in `front-end/components/DashSections.tsx` now always includes a My Assistant entry)
  - [x] Verify: on a phone-width viewport the dashboard shows "My Assistant" above the fold without opening the sidebar
- [x] [Assistant · slide 3] Add a "Job done need to invoice." entry button on the assistant start screen
  - [x] Add a fourth option that starts an invoice-generation flow (the start screen lives in `AsstChat.tsx`, not `AsstComposer.tsx`)
  - [x] Use the wording "Job done, need to invoice." (slide note: contractor deleted their notes — confirm exact wording with them; trivial copy change in `asstChat.prompt.invoiceDone` if their phrasing differs)
  - [x] Wire it to the invoice flow: details → amount → pick/add client → standalone invoice (due on receipt) with link + send actions; lands in /invoices
  - [x] Verify: the new button appears on the assistant entry screen and launches the invoice flow (verified end-to-end in browser: $500 invoice for Maria Gonzalez, public page renders job name + amount)
- [x] [Assistant · slides 4 & 5] Add a "Job Completed" option to the start-date and duration steps
  - [x] In `backend/src/agents/domain/business/contract-terms-wizard-spec/mod.ts` add a "Job Completed" option to the `start_date` step ("When does the job start?") and the `wraps` step ("How long will the job take?"), with matching i18n keys (EN "Job Completed" / ES "Trabajo terminado")
  - [x] (slide note: contractor deleted their notes — confirm the exact button wording; "Job Completed" shipped as their best recollection — one-line lang change if they recall differently)
  - [x] Handle the new option id downstream where wizard answers map onto the Contract DTO (term values localize on doc + PDF; pairs with "Due Now" for done-job paperwork)
  - [x] Verify: both steps render a "Job Completed" choice in `front-end/islands/AsstChat.tsx` and selecting it carries through to the draft (verified in browser; terms show on the review + public contract)
- [x] [Assistant · slide 6] Add a "Due Now" option to the payment-terms step
  - [x] In `backend/src/agents/domain/business/contract-terms-wizard-spec/mod.ts` add a "Due Now" option to the `payment_terms` step (first in the list, sub "Full amount due on signing"), with i18n keys; the doc/PDF payment schedule reads "Due on signing" and the auto-generated first invoice is due same-day instead of net-7
  - [x] (slide note: contractor deleted their notes — confirm exact wording; "Due Now" shipped as their recollection)
  - [x] Verify: the payment step renders "Due Now" and the choice persists onto the contract terms (verified in browser through the full wizard)
- [x] [Assistant · slides 7 & 9] Make the Job Details step editable (titles + line items)
  - [x] In the Job Details picker (assistant step rendering the "Toilet Replacement / Toilet Install / Bathroom Toilet Service" cards) make the selected card's bold title editable, not just the line items (click-to-edit titles; edited titles re-translate — shipped in commit e67c3f1)
  - [x] slide 9 (Quotes note): "I need to be able to edit the main bold words because it does not fit my description at all" — allow the contractor to rewrite the bold job title/headers
  - [x] Touch the relevant island (`front-end/islands/AsstChat.tsx`) and the job-details data path in `backend/src/agents/domain` / `backend/src/paperwork/dto/quote.ts`
  - [x] Verify: pick a job template, edit the bold title and a line item → edits persist into the quote draft and render on the preview
- [x] [Assistant · slides 8 & 10] Add a Back button throughout the assistant steps
  - [x] Add a working "Back" control on every wizard step in `front-end/islands/AsstChat.tsx` (wizard steps rewind server-side; the Job Details picker, price screen, confirm screen, and invoice-flow steps all have Back; the draft/quote screen edits per-term without rewinding)
  - [x] Ensure going Back restores the prior step's selections without losing later answers where possible (rewind now returns the popped pick and the re-asked step highlights it as "Your previous choice")
  - [x] Verify: from the draft/quote screen and each step, Back returns to the previous step with prior input intact (verified in browser: wraps → Back → start-date re-asked with "Job Completed" highlighted)
- [x] [Assistant · slide 18] Build out the "I know the job, help me price it." flow
  - [x] First screen = Job Details, then ask any needed questions (by design the assistant favors named assumptions over interrogating the contractor — the polished options on the confirm screen surface those assumptions for editing, which covers the "needed questions" intent without extra steps)
  - [x] Next screen = confirm details (the Job Details picker now runs BEFORE pricing for this flow: pick/edit one of three polished versions or write your own)
  - [x] Then present three pricing options + a fourth Custom for the contractor to choose (suggestions are priced on the CONFIRMED scope)
  - [x] Then continue through the remaining wizard steps (start date, duration, payment, warranty) — the end-of-flow picker is skipped since details were already confirmed
  - [x] Implement in `AsstChat.tsx` and the agent flow under `backend/src/agents/domain`
  - [x] Verify: selecting "I know the job, help me price it." walks Job Details → confirm → 3+1 pricing options → remaining steps end-to-end (verified in browser, including Back from pricing → confirm)
- [ ] [Assistant · slide 19] Define / build the "Just give me a quick quote." flow — BLOCKED on contractor input
  - [ ] Clarify with the contractor how this differs from "I know my price, write it up." (slide note: "I am not sure how this is any different… We need to see how this option could work") — the two are currently a deliberate alias (roadmap p.11 merged them); nobody can define the distinct flow but the contractor
  - [ ] Once defined, implement the distinct path in `AsstChat.tsx`
  - [ ] Verify: the "Just give me a quick quote." option produces the agreed distinct flow
- [x] [Assistant · slide 10] Auto-generate a "Job Name" (≤3 words) from job details
  - [x] Add logic (agent step in `backend/src/agents/domain`) that summarizes the job details into a job name of three words or less (generate-job-options + polish-job-details, clamped to 3 words; the "write it myself" path now clamps to 3 too)
  - [x] Use this Job Name consistently across the platform (public quote hero + title, contract hero, invoice hero, email subject, SMS body, quote/contract list cards; standalone invoices carry their own jobName)
  - [x] Verify: creating a quote yields a ≤3-word Job Name that appears identically on the quote, contract, invoice, and message templates
- [x] [Quotes · slide 13] Progress the status badge Draft → Sent → Viewed → Approved
  - [x] Extend quote status handling in `backend/src/paperwork/dto/quote.ts` (and view tracking in `backend/src/paperwork/entrypoints/view-controller`) to support Sent, Viewed (on open), and Approved (on signature)
  - [x] Update the badge rendering in `front-end/islands/QuoteCard.tsx` / `front-end/islands/QuoteTrack.tsx` to show the current state instead of a static "Draft"
  - [x] Verify: send a quote → badge shows "Sent"; recipient opens it → "Viewed"; recipient signs → "Approved" (public quote GET now records a View + flips sent→viewed; card stage derives "opened" immediately)
- [ ] [Quotes · slide 13] Apply the Quote + Agreement preview changes — BLOCKED on a missing reference doc
  - [ ] Apply the updates described in the reference doc "Quote & Agreement - Preview.docx" to the quote preview (`front-end/islands/QuoteCard.tsx` / `front-end/components/QuotesSections.tsx`)
  - [ ] (Action needed: obtain "Quote & Agreement - Preview.docx" from the contractor — it is referenced on slide 13 but not in this deck; nothing can proceed without it)
  - [ ] Verify: the preview matches the Preview.docx spec once the doc is in hand
- [ ] [Contracts · slide 14] Apply the final contract-document changes — BLOCKED on a missing reference doc
  - [ ] Apply the updates in the reference doc "Quote & Agreement - Final.docx" to the contract document renderer (`front-end/components/contract-doc.tsx` and the PDF path in `backend/src/paperwork/domain`)
  - [ ] (Action needed: obtain "Quote & Agreement - Final.docx" — referenced on slide 14 but not in this deck; nothing can proceed without it)
  - [ ] Verify: rendered contract page + PDF match Final.docx once the doc is in hand
- [x] [Email · slide 15] Remove the literal quotation marks from the email subject
  - [x] In the email subject builder strip the surrounding `""` — the subject now builds from the paperworkEmail.quote.subject i18n key with no quote characters (send-paperwork-email/mod.ts)
  - [x] Verify: a sent quote email has a subject with no stray double-quote characters
- [x] [Contract · slide 16] Update the "04 SIGN HERE" block copy/labels
  - [x] In `front-end/islands/PublicSignContract.tsx` / `front-end/components/contract-doc.tsx` set: heading "04 SIGN HERE"; line "By signing below, [Client] agrees to everything above."; left column CONTRACTOR = business (e.g. HANS LLC) with "SIGNATURE / By: [contractor name] / Date:"; right column "YOUR Signature" = "Sign & type name below"
  - [x] Keep everything else in the block the same
  - [x] Verify: the public sign page shows the contractor signature block on the left and the customer "Sign & type name below" on the right with correct labels (verified in browser on an unsigned contract)
- [x] [Messages · slide 10] Send a completion Text + Email after a quote/contract is signed
  - [x] On signature/approval, trigger a completion notification through `backend/src/communication` (both SMS and email) — fire after the Approved transition from the slide-13 item (contract path: SendSignedConfirmation, already shipped; quote-accept path: new SendAcceptedAlert emails + texts the contractor)
  - [x] Verify: signing a contract sends the contractor (and/or customer) a completion text and email (verified in dev logs: email ok=true, sms ok=true on accept)
- [x] [Messages · slide 17] Implement the customer text template
  - [x] Use this body for the quote-ready SMS: "Hi [Customer Name], this is [Contractor Name] from [Business Name]. Your Quote + Agreement for [Job Name] is ready: [LINK] Please let me know if you have any questions. I look forward to working with you!"
  - [x] Wire placeholders to real values (Customer/Contractor/Business/Job Name, LINK as a minted short link) in send-paperwork-sms (EN + ES, graceful fallbacks when a piece is missing)
  - [x] Verify: a sent quote SMS renders the template with all placeholders filled and a working link
- [x] [Payments · slide 11] Let contractors enter their payout handles
  - [x] Add a settings UI for Venmo, CashApp, Zelle, PayPal, Check, Cash (SettingsPage PaymentsEditCard covers venmo/cashapp/zelle/paypal/check/ach/card/cash)
  - [x] For each handle use a confirm-by-retype input ("here is my handle, retype to make sure the right one was typed in") and persist on the user profile
  - [x] Keep this consistent with project rule: payments are manual claim→confirm only (no card/merchant processing)
  - [ ] (slide 9 Notes also lists "Direct Deposit" and "Credit Cards" under Payments — confirm scope with contractor; these conflict with the manual-only model and may be out of scope for now) — BLOCKED: needs contractor decision; ACH shows as contractor-written instructions only, card stays a label with no processor
  - [x] Verify: enter Venmo + Zelle handles with retype confirmation → handles persist and surface to the customer on the invoice/payment view
- [x] [Clients · slide 9] Add a Business Name field to clients
  - [x] Add an optional "Business Name" field to the client form (the real add-client modal lives in `ClientsPage.tsx`; `ContactForm.tsx` is the landing OTP capture) and the customer DTO (`backend/src/crm/dto`)
  - [x] Display business name where clients are listed (`front-end/islands/ClientsBoard.tsx` / `ClientsPage.tsx`) — many of these customers are businesses
  - [x] Verify: create/edit a client with a business name → it persists and shows in the clients list (verified in browser: card + detail drawer)
- [x] [Invoices · slide 3] Generate an invoice from a completed job
  - [x] Back the "Job done need to invoice." button (slide-3 assistant item) with invoice creation via `backend/src/paperwork/entrypoints/invoice-controller/mod.ts` from the job/quote data
  - [x] Surface the new invoice in `front-end/islands/InvoicesPage.tsx`
  - [x] Verify: from a completed job the contractor generates an invoice that appears in /invoices with correct amounts
- [x] [Invoices · slide 20] Adjust invoices via discount or change order
  - [x] In `front-end/islands/InvoicesPage.tsx` + `backend/src/paperwork/entrypoints/invoice-controller` add the ability to apply a discount to an invoice
  - [x] For a change order, generate a new approval link the customer can approve (reuse `backend/src/paperwork/dto/change-order.ts` and `front-end/islands/PublicChangeOrderActions.tsx`)
  - [x] Fix whatever is currently broken ("This is not working") — fixed: post-approval revisit double-counted the delta (original amount now snapshotted at create); approve no longer reports ok when the invoice write fails; approve/decline now notify the contractor; pending change orders + their links are now listed (recoverable) in the Adjust panel
  - [x] Verify: apply a discount → invoice total updates; create a change order → a new public approval link is issued and the customer can approve it (verified in browser + curl: $10 → +$5 CO → approve → $15, math stable on revisit, bell notification fired)
- [x] [Mobile · slides 21 & 10] Flawless responsive view across all screens
  - [x] Audit every route/island for small-screen layout, reusing `front-end/islands/MobileViewport.tsx`; ensure the same UX translates to phone widths (MobileViewport mounts globally in _app.tsx; every UI route has page-CSS breakpoints — audited file-by-file)
  - [x] Verify: walk the full flow (dashboard, assistant, quote, contract sign, invoices, payments, settings) at phone width with no broken layouts
- [x] [Landing · slide 21] Add a top-of-page Login button to a clean login component
  - [x] In the landing route (`front-end/routes/index.tsx` / `front-end/islands/LandingScripts.tsx`) add a Login button at the top of the view linking to a clean login component (`front-end/routes/login.tsx` / `front-end/islands/LoginForm.tsx`) using the same login flow
  - [x] Verify: clicking Login from the landing page opens the clean login component and the existing OTP login flow still works (verified live: landing → /login → phone → /verify → OTP → dashboard)
- [x] [i18n · slide 21] Spanish localization for Mexican, South American, and Latino dialects
  - [x] Extend translations under `lang/` and `backend/src/core/i18n` to cover Spanish with dialect-appropriate phrasing — 2,200+ keys with full EN/ES parity, neutral Latin-American Spanish (tuteo, no vosotros); assistant LLM prompts enforce neutral LatAm Spanish. (The dead, never-mounted `LangToggle.tsx` island was removed — the live toggles are the landing switch and Settings.)
  - [x] Verify: toggling Spanish renders the full app + assistant in natural Spanish with no missing keys (key parity asserted; landing + login + assistant walked in ES)
- [x] [Pricing · slide 22] Update pricing model

  - [x] Remove the free tier and remove the percentage-based pricing for now
  - [x] Add a Starter Package at $15/month ("legitimize your business for less than an ad-free Netflix subscription"; includes quotes etc.), plus $99 and $199 tiers (Pro $99 "Most popular", Crew $199)
  - [x] Update wherever pricing is presented (landing/marketing route and any billing copy)
  - [x] Verify: pricing UI shows Starter $15/mo, $99, and $199 with no free tier and no percentage option (verified in browser, EN + ES)

- [x] when doing a quote we need a copy link button in addition to email, text and text + email (send menu in the assistant review + the quote card back face both copy the public link)
- [x] add ability to add invoices independent of quotes invoices are contracts except without the signature and the terms. and it says invoice. (New invoice button on /invoices → customer + amount + due date; public page renders an invoice-labeled doc, no signature/terms)
- [x] we need the ability to create change orders. (Adjust panel on each invoice → change order with public approval link; pending orders stay listed with copy-link)
- [x] when we finish the assistant flow there needs to be a way to swap between quote and invoice (the finished-flow review has a "Send as: Quote + Agreement | Invoice" toggle — invoice mode drops signature/terms and sends a payable invoice for the same total)
- [x] the assistant needs a button for invoice at the begining but it needs to have an easy flow
      just like the first button in the asssitant (4th starter "Job done, need to invoice.": details → amount → client → invoice, same shape as the first button's flow)
