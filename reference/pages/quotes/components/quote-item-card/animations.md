# Animations — quote-item-card

## Keyframes
- `pulse-dot` — `.qcard__status-dot` (2.4s opacity loop).

## Transitions
The flip and hover state changes are pure CSS transitions, parallel to `pages/contracts/components/contract-card/animations.md`. See that file and `styles.css` for the full transition list.
