# Animations — client-toolbar

None. Just a CSS transition on the filter pills:

```css
.ctoolbar2__filter { transition: all var(--dur-fast) var(--ease-out); }
.ctoolbar2__filter--active { background: #fff; color: var(--brand-teal); box-shadow: var(--shadow-sm); }
```
