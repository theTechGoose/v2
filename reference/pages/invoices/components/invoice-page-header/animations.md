# Animations — invoice-page-header

## `pulse-dot` — `.qph__eyebrow-dot`
2.4s opacity loop. See `shared/design-tokens/motion.md`.

## Transitions
- `.qph__cta { transition: transform var(--dur-fast) var(--ease-out); }` — hover translateY(-1px).
