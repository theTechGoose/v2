# Animations — loop-bar

## `pulse` — `.loopbar__lbl-dot`
2.4s infinite pink ripple. See `shared/design-tokens/motion.md`.

## Transitions
`.loopbar__cta { transition: transform var(--dur-fast) var(--ease-out); }` — hover translateY(-1px).
