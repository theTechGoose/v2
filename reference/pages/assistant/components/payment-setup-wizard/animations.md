# Animations — payment-setup-wizard

No keyframes. Just CSS transitions on `.wiz-opt:hover`, `.wiz-chip`, `.wiz-pill`, and `.wiz__foot-finalize` for hover/focus states. See `styles.css` for the full transition list.
