# Animations — greeting-hero (dashboard)

Inherits from `shared/hero-banner/animations.md`. The dashboard variant adds the `<Ticker>` count-up on the dollar amount inside `.hero__title em` (JS-driven, ~720ms).
