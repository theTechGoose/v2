# Animations — panel

None. The shell is purely structural; any motion (e.g. an entry transition) lives on the row components placed inside (`.job`, `.quote-item`, `.ccard`).

The optional pulsing dot in `.panel__head` borrows the `.hero__pill-dot` element, which animates `ppulse` (see `shared/hero-banner/animations.md`).
