window.UIB = {"overview": [{"title": "index.md", "md": "# Paperwork Monster — UI breakdown / build spec\n\nReverse-documentation of the existing **Fresh 2 (Preact + Tailwind 4)** app at\n`front-end/`, decomposed into a build spec a later session can rebuild from\n**mechanically**, page by page and component by component, without opening the\nsource. Because the source is fully readable TypeScript/CSS (not an opaque\nmock), classification is **ground truth, not inference**: `islands/` → island,\n`components/` → static / page-composition, `routes/` → page.\n\nStart here, then read `design-tokens.md` and `data-model.md`, then the page +\ncomponent specs.\n\n## How this maps to the source\n- **Tokens:** the \"Sabor Design System\" CSS custom properties (`design-tokens.md`).\n  Single light theme — no dark mode anywhere.\n- **Two surfaces:** the **authenticated app** (Sabor tokens, semantic CSS classes\n  per feature file) and the **public customer-facing** pages (`/q /c /co /i` —\n  their own inline-styled palette; see `design-tokens.md` § Public surface).\n- **Data:** see `data-model.md` (entities, enums, endpoint map, hazards). All\n  stored entities are user-owned (carry a `userId`).\n\n---\n\n## Page inventory\n| Page (folder) | Route(s) | Purpose | Key components |\n|---|---|---|---|\n| `landing` | `/` (`index.tsx`) | Marketing site (static + vanilla-JS animation in `static/landing-scripts.js`) | phone-chat (island); rest is inline markup |\n| `login` | `/login` | Phone entry (auth step 1) | login-form |\n| `verify` | `/verify?phone=` | OTP entry (auth step 2; dev OTP `000000`) | code-input |\n| `dashboard` | `/dashboard` | Home: stats, sections, setup checklist | dashboard-page, dash-sections, ticker, setup-checklist, assistant-coachmark, welcome-back-toast |\n| `quotes` | `/quotes` | Quote pipeline | quotes-page, quotes-sections, quote-card, delete-quote-button |\n| `contracts` | `/contracts` | Contracts list (derived `mood`) | contracts-page, contracts-sections, contract-card, contract-track |\n| `invoices` | `/invoices` | Invoice list + create/schedule/send | invoices-page, invoices-hero, invoices-kpis, invoice-card, new-invoice-modal |\n| `payments` | `/payments` | Manual-only payments (claim→confirm) | payments-page, payments-hero, payments-kpis, payment-card, payment-side-rail, landed-row |\n| `clients` | `/clients` | Customer board (derived status/segment) | clients-page, clients-sections, clients-board |\n| `assistant` | `/assistant`, `/assistant/:threadId` | AI chat (\"Bossie\"); `/messages` 302s here | asst-chat, asst-threads, chat-header-live, money-input, onboarding-progress, redirect-toast |\n| `settings` | `/settings` | Business profile + danger-zone wipe | settings-page |\n| `admin` | `/admin` | Super-admin user table + impersonation | admin-page |\n| `accounts-manager` | `/accounts-manager` | Shell-only stub (honest-empty) | — |\n| `public-quote` | `/q/:id` | Customer quote doc + accept/decline | public-quote-actions, public-accept-quote |\n| `public-change-order` | `/co/:id` | Customer change-order doc + approve/decline | public-change-order-actions |\n| `public-contract` | `/c/:id` | Customer contract doc + sign | public-contract-view, public-sign-contract (+ shared `contract-doc`) |\n| `public-invoice` | `/i/:id` | Customer invoice doc + claim payment | public-invoice-claim |\n\n**Redirect-only routes (not pages):** `/messages` → `/assistant?from=messages`;\n`/s/:code` → resolves a shortlink then 302s to `/q|/c|/i`; `/test.tsx` trivial\ndev page; `routes/api/*` (auth + `[...path]` proxy) are backend plumbing.\n\n---\n\n## Shared-component usage matrix\n(`shared-components/`; ✓ = used on that page. App = the 10 shell pages\ndashboard/quotes/contracts/invoices/payments/clients/assistant/settings/admin/accounts-manager.)\n\n| Component | Tier | Used on |\n|---|---|---|\n| `mobile-viewport` | island (behavior-only, renders null) | **every page** (global, `_app.tsx`) |\n| `impersonation-banner` | island (self-gating) | **every page** (global, `_app.tsx`) |\n| `dash-sidebar` | island | all 10 App pages |\n| `dash-topbar` | island | all 10 App pages |\n| `skeletons` | static | dashboard, quotes, contracts, invoices, payments, clients, settings (7) |\n| `quote-track` | island | quotes, invoices, payments (3) |\n| `contract-doc` | static / page-composition | public-contract route + PublicContractView island |\n| `ui-section-head` | static | contract-doc (1) — transitively the public-contract surface |\n| `ui-button` | static | **built-but-unwired** (0 live; only the Counter story) |\n| `ui-icon` | static | only via dead `Composer` → effectively unwired |\n| `ui-brand` | static | only via dead `AppNav` → transitively dead |\n\n---\n\n## Interaction / tier summary (the runtime architecture)\n\n**The dominant pattern is the \"whole-page island.\"** Every authenticated feature\npage mounts the app shell (`DashSidebar` + `DashTopbar`) around a single large\nisland (`DashboardPage`, `QuotesPage`, `ContractsPage`, `InvoicesPage`,\n`PaymentsPage`, `ClientsPage`, `SettingsPage`, `AdminPage`) that **client-fetches\nits data on mount** via `clients/*.ts`. This is the codebase's central\n**anti-pattern**: several of these (and the impersonation/logout/wipe paths)\n`location.reload()` / `location.href=…` after a mutation to re-read server state.\n\n> **Rebuild target for each:** keep client-only interactions (filters, modals,\n> disclosure) as islands, but move **server mutations** to `<form method=POST>`\n> + **Post/Redirect/Get** (303 → re-render) or a **Fresh Partial** for in-place\n> fragment refresh, with feedback as a **flash message** on the redirect — never\n> a client toast + `location.reload()`. Each component spec names its specific\n> fix. (Some `location.href` navigations after a cookie/session swap — logout,\n> impersonate, account-wipe → `/login` — are legitimately navigations, not\n> reload-to-refresh; those are flagged \"mild / justified\".)\n\n**Public pages do it right:** SSR documents (`ssrBackendGet…`) with small\nclient-action islands (accept / sign / claim). Keep that shape.\n\n**Auth pages:** static SSR shell + one small island (LoginForm / CodeInput).\n\n**Pushed / live (need a stream at build time):**\n- `AsstChat` voice dictation → **websocket** `/api/voice/stream` (Vite proxies\n  the upgrade to the backend `/voice/stream` → AssemblyAI). The only genuinely\n  pushed channel. Everything else is request-response.\n- `DashTopbar` **polls** (notifications 10 s, unread 30 s, ticker rotate 3.8 s) —\n  flagged: back off / consolidate / move to SSE.\n\n**Cross-island coupling:** `DashTopbar` dispatches a `window` `pm:sb-toggle`\nevent that `DashSidebar` listens for (string-keyed event bus instead of a shared\nsignal) — flagged fragile.\n\n**Data-shape hazards (design-time, not benchmarked — see `data-model.md` §5):**\nDashboardStats whole-account rollup rendered in the shell on every page\n(`lib/dash-cache.ts` is the existing mitigation); sidebar unread badge; `/clients`\nN×per-customer rollups + global top/segments; `/quotes` engagement/stage\nderivation; invoice aging buckets; payment-sum rollups over jobs; the public\n`/…/public` composite-read **join fan-outs** (invoice → contract → quote →\ncustomer → profile → methods → siblings) on every customer link-load.\n\n---\n\n## Build order\n1. **Design tokens** → Tailwind 4 `@theme` (`design-tokens.md`).\n2. **Shared primitives** (no deps): `ui-button`, `ui-icon`, `ui-brand`,\n   `ui-section-head`, `skeletons`.\n3. **Shared cross-cutting + app shell**: `mobile-viewport`, `impersonation-banner`,\n   then `dash-sidebar`, `dash-topbar`, `quote-track`.\n4. **Shared composite**: `contract-doc` (uses `ui-section-head` + embeds\n   `public-sign-contract`).\n5. **Per-page**: primitives → composites → the page island, in dependency order\n   (e.g. `quote-card` / `quotes-sections` before `quotes-page`; `payment-card` /\n   `payments-kpis` / `payments-hero` before `payments-page`; `money-input` +\n   `quote-card` before `asst-chat`).\n6. **Page compositions** (the route shells).\n\nPublic surface is independent of Sabor — build its primitives from the public\npalette, not the app tokens.\n\n---\n\n## Confirmed dead / unused code (do NOT rebuild)\nVerified by zero live references (import + JSX + string) across `routes/ islands/\ncomponents/ lib/ clients/ static/`:\n- **Islands:** `HeroRotor`, `DemoPhoneChat`, `LandingScripts` (landing animation\n  is the vanilla `static/landing-scripts.js` + `#rotor-track`, not these),\n  `ContactForm` (landing's contact form is inline; `clients` uses an inline form\n  in `ClientsBoard`), `AsstComposer`, `Composer`, `DocTabs`, `Counter` (story-only).\n- **Components:** `AppNav` (orphaned — and the only consumer of `ui/Brand`),\n  `AssistantSections`, `MessageBubble`.\n- **Built-but-unwired primitives:** `components/ui/Button.tsx`,\n  `components/ui/SectionHead.tsx` (SectionHead is, however, used by `contract-doc`\n  — so it IS live there), and `components/Button.tsx` (legacy, Counter-story only;\n  superseded by `ui/Button`).\n- **CSS-only dead:** `pm-assistant-shake` keyframes (no selector assigns it);\n  `.sb__brand-sub` / `.sb__textus-tag` / `.sb__label`, `.topbar__search` /\n  `.topbar__btn` (styled, not rendered); a stray `.__unused__` rule in\n  `dashboard.css`; `.dashpage-error` and `.settings-edit__input` are *referenced\n  in markup but have no CSS rule* (render unstyled — confirm intent).\n\n---\n\n## Risks / unknowns the build session should verify early\n- **Screenshots are pending.** No running backend this session, and the app is\n  auth-gated, so component specs carry a `capture-checklist.md` (route, dev OTP\n  `000000`, real `@media` viewports, states to drive) instead of fabricated\n  stills. Run captures against a seeded dev backend before pixel-diffing.\n- **`AsstChat` (8450 LOC)** is documented as a faithful **macro** decomposition\n  (message-type taxonomy, composer, quote/terms phases, voice ws), not a\n  line-by-line transcription — expect to refine its sub-component split during build.\n- **Units:** several money fields are bare `amount`/`value` that are **cents** by\n  convention (per `lib/format.ts` + `dashboard.ts` comments) but aren't annotated;\n  `data-model.md` flags the ambiguous ones. Verify at the API boundary.\n- **Timestamps** are inconsistent (epoch-ms numbers on User/Message vs ISO strings\n  elsewhere) — `data-model.md` § Open questions.\n- **Breakpoints:** the app shell cuts at **640/641px**, not the product-wide\n  720px — always use a component's *own* `@media` widths.\n- **Two icon systems** (`ui/Icon` 16-name vs `lib/dash-icons` ~50-glyph) and\n  **dual `.btn`/`.brand`/`.section-head`** definitions across `verify.css` vs\n  `landing.css` vs `dashboard.css` — the `ui-*` primitives ship no CSS of their\n  own, so their look depends on which feature stylesheet the host page loaded.\n\n---\n\n## Completeness audit\n\n`Unassigned: none` — every route maps to a page folder (or a documented\nredirect), and every live island/component maps to a spec folder; every dead\nmodule is in the dead-code list above.\n\n**Totals:** 17 page folders · 11 shared components · **56 components** with a\nreal `isolate/` proposal · **282** case files · 137 `.md` specs · 61\n`capture-checklist.md` · **338/338 isolate+case JSON validate** (0 invalid,\n0 empty case dirs). Capture pass (live screenshots) intentionally deferred to\nthe build session per the capture policy (auth-gated, no running backend).\n\n**Per-page spec status — all complete:**\n| Page | Components | Status |\n|---|---|---|\n| landing | phone-chat | ✅ md+css+js+isolate; vanilla `landing-scripts.js` documented |\n| login | login-form | ✅ |\n| verify | code-input | ✅ |\n| dashboard | dashboard-page, dash-sections, ticker, setup-checklist, assistant-coachmark, welcome-back-toast | ✅ |\n| quotes | quotes-page, quotes-sections, quote-card, delete-quote-button | ✅ |\n| contracts | contracts-page, contracts-sections, contract-card, contract-track | ✅ |\n| invoices | invoices-page, invoices-hero, invoices-kpis, invoice-card, new-invoice-modal | ✅ |\n| payments | payments-page, payments-hero, payments-kpis, payment-card, payment-side-rail, landed-row | ✅ |\n| clients | clients-page, clients-sections, clients-board | ✅ |\n| assistant | asst-chat (macro decomp), asst-threads, chat-header-live, money-input, onboarding-progress, redirect-toast | ✅ |\n| settings | settings-page | ✅ |\n| admin | admin-page | ✅ |\n| accounts-manager | — (honest-empty stub) | ✅ documented stub |\n| public-quote | public-quote-actions, public-accept-quote | ✅ |\n| public-change-order | public-change-order-actions | ✅ |\n| public-contract | public-contract-view, public-sign-contract | ✅ |\n| public-invoice | public-invoice-claim | ✅ |\n| shared-components | dash-sidebar, dash-topbar, mobile-viewport, impersonation-banner, skeletons, quote-track, contract-doc, ui-button, ui-icon, ui-brand, ui-section-head | ✅ |\n\n### Known bugs / fixes surfaced during decomposition\nConcrete issues the per-component specs found in the *current* source — the\nrebuild should NOT reproduce them (each is detailed in the owning spec):\n\n- **`location.reload()` after mutation** (the page-island anti-pattern's tail):\n  `DeleteQuoteButton`, every `InvoiceCard` mutation + `new-invoice-modal` create,\n  `PublicSignContract` (900 ms panel → hard reload). Fix: form+PRG / Fresh Partial\n  / lift an `onDone` refetch. *(Payments & Contracts pages do NOT reload;\n  `AsstChat` is a whole-page island done right — pure client state.)*\n- **Unstyled classes** (referenced in markup, no CSS rule): `.qpage-error`,\n  `.kpage-error`, `.dashpage-error`, `.settings-edit__input`; `PSideTopPayors`\n  `.qside__*`; `LandedRow` `.qdone__body/__sub`. **CSS clip bug:**\n  `.kcard__numeral{bottom:-18px}` under `overflow:hidden` clips the numeral\n  (the sibling `.qcard__numeral` fix wasn't ported back).\n- **Inert/no-op buttons:** `DashSections` nudge/view buttons, `QuotesHero`\n  \"New quote\" CTA, `PaymentCard` front+back buttons (all `stopPropagation`-only).\n- **A11y:** clickable `<header>`/`<article>`/`<div>` instead of buttons\n  (QuoteTrack, ContractTrack, ContractCard flip); `PublicSignContract` is\n  pointer-only → keyboard/SR users **cannot sign**; mobile sidebar drawer + Add-\n  client modal have no focus-trap/Esc; missing `aria-current`/`aria-expanded`/\n  `aria-pressed`.\n- **Synthesized \"data fiction\":** `ContractCard` progress + back-face milestones,\n  `PaymentsPage` client-derived status (`attention` mood structurally\n  unreachable), `PSideFlow` trend bucketed by array index not `receivedAt`.\n- **Polling / event buses:** `DashTopbar` polls (10 s/30 s/3.8 s);\n  `pm:sb-toggle` (sidebar↔topbar), `pm:asst-*` (AsstChat↔ChatHeaderLive),\n  `AssistantCoachmark` string-queries `.sb__textus` 30×120 ms.\n- **Reduced-motion gaps:** `Ticker` rAF count-up + skeleton shimmer rely on the\n  global tokens clamp with no component-local guard.\n\n> Two earlier sub-agent runs were interrupted (one no-op'd; the account session\n> limit truncated the first page-spec wave). A second finishing pass completed\n> every gap; the counts above are the post-finishing verified state.\n"}, {"title": "design-tokens.md", "md": "# Design tokens — \"Sabor Design System\"\n\nSource of truth: `static/_proto/colors_and_type.css` (copied verbatim to\n`assets/tokens-source.css`). Tokens are CSS custom properties declared on\n`:root`; the whole app surface consumes them via semantic classes defined in\nthe per-feature CSS files (`static/dashboard.css`, `quotes.css`, …).\n\n**Theme variants: none.** There is exactly one theme (light). No\n`[data-theme]`, no `prefers-color-scheme`, no `.dark` block exists anywhere in\n`static/*.css`. Do not scaffold a dark mode — it was never designed.\n\n**Two surfaces, two palettes.** The *authenticated app* and *marketing/auth*\npages use the Sabor tokens below. The *public customer-facing* pages\n(`/q /c /co /i`) are rendered with **hard-coded inline-style hex values**, not\ntokens — a deliberately separate, document-like palette (see\n[§ Public surface palette](#public-surface-palette)). Keep them distinct when\nrebuilding; do not \"tokenize\" the public pages against Sabor or their look\nshifts.\n\n---\n\n## Palette\n\n### Brand anchors\n| Token | Hex | Role |\n|---|---|---|\n| `--brand-mint` | `#f7fff7` | Mint Cream — app surface / `--bg` |\n| `--brand-pink` | `#ff6b6b` | Grapefruit — accent |\n| `--brand-teal` | `#1a535c` | Dark Teal — primary text `--fg` |\n| `--brand-green` | `#519843` | Medium Jungle — **primary action** |\n| `--brand-coffee` | `#644536` | Coffee Bean — neutral / shadow tint |\n\n### Tonal scales (derived)\n- **pink**: `50 #fff1f1 · 100 #ffd9d9 · 200 #ffb3b3 · 300 #ff8d8d · 400 #ff7a7a · 500 #ff6b6b · 600 #fa5252 · 700 #e03131 · 800 #c92a2a`\n- **teal**: `50 #e8f1f2 · 100 #c8dde0 · 200 #8fbabf · 300 #56969e · 400 #2d737c · 500 #1a535c · 600 #144048 · 700 #0f3036 · 800 #0a2024`\n- **green**: `50 #ecf5e9 · 100 #cfe5c8 · 200 #a5cd98 · 300 #7bb568 · 400 #5fa34f · 500 #519843 · 600 #427a37 · 700 #335d2a`\n- **coffee**: `50 #f2ebe8 · 100 #dbc9c2 · 200 #b89d90 · 300 #94715f · 400 #785544 · 500 #644536 · 600 #4f362a · 700 #3a271f`\n- **mint** (surface): `50 #fcfffc · 100 #f7fff7 · 200 #ecf6ec · 300 #ddebdd`\n\n### Semantic colors\n| Token | Value | |\n|---|---|---|\n| `--bg` | `var(--brand-mint)` | page background |\n| `--bg-alt` / `--bg-raised` | `#ffffff` | cards / raised surfaces |\n| `--bg-sunken` | `var(--mint-200)` `#ecf6ec` | wells, insets |\n| `--bg-overlay` | `rgba(100,69,54,.40)` | modal scrim (coffee 40%) |\n| `--fg` | `var(--brand-teal)` | body text |\n| `--fg-strong` | `var(--teal-700)` `#0f3036` | headings |\n| `--fg-muted` | `rgba(26,83,92,.72)` | secondary text |\n| `--fg-subtle` | `rgba(26,83,92,.55)` | tertiary text |\n| `--fg-on-primary` / `--fg-on-accent` | `#ffffff` | text on green/pink |\n| `--border` | `rgba(100,69,54,.16)` | hairline (coffee 16%) |\n| `--border-strong` | `rgba(100,69,54,.32)` | emphasized border |\n| `--border-focus` | `var(--brand-green)` | focus ring color |\n| `--primary` | `var(--brand-green)` `#519843` | primary button |\n| `--primary-hover` | `var(--green-600)` `#427a37` | |\n| `--primary-press` | `var(--green-700)` `#335d2a` | |\n| `--secondary` | `var(--brand-teal)` | |\n| `--accent` | `var(--brand-pink)` | |\n| `--neutral` | `var(--brand-coffee)` | |\n| `--success` / `--success-bg` | `#519843` / `var(--green-50)` | |\n| `--warning` / `--warning-bg` | `#e8a33d` / `#fff6e5` | |\n| `--danger` / `--danger-bg` | `var(--pink-700)` `#e03131` / `var(--pink-50)` | |\n| `--info` / `--info-bg` | `var(--brand-teal)` / `var(--teal-50)` | |\n\n---\n\n## Typography\n- **Fonts** (Google Fonts CDN `@import`): `--font-heading` = **Nunito** (400–800),\n  `--font-body` = **Inter** (400–700), `--font-mono` = ui-monospace stack.\n  To self-host: drop variable TTFs in `./fonts/` and swap the `@import` for\n  `@font-face`.\n- **Type scale**: `--type-display 56px · --type-h1 40px · --type-h2 32px ·\n  --type-h3 24px · --type-h4 20px · --type-body 16px · --type-small 14px ·\n  --type-micro 12px`.\n- **Line heights**: `--lh-display 1.05 · --lh-tight 1.15 · --lh-snug 1.30 ·\n  --lh-body 1.55`.\n- **Tracking**: `--track-tight -0.01em · --track-normal 0 · --track-loose 0.08em`.\n- **Weights**: `--w-regular 400 · --w-medium 500 · --w-semibold 600 ·\n  --w-bold 700 · --w-extra 800`.\n- Heading utility classes (`.h1`–`.h4`, `.ds-display`) use Nunito + extra/bold\n  weight + tight tracking + `text-wrap: balance`. Body (`.body`/`p`),\n  `.small`, `.micro` use Inter. `.eyebrow` = Nunito bold micro, uppercase,\n  `--track-loose`, colored `--brand-green`.\n\n## Spacing — 8pt soft grid\n`--space-1 4 · -2 8 · -3 12 · -4 16 · -5 24 · -6 32 · -7 48 · -8 64 · -9 96` (px).\n\n## Radii\n`--radius-sm 8 · -md 12 · -lg 16 · -xl 24 · -2xl 32 · -pill 999` (px).\n\n## Shadows (warm, coffee-tinted)\n- `--shadow-sm`: `0 1px 2px rgba(100,69,54,.06), 0 1px 3px rgba(100,69,54,.04)`\n- `--shadow-md`: `0 4px 8px rgba(100,69,54,.08), 0 2px 4px rgba(100,69,54,.04)`\n- `--shadow-lg`: `0 12px 24px rgba(100,69,54,.10), 0 4px 8px rgba(100,69,54,.06)`\n- `--shadow-xl`: `0 24px 48px rgba(100,69,54,.12), 0 8px 16px rgba(100,69,54,.06)`\n- `--shadow-focus`: `0 0 0 4px rgba(81,152,67,.24)` (green focus ring)\n\n## Motion\n- Easings: `--ease-bounce cubic-bezier(.34,1.56,.64,1)` ·\n  `--ease-standard cubic-bezier(.2,0,0,1)` · `--ease-out cubic-bezier(0,0,.2,1)`.\n- Durations: `--dur-micro 120ms · --dur-fast 200ms · --dur-base 320ms`.\n- **Reduced motion**: global `@media (prefers-reduced-motion: reduce)` clamps\n  every `animation-duration`/`transition-duration` to `0.01ms !important`\n  (in tokens CSS). `static/_proto/styles.css` also forces `scroll-behavior:auto`.\n  Feature CSS files add their own reduced-motion blocks (assistant ×4,\n  assistant-page ×5, dashboard ×2, landing ×3, verify ×1) — **the rebuild must\n  preserve reduced-motion in every animated component.**\n\n## Layout\n`--container-marketing 1200px · --container-product 1280px · --sidebar-w 240px`.\n`.container` = `max-width:1200px; margin:0 auto; padding:0 clamp(20px,5vw,56px)`.\n\n## Breakpoints (measured from real `@media` queries in `static/*.css`)\nThe product surface keys off three breakpoints; the rest are page-specific.\n| Width | Meaning (most-used) |\n|---|---|\n| **720px** | primary mobile cutover (most common: 8 queries) — sidebar→drawer, stacked layouts |\n| **980px** | tablet / two-pane collapse (7 queries) |\n| **1280px** | `--container-product` max width (5 queries) |\n| 1200px | `--container-marketing` (landing) |\n| others | 480, 560, 640/641, 700, 760, 768, 880, 1024, 1100/1099, 1201, 1400 — confirm per page against that page's CSS |\n\nAlways re-shoot/verify a component at *its own* CSS's breakpoints, not these\ndefaults blindly.\n\n---\n\n## Public surface palette\nThe public pages (`routes/q|c|co|i/[id].tsx` and the `Public*` islands) are\n**not tokenized** — they use inline-style hex literals to read like a printed\ndocument. Recurring values observed:\n| Hex | Role |\n|---|---|\n| `#f7f6f1` | warm paper page background |\n| `#ffffff` | card |\n| `#1c2c30` | body text |\n| `#144852` | headings / strong (a deeper teal than app `--fg`) |\n| `#519843` | accent / eyebrow / bullets (shared with app green) |\n| `#6b7a7e` | muted text |\n| `#4a5a5e` | secondary text |\n| `#e3e8e6` | hairline divider |\n| `#b9c1bf` | footer/\"powered by\" subtle |\n| `#a83b3b` | declined/danger badge text |\n| Font | system stack `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto…` (NOT Nunito/Inter), with `'Helvetica Neue'` for the money/hero numerals |\n| Card radius | `18px`; total-band radius `14px`; shadow `0 8px 32px rgba(20,72,82,.08)` |\n\nThese pages still load `/landing.css` for resets, but their components are\ninline-styled. Rebuild them as their own primitive set; do not map to Sabor.\n\n---\n\n## Tailwind 4 `@theme` mapping (rebuild target)\n\nFresh 2 ships Tailwind 4 (CSS-first). Emit a single `@theme` block of custom\nproperties — **not** a `tailwind.config.js`. Mapping (representative; mirror the\nfull token list above 1:1):\n\n```css\n@theme {\n  /* color — name them so utilities read naturally: bg-surface, text-fg, etc. */\n  --color-surface: #f7fff7;          /* --brand-mint / --bg */\n  --color-surface-raised: #ffffff;\n  --color-surface-sunken: #ecf6ec;   /* mint-200 */\n  --color-fg: #1a535c;               /* brand-teal */\n  --color-fg-strong: #0f3036;        /* teal-700 */\n  --color-fg-muted: rgba(26,83,92,.72);\n  --color-fg-subtle: rgba(26,83,92,.55);\n  --color-primary: #519843;          /* green-500 */\n  --color-primary-hover: #427a37;    /* green-600 */\n  --color-primary-press: #335d2a;    /* green-700 */\n  --color-accent: #ff6b6b;           /* pink-500 */\n  --color-neutral: #644536;          /* coffee-500 */\n  --color-danger: #e03131;           /* pink-700 */\n  --color-warning: #e8a33d;\n  --color-border: rgba(100,69,54,.16);\n  /* full pink/teal/green/coffee/mint 50–800 ramps → --color-pink-50 … etc. */\n\n  /* type */\n  --font-heading: \"Nunito\", system-ui, sans-serif;\n  --font-body: \"Inter\", system-ui, sans-serif;\n  --text-display: 56px; --text-h1: 40px; --text-h2: 32px; --text-h3: 24px;\n  --text-h4: 20px; --text-base: 16px; --text-sm: 14px; --text-xs: 12px;\n\n  /* spacing — Tailwind's --spacing base + named steps, or map 1:1 */\n  --spacing-1: 4px; /* … through --spacing-9: 96px */\n\n  /* radius */\n  --radius-sm: 8px; --radius-md: 12px; --radius-lg: 16px; --radius-xl: 24px;\n  --radius-2xl: 32px; --radius-pill: 999px;\n\n  /* shadow */\n  --shadow-sm: 0 1px 2px rgba(100,69,54,.06), 0 1px 3px rgba(100,69,54,.04);\n  --shadow-md: 0 4px 8px rgba(100,69,54,.08), 0 2px 4px rgba(100,69,54,.04);\n  --shadow-lg: 0 12px 24px rgba(100,69,54,.10), 0 4px 8px rgba(100,69,54,.06);\n  --shadow-xl: 0 24px 48px rgba(100,69,54,.12), 0 8px 16px rgba(100,69,54,.06);\n\n  /* motion */\n  --ease-bounce: cubic-bezier(.34,1.56,.64,1);\n  --ease-standard: cubic-bezier(.2,0,0,1);\n  --ease-out: cubic-bezier(0,0,.2,1);\n\n  /* breakpoints */\n  --breakpoint-mobile: 720px;\n  --breakpoint-tablet: 980px;\n  --breakpoint-desktop: 1280px;\n}\n```\n\n> Note: the *current* app does not use Tailwind utilities — it uses hand-written\n> semantic CSS classes (`.btn`, `.card`, `.app`, `.main`, …) per feature file.\n> The rebuild can either (a) port those semantic classes into `@layer\n> components` backed by these `@theme` tokens, or (b) re-express them as utility\n> compositions. Each component spec lists the exact source classes + extracted\n> CSS so either path is mechanical.\n"}, {"title": "data-model.md", "md": "# Data Model (implied backend) — Paperwork Monster\n\nReverse-documented from the Fresh 2 front-end's **typed API clients** (`clients/*.ts`),\n**SSR/inline route interfaces** (`routes/{q,i,co,c,s}/*`), **shape/derive helpers**\n(`lib/*`), and **static seeds** (`lib/*-seed.ts`). The clients' request/response\nTypeScript interfaces ARE the implied entity shapes; the seeds are fallback fixtures\nthe pages render until the backend's analytics/agent endpoints come online.\n\nConventions discovered (apply globally unless noted):\n\n- **Every stored entity carries `userId`** — all data is user-owned. The few shapes\n  without one are either *composite reads* (`ProfileSnapshot`, `Job`, `DashboardStats`)\n  or *public projections* (`QuotePublic`, `InvoicePublic`, `ContractPublic`,\n  `ChangeOrderPublic`) where ownership is implicit in the route.\n- **Money is INTEGER CENTS** everywhere a field is named `*Cents` or flows through\n  `lib/format.ts` / `lib/payment-split.ts`. Some older DTO fields are just `amount` /\n  `value` / `totalAmount` / `estimatedTotal` and the comments say these are cents too\n  (e.g. `dashboard.ts`: \"Money fields from the dashboard payload are CENTS\"). Flagged\n  per-field below where ambiguous.\n- **Timestamps**: mixed. Most are ISO-8601 strings (`createdAt: string`). A handful are\n  epoch-millis numbers (`ProfileUser.createdAt: number`, `auth.User.createdAt: number`,\n  `Message.createdAt: number`). This inconsistency is real, not invented.\n- **Multi-language fields** are `Record<langCode, string>` maps (`en` / `es`), suffixed\n  `…ByLang`. Terms are stored in English and localized at render (`lib/term-i18n.ts`).\n- **Payments are MANUAL-ONLY**: no card/merchant processing. Customer *claims* a payment\n  (`paymentIntent` on the invoice) → contractor *confirms*. See Payment / Invoice below.\n\n---\n\n## 1. Entities\n\n### 1.1 User / ProfileUser / AdminUserView\n\nThe authenticated account. Three projections exist across clients; they describe one\nbackend `User` row.\n\n```ts\n// lib/auth.ts (SSR session resolve) & clients/profile.ts ProfileUser\ninterface User {\n  id: string;\n  phoneNumber: string;          // E.164, the login identity (OTP)\n  name?: string;\n  email?: string;\n  language?: \"en\" | \"es\";       // contractor's UI language\n  superAdmin?: boolean;         // gates /admin; server-enforced\n  createdAt: number;            // epoch ms\n  updatedAt: number;\n}\n\n// clients/admin.ts — super-admin search projection\ninterface AdminUserView {\n  id: string;\n  name?: string;\n  phoneNumber: string;\n  businessName?: string;        // denormalized from BusinessIdentity\n  superAdmin: boolean;\n}\n```\n\n- **Auth artifacts (implied, not typed as entities):** a **Session** (cookie\n  `pm_session` → backend looks it up via `GET /me`, header `x-session-id`), and an\n  **OTP** challenge (phone + code, with `expired` / `rate_limited` states — see\n  `verify.ts VerifyOtpError`). New-user detection lives on verify (`isNewUser`).\n\n### 1.2 Business profile sub-entities (each keyed by `userId`, 1:1 with User)\n\n```ts\n// clients/profile.ts\ninterface BusinessIdentity {\n  userId: string;\n  businessName?: string;        // backend canonical; displayName mirrors it\n  displayName?: string;\n  legalName?: string;\n  businessLicense?: string;\n  logoFileId?: string;          // → FileRecord\n  logoUrl?: string;\n  tagline?: string;\n  websiteUrl?: string;\n  commsLanguage?: string;       // DEFAULT outbound language (first enabled)\n  commsLanguages?: string[];    // set of send languages (drives multi-lang send)\n  acceptedPaymentMethods?: Partial<Record<PaymentMethodKey, {\n    enabled?: boolean;\n    handle?: string;            // venmo @, etc.\n    cashtag?: string;           // cashapp\n    mailTo?: string;            // check\n    routingNumber?: string;     // ach\n    accountNumberMasked?: string;\n    instructions?: string;\n  }>>;\n  createdAt: string;\n  updatedAt: string;\n}\n// PaymentMethodKey = \"check\"|\"venmo\"|\"zelle\"|\"cashapp\"|\"paypal\"|\"cash\"|\"ach\"|\"card\"|\"other\"\n\ninterface BusinessAddress {\n  userId: string;\n  street?: string; unit?: string; city?: string; state?: string;\n  postal?: string; country?: string;\n  createdAt: string; updatedAt: string;\n}\n\ninterface BusinessInsurance {\n  userId: string;\n  provider?: string; policyNumber?: string;\n  coverageCents?: number;       // CENTS\n  expiresAt?: string;\n  insuranceFileId?: string;     // → FileRecord\n  insuranceUploadedAt?: string;\n  createdAt: string; updatedAt: string;\n}\n\ninterface ContractDefaults {\n  userId: string;\n  paymentTermsDays?: number;\n  depositPct?: number;\n  warrantyDays?: number;\n  createdAt: string; updatedAt: string;\n}\n\n// Tax block — TIN is never stored raw; backend hashes/masks.\ninterface TaxInfo { tinMasked?: string; w9FileId?: string; w9UploadedAt?: string; }\n```\n\n```ts\n// Composite read returned by GET /profile — not a stored row, a join.\ninterface ProfileSnapshot {\n  user: ProfileUser;\n  identity: BusinessIdentity | null;\n  address: BusinessAddress | null;\n  insurance: BusinessInsurance | null;\n  tax: TaxInfo | null;\n  contractDefaults: ContractDefaults | null;\n  references: unknown[];        // shape not yet exercised by the front-end\n  initials: string;             // derived\n}\n```\n\n### 1.3 Customer / Contact\n\nOne backend `Customer` row, surfaced as a thin DTO (`Customer`/`CustomerLite`) and as a\nrollup-enriched analytics card (`CustomerCard`).\n\n```ts\n// clients/dashboard.ts Customer  &  clients/assistant.ts CustomerLite\ninterface Customer {\n  id: string;\n  userId: string;\n  name: string;\n  email?: string;\n  phoneNumber?: string;\n  // assistant.CustomerLite also allows arbitrary [k:string]:unknown\n}\n\n// clients/clients.ts — analytics rollup card (stored fields + DERIVED fields)\ninterface CustomerCard {\n  id: string; userId: string;\n  name: string; email?: string; phoneNumber?: string;\n  address?: string; notes?: string; businessName?: string;\n  segment?: \"property_mgmt\" | \"homeowner\" | \"small_biz\" | \"hoa\"; // stored\n  vip?: boolean;\n  createdAt: string; updatedAt: string;\n  // ---- DERIVED / rolled-up (computed by /clients, NOT stored) ----\n  lastWhen: string | null;          // last contact ISO\n  lastWhenRel: string;              // humanized\n  lastTone: \"hot\" | \"warm\" | \"cold\";\n  balanceCents: number;             // CENTS; >0 owes, <0 credit\n  balanceSub: string;\n  activeJobs: number;\n  jobsSub: string;\n  status: \"active\" | \"lead\" | \"owes\" | \"regular\" | \"cold\";\n  temp: number;                     // 0–100 \"warmth\"\n  daysSinceContact: number;\n  revenue12moCents: number;         // CENTS, trailing-12mo rollup\n}\n```\n\n- **Segment enum** has a UI-only `\"unsorted\"` member (`ClientSegmentKey`) used as the\n  null-bucket label; stored `segment` excludes it.\n- Create body: `{ name, phoneNumber?, email?, businessName? }`.\n\n### 1.4 Quote + QuoteLineItem\n\nStored `Quote`, surfaced three ways: lean assistant DTO, analytics-enriched `QuoteCard`,\nand the public projection `QuotePublic`.\n\n```ts\ninterface QuoteLineItem {\n  description: string;\n  quantity?: number;\n  unit?: string;\n  price?: number;               // CENTS (per-unit)\n}\n\n// clients/assistant.ts — lean stored shape\ninterface Quote {\n  id: string; userId: string;\n  customerId?: string;          // → Customer (\"—\"/unset = unlinked quote\n                                //   (must NOT collapse into a phantom client))\n  summary: string;\n  description?: string;         // polished narrative / newline bullets\n  lineItems: QuoteLineItem[];\n  estimatedTotal: number;       // CENTS\n  status: \"draft\" | \"sent\" | \"accepted\" | \"declined\" | \"expired\";  // STORED status\n  createdAt: string; updatedAt: string;\n}\n\n// clients/quotes.ts — analytics card (stored + multi-lang + DERIVED engagement)\ninterface QuoteCard {\n  id: string; userId: string; customerId?: string;\n  customerName: string | null;\n  summary?: string;\n  jobName?: string;             // ≤3-word platform-wide identifier\n  description?: string;\n  descriptionByLang?: Record<string, string>;   // MULTI-LANG\n  jobNameByLang?: Record<string, string>;        // MULTI-LANG\n  summaryByLang?: Record<string, string>;        // MULTI-LANG\n  lineItems?: QuoteLineItem[];\n  estimatedTotal?: number;      // CENTS\n  status?: string;              // stored status (see above)\n  sentAt?: string; acceptedAt?: string; lostAt?: string;\n  createdAt: string; updatedAt: string;\n  // ---- DERIVED ----\n  stage: \"draft\"|\"sent\"|\"opened\"|\"cooling\"|\"stale\"|\"won\"|\"lost\"; // engagement stage\n  daysIn: number;\n  opens: number;                // count of OpenEvents (scan)\n  lastOpenAt: string | null;\n  sentDays: number | null;\n  decidedDays: number | null;\n}\n```\n\nImportant distinction: **`status`** (stored lifecycle: draft/sent/accepted/declined/\nexpired) vs **`stage`** (derived engagement: adds opened/cooling/stale/won/lost from\nopen-events + age). The seed (`lib/quotes-seed.ts`) only models the derived `Stage`.\n\n```ts\n// routes/q/[id].tsx — public projection (GET /quotes/:id/public)\ninterface QuotePublic {\n  id: string;\n  summary: string; description?: string; jobName?: string;\n  descriptionByLang?: Record<string,string>;\n  jobNameByLang?: Record<string,string>;\n  summaryByLang?: Record<string,string>;\n  customerId?: string;\n  estimatedTotal?: number;      // CENTS\n  lineItems: QuoteLineItem[];\n  status?: string;              // \"accepted\" / \"lost\" gate the public UI\n  acceptedAt?: string; createdAt?: string;\n  contractor?: {                // JOINED from profile for the public doc\n    name?: string; businessName?: string; phoneNumber?: string; email?: string;\n    addressLine?: string; commsLanguage?: string; hasLogo?: boolean;\n  };\n  customer?: { name?: string };\n}\n```\n\n**Quote engagement (OpenEvent)** — implied child rows powering `opens`/`lastOpenAt`/\nthe per-quote opens timeline. Seed shape (`lib/quotes-seed.ts`):\n\n```ts\ninterface OpenEvent { when: string; time: string; device: string; }\n// real backend would key these by quoteId + timestamp + device/userAgent.\n```\n\n### 1.5 Contract\n\n```ts\n// clients/contracts.ts — stored + server-derived `mood`\ninterface Contract {\n  id: string; userId: string;\n  quoteId: string;              // → Quote (1:1 source)\n  customerId?: string;          // → Customer\n  status?: string;              // stored; counts use draft|signed\n  effectiveDate?: string; startDate?: string; estimatedCompletionDate?: string;\n  totalAmount?: number;         // CENTS\n  signedAt?: string;\n  createdAt: string; updatedAt: string;\n  mood?: \"draft\"|\"starting-soon\"|\"active\"|\"wrapping-up\"|\"completed\"|\"stale\"; // DERIVED\n}\n\n// components/contract-doc.tsx — public projection (GET /contracts/:id/public)\ninterface ContractPublic {\n  id: string; quoteId?: string; customerId?: string;\n  status?: string;              // \"signed\" / \"declined\" gate the public UI\n  totalAmount?: number;         // CENTS\n  effectiveDate?: string; startDate?: string; estimatedCompletionDate?: string;\n  signedAt?: string;\n  customerSignedName?: string;  // captured at e-sign\n  contractor?: { name?; businessName?; phoneNumber?; email?; addressLine?;\n                 state?; commsLanguage?; hasLogo? };\n  customer?: { name?; phoneNumber?; email? };\n  jobDetails?: {                // JOINED from the source quote\n    summary?; jobName?; description?;\n    descriptionByLang?: Record<string,string>;  // MULTI-LANG\n    jobNameByLang?: Record<string,string>;\n    summaryByLang?: Record<string,string>;\n    lineItems?: QuoteLineItem[];\n  };\n  terms?: Term[];               // wizard-captured term grid\n  createdAt?: string;\n}\ninterface Term { stepId: string; label: string; value: string; } // value stored in EN\n```\n\nE-sign body: `POST /contracts/:id/sign  { name, signature /* dataURL */ }`.\n\n### 1.6 ChangeOrder\n\n```ts\n// routes/co/[id].tsx — public projection (GET /change-orders/:id/public)\ninterface ChangeOrderPublic {\n  id: string;\n  description: string;\n  deltaAmountCents: number;     // CENTS; +add / −credit, applied to the live invoice\n  status: \"pending\" | \"approved\" | \"declined\";\n  currentAmount?: number;       // CENTS (previous total)\n  newAmount?: number;           // CENTS (proposed/new total)\n  businessName?: string;\n  commsLanguage?: \"en\" | \"es\";\n  decidedAt?: string;\n}\n// Implied stored FKs (not in public projection): userId, contractId/invoiceId — the\n// approve path writes the delta onto the bound invoice (\"invoice_update_failed\").\n```\n\nApprove/decline: `POST /change-orders/:id/{approve|decline}` (no body).\n\n### 1.7 Invoice (+ paymentIntent claim)\n\n```ts\n// clients/dashboard.ts — stored\ninterface Invoice {\n  id: string; userId: string;\n  contractId?: string;          // OPTIONAL — standalone invoices have no contract\n  customerId?: string;\n  amount?: number;              // CENTS\n  issuedDate?: string;\n  dueDate: string;              // required\n  status?: \"scheduled\"|\"draft\"|\"pending\"|\"sent\"|\"viewed\"|\"claimed\"|\"paid\"|\"void\";\n  paidAt?: string;\n  createdAt: string; updatedAt: string;\n  urgency?: { label: string; tone: \"ok\"|\"warn\"|\"danger\"; daysOverdue?: number }; // DERIVED\n  scheduledFor?: string;        // fire date for status=scheduled\n  installmentIndex?: number;    // milestone N of…\n  installmentTotal?: number;    // …M\n  remindersMuted?: boolean;\n  paymentIntent?: {             // CUSTOMER CLAIM awaiting contractor confirm\n    method: string;\n    amount: number;             // CENTS\n    reference?: string;\n    claimedAt: string;\n    claimedBy?: string;\n  };\n}\n\n// routes/i/[id].tsx — public projection (GET /invoices/:id/public)\ninterface InvoicePublic {\n  id: string; contractId?: string; customerId?: string;\n  status?: string; amount?: number; dueDate?: string; issuedDate?: string; paidAt?: string;\n  installmentIndex?: number; installmentTotal?: number;\n  paymentIntent?: { method; amount; reference?; claimedAt; claimedBy? };\n  contractor?: Contractor & { acceptedPaymentMethods?: Record<string,{enabled?}> };\n  customer?: { name?; email?; phoneNumber? };\n  jobDetails?: { summary?; jobName?; description?;\n                 jobNameByLang?; summaryByLang?; descriptionByLang? }; // JOINED + MULTI-LANG\n  siblings?: Array<{ id; amount?; status?; paidAt?;                    // co-installments\n                     installmentIndex?; installmentTotal? }>;\n  acceptedMethods?: Array<{ method: string; handle?: string }>;        // JOINED from identity\n}\n```\n\nCustomer claim body: `POST /invoices/:id/claim-payment { method, reference?, claimedBy? }`\n→ sets `status:\"claimed\"` + `paymentIntent`. Contractor later confirms (creates a\nPayment, flips to `paid`). **No card processing** anywhere.\n\n### 1.8 Payment\n\n```ts\n// clients/payments.ts — the contractor-confirmed payment record\ninterface Payment {\n  id: string; userId: string;\n  invoiceId: string;            // → Invoice\n  amount: number;               // CENTS\n  method: \"cash\"|\"check\"|\"ach\"|\"card\"|\"venmo\"|\"zelle\"|\"cashapp\"|\"paypal\"|\"other\";\n  receivedAt: string;\n  reference?: string;\n  createdAt: string; updatedAt: string;\n}\n```\n\n`method` includes `card`/`ach` in the enum, but per the manual-only rule these are\n*recorded* methods (the contractor logs how they were paid), not processed in-app.\n\n**PaymentSplit (derived, not stored)** — `lib/payment-split.ts` is the single source of\ntruth turning a contract total + a terms label into milestones:\n\n```ts\ninterface PaymentSplitPart {\n  role: \"deposit\"|\"midpoint\"|\"milestone\"|\"completion\"|\"full\";\n  pct: number;                  // 0–100\n  amountCents: number;          // CENTS; parts sum to exactly totalCents\n}\n```\n\n### 1.9 Assistant: Conversation (thread), Message, JobOption\n\n```ts\n// clients/assistant.ts\ninterface Conversation {           // === the \"AssistantThread\"\n  id: string; userId: string;\n  customerId?: string; quoteId?: string; contractId?: string; invoiceId?: string;\n  currentPhase: \"quote\" | \"terms\";  // AgentPhase\n  title?: string; customerName?: string; preview?: string;\n  hasUnreadEvent?: boolean;         // set by accept-contract; cleared on next read\n  quoteStatus?: string;             // DENORMALIZED to avoid N+1 in the sidebar\n  contractStatus?: string;          // DENORMALIZED\n  invoiceStatus?: string;           // DENORMALIZED\n  createdAt: string; updatedAt: string;\n}\n\ninterface Message {\n  id: string; conversationId: string;\n  role: \"user\" | \"assistant\" | \"system\";\n  kind?: \"text\"|\"voice\"|\"image\"|\"action\"|\"action_card\"|\"wizard\"\n        |\"phase_divider\"|\"continue_cta\";\n  content: string;\n  createdAt: number;               // epoch ms\n}\n\ninterface ConversationDetail {     // composite read for GET /agents/conversations/:id\n  conversation: Conversation;\n  messages: Message[];\n  customer?: CustomerLite;\n  contract?: { id; status?; totalAmount? };\n}\n\n// LLM-generated scope-of-work option (not persisted as its own row; the picked\n// option seeds the quote's jobName/summary/description + …ByLang maps).\ninterface JobOption {\n  id: string; jobName: string; summary: string; bullets: string[];\n  byLang?: Record<string, { jobName: string; summary: string; bullets: string[] }>; // MULTI-LANG\n}\n```\n\n### 1.10 Notification\n\n```ts\n// clients/dashboard.ts\ninterface Notification {\n  id: string; userId: string;\n  type: \"quote_sent\"|\"quote_accepted\"|\"contract_signed\"|\"invoice_claimed\"\n      |\"invoice_paid\"|\"invoice_overdue\"|\"customer_replied\"|\"generic\";\n  title: string; body?: string;\n  entityType?: \"quote\"|\"contract\"|\"invoice\"|\"customer\"|\"conversation\";\n  entityId?: string;\n  read: boolean; readAt?: string;\n  createdAt: string;\n}\n```\n\n### 1.11 FileRecord (blob store)\n\n```ts\n// clients/files.ts — JSON+base64 upload\ninterface FileRecord {\n  id: string; userId: string;\n  filename: string; mimeType: string;\n  sizeBytes: number; pageCount: number; sha256: string;\n  createdAt: string;\n}\n// Referenced by logoFileId, insuranceFileId, w9FileId, and agents/chat payload.fileId.\n```\n\n### 1.12 ShortLink\n\n```ts\n// routes/s/[code].tsx — GET /s/:code resolver, then 302 → /q|/c|/i/:id\ninterface ShortLinkResolution { kind: \"quote\"|\"contract\"|\"invoice\"; id: string; }\n// Implied stored row: { code, userId, kind, entityId, createdAt } keyed by short code.\n```\n\n### 1.13 Composite / analytics read models (computed, NOT stored rows)\n\n```ts\n// GET /jobs — a per-active-job rollup join (quote+contract+payments)\ninterface Job {\n  id: string;\n  customer: { id; name };\n  quote: { id; summary; estimatedTotalCents };     // CENTS\n  contract: { id; status? } | null;\n  totalCents: number; paidCents: number; pctPaid: number;  // CENTS + derived %\n  nextDueDate: string | null;\n  status: \"awaiting\"|\"on_track\"|\"awaiting_permit\"|\"overdue\"|\"complete\";\n  statusLabel: string;\n}\n\n// GET /analytics/dashboard — the whole-account rollup (all CENTS)\ninterface DashboardStats {\n  customers: number;\n  quotes:    { total; draft; sent; accepted };\n  contracts: { total; draft; signed };\n  invoices:  { total; pending; paid; overdue;\n               agingBuckets: { current; aging1_14d; overdue15_30d; overdue30plus } };\n  quotedValueCents: number;        // Σ estimatedTotal over status==='sent'\n  awaitingResponse: number;\n  revenue:  { ytdCents; lastMonthCents; monthOverMonthPct; sparkline12mo: number[/*12*/] };\n  payments: { receivedYtdCents; methodMixCents: Record<string,number>;\n              topPayors: Array<{ customerId; totalCents }> };\n}\n\n// GET /analytics/quotes/win-rate, /insight, /clients/top, /clients/segments\ninterface WinRate { windowDays; decided; won; lost; winRate: number|null; }\ninterface Insight { text: string;\n  kind: \"open_count\"|\"median_days_to_decide\"|\"best_day_of_week\"|\"static_fallback\"; }\ninterface TopClient { customerId; name; revenue12moCents; rank; barPct; }   // CENTS\ninterface ClientSegmentRow { key: ClientSegmentKey; label; count; pct; }\n```\n\n---\n\n## 2. Cardinality at load (seed fixtures)\n\nAll seeds are **deterministic & hardcoded** (literal arrays/objects; copy pulled from\ni18n keys via `tFor`, numbers inline). Nothing is randomly generated. They are *fallback\nfixtures* the pages show until the live analytics/agent endpoints return.\n\n| Seed source | Export | Count | Notes |\n|---|---|---|---|\n| `lib/clients-seed.ts` | `CLIENTS` | **12** | the canonical demo client roster |\n| | `STORIES` / `SINCE_DAYS` | 12 / 12 | keyed by client name (parallel maps) |\n| | `FILTERS` | 6 | all + 5 statuses; counts derived from `CLIENTS` |\n| | `TOP_CLIENTS` | 5 | hardcoded revenue bars |\n| | `SEGMENTS` | 4 | property mgmt / homeowners / small biz / HOAs |\n| `lib/quotes-seed.ts` | `QPIPELINE` | **15** | 2 draft, 7 out-for-response, 4 won, 2 lost |\n| | `QSTORIES` | 9 | only the non-decided quotes |\n| | `buildOpens` fixtures | 7 | OpenEvent timeline pool, sliced per quote |\n| `lib/dash-seed.ts` | `SEED_JOBS` | 5 | |\n| | `SEED_QUOTES` | 4 | dashboard \"pending quotes\" strip |\n| | `SEED_OUTSTANDING` | 3 | receivables strip |\n| | `SEED_ACTIVITY` | 4 | activity feed |\n| | `SEED_KPIS` | 1 obj | fixed KPI tile values |\n| `lib/asst-seed.ts` | `seedThreads` | 3 groups / **7** items | t1–t7; `seedTotal()` returns 12 (7 + hardcoded 5) |\n\nSeed *UI-row* types (`JobRow`, `QuoteRow`, `OutstandingRow`, `ActivityEntry` from\n`components/DashSections.tsx`; `ThreadGroup`/`ThreadEntry`, `Client`, `Quote`/`Stage`)\nare **presentational shapes**, not backend DTOs — they carry display gradients/labels and\nmust not be confused with the real entities above.\n\n---\n\n## 3. Endpoint map (verb + path, grouped by feature)\n\n### Auth / session (`landing.ts`, `verify.ts`, `auth.ts`)\n- `POST /auth/send-otp` `{ phoneNumber, language? }` → `{ sent: true }`\n- `POST /auth/verify-otp` `{ phoneNumber, code }` → `{ sessionId, userId, isNewUser }`\n  (client maps to a discriminated union; routes new users → `/assistant?onboard=1`)\n- `GET  /me` → `User` (SSR session resolve; header `x-session-id`)\n- `GET  /me/wipe` → `{ ok, deleted }` (irreversible account+data wipe)\n- `PUT  /me` → `ProfileUser` (name/email/language)\n\n### Profile / business (`profile.ts`)\n- `GET  /profile` → `ProfileSnapshot`  *(also used by `dashboard.ts` as `Profile`)*\n- `PUT  /profile/identity` → `BusinessIdentity`\n- `PUT  /profile/address` → `BusinessAddress`\n- `PUT  /profile/insurance` → `BusinessInsurance`\n- `PUT  /profile/tax` → `TaxInfo`\n\n### Dashboard / analytics (`dashboard.ts`)\n- `GET  /analytics/dashboard` → `DashboardStats`\n- `GET  /jobs` → `Job[]`\n- `GET  /notifications?limit=` → `Notification[]`\n- `GET  /notifications/unread-count` → `{ count }`\n- `POST /notifications/:id/read` ; `POST /notifications/read-all`\n\n### Customers / clients analytics (`clients.ts`)\n- `GET  /clients` → `CustomerCard[]`   *(rollup-enriched)*\n- `GET  /customers` → `Customer[]` / `CustomerLite[]`\n- `POST /customers` → `CustomerCard` ; `PUT /customers/:id` → `CustomerCard`\n- `GET  /analytics/clients/top?limit=` → `{ results: TopClient[] }`\n- `GET  /analytics/clients/segments` → `{ segments: ClientSegmentRow[] }`\n\n### Quotes (`quotes.ts`, `assistant.ts`, public route)\n- `GET  /quotes?status=` → `QuoteCard[]`\n- `GET  /quotes/:id` → `QuoteCard` / `Quote`\n- `PUT  /quotes/:id` → `QuoteCard` ; `DELETE /quotes/:id` → `{ ok }`\n- `POST /quotes/:id/email` `{ to?, from? }` → `{ ok }`\n- `GET  /analytics/quotes/win-rate?days=` → `WinRate`\n- `GET  /analytics/quotes/insight` → `Insight`\n- **Public:** `GET /quotes/:id/public` → `QuotePublic` ;\n  `POST /quotes/:id/accept` `{ name?, signature? }` ;\n  `POST /quotes/:id/decline` `{ reason?, note?, name? }` ;\n  `POST /quotes/:id/inquiry` `{ question, contactBack?, name? }`\n\n### Contracts (`contracts.ts`, public route)\n- `GET  /contracts?status=` → `Contract[]` (each with derived `mood`)\n- `GET  /contracts/:id` → `Contract` ; `PUT /contracts/:id` ; `DELETE /contracts/:id`\n- **Public:** `GET /contracts/:id/public` → `ContractPublic` ;\n  `POST /contracts/:id/sign` `{ name, signature }`\n\n### Change orders (public route)\n- `GET  /change-orders/:id/public` → `ChangeOrderPublic`\n- `POST /change-orders/:id/approve` ; `POST /change-orders/:id/decline`\n\n### Invoices (`dashboard.ts`, public route)\n- `GET  /invoices?status=` → `Invoice[]`\n- `POST /invoices` `{ customerId?, amount?(CENTS), dueDate, issuedDate?, status? }` → `Invoice`\n- **Public:** `GET /invoices/:id/public` → `InvoicePublic` ;\n  `POST /invoices/:id/claim-payment` `{ method, reference?, claimedBy? }`\n\n### Payments (`payments.ts`)\n- `GET  /payments` → `Payment[]`\n- `GET  /payments?method=<m>` → `Payment[]`\n- `GET  /payments?invoiceId=<id>` → `Payment[]`\n- `GET  /payments/:id` → `Payment`\n\n### Assistant / agents (`assistant.ts`)\n- `GET  /agents/conversations?limit=` → `Conversation[]` (404-safe → `[]`)\n- `GET  /agents/conversations/:id` → `ConversationDetail`\n- `POST /agents/conversations` → `Conversation`\n- `POST /agents/conversations/:id/transition-to-terms` → `ConversationDetail`\n- `POST /agents/conversations/:id/lock-quote` `{ quoteId }`\n- `POST /agents/conversations/:id/accept-contract` `{ contractId }`  *(dev simulate sign)*\n- `POST /agents/conversations/:id/send-contract` `{ contractId, channel, language? }`\n- `POST /agents/conversations/:id/send-invoice`\n- `POST /agents/conversations/:id/bind-customer` `{ customerId }`\n- `POST /agents/conversations/sample-quote` → `{ quoteId, created }`\n- `POST /agents/chat` `{ conversationId?, content?, kind?, payload? }` → `ChatResult`\n- `POST /agents/wizard/answer` ; `POST /agents/wizard/back`\n- `POST /agents/job-details/polish` `{ raw, priceCents? }`\n- `POST /agents/job-details/options` `{ raw, priceCents? }` → `{ options: JobOption[] }`\n- `POST /agents/job-details/prices` `{ raw }`\n- `POST /agents/job-details/professionalize` `{ text }`\n- `POST /agents/job-details/translate` `{ texts, to }` → `{ texts }`\n- `GET  /customers` (customer picker)\n\n### Files (`files.ts`)\n- `POST /files` `{ filename, mimeType, base64 }` → `FileRecord`\n\n### Short links (route)\n- `GET  /s/:code` → `ShortLinkResolution` (302 redirect)\n\n### Admin (super-admin) (`admin.ts`)\n- `GET  /admin/users?q=<phone>` → `AdminUserView[]`\n- `POST /admin/users/:id/grant` ; `POST /admin/users/:id/revoke`\n- `POST /admin/impersonate/:id` ; `POST /admin/stop-impersonating` → `ImpersonationResponse`\n- `GET  /admin/whoami` → `WhoAmI`\n\n### Public-logo (referenced by `/q` and `/i` routes)\n- `GET  /api/public-logo/quote/:id` , `GET /api/public-logo/invoice/:id` (image proxy)\n\n---\n\n## 4. Consumption reverse-index (page/island → entity)\n\nDerived from which client each island imports (`grep` over `islands/` + `routes/`).\n\n| Island / route | Entities consumed | Endpoints |\n|---|---|---|\n| `DashboardPage` | `DashboardStats`, `QuoteCard[]`, `Job[]`, `ProfileSnapshot` | `/analytics/dashboard`, `/quotes`, `/profile` (+ `/jobs`) |\n| `DashSidebar` (mounted on **every** authed page) | `DashboardStats`, `ProfileSnapshot`, unread `count` | `/analytics/dashboard`, `/profile`, `/notifications/unread-count` — via `lib/dash-cache.ts` |\n| `DashTopbar` | `Notification[]`, unread `count` | `/notifications*` |\n| `QuotesPage` | `QuoteCard[]`, `WinRate`, `Insight` | `/quotes`, `/analytics/quotes/*` |\n| `QuoteCard`/`QuoteTrack`/`DeleteQuoteButton` | `QuoteCard` | `/quotes/:id`, `DELETE /quotes/:id` |\n| `ContractsPage` (+ `ContractCard`/`ContractTrack`) | `Contract[]`(→`ContractCard`), `QuoteCard`, `CustomerCard` | `/contracts`, `/quotes`, `/clients` |\n| `InvoicesPage` | `Invoice[]`, `CustomerCard`, `QuoteCard`(jobDetails) | `/invoices`, `/clients` |\n| `PaymentsPage` | `Payment[]`, `DashboardStats` | `/payments`, `/analytics/dashboard` |\n| `ClientsPage`/`ClientsBoard` | `CustomerCard[]`, `TopClient[]`, `ClientSegmentRow[]` | `/clients`, `/analytics/clients/*` |\n| `AsstChat`/`AsstThreads`/`AsstComposer`/`Composer`/`ChatHeaderLive` | `Conversation[]`, `ConversationDetail`, `Message[]`, `Quote`, `Contract`, `CustomerCard`, `FileRecord`, `DashboardStats`(overdue gate) | `/agents/*`, `/quotes/:id`, `/contracts`, `/clients`, `/files` |\n| `SettingsPage`/`SetupChecklist` | `ProfileSnapshot`, `BusinessIdentity/Address/Insurance/Tax`, `FileRecord` | `/profile*`, `/files` |\n| `AdminPage`/`ImpersonationBanner` | `AdminUserView`, `WhoAmI`, `ImpersonationResponse` | `/admin/*` |\n| `LoginForm`/`LandingScripts`/`ContactForm` | OTP send | `/auth/send-otp` |\n| `CodeInput` | OTP verify | `/auth/verify-otp` |\n| **Public** `routes/q/[id]` + `PublicQuoteActions`/`PublicAcceptQuote` | `QuotePublic` | `/quotes/:id/public`, accept/decline/inquiry |\n| **Public** `routes/c/[id]` + `PublicContractView`/`PublicSignContract` | `ContractPublic` | `/contracts/:id/public`, `/sign` |\n| **Public** `routes/i/[id]` + `PublicInvoiceClaim` | `InvoicePublic` | `/invoices/:id/public`, `/claim-payment` |\n| **Public** `routes/co/[id]` + `PublicChangeOrderActions` | `ChangeOrderPublic` | `/change-orders/:id/public`, approve/decline |\n| **Public** `routes/s/[code]` | `ShortLinkResolution` | `/s/:code` |\n\n---\n\n## 5. Data-shape hazards (design-time flags — not benchmarked)\n\nEach is a read pattern that gets expensive on a real backend at scale. Mitigation tagged\n**[cache / point-read / denormalize — don't scan]**.\n\n1. **`DashboardStats` is a whole-account rollup rendered on every authed page.**\n   It aggregates counts across *all* quotes, contracts, invoices, customers, plus\n   `quotedValueCents` (Σ over sent quotes), `revenue.sparkline12mo` (12-month paid-invoice\n   rollup), `payments.methodMixCents` and `topPayors` (Σ over all payments). The front-end\n   already de-dupes via `lib/dash-cache.ts` (single-flight + sessionStorage warm start)\n   **specifically because** `DashSidebar` mounts on every route and was firing\n   `/profile`+`/analytics/dashboard` per navigation (audit1 #20). **[precompute these\n   counters incrementally / cache; never recompute by scanning on each request].**\n\n2. **Sidebar unread badge — `/notifications/unread-count` per page mount.** A global\n   count rendered in the app shell. **[maintain a counter; point-read].**\n\n3. **`GET /clients` returns rollup-enriched `CustomerCard[]`** — every card carries\n   `balanceCents`, `activeJobs`, `revenue12moCents`, `temp`, `daysSinceContact`, `status`,\n   `lastWhen`, each a per-customer aggregate over that customer's invoices/payments/jobs.\n   Listing N customers ⇒ N rollups. Plus `/analytics/clients/top` and `/segments` are\n   separate whole-account aggregates. **[denormalize rollups onto the customer row;\n   refresh on write].**\n\n4. **`GET /quotes` returns engagement-derived `QuoteCard[]`** — `opens`/`lastOpenAt`\n   require scanning OpenEvent rows per quote; `stage`/`daysIn`/`decidedDays` are derived\n   from timestamps + opens. Plus `/analytics/quotes/win-rate` and `/insight` scan decided\n   quotes. **[store a denormalized `opensCount`/`lastOpenAt` on the quote; bucket counts].**\n\n5. **`GET /jobs`** — each `Job` sums payments to compute `paidCents`/`pctPaid`/`nextDueDate`\n   across the active set, joining quote+contract+payments. **[materialize the job rollup].**\n\n6. **`ClientsPage` filter counts** (`FILTERS` — all + per-status) are computed in the seed\n   by `CLIENTS.filter(...).length`. On a real backend that's a count-per-status bucket over\n   the whole roster, recomputed on each visit. **[status-bucketed counters].**\n\n7. **Public composite reads fan out a join per page load.** `InvoicePublic` joins\n   invoice → contract → quote(jobDetails, multi-lang) → customer → contractor profile →\n   accepted payment methods → sibling installments. `QuotePublic`/`ContractPublic` join\n   the contractor profile + customer. These are customer-facing, uncached, hit from SMS/\n   email links (potentially bursty). **[serve from a denormalized public projection /\n   point-read, not a live multi-table join].** The codebase already shows the intended\n   pattern: `Conversation` **denormalizes** `quoteStatus`/`contractStatus`/`invoiceStatus`\n   onto the thread row, explicitly \"to drive the sidebar chip without an N+1.\"\n\n8. **Invoice `urgency` + dashboard `agingBuckets`** are date-derived over all open\n   invoices. Recomputing aging on every dashboard load scans the receivables. **[bucket on\n   write / scheduled recompute].**\n\n9. **Contract list `mood`** is derived server-side per row (`deriveMood`) and the front-end\n   then groups all contracts by mood into three tracks — full-list read with no pagination\n   visible. **[paginate / pre-bucket if contract volume grows].**\n\n---\n\n## Open questions / ambiguities\n\n- **`status` vs `stage`/`mood` source of truth.** Quotes expose a stored `status`\n  (draft/sent/accepted/declined/expired) AND a derived `stage`\n  (adds opened/cooling/stale/won/lost). Contracts expose stored `status` AND derived\n  `mood`. The exact persisted `status` value-set for contracts/invoices beyond what the\n  counts use (`draft`/`signed`; the 8-value invoice union) isn't fully pinned by the\n  clients — the public projections only ever check a subset (\"signed\"/\"declined\",\n  \"accepted\"/\"lost\", \"paid\"/\"claimed\").\n- **Timestamp type inconsistency is real:** `User`/`ProfileUser`/`Message` use epoch-ms\n  `number`; nearly everything else uses ISO `string`. Not resolved here.\n- **`ProfileSnapshot.references: unknown[]`** — shape never exercised by the front-end.\n- **ChangeOrder stored FKs** (`userId`, `contractId`/`invoiceId`) are inferred from the\n  approve path (\"invoice_update_failed\") — the only typed view is the public projection,\n  which omits them.\n- **OpenEvent / quote-opens persistence** is only modeled in the seed; the real row shape\n  (device/userAgent/IP/timestamp keying) isn't declared in any client.\n- **Money units on bare `amount`/`value`/`totalAmount`/`estimatedTotal`/`price` fields**:\n  treated as CENTS here per the `dashboard.ts` comment and `lib/format.ts` contract, but\n  these fields are not individually annotated, so a few could be dollars on the wire — a\n  point to confirm against the backend DTOs.\n"}], "shared": [{"name": "contract-doc", "component": "ContractDoc", "spec": "# contract-doc (ContractDoc)\n\nThe rendered service agreement — the whole printable contract document a\ncustomer reads at `/c/:id`. One pure function (`ContractDoc`) takes a single\n`ContractPublic` projection and lays out: brand strip → pink-ribbon card →\ndoc-tag + status pill → hero title + \"between … and …\" line → To/From party\ncards → Job details (bullets + line-item table + green total card) → Payment\nschedule milestones → Terms (wizard grid + numbered legal clauses) → Signature\nblock → contact footer → \"powered by\" strip. It also exports `ErrorCard` (the\nexpired/gone state) and the colour constants `INK`, `LINE`, `BG` (the host\nroute imports these for its page wrapper).\n\n> **Genuinely shared.** Imported by BOTH the public route's island\n> (`islands/PublicContractView.tsx`) AND — historically — the route directly;\n> today the route mounts the island and the island renders `ContractDoc`. It\n> lives in `components/` (not `islands/`) so it can render server- or\n> client-side without hydration. Category `shared-components`.\n\n## 1. Classification & behavior\n- **Bucket:** `static` / **page-composition** (`components/contract-doc.tsx`,\n  1066 lines). A pure presentational function — no hooks, state, fetch, or\n  effects of its own. It *composes* one island (`PublicSignContract`) into its\n  signature section.\n- **Interaction tier:** **page-composition / static.** All output is\n  deterministic from the `contract` prop. The only interactivity it introduces\n  is the embedded sign-pad island (documented separately under\n  `pages/public-contract/components/public-sign-contract/`).\n- **Server-mutation action endpoint:** none directly. The signature it mounts\n  posts `POST /api/contracts/:id/sign` (see public-sign-contract). No flash;\n  the island reloads.\n- **Data source:** none of its own — `contract` is a prop. Upstream it comes\n  from `ssrBackendGet`/client-fetch of **`GET /contracts/:id/public`** →\n  `ContractPublic` (data-model § 1.5). Auth-FREE customer link.\n  - **Honest-empty / expired:** handled one level up — when the fetch fails the\n    island/route renders `ErrorCard` (exported here) instead of `ContractDoc`.\n    `ContractDoc` itself assumes a present `contract` and tolerates missing\n    optional sub-objects (no `customer`, no `lineItems`, etc.) by conditionally\n    rendering each section.\n- **Liveness:** none. Pure request-response render. The signed/declined state\n  is whatever the projection's `status` was at fetch time; it does not poll —\n  the embedded sign island forces a full `location.reload()` to repaint as\n  \"signed\" (see anti-patterns).\n- **Data-shape hazards:**\n  - **Public composite-read fan-out (flagged).** `ContractPublic` is a join:\n    contract → source quote (`jobDetails`, incl. multi-lang\n    `*ByLang`) → customer → contractor profile (name/business/phone/email/\n    address/state/commsLanguage/hasLogo) → wizard `terms[]`. Customer-facing,\n    uncached, hit from SMS/email links (bursty). Data-model hazard #7:\n    **[serve from a denormalized public projection / point-read, not a live\n    multi-table join].** (Smaller fan-out than the invoice — no payment-method\n    or sibling-installment join here.)\n  - **Money is INTEGER CENTS.** `totalAmount` and each `lineItem.price` are\n    cents; `fmtMoneyExact` formats. `total = contract.totalAmount ?? Σ(price×qty)`\n    — if `totalAmount` is absent it is recomputed from line items, so a wrong\n    unit on either side double-counts.\n  - **Terms stored in English, localized at render.** `Term.value` is EN;\n    `expandTermValue` + `localizeTermValue` (`lib/term-i18n.ts`) translate at\n    render per `lang`. Free-text and ratios (\"50/50\") pass through; only known\n    preset labels + numeric durations are mapped (data-model § 1.5 note, MEMORY\n    `project_job_details_multilang`).\n  - **`commsLanguage` is the language switch**, read off `contractor`, not the\n    customer — the doc renders in the *contractor's* outgoing-comms language.\n  - **Section numbering is computed, not literal.** A `let sec` counter +\n    `num()` increments only when a section actually renders (short-circuit), so\n    the 01/02/03 sequence is gapless even though Job details / Payment schedule\n    / Signature are each conditional. A rebuild that hard-codes numbers will\n    show gaps.\n  - **Gating booleans:** `signed = status==='signed'`, `declined =\n    status==='declined'`; everything else = \"awaiting\". `declined` suppresses\n    the **entire** signature section (no pad, no signed band).\n\n## 2. Anatomy (inline-style + class structure)\nAll styling is **inline `style=` strings** (public palette hex). The only CSS\nclasses are layout hooks the host route's `<style>` targets at the 720px\nbreakpoint + print: `.ctr`, `.ctr__inner`, `.ctr__title`, `.ctr__total-amt`,\n`.ctr__terms-grid`, `.ctr__milestones`, `.ctr__no-print`, `.ctr__tofrom`.\n\n```\n<>                                                    ← Fragment (no wrapper)\n  <div class=\"ctr__no-print\">                         ← brand strip (hidden in print)\n     [hasLogo] <img src=\"/api/public-logo/contract/{id}\">\n     <div>{businessLabel}</div>                       ← TEAL .18em caps 11px\n     [addressLine] <div>{addressLine}</div>\n  <article class=\"ctr\"                                ← CREAM #fffdf7, radius 24, big shadow,\n                                                          1px rgba(255,107,107,.10) border\n     <div [8px pink ribbon] linear-gradient PINK→PINK_DARK>\n     <div class=\"ctr__inner\" padding 36px 44px 40px>\n        <div [flex space-between]>                     ← top row\n           <span doc-tag pill>{docTag} · #{ID8}</span> ← rgba(20,72,82,.10), TEAL caps\n           <Pill … />                                  ← status: signed(green) / declined(red #a83b3b) / awaiting(teal)\n        <h1 class=\"ctr__title\">{heroTitle}</h1>        ← Helvetica Neue 900, 42px, TEAL\n        [customerName] <div>between <b>{biz}</b> and <b>{cust}</b> · effective <b>{date}</b></div>\n        <section class=\"ctr__tofrom\"                   ← grid auto-fit minmax(220px,1fr) gap14\n           <PartyCard role={to}  … />                  ← customer\n           <PartyCard role={from} … />                 ← contractor\n        [items>0] <section>                            ← JOB DETAILS\n           <SectionHeader n=\"01\" title={jobDetails}/>  ← see §\"SectionHeader\" below\n           {detailLines → <ul> bullets (green dot) | <p> if single}\n           [items>1] <table>                           ← description / [qty] / amount cols\n           <div [green total card]                     ← linear 135deg #e8f3e2→#dceadb,\n                 .ctr__total-amt>{fmtMoneyExact(total)} ←   1px rgba(81,152,67,.25), radius16\n        [milestones>0] <section>                       ← PAYMENT SCHEDULE\n           <SectionHeader n=\"02\" .../>\n           <div class=\"ctr__milestones\" grid auto-fit minmax(140px,1fr)>\n              {milestones.map → KV-ish card label/amount/when}\n        <section>                                      ← TERMS (always renders)\n           <SectionHeader n=\"03\" .../>\n           [hasTermGrid] <div class=\"ctr__terms-grid\" grid 1fr 1fr>{<KV/> rows}</div>\n           [hasTermGrid] <hr 1px LINE/>\n           <ol>{clauses.map → <li><b>{title}.</b> {body}</li>}</ol>  ← 14 numbered legal clauses\n        [!declined] <section>                          ← SIGNATURE\n           <SectionHeader n=\"04\" title={signHere}/>\n           <div>{signed ? bothCaptured : bySigning(customerFirst)}</div>\n           <div [grid auto-fit minmax(200px,1fr)]>\n              <div [contractor card]>                  ← cursive name (Snell Roundhand), \"by\", date\n              {signed\n                ? <div [customer SIGNED card]>          ← cursive customerSignedName + signedAt\n                : <div [YOUR signature card]>}          ← dashed underline + \"sign / type below\"\n           {!signed && <PublicSignContract contractId={id} lang={lang} />}  ← ISLAND\n           {signed && <div [green \"Signed and binding\" band + check svg]>}\n        [phone||email] <footer [contact]>              ← initials avatar + \"Questions? Call/email …\"\n  <div [powered-by strip]> <img /logo.png> {poweredBy} #{ID8}\n```\n\n- **Local sub-components (all in this file):** `Pill`, `SectionHeader`, `KV`,\n  `PartyCard`. Local helpers: `cstr(lang)` (i18n string table), `expandStateName`,\n  `expandTermValue`, `isEmptyWarranty`, `computeMilestones` (→ `lib/payment-split.ts`),\n  `sumLineTotals`, `initialsFromName`, `fmtDate`, `termValue`.\n- **⚠️ SectionHeader is NOT ui/SectionHead.** This component defines its own\n  private `SectionHeader({n,title})` (a numbered teal badge + caps title +\n  hairline rule). It does **NOT** import the shared `components/ui/SectionHead`\n  (which, per `shared-components/ui-section-head/`, is confirmed-unused, zero\n  imports). The build-brief's claim that contract-doc \"uses ui/SectionHead\" is\n  inaccurate — record the private `SectionHeader` instead. (See Open questions.)\n- **External deps:** `lib/format.ts` (`detailLines`, `fmtMoneyExact`, `fmtPhone`,\n  `telHref`), `lib/i18n.ts` (`tFor`), `lib/term-i18n.ts` (`localizeTermValue`),\n  `lib/payment-split.ts` (`computePaymentSplit`, `MilestoneRole`), and the\n  `PublicSignContract` island.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `contract` | `ContractPublic` (required) | — | json (single object) | no |\n\n`ContractPublic` shape (data-model § 1.5): `id`, `quoteId?`, `customerId?`,\n`status?`, `totalAmount?`(cents), `effectiveDate?`, `startDate?`,\n`estimatedCompletionDate?`, `signedAt?`, `customerSignedName?`, `contractor?`\n(name/businessName/phoneNumber/email/addressLine/state/commsLanguage/hasLogo),\n`customer?` (name/phoneNumber/email), `jobDetails?` (summary/jobName/description\n+ `*ByLang` maps + `lineItems[]`), `terms?: Term[]`, `createdAt?`.\n`ErrorCard({ message, lang })` is the secondary export.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| unsigned | `status` not signed/declined → teal \"Awaiting your signature\" pill; mounts `PublicSignContract` | `cases/unsigned/unsigned.json` |\n| signed | `status='signed'` → green pill, filled cursive customer signature, \"Signed and binding\" band, no pad | `cases/signed/signed.json` |\n| declined | `status='declined'` → red pill; **whole signature section suppressed** | `cases/declined/declined.json` |\n| es | `contractor.commsLanguage='es'` flips the whole doc; `*ByLang.es` copy used | `cases/es/es.json` |\n| no-line-items | `lineItems=[]` → Job details section omitted; total from `totalAmount`; numbering stays gapless | `cases/no-line-items/no-line-items.json` |\n| single-line-item | one item → bullets render, line-item table suppressed (`items.length>1` gate); \"Due Now\" terms → single full milestone \"on signing\" | `cases/single-line-item/single-line-item.json` |\n| error/expired | (separate export) `ErrorCard` \"This link is no longer available / can't open\" | covered by `public-contract-view/cases/error` |\n\n## 5. Events\n- `ContractDoc` itself emits no events (static).\n- The embedded `PublicSignContract` island owns the signature drawing + submit\n  events — see that component's spec § 5.\n\n## 6. Motion (real CSS only)\n- The doc has **no animation** of its own. No transitions, no keyframes.\n- The host route adds a 720px-breakpoint resize of padding/font + a print\n  block (see `css/contract-doc.css`).\n- The embedded sign island has the only motion (button `transform 160ms` +\n  borrowed `.spinner`). No rAF/canvas animation in the doc; the canvas drawing\n  is in the island (jank lint there).\n- **Reduced motion:** N/A for the doc (nothing animates). The public surface\n  does **not** load the Sabor tokens reduced-motion clamp — flag carried on the\n  sign island/skeleton.\n\n## 7. Responsive\n- Mobile-first; the doc lives inside the route's `max-width:760px` column.\n- All multi-column blocks use `grid-template-columns: repeat(auto-fit, minmax(…,1fr))`\n  (To/From 220px, milestones 140px, signature 200px) so they collapse to one\n  column on phones without a media query. The Terms grid is a fixed\n  `1fr 1fr` collapsed to `1fr` by the route's `.ctr__terms-grid` 720px override.\n- At ≤720px the route shrinks `.ctr__inner` padding (36/44→28/22), `.ctr__title`\n  42→30, `.ctr__total-amt` 42→34, milestones → fixed 2-up. Verify at ~390 and\n  640.\n\n## 8. A11y\n- Headings: a single `<h1>` (hero title). Section headers are styled `<div>`s\n  (not `<h2>`) — heading hierarchy is shallow (rebuild could promote\n  `SectionHeader` to `<h2>`).\n- Status, doc-tag, milestone labels are plain text/colour — colour-only status\n  (green/red/teal pill) but each pill also carries a text label, so not\n  colour-alone.\n- Links (`tel:`/`mailto:`) are real anchors. The brand-strip `<img>` and\n  `/logo.png` have empty `alt=\"\"` (decorative) — correct.\n- Signature-section a11y lives on the embedded island (canvas keyboard\n  fallback discussion there). The doc's \"type your name below\" copy points at\n  it.\n\n## 9. Used on\n- **public-contract route** (`routes/c/[id].tsx`) — via the\n  `PublicContractView` island which renders `<ContractDoc>` after the\n  client-side fetch resolves.\n- **`islands/PublicContractView.tsx`** — the direct importer (renders\n  `ContractDoc` or `ErrorCard`).\n- Evidence: `grep -rn \"contract-doc\"` → `routes/c/[id].tsx` (imports `BG, INK,\n  LINE`) + `islands/PublicContractView.tsx` (imports `ContractDoc`,\n  `ContractPublic`, `ErrorCard`). No authed-app importer.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"contract-doc\",\n  \"components\": {\n    \"ContractDoc\": {\n      \"_note\": \"Renders <ContractDoc contract={ContractPublic} />. The single `contract` prop is a public projection (GET /contracts/:id/public). commsLanguage on contractor flips the whole doc en/es. status gates: 'signed' | 'declined' | (anything else = awaiting). Mounts the PublicSignContract island in the unsigned, non-declined state. Money is INTEGER CENTS.\",\n      \"contract\": {\n        \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n        \"quoteId\": \"qt_5521aa\",\n        \"customerId\": \"cus_77a1\",\n        \"status\": \"draft\",\n        \"totalAmount\": 1450000,\n        \"effectiveDate\": \"2026-06-10\",\n        \"startDate\": \"2026-06-24\",\n        \"estimatedCompletionDate\": \"2026-07-18\",\n        \"contractor\": {\n          \"name\": \"Hans Hansen\",\n          \"businessName\": \"Hansen Decks & Patios\",\n          \"phoneNumber\": \"+15125550148\",\n          \"email\": \"hans@hansendecks.com\",\n          \"addressLine\": \"812 Cedar Ln, Austin, TX 78704\",\n          \"state\": \"TX\",\n          \"commsLanguage\": \"en\",\n          \"hasLogo\": false\n        },\n        \"customer\": {\n          \"name\": \"Maria Delgado\",\n          \"phoneNumber\": \"+15125550199\",\n          \"email\": \"maria.delgado@example.com\"\n        },\n        \"jobDetails\": {\n          \"jobName\": \"Backyard Deck Build\",\n          \"summary\": \"Quote: build a 16x20 cedar deck\",\n          \"description\": \"Demo and haul the old deck.\\nFrame and build a 16x20 pressure-treated deck with cedar surface boards.\\nInstall two sets of stairs and a 36in railing.\\nSeal and stain the finished surface.\",\n          \"lineItems\": [\n            { \"description\": \"Demo & haul existing deck\", \"price\": 180000, \"quantity\": 1, \"unit\": \"job\" },\n            { \"description\": \"Framing & decking — cedar 16x20\", \"price\": 920000, \"quantity\": 1 },\n            { \"description\": \"Stairs & railing\", \"price\": 250000, \"quantity\": 1 },\n            { \"description\": \"Seal & stain\", \"price\": 100000, \"quantity\": 1 }\n          ]\n        },\n        \"terms\": [\n          { \"stepId\": \"customer\", \"label\": \"Customer\", \"value\": \"Maria Delgado\" },\n          { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"50% deposit, 50% on completion\" },\n          { \"stepId\": \"warranty\", \"label\": \"Warranty\", \"value\": \"12 months\" },\n          { \"stepId\": \"governing_state\", \"label\": \"Governing law\", \"value\": \"Use my business state\" }\n        ],\n        \"createdAt\": \"2026-06-10T15:04:00Z\"\n      }\n    }\n  }\n}\n", "cases": [{"name": "declined.json", "content": "{\n  \"_name\": \"Declined — red status pill; ENTIRE signature section suppressed (no sign island, no signed band)\",\n  \"contract\": {\n    \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n    \"quoteId\": \"qt_5521aa\",\n    \"customerId\": \"cus_77a1\",\n    \"status\": \"declined\",\n    \"totalAmount\": 1450000,\n    \"effectiveDate\": \"2026-06-10\",\n    \"startDate\": \"2026-06-24\",\n    \"estimatedCompletionDate\": \"2026-07-18\",\n    \"contractor\": {\n      \"name\": \"Hans Hansen\",\n      \"businessName\": \"Hansen Decks & Patios\",\n      \"phoneNumber\": \"+15125550148\",\n      \"email\": \"hans@hansendecks.com\",\n      \"addressLine\": \"812 Cedar Ln, Austin, TX 78704\",\n      \"state\": \"TX\",\n      \"commsLanguage\": \"en\"\n    },\n    \"customer\": { \"name\": \"Maria Delgado\", \"email\": \"maria.delgado@example.com\", \"phoneNumber\": \"+15125550199\" },\n    \"jobDetails\": {\n      \"jobName\": \"Backyard Deck Build\",\n      \"summary\": \"build a 16x20 cedar deck\",\n      \"description\": \"Demo and haul the old deck.\\nFrame and build a 16x20 cedar deck.\",\n      \"lineItems\": [\n        { \"description\": \"Framing & decking — cedar 16x20\", \"price\": 920000, \"quantity\": 1 },\n        { \"description\": \"Stairs & railing\", \"price\": 250000, \"quantity\": 1 }\n      ]\n    },\n    \"terms\": [\n      { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"50% deposit, 50% on completion\" }\n    ],\n    \"createdAt\": \"2026-06-10T15:04:00Z\"\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — contractor.commsLanguage='es' flips the whole doc; per-lang jobName/summary/description used\",\n  \"contract\": {\n    \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n    \"quoteId\": \"qt_5521aa\",\n    \"customerId\": \"cus_77a1\",\n    \"status\": \"draft\",\n    \"totalAmount\": 1450000,\n    \"effectiveDate\": \"2026-06-10\",\n    \"startDate\": \"2026-06-24\",\n    \"estimatedCompletionDate\": \"2026-07-18\",\n    \"contractor\": {\n      \"name\": \"Hans Hansen\",\n      \"businessName\": \"Hansen Decks & Patios\",\n      \"phoneNumber\": \"+15125550148\",\n      \"email\": \"hans@hansendecks.com\",\n      \"addressLine\": \"812 Cedar Ln, Austin, TX 78704\",\n      \"state\": \"TX\",\n      \"commsLanguage\": \"es\"\n    },\n    \"customer\": { \"name\": \"Maria Delgado\", \"email\": \"maria.delgado@example.com\", \"phoneNumber\": \"+15125550199\" },\n    \"jobDetails\": {\n      \"jobName\": \"Construcción de Terraza\",\n      \"jobNameByLang\": { \"en\": \"Backyard Deck Build\", \"es\": \"Construcción de Terraza\" },\n      \"summary\": \"construir una terraza de cedro 16x20\",\n      \"summaryByLang\": { \"en\": \"build a 16x20 cedar deck\", \"es\": \"construir una terraza de cedro 16x20\" },\n      \"description\": \"Demoler y retirar la terraza vieja.\\nEnmarcar y construir una terraza de cedro 16x20.\\nInstalar escaleras y barandilla.\\nSellar y teñir.\",\n      \"descriptionByLang\": {\n        \"en\": \"Demo and haul the old deck.\\nFrame and build a 16x20 cedar deck.\",\n        \"es\": \"Demoler y retirar la terraza vieja.\\nEnmarcar y construir una terraza de cedro 16x20.\\nInstalar escaleras y barandilla.\\nSellar y teñir.\"\n      },\n      \"lineItems\": [\n        { \"description\": \"Demolición y retiro de terraza\", \"price\": 180000, \"quantity\": 1 },\n        { \"description\": \"Estructura y cubierta — cedro 16x20\", \"price\": 920000, \"quantity\": 1 },\n        { \"description\": \"Escaleras y barandilla\", \"price\": 250000, \"quantity\": 1 },\n        { \"description\": \"Sellado y teñido\", \"price\": 100000, \"quantity\": 1 }\n      ]\n    },\n    \"terms\": [\n      { \"stepId\": \"customer\", \"label\": \"Customer\", \"value\": \"Maria Delgado\" },\n      { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"50% deposit, 50% on completion\" },\n      { \"stepId\": \"warranty\", \"label\": \"Warranty\", \"value\": \"12 months\" }\n    ],\n    \"createdAt\": \"2026-06-10T15:04:00Z\"\n  }\n}\n"}, {"name": "no-line-items.json", "content": "{\n  \"_name\": \"No line items — Job details section suppressed (items.length===0); total falls back to contract.totalAmount; gapless section numbers still 01,02…\",\n  \"contract\": {\n    \"id\": \"ctr_aa00bb11cc22dd33\",\n    \"quoteId\": \"qt_lump1\",\n    \"status\": \"draft\",\n    \"totalAmount\": 600000,\n    \"startDate\": \"2026-07-01\",\n    \"contractor\": {\n      \"name\": \"Hans Hansen\",\n      \"businessName\": \"Hansen Decks & Patios\",\n      \"phoneNumber\": \"+15125550148\",\n      \"commsLanguage\": \"en\"\n    },\n    \"customer\": { \"name\": \"Maria Delgado\" },\n    \"jobDetails\": {\n      \"jobName\": \"Fence Repair\",\n      \"summary\": \"repair the back fence\",\n      \"lineItems\": []\n    },\n    \"terms\": [\n      { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"Due on completion\" }\n    ],\n    \"createdAt\": \"2026-06-15T10:00:00Z\"\n  }\n}\n"}, {"name": "signed.json", "content": "{\n  \"_name\": \"Signed — green status pill, filled customer signature, 'Signed and binding' band (no sign island)\",\n  \"contract\": {\n    \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n    \"quoteId\": \"qt_5521aa\",\n    \"customerId\": \"cus_77a1\",\n    \"status\": \"signed\",\n    \"totalAmount\": 1450000,\n    \"effectiveDate\": \"2026-06-10\",\n    \"startDate\": \"2026-06-24\",\n    \"estimatedCompletionDate\": \"2026-07-18\",\n    \"signedAt\": \"2026-06-12T18:22:00Z\",\n    \"customerSignedName\": \"Maria Delgado\",\n    \"contractor\": {\n      \"name\": \"Hans Hansen\",\n      \"businessName\": \"Hansen Decks & Patios\",\n      \"phoneNumber\": \"+15125550148\",\n      \"email\": \"hans@hansendecks.com\",\n      \"addressLine\": \"812 Cedar Ln, Austin, TX 78704\",\n      \"state\": \"TX\",\n      \"commsLanguage\": \"en\"\n    },\n    \"customer\": { \"name\": \"Maria Delgado\", \"email\": \"maria.delgado@example.com\", \"phoneNumber\": \"+15125550199\" },\n    \"jobDetails\": {\n      \"jobName\": \"Backyard Deck Build\",\n      \"summary\": \"build a 16x20 cedar deck\",\n      \"description\": \"Demo and haul the old deck.\\nFrame and build a 16x20 cedar deck.\\nInstall stairs and railing.\\nSeal and stain.\",\n      \"lineItems\": [\n        { \"description\": \"Demo & haul existing deck\", \"price\": 180000, \"quantity\": 1 },\n        { \"description\": \"Framing & decking — cedar 16x20\", \"price\": 920000, \"quantity\": 1 },\n        { \"description\": \"Stairs & railing\", \"price\": 250000, \"quantity\": 1 },\n        { \"description\": \"Seal & stain\", \"price\": 100000, \"quantity\": 1 }\n      ]\n    },\n    \"terms\": [\n      { \"stepId\": \"customer\", \"label\": \"Customer\", \"value\": \"Maria Delgado\" },\n      { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"50% deposit, 50% on completion\" },\n      { \"stepId\": \"warranty\", \"label\": \"Warranty\", \"value\": \"12 months\" }\n    ],\n    \"createdAt\": \"2026-06-10T15:04:00Z\"\n  }\n}\n"}, {"name": "single-line-item.json", "content": "{\n  \"_name\": \"Single line item — description bullets render but the line-item TABLE is suppressed (items.length<=1); only the green total card shows the money. 'Due Now' payment terms collapse milestones to a single full payment owed on signing.\",\n  \"contract\": {\n    \"id\": \"ctr_one1one1one1one1\",\n    \"quoteId\": \"qt_one1\",\n    \"status\": \"draft\",\n    \"totalAmount\": 320000,\n    \"startDate\": \"2026-06-30\",\n    \"contractor\": {\n      \"name\": \"Hans Hansen\",\n      \"businessName\": \"Hansen Decks & Patios\",\n      \"phoneNumber\": \"+15125550148\",\n      \"email\": \"hans@hansendecks.com\",\n      \"commsLanguage\": \"en\"\n    },\n    \"customer\": { \"name\": \"Maria Delgado\", \"phoneNumber\": \"+15125550199\" },\n    \"jobDetails\": {\n      \"jobName\": \"Pressure Wash & Seal\",\n      \"summary\": \"pressure wash and seal the driveway\",\n      \"description\": \"Pressure wash the full driveway and apply two coats of sealer.\",\n      \"lineItems\": [\n        { \"description\": \"Pressure wash + 2-coat seal — driveway\", \"price\": 320000, \"quantity\": 1 }\n      ]\n    },\n    \"terms\": [\n      { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"Due Now\" }\n    ],\n    \"createdAt\": \"2026-06-16T09:30:00Z\"\n  }\n}\n"}, {"name": "unsigned.json", "content": "{\n  \"_name\": \"Unsigned — awaiting signature (mounts PublicSignContract)\",\n  \"contract\": {\n    \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n    \"quoteId\": \"qt_5521aa\",\n    \"customerId\": \"cus_77a1\",\n    \"status\": \"draft\",\n    \"totalAmount\": 1450000,\n    \"effectiveDate\": \"2026-06-10\",\n    \"startDate\": \"2026-06-24\",\n    \"estimatedCompletionDate\": \"2026-07-18\",\n    \"contractor\": {\n      \"name\": \"Hans Hansen\",\n      \"businessName\": \"Hansen Decks & Patios\",\n      \"phoneNumber\": \"+15125550148\",\n      \"email\": \"hans@hansendecks.com\",\n      \"addressLine\": \"812 Cedar Ln, Austin, TX 78704\",\n      \"state\": \"TX\",\n      \"commsLanguage\": \"en\"\n    },\n    \"customer\": { \"name\": \"Maria Delgado\", \"email\": \"maria.delgado@example.com\", \"phoneNumber\": \"+15125550199\" },\n    \"jobDetails\": {\n      \"jobName\": \"Backyard Deck Build\",\n      \"summary\": \"build a 16x20 cedar deck\",\n      \"description\": \"Demo and haul the old deck.\\nFrame and build a 16x20 cedar deck.\\nInstall stairs and railing.\\nSeal and stain.\",\n      \"lineItems\": [\n        { \"description\": \"Demo & haul existing deck\", \"price\": 180000, \"quantity\": 1 },\n        { \"description\": \"Framing & decking — cedar 16x20\", \"price\": 920000, \"quantity\": 1 },\n        { \"description\": \"Stairs & railing\", \"price\": 250000, \"quantity\": 1 },\n        { \"description\": \"Seal & stain\", \"price\": 100000, \"quantity\": 1 }\n      ]\n    },\n    \"terms\": [\n      { \"stepId\": \"customer\", \"label\": \"Customer\", \"value\": \"Maria Delgado\" },\n      { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"50% deposit, 50% on completion\" },\n      { \"stepId\": \"warranty\", \"label\": \"Warranty\", \"value\": \"12 months\" }\n    ],\n    \"createdAt\": \"2026-06-10T15:04:00Z\"\n  }\n}\n"}], "css": "/* contract-doc.css — EXTRACTED real CSS for the rendered contract document.\n *\n * IMPORTANT: contract-doc.tsx carries NO stylesheet of its own. Every visual\n * is an inline `style=` string (see js/contract-doc.tsx and the .md § Anatomy\n * for the literal inline values). The only CSS that touches it lives in the\n * HOST ROUTE (`routes/c/[id].tsx`) <style> block, reproduced here because the\n * doc's class hooks (`.ctr`, `.ctr__inner`, `.ctr__title`, `.ctr__total-amt`,\n * `.ctr__terms-grid`, `.ctr__milestones`, `.ctr__no-print`) are ONLY styled by\n * these rules. The classes exist on the doc purely so the route can override\n * the inline padding/size at the mobile breakpoint and hide the brand strip in\n * print. Rebuild the doc as inline styles + this single media/print pair.\n *\n * Surface: PUBLIC palette (inline hex), NOT Sabor tokens.\n *   PINK #FF6B6B · PINK_DARK #d94e4e · TEAL #144852 · GREEN #519843\n *   INK #1c2c30 · MUTED #6b7a7e · LINE #e3e8e6 · CREAM #fffdf7 · BG #f7f6f1\n */\n\n/* --- Mobile cutover (route inline <style>, max-width:720px) ------------------ */\n@media (max-width: 720px) {\n  .ctr__inner { padding: 28px 22px !important; }      /* from 36px 44px 40px */\n  .ctr__title { font-size: 30px !important; }          /* from 42px */\n  .ctr__total-amt { font-size: 34px !important; }      /* from 42px */\n  .ctr__terms-grid { grid-template-columns: 1fr !important; }     /* 2-col → 1 */\n  .ctr__milestones { grid-template-columns: 1fr 1fr !important; } /* auto-fit → fixed 2 */\n}\n\n/* --- Print (route inline <style>) ------------------------------------------- */\n@media print {\n  /* host route also sets body background #fff !important */\n  .ctr__no-print { display: none !important; }  /* hides logo + brand strip */\n  .ctr {\n    box-shadow: none !important;\n    border: 1px solid #e3e8e6;  /* LINE — interpolated from the route */\n  }\n}\n", "cssName": "contract-doc.css", "source": "/**\n * Contract rendering for the public agreement page (/c/:id).\n *\n * Extracted from the route so a hydrating island (PublicContractView) can\n * fetch the contract client-side and paint a skeleton first, instead of\n * blocking SSR on the backend round-trip (which left the page blank-white\n * until the first byte). See problems.md #25.\n */\nimport PublicSignContract from \"../islands/PublicSignContract.tsx\";\nimport { computePaymentSplit, type MilestoneRole } from \"../lib/payment-split.ts\";\nimport {\n  detailLines,\n  fmtMoneyExact,\n  fmtPhone,\n  telHref,\n} from \"../lib/format.ts\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\nimport { localizeTermValue } from \"../lib/term-i18n.ts\";\n\ninterface Contractor {\n  name?: string;\n  businessName?: string;\n  phoneNumber?: string;\n  email?: string;\n  addressLine?: string;\n  state?: string;\n  /** Outgoing-comms language (roadmap p.13) — drives this page's copy. */\n  commsLanguage?: string;\n  /** True when the contractor uploaded a business logo — rendered via the\n   *  public-logo endpoint above the brand strip (roadmap p.2). */\n  hasLogo?: boolean;\n}\n\n/** Two-letter US state abbreviations → full names. Used to expand the\n *  \"Use my business state\" / \"Yes\" wizard answers into customer-readable\n *  sentences on the contract page. */\nconst US_STATES: Record<string, string> = {\n  AL: \"Alabama\",\n  AK: \"Alaska\",\n  AZ: \"Arizona\",\n  AR: \"Arkansas\",\n  CA: \"California\",\n  CO: \"Colorado\",\n  CT: \"Connecticut\",\n  DE: \"Delaware\",\n  DC: \"District of Columbia\",\n  FL: \"Florida\",\n  GA: \"Georgia\",\n  HI: \"Hawaii\",\n  ID: \"Idaho\",\n  IL: \"Illinois\",\n  IN: \"Indiana\",\n  IA: \"Iowa\",\n  KS: \"Kansas\",\n  KY: \"Kentucky\",\n  LA: \"Louisiana\",\n  ME: \"Maine\",\n  MD: \"Maryland\",\n  MA: \"Massachusetts\",\n  MI: \"Michigan\",\n  MN: \"Minnesota\",\n  MS: \"Mississippi\",\n  MO: \"Missouri\",\n  MT: \"Montana\",\n  NE: \"Nebraska\",\n  NV: \"Nevada\",\n  NH: \"New Hampshire\",\n  NJ: \"New Jersey\",\n  NM: \"New Mexico\",\n  NY: \"New York\",\n  NC: \"North Carolina\",\n  ND: \"North Dakota\",\n  OH: \"Ohio\",\n  OK: \"Oklahoma\",\n  OR: \"Oregon\",\n  PA: \"Pennsylvania\",\n  RI: \"Rhode Island\",\n  SC: \"South Carolina\",\n  SD: \"South Dakota\",\n  TN: \"Tennessee\",\n  TX: \"Texas\",\n  UT: \"Utah\",\n  VT: \"Vermont\",\n  VA: \"Virginia\",\n  WA: \"Washington\",\n  WV: \"West Virginia\",\n  WI: \"Wisconsin\",\n  WY: \"Wyoming\",\n};\n\nfunction expandStateName(code: string | undefined): string | undefined {\n  if (!code) return undefined;\n  const upper = code.trim().toUpperCase();\n  return US_STATES[upper] ?? code;\n}\n\ninterface LineItem {\n  description: string;\n  price?: number;\n  quantity?: number;\n  unit?: string;\n}\n\ninterface Term {\n  stepId: string;\n  label: string;\n  value: string;\n}\n\nexport interface ContractPublic {\n  id: string;\n  quoteId?: string;\n  customerId?: string;\n  status?: string;\n  totalAmount?: number;\n  effectiveDate?: string;\n  startDate?: string;\n  estimatedCompletionDate?: string;\n  signedAt?: string;\n  customerSignedName?: string;\n  contractor?: Contractor;\n  customer?: { name?: string; phoneNumber?: string; email?: string };\n  jobDetails?: {\n    summary?: string;\n    jobName?: string;\n    description?: string;\n    /** Per-language title/summary/description (keyed by lang code). When\n     *  present, the doc renders these in its own language instead of the\n     *  single-language fields above. */\n    descriptionByLang?: Record<string, string>;\n    jobNameByLang?: Record<string, string>;\n    summaryByLang?: Record<string, string>;\n    lineItems?: LineItem[];\n  };\n  terms?: Term[];\n  createdAt?: string;\n}\n\nconst PINK = \"#FF6B6B\";\nconst PINK_DARK = \"#d94e4e\";\nconst TEAL = \"#144852\";\nconst GREEN = \"#519843\";\nexport const INK = \"#1c2c30\";\nconst MUTED = \"#6b7a7e\";\nexport const LINE = \"#e3e8e6\";\nconst CREAM = \"#fffdf7\";\nexport const BG = \"#f7f6f1\";\n\n/** Roadmap p.13: the public contract is customer-facing, so it renders in\n *  the contractor's OUTGOING-COMMS language (default en). One table so the\n *  whole document flips together. */\nfunction cstr(lang: Lang) {\n  const clauseKeys = [\n    \"governingLaw\",\n    \"jobDetails\",\n    \"paymentTerms\",\n    \"changeOrders\",\n    \"customerResponsibilities\",\n    \"delays\",\n    \"warranty\",\n    \"limitationOfLiability\",\n    \"rightToStopWork\",\n    \"termination\",\n    \"disputeResolution\",\n    \"permits\",\n    \"indemnification\",\n    \"entireAgreement\",\n  ];\n  return {\n    docTag: tFor(lang, \"contractDoc.docTag\"),\n    signed: (d: string) => tFor(lang, \"contractDoc.signed\", { date: d }),\n    declined: tFor(lang, \"status.declined\"),\n    awaiting: tFor(lang, \"contractDoc.awaiting\"),\n    between: tFor(lang, \"contractDoc.between\"),\n    and: tFor(lang, \"contractDoc.and\"),\n    effective: tFor(lang, \"contractDoc.effective\"),\n    to: tFor(lang, \"contractDoc.to\"),\n    from: tFor(lang, \"contractDoc.from\"),\n    jobDetails: tFor(lang, \"contractDoc.jobDetails\"),\n    tableDescription: tFor(lang, \"contractDoc.tableDescription\"),\n    tableQty: tFor(lang, \"contractDoc.tableQty\"),\n    tableAmount: tFor(lang, \"contractDoc.tableAmount\"),\n    unitEach: tFor(lang, \"contractDoc.unitEach\"),\n    agreementValue: tFor(lang, \"contractDoc.agreementValue\"),\n    allIn: tFor(lang, \"contractDoc.allIn\"),\n    paymentSchedule: tFor(lang, \"contractDoc.paymentSchedule\"),\n    terms: tFor(lang, \"contractDoc.terms\"),\n    start: tFor(lang, \"contractDoc.start\"),\n    estCompletion: tFor(lang, \"contractDoc.estCompletion\"),\n    estimated: (v: string) => tFor(lang, \"contractDoc.estimated\", { value: v }),\n    signHere: tFor(lang, \"contractDoc.signHere\"),\n    bothCaptured: tFor(lang, \"contractDoc.bothCaptured\"),\n    bySigning: (first?: string) =>\n      first\n        ? tFor(lang, \"contractDoc.bySigningNamed\", { name: first })\n        : tFor(lang, \"contractDoc.bySigning\"),\n    contractor: tFor(lang, \"contractDoc.contractor\"),\n    by: tFor(lang, \"contractDoc.by\"),\n    date: tFor(lang, \"contractDoc.date\"),\n    today: tFor(lang, \"contractDoc.today\"),\n    yourSignature: tFor(lang, \"contractDoc.yourSignature\"),\n    signTypeBelow: tFor(lang, \"contractDoc.signTypeBelow\"),\n    clientSignature: tFor(lang, \"contractDoc.clientSignature\"),\n    signatureOf: (n: string) =>\n      tFor(lang, \"contractDoc.signatureOf\", { name: n }),\n    signedBinding: tFor(lang, \"contractDoc.signedBinding\"),\n    signedNote: tFor(lang, \"contractDoc.signedNote\"),\n    qBefore: tFor(lang, \"contractDoc.qBefore\"),\n    callWord: tFor(lang, \"contractDoc.callWord\"),\n    orWord: tFor(lang, \"contractDoc.orWord\"),\n    emailWord: tFor(lang, \"contractDoc.emailWord\"),\n    lookForward: tFor(lang, \"contractDoc.lookForward\"),\n    poweredBy: tFor(lang, \"contractDoc.poweredBy\"),\n    termLabels: {\n      customer: tFor(lang, \"contractDoc.termLabel.customer\"),\n      start_date: tFor(lang, \"contractDoc.termLabel.startDate\"),\n      wraps: tFor(lang, \"contractDoc.termLabel.wraps\"),\n      payment_terms: tFor(lang, \"contractDoc.termLabel.paymentTerms\"),\n      warranty: tFor(lang, \"contractDoc.termLabel.warranty\"),\n    } as Record<string, string>,\n    clauses: clauseKeys.map((k) =>\n      [\n        tFor(lang, `contractDoc.clause.${k}.title`),\n        tFor(lang, `contractDoc.clause.${k}.body`),\n      ] as [string, string]\n    ),\n  };\n}\n\nexport function ErrorCard(\n  { message, lang = \"en\" }: { message: string; lang?: Lang },\n) {\n  return (\n    <>\n      <div\n        style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${GREEN};text-align:center;margin-bottom:18px`}\n      >\n        {tFor(lang, \"brand.name\")}\n      </div>\n      <div style=\"background:#fff;border-radius:18px;padding:32px;box-shadow:0 8px 32px rgba(20,72,82,0.08);text-align:center\">\n        <div style={`font-weight:800;color:${TEAL};font-size:18px`}>\n          {tFor(lang, \"contractDoc.cantOpen\")}\n        </div>\n        <p style={`margin:8px 0 0;color:${MUTED};font-size:14px`}>{message}</p>\n      </div>\n    </>\n  );\n}\n\nexport function ContractDoc({ contract }: { contract: ContractPublic }) {\n  const signed = contract.status === \"signed\";\n  const declined = contract.status === \"declined\";\n  const total = contract.totalAmount ??\n    sumLineTotals(contract.jobDetails?.lineItems);\n  const customerName = contract.customer?.name?.trim();\n  const customerFirst = customerName?.split(/\\s+/)[0];\n  const contractor = contract.contractor;\n  const es = contractor?.commsLanguage === \"es\";\n  const lang: Lang = es ? \"es\" : \"en\";\n  const t = cstr(lang);\n  const businessLabel = contractor?.businessName?.trim() ||\n    contractor?.name?.trim() || tFor(lang, \"brand.name\");\n  const contractorName = contractor?.name?.trim();\n  const senderInitials = initialsFromName(contractorName ?? businessLabel);\n\n  const items = contract.jobDetails?.lineItems ?? [];\n  const showQty = items.some((li) => (li.quantity ?? 1) > 1);\n  // Title/summary in the document's language when the per-language fields are\n  // present (populated from the picked job option); else the single value.\n  const summary =\n    (contract.jobDetails?.summaryByLang?.[lang] ??\n      contract.jobDetails?.summary ?? tFor(lang, \"contractDoc.serviceAgreement\"))\n      .replace(\n        /^\\s*quote\\s*:\\s*/i,\n        \"\",\n      ).trim();\n  const jobNameRaw =\n    (contract.jobDetails?.jobNameByLang?.[lang] ?? contract.jobDetails?.jobName)\n      ?.trim();\n  const heroTitle = (jobNameRaw && jobNameRaw.length > 0)\n    ? jobNameRaw\n    : summary.replace(/\\b\\w/g, (c) => c.toUpperCase());\n\n  const effective = contract.effectiveDate ?? contract.createdAt;\n  const milestones = computeMilestones(total, contract.terms, lang);\n\n  // Sequential section numbers. Sections are conditionally rendered, so\n  // hardcoding 01/02/03 leaves gaps (01,03,…) when one is absent — which\n  // reads as a mistake. Each rendered SectionHeader pulls the next number\n  // in source order via short-circuit eval, so the sequence is always\n  // gapless regardless of which optional sections appear.\n  let sec = 0;\n  const num = () => String(++sec).padStart(2, \"0\");\n\n  return (\n    <>\n      {/* Sticky brand strip */}\n      <div class=\"ctr__no-print\" style={`text-align:center;margin-bottom:18px`}>\n        {contractor?.hasLogo\n          ? (\n            <img\n              src={`/api/public-logo/contract/${contract.id}`}\n              alt=\"\"\n              style=\"max-height:48px;max-width:160px;object-fit:contain;display:block;margin:0 auto 8px;border-radius:8px\"\n            />\n          )\n          : null}\n        <div\n          style={`font-size:11px;font-weight:800;letter-spacing:.18em;text-transform:uppercase;color:${TEAL}`}\n        >\n          {businessLabel}\n        </div>\n        {contractor?.addressLine\n          ? (\n            <div style={`margin-top:4px;font-size:12px;color:${MUTED}`}>\n              {contractor.addressLine}\n            </div>\n          )\n          : null}\n      </div>\n\n      <article\n        class=\"ctr\"\n        style={`background:${CREAM};border-radius:24px;box-shadow:0 14px 50px rgba(20,72,82,0.10);overflow:hidden;border:1px solid rgba(255,107,107,0.10)`}\n      >\n        {/* Pink ribbon header */}\n        <div\n          style={`height:8px;background:linear-gradient(90deg,${PINK} 0%,${PINK_DARK} 100%)`}\n        />\n\n        <div class=\"ctr__inner\" style=\"padding:36px 44px 40px\">\n          {/* Top row: doc tag + status pill */}\n          <div style=\"display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap\">\n            <div>\n              <span\n                style={`display:inline-block;background:rgba(20,72,82,0.10);color:${TEAL};font-size:11px;font-weight:800;letter-spacing:.18em;text-transform:uppercase;padding:6px 12px;border-radius:999px`}\n              >\n                {t.docTag} · #{contract.id.slice(0, 8).toUpperCase()}\n              </span>\n            </div>\n            {signed\n              ? (\n                <Pill\n                  bg=\"rgba(81,152,67,0.15)\"\n                  color={GREEN}\n                  label={t.signed(\n                    contract.signedAt ? fmtDate(contract.signedAt) : \"\",\n                  )}\n                />\n              )\n              : declined\n              ? (\n                <Pill\n                  bg=\"rgba(168,59,59,0.10)\"\n                  color=\"#a83b3b\"\n                  label={t.declined}\n                />\n              )\n              : (\n                <Pill\n                  bg=\"rgba(20,72,82,0.10)\"\n                  color={TEAL}\n                  label={t.awaiting}\n                />\n              )}\n          </div>\n\n          {/* Hero title */}\n          <h1\n            class=\"ctr__title\"\n            style={`margin:18px 0 0;font-family:'Helvetica Neue',Helvetica,Arial,sans-serif;font-weight:900;font-size:42px;letter-spacing:-0.025em;color:${TEAL};line-height:1.02`}\n          >\n            {heroTitle}\n          </h1>\n          {customerName && (\n            <div style={`margin-top:10px;color:${MUTED};font-size:14px`}>\n              {t.between}{\" \"}\n              <strong style={`color:${INK}`}>{businessLabel}</strong> {t.and}\n              {\" \"}\n              <strong style={`color:${INK}`}>{customerName}</strong>\n              {effective\n                ? (\n                  <>\n                    {\" \"}· {t.effective}{\" \"}\n                    <strong style={`color:${INK}`}>{fmtDate(effective)}</strong>\n                  </>\n                )\n                : null}\n            </div>\n          )}\n\n          {/* To / From block — auto-fit so the two cards stack on narrow\n              phones instead of squeezing into ~175px columns (which wrapped\n              the contractor's phone number mid-digit). Two-up on wider screens. */}\n          <section\n            style=\"margin-top:24px;display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:14px\"\n            class=\"ctr__tofrom\"\n          >\n            <PartyCard\n              role={t.to}\n              name={customerName}\n              email={contract.customer?.email}\n              phone={contract.customer?.phoneNumber}\n            />\n            <PartyCard\n              role={t.from}\n              name={contractorName}\n              businessName={contractor?.businessName?.trim()}\n              email={contractor?.email}\n              phone={contractor?.phoneNumber}\n              address={contractor?.addressLine}\n            />\n          </section>\n\n          {/* Job details */}\n          {items.length > 0 && (\n            <section style=\"margin-top:36px\">\n              <SectionHeader n={num()} title={t.jobDetails} />\n              {(() => {\n                const lines = detailLines(\n                  contract.jobDetails?.descriptionByLang?.[lang] ??\n                    contract.jobDetails?.description,\n                );\n                if (lines.length === 0) return null;\n                return lines.length > 1\n                  ? (\n                    <ul\n                      style={`margin:0;padding:0;list-style:none;color:${INK};font-size:15px;line-height:1.6`}\n                    >\n                      {lines.map((l, i) => (\n                        <li\n                          key={i}\n                          style=\"position:relative;padding:5px 0 5px 22px\"\n                        >\n                          <span\n                            style={`position:absolute;left:2px;top:13px;width:6px;height:6px;border-radius:50%;background:${GREEN}`}\n                          >\n                          </span>\n                          {l}\n                        </li>\n                      ))}\n                    </ul>\n                  )\n                  : (\n                    <p\n                      style={`margin:0;color:${INK};font-size:15px;line-height:1.6;white-space:pre-wrap`}\n                    >\n                      {lines[0]}\n                    </p>\n                  );\n              })()}\n              {\n                /* Line-item breakdown only for multi-line quotes — a single\n                  line just repeats the total card, and the Job details\n                  bullets above already describe the scope. */\n              }\n              {items.length > 1 && (\n                <table style=\"width:100%;border-collapse:collapse;margin-top:14px\">\n                  <thead>\n                    <tr>\n                      <th\n                        style={`padding:8px 0;font-size:11px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:${MUTED};border-bottom:1px solid ${LINE};text-align:left`}\n                      >\n                        {t.tableDescription}\n                      </th>\n                      {showQty && (\n                        <th\n                          style={`padding:8px 0;font-size:11px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:${MUTED};border-bottom:1px solid ${LINE};text-align:right`}\n                        >\n                          {t.tableQty}\n                        </th>\n                      )}\n                      <th\n                        style={`padding:8px 0;font-size:11px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:${MUTED};border-bottom:1px solid ${LINE};text-align:right`}\n                      >\n                        {t.tableAmount}\n                      </th>\n                    </tr>\n                  </thead>\n                  <tbody>\n                    {items.map((li, i) => {\n                      const lineTotal = (li.price ?? 0) * (li.quantity ?? 1);\n                      return (\n                        <tr key={i}>\n                          <td\n                            style={`padding:14px 0;border-bottom:1px solid ${LINE};color:${INK};font-size:15px;font-weight:600`}\n                          >\n                            {li.description}\n                          </td>\n                          {showQty && (\n                            <td\n                              style={`padding:14px 0;border-bottom:1px solid ${LINE};color:${MUTED};font-size:13px;text-align:right`}\n                            >\n                              {li.quantity ?? 1} {li.unit ?? t.unitEach}\n                            </td>\n                          )}\n                          <td\n                            style={`padding:14px 0;border-bottom:1px solid ${LINE};color:${INK};font-size:15px;font-weight:800;text-align:right;font-variant-numeric:tabular-nums`}\n                          >\n                            {fmtMoneyExact(lineTotal)}\n                          </td>\n                        </tr>\n                      );\n                    })}\n                  </tbody>\n                </table>\n              )}\n              {/* Total card — the money moment */}\n              <div\n                style={`margin-top:20px;background:linear-gradient(135deg,#e8f3e2 0%,#dceadb 100%);border:1px solid rgba(81,152,67,0.25);border-radius:16px;padding:22px 24px;display:flex;justify-content:space-between;align-items:center;gap:16px`}\n              >\n                <div>\n                  <div\n                    style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${GREEN}`}\n                  >\n                    {t.agreementValue}\n                  </div>\n                  <div style={`margin-top:4px;color:${MUTED};font-size:12px`}>\n                    {t.allIn}\n                  </div>\n                </div>\n                <div\n                  class=\"ctr__total-amt\"\n                  style={`font-family:'Helvetica Neue',Helvetica,Arial,sans-serif;font-weight:900;font-size:42px;letter-spacing:-0.03em;color:${TEAL};line-height:1;font-variant-numeric:tabular-nums`}\n                >\n                  {fmtMoneyExact(total)}\n                </div>\n              </div>\n            </section>\n          )}\n\n          {/* Payment milestones */}\n          {milestones.length > 0 && (\n            <section style=\"margin-top:36px\">\n              <SectionHeader n={num()} title={t.paymentSchedule} />\n              <div\n                class=\"ctr__milestones\"\n                style=\"display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:10px\"\n              >\n                {milestones.map((m, i) => (\n                  <div\n                    key={i}\n                    style={`background:#fff;border:1px solid ${LINE};border-radius:14px;padding:14px 16px`}\n                  >\n                    <div\n                      style={`font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n                    >\n                      {m.label}\n                    </div>\n                    <div\n                      style={`margin-top:6px;color:${TEAL};font-weight:900;font-size:20px;font-variant-numeric:tabular-nums`}\n                    >\n                      {fmtMoneyExact(m.amount)}\n                    </div>\n                    <div style={`margin-top:2px;color:${MUTED};font-size:12px`}>\n                      {m.when}\n                    </div>\n                  </div>\n                ))}\n              </div>\n            </section>\n          )}\n\n          {\n            /* Terms — the wizard-captured term grid (Start, completion,\n              payment, warranty) and the plain-English legal clauses, under a\n              single \"Terms\" header. The grid is conditional; the clauses are\n              always present, so this whole section always renders. Warranty\n              row is hidden when \"No warranty\" was chosen — clause 7 still\n              applies. */\n          }\n          {(() => {\n            const hasTermGrid =\n              !!(contract.startDate || contract.estimatedCompletionDate ||\n                (contract.terms && contract.terms.length > 0));\n            return (\n              <section style=\"margin-top:36px\">\n                <SectionHeader n={num()} title={t.terms} />\n                {hasTermGrid && (\n                  <div\n                    class=\"ctr__terms-grid\"\n                    style=\"display:grid;grid-template-columns:1fr 1fr;gap:12px\"\n                  >\n                    {contract.startDate && (\n                      <KV k={t.start} v={fmtDate(contract.startDate)} />\n                    )}\n                    {contract.estimatedCompletionDate && (\n                      <KV\n                        k={t.estCompletion}\n                        v={fmtDate(contract.estimatedCompletionDate)}\n                      />\n                    )}\n                    {(contract.terms ?? [])\n                      .filter((term) =>\n                        term.stepId !== \"customer\" && !isEmptyWarranty(term)\n                      )\n                      .map((term) => (\n                        <KV\n                          key={term.stepId}\n                          k={t.termLabels[term.stepId] ?? term.label}\n                          v={expandTermValue(term, contractor?.state, lang)}\n                        />\n                      ))}\n                  </div>\n                )}\n                {hasTermGrid && (\n                  <div\n                    style={`margin-top:22px;height:1px;background:${LINE}`}\n                  />\n                )}\n                <ol\n                  style={`margin:${\n                    hasTermGrid ? \"22px\" : \"0\"\n                  } 0 0;padding-left:20px;color:${INK};font-size:14px;line-height:1.65`}\n                >\n                  {t.clauses.map(([title, body]) => (\n                    <li key={title}>\n                      <strong>{title}.</strong> {body}\n                    </li>\n                  ))}\n                </ol>\n              </section>\n            );\n          })()}\n\n          {\n            /* Signature block — both cards render in both states; the\n              right card swaps from \"type your name below\" placeholder to\n              the customer's filled cursive name + date after signing. */\n          }\n          {!declined && (\n            <section style=\"margin-top:36px\">\n              <SectionHeader n={num()} title={t.signHere} />\n              <div\n                style={`margin:-4px 0 0;color:${MUTED};font-size:13px;line-height:1.5`}\n              >\n                {signed ? t.bothCaptured : t.bySigning(customerFirst)}\n              </div>\n              <div style=\"margin-top:18px;display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:18px;align-items:stretch\">\n                {/* Contractor card */}\n                <div\n                  style={`padding:14px 16px;background:#fff;border:1px solid ${LINE};border-radius:12px;min-height:96px;display:flex;flex-direction:column;justify-content:flex-end`}\n                >\n                  <div\n                    style={`font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n                  >\n                    {t.contractor}\n                  </div>\n                  <div\n                    style={`margin-top:2px;font-size:14px;font-weight:800;color:${INK}`}\n                  >\n                    {businessLabel}\n                  </div>\n                  <div\n                    style={`margin-top:6px;font-family:'Snell Roundhand','Brush Script MT',cursive;font-size:26px;color:${TEAL};line-height:1.1`}\n                  >\n                    {contractorName ?? businessLabel}\n                  </div>\n                  {contractorName && (\n                    <div style={`margin-top:6px;font-size:11px;color:${MUTED}`}>\n                      {t.by} {contractorName}\n                    </div>\n                  )}\n                  <div style={`margin-top:2px;font-size:11px;color:${MUTED}`}>\n                    {t.date} {effective ? fmtDate(effective) : t.today}\n                  </div>\n                </div>\n                {/* Customer card — placeholder OR filled */}\n                {signed\n                  ? (\n                    <div\n                      style={`padding:14px 16px;background:#fff;border:1px solid ${LINE};border-radius:12px;min-height:96px;display:flex;flex-direction:column;justify-content:flex-end`}\n                    >\n                      <div\n                        style={`font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n                      >\n                        {customerFirst\n                          ? t.signatureOf(customerName ?? customerFirst)\n                          : t.clientSignature}\n                      </div>\n                      <div\n                        style={`margin-top:6px;font-family:'Snell Roundhand','Brush Script MT',cursive;font-size:26px;color:${TEAL};line-height:1.1`}\n                      >\n                        {contract.customerSignedName ?? customerName ?? \"—\"}\n                      </div>\n                      <div\n                        style={`margin-top:4px;font-size:11px;color:${MUTED}`}\n                      >\n                        {contract.signedAt\n                          ? fmtDate(contract.signedAt)\n                          : t.today}\n                      </div>\n                    </div>\n                  )\n                  /* Unsigned: \"YOUR Signature\" card (roadmap slide 16) —\n                     mirrors the contractor card and points the customer at\n                     the actual sign pad (PublicSignContract) right below.\n                     It deliberately has no dashed pad styling so it doesn't\n                     read as a second signature box. */\n                  : (\n                    <div\n                      style={`padding:14px 16px;background:#fff;border:1px solid ${LINE};border-radius:12px;min-height:96px;display:flex;flex-direction:column;justify-content:flex-end`}\n                    >\n                      <div\n                        style={`font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n                      >\n                        {t.yourSignature}\n                      </div>\n                      <div\n                        style={`margin-top:2px;font-size:14px;font-weight:800;color:${INK}`}\n                      >\n                        {customerName ?? t.clientSignature}\n                      </div>\n                      <div\n                        style={`margin-top:6px;flex:1;display:flex;align-items:flex-end`}\n                      >\n                        <div\n                          style={`width:100%;border-bottom:1.5px dashed ${LINE}`}\n                        />\n                      </div>\n                      <div\n                        style={`margin-top:8px;font-size:12px;font-weight:700;color:${TEAL}`}\n                      >\n                        {t.signTypeBelow}\n                      </div>\n                    </div>\n                  )}\n              </div>\n              {!signed && (\n                <PublicSignContract contractId={contract.id} lang={lang} />\n              )}\n              {signed && (\n                <div\n                  style={`margin-top:22px;padding:18px 22px;background:linear-gradient(135deg,rgba(81,152,67,0.12) 0%,rgba(81,152,67,0.04) 100%);border:1px solid rgba(72,158,95,0.35);border-radius:16px;display:flex;align-items:center;gap:14px`}\n                >\n                  <div\n                    style={`width:40px;height:40px;border-radius:50%;background:${GREEN};color:#fff;display:flex;align-items:center;justify-content:center;flex-shrink:0`}\n                  >\n                    <svg\n                      width=\"20\"\n                      height=\"20\"\n                      viewBox=\"0 0 24 24\"\n                      fill=\"none\"\n                      stroke=\"currentColor\"\n                      stroke-width=\"3\"\n                      stroke-linecap=\"round\"\n                      stroke-linejoin=\"round\"\n                    >\n                      <path d=\"M20 6 9 17l-5-5\" />\n                    </svg>\n                  </div>\n                  <div>\n                    <div\n                      style={`font-weight:800;color:${GREEN};font-size:16px`}\n                    >\n                      {t.signedBinding}\n                    </div>\n                    <div style={`margin-top:2px;color:${MUTED};font-size:13px`}>\n                      {t.signedNote}\n                    </div>\n                  </div>\n                </div>\n              )}\n            </section>\n          )}\n\n          {/* Contact card */}\n          {(contractor?.phoneNumber || contractor?.email) && (\n            <footer\n              style={`margin-top:36px;padding-top:22px;border-top:1px solid ${LINE};display:flex;align-items:flex-start;gap:14px;flex-wrap:wrap`}\n            >\n              <div\n                style={`width:44px;height:44px;border-radius:50%;background:linear-gradient(135deg,${GREEN} 0%,#71a85f 100%);color:#fff;font-weight:800;font-size:14px;display:flex;align-items:center;justify-content:center;letter-spacing:.04em;flex-shrink:0`}\n              >\n                {senderInitials}\n              </div>\n              <div style=\"min-width:0;flex:1\">\n                <div style={`color:${INK};font-size:14px;line-height:1.5`}>\n                  {t.qBefore} {contractor?.phoneNumber && (\n                    <>\n                      {t.callWord}{\" \"}\n                      <a\n                        href={telHref(contractor.phoneNumber)}\n                        style={`color:${TEAL};text-decoration:none;font-weight:700;white-space:nowrap`}\n                      >\n                        {fmtPhone(contractor.phoneNumber)}\n                      </a>\n                    </>\n                  )}\n                  {contractor?.phoneNumber && contractor?.email ? t.orWord : \"\"}\n                  {contractor?.email && (\n                    <>\n                      {t.emailWord}{\" \"}\n                      <a\n                        href={`mailto:${contractor.email}`}\n                        style={`color:${TEAL};text-decoration:none;font-weight:700`}\n                      >\n                        {contractor.email}\n                      </a>\n                    </>\n                  )}\n                  {t.lookForward}\n                </div>\n              </div>\n            </footer>\n          )}\n        </div>\n      </article>\n      <div\n        style={`display:flex;align-items:center;justify-content:center;gap:8px;margin-top:18px;font-size:11px;color:#a8b2b3;letter-spacing:.04em`}\n      >\n        <img\n          src=\"/logo.png\"\n          alt=\"\"\n          height=\"16\"\n          style=\"height:16px;width:auto;opacity:0.7;display:block\"\n        />\n        {t.poweredBy} #{contract.id.slice(0, 8).toUpperCase()}\n      </div>\n    </>\n  );\n}\n\nfunction Pill(\n  { bg, color, label }: { bg: string; color: string; label: string },\n) {\n  return (\n    <span\n      style={`background:${bg};color:${color};font-weight:800;font-size:11px;letter-spacing:.12em;text-transform:uppercase;padding:7px 14px;border-radius:999px;flex-shrink:0`}\n    >\n      {label}\n    </span>\n  );\n}\n\n/** One consistent section header across the whole document: a teal numbered\n *  badge + an uppercase letter-spaced title + a full-width hairline rule.\n *  The header owns the spacing below it, so section content sits flush\n *  under the rule (content margin-top:0). */\nfunction SectionHeader({ n, title }: { n: string; title: string }) {\n  return (\n    <div style=\"margin-bottom:16px\">\n      <div style=\"display:flex;align-items:center;gap:11px\">\n        <span\n          style={`display:inline-flex;align-items:center;justify-content:center;width:25px;height:25px;border-radius:50%;background:${TEAL};color:#fff;font-weight:800;font-size:11px;flex-shrink:0;font-variant-numeric:tabular-nums`}\n        >\n          {n}\n        </span>\n        <div\n          style={`font-family:'Helvetica Neue',Helvetica,Arial,sans-serif;font-weight:800;color:${TEAL};font-size:13px;letter-spacing:.14em;text-transform:uppercase`}\n        >\n          {title}\n        </div>\n      </div>\n      <div style={`margin-top:12px;height:1px;background:${LINE}`} />\n    </div>\n  );\n}\n\nfunction KV({ k, v }: { k: string; v: string }) {\n  return (\n    <div\n      style={`background:#fff;border:1px solid ${LINE};border-radius:12px;padding:12px 14px`}\n    >\n      <div\n        style={`font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n      >\n        {k}\n      </div>\n      <div\n        style={`margin-top:5px;color:${INK};font-weight:700;font-size:14px;line-height:1.35`}\n      >\n        {v}\n      </div>\n    </div>\n  );\n}\n\nfunction PartyCard(props: {\n  role: string;\n  name?: string;\n  businessName?: string;\n  email?: string;\n  phone?: string;\n  address?: string;\n}) {\n  const displayName = props.name?.trim();\n  const biz = props.businessName?.trim();\n  return (\n    <div\n      style={`background:#fff;border:1px solid ${LINE};border-radius:12px;padding:14px 16px`}\n    >\n      <div\n        style={`font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n      >\n        {props.role}\n      </div>\n      <div\n        style={`margin-top:6px;color:${INK};font-weight:800;font-size:15px;line-height:1.25`}\n      >\n        {displayName ?? \"—\"}\n      </div>\n      {biz && biz !== displayName && (\n        <div\n          style={`margin-top:2px;color:${MUTED};font-size:12.5px;line-height:1.3`}\n        >\n          {biz}\n        </div>\n      )}\n      {props.phone && (\n        <div style={`margin-top:4px;font-size:12.5px;line-height:1.35`}>\n          <a\n            href={telHref(props.phone)}\n            style={`color:${TEAL};text-decoration:none;font-weight:600;white-space:nowrap`}\n          >\n            {fmtPhone(props.phone)}\n          </a>\n        </div>\n      )}\n      {props.email && (\n        <div style={`margin-top:2px;font-size:12.5px;line-height:1.35`}>\n          <a\n            href={`mailto:${props.email}`}\n            style={`color:${TEAL};text-decoration:none;font-weight:600`}\n          >\n            {props.email}\n          </a>\n        </div>\n      )}\n      {props.address && (\n        <div\n          style={`margin-top:4px;color:${MUTED};font-size:12px;line-height:1.35`}\n        >\n          {props.address}\n        </div>\n      )}\n    </div>\n  );\n}\n\nfunction sumLineTotals(items: LineItem[] | undefined): number {\n  if (!items) return 0;\n  return items.reduce((s, li) => s + (li.price ?? 0) * (li.quantity ?? 1), 0);\n}\n\nfunction initialsFromName(name?: string): string {\n  if (!name) return \"PM\";\n  const parts = name.trim().split(/\\s+/).filter(Boolean);\n  if (parts.length >= 2) {\n    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();\n  }\n  return (parts[0] ?? \"PM\").slice(0, 2).toUpperCase();\n}\n\nfunction fmtDate(iso: string): string {\n  const isDateOnly = /^\\d{4}-\\d{2}-\\d{2}$/.test(iso);\n  const d = new Date(isDateOnly ? `${iso}T12:00:00Z` : iso);\n  if (Number.isNaN(+d)) return iso;\n  return d.toLocaleDateString(\"en-US\", {\n    month: \"long\",\n    day: \"numeric\",\n    year: \"numeric\",\n    timeZone: \"UTC\",\n  });\n}\n\nfunction termValue(\n  terms: Term[] | undefined,\n  stepId: string,\n): string | undefined {\n  return terms?.find((t) => t.stepId === stepId)?.value;\n}\n\n/** Expand wizard-shorthand answers into customer-readable sentences.\n *  E.g. \"Use my business state\" → \"California\". \"Yes\" / \"No\" for state\n *  notices → a sentence the customer can actually act on. */\n/** Localize a wizard-captured term value for the customer copy. Maps the\n *  known preset option labels to Spanish and converts numeric durations\n *  (\"12 months\" → \"12 meses\"); custom free-text and universal ratios\n *  (\"50/50\") pass through unchanged. Mirrors the backend PDF renderer. */\nfunction expandTermValue(\n  term: Term,\n  contractorState: string | undefined,\n  lang: Lang = \"en\",\n): string {\n  const stateName = expandStateName(contractorState);\n  if (term.stepId === \"wraps\") {\n    const v = localizeTermValue(term.value, lang);\n    return tFor(lang, \"contractDoc.estimated\", { value: v });\n  }\n  if (term.stepId === \"governing_state\") {\n    if (/use my business|business state/i.test(term.value)) {\n      if (!stateName) return term.value;\n      return tFor(lang, \"contractDoc.termValue.stateLaw\", { state: stateName });\n    }\n    if (/job\\s*site|use the job/i.test(term.value)) {\n      return tFor(lang, \"contractDoc.termValue.jobSiteState\");\n    }\n    // User picked a specific state (\"Pick a different state — TX\")\n    return expandStateName(term.value) ?? term.value;\n  }\n  if (term.stepId === \"state_notices\") {\n    const v = term.value.trim().toLowerCase();\n    if (v === \"yes\") {\n      return stateName\n        ? tFor(lang, \"contractDoc.termValue.stateNoticesYesState\", {\n          state: stateName,\n        })\n        : tFor(lang, \"contractDoc.termValue.stateNoticesYes\");\n    }\n    if (v === \"no\") {\n      return tFor(lang, \"contractDoc.termValue.stateNoticesNo\");\n    }\n    if (v.startsWith(\"review\")) {\n      return tFor(lang, \"contractDoc.termValue.stateNoticesReview\");\n    }\n    return term.value;\n  }\n  return localizeTermValue(term.value, lang);\n}\n\n/** Hide warranty term row when the contractor selected \"No warranty\" — the\n *  legal-text warranty clause in the Fine Print still applies. The warranty\n *  step id from the wizard is `warranty`; some users type \"none\" / \"n/a\". */\nfunction isEmptyWarranty(term: Term): boolean {\n  if (term.stepId !== \"warranty\") return false;\n  const v = term.value.trim().toLowerCase();\n  return v === \"\" || v === \"no warranty\" || v === \"none\" || v === \"n/a\" ||\n    v === \"no\";\n}\n\nfunction computeMilestones(\n  total: number,\n  terms: Term[] | undefined,\n  lang: Lang = \"en\",\n): { label: string; amount: number; when: string }[] {\n  if (!total || total <= 0) return [];\n  const L = {\n    deposit: tFor(lang, \"contractDoc.milestone.deposit\"),\n    balance: tFor(lang, \"contractDoc.milestone.balance\"),\n    midpoint: tFor(lang, \"contractDoc.milestone.midpoint\"),\n    final: tFor(lang, \"contractDoc.milestone.final\"),\n    beforeStart: tFor(lang, \"contractDoc.milestone.beforeStart\"),\n    onCompletion: tFor(lang, \"contractDoc.milestone.onCompletion\"),\n    atMidpoint: tFor(lang, \"contractDoc.milestone.atMidpoint\"),\n    onSigning: tFor(lang, \"contractDoc.milestone.onSigning\"),\n  };\n  const roleLabel: Record<MilestoneRole, { label: string; when: string }> = {\n    deposit: { label: L.deposit, when: L.beforeStart },\n    midpoint: { label: L.midpoint, when: L.atMidpoint },\n    milestone: { label: L.midpoint, when: L.atMidpoint },\n    completion: { label: L.balance, when: L.onCompletion },\n    full: { label: L.final, when: L.onCompletion },\n  };\n  const term = termValue(terms, \"payment_terms\");\n  // \"Due Now\" terms collapse to a single full payment — but it's owed at\n  // signing, not \"on completion\".\n  const dueNow = /\\bdue now\\b/i.test(term ?? \"\");\n  return computePaymentSplit(term, total).map((\n    p,\n  ) => ({\n    ...roleLabel[p.role],\n    ...(dueNow && p.role === \"full\" ? { when: L.onSigning } : {}),\n    amount: p.amountCents,\n  }));\n}\n", "sourceName": "contract-doc.tsx", "checklist": "# Capture checklist — contract-doc (ContractDoc)\n\n**Theme:** light only (public document palette).\n**Backend:** REQUIRED — needs a real signed/unsigned contract id. The page is\nauth-FREE (customer link), so a valid `/c/:id` URL is all you need; obtain one\nby sending a contract from the authed app (assistant flow) and copying the\nshort link, or hit `/contracts/:id/public` to confirm a live id.\n**NO fabricated screenshots** — only shoot against a real backend response.\n\n## Route / URL\n- `http://localhost:5280/c/<contractId>` — the doc renders inside\n  `PublicContractView` after its client fetch resolves.\n- The doc is the `<article class=\"ctr\">` plus its brand strip + powered-by\n  strip.\n\n## Viewports (mobile-first)\n- **~390px** (primary phone) — verify the route's 720px overrides fire:\n  `.ctr__inner` padding 28/22, `.ctr__title` 30px, `.ctr__total-amt` 34px,\n  Terms grid 1-col, milestones 2-up; To/From + signature cards stack.\n- **640px** — mid tablet.\n- (Optional 760px to see the full max-width column.)\n\n## Element(s) to crop\n1. Full document top: brand strip → ribbon → doc-tag + status pill → hero\n   title + \"between … and …\" line.\n2. To/From party cards (auto-fit two-up vs stacked at 390).\n3. Job details: bullet list + line-item table + green total card (\"the money\n   moment\").\n4. Payment schedule milestone cards.\n5. Terms: wizard KV grid + numbered legal clause list.\n6. Signature block (see transient states).\n7. Contact footer (initials avatar) + powered-by strip.\n\n## Transient states to drive (each is a different contract id/status)\n1. **unsigned** — teal \"Awaiting your signature\" pill; \"YOUR signature\" dashed\n   card; `PublicSignContract` pad mounted below.\n2. **signed** — green \"Signed {date}\" pill; filled cursive customer signature\n   card; green \"Signed and binding\" band; NO pad.\n3. **declined** — red \"Declined\" pill; signature section entirely absent.\n4. **es** — a contract whose `contractor.commsLanguage='es'`: whole doc in\n   Spanish (docTag \"Cotización y Acuerdo\", \"Esperando tu firma\", clause\n   titles, milestone labels).\n5. **no-line-items / single-line-item** — confirm the line-item table is\n   suppressed and section numbers stay gapless (01,02…).\n\n## Motion to film\n- None in the doc. (Sign-pad motion is filmed under public-sign-contract.)\n\n## Print\n- `Cmd-P` / print preview at desktop width: `body` white, brand strip\n  (`.ctr__no-print`) hidden, card shadow removed + 1px LINE border. One frame.\n"}, {"name": "dash-sidebar", "component": "DashSidebar", "spec": "# DashSidebar\n\nApp-shell left navigation rail. First child of `<div class=\"app\">`. Renders on\nall 11 authenticated routes.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (file lives in `islands/DashSidebar.tsx`).\n- **Interaction tier:** `island` (client-only state) + cross-island event bus.\n- **Client state owned:**\n  - `collapsed: boolean` — desktop rail width toggle, persisted to\n    `localStorage[\"pm:sb-collapsed\"]` (`\"1\"`/`\"0\"`), seeded from it on mount.\n  - `mobileOpen: boolean` — mobile drawer open/closed (not persisted).\n  - `s: SbState` — `{ counts, identity, lang, superAdmin }` projected from the\n    shared dashboard cache.\n- **Data source per region:**\n  - Counts (clients/quotes/contracts/invoices badges), identity (display name,\n    business, initials), language, superAdmin flag → **`lib/dash-cache.ts`**\n    (`readCached()` seeds first paint synchronously; `refreshDash()` fetches;\n    `subscribeDash()` keeps it in lockstep with sibling islands). The cache is\n    fed by `clients/dashboard.ts` (`/analytics/dashboard` + profile). Counts use\n    *active* totals only: `clients=customers`, `quotes=quotes.sent`,\n    `contracts=contracts.signed`, `invoices=invoices.pending`.\n  - Nav entries are a hard-coded `NAV` array (home/clients/quotes/contracts/\n    invoices/payments) + a super-admin-only Admin entry.\n- **Honest-empty:** a badge is omitted unless `count != null && count > 0` — a\n  fresh account shows no pills (deliberate; an empty pill \"looks broken\").\n  `identity` block is omitted entirely until a name or business exists.\n- **Server mutations:**\n  - Logout → `POST /api/auth/logout` then `globalThis.location.href = \"/\"`. This\n    is a logout *navigation* (cookie cleared), not a refresh-after-mutation, so\n    the full nav is acceptable; flash feedback is N/A.\n- **Cross-island coupling (ANTI-PATTERN, minor):** listens for a global\n  `pm:sb-toggle` CustomEvent (dispatched by `DashTopbar`'s hamburger) to drive\n  collapse (desktop, `innerWidth >= 641`) or the drawer (mobile). This is an\n  event-bus instead of a shared signal — fragile if either island fails to\n  hydrate. Fix: lift `collapsed`/`mobileOpen` into a shared `@preact/signals`\n  store both islands import, removing the string-keyed window event.\n- **Liveness:** request-response. No websocket, no polling here (counts refresh\n  is push-driven via `subscribeDash`, plus one `refreshDash()` on mount).\n- **Reactivity:** reads `langSignal.value` so the whole rail re-localizes\n  instantly when Settings flips language.\n- **Data-shape hazards:**\n  - `projectSidebar` tolerates `snap === null` (returns `INITIAL_STATE`).\n  - Initials derivation: single-word name → first 2 chars upper; else first +\n    last initial; falls back to `\"•\"`.\n  - `showNav={false}` (accounts-manager view) renders the rail chrome with **no\n    nav tabs** — counts still fetch but nothing badges.\n\n## 2. Anatomy\n```\n<>  (fragment)\n  {mobileOpen && <div class=\"sb-backdrop\" onClick=close aria-hidden>}    ← mobile only\n  <aside class=\"sb [sb--collapsed] [sb--open]\">\n    <div class=\"sb__inner\">\n      <a class=\"sb__brand\" href=\"/dashboard\">                            ← logo img + brand name\n        <div class=\"sb__brand-logo\"><img src=\"/logo-monster-card.png\"/></div>\n        <div class=\"sb__brand-text\"><div class=\"sb__brand-name\">{brand.name}</div></div>\n      </a>\n      <a class=\"sb__textus\" href=\"/assistant\">                           ← \"My Assistant\" CTA\n        <div class=\"sb__textus-icon\"><I crown/></div>\n        <div class=\"sb__textus-text\"><span>{nav.assistant}</span></div>\n      </a>\n      <div class=\"sb__divider\" />\n      {showNav && <nav class=\"sb__nav\">                                  ← NAV.map → .nav-item\n        a.nav-item[.nav-item--active] × 6 (icon + label + optional count pill)\n        {superAdmin && a.nav-item → /admin (shield, \"Admin\", not i18n'd)}\n      </nav>}\n      <div class=\"sb__bottom\">\n        {identity && <a class=\"sb__footer\" href=\"/settings\">}            ← avatar initials + name/biz + chevron (INLINE-styled)\n        <div class=\"sb__bottom-head\"><button class=\"sb__toggle\">         ← collapse/expand (hamburger+arrow glyph)\n        <button class=\"nav-item nav-item--logout\" onClick=logout>        ← logout\n      </div>\n    </div>\n  </aside>\n```\n- **Slots/children:** none (no `children`); fully self-composed.\n- **Icon dependency:** glyphs come from `lib/dash-icons.tsx` (`I` renderer +\n  `ICN` map). Copied into `js/dash-icons.tsx`. Uses `ICN`:\n  `home,user,quote,contract,invoice,pay` (nav), `crown` (assistant),\n  `chev` (footer), `logout`, `shield` (admin); plus an inline hamburger+arrow\n  `<path>` for the toggle.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `active` | string (`home\\|clients\\|quotes\\|contracts\\|invoices\\|payments\\|admin\\|messages`) | `\"home\"` | select | no |\n| `showNav` | boolean | `true` | boolean | no |\n\n(Both are SSR props passed by the route. All other display data comes from the\nclient cache, not props.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | expanded rail, `active=\"home\"`, counts present, identity present | `cases/default/default.json` |\n| collapsed | `sb--collapsed` (84px rail, labels/counts/text hidden) | `cases/collapsed/collapsed.json` |\n| active-quotes | `active=\"quotes\"` highlights Quotes (green pill) | `cases/active-quotes/active-quotes.json` |\n| empty | fresh account: no counts, no identity footer | `cases/empty/empty.json` |\n| super-admin | Admin tab visible | `cases/super-admin/super-admin.json` |\n| mobile-open | `<=640px`, drawer slid in + backdrop | `cases/mobile-open/mobile-open.json` |\n| no-nav | `showNav={false}` (accounts-manager chrome only) | `cases/no-nav/no-nav.json` |\n\nState is internal (`_signals`/`_innerHtml` not applicable to useState islands in\nisolate — model via `localStorage` seed + viewport for collapsed/mobile, and via\nmocked cache projection for counts/identity). Cases below set the props plus a\n`_signals` hint for the projected `SbState` the harness should stub.\n\n## 5. Events\n- `ev.expect(e => e.source === \"a.sb__brand\" && e.type === \"click\")` → nav `/dashboard`.\n- `ev.expect(e => e.source === \"a.sb__textus\" && e.type === \"click\")` → nav `/assistant`.\n- `ev.expect(e => e.source === \"a.nav-item\" && e.type === \"click\")` → nav to item.href.\n- `ev.expect(e => e.source === \"button.sb__toggle\" && e.type === \"click\")` →\n  toggles `collapsed`, writes `localStorage[\"pm:sb-collapsed\"]`.\n- `ev.expect(e => e.source === \"button.nav-item--logout\" && e.type === \"click\")`\n  → POST `/api/auth/logout`, then redirect `/`.\n- `ev.expect(e => e.source === \"div.sb-backdrop\" && e.type === \"click\")` → `mobileOpen=false`.\n- External: `globalThis` `pm:sb-toggle` (received) →\n  `innerWidth>=641 ? toggle() : setMobileOpen(o=>!o)`.\n\n## 6. Motion (extracted)\n- **Rail width:** `transition: width 280ms cubic-bezier(.34,1.56,.64,1)`\n  (bouncy ease) on `.sb` when toggling collapsed. **Jank finding:** animating\n  `width` triggers layout on every frame; on slow devices the nav text reflows.\n  Fix: keep `width` (rail must reserve real space) but `content-visibility`/\n  `will-change: width` to hint compositing; labels already `display:none` on\n  collapse so no clip-fade.\n- **Toggle glyph:** `transform: rotate(180deg)` over `220ms` bounce when\n  collapsed — chevron points right.\n- **Assistant CTA hover:** `translateY(-2px) + brightness(1.05)` over `200ms` bounce.\n- **`pm-assistant-shake` keyframes** exist (a periodic nudge) but **no selector\n  currently assigns the animation** — the CTA does not shake at rest. Dead-ish\n  keyframe; hover/focus + reduced-motion already null it.\n- **Mobile drawer:** `transform: translateX(-100%→0)` over `240ms` bounce;\n  backdrop `sbFadeIn` opacity `0→1` over `180ms` ease-out.\n- **Reduced motion:** `.sb__textus { animation: none }` under\n  `@media (prefers-reduced-motion: reduce)`. The width/transform transitions are\n  additionally clamped to `0.01ms` by the global tokens reduced-motion block.\n\n## 7. Responsive (this component's own breakpoints)\n- **Breakpoint is 640px / 641px** — NOT the product-default 720px. Verify here.\n- `> 640px` (desktop): static flex rail, 216px expanded / 84px collapsed.\n- `<= 640px`: `position:fixed`, width 260px, translated off-canvas\n  (`translateX(-100%)`); `.sb--open` slides it in; `.sb-backdrop` scrim appears.\n  The parent `.app` switches to block/body-scroll on mobile (see app-shell).\n\n## 8. A11y\n- `<aside>` landmark; nav items are real `<a>`; toggle + logout are `<button>`.\n- Toggle has dynamic `aria-label` (`sidebar.expand`/`sidebar.collapse`) +\n  `title`. Backdrop is `aria-hidden`. Brand/assistant links have `title`.\n- Footer avatar span is `aria-hidden` (initials are decorative; name follows).\n- **Gaps:** the mobile drawer has no focus trap and no `Esc`-to-close; opening it\n  doesn't move focus into the drawer. Active item relies on color/weight only —\n  no `aria-current=\"page\"` (add it for SR users).\n- Reduced motion handled (see Motion).\n\n## 9. Used on\nShared across **11 authenticated routes** (all import `islands/DashSidebar.tsx`):\n`/dashboard`, `/clients`, `/quotes`, `/contracts`, `/invoices`, `/payments`,\n`/settings`, `/assistant` (+ `/assistant/[threadId]`, `active=\"messages\"`),\n`/admin`, `/accounts-manager` (`showNav={false}`). Evidence: grep of `DashSidebar`\nimport across `routes/`. Shared — single source of app navigation.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"dash-sidebar\",\n  \"controls\": {\n    \"active\": {\n      \"type\": \"select\",\n      \"value\": \"home\",\n      \"options\": [\"home\", \"clients\", \"quotes\", \"contracts\", \"invoices\", \"payments\", \"admin\", \"messages\"]\n    },\n    \"showNav\": { \"type\": \"boolean\", \"value\": true }\n  }\n}\n", "cases": [{"name": "active-quotes.json", "content": "{\n  \"_name\": \"Quotes active\",\n  \"active\": \"quotes\",\n  \"showNav\": true,\n  \"_signals\": {\n    \"collapsed\": false,\n    \"mobileOpen\": false,\n    \"s\": {\n      \"counts\": { \"clients\": 8, \"quotes\": 3, \"contracts\": 2, \"invoices\": 4 },\n      \"identity\": { \"display\": \"Raphael Castro\", \"biz\": \"RC Incorporated\", \"initials\": \"RC\" },\n      \"lang\": \"en\",\n      \"superAdmin\": false\n    }\n  }\n}\n"}, {"name": "collapsed.json", "content": "{\n  \"_name\": \"Collapsed rail (84px)\",\n  \"active\": \"home\",\n  \"showNav\": true,\n  \"_signals\": {\n    \"collapsed\": true,\n    \"mobileOpen\": false,\n    \"s\": {\n      \"counts\": { \"clients\": 8, \"quotes\": 3, \"contracts\": 2, \"invoices\": 4 },\n      \"identity\": { \"display\": \"Raphael Castro\", \"biz\": \"RC Incorporated\", \"initials\": \"RC\" },\n      \"lang\": \"en\",\n      \"superAdmin\": false\n    }\n  }\n}\n"}, {"name": "default.json", "content": "{\n  \"_name\": \"Default — expanded, Dashboard active\",\n  \"active\": \"home\",\n  \"showNav\": true,\n  \"_signals\": {\n    \"collapsed\": false,\n    \"mobileOpen\": false,\n    \"s\": {\n      \"counts\": { \"clients\": 8, \"quotes\": 3, \"contracts\": 2, \"invoices\": 4 },\n      \"identity\": { \"display\": \"Raphael Castro\", \"biz\": \"RC Incorporated\", \"initials\": \"RC\" },\n      \"lang\": \"en\",\n      \"superAdmin\": false\n    }\n  }\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Fresh account — no badges, no identity footer\",\n  \"active\": \"home\",\n  \"showNav\": true,\n  \"_signals\": {\n    \"collapsed\": false,\n    \"mobileOpen\": false,\n    \"s\": { \"counts\": {}, \"identity\": null, \"lang\": \"en\", \"superAdmin\": false }\n  }\n}\n"}, {"name": "mobile-open.json", "content": "{\n  \"_name\": \"Mobile drawer open (<=640px) + backdrop\",\n  \"active\": \"home\",\n  \"showNav\": true,\n  \"_viewport\": { \"width\": 390, \"height\": 844 },\n  \"_signals\": {\n    \"collapsed\": false,\n    \"mobileOpen\": true,\n    \"s\": {\n      \"counts\": { \"clients\": 8, \"quotes\": 3, \"contracts\": 2, \"invoices\": 4 },\n      \"identity\": { \"display\": \"Raphael Castro\", \"biz\": \"RC Incorporated\", \"initials\": \"RC\" },\n      \"lang\": \"en\",\n      \"superAdmin\": false\n    }\n  }\n}\n"}, {"name": "no-nav.json", "content": "{\n  \"_name\": \"showNav=false — accounts-manager chrome (no tabs)\",\n  \"active\": \"home\",\n  \"showNav\": false,\n  \"_signals\": {\n    \"collapsed\": false,\n    \"mobileOpen\": false,\n    \"s\": {\n      \"counts\": {},\n      \"identity\": { \"display\": \"Raphael Castro\", \"biz\": \"RC Incorporated\", \"initials\": \"RC\" },\n      \"lang\": \"en\",\n      \"superAdmin\": true\n    }\n  }\n}\n"}, {"name": "super-admin.json", "content": "{\n  \"_name\": \"Super-admin — Admin tab visible\",\n  \"active\": \"admin\",\n  \"showNav\": true,\n  \"_signals\": {\n    \"collapsed\": false,\n    \"mobileOpen\": false,\n    \"s\": {\n      \"counts\": { \"clients\": 8, \"quotes\": 3, \"contracts\": 2, \"invoices\": 4 },\n      \"identity\": { \"display\": \"Raphael Castro\", \"biz\": \"RC Incorporated\", \"initials\": \"RC\" },\n      \"lang\": \"en\",\n      \"superAdmin\": true\n    }\n  }\n}\n"}], "css": "/* ============================================================\n   DashSidebar — extracted from static/dashboard.css\n   App-shell context (the sidebar lives as the first child of `.app`).\n   Tokens referenced: --brand-teal, --brand-green, --font-heading,\n   --font-body. See ui-breakdown/design-tokens.md.\n   ============================================================ */\n\n/* --- shell parent (context only; owned by app-shell, not this island) --- */\n.app {\n  height: 100vh;\n  height: var(--app-vh, 100dvh); /* MobileViewport mirrors visualViewport.height */\n  display: flex;\n  min-height: 0;\n  overflow: hidden;\n}\n\n/* ------ Sidebar rail ------ */\n.sb {\n  background: var(--brand-teal);\n  color: #fff;\n  flex-shrink: 0;\n  width: 216px;\n  transition: width 280ms cubic-bezier(0.34, 1.56, 0.64, 1);\n  display: flex;\n  flex-direction: column;\n  position: relative;\n  overflow: hidden;\n}\n.sb--collapsed {\n  width: 84px;\n}\n.sb::before { /* ambient green+pink corner glow */\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background:\n    radial-gradient(circle at 20% 90%, rgba(81, 152, 67, 0.25) 0%, transparent 40%),\n    radial-gradient(circle at 100% 10%, rgba(255, 107, 107, 0.15) 0%, transparent 40%);\n  pointer-events: none;\n}\n.sb__inner {\n  position: relative;\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  padding: 22px 16px 16px;\n  gap: 14px;\n}\n\n/* ------ Brand ------ */\n.sb__brand {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 0 4px;\n  height: 44px;\n  min-width: 0;\n}\n.sb__brand-text { min-width: 0; flex: 1; overflow: hidden; }\n.sb__brand-logo {\n  width: 36px;\n  height: 36px;\n  flex-shrink: 0;\n  background: var(--brand-green);\n  border-radius: 11px;\n  display: grid;\n  place-items: center;\n  box-shadow:\n    0 8px 24px -4px rgba(81, 152, 67, 0.55),\n    0 0 40px 4px rgba(81, 152, 67, 0.32);\n  transform: rotate(-4deg);\n}\n.sb__brand-name {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  color: #fff;\n  letter-spacing: -0.02em;\n  line-height: 1.05;\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n.sb__brand-sub { /* NOTE: declared in CSS but never rendered by DashSidebar.tsx */\n  font-size: 10px;\n  color: rgba(255, 255, 255, 0.55);\n  margin-top: 2px;\n  font-weight: 600;\n  letter-spacing: 0.04em;\n  text-transform: uppercase;\n  white-space: nowrap;\n}\n.sb--collapsed .sb__brand-text { display: none; }\n.sb--collapsed .sb__brand { justify-content: center; padding: 0; }\n\n/* ------ Assistant CTA (\"My Assistant\") ------ */\n.sb__textus {\n  background: rgba(255, 255, 255, 0.12);\n  color: #fff;\n  border: none;\n  border-radius: 14px;\n  padding: 10px 12px;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  cursor: pointer;\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  width: 100%;\n  box-shadow: none;\n  transition: transform 200ms cubic-bezier(0.34, 1.56, 0.64, 1), filter 200ms;\n  position: relative;\n  overflow: hidden;\n}\n.sb__textus:hover { transform: translateY(-2px); filter: brightness(1.05); }\n/* Subtle attention shake — defined but only applied if an `animation:` rule\n   referencing it is added; currently no selector assigns pm-assistant-shake,\n   so the CTA does NOT shake at rest (hover/focus already cancel it). */\n@keyframes pm-assistant-shake {\n  0%, 88%, 100% { transform: translate(0, 0) rotate(0); }\n  90% { transform: translate(-3px, 0) rotate(-0.6deg); }\n  92% { transform: translate(3px, 0) rotate(0.6deg); }\n  94% { transform: translate(-2px, 0) rotate(-0.4deg); }\n  96% { transform: translate(2px, 0) rotate(0.4deg); }\n  98% { transform: translate(-1px, 0) rotate(0); }\n}\n.sb__textus:hover, .sb__textus:focus-visible { animation: none; }\n@media (prefers-reduced-motion: reduce) {\n  .sb__textus { animation: none; }\n}\n.sb__textus-icon {\n  width: 32px;\n  height: 32px;\n  background: rgba(255, 255, 255, 0.22);\n  border-radius: 12px;\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n}\n.sb__textus-text {\n  display: flex;\n  flex-direction: column;\n  gap: 1px;\n  line-height: 1.1;\n  text-align: left;\n}\n.sb__textus-tag { /* declared; not rendered by current TSX */\n  font-size: 9px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  opacity: 0.85;\n}\n.sb--collapsed .sb__textus-text { display: none; }\n.sb--collapsed .sb__textus { justify-content: center; padding: 14px 0; }\n\n/* ------ Divider ------ */\n.sb__divider {\n  height: 1px;\n  background: rgba(255, 255, 255, 0.12);\n  margin: 4px 6px;\n}\n\n/* ------ Nav list ------ */\n.sb__nav {\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n  flex: 1;\n  overflow-y: auto;\n}\n.sb__nav::-webkit-scrollbar { width: 0; }\n.sb__label { /* section label; not currently rendered */\n  font-size: 10px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: rgba(255, 255, 255, 0.45);\n  padding: 14px 12px 6px;\n}\n.sb--collapsed .sb__label { text-align: center; padding: 14px 0 6px; font-size: 8px; }\n\n.nav-item {\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  padding: 10px 12px;\n  border-radius: 12px;\n  color: rgba(255, 255, 255, 0.7);\n  cursor: pointer;\n  border: none;\n  background: transparent;\n  width: 100%;\n  text-align: left;\n  font-family: var(--font-body);\n  font-size: 14px;\n  font-weight: 600;\n  transition: all 180ms;\n  position: relative;\n}\n.nav-item:hover { background: rgba(255, 255, 255, 0.08); color: #fff; }\n.nav-item--active {\n  background: var(--brand-green);\n  color: #fff;\n  font-weight: 700;\n  box-shadow: 0 6px 14px rgba(81, 152, 67, 0.35);\n}\n.nav-item--active:hover { background: var(--brand-green); color: #fff; }\n.nav-item--logout { color: rgba(255, 200, 200, 0.8); }\n.nav-item--logout:hover { background: rgba(220, 60, 60, 0.18); color: #fff; }\n.nav-item__icon {\n  width: 22px;\n  height: 22px;\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n}\n.nav-item__count {\n  margin-left: auto;\n  background: rgba(255, 255, 255, 0.25);\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 10px;\n  padding: 2px 7px;\n  border-radius: 999px;\n  min-width: 20px;\n  text-align: center;\n}\n.nav-item--active .nav-item__count { background: rgba(255, 255, 255, 0.35); color: #fff; }\n.sb--collapsed .nav-item { justify-content: center; padding: 10px 0; }\n.sb--collapsed .nav-item__label,\n.sb--collapsed .nav-item__count { display: none; }\n\n/* ------ Bottom block (user footer + collapse toggle + logout) ------ */\n.sb__bottom {\n  margin-top: auto;\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n  border-top: 1px solid rgba(255, 255, 255, 0.10);\n  padding-top: 8px;\n}\n.sb__bottom-head {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 4px 4px 2px;\n}\n.sb__bottom-head .sb__label { padding: 0; margin: 0; }\n.sb__toggle {\n  width: 28px;\n  height: 28px;\n  border-radius: 9px;\n  background: rgba(255, 255, 255, 0.08);\n  border: none;\n  color: rgba(255, 255, 255, 0.65);\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n  transition:\n    background 160ms, color 160ms,\n    transform 220ms cubic-bezier(0.34, 1.56, 0.64, 1);\n}\n.sb__toggle:hover { background: rgba(255, 255, 255, 0.16); color: #fff; }\n.sb--collapsed .sb__toggle { transform: rotate(180deg); } /* chevron flips to point right */\n.sb--collapsed .sb__bottom-head { justify-content: center; padding: 4px 0 2px; }\n.sb--collapsed .sb__bottom-head .sb__label { display: none; }\n\n/* NOTE: the user footer (.sb__footer) + its avatar/initials are styled INLINE\n   in DashSidebar.tsx (no class rules in CSS). The collapsed rail does not hide\n   the footer text — it stays full-width-styled even at 84px (a known squeeze;\n   see component .md Responsive). */\n\n/* ------ Mobile (<=640px): rail becomes a slide-in overlay drawer ------ */\n@media (max-width: 640px) {\n  .sb {\n    position: fixed;\n    top: 0;\n    left: 0;\n    bottom: 0;\n    z-index: 60;\n    width: 260px;\n    transform: translateX(-100%);\n    transition: transform 240ms cubic-bezier(0.34, 1.56, 0.64, 1);\n    box-shadow: 0 12px 32px rgba(20, 72, 82, 0.22);\n  }\n  .sb.sb--open { transform: translateX(0); }\n  .sb-backdrop {\n    position: fixed;\n    inset: 0;\n    background: rgba(28, 44, 48, 0.45);\n    z-index: 55;\n    animation: sbFadeIn 180ms ease-out;\n  }\n  @keyframes sbFadeIn { from { opacity: 0; } to { opacity: 1; } }\n}\n@media (min-width: 641px) {\n  /* desktop: hamburger collapses the rail QuickBooks-style; backdrop hidden */\n  .sb-backdrop { display: none; }\n}\n", "cssName": "dash-sidebar.css", "source": "import { useEffect, useState } from \"preact/hooks\";\nimport { I, ICN, type IconName } from \"../lib/dash-icons.tsx\";\nimport { type DashboardStats } from \"../clients/dashboard.ts\";\nimport {\n  type CachedDash,\n  readCached,\n  refreshDash,\n  subscribeDash,\n} from \"../lib/dash-cache.ts\";\nimport { langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface NavEntry {\n  id: string;\n  icon: IconName;\n  label: string;\n  href: string;\n  countKey?: CountKey;\n}\ntype CountKey = \"clients\" | \"quotes\" | \"contracts\" | \"invoices\";\n\n// Canonical NAV from the reference design (Paperwork Monster Dashboard.html).\n// `countKey` maps a sidebar entry to a derived count from /analytics/dashboard.\n// We never seed counts client-side — if the fetch fails or returns 0, the\n// badge is omitted (rendering an empty pill on a fresh account looks broken).\nconst NAV: NavEntry[] = [\n  { id: \"home\", icon: \"home\", label: \"Dashboard\", href: \"/dashboard\" },\n  {\n    id: \"clients\",\n    icon: \"user\",\n    label: \"Clients\",\n    href: \"/clients\",\n    countKey: \"clients\",\n  },\n  {\n    id: \"quotes\",\n    icon: \"quote\",\n    label: \"Quotes\",\n    href: \"/quotes\",\n    countKey: \"quotes\",\n  },\n  {\n    id: \"contracts\",\n    icon: \"contract\",\n    label: \"Contracts\",\n    href: \"/contracts\",\n    countKey: \"contracts\",\n  },\n  {\n    id: \"invoices\",\n    icon: \"invoice\",\n    label: \"Invoices\",\n    href: \"/invoices\",\n    countKey: \"invoices\",\n  },\n  { id: \"payments\", icon: \"pay\", label: \"Payments\", href: \"/payments\" },\n];\n\ninterface Props {\n  active?: string;\n  /** When false, the nav-tab list (Dashboard/Clients/…/Admin) is omitted and\n   *  only the sidebar chrome — brand, Assistant CTA, user footer, collapse,\n   *  logout — renders. Used by the accounts-manager view, which wants the\n   *  regular shell with no tabs yet. */\n  showNav?: boolean;\n}\n\ninterface SbState {\n  counts: Partial<Record<CountKey, number>>;\n  identity: { display: string; biz?: string; initials: string } | null;\n  /** Contractor UI language (roadmap p.13). */\n  lang: \"en\" | \"es\";\n  /** Super-admin flag — gates the Admin tab. Server-enforced; this only\n   *  controls visibility. */\n  superAdmin: boolean;\n}\n\nconst INITIAL_STATE: SbState = {\n  counts: {},\n  identity: null,\n  lang: \"en\",\n  superAdmin: false,\n};\n\nfunction deriveInitials(\n  name: string | undefined,\n  fallback: string | undefined,\n): string {\n  const src = name?.trim() || fallback?.trim();\n  if (!src) return \"•\";\n  const parts = src.split(/\\s+/).filter(Boolean);\n  if (parts.length === 1) return parts[0].slice(0, 2).toUpperCase();\n  return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();\n}\n\nfunction pickCounts(stats: DashboardStats): Partial<Record<CountKey, number>> {\n  // \"Active\" totals only — drafts shouldn't badge as new work since the user\n  // hasn't sent them yet. Quotes uses sent (not total) for the same reason.\n  return {\n    clients: stats.customers,\n    quotes: stats.quotes.sent,\n    contracts: stats.contracts.signed,\n    invoices: stats.invoices.pending,\n  };\n}\n\n/** Project the shared dash-cache snapshot onto the sidebar's slice. */\nfunction projectSidebar(snap: CachedDash | null): SbState {\n  if (!snap) return INITIAL_STATE;\n  const { stats, profile } = snap;\n  const userName = profile?.user.name?.trim();\n  const bizName =\n    (profile?.identity?.businessName ?? profile?.identity?.displayName)?.trim();\n  const counts = stats ? pickCounts(stats) : {};\n  const lang = profile?.user.language === \"es\" ? \"es\" : \"en\";\n  const superAdmin = profile?.user.superAdmin === true;\n  if (!userName && !bizName) {\n    return { counts, identity: null, lang, superAdmin };\n  }\n  const display = userName || bizName || tFor(lang, \"common.account\");\n  const biz = userName && bizName ? bizName : undefined;\n  const fallback = profile?.initials?.trim();\n  const initials = (fallback && fallback !== \"?\" ? fallback : null) ??\n    deriveInitials(userName, bizName);\n  return { counts, identity: { display, biz, initials }, lang, superAdmin };\n}\n\nexport default function DashSidebar({ active = \"home\", showNav = true }: Props) {\n  const [collapsed, setCollapsed] = useState<boolean>(() => {\n    if (typeof globalThis.localStorage === \"undefined\") return false;\n    return globalThis.localStorage.getItem(\"pm:sb-collapsed\") === \"1\";\n  });\n  const [mobileOpen, setMobileOpen] = useState(false);\n  // Seed from the shared cache so a navigation between pages renders the\n  // last-known counts/identity immediately and the badge doesn't flash\n  // empty before the refetch lands.\n  const [s, setS] = useState<SbState>(() => projectSidebar(readCached()));\n\n  useEffect(() => {\n    // Roadmap p.2/p.4: the topbar hamburger drives the rail. On desktop it\n    // collapses/expands the rail QuickBooks-style; on mobile it slides the\n    // drawer in/out. (Previously it only ever toggled the mobile drawer, so\n    // on desktop the hamburger appeared to \"do nothing\".)\n    const onToggle = () => {\n      const wide = typeof globalThis.innerWidth === \"number\" &&\n        globalThis.innerWidth >= 641;\n      if (wide) toggle();\n      else setMobileOpen((o) => !o);\n    };\n    globalThis.addEventListener(\"pm:sb-toggle\", onToggle as EventListener);\n    return () =>\n      globalThis.removeEventListener(\"pm:sb-toggle\", onToggle as EventListener);\n  }, []);\n\n  useEffect(() => {\n    let alive = true;\n    // Subscribe so any sibling island that triggers a refresh propagates\n    // here without us re-fetching.\n    const unsub = subscribeDash((snap) => {\n      if (!alive) return;\n      setS(projectSidebar(snap));\n    });\n    refreshDash().then((snap) => {\n      if (!alive) return;\n      setS(projectSidebar(snap));\n    });\n    return () => {\n      alive = false;\n      unsub();\n    };\n  }, []);\n\n  function toggle() {\n    setCollapsed((c) => {\n      const next = !c;\n      try {\n        globalThis.localStorage?.setItem(\"pm:sb-collapsed\", next ? \"1\" : \"0\");\n      } catch { /* SSR-safe */ }\n      return next;\n    });\n  }\n\n  async function logout() {\n    try {\n      await fetch(\"/api/auth/logout\", {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n      });\n    } catch {\n      /* network down — proxy still clears the cookie locally on retry; redirect anyway */\n    }\n    globalThis.location.href = \"/\";\n  }\n\n  // Reactive UI language: seeded from the profile cache, flipped instantly\n  // when Settings changes it. Reading langSignal here re-renders the rail.\n  const lang = langSignal.value;\n\n  return (\n    <>\n      {mobileOpen && (\n        <div\n          class=\"sb-backdrop\"\n          onClick={() => setMobileOpen(false)}\n          aria-hidden=\"true\"\n        />\n      )}\n      <aside\n        class={`sb ${collapsed ? \"sb--collapsed\" : \"\"} ${\n          mobileOpen ? \"sb--open\" : \"\"\n        }`}\n      >\n        <div class=\"sb__inner\">\n          <a\n            class=\"sb__brand\"\n            href=\"/dashboard\"\n            title={tFor(lang, \"nav.brandTitle\")}\n            style=\"text-decoration:none;color:inherit\"\n          >\n            <div class=\"sb__brand-logo\">\n              <img\n                src=\"/logo-monster-card.png\"\n                alt=\"\"\n                style=\"width:30px;height:30px;object-fit:cover;border-radius:8px;display:block\"\n              />\n            </div>\n            <div class=\"sb__brand-text\">\n              <div class=\"sb__brand-name\">{tFor(lang, \"brand.name\")}</div>\n            </div>\n          </a>\n\n          <a\n            class=\"sb__textus\"\n            href=\"/assistant\"\n            title={tFor(lang, \"nav.assistantTitle\")}\n          >\n            <div class=\"sb__textus-icon\">\n              <I d={ICN.crown} size={18} />\n            </div>\n            <div class=\"sb__textus-text\">\n              <span>{tFor(lang, \"nav.assistant\")}</span>\n            </div>\n          </a>\n\n          <div class=\"sb__divider\" />\n\n          {showNav && (\n          <nav class=\"sb__nav\">\n            {NAV.map((item) => {\n              const count = item.countKey ? s.counts[item.countKey] : undefined;\n              return (\n                <a\n                  key={item.id}\n                  href={item.href}\n                  class={`nav-item ${\n                    active === item.id ? \"nav-item--active\" : \"\"\n                  }`}\n                >\n                  <span class=\"nav-item__icon\">\n                    <I d={ICN[item.icon]} size={18} />\n                  </span>\n                  <span class=\"nav-item__label\">\n                    {tFor(lang, `nav.${item.id}`)}\n                  </span>\n                  {count != null && count > 0\n                    ? <span class=\"nav-item__count\">{count}</span>\n                    : null}\n                </a>\n              );\n            })}\n            {\n              /* Admin tab — super-admins only. Server enforces the /admin\n                gate too (route middleware + every endpoint); this just hides\n                the entry for everyone else. Label is intentionally not i18n'd\n                (internal operator tool). */\n            }\n            {s.superAdmin && (\n              <a\n                href=\"/admin\"\n                class={`nav-item ${\n                  active === \"admin\" ? \"nav-item--active\" : \"\"\n                }`}\n              >\n                <span class=\"nav-item__icon\">\n                  <I d={ICN.shield} size={18} />\n                </span>\n                <span class=\"nav-item__label\">Admin</span>\n              </a>\n            )}\n          </nav>\n          )}\n\n          <div class=\"sb__bottom\">\n            {s.identity && (\n              <a\n                href=\"/settings\"\n                class=\"sb__footer\"\n                style=\"display:flex;align-items:center;gap:10px;padding:10px 8px;margin:0 0 6px;border-radius:10px;text-decoration:none;color:inherit;background:rgba(255,255,255,0.04);\"\n                title={s.identity.biz\n                  ? `${s.identity.display} · ${s.identity.biz}`\n                  : s.identity.display}\n              >\n                <span\n                  aria-hidden=\"true\"\n                  style=\"flex:0 0 auto;width:32px;height:32px;border-radius:50%;background:var(--brand-green,#519843);color:#fff;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:12px;letter-spacing:0.04em\"\n                >\n                  {s.identity.initials}\n                </span>\n                <span\n                  class=\"sb__footer-text\"\n                  style=\"min-width:0;flex:1;display:flex;flex-direction:column;line-height:1.2;overflow:hidden\"\n                >\n                  <span style=\"font-size:13px;font-weight:700;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis\">\n                    {s.identity.display}\n                  </span>\n                  {s.identity.biz && (\n                    <span style=\"font-size:11px;color:rgba(255,255,255,0.6);white-space:nowrap;overflow:hidden;text-overflow:ellipsis\">\n                      {s.identity.biz}\n                    </span>\n                  )}\n                </span>\n                <span\n                  class=\"sb__footer-chev\"\n                  style=\"flex:0 0 auto;color:rgba(255,255,255,0.55)\"\n                >\n                  <I d={ICN.chev} size={14} />\n                </span>\n              </a>\n            )}\n            <div\n              class=\"sb__bottom-head\"\n              style=\"display:flex;align-items:center;justify-content:flex-end;padding:4px 4px 2px\"\n            >\n              <button\n                type=\"button\"\n                class=\"sb__toggle\"\n                onClick={toggle}\n                aria-label={collapsed\n                  ? tFor(lang, \"sidebar.expand\")\n                  : tFor(lang, \"sidebar.collapse\")}\n                title={collapsed\n                  ? tFor(lang, \"sidebar.expandTitle\")\n                  : tFor(lang, \"sidebar.collapseTitle\")}\n                style=\"width:28px;height:28px;border-radius:9px;background:rgba(255,255,255,0.08);border:none;color:rgba(255,255,255,0.65);cursor:pointer;display:grid;place-items:center;flex-shrink:0\"\n              >\n                <I\n                  d={\n                    <>\n                      {\n                        /* QuickBooks-style hamburger + arrow — CSS rotates it\n                          180° so the chevron points right when the rail is\n                          collapsed (roadmap p.4). */\n                      }\n                      <path d=\"M3 6h13M3 12h13M3 18h13\" />\n                      <path d=\"M21 9l-3 3 3 3\" />\n                    </>\n                  }\n                  size={14}\n                />\n              </button>\n            </div>\n            {\n              /* Roadmap p.4: removed the duplicate \"Settings\" nav link — the user block\n              above (which opens /settings) is the single entry point. */\n            }\n            <button\n              type=\"button\"\n              class=\"nav-item nav-item--logout\"\n              onClick={logout}\n            >\n              <span class=\"nav-item__icon\">\n                <I d={ICN.logout} size={18} />\n              </span>\n              <span class=\"nav-item__label\">\n                {tFor(lang, \"nav.logout\")}\n              </span>\n            </button>\n          </div>\n        </div>\n      </aside>\n    </>\n  );\n}\n", "sourceName": "DashSidebar.tsx", "checklist": "# Capture checklist — DashSidebar\n\n**Theme:** light only.\n**Auth:** log in with dev master OTP `000000` (any seeded phone). Sidebar renders\non every authed route.\n\n## Route / URL\n- Any authed page, e.g. `http://localhost:5280/dashboard`.\n- For `super-admin`: log in as a super-admin account (Admin tab only renders for them).\n- For `no-nav`: `http://localhost:5280/accounts-manager`.\n\n## Viewports (this component's own @media)\n- **1280px** (desktop expanded + collapsed).\n- **641px** (just above the drawer cutover — still a fixed rail).\n- **390px** (mobile drawer; iPhone-ish).\n\n## Element(s) to crop\n- The `<aside class=\"sb\">` rail (full height). On mobile also capture the\n  `.sb-backdrop` scrim behind the open drawer.\n\n## Transient states to drive\n1. **default** — `/dashboard`, expanded, with seeded counts (clients/quotes/\n   contracts/invoices badges visible).\n2. **collapsed** — click `.sb__toggle` (or set `localStorage[\"pm:sb-collapsed\"]=\"1\"`\n   and reload). Capture 84px rail with labels/counts hidden + toggle glyph rotated.\n3. **active-quotes** — navigate to `/quotes`; Quotes row shows green active pill.\n4. **empty** — a brand-new account (no clients/quotes/etc. and no name set):\n   no badges, no `.sb__footer` block.\n5. **super-admin** — Admin row present below Payments.\n6. **mobile-open** — at 390px, click the topbar hamburger (`.topbar__menu`) to\n   dispatch `pm:sb-toggle`; capture the slid-in drawer + scrim.\n7. **no-nav** — `/accounts-manager`: rail chrome with brand + assistant + footer\n   + toggle + logout, NO nav tabs.\n\n## Motion to film\n- Collapse/expand width transition (280ms bounce) + toggle glyph rotate (220ms).\n- Mobile drawer slide-in (240ms bounce) + backdrop fade (180ms).\n- Assistant CTA hover lift. Re-shoot each with `prefers-reduced-motion: reduce`.\n"}, {"name": "dash-topbar", "component": "DashTopbar", "spec": "# DashTopbar\n\nApp-shell top bar: hamburger + date/greeting + live-activity ticker. First child\nof `<main class=\"main\">` on every authenticated route.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/DashTopbar.tsx`).\n- **Interaction tier:** `island` (client-only state) with **polling**.\n- **Client state owned:**\n  - `unread: number` (held via `setUnread`, currently not rendered — the bell/\n    badge UI is hidden until built).\n  - `items: Notification[]` — recent activity list for the ticker.\n  - `tickerIdx: number` — rotation index.\n  - Greeting date is computed inline each render from `new Date()` + `langSignal`.\n- **Data source per region:**\n  - Greeting name → `greetingName` prop (SSR). Date → computed client-side from\n    `WEEKDAY_KEYS`/`MONTH_KEYS` via `tFor(lang, …)` so it re-localizes live.\n  - Ticker items → `clients/dashboard.ts` `dashboardClient.notifications(10)`;\n    unread → `dashboardClient.unreadCount()`. Seeded from `initialNotifications`\n    / `initialUnread` SSR props.\n- **Honest-empty:** when `items.length === 0`, `ticker` is `null` and the whole\n  pill is omitted — NO seeded \"Cobblestone Cafe paid $1,000\" fallback (it read\n  as fake first-run activity). Likewise search + notification affordances are\n  intentionally not rendered until those features ship.\n- **Liveness — polling (request-response, NOT pushed):**\n  - `setInterval` every **30 000ms** → `unreadCount()`.\n  - `setInterval` every **10 000ms** → `notifications(10)`.\n  - `setInterval` every **3 800ms** → advance `tickerIdx` (rotate displayed item).\n  - **FLAG:** the 10s notifications poll is aggressive for low-change data and\n    runs on every authed page. Fix: back off (30–60s) or move to a single\n    shared poller / SSE, since the same data feeds DashSidebar's cache.\n- **Cross-island coupling:** hamburger dispatches a global\n  `pm:sb-toggle` CustomEvent that `DashSidebar` listens for (collapse on desktop\n  / drawer on mobile). Event-bus pattern — see DashSidebar spec for the\n  shared-signal fix.\n- **No server mutation, no `location.reload()`.**\n- **Data-shape hazards:**\n  - `greetingDate` prop is **`@deprecated` and ignored** — the date is computed\n    in-component. Don't pass it expecting it to render.\n  - Ticker title is injected via `dangerouslySetInnerHTML` (`ticker.html`) —\n    server-derived markup (e.g. `<strong>…</strong>`), trusted, no user input.\n    Rebuild must treat `Notification.title` as HTML, not text.\n  - `greetingOverride` (used by the Assistant route) replaces the greeting line\n    verbatim, bypassing the `dashTopbar.greeting` template.\n  - `fmtAgo` clamps to `>=1m`; non-finite dates → `\"\"`.\n\n## 2. Anatomy\n```\n<header class=\"topbar\">\n  <button class=\"topbar__menu\" aria-label=\"Toggle sidebar\"            ← hamburger (inline 3-line path)\n          onClick=dispatch('pm:sb-toggle')>\n  <div class=\"topbar__greet\">\n    <div class=\"topbar__greet-line\">{Weekday · Month D}</div>\n    <div class=\"topbar__greet-name\">{greetingOverride ?? \"Hey, {name} 👋\"}</div>\n  </div>\n  <div style=\"flex:1\" aria-hidden />                                  ← spacer (pushes ticker right)\n  {ticker && <a class=\"topbar__ticker\" href=\"/dashboard#activity\">    ← only when items exist\n    <span class=\"topbar__ticker-dot\" />                              ← pulsing green dot\n    <span class=\"topbar__ticker-track\" aria-live=\"polite\">\n      <span class=\"topbar__ticker-item\" key={idx} dangerouslySetInnerHTML />\n    </span>\n    <span class=\"topbar__ticker-time\">{time} ago</span>\n  </a>}\n</header>\n```\n- **Slots/children:** none. `.topbar__search`/`.topbar__btn` exist in CSS but are\n  not rendered by this island.\n- **Icon dependency:** uses the `I` renderer from `lib/dash-icons.tsx` with an\n  inline `<path d=\"M3 6h18M3 12h18M3 18h18\"/>` (hamburger) — no `ICN` lookup.\n  `js/dash-icons.tsx` copied for self-containment.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `greetingName` | string (required) | — | text | no |\n| `greetingDate` | string | — | text | no — **@deprecated/ignored** |\n| `greetingOverride` | string | `undefined` | text | no |\n| `initialUnread` | number | `0` | number | no |\n| `initialNotifications` | `Notification[]` | `[]` | (json) | no |\n| `lang` | `\"en\"\\|\"es\"` | (reads `langSignal`) | select | no |\n\n`Notification` shape: `{ title: string (HTML), createdAt: ISO string, … }`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | greeting + ticker with items | `cases/default/default.json` |\n| empty | no notifications → ticker omitted | `cases/empty/empty.json` |\n| override | `greetingOverride` line (Assistant route) | `cases/override/override.json` |\n| es | Spanish greeting/date localization | `cases/es/es.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"button.topbar__menu\" && e.type === \"click\")` →\n  dispatches `CustomEvent(\"pm:sb-toggle\")`.\n- `ev.expect(e => e.source === \"a.topbar__ticker\" && e.type === \"click\")` → nav\n  `/dashboard#activity`.\n- Internal timers (not user events): unread poll 30s, notifications poll 10s,\n  ticker rotate 3.8s.\n\n## 6. Motion (extracted)\n- **Ticker dot:** `tickerPulse` 1.6s ease-in-out infinite — expanding\n  box-shadow halo `0→6px` then fade.\n- **Ticker item swap:** `tickerSlideIn` 360ms `--ease-bounce` — `opacity 0→1` +\n  `translateY(8px→0)`. Re-keyed by `tickerIdx` so each rotation re-triggers.\n  **Jank finding:** the item is `position:absolute; inset:0`; on swap the new\n  item animates in but the old one is replaced (Preact key change), so there's\n  no cross-fade overlap — clean. The pulsing dot's `box-shadow` animation paints\n  on the main thread but is tiny; acceptable.\n- **Hamburger / ticker hover:** `translateY(-1px)` over 160/180ms.\n- **Reduced motion:** no component-local block; relies on the global tokens\n  reduced-motion clamp (`animation/transition-duration: 0.01ms`). Verify the\n  pulse + slide are effectively stilled.\n\n## 7. Responsive\n- **Breakpoint 640px** (own CSS). `<=640px`: `.topbar` becomes\n  `position:sticky; top:0` with a more opaque mint background; `.topbar__ticker`\n  and `.topbar__search` are `display:none` (only hamburger + greeting remain);\n  greeting-name shrinks to 14px. Above 640px: static 60px bar, ticker visible\n  (max-width 280px, right-aligned).\n\n## 8. A11y\n- `<header>` landmark; hamburger is a `<button>` with `aria-label` (i18n\n  `dashTopbar.toggleSidebar`). Ticker is an `<a>` with `aria-label`\n  (`dashTopbar.liveActivity`); the rotating text region is `aria-live=\"polite\"`\n  so SR users hear new activity. Spacer div is `aria-hidden`.\n- **Gaps:** ticker injects HTML via `dangerouslySetInnerHTML`; ensure server\n  markup stays SR-friendly. No reduced-motion-specific handling locally.\n\n## 9. Used on\nShared across the same **11 authenticated routes** as DashSidebar (every page\nthat renders the app-shell `<main>`): dashboard, clients, quotes, contracts,\ninvoices, payments, settings, assistant (+ `[threadId]`, uses `greetingOverride`),\nadmin, accounts-manager. Evidence: grep of `DashTopbar` import in `routes/`.\nShared — single app top bar.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"dash-topbar\",\n  \"controls\": {\n    \"greetingName\": { \"type\": \"text\", \"value\": \"Raphael\" },\n    \"greetingOverride\": { \"type\": \"text\", \"value\": \"\" },\n    \"initialUnread\": { \"type\": \"number\", \"value\": 0 },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "default.json", "content": "{\n  \"_name\": \"Default — greeting + live ticker\",\n  \"greetingName\": \"Raphael\",\n  \"initialUnread\": 2,\n  \"initialNotifications\": [\n    { \"title\": \"<strong>Cobblestone Cafe</strong> accepted your quote\", \"createdAt\": \"2026-06-18T14:35:00.000Z\" },\n    { \"title\": \"<strong>Maple Dental</strong> paid invoice #1042\", \"createdAt\": \"2026-06-18T13:10:00.000Z\" }\n  ],\n  \"lang\": \"en\"\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — no notifications, ticker hidden\",\n  \"greetingName\": \"Raphael\",\n  \"initialUnread\": 0,\n  \"initialNotifications\": [],\n  \"lang\": \"en\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish localization\",\n  \"greetingName\": \"Raphael\",\n  \"initialUnread\": 1,\n  \"initialNotifications\": [\n    { \"title\": \"<strong>Cobblestone Cafe</strong> aceptó tu presupuesto\", \"createdAt\": \"2026-06-18T14:35:00.000Z\" }\n  ],\n  \"lang\": \"es\"\n}\n"}, {"name": "override.json", "content": "{\n  \"_name\": \"Greeting override (Assistant route)\",\n  \"greetingName\": \"Raphael\",\n  \"greetingOverride\": \"Let's build a quote\",\n  \"initialUnread\": 0,\n  \"initialNotifications\": [],\n  \"lang\": \"en\"\n}\n"}], "css": "/* ============================================================\n   DashTopbar — extracted from static/dashboard.css\n   Lives as the first child of `<main class=\"main\">` in the app-shell.\n   Tokens: --brand-teal, --brand-green, --mint-100, --border, --green-50,\n   --fg-muted, --fg-subtle, --ease-bounce, --font-heading, --font-mono.\n   ============================================================ */\n\n/* parent column (context only) */\n.main {\n  flex: 1;\n  min-width: 0;\n  display: flex;\n  flex-direction: column;\n  overflow: hidden;\n}\n\n.topbar {\n  height: 60px;\n  flex-shrink: 0;\n  padding: 0 20px;\n  display: flex;\n  align-items: center;\n  gap: 14px;\n  background: rgba(255, 255, 255, 0.5);\n  backdrop-filter: blur(10px);\n  border-bottom: 1px solid var(--border);\n}\n\n/* ------ Hamburger (dispatches pm:sb-toggle) ------ */\n.topbar__menu {\n  width: 38px;\n  height: 38px;\n  border-radius: 11px;\n  background: var(--mint-100);\n  border: 1px solid var(--border);\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  color: var(--brand-teal);\n  flex-shrink: 0;\n  transition: background 160ms, color 160ms, transform 160ms;\n  padding: 0;\n  margin-right: 8px;\n}\n.topbar__menu:hover { background: #fff; transform: translateY(-1px); }\n.topbar__menu:active { transform: translateY(0); }\n.topbar__menu svg { display: block; }\n\n/* ------ Greeting ------ */\n.topbar__greet { flex-shrink: 0; }\n.topbar__greet-line {\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin-bottom: 1px;\n}\n.topbar__greet-name {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  color: var(--brand-teal);\n  line-height: 1;\n  white-space: nowrap;\n}\n\n/* ------ Live activity ticker ------ */\n.topbar__ticker {\n  flex-shrink: 1;\n  min-width: 0;\n  max-width: 280px;\n  height: 36px;\n  display: inline-flex;\n  align-items: center;\n  gap: 10px;\n  padding: 0 14px 0 12px;\n  background: var(--green-50);\n  border: 1px solid rgba(81, 152, 67, 0.20);\n  border-radius: 999px;\n  overflow: hidden;\n  cursor: pointer;\n  transition: all 180ms;\n  color: inherit;\n  text-decoration: none;\n}\n.topbar__ticker:hover {\n  background: #fff;\n  border-color: rgba(81, 152, 67, 0.35);\n  transform: translateY(-1px);\n}\n.topbar__ticker-dot {\n  flex-shrink: 0;\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  background: var(--brand-green);\n  box-shadow: 0 0 0 0 rgba(81, 152, 67, 0.5);\n  animation: tickerPulse 1.6s ease-in-out infinite;\n}\n@keyframes tickerPulse {\n  0%   { box-shadow: 0 0 0 0 rgba(81, 152, 67, 0.55); }\n  70%  { box-shadow: 0 0 0 6px rgba(81, 152, 67, 0); }\n  100% { box-shadow: 0 0 0 0 rgba(81, 152, 67, 0); }\n}\n.topbar__ticker-track {\n  flex: 1;\n  min-width: 0;\n  position: relative;\n  height: 18px;\n  overflow: hidden;\n}\n.topbar__ticker-item {\n  position: absolute;\n  inset: 0;\n  display: flex;\n  align-items: center;\n  font-size: 12.5px;\n  color: var(--brand-teal);\n  font-weight: 500;\n  letter-spacing: -0.005em;\n  white-space: nowrap;\n  text-overflow: ellipsis;\n  overflow: hidden;\n  animation: tickerSlideIn 360ms var(--ease-bounce) both;\n}\n@keyframes tickerSlideIn {\n  from { opacity: 0; transform: translateY(8px); }\n  to   { opacity: 1; transform: translateY(0); }\n}\n.topbar__ticker-item strong {\n  color: var(--brand-teal);\n  font-family: var(--font-heading);\n  font-weight: 800;\n  margin-right: 4px;\n}\n.topbar__ticker-time {\n  flex-shrink: 0;\n  font-size: 10.5px;\n  color: var(--fg-subtle);\n  font-family: var(--font-mono, ui-monospace, monospace);\n  letter-spacing: 0;\n}\n\n/* ------ Search + notification controls (styled in CSS but NOT rendered by\n   the current island — search/notifications were hidden until those features\n   ship; kept here for context / future re-enable) ------ */\n.topbar__search {\n  flex: 1; min-width: 0; max-width: 560px;\n  display: flex; align-items: center; gap: 10px;\n  background: rgba(255, 255, 255, 0.85);\n  border: 1px solid var(--border);\n  border-radius: 999px; padding: 8px 16px; margin: 0 auto;\n}\n.topbar__search input {\n  border: none; outline: none; background: transparent; flex: 1;\n  font-family: var(--font-body); font-size: 13px; color: var(--brand-teal);\n}\n.topbar__search input::placeholder { color: var(--fg-subtle); }\n.topbar__kbd {\n  font-family: var(--font-mono); font-size: 10px; color: var(--fg-subtle);\n  background: rgba(100, 69, 54, 0.06); padding: 2px 6px; border-radius: 4px; flex-shrink: 0;\n}\n.topbar__btn {\n  width: 40px; height: 40px; border-radius: 12px; background: #fff;\n  border: 1px solid var(--border); cursor: pointer; display: grid;\n  place-items: center; color: var(--brand-teal); position: relative; flex-shrink: 0;\n}\n.topbar__btn-dot {\n  position: absolute; top: 8px; right: 9px; width: 9px; height: 9px;\n  background: var(--brand-pink); border: 2px solid #fff; border-radius: 999px;\n}\n\n/* ------ Mobile (<=640px) ------ */\n@media (max-width: 640px) {\n  .main { display: block; overflow: visible; }\n  .topbar {\n    position: sticky;\n    top: 0;\n    z-index: 30;\n    padding: 0 12px;\n    gap: 8px;\n    background: rgba(247, 243, 235, 0.92); /* more opaque so content stays legible */\n  }\n  .topbar__greet-name { font-size: 14px; }\n  .topbar__search { display: none; }\n  .topbar__ticker { display: none; }\n}\n", "cssName": "dash-topbar.css", "source": "import { useEffect, useState } from \"preact/hooks\";\nimport { I } from \"../lib/dash-icons.tsx\";\nimport { dashboardClient, type Notification } from \"../clients/dashboard.ts\";\nimport { langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  /** @deprecated Ignored — the date is now computed reactively in-component\n   *  so it re-localizes live on a language change instead of arriving frozen. */\n  greetingDate?: string;\n  greetingName: string;\n  /** When set, replaces the default \"Hey, {name} 👋\" line verbatim. Used by the Assistant route. */\n  greetingOverride?: string;\n  initialUnread?: number;\n  initialNotifications?: Notification[];\n  lang?: \"en\" | \"es\";\n}\n\n// No fallback ticker — when the user has zero real notifications we hide\n// the ticker entirely. Showing seeded \"Cobblestone Cafe paid $1,000\" to a\n// brand-new account read as fake activity and misled first-run users.\nfunction fmtAgo(iso: string): string {\n  const t = new Date(iso).getTime();\n  if (!Number.isFinite(t)) return \"\";\n  const m = Math.max(1, Math.floor((Date.now() - t) / 60_000));\n  if (m < 60) return `${m}m`;\n  const h = Math.floor(m / 60);\n  if (h < 24) return `${h}h`;\n  return `${Math.floor(h / 24)}d`;\n}\n\n// Full weekday / month name keys (same set the dashboard route uses). The\n// greeting date is built here so it re-localizes live when langSignal flips,\n// rather than arriving as a frozen pre-formatted string from the route.\nconst WEEKDAY_KEYS = [\n  \"dashboardPage.weekday.sunday\",\n  \"dashboardPage.weekday.monday\",\n  \"dashboardPage.weekday.tuesday\",\n  \"dashboardPage.weekday.wednesday\",\n  \"dashboardPage.weekday.thursday\",\n  \"dashboardPage.weekday.friday\",\n  \"dashboardPage.weekday.saturday\",\n];\nconst MONTH_KEYS = [\n  \"dashboardPage.month.january\",\n  \"dashboardPage.month.february\",\n  \"dashboardPage.month.march\",\n  \"dashboardPage.month.april\",\n  \"dashboardPage.month.may\",\n  \"dashboardPage.month.june\",\n  \"dashboardPage.month.july\",\n  \"dashboardPage.month.august\",\n  \"dashboardPage.month.september\",\n  \"dashboardPage.month.october\",\n  \"dashboardPage.month.november\",\n  \"dashboardPage.month.december\",\n];\n\nexport default function DashTopbar(\n  {\n    greetingName,\n    greetingOverride,\n    initialUnread = 0,\n    initialNotifications = [],\n  }: Props,\n) {\n  // Reactive app language (seeded from user.language, flipped live by Settings).\n  const lang = langSignal.value;\n  const now = new Date();\n  const greetingDate = `${tFor(lang, WEEKDAY_KEYS[now.getDay()])} · ${\n    tFor(lang, MONTH_KEYS[now.getMonth()])\n  } ${now.getDate()}`;\n  const [, setUnread] = useState(initialUnread);\n  const [items, setItems] = useState<Notification[]>(initialNotifications);\n  const [tickerIdx, setTickerIdx] = useState(0);\n\n  useEffect(() => {\n    let stopped = false;\n    const idA = setInterval(async () => {\n      try {\n        const { count } = await dashboardClient.unreadCount();\n        if (!stopped) setUnread(count);\n      } catch { /* ignore */ }\n    }, 30_000);\n    const idB = setInterval(async () => {\n      try {\n        const next = await dashboardClient.notifications(10);\n        if (!stopped) setItems(next);\n      } catch { /* ignore */ }\n    }, 10_000);\n    return () => {\n      stopped = true;\n      clearInterval(idA);\n      clearInterval(idB);\n    };\n  }, []);\n\n  useEffect(() => {\n    const id = setInterval(() => setTickerIdx((n) => n + 1), 3_800);\n    return () => clearInterval(id);\n  }, []);\n\n  const liveItems = items.length > 0 ? items : null;\n  const ticker = liveItems\n    ? {\n      html: liveItems[tickerIdx % liveItems.length].title,\n      time: fmtAgo(liveItems[tickerIdx % liveItems.length].createdAt),\n    }\n    : null;\n\n  return (\n    <header class=\"topbar\">\n      <button\n        class=\"topbar__menu\"\n        type=\"button\"\n        aria-label={tFor(lang, \"dashTopbar.toggleSidebar\")}\n        onClick={() =>\n          globalThis.dispatchEvent(new CustomEvent(\"pm:sb-toggle\"))}\n      >\n        <I d={<path d=\"M3 6h18M3 12h18M3 18h18\" />} size={18} />\n      </button>\n      <div class=\"topbar__greet\">\n        <div class=\"topbar__greet-line\">{greetingDate}</div>\n        <div class=\"topbar__greet-name\">\n          {greetingOverride ?? tFor(lang, \"dashTopbar.greeting\", { name: greetingName })}\n        </div>\n      </div>\n      {\n        /* Search + notifications drawer are not built yet; hide their\n          affordances until the underlying features ship rather than\n          advertise dead controls (audit #6, #7). */\n      }\n      <div style=\"flex:1\" aria-hidden=\"true\" />\n      {ticker\n        ? (\n          // Anchored to /dashboard#activity so the pill is no longer inert:\n          // on the dashboard it scrolls to the on-page activity panel; from\n          // any other page it routes to the dashboard and lands on the same\n          // anchor (#21 — the click was decorative on day 1).\n          <a\n            href=\"/dashboard#activity\"\n            class=\"topbar__ticker\"\n            aria-label={tFor(lang, \"dashTopbar.liveActivity\")}\n          >\n            <span class=\"topbar__ticker-dot\" />\n            <span class=\"topbar__ticker-track\" aria-live=\"polite\">\n              <span\n                class=\"topbar__ticker-item\"\n                key={tickerIdx}\n                // Trusted server-derived activity markup (no user input).\n                // deno-lint-ignore react-no-danger\n                dangerouslySetInnerHTML={{ __html: ticker.html }}\n              />\n            </span>\n            <span class=\"topbar__ticker-time\">\n              {tFor(lang, \"dashTopbar.timeAgo\", { time: ticker.time })}\n            </span>\n          </a>\n        )\n        : null}\n    </header>\n  );\n}\n", "sourceName": "DashTopbar.tsx", "checklist": "# Capture checklist — DashTopbar\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`. Topbar renders on every authed route.\n\n## Route / URL\n- `http://localhost:5280/dashboard` (default + empty).\n- `http://localhost:5280/assistant` for the `greetingOverride` variant.\n- Flip language in Settings (or `langSignal`) for the `es` variant.\n\n## Viewports (own @media)\n- **1280px** (full bar with ticker, right-aligned).\n- **641px** (ticker still visible, just above mobile cutover).\n- **390px** (sticky bar, ticker + search hidden — hamburger + greeting only).\n\n## Element(s) to crop\n- The `<header class=\"topbar\">` strip (60px tall on desktop).\n- Tight crop on `.topbar__ticker` to show the pulsing dot + rotating item + time.\n\n## Transient states to drive\n1. **default** — account with >=1 recent notification → ticker pill present.\n   Wait ~4s between shots to catch the item rotating (`tickerIdx` advances every\n   3.8s) and the `tickerSlideIn` entrance.\n2. **empty** — fresh account / no notifications → NO ticker pill (only hamburger\n   + greeting + empty space).\n3. **override** — `/assistant` → greeting line replaced with the route's custom text.\n4. **es** — Spanish weekday/month + greeting.\n\n## Motion to film\n- `tickerPulse` (1.6s halo on the dot) and `tickerSlideIn` (360ms bounce on item\n  swap — capture the moment of rotation). Re-shoot with\n  `prefers-reduced-motion: reduce` to confirm the global clamp stills them.\n"}, {"name": "impersonation-banner", "component": "ImpersonationBanner", "spec": "# ImpersonationBanner\n\nPersistent \"you are impersonating\" warning bar. Mounted **globally** in\n`routes/_app.tsx`. Self-gates: renders `null` for ordinary sessions.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ImpersonationBanner.tsx`).\n- **Interaction tier:** `island` (client-only state) with a server mutation.\n- **Client state owned:**\n  - `who: WhoAmI | null` — result of the self-gate fetch.\n  - `busy: boolean` — true while the stop-impersonating request is in flight.\n- **Self-gate (data source):** on mount calls `clients/admin.ts`\n  `adminClient.whoami()` → `GET /admin/whoami`. If the response's\n  `who.impersonating` is falsy (or the call throws — not logged in / backend\n  down), it returns `null` and renders nothing. So it is **safe to mount on\n  every page**; the cost is one `/admin/whoami` request per full page load.\n- **Honest-empty:** the entire component is absent unless an active impersonation\n  session is detected. No skeleton, no placeholder.\n- **Server mutation + feedback:**\n  - \"Return to your account\" → `adminClient.stopImpersonating()`\n    (`/admin/stop-impersonating`; server swaps the session cookie back), then\n    `globalThis.location.href = \"/admin\"`.\n  - **ANTI-PATTERN (mild, justified):** this is a full-document navigation after\n    a mutation. It is *somewhat* warranted — the cookie identity changes\n    server-side and every island holding the impersonated user's data must be\n    rebuilt from a fresh request. The clean fix is a server-side **PRG**: have\n    `/admin/stop-impersonating` respond `303 → /admin` so the browser navigates\n    via the server rather than a client `location.href` (also avoids a flash of\n    the stale impersonated page). Flash feedback while in flight = the button\n    label switches to \"Returning…\" and is `disabled`.\n- **Liveness:** request-response (one whoami on mount). No polling/websocket.\n- **Data-shape hazards:**\n  - `actingLabel` = `acting.name || acting.businessName || fmtPhone(acting.phoneNumber)`,\n    falling back to `\"another user\"` if `who.user` is missing.\n  - `who.impersonator?.name` optional → renders `(as {name})` suffix only if present.\n\n## 2. Anatomy\n```\n{ who?.impersonating ? (\n  <div role=\"status\" style=\"...fixed maroon bar...\">          ← .impersonation-banner\n    <span>Impersonating <strong>{actingLabel}</strong>{ (as {impersonator.name}) }</span>\n    <button onClick=stop disabled={busy}>                      ← .impersonation-banner__btn\n      {busy ? \"Returning…\" : \"Return to your account\"}\n    </button>\n  </div>\n) : null }\n```\n- **Slots/children:** none. **All styling inline** (no CSS classes; see\n  `css/impersonation-banner.css` for the extracted literals).\n- Copy is **not internationalized** (operator-only tool, English literal).\n\n## 3. Props\nNone.\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| — | — | — | — | — |\n\n(Display data comes entirely from the `whoami` fetch.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| hidden | ordinary session / not logged in → renders nothing | `cases/hidden/hidden.json` |\n| impersonating | banner shown, idle | `cases/impersonating/impersonating.json` |\n| busy | \"Return\" clicked, request in flight (disabled, \"Returning…\") | `cases/busy/busy.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"button.impersonation-banner__btn\" && e.type === \"click\")`\n  → `setBusy(true)` → `stopImpersonating()` → redirect `/admin` (on failure,\n  `setBusy(false)` and the banner stays).\n\n## 6. Motion\n- None. No transitions/animations. (No reduced-motion concern.) It simply pins to\n  the top of the viewport with a static box-shadow.\n- **Note:** being `position:fixed; top:0` it overlays page content; pages do not\n  reserve space, so on impersonation sessions the very top of the underlying\n  page is covered ~33px. Rebuild may want a body top-padding when active.\n\n## 7. Responsive\n- No `@media` queries. The bar is `flex-wrap: wrap`, full-width, centered — on\n  narrow screens the label + button wrap to two lines. Works at all widths.\n\n## 8. A11y\n- `role=\"status\"` so SR users are informed of the impersonation context (polite\n  live region). Button is a real `<button>` with `disabled` while busy.\n- **Gaps:** no `aria-live` priority beyond `status`; the maroon/white contrast is\n  strong (passes). No focus management (it just appears at page top).\n\n## 9. Used on\n**Global** — `<ImpersonationBanner />` in `routes/_app.tsx`, so it is present on\nevery route but only visible during a super-admin impersonation session.\nEvidence: import in `routes/_app.tsx`. Single global instance.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"impersonation-banner\",\n  \"controls\": {}\n}\n", "cases": [{"name": "busy.json", "content": "{\n  \"_name\": \"Returning — request in flight (disabled)\",\n  \"_signals\": {\n    \"who\": {\n      \"impersonating\": true,\n      \"user\": { \"name\": \"Maria Lopez\", \"businessName\": \"Lopez Landscaping\", \"phoneNumber\": \"+15125550143\" },\n      \"impersonator\": { \"name\": \"Raphael Castro\" }\n    },\n    \"busy\": true\n  }\n}\n"}, {"name": "hidden.json", "content": "{\n  \"_name\": \"Ordinary session — renders nothing\",\n  \"_signals\": {\n    \"who\": { \"impersonating\": false },\n    \"busy\": false\n  }\n}\n"}, {"name": "impersonating.json", "content": "{\n  \"_name\": \"Impersonating — banner idle\",\n  \"_signals\": {\n    \"who\": {\n      \"impersonating\": true,\n      \"user\": { \"name\": \"Maria Lopez\", \"businessName\": \"Lopez Landscaping\", \"phoneNumber\": \"+15125550143\" },\n      \"impersonator\": { \"name\": \"Raphael Castro\" }\n    },\n    \"busy\": false\n  }\n}\n"}], "css": "/* ============================================================\n   ImpersonationBanner — NO stylesheet classes. Every style is INLINE in\n   ImpersonationBanner.tsx. Reproduced here as ready-to-use rules so the\n   banner can be rebuilt without re-deriving the literals.\n\n   Palette is a deliberate hard-red operator chrome — NOT Sabor tokens (this is\n   a super-admin-only warning bar, intentionally off-palette so it can't be\n   mistaken for product UI).\n   ============================================================ */\n\n/* the fixed banner bar (role=\"status\") */\n.impersonation-banner {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  z-index: 9999;\n  display: flex;\n  gap: 12px;\n  align-items: center;\n  justify-content: center;\n  flex-wrap: wrap;\n  padding: 8px 14px;\n  background: #7a2e2e;            /* hard maroon — off-token operator warning */\n  color: #fff;\n  font: inherit;\n  font-size: 13px;\n  font-weight: 600;\n  box-shadow: 0 1px 6px rgba(0, 0, 0, 0.25);\n}\n\n/* \"Return to your account\" button */\n.impersonation-banner__btn {\n  padding: 5px 12px;\n  border-radius: 8px;\n  border: 0;\n  background: #fff;\n  color: #7a2e2e;\n  cursor: pointer;\n  font: inherit;\n  font-weight: 700;\n}\n/* disabled (busy) state: button keeps styles; `disabled` attr set while the\n   stop-impersonating request is in flight (label flips to \"Returning…\"). */\n", "cssName": "impersonation-banner.css", "source": "/**\n * Persistent \"you are impersonating\" banner. Mounted globally in _app.tsx.\n *\n * On mount it asks the backend (/admin/whoami) whether the current session is\n * an impersonation session. If so it pins a banner to the top of the viewport\n * naming who you're acting as, with a \"Return to your account\" control that\n * hits /admin/stop-impersonating (server swaps the cookie back) and reloads.\n *\n * Renders nothing for ordinary sessions, so it's safe to mount everywhere.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport { adminClient, type WhoAmI } from \"../clients/admin.ts\";\nimport { fmtPhone } from \"../lib/format.ts\";\n\nexport default function ImpersonationBanner() {\n  const [who, setWho] = useState<WhoAmI | null>(null);\n  const [busy, setBusy] = useState(false);\n\n  useEffect(() => {\n    let alive = true;\n    adminClient.whoami()\n      .then((w) => {\n        if (alive) setWho(w);\n      })\n      .catch(() => {/* not logged in / backend down — show nothing */});\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  if (!who?.impersonating) return null;\n\n  const acting = who.user;\n  const actingLabel = acting\n    ? (acting.name || acting.businessName || fmtPhone(acting.phoneNumber))\n    : \"another user\";\n\n  async function stop() {\n    setBusy(true);\n    try {\n      await adminClient.stopImpersonating();\n      globalThis.location.href = \"/admin\";\n    } catch {\n      setBusy(false);\n    }\n  }\n\n  return (\n    <div\n      role=\"status\"\n      style=\"position:fixed;top:0;left:0;right:0;z-index:9999;display:flex;gap:12px;align-items:center;justify-content:center;flex-wrap:wrap;padding:8px 14px;background:#7a2e2e;color:#fff;font:inherit;font-size:13px;font-weight:600;box-shadow:0 1px 6px rgba(0,0,0,.25)\"\n    >\n      <span>\n        Impersonating <strong>{actingLabel}</strong>\n        {who.impersonator?.name ? ` (as ${who.impersonator.name})` : \"\"}\n      </span>\n      <button\n        type=\"button\"\n        onClick={stop}\n        disabled={busy}\n        style=\"padding:5px 12px;border-radius:8px;border:0;background:#fff;color:#7a2e2e;cursor:pointer;font:inherit;font-weight:700\"\n      >\n        {busy ? \"Returning…\" : \"Return to your account\"}\n      </button>\n    </div>\n  );\n}\n", "sourceName": "ImpersonationBanner.tsx", "checklist": "# Capture checklist — ImpersonationBanner\n\n**Theme:** light only (banner is off-palette maroon regardless).\n**Auth:** must be a **super-admin** session. Dev master OTP `000000` for login,\nthen from `/admin` use the impersonate control on a target user.\n\n## Route / URL\n- Start at `http://localhost:5280/admin`, impersonate a user → the banner pins to\n  the top of EVERY page while impersonating. Capture on `/dashboard`.\n\n## Viewports (no own @media)\n- **1280px** (single line: \"Impersonating <name> (as <you>)\" + button).\n- **390px** (label + button wrap to two lines via `flex-wrap`).\n\n## Element(s) to crop\n- The fixed bar at `top:0` (`role=\"status\"`, maroon `#7a2e2e`). Include enough of\n  the page below to show it overlays content.\n\n## Transient states to drive\n1. **hidden** — ordinary (non-impersonation) session: confirm NO bar renders\n   (whoami → `impersonating:false`).\n2. **impersonating** — active impersonation: idle bar with \"Return to your\n   account\" button.\n3. **busy** — click \"Return to your account\"; capture the disabled button\n   reading \"Returning…\" before the redirect to `/admin` fires (drive by stalling\n   the `/admin/stop-impersonating` response, or screenshot quickly).\n\n## Motion\n- None to film (no transitions/animations).\n"}, {"name": "mobile-viewport", "component": "MobileViewport", "spec": "# MobileViewport\n\nBehavior-only island. Renders `null`. Mounted **once globally** in\n`routes/_app.tsx`. Mirrors the iOS visual viewport into CSS custom properties so\nevery page can stay clear of the on-screen keyboard.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/MobileViewport.tsx`).\n- **Interaction tier:** `client-only` (no DOM, no props, no server I/O). Pure\n  side-effect island — a `useEffect` that subscribes to viewport events.\n- **What it owns:** no React/Preact state. It writes/clears four `:root` CSS\n  custom properties and listens to DOM/viewport events. On unmount it removes\n  all four properties and detaches every listener.\n- **Outputs (the contract):**\n  - `--app-vh` = `visualViewport.height` **only while** the keyboard overlaps\n    (`innerHeight - vv.height > 2`); otherwise the property is *removed* so\n    consumers fall back to `100dvh` and follow the keyboard animation smoothly.\n  - `--kb-inset` = `max(0, innerHeight - vv.height)` — full keyboard overlap;\n    added as bottom scroll-room so trailing fields/buttons clear the keyboard.\n  - `--vvh` = `vv.height` (always set) — for fixed shells sized to the visible band.\n  - `--vvt` = `vv.offsetTop` (always set) — vertical offset of the visible band.\n- **Events it binds:** `visualViewport` `resize` + `scroll` → `apply()`;\n  `window` `resize` → `apply()` (Android, which resizes the layout viewport but\n  may not fire `vv.resize`); `document` `focusin`/`focusout` to lift the focused\n  field (and any trailing submit button in the same `form`/`[data-kbd-group]`)\n  clear of the keyboard via repeated timed `scrollBy` nudges.\n- **Liveness:** event-driven (request-response to viewport changes); no polling,\n  no network. Desktop is a no-op (keyboard inset 0; `vv` may be absent → early\n  return).\n- **No anti-patterns** (no `location.reload`, no SSR-frozen props).\n- **Data-shape hazards:** depends on `globalThis.visualViewport` (absent on very\n  old engines → it bails). Uses a 2600ms \"focus settle\" window with staged\n  re-corrections (`[60,250,500,800,1200,1800,2400]`ms) because the iOS keyboard\n  + predictive-text bar settle in stages.\n\n## 2. Anatomy\n- No DOM. `return null`. Its \"anatomy\" is the CSS-variable contract above plus\n  the focus-reveal scroll logic. No slots/children.\n- Helper `scrollableAncestor(el)` walks up to the nearest real\n  `overflow-y:auto|scroll` container (e.g. the chat thread `.chat__scroll`),\n  returning `null` for plain body-scroll pages.\n\n## 3. Props\nNone. (No props interface.)\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| — | — | — | — | — |\n\n## 4. States → cases\nThis island has no visual states; \"states\" are environment conditions. Cases\ndocument the variable outputs the harness should assert against a mocked\n`visualViewport`.\n| state | meaning | case |\n|---|---|---|\n| desktop | no `visualViewport` overlap → `--app-vh` absent, `--kb-inset:0` | `cases/desktop/desktop.json` |\n| keyboard-open | iOS keyboard overlaps → `--app-vh` set, `--kb-inset>0` | `cases/keyboard-open/keyboard-open.json` |\n\n## 5. Events\n- Not user-facing UI events. Predicate sketches against the side effects:\n  - `ev.expect(e => e.source === \"visualViewport\" && e.type === \"resize\")` →\n    recomputes `--app-vh`/`--kb-inset`/`--vvh`/`--vvt`.\n  - `ev.expect(e => e.source === \"document\" && e.type === \"focusin\" && e.targetMatches(\"input,textarea,select,[contenteditable=true]\"))`\n    → schedules staged scroll corrections to reveal the field + trailing submit.\n  - `ev.expect(e => e.source === \"document\" && e.type === \"focusout\")` → stops\n    re-correcting (`activeCorrect=null`).\n\n## 6. Motion\n- No CSS animation of its own. It *enables* smooth shell resize by deferring to\n  `100dvh` (removing `--app-vh`) when no keyboard is present, so the shell tracks\n  the keyboard animation frame-for-frame instead of snapping at the end. Scroll\n  corrections use `behavior:\"auto\"` (instant) deliberately. No reduced-motion\n  handling needed (no animation).\n\n## 7. Responsive\n- Active only where `visualViewport` reports overlap — effectively mobile\n  (iOS/Android) with a soft keyboard. Desktop = no-op. No breakpoints of its own;\n  its consumers (`.app`, public doc pages, chat/login shells) gate on 640px etc.\n\n## 8. A11y\n- Improves accessibility indirectly: keeps focused inputs and their submit\n  buttons visible above the keyboard (iOS native scroll-into-view is unreliable\n  inside nested scroll containers). No roles/labels (no DOM).\n\n## 9. Used on\n**Global** — mounted once in `routes/_app.tsx` (`<MobileViewport />`), so it is\nin effect on every page (app + auth + public). Evidence: import in\n`routes/_app.tsx`. Single global instance; never page-local.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"mobile-viewport\",\n  \"controls\": {}\n}\n", "cases": [{"name": "desktop.json", "content": "{\n  \"_name\": \"Desktop / no keyboard — vars cleared\",\n  \"_mocks\": {\n    \"visualViewport\": { \"height\": 900, \"offsetTop\": 0 },\n    \"innerHeight\": 900\n  },\n  \"_expect\": {\n    \"--app-vh\": \"(removed)\",\n    \"--kb-inset\": \"0px\",\n    \"--vvh\": \"900px\",\n    \"--vvt\": \"0px\"\n  }\n}\n"}, {"name": "keyboard-open.json", "content": "{\n  \"_name\": \"iOS keyboard open — overlap drives vars\",\n  \"_mocks\": {\n    \"visualViewport\": { \"height\": 410, \"offsetTop\": 124 },\n    \"innerHeight\": 844\n  },\n  \"_expect\": {\n    \"--app-vh\": \"410px\",\n    \"--kb-inset\": \"434px\",\n    \"--vvh\": \"410px\",\n    \"--vvt\": \"124px\"\n  }\n}\n"}], "css": "/* ============================================================\n   MobileViewport — produces no DOM and ships NO CSS of its own.\n   It WRITES four CSS custom properties on :root (document.documentElement)\n   that other stylesheets read. This file documents the contract + the real\n   consumers extracted from the feature CSS, so the variables can be wired up.\n   ============================================================ */\n\n/*\n  Variables written by the island (cleared on unmount):\n\n    --app-vh  : visualViewport.height in px, ONLY while the soft keyboard\n                overlaps (>2px). Removed otherwise so consumers fall back to\n                100dvh and track the keyboard animation frame-for-frame.\n    --kb-inset: max(0, innerHeight - visualViewport.height) — full keyboard\n                overlap height. 0 on desktop / Android (layout already resized).\n    --vvh     : visualViewport.height (always set) — for fixed keyboard-aware\n                shells sized exactly to the visible band.\n    --vvt     : visualViewport.offsetTop (always set) — vertical offset of the\n                visible band; fixed overlays pin to (vvt, vvt+vvh).\n*/\n\n/* ---- Real consumer #1: app shell height (static/dashboard.css `.app`) ---- */\n.app {\n  height: 100vh;\n  height: var(--app-vh, 100dvh); /* shrinks with the keyboard; dvh fallback w/o JS */\n}\n\n/* ---- Real consumer #2: body-scroll room on mobile (static/dashboard.css,\n   @media max-width:640px `.app`) — adds keyboard-height of bottom padding so\n   trailing form fields/buttons can scroll clear of the keyboard ---- */\n@media (max-width: 640px) {\n  .app {\n    padding-bottom: var(--kb-inset, 0px);\n    scroll-padding-bottom: var(--kb-inset, 0px);\n  }\n}\n\n/*\n  ---- Other documented consumers (not duplicated here; live in their own\n       feature CSS) ----\n    * routes/_app.tsx sets body bottom padding to var(--kb-inset) for tall\n      scrollable public doc pages (quote / contract / invoice sign pages).\n    * Keyboard-aware fixed shells (login/verify card, chat composer) size to\n      `height: var(--vvh)` + `transform: translateY(var(--vvt))` so they overlay\n      exactly the band above the keyboard.\n*/\n", "cssName": "mobile-viewport.css", "source": "import { useEffect } from \"preact/hooks\";\n\n/** Nearest scrollable ancestor (overflow-y auto/scroll with real overflow),\n *  e.g. the chat thread's `.chat__scroll`. Returns null for plain body-scroll\n *  pages so the caller falls back to scrolling the window. */\nfunction scrollableAncestor(el: HTMLElement | null): HTMLElement | null {\n  let node = el?.parentElement ?? null;\n  while (node) {\n    const oy = getComputedStyle(node).overflowY;\n    if (\n      (oy === \"auto\" || oy === \"scroll\") &&\n      node.scrollHeight > node.clientHeight + 1\n    ) {\n      return node;\n    }\n    node = node.parentElement;\n  }\n  return null;\n}\n\n/**\n * Mirrors the visual viewport into two CSS custom properties so every page\n * can stay clear of the on-screen keyboard. Mounted once globally (routes/\n * _app.tsx). Renders nothing; harmless on desktop (keyboard inset is 0).\n *\n *   --app-vh   = visualViewport.height — the height ABOVE the keyboard.\n *   --kb-inset = keyboard overlap height (layout height − visual height).\n *\n * iOS Safari overlays the keyboard *without* shrinking `100dvh` or the\n * layout viewport, so two distinct problems arise:\n *\n *   1. Full-height shells (login card, chat shell) sized to `100dvh` stay\n *      full-screen, pushing inputs/buttons behind the keyboard. Sizing them\n *      to `--app-vh` keeps them in the space above it.\n *   2. Tall *scrollable* pages (the public agreement, quote, invoice) can\n *      only scroll until the bottom content reaches the bottom of the layout\n *      viewport — which the keyboard covers — so the last ~keyboard-height of\n *      content (e.g. the sign-your-name field + Sign button) can't be\n *      scrolled into view. Adding `--kb-inset` of bottom padding to the body\n *      (see _app.tsx) extends the scroll range by exactly the keyboard height\n *      so that content can clear it.\n */\nexport default function MobileViewport() {\n  useEffect(() => {\n    const vv = globalThis.visualViewport;\n    if (!vv) return;\n    const root = document.documentElement;\n    // Set while a field is focused so viewport-resize events (keyboard settling)\n    // re-run the reveal; cleared on blur / after the settle window.\n    let activeCorrect: (() => void) | null = null;\n    let focusUntil = 0;\n    const apply = () => {\n      // How much the keyboard overlaps the layout viewport.\n      const overlap = globalThis.innerHeight - vv.height;\n      // Visual-viewport overlay vars. Keyboard-sensitive shells (login/verify\n      // card, chat) position themselves as `position:fixed; top:0; left:0;\n      // right:0; height:var(--vvh); transform:translateY(var(--vvt))` so they\n      // overlay EXACTLY the region the user can see above the keyboard.\n      //\n      // Measured on real iOS 18 WebKit: opening the keyboard leaves `100dvh`\n      // unchanged AND scrolls the page down by `visualViewport.offsetTop`\n      // (e.g. height 410, offsetTop 124). A normal-flow shell anchored at\n      // layout y=0 therefore can't line up with the visible band no matter\n      // what height we give it — a centered card ends up jammed high with a\n      // dead gap below (the \"weird gap\"). Pinning a fixed overlay to\n      // (offsetTop, offsetTop+height) lines it up exactly: centered cards\n      // center in the visible band, bottom-anchored composers hug the keyboard.\n      root.style.setProperty(\"--vvh\", `${vv.height}px`);\n      root.style.setProperty(\"--vvt\", `${vv.offsetTop}px`);\n      if (overlap > 2) {\n        // Back-compat for any remaining `min-height:var(--app-vh)` consumers\n        // (scrollable public doc pages): track the visual viewport height.\n        root.style.setProperty(\"--app-vh\", `${vv.height}px`);\n      } else {\n        // No keyboard, OR Android where interactive-widget=resizes-content has\n        // already shrunk the layout viewport. Defer to native 100dvh (the CSS\n        // fallback): it tracks the keyboard animation smoothly in lockstep,\n        // whereas writing a JS pixel height only lands at the *end* of the\n        // animation — which made the chat shell jump and briefly stranded the\n        // composer behind the keyboard. Removing the override lets the shell\n        // follow dvh frame-for-frame.\n        root.style.removeProperty(\"--app-vh\");\n      }\n      // Keyboard inset = bottom scroll-room added to body-scroll pages so the\n      // last screenful of content (the focused field + its submit button) can be\n      // scrolled clear of the keyboard. It must be the FULL keyboard overlap:\n      // iOS often offsets the visual viewport (vv.offsetTop large) so the\n      // previous `overlap - offsetTop` collapsed to a few px, starving the page\n      // of the room needed to lift the trailing button. 0 on Android (layout\n      // already resized) and on desktop.\n      root.style.setProperty(\n        \"--kb-inset\",\n        `${Math.max(0, overlap)}px`,\n      );\n      // While a field is focused and the keyboard is still settling, re-run the\n      // reveal on every viewport change. The keyboard animation can outlast any\n      // fixed timeout (notably in the simulator), so a one-shot scroll measured\n      // mid-animation under-scrolls; reacting to the resize that ACTUALLY lands\n      // the smaller viewport is what makes the trailing submit reliably clear.\n      if (activeCorrect && Date.now() < focusUntil) activeCorrect();\n    };\n    apply();\n    vv.addEventListener(\"resize\", apply);\n    vv.addEventListener(\"scroll\", apply);\n\n    // Keep the focused field clear of the keyboard. iOS's native\n    // scroll-into-view is unreliable inside our nested scroll containers (the\n    // chat thread) and under the fixed/translated shells — it left wizard\n    // inputs (e.g. the customer Phone Number field) and their action buttons\n    // clipped behind the keyboard. After focus + the keyboard settling, we\n    // explicitly center the field in its scroll container, which sits entirely\n    // above the keyboard, so it (and the row/buttons around it) is always\n    // visible. Covers every input on every page from one place.\n    let focusTimer = 0;\n    const onFocusIn = (e: FocusEvent) => {\n      const t = e.target as HTMLElement | null;\n      if (\n        !t ||\n        typeof t.matches !== \"function\" ||\n        !t.matches(\"input, textarea, select, [contenteditable='true']\")\n      ) {\n        return;\n      }\n      globalThis.clearTimeout(focusTimer);\n      try {\n          // Reveal the field — and, if a submit/primary button trails it in the\n          // same form (the contract \"sign\" button, the wizard \"Next\"), keep the\n          // BUTTON clear of the keyboard too. Centering alone clamps at the page\n          // bottom (the button is the last element) and iOS's layout viewport\n          // doesn't fully exclude the keyboard, so scrollIntoView can't lift it.\n          // Compute the exact overshoot past the visible band (visualViewport)\n          // and scroll precisely that much.\n          const group = t.closest(\"form, [data-kbd-group]\");\n          let target: Element = t;\n          if (group) {\n            // The primary action (sign / \"I sent it\" / Next) is the last button\n            // that FOLLOWS the field in the group. Revealing it keeps the field\n            // (just above it) visible too. Method-picker buttons precede the\n            // field, so they're correctly ignored.\n            const after = Array.from(group.querySelectorAll(\"button\")).filter(\n              (b) =>\n                (t.compareDocumentPosition(b) &\n                  Node.DOCUMENT_POSITION_FOLLOWING) !== 0,\n            );\n            if (after.length) target = after[after.length - 1];\n          }\n          const sc = scrollableAncestor(target as HTMLElement);\n          // Lift the target clear of the keyboard. Runs repeatedly while the\n          // keyboard settles (see the resize-driven re-run in `apply()`), so each\n          // pass just measures the current layout and nudges toward the goal.\n          const correct = () => {\n            const r = target.getBoundingClientRect();\n            if (sc) {\n              // Nested scroll container (the chat thread): coords are relative\n              // to the container, so the overshoot nudge applies as-is.\n              const overshoot = r.bottom - (vv.height - 16);\n              if (overshoot > 1) sc.scrollBy({ top: overshoot, behavior: \"auto\" });\n              else if (r.top < 8) sc.scrollBy({ top: r.top - 8, behavior: \"auto\" });\n              return;\n            }\n            // Body-scroll page (public quote/contract/invoice). getBoundingClient-\n            // Rect is relative to the LAYOUT viewport (top = 0), but the band the\n            // user can actually see is [vv.offsetTop, vv.offsetTop + vv.height] —\n            // iOS offsets (and sometimes shrinks) the visual viewport when the\n            // keyboard opens. Convert into band coordinates by subtracting\n            // vv.offsetTop; the old code omitted this and so mis-measured every\n            // keyboard-open by exactly vv.offsetTop, leaving the field clipped\n            // off the top and the submit button stranded.\n            const topBand = vv.offsetTop;\n            const fieldTop = t.getBoundingClientRect().top - topBand;\n            const btnBot = r.bottom - topBand;\n            let delta = 0;\n            // 1) Lift the trailing submit if it sits below the visible band\n            //    (behind the keyboard).\n            if (btnBot > vv.height - 24) delta = btnBot - (vv.height - 24);\n            // 2) But never at the cost of clipping the focused field off the top —\n            //    if that scroll (or the current position) pushes the field above\n            //    the band, pull back so the field stays in view. This also lowers\n            //    content when iOS has over-scrolled the field above the fold.\n            if (fieldTop - delta < 8) delta = fieldTop - 8;\n            if (Math.abs(delta) > 1) {\n              globalThis.scrollBy({ top: delta, behavior: \"auto\" });\n            }\n          };\n          // Register so `apply()` re-runs the reveal on every viewport change\n          // until the keyboard has settled, and add fixed fallbacks in case the\n          // resize event doesn't fire (desktop, or engines that don't shrink the\n          // visual viewport).\n          activeCorrect = correct;\n          // Long enough to catch the keyboard settling in stages — the predictive-\n          // text bar appears a beat after the keyboard, shrinking the viewport a\n          // second time; a short window would close before that lands.\n          focusUntil = Date.now() + 2600;\n          for (const ms of [60, 250, 500, 800, 1200, 1800, 2400]) {\n            globalThis.setTimeout(() => {\n              if (activeCorrect === correct) correct();\n            }, ms);\n          }\n      } catch {\n        /* older engines: best-effort, native behavior still applies */\n      }\n    };\n    const onFocusOut = () => {\n      // Stop re-correcting once the field is blurred (keyboard closing).\n      activeCorrect = null;\n      focusUntil = 0;\n    };\n    document.addEventListener(\"focusin\", onFocusIn);\n    document.addEventListener(\"focusout\", onFocusOut);\n    // Android (interactive-widget=resizes-content) resizes the *layout*\n    // viewport when the keyboard opens, which fires window.resize but NOT\n    // always visualViewport.resize — so without this --app-vh went stale and\n    // the chat shell stayed full-height, dropping the composer behind the\n    // keyboard with a dead gap. iOS only changes the visual viewport, caught\n    // above. Listening to both covers every platform.\n    globalThis.addEventListener(\"resize\", apply);\n    return () => {\n      vv.removeEventListener(\"resize\", apply);\n      vv.removeEventListener(\"scroll\", apply);\n      globalThis.removeEventListener(\"resize\", apply);\n      document.removeEventListener(\"focusin\", onFocusIn);\n      document.removeEventListener(\"focusout\", onFocusOut);\n      globalThis.clearTimeout(focusTimer);\n      root.style.removeProperty(\"--app-vh\");\n      root.style.removeProperty(\"--kb-inset\");\n      root.style.removeProperty(\"--vvh\");\n      root.style.removeProperty(\"--vvt\");\n    };\n  }, []);\n  return null;\n}\n", "sourceName": "MobileViewport.tsx", "checklist": "# Capture checklist — MobileViewport\n\n**Nothing to screenshot** — this island renders no DOM. It is verified by\nobserving the CSS custom properties on `<html>` and the focus-reveal scroll\nbehavior, not by pixels.\n\n## How to verify (instead of cropping)\n**Auth:** dev master OTP `000000`. Best on the **iOS Simulator** (idb, py3.9\nvenv; force the soft keyboard with cmd-K off so the on-screen keyboard shows).\n\n1. **keyboard-open** — open a page with a text field near the bottom (e.g.\n   `/settings`, or a public sign page `/c/<id>`). Focus the field; the keyboard\n   slides up. In devtools/console, read:\n   `getComputedStyle(document.documentElement).getPropertyValue('--app-vh')` →\n   should equal `visualViewport.height`px; `--kb-inset` should be the full\n   overlap; `--vvh`/`--vvt` should mirror the visual viewport. Confirm the field\n   AND its trailing submit button stay above the keyboard.\n2. **desktop** — same page on desktop: `--app-vh` absent (falls back to 100dvh),\n   `--kb-inset` = `0px`.\n3. **unmount** — navigate away; confirm all four properties are removed from `<html>`.\n\n## Notes\n- Cache-bust openurl when re-testing in the simulator.\n- No motion to film; the value is smooth keyboard tracking via the dvh fallback\n  (no JS-driven height snap).\n"}, {"name": "quote-track", "component": "QuoteTrack", "spec": "# QuoteTrack\n\nCollapsible \"track\" group (numbered section + count + collapsible body) used to\ngroup pipeline stages on the Quotes, Invoices and Payments pages.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/QuoteTrack.tsx`).\n- **Interaction tier:** `island` (client-only state) — a controlled disclosure.\n- **Client state owned:**\n  - `open: boolean` — section expanded/collapsed. Seeded from\n    `localStorage[storageKey]` (`\"1\"`/`\"0\"`) if a `storageKey` is given, else\n    `defaultOpen`. Persisted to that key on every change (so a contractor's\n    open/closed preference per track survives reloads).\n- **Data source:** none of its own — it's a layout/disclosure shell. `count`,\n  `title`, `num`, `unit` are **props** supplied by the parent page island; the\n  body content comes via `children`.\n- **Honest-empty:** the count label always renders (e.g. \"0 quotes\"); the body\n  may be empty if the parent passes no children — the parent is responsible for\n  its own empty state inside the body.\n- **Liveness:** none (request-response via parent). No polling/websocket.\n- **Anti-patterns:** none. State is local + persisted; no `location.reload`.\n  (Note: `count` is a frozen prop — if the parent page island re-fetches, it must\n  re-render QuoteTrack with the new count; QuoteTrack does not fetch.)\n- **Reactivity / i18n:** `lang` is a prop (defaults `\"en\"`); the count label uses\n  `tFor(lang, \"quoteTrack.count.{one|other}\", { n })` UNLESS a `unit` prop is\n  given, in which case it builds `\"{count} {unit}\"`/`\"{count} {unit}s\"` directly\n  (used by /invoices and /payments which want non-\"quote\" wording).\n- **Data-shape hazards:**\n  - `unit` pluralization is naive English (`unit + \"s\"`) — fine for\n    \"invoice\"/\"payment\", wrong for irregular nouns. The localized path (no\n    `unit`) only handles the `quote` unit.\n  - `storageKey` collisions across pages would share open-state — parents must\n    pass distinct keys per track.\n\n## 2. Anatomy\n```\n<section class=\"qtrack [qtrack--collapsed]\">\n  <header class=\"qtrack__head\" onClick=toggle>\n    <span class=\"qtrack__chev\"><I chev size=14 sw=2.5/></span>   ← rotates 90deg open / 0deg collapsed\n    <span class=\"qtrack__num\">{num}</span>                       ← e.g. \"01\"\n    <span class=\"qtrack__title\">{title}</span>                   ← e.g. \"Out for response\"\n    <span class=\"qtrack__count\">{count} {unit}s | tFor(...)}</span>\n  </header>\n  <div class=\"qtrack__body\">\n    <div class=\"qtrack__body-inner\">{children}</div>             ← cards/rows slot\n  </div>\n</section>\n```\n- **Slots/children:** yes — `children` render inside `.qtrack__body-inner` (the\n  parent page passes the track's cards/rows here).\n- **Icon dependency:** `I` + `ICN.chev` from `lib/dash-icons.tsx` (copied to\n  `js/dash-icons.tsx`).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `num` | string (required) | — | text | no |\n| `title` | string (required) | — | text | no |\n| `count` | number (required) | — | number | no |\n| `unit` | string | `undefined` (falls back to localized \"quote\") | text | no |\n| `defaultOpen` | boolean | `true` | boolean | no |\n| `storageKey` | string | `undefined` (no persistence) | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n| `children` | ComponentChildren | — | (slot) | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| open | expanded body (`defaultOpen=true`) | `cases/open/open.json` |\n| collapsed | `qtrack--collapsed`, body height 0 | `cases/collapsed/collapsed.json` |\n| custom-unit | `unit=\"invoice\"` (Invoices/Payments wording) | `cases/custom-unit/custom-unit.json` |\n| zero | `count=0` → \"0 quotes\" | `cases/zero/zero.json` |\n| es | Spanish localized count | `cases/es/es.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"header.qtrack__head\" && e.type === \"click\")` →\n  toggles `open`; writes `localStorage[storageKey]` when `storageKey` set.\n\n## 6. Motion (extracted)\n- **Chevron:** `transform: rotate(90deg→0deg)` over `240ms var(--ease-bounce)`\n  (bouncy) when collapsing.\n- **Body:** `grid-template-rows: 1fr → 0fr` (+ `margin-top 14px→0`) over `320ms\n  var(--ease-out)`. This is the height-animation-without-fixed-px technique:\n  the inner `.qtrack__body-inner` is `overflow:hidden; min-height:0` so it clips\n  smoothly as the row track shrinks.\n  - **Jank finding:** animating `grid-template-rows` is supported in modern\n    engines and is smooth here; older Safari ignored it (snap, not jank). The\n    `.qtrack__head` border-color transition (200ms) on hover is trivial.\n- **Reduced motion:** no component-local guard — relies on the global tokens\n  reduced-motion clamp. Verify the collapse becomes instant.\n\n## 7. Responsive\n- No own `@media` queries. The header is a flex row that wraps naturally; the\n  body inherits the parent grid (`.qcards`, etc.) responsiveness. Verify within\n  the host page's quotes.css breakpoints.\n\n## 8. A11y\n- **Gaps:** the disclosure header is a `<header>` with an `onClick`, NOT a\n  `<button>` — it is not keyboard-focusable or operable, and exposes no\n  `aria-expanded`/`aria-controls`. Rebuild fix: make the header a `<button>`\n  (or add `role=\"button\"` + `tabindex=0` + key handlers) with\n  `aria-expanded={open}` and `aria-controls` pointing at the body id. `user-select:none`\n  is set to avoid text-selection on click.\n\n## 9. Used on\nShared across **3 page islands**: `islands/QuotesPage.tsx`,\n`islands/InvoicesPage.tsx`, `islands/PaymentsPage.tsx`. Evidence: grep of\n`QuoteTrack` import. CSS lives in `static/quotes.css` (Invoices/Payments pages\nreuse the quotes track styles + the `unit` prop to relabel).\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"quote-track\",\n  \"controls\": {\n    \"num\": { \"type\": \"text\", \"value\": \"01\" },\n    \"title\": { \"type\": \"text\", \"value\": \"Out for response\" },\n    \"count\": { \"type\": \"number\", \"value\": 3 },\n    \"unit\": { \"type\": \"text\", \"value\": \"\" },\n    \"defaultOpen\": { \"type\": \"boolean\", \"value\": true },\n    \"storageKey\": { \"type\": \"text\", \"value\": \"pm:qtrack:1\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "collapsed.json", "content": "{\n  \"_name\": \"Collapsed — Track 02 (body clipped)\",\n  \"num\": \"02\",\n  \"title\": \"Drafts\",\n  \"count\": 5,\n  \"defaultOpen\": false,\n  \"storageKey\": \"pm:qtrack:2\",\n  \"lang\": \"en\",\n  \"_signals\": { \"open\": false },\n  \"_innerHtml\": \"<div class=\\\"qcards\\\">[ hidden body ]</div>\"\n}\n"}, {"name": "custom-unit.json", "content": "{\n  \"_name\": \"Custom unit — Invoices/Payments wording\",\n  \"num\": \"01\",\n  \"title\": \"Awaiting payment\",\n  \"count\": 1,\n  \"unit\": \"invoice\",\n  \"defaultOpen\": true,\n  \"storageKey\": \"pm:itrack:1\",\n  \"lang\": \"en\",\n  \"_signals\": { \"open\": true }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish localized count\",\n  \"num\": \"01\",\n  \"title\": \"En espera de respuesta\",\n  \"count\": 2,\n  \"defaultOpen\": true,\n  \"lang\": \"es\",\n  \"_signals\": { \"open\": true }\n}\n"}, {"name": "open.json", "content": "{\n  \"_name\": \"Open — Track 01 expanded\",\n  \"num\": \"01\",\n  \"title\": \"Out for response\",\n  \"count\": 3,\n  \"defaultOpen\": true,\n  \"storageKey\": \"pm:qtrack:1\",\n  \"lang\": \"en\",\n  \"_signals\": { \"open\": true },\n  \"_innerHtml\": \"<div class=\\\"qcards\\\" style=\\\"opacity:.4\\\">[ quote cards slot ]</div>\"\n}\n"}, {"name": "zero.json", "content": "{\n  \"_name\": \"Zero — \\\"0 quotes\\\"\",\n  \"num\": \"03\",\n  \"title\": \"Closed\",\n  \"count\": 0,\n  \"defaultOpen\": true,\n  \"lang\": \"en\",\n  \"_signals\": { \"open\": true }\n}\n"}], "css": "/* ============================================================\n   QuoteTrack — extracted from static/quotes.css\n   Tokens: --brand-teal, --brand-pink, --fg-muted, --font-heading, --font-body,\n   --dur-fast, --ease-out, --ease-bounce.\n   ============================================================ */\n\n.qtrack {\n  margin-bottom: 28px;\n}\n\n.qtrack__head {\n  display: flex;\n  align-items: baseline;\n  gap: 12px;\n  margin-bottom: 14px;\n  cursor: pointer;\n  user-select: none;\n  padding: 6px 0;\n  border-bottom: 1px solid rgba(26, 83, 92, 0.08);\n  transition: border-color var(--dur-fast) var(--ease-out);\n}\n.qtrack__head:hover {\n  border-color: rgba(26, 83, 92, 0.2);\n}\n\n.qtrack__chev {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 18px;\n  height: 18px;\n  color: var(--brand-teal);\n  transition: transform 240ms var(--ease-bounce);\n  transform: rotate(90deg);          /* open: chevron points down */\n}\n.qtrack--collapsed .qtrack__chev {\n  transform: rotate(0deg);           /* collapsed: chevron points right */\n}\n\n.qtrack__num {\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 800;\n  letter-spacing: 0.1em;\n  color: var(--brand-pink);\n}\n.qtrack__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 20px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  line-height: 1;\n}\n.qtrack__count {\n  font-family: var(--font-body);\n  font-size: 13px;\n  color: var(--fg-muted);\n  margin-left: auto;\n}\n\n/* Collapsible body — animates via grid-template-rows 1fr -> 0fr */\n.qtrack__body {\n  display: grid;\n  grid-template-rows: 1fr;\n  transition:\n    grid-template-rows 320ms var(--ease-out),\n    margin-top 320ms var(--ease-out);\n  margin-top: 14px;\n}\n.qtrack__body-inner {\n  overflow: hidden;\n  min-height: 0;\n}\n.qtrack--collapsed .qtrack__body {\n  grid-template-rows: 0fr;\n  margin-top: 0;\n}\n\n/* No component-local reduced-motion block — relies on the global tokens\n   reduced-motion clamp for the chevron rotate + grid-rows collapse. */\n", "cssName": "quote-track.css", "source": "/**\n * Collapsible track group for the quotes pipeline. Track 1 (Out for response)\n * defaults to open; tracks 2 and 3 collapsed. Open state persists per track in\n * localStorage so contractors who always work on drafts get them open on next\n * visit.\n */\nimport type { ComponentChildren } from \"preact\";\nimport { useEffect, useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  num: string;\n  title: string;\n  count: number;\n  /** Singular word used in the count label (\"0 quotes\" / \"1 quote\"). The\n   *  component is reused by /invoices and /payments which want different\n   *  wording. When omitted, falls back to the localized \"quote\" unit. */\n  unit?: string;\n  defaultOpen?: boolean;\n  storageKey?: string;\n  lang?: \"en\" | \"es\";\n  children?: ComponentChildren;\n}\n\nexport default function QuoteTrack(\n  {\n    num,\n    title,\n    count,\n    unit,\n    defaultOpen = true,\n    storageKey,\n    lang = \"en\",\n    children,\n  }: Props,\n) {\n  const [open, setOpen] = useState<boolean>(() => {\n    if (typeof globalThis.localStorage === \"undefined\" || !storageKey) {\n      return defaultOpen;\n    }\n    const raw = globalThis.localStorage.getItem(storageKey);\n    if (raw === \"1\") return true;\n    if (raw === \"0\") return false;\n    return defaultOpen;\n  });\n\n  useEffect(() => {\n    if (!storageKey) return;\n    try {\n      globalThis.localStorage?.setItem(storageKey, open ? \"1\" : \"0\");\n    } catch { /* ignore */ }\n  }, [open, storageKey]);\n\n  return (\n    <section class={`qtrack ${open ? \"\" : \"qtrack--collapsed\"}`}>\n      <header class=\"qtrack__head\" onClick={() => setOpen((v) => !v)}>\n        <span class=\"qtrack__chev\">\n          <I d={ICN.chev} size={14} sw={2.5} />\n        </span>\n        <span class=\"qtrack__num\">{num}</span>\n        <span class=\"qtrack__title\">{title}</span>\n        <span class=\"qtrack__count\">\n          {unit\n            ? `${count} ${count === 1 ? unit : `${unit}s`}`\n            : tFor(\n              lang,\n              `quoteTrack.count.${count === 1 ? \"one\" : \"other\"}`,\n              { n: count },\n            )}\n        </span>\n      </header>\n      <div class=\"qtrack__body\">\n        <div class=\"qtrack__body-inner\">\n          {children}\n        </div>\n      </div>\n    </section>\n  );\n}\n", "sourceName": "QuoteTrack.tsx", "checklist": "# Capture checklist — QuoteTrack\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/quotes` (default — localized \"quote\" unit; Track 01\n  \"Out for response\" defaults open, Tracks 02/03 collapsed).\n- `http://localhost:5280/invoices` and `/payments` for the `unit` variants.\n\n## Viewports\n- **1280px** and **640px** (header + body grid reflow per quotes.css).\n\n## Element(s) to crop\n- A single `<section class=\"qtrack\">` (header row + body cards). Capture both an\n  expanded and a `qtrack--collapsed` instance.\n\n## Transient states to drive\n1. **open** — Track 01 expanded with cards in the body.\n2. **collapsed** — click `.qtrack__head` to collapse (chevron rotates to point\n   right; body clips to 0). Reload to confirm `localStorage[storageKey]`\n   persisted the closed state.\n3. **custom-unit** — /invoices or /payments → count reads \"1 invoice\" / \"N\n   payments\".\n4. **zero** — a track with `count=0` → \"0 quotes\".\n5. **es** — Spanish UI language → localized count label.\n\n## Motion to film\n- Collapse/expand: chevron rotate (240ms bounce) + body `grid-template-rows\n  1fr→0fr` (+ margin) over 320ms ease-out. Film one toggle. Re-shoot with\n  `prefers-reduced-motion: reduce` (relies on global clamp — verify it stills).\n"}, {"name": "skeletons", "component": "Skeletons", "spec": "# Skeletons\n\nShared loading-skeleton primitives. Static module exporting several shimmer\nshapes used as the SSR'd first render by the per-page top-level islands (which\nfetch on mount, so initial paint = loading).\n\n## 1. Classification & behavior\n- **Bucket:** `static` (`components/Skeletons.tsx` — pure presentational, no\n  island, no state, no fetch).\n- **Interaction tier:** `static` (renders identically on server and client; no\n  events, no PRG, no signals).\n- **Exports (5):**\n  - `ShimmerStyle()` — renders a `<style>` injecting the `@keyframes pmShimmer`.\n    Must be rendered once on any page that uses skeletons (the keyframe is not in\n    a stylesheet).\n  - `SkelBlock({ h, w?, r?, mt? })` — the atomic shimmer rectangle.\n  - `PageHeaderSkeleton()` — title bar + subtitle (two stacked blocks).\n  - `CardGridSkeleton({ rows? })` — two `.panel`s in a `.grid`, each a header\n    block + `rows` row-blocks.\n  - `ListSkeleton({ rows? })` — single `.panel`, header block + `rows` rows.\n- **Data source:** none — purely shape-based. **Honest-empty:** N/A (it *is* the\n  pre-data state; replaces the old literal \"Loading X…\" text to avoid the empty\n  blink the audit flagged on cold-route navigations).\n- **Liveness:** none. **Anti-patterns:** none.\n- **Data-shape hazards:** the composite skeletons depend on the host page's\n  `.grid`/`.panel` CSS existing (they reuse the real layout classes so the\n  skeleton matches loaded content and avoids layout shift). The shimmer\n  keyframe must be present (`<ShimmerStyle/>`), or blocks render as flat grey.\n\n## 2. Anatomy\n- `SkelBlock` → a single `<div>` with inline `height/width/border-radius/\n  margin-top` + the shimmer gradient/animation.\n- `PageHeaderSkeleton` → `<section>` with `SkelBlock h=32 w=60%` then\n  `SkelBlock h=14 w=40% mt=12`.\n- `CardGridSkeleton` → `<div class=\"grid\">` containing two `<div class=\"panel\">`,\n  each with `SkelBlock h=20 w=40%` then `rows`×`SkelBlock h=56` (first `mt=18`,\n  rest `mt=12`).\n- `ListSkeleton` → `<div class=\"panel\">` with `SkelBlock h=20 w=30%` then\n  `rows`×`SkelBlock h=64` (first `mt=18`, rest `mt=12`).\n- **Slots/children:** none.\n\n## 3. Props\n`SkelBlock`:\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `h` | number (px, required) | — | number | no |\n| `w` | string (CSS width) | `\"100%\"` | text | no |\n| `r` | number (px radius) | `8` | number | no |\n| `mt` | number (px margin-top) | `0` | number | no |\n\n`CardGridSkeleton` / `ListSkeleton`:\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `rows` | number | `4` (grid) / `5` (list) | number | no |\n\n`PageHeaderSkeleton` / `ShimmerStyle`: no props.\n\n## 4. States → cases\nEach export is documented as its own variant/case (no behavioral states — these\nare single-render shapes).\n| variant | meaning | case |\n|---|---|---|\n| skel-block | the atomic block (`h=56`) | `cases/skel-block/skel-block.json` |\n| page-header | title + subtitle header | `cases/page-header/page-header.json` |\n| card-grid | two-column panel grid (used by Dashboard/Quotes/Clients/Contracts/Settings) | `cases/card-grid/card-grid.json` |\n| list | single-column list (used by Payments/Invoices) | `cases/list/list.json` |\n\n## 5. Events\n- None. Static, non-interactive (`ev` — no emitted events).\n\n## 6. Motion (extracted)\n- **`pmShimmer`** keyframes: `background-position 200% 0 → -200% 0`, applied as\n  `animation: pmShimmer 1.4s ease-in-out infinite` over a 3-stop horizontal\n  linear-gradient (`#eef2ed → #f6f7f3 → #eef2ed`, `background-size:200% 100%`).\n  Produces a left-to-right sheen.\n  - **Jank finding:** animating `background-position` is cheap (compositor-ish,\n    no layout) — fine. Many blocks animate in sync (shared keyframe) which reads\n    as one coherent sweep.\n- **Reduced motion:** NO component-local guard. Relies on the global tokens\n  `@media (prefers-reduced-motion: reduce)` clamp. Fix candidate: add a local\n  `prefers-reduced-motion` rule that sets `animation:none` and a static grey, so\n  the skeleton doesn't depend on the global being loaded.\n\n## 7. Responsive\n- No own `@media`. Responsiveness comes from the borrowed `.grid` (collapses to\n  one column at `<=640px` per dashboard.css) and `.panel` shells. `SkelBlock`\n  widths are %-based so they fluidly scale.\n\n## 8. A11y\n- No `role=\"status\"`/`aria-busy` or `aria-hidden` is set — the shimmer blocks are\n  bare `<div>`s announced as nothing. Acceptable (decorative), but a rebuild\n  could add `aria-hidden=\"true\"` on the blocks and `aria-busy=\"true\"` /\n  `role=\"status\"` on the container for SR users. No focusable elements.\n\n## 9. Used on\nUsed by **7 page islands** as their loading state:\n`islands/DashboardPage.tsx`, `QuotesPage.tsx`, `InvoicesPage.tsx`,\n`PaymentsPage.tsx`, `ContractsPage.tsx`, `ClientsPage.tsx`, `SettingsPage.tsx`.\nEvidence: grep of `Skeletons` import. Shared primitive (CardGridSkeleton on the\ngrid pages, ListSkeleton on Payments/Invoices).\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"skeletons\",\n  \"controls\": {\n    \"_variant\": {\n      \"type\": \"select\",\n      \"value\": \"card-grid\",\n      \"options\": [\"skel-block\", \"page-header\", \"card-grid\", \"list\"]\n    },\n    \"h\": { \"type\": \"number\", \"value\": 56 },\n    \"w\": { \"type\": \"text\", \"value\": \"100%\" },\n    \"r\": { \"type\": \"number\", \"value\": 8 },\n    \"mt\": { \"type\": \"number\", \"value\": 0 },\n    \"rows\": { \"type\": \"number\", \"value\": 4 }\n  }\n}\n", "cases": [{"name": "card-grid.json", "content": "{\n  \"_name\": \"CardGridSkeleton — two-column panels\",\n  \"_variant\": \"card-grid\",\n  \"rows\": 4,\n  \"_mocks\": { \"ShimmerStyle\": \"stub\" }\n}\n"}, {"name": "list.json", "content": "{\n  \"_name\": \"ListSkeleton — single-column rows\",\n  \"_variant\": \"list\",\n  \"rows\": 5,\n  \"_mocks\": { \"ShimmerStyle\": \"stub\" }\n}\n"}, {"name": "page-header.json", "content": "{\n  \"_name\": \"PageHeaderSkeleton — title + subtitle\",\n  \"_variant\": \"page-header\",\n  \"_mocks\": { \"ShimmerStyle\": \"stub\" }\n}\n"}, {"name": "skel-block.json", "content": "{\n  \"_name\": \"SkelBlock — atomic shimmer rectangle\",\n  \"_variant\": \"skel-block\",\n  \"h\": 56,\n  \"w\": \"100%\",\n  \"r\": 8,\n  \"mt\": 0,\n  \"_mocks\": { \"ShimmerStyle\": \"stub\" }\n}\n"}], "css": "/* ============================================================\n   Skeletons — the shimmer CSS is NOT in any feature stylesheet; it is INLINED\n   in components/Skeletons.tsx:\n     * the @keyframes is injected via <ShimmerStyle/> (a <style> element).\n     * each block's gradient/animation is an inline `style=\"\"` string (SHIMMER).\n   Reproduced here verbatim so the primitives are rebuildable from CSS.\n\n   NOT tokenized — uses literal greys (#eef2ed / #f6f7f3), close to mint-200.\n   ============================================================ */\n\n/* Injected by <ShimmerStyle/> (component renders this <style> once per page) */\n@keyframes pmShimmer {\n  0%   { background-position: 200% 0; }\n  100% { background-position: -200% 0; }\n}\n\n/* The SHIMMER constant applied inline to every SkelBlock */\n.skel-block {\n  background: linear-gradient(90deg, #eef2ed 0%, #f6f7f3 50%, #eef2ed 100%);\n  background-size: 200% 100%;\n  animation: pmShimmer 1.4s ease-in-out infinite;\n  /* height / width / border-radius / margin-top are set per-instance inline:\n     height:{h}px; width:{w ?? 100%}; border-radius:{r ?? 8}px; margin-top:{mt ?? 0}px */\n}\n\n/* No reduced-motion guard is shipped with this component — the global tokens\n   reduced-motion block (animation-duration: 0.01ms !important) is what stops\n   the shimmer. Rebuild should keep relying on that global, or add a local\n   @media (prefers-reduced-motion: reduce){ .skel-block{ animation:none } }. */\n\n/*\n  Layout classes the composite skeletons REUSE from the page's own feature CSS\n  (NOT defined here — they come from static/dashboard.css / quotes.css / etc.):\n    .grid   — two-column responsive panel grid (CardGridSkeleton)\n    .panel  — raised card surface (CardGridSkeleton, ListSkeleton)\n  The skeleton deliberately borrows the real layout shells so its shape matches\n  the loaded content and there's no layout shift on swap-in.\n*/\n", "cssName": "skeletons.css", "source": "/**\n * Shared loading-skeleton primitives used by the per-page top-level islands\n * (DashboardPage, ClientsPage, QuotesPage, …). The page islands fetch on\n * mount, so the SSR'd first render is the loading state — replacing the\n * literal \"Loading X…\" line with a layout-shaped skeleton avoids the empty\n * blink the audit flagged on cold-route navigations.\n */\n\nconst SHIMMER_KEYFRAMES =\n  `@keyframes pmShimmer{0%{background-position:200% 0}100%{background-position:-200% 0}}`;\n\nconst SHIMMER =\n  \"background:linear-gradient(90deg,#eef2ed 0%,#f6f7f3 50%,#eef2ed 100%);background-size:200% 100%;animation:pmShimmer 1.4s ease-in-out infinite\";\n\nexport function ShimmerStyle() {\n  return <style>{SHIMMER_KEYFRAMES}</style>;\n}\n\ninterface SkelBlockProps {\n  h: number;\n  w?: string;\n  r?: number;\n  mt?: number;\n}\n\nexport function SkelBlock({ h, w, r = 8, mt = 0 }: SkelBlockProps) {\n  return (\n    <div\n      style={`height:${h}px;width:${\n        w ?? \"100%\"\n      };border-radius:${r}px;${SHIMMER};margin-top:${mt}px`}\n    />\n  );\n}\n\n/** Shared header skeleton: title, sub-text, two-row chip strip. */\nexport function PageHeaderSkeleton() {\n  return (\n    <section style=\"margin-bottom:18px\">\n      <SkelBlock h={32} w=\"60%\" />\n      <SkelBlock h={14} w=\"40%\" mt={12} />\n    </section>\n  );\n}\n\n/** Generic two-column card grid skeleton — matches `.grid` panel layouts. */\nexport function CardGridSkeleton({ rows = 4 }: { rows?: number }) {\n  return (\n    <div class=\"grid\">\n      <div class=\"panel\">\n        <SkelBlock h={20} w=\"40%\" />\n        {Array.from({ length: rows }).map((_, i) => (\n          <SkelBlock key={i} h={56} mt={i === 0 ? 18 : 12} />\n        ))}\n      </div>\n      <div class=\"panel\">\n        <SkelBlock h={20} w=\"40%\" />\n        {Array.from({ length: rows }).map((_, i) => (\n          <SkelBlock key={i} h={56} mt={i === 0 ? 18 : 12} />\n        ))}\n      </div>\n    </div>\n  );\n}\n\n/** Single-column list skeleton for /payments, /invoices etc. */\nexport function ListSkeleton({ rows = 5 }: { rows?: number }) {\n  return (\n    <div class=\"panel\">\n      <SkelBlock h={20} w=\"30%\" />\n      {Array.from({ length: rows }).map((_, i) => (\n        <SkelBlock key={i} h={64} mt={i === 0 ? 18 : 12} />\n      ))}\n    </div>\n  );\n}\n", "sourceName": "Skeletons.tsx", "checklist": "# Capture checklist — Skeletons\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## How to drive (these are the cold-load first paint)\nThe page islands fetch on mount, so the skeleton shows for the brief moment\nbefore data arrives. To capture reliably, throttle the network (DevTools → Slow\n3G) or pause the relevant `clients/*.ts` fetch, then hard-navigate to the route.\n\n## Routes / URLs (which variant shows where)\n- **card-grid** → `http://localhost:5280/dashboard` (also /quotes, /clients,\n  /contracts, /settings).\n- **list** → `http://localhost:5280/payments` (also /invoices).\n- **page-header** + **skel-block** are sub-parts visible within the above; or\n  render them directly in isolate.\n\n## Viewports (borrowed from `.grid`/`.panel`)\n- **1280px** (two-column grid).\n- **640px** (grid collapses to one column).\n\n## Element(s) to crop\n- The full skeleton region inside `.content` (header + grid/list).\n\n## Transient states to drive\n1. **card-grid** — throttled load of /dashboard before data lands.\n2. **list** — throttled load of /payments.\n3. **page-header** / **skel-block** — via isolate variant control.\n\n## Motion to film\n- `pmShimmer` left-to-right sheen (1.4s loop). Film one full cycle. Re-shoot with\n  `prefers-reduced-motion: reduce` to confirm the global clamp stills it (this\n  component ships no local reduced-motion guard).\n"}, {"name": "ui-brand", "component": "UiBrand", "spec": "# ui/Brand\n\nWordmark: a pink rounded \"P\" mark + the text \"Paperwork Monster\", linking to\n`/dashboard`.\n\n## 1. Classification & behavior\n- **Bucket:** `static` (`components/ui/Brand.tsx` — pure presentational link).\n- **Interaction tier:** `static`. It's an `<a href=\"/dashboard\">` — navigation\n  only, no state/events/fetch.\n- **Data source / liveness / mutations:** none. **Anti-patterns:** none.\n- **Data-shape hazards:**\n  - The wordmark text \"Paperwork Monster\" is a **hard-coded literal** in the\n    component — NOT i18n'd (unlike `DashSidebar`, which uses `brand.name`). If\n    the rebuild wants localization, swap to `tFor(lang,\"brand.name\")`.\n  - Styling depends on the page having loaded **verify.css** (only that file\n    defines `.brand__mark`). On a landing.css-only surface the mark won't style\n    (landing's `.brand` expects an `<img>`). See css/ hazard.\n\n## 2. Anatomy\n```\n<a class=\"brand\" href=\"/dashboard\" [style=\"color:#fff\" when inverse]>\n  <span class=\"brand__mark\">P</span>                       ← pink 32x32 rounded tile, white \"P\"\n  <span [style=\"font-size:16px\" when size===\"sm\"]>Paperwork Monster</span>\n</a>\n```\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `size` | `\"sm\"\\|\"md\"` | `\"md\"` | select | no |\n| `inverse` | boolean | `false` | boolean | no |\n\n- `size=\"sm\"` shrinks only the wordmark text to 16px (mark stays 32×32).\n- `inverse=true` sets the link color to `#fff` (for dark backgrounds).\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| md | default size, normal color | `cases/md/md.json` |\n| sm | small wordmark | `cases/sm/sm.json` |\n| inverse | white text (dark bg) | `cases/inverse/inverse.json` |\n\n## 5. Events\n- None emitted. `ev` — navigation only on click (it's a link to `/dashboard`).\n\n## 6. Motion\n- None. (The wrapping surface may add hover styles, but the component declares no\n  transitions/animation.) No reduced-motion concern.\n\n## 7. Responsive\n- No `@media`. Fixed sizes; `size=\"sm\"` is the only size lever.\n\n## 8. A11y\n- Real `<a>` — focusable, keyboard-activatable. The \"P\" mark span has no\n  `aria-hidden`; SR reads \"P Paperwork Monster\". Minor: add `aria-hidden=\"true\"`\n  to `.brand__mark` so the mark isn't announced as a stray letter. No explicit\n  `aria-label` (link text suffices).\n\n## 9. Used on\n- **Only consumer:** `components/AppNav.tsx`. But **AppNav has zero imports**\n  anywhere (orphaned). So `ui/Brand` is **transitively dead code** — present in\n  the design system, reachable only through an unused component. Evidence: grep\n  of `ui/Brand` (1 hit: AppNav) + grep of `AppNav` (0 hits). Flag for adoption or\n  removal. (Authenticated pages render their brand via `DashSidebar`'s\n  `.sb__brand`, not this primitive.)\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"ui-brand\",\n  \"controls\": {\n    \"size\": { \"type\": \"select\", \"value\": \"md\", \"options\": [\"sm\", \"md\"] },\n    \"inverse\": { \"type\": \"boolean\", \"value\": false }\n  }\n}\n", "cases": [{"name": "inverse.json", "content": "{\n  \"_name\": \"Inverse (white text, dark bg)\",\n  \"size\": \"md\",\n  \"inverse\": true,\n  \"_note\": \"place on a dark surface in isolate to see the white wordmark\"\n}\n"}, {"name": "md.json", "content": "{\n  \"_name\": \"Medium (default)\",\n  \"size\": \"md\",\n  \"inverse\": false\n}\n"}, {"name": "sm.json", "content": "{\n  \"_name\": \"Small wordmark\",\n  \"size\": \"sm\",\n  \"inverse\": false\n}\n"}], "css": "/* ============================================================\n   ui/Brand — wordmark. Emits `.brand` (link) + `.brand__mark` (the \"P\" tile).\n   Canonical CSS: static/verify.css (the ONLY definition with `.brand__mark`,\n   which is the exact structure this component renders). landing.css also has a\n   `.brand` but a DIFFERENT structure (img logo + `span em`), not this component.\n   Tokens: --brand-pink, --fg, --font-heading.\n   ============================================================ */\n\n/* ---------- CANONICAL: static/verify.css ---------- */\n.brand {\n  display: inline-flex;\n  align-items: center;\n  gap: 10px;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 18px;\n  color: var(--fg);            /* overridden inline to #fff when inverse */\n}\n.brand__mark {\n  width: 32px;\n  height: 32px;\n  border-radius: 9px;\n  background: var(--brand-pink);\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-weight: 800;\n}\n\n/* Inline prop-driven overrides applied by Brand.tsx (NOT in CSS):\n     inverse -> style=\"color:#fff\"  on the <a>\n     size=\"sm\" -> style=\"font-size:16px\" on the wordmark <span>\n   (size=\"md\" leaves the 18px .brand default; the mark stays 32x32 either way) */\n\n/* ---------- DIVERGENT (landing.css `.brand`, different markup — for reference)\n.brand { display:flex; align-items:center; gap:10px; font-family:var(--font-heading);\n         font-weight:800; font-size:19px; color:var(--brand-teal);\n         letter-spacing:-0.02em; flex-shrink:0; }\n.brand img { width:38px; height:38px; display:block; }     // uses an <img>, not .brand__mark\n.brand span em { font-style:normal; color:var(--brand-green); }\n   -- ui/Brand does NOT render an <img> or an <em>; it renders <span class=\"brand__mark\">P</span>,\n      so it only looks correct on a surface that loads verify.css. */\n", "cssName": "ui-brand.css", "source": "interface Props {\n  size?: \"sm\" | \"md\";\n  inverse?: boolean;\n}\n\nexport function Brand({ size = \"md\", inverse = false }: Props) {\n  return (\n    <a class=\"brand\" href=\"/dashboard\" style={inverse ? \"color:#fff\" : undefined}>\n      <span class=\"brand__mark\">P</span>\n      <span style={size === \"sm\" ? \"font-size:16px\" : undefined}>\n        Paperwork Monster\n      </span>\n    </a>\n  );\n}\n", "sourceName": "Brand.tsx", "checklist": "# Capture checklist — ui/Brand\n\n**Theme:** light only.\n**Caveat:** only consumer is the orphaned `components/AppNav.tsx` — there is no\nlive route rendering it. Capture via **isolate** (load verify.css for correct\n`.brand__mark` styling).\n\n## Viewports\n- One desktop width — no responsive behavior.\n\n## Element(s) to crop\n- The `<a class=\"brand\">` (mark + wordmark), tight.\n\n## States to drive\n1. **md** — default, teal/`--fg` text, pink \"P\" mark.\n2. **sm** — wordmark text at 16px (mark unchanged).\n3. **inverse** — render on a dark/teal background so the white wordmark shows.\n\n## Motion\n- None.\n"}, {"name": "ui-button", "component": "UiButton", "spec": "# ui/Button (Button + AnchorButton)\n\nDesign-system button primitive. Exports a `<button>` (`Button`) and an\n`<a>` (`AnchorButton`) sharing the same variant/size class composition.\n\n## 1. Classification & behavior\n- **Bucket:** `static` (`components/ui/Button.tsx` — stateless presentational).\n- **Interaction tier:** `static` (renders a plain element; behavior comes from\n  the consumer's `onClick`/`href` spread via `...rest`). No island, no state.\n- **Class composition:** `classes(variant, size, extra)` →\n  `[\"btn\", \"btn-{variant}\", size===\"lg\" ? \"btn-lg\" : \"\", extra].filter(Boolean).join(\" \")`.\n  So `<Button variant=\"outline\" size=\"lg\" class=\"w-full\">` →\n  `class=\"btn btn-outline btn-lg w-full\"`.\n- **Data source / liveness / mutations:** none. Pure prop → markup.\n- **Anti-patterns:** none in the component. **But:** the styling is\n  surface-dependent — `.btn-*` is defined differently on the auth/app surface\n  (`verify.css`, canonical, all 3 variants) vs the marketing surface\n  (`landing.css`, pink restyle, NO ghost). The component does not import its own\n  CSS; it inherits whatever `.btn-*` the page loaded. See css/ for the hazard.\n- **Data-shape hazards:** `Omit<…, \"size\" | \"class\">` — native `size`/`class` are\n  overridden by the typed props; everything else (`onClick`, `disabled`,\n  `type`, `href`, `target`, `aria-*`) passes through `...rest`. `disabled`\n  styling only exists in the verify.css set (`.btn[disabled]`).\n\n## 2. Anatomy\n```\nButton:       <button class=\"btn btn-{variant} [btn-lg] [extra]\" {...rest}>{children}</button>\nAnchorButton: <a      class=\"btn btn-{variant} [btn-lg] [extra]\" {...rest}>{children}</a>\n```\n- **Slots/children:** `children` is the label (text + optional leading icon — the\n  `.btn` rule sets `gap:8px` for an inline icon).\n\n## 3. Props (both Button & AnchorButton)\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `variant` | `\"primary\"\\|\"outline\"\\|\"ghost\"` | `\"primary\"` | select | no |\n| `size` | `\"md\"\\|\"lg\"` | `\"md\"` | select | no |\n| `class` | string | `undefined` | text | no |\n| `children` | ComponentChildren | — | (slot) | no |\n| `...rest` | button/anchor attrs (`onClick`, `href`, `disabled`, `type`, …) | — | (passthrough) | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| primary | default solid (pink) | `cases/primary/primary.json` |\n| outline | white + border | `cases/outline/outline.json` |\n| ghost | transparent (verify.css only) | `cases/ghost/ghost.json` |\n| lg | large size | `cases/lg/lg.json` |\n| disabled | `disabled` attr (Button only) | `cases/disabled/disabled.json` |\n| anchor | AnchorButton with href | `cases/anchor/anchor.json` |\n\n## 5. Events\n- Emits nothing itself. The consumer wires events:\n  `ev.expect(e => e.source === \"button.btn\" && e.type === \"click\")` — only when\n  the consumer passes `onClick`. `disabled` Button suppresses click.\n\n## 6. Motion (extracted, canonical verify.css)\n- `.btn` transitions `transform 120ms ease, background 200ms ease, color 200ms`.\n- `.btn:active { transform: translateY(1px) }` — press nudge.\n- Marketing (landing.css) variant differs: `transition: all 200ms\n  var(--ease-bounce)`, hover `translateY(-1px)` + shadow growth, active\n  `scale(.97)`.\n- **Reduced motion:** none local; global tokens clamp covers it.\n\n## 7. Responsive\n- No `@media`. Size is fixed by `md`/`lg`; width follows content unless the\n  consumer adds a width class via `class`.\n\n## 8. A11y\n- Renders a real `<button>` / `<a>` — native semantics, focusable, keyboard\n  operable. `disabled` sets the native attribute (+ `.btn[disabled]` dims it).\n- AnchorButton has no `role=\"button\"` (it's a true link — correct when it\n  navigates). Ensure consumers don't use AnchorButton for non-navigation\n  actions. Focus ring inherited from global `--shadow-focus` token usage.\n\n## 9. Used on\n**CONFIRMED UNUSED.** Grep for `ui/Button` across `routes/`, `islands/`,\n`components/` returns **zero imports**. This is a built-but-unwired design-system\nprimitive. (The app currently hand-writes `<button class=\"btn …\">` / `<a>` and\nuses the legacy `components/Button.tsx` only inside the Counter denostories\ndemo.) Spec'd as shared per the design-system intent; flag as dead code pending\nadoption or removal.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"ui-button\",\n  \"controls\": {\n    \"variant\": { \"type\": \"select\", \"value\": \"primary\", \"options\": [\"primary\", \"outline\", \"ghost\"] },\n    \"size\": { \"type\": \"select\", \"value\": \"md\", \"options\": [\"md\", \"lg\"] },\n    \"class\": { \"type\": \"text\", \"value\": \"\" },\n    \"_label\": { \"type\": \"text\", \"value\": \"Send quote\" }\n  }\n}\n", "cases": [{"name": "anchor.json", "content": "{\n  \"_name\": \"AnchorButton (link)\",\n  \"_name_note\": \"render the AnchorButton export, not Button\",\n  \"variant\": \"outline\",\n  \"size\": \"lg\",\n  \"href\": \"/dashboard\",\n  \"_innerHtml\": \"Go to dashboard\"\n}\n"}, {"name": "disabled.json", "content": "{\n  \"_name\": \"Disabled\",\n  \"variant\": \"primary\",\n  \"size\": \"md\",\n  \"disabled\": true,\n  \"_innerHtml\": \"Sending…\"\n}\n"}, {"name": "ghost.json", "content": "{\n  \"_name\": \"Ghost (md) — verify.css surface only\",\n  \"variant\": \"ghost\",\n  \"size\": \"md\",\n  \"_innerHtml\": \"Cancel\"\n}\n"}, {"name": "lg.json", "content": "{\n  \"_name\": \"Primary large\",\n  \"variant\": \"primary\",\n  \"size\": \"lg\",\n  \"_innerHtml\": \"Get started\"\n}\n"}, {"name": "outline.json", "content": "{\n  \"_name\": \"Outline (md)\",\n  \"variant\": \"outline\",\n  \"size\": \"md\",\n  \"_innerHtml\": \"Save draft\"\n}\n"}, {"name": "primary.json", "content": "{\n  \"_name\": \"Primary (md)\",\n  \"variant\": \"primary\",\n  \"size\": \"md\",\n  \"_innerHtml\": \"Send quote\"\n}\n"}], "css": "/* ============================================================\n   ui/Button — design-system Button + AnchorButton.\n   Emits classes: `btn`, `btn-{primary|outline|ghost}`, `btn-lg`.\n\n   HAZARD: there are TWO live definitions of these classes in the codebase.\n   The component's variant API (primary|outline|ghost) maps cleanly to the\n   verify.css set (the only one with all three + disabled), so THAT is the\n   canonical rebuild target. The landing.css set is a divergent MARKETING\n   restyle (pink primary, no ghost) loaded only on the public/landing surface.\n   A third, UNRELATED set in dashboard.css uses BEM `.btn--quote/.btn--ghost/\n   .btn--accent` (double-dash) and is NOT this component — ignore it.\n   ============================================================ */\n\n/* ---------- CANONICAL: static/verify.css (auth/app surface) ---------- */\n.btn {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  padding: 10px 18px;\n  border-radius: var(--radius-pill);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 15px;\n  transition: transform 120ms ease, background 200ms ease, color 200ms ease;\n}\n.btn:active { transform: translateY(1px); }\n\n.btn-primary {\n  background: var(--accent);          /* brand-pink */\n  color: #fff;\n  box-shadow: var(--shadow-sm);\n}\n.btn-primary:hover { background: var(--accent-hover); }\n\n.btn-outline {\n  background: #fff;\n  color: var(--fg);\n  border: 2px solid var(--border-strong);\n}\n.btn-outline:hover { border-color: var(--brand-teal); }\n\n.btn-ghost {\n  background: transparent;\n  color: var(--fg);\n}\n.btn-ghost:hover { background: var(--bg-sunken); }\n\n.btn-lg {\n  padding: 14px 24px;\n  font-size: 16px;\n}\n\n.btn[disabled] {\n  opacity: 0.55;\n  cursor: not-allowed;\n}\n\n/* ---------- DIVERGENT (do not merge): static/landing.css marketing restyle ----------\n.btn { display:inline-flex; align-items:center; gap:8px;\n       font-family:var(--font-heading); font-weight:700; border:none; cursor:pointer;\n       white-space:nowrap; transition:all 200ms var(--ease-bounce); text-decoration:none; }\n.btn-primary { background:var(--brand-pink); color:#fff; padding:12px 22px;\n               border-radius:var(--radius-lg); font-size:15px;\n               box-shadow:0 8px 20px rgba(255,107,107,.32); }\n.btn-primary:hover { background:var(--pink-600); transform:translateY(-1px);\n                     box-shadow:0 12px 28px rgba(255,107,107,.4); }\n.btn-primary:active { transform:scale(.97); }\n.btn-lg { padding:16px 28px; font-size:16px; border-radius:18px; }\n.btn-outline { background:#fff; color:var(--brand-teal); padding:16px 26px;\n               border-radius:18px; border:1.5px solid var(--border-strong); font-size:16px; }\n.btn-outline:hover { border-color:var(--brand-teal); transform:translateY(-1px); }\n   -- NOTE: landing.css has NO .btn-ghost; ghost variant only renders correctly\n      on a surface that loads verify.css.\n*/\n", "cssName": "ui-button.css", "source": "import type { ComponentChildren, JSX } from \"preact\";\n\ntype Variant = \"primary\" | \"outline\" | \"ghost\";\n\ntype ButtonAttrs = JSX.IntrinsicElements[\"button\"];\ntype AnchorAttrs = JSX.IntrinsicElements[\"a\"];\n\nexport interface ButtonProps extends Omit<ButtonAttrs, \"size\" | \"class\"> {\n  variant?: Variant;\n  size?: \"md\" | \"lg\";\n  class?: string;\n  children?: ComponentChildren;\n}\n\nfunction classes(variant: Variant, size: \"md\" | \"lg\", extra?: string): string {\n  return [\"btn\", `btn-${variant}`, size === \"lg\" ? \"btn-lg\" : \"\", extra ?? \"\"]\n    .filter(Boolean).join(\" \");\n}\n\nexport function Button(\n  { variant = \"primary\", size = \"md\", class: cls, children, ...rest }:\n    ButtonProps,\n) {\n  return (\n    <button class={classes(variant, size, cls)} {...rest}>{children}</button>\n  );\n}\n\nexport interface AnchorButtonProps extends Omit<AnchorAttrs, \"size\" | \"class\"> {\n  variant?: Variant;\n  size?: \"md\" | \"lg\";\n  class?: string;\n  children?: ComponentChildren;\n}\n\nexport function AnchorButton(\n  { variant = \"primary\", size = \"md\", class: cls, children, ...rest }:\n    AnchorButtonProps,\n) {\n  return <a class={classes(variant, size, cls)} {...rest}>{children}</a>;\n}\n", "sourceName": "Button.tsx", "checklist": "# Capture checklist — ui/Button\n\n**Theme:** light only.\n**Caveat:** this component is currently UNUSED in the running app (zero imports),\nso there is no live route that renders it. Capture via **isolate** only, or\ntemporarily drop it into a scratch route.\n\n## Surface dependency (important)\n`.btn-*` styling differs by which feature CSS is loaded:\n- Load **verify.css** for the canonical look (all 3 variants + disabled).\n- Load **landing.css** to see the marketing restyle (pink, no ghost). Shoot both\n  if documenting the divergence.\n\n## Viewports\n- One desktop width (e.g. **1280px**) is sufficient — no responsive behavior.\n\n## Element(s) to crop\n- Each button variant individually (tight crop), plus a hover and `:active` frame.\n\n## Transient states to drive\n1. **primary / outline / ghost** at `size=md`.\n2. **lg** size.\n3. **disabled** (Button) — dimmed, `cursor:not-allowed`.\n4. **anchor** — AnchorButton with `href`.\n5. **hover** + **:active** (press nudge `translateY(1px)` on verify.css).\n\n## Motion to film\n- Hover/active transitions (120–200ms). On the landing.css surface, the bounce\n  hover-lift + active scale. Re-shoot with reduced motion (global clamp).\n"}, {"name": "ui-icon", "component": "UiIcon", "spec": "# ui/Icon\n\nMinimal stroke-only SVG icon set. Each icon is a 24×24 `viewBox`, rendered as a\n`currentColor` stroke (`fill:none`, `stroke-width:1.7`, round caps/joins).\n\n## 1. Classification & behavior\n- **Bucket:** `static` (`components/ui/Icon.tsx` — pure presentational).\n- **Interaction tier:** `static`. No state, no events, no fetch.\n- **How it renders:** looks up `PATHS[name]` (a `Record<Name, JSX.Element>`) and\n  wraps it in an `<svg>` with the fixed attributes documented in css/. `size`\n  sets both width & height; `color` is inherited via `stroke=\"currentColor\"`.\n- **Data source / liveness / mutations:** none. **Anti-patterns:** none.\n- **Data-shape hazards:**\n  - `name` is a closed union of **16** values — passing anything else is a type\n    error and `PATHS[name]` would be `undefined` (renders an empty svg).\n  - This is a SEPARATE icon system from `lib/dash-icons.tsx` (`I`/`ICN`, ~50\n    glyphs, `stroke-width:2`). Do not conflate; names don't all overlap.\n\n## 2. Anatomy\n```\n<svg width={size} height={size} viewBox=\"0 0 24 24\" fill=\"none\"\n     stroke=\"currentColor\" stroke-width={1.7} stroke-linecap=\"round\"\n     stroke-linejoin=\"round\" class={cls} {...rest}>\n  {PATHS[name]}   ← one or more <path>/<circle>/<rect> per icon\n</svg>\n```\n- **Slots/children:** none (glyph is selected by `name`).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `name` | union (see options) | — (required) | select | no |\n| `size` | number (px) | `18` | number | no |\n| `class` | string | `\"\"` | text | no |\n| `...rest` | SVG attrs (`aria-hidden`, `style`, …) | — | passthrough | no |\n\n**`name` select options (the full union, 16):**\n`home`, `doc`, `file-text`, `receipt`, `chat`, `users`, `settings`, `bell`,\n`search`, `send`, `mic`, `image`, `check`, `chevron-right`, `menu`, `logo`.\n\n## 4. States → cases\nNo behavioral states — just glyph/size variants.\n| state | meaning | case |\n|---|---|---|\n| default | `name=\"home\"`, size 18 | `cases/default/default.json` |\n| sized | larger `size` | `cases/sized/sized.json` |\n| colored | inherits parent `color` | `cases/colored/colored.json` |\n| all-glyphs | render the full set as a sheet | `cases/all-glyphs/all-glyphs.json` |\n\n## 5. Events\n- None emitted. (Consumers wrap it in a button and own the click.)\n\n## 6. Motion\n- None. Static SVG. No transitions/animation/reduced-motion concern (any motion\n  is from a parent, e.g. a hover color transition on the wrapping button).\n\n## 7. Responsive\n- No `@media`. Scales only via the `size` prop; remains crisp at any size (vector).\n\n## 8. A11y\n- Decorative by default — no `role`/`aria-label`. Consumers should pass\n  `aria-hidden=\"true\"` (icon-only buttons) via `...rest` and label the parent\n  control, OR add `role=\"img\"` + `aria-label` when the icon conveys meaning\n  standalone. `currentColor` keeps contrast tied to the surrounding text color.\n\n## 9. Used on\n- **Live consumer:** `islands/Composer.tsx` (the Assistant chat composer) — uses\n  `name=\"image\"`, `name=\"mic\"`, `name=\"send\"`.\n- **Dead consumer:** `components/AppNav.tsx` (uses `home/doc/file-text/receipt/\n  chat/users/settings`) — but **AppNav itself has zero imports** (orphaned), so\n  those names are only referenced by dead code.\n- Evidence: grep of `ui/Icon` import. Shared primitive; effectively used live\n  only by Composer today.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"ui-icon\",\n  \"controls\": {\n    \"name\": {\n      \"type\": \"select\",\n      \"value\": \"home\",\n      \"options\": [\"home\", \"doc\", \"file-text\", \"receipt\", \"chat\", \"users\", \"settings\", \"bell\", \"search\", \"send\", \"mic\", \"image\", \"check\", \"chevron-right\", \"menu\", \"logo\"]\n    },\n    \"size\": { \"type\": \"number\", \"value\": 18 },\n    \"class\": { \"type\": \"text\", \"value\": \"\" }\n  }\n}\n", "cases": [{"name": "all-glyphs.json", "content": "{\n  \"_name\": \"Full set sheet (render every name)\",\n  \"_note\": \"harness should map the 16-name union into a grid, each at size 24\",\n  \"_names\": [\"home\", \"doc\", \"file-text\", \"receipt\", \"chat\", \"users\", \"settings\", \"bell\", \"search\", \"send\", \"mic\", \"image\", \"check\", \"chevron-right\", \"menu\", \"logo\"],\n  \"size\": 24\n}\n"}, {"name": "colored.json", "content": "{\n  \"_name\": \"Inherits parent color (green via wrapper)\",\n  \"name\": \"check\",\n  \"size\": 24,\n  \"style\": \"color:#519843\"\n}\n"}, {"name": "default.json", "content": "{\n  \"_name\": \"Default — home @ 18\",\n  \"name\": \"home\",\n  \"size\": 18\n}\n"}, {"name": "sized.json", "content": "{\n  \"_name\": \"Larger — send @ 32\",\n  \"name\": \"send\",\n  \"size\": 32\n}\n"}], "css": "/* ============================================================\n   ui/Icon — has NO stylesheet rules. It is a presentational <svg> whose\n   appearance is set entirely by SVG attributes in components/ui/Icon.tsx.\n   This file documents the rendering contract so it can be reproduced.\n\n   Per-icon rendered attributes (hard-coded in the component, NOT overridable):\n     width  = {size}            (default 18)\n     height = {size}            (default 18)\n     viewBox = \"0 0 24 24\"      (24x24 grid)\n     fill   = \"none\"            (stroke-only)\n     stroke = \"currentColor\"    (inherits text color of the parent)\n     stroke-width = 1.7\n     stroke-linecap = \"round\"\n     stroke-linejoin = \"round\"\n     class  = {cls} (from `class` prop; default \"\")\n     ...rest spreads remaining SVG attrs (e.g. aria-hidden)\n\n   So to recolor an icon, set `color` on the parent; to resize, pass `size`.\n   ============================================================ */\n\n/* Example consumer pattern (no rules required for the component itself):\n   .composer__icon-btn { color: var(--brand-teal); }   // icon inherits this\n   .composer__icon-btn:hover { color: var(--brand-green); }\n*/\n\n/* NOTE: this is a DIFFERENT, smaller icon set than lib/dash-icons.tsx (the `I`\n   renderer + `ICN` map used by the dashboard rail/topbar/quote-track). ui/Icon\n   has 16 names + a single `name` union; dash-icons has ~50 glyphs. They are not\n   interchangeable. ui/Icon also uses stroke-width 1.7 (thinner) vs dash-icons'\n   default sw=2. */\n", "cssName": "ui-icon.css", "source": "/**\n * Minimal icon set. Each icon is a 24×24 stroke-only SVG.\n * Add more as the UI grows; keep them dependency-free.\n */\nimport type { JSX } from \"preact\";\n\ntype Name =\n  | \"home\"\n  | \"doc\"\n  | \"file-text\"\n  | \"receipt\"\n  | \"chat\"\n  | \"users\"\n  | \"settings\"\n  | \"bell\"\n  | \"search\"\n  | \"send\"\n  | \"mic\"\n  | \"image\"\n  | \"check\"\n  | \"chevron-right\"\n  | \"menu\"\n  | \"logo\";\n\ninterface Props\n  extends Omit<JSX.HTMLAttributes<SVGSVGElement>, \"name\" | \"size\"> {\n  name: Name;\n  size?: number;\n}\n\nconst PATHS: Record<Name, JSX.Element> = {\n  home: <path d=\"M3 11l9-8 9 8M5 10v10h14V10\" />,\n  doc: <path d=\"M7 3h7l5 5v13H7zM14 3v5h5\" />,\n  \"file-text\": (\n    <>\n      <path d=\"M7 3h7l5 5v13H7z\" />\n      <path d=\"M9 13h8M9 17h8M9 9h4\" />\n    </>\n  ),\n  receipt: (\n    <>\n      <path d=\"M6 3h12v18l-3-2-3 2-3-2-3 2z\" />\n      <path d=\"M9 8h6M9 12h6\" />\n    </>\n  ),\n  chat: (\n    <path d=\"M21 15a4 4 0 0 1-4 4H8l-5 3v-13a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4z\" />\n  ),\n  users: (\n    <>\n      <circle cx=\"9\" cy=\"8\" r=\"3\" />\n      <circle cx=\"17\" cy=\"9\" r=\"2\" />\n      <path d=\"M3 20c0-3 3-5 6-5s6 2 6 5M14 20c0-2 2-3 4-3s4 1 4 3\" />\n    </>\n  ),\n  settings: (\n    <>\n      <circle cx=\"12\" cy=\"12\" r=\"3\" />\n      <path d=\"M19 12a7 7 0 0 0-.1-1l2-1.5-2-3.5-2.4.8a7 7 0 0 0-1.7-1L14.5 3h-5L9.2 5.8a7 7 0 0 0-1.7 1L5 6l-2 3.5 2 1.5a7 7 0 0 0 0 2l-2 1.5 2 3.5 2.4-.8a7 7 0 0 0 1.7 1L9.5 21h5l.3-2.8a7 7 0 0 0 1.7-1l2.4.8 2-3.5-2-1.5a7 7 0 0 0 .1-1z\" />\n    </>\n  ),\n  bell: <path d=\"M6 16V11a6 6 0 0 1 12 0v5l2 2H4zM10 21a2 2 0 0 0 4 0\" />,\n  search: (\n    <>\n      <circle cx=\"11\" cy=\"11\" r=\"7\" />\n      <path d=\"M21 21l-4.5-4.5\" />\n    </>\n  ),\n  send: <path d=\"M22 2L11 13M22 2l-7 20-4-9-9-4z\" />,\n  mic: (\n    <>\n      <rect x=\"9\" y=\"3\" width=\"6\" height=\"12\" rx=\"3\" />\n      <path d=\"M5 11a7 7 0 0 0 14 0M12 18v3\" />\n    </>\n  ),\n  image: (\n    <>\n      <rect x=\"3\" y=\"3\" width=\"18\" height=\"18\" rx=\"2\" />\n      <circle cx=\"9\" cy=\"9\" r=\"2\" />\n      <path d=\"M21 15l-6-6-9 9\" />\n    </>\n  ),\n  check: <path d=\"M5 12l5 5L20 6\" />,\n  \"chevron-right\": <path d=\"M9 6l6 6-6 6\" />,\n  menu: <path d=\"M3 6h18M3 12h18M3 18h18\" />,\n  logo: <path d=\"M4 18h16l-2-9-3 3-3-6-3 6-3-3z\" />,\n};\n\nexport function Icon({ name, size = 18, class: cls = \"\", ...rest }: Props) {\n  return (\n    <svg\n      width={size}\n      height={size}\n      viewBox=\"0 0 24 24\"\n      fill=\"none\"\n      stroke=\"currentColor\"\n      stroke-width={1.7}\n      stroke-linecap=\"round\"\n      stroke-linejoin=\"round\"\n      class={cls}\n      {...rest}\n    >\n      {PATHS[name]}\n    </svg>\n  );\n}\n", "sourceName": "Icon.tsx", "checklist": "# Capture checklist — ui/Icon\n\n**Theme:** light only.\n\n## Where it renders live\n- `islands/Composer.tsx` (Assistant composer) — `image`, `mic`, `send` icons.\n  Route: `http://localhost:5280/assistant` (dev master OTP `000000`). The other\n  13 names only render via isolate or the orphaned AppNav.\n\n## Best captured via isolate\nRender the **all-glyphs** case to produce a single sheet of all 16 icons at\nsize 24 for the spec. Also shoot `default`, `sized`, `colored`.\n\n## Viewports\n- One width is enough (vector; no responsive behavior). Capture at 1x and 2x DPR\n  to confirm crispness.\n\n## Element(s) to crop\n- Individual icons (tight), plus the full 16-icon sheet.\n\n## States to drive\n1. **default** — `home` @ 18.\n2. **sized** — `send` @ 32.\n3. **colored** — wrap in a `color:#519843` parent → stroke turns green.\n4. **all-glyphs** — the full union grid.\n\n## Motion\n- None.\n"}, {"name": "ui-section-head", "component": "UiSectionHead", "spec": "# ui/SectionHead\n\nCentered marketing section header: optional eyebrow pill + `<h2>` + optional lede\nparagraph.\n\n## 1. Classification & behavior\n- **Bucket:** `static` (`components/ui/SectionHead.tsx` — pure presentational).\n- **Interaction tier:** `static`. No state, events, fetch, or motion.\n- **Data source / liveness / mutations:** none. **Anti-patterns:** none.\n- **Conditional rendering:** `eyebrow` and `lede` are optional — each is rendered\n  only when truthy (`eyebrow ? <span> : null`, `lede ? <p> : null`). `title` is\n  required and always renders.\n- **Data-shape hazards:**\n  - `lede` is `ComponentChildren` (not just string) — it can carry inline markup\n    (e.g. `<em>`, links). `title` is a plain `string`.\n  - Styling depends on which surface CSS is loaded (verify.css vs landing.css —\n    two visibly different looks; see css/). landing.css additionally renders a\n    leading dot via `.eyebrow-pill::before` and supports an accent `<h2><em>`.\n\n## 2. Anatomy\n```\n<div class=\"section-head\">\n  {eyebrow && <span class=\"eyebrow-pill\">{eyebrow}</span>}   ← uppercase green pill\n  <h2>{title}</h2>\n  {lede && <p class=\"lede\">{lede}</p>}                       ← muted 18px paragraph\n</div>\n```\n- **Slots/children:** `lede` acts as a slot (ComponentChildren). No `children` prop.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `eyebrow` | string | `undefined` | text | no |\n| `title` | string (required) | — | text | no |\n| `lede` | ComponentChildren | `undefined` | text | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| full | eyebrow + title + lede | `cases/full/full.json` |\n| no-eyebrow | title + lede only | `cases/no-eyebrow/no-eyebrow.json` |\n| title-only | just the h2 | `cases/title-only/title-only.json` |\n\n## 5. Events\n- None emitted (static, non-interactive).\n\n## 6. Motion\n- None in the component. (landing.css adds no animation either; the eyebrow dot\n  is static.) No reduced-motion concern.\n\n## 7. Responsive\n- `.section-head` is `max-width:720px; margin:auto; text-align:center` — fluidly\n  centered. On the landing.css variant the `<h2>` uses `clamp(32px,4.4vw,52px)`,\n  so the heading scales with viewport width. No explicit `@media`.\n\n## 8. A11y\n- Emits a proper `<h2>` (consumers must ensure heading-level order on the page —\n  it's hard-coded to h2, so it should sit under an h1). Eyebrow + lede are\n  decorative/supporting. No interactive elements. No issues beyond heading-order\n  discipline.\n\n## 9. Used on\n- **CONFIRMED UNUSED.** Grep for `ui/SectionHead` across the repo returns **zero\n  imports**. A built-but-unwired marketing primitive (the landing page composes\n  its section heads inline rather than via this component). Spec'd as shared per\n  intent; flag as dead code pending adoption or removal.\n", "fixture": "{\n  \"category\": \"shared-components\",\n  \"folder\": \"ui-section-head\",\n  \"controls\": {\n    \"eyebrow\": { \"type\": \"text\", \"value\": \"Why Paperwork Monster\" },\n    \"title\": { \"type\": \"text\", \"value\": \"Quotes, contracts, and invoices in one place\" },\n    \"lede\": { \"type\": \"text\", \"value\": \"Stop juggling tools. Send a quote, get it signed, collect payment — all from your phone.\" }\n  }\n}\n", "cases": [{"name": "full.json", "content": "{\n  \"_name\": \"Full — eyebrow + title + lede\",\n  \"eyebrow\": \"Why Paperwork Monster\",\n  \"title\": \"Quotes, contracts, and invoices in one place\",\n  \"lede\": \"Stop juggling tools. Send a quote, get it signed, collect payment — all from your phone.\"\n}\n"}, {"name": "no-eyebrow.json", "content": "{\n  \"_name\": \"No eyebrow — title + lede\",\n  \"title\": \"Built for contractors who hate paperwork\",\n  \"lede\": \"Everything you need to look professional and get paid faster.\"\n}\n"}, {"name": "title-only.json", "content": "{\n  \"_name\": \"Title only\",\n  \"title\": \"How it works\"\n}\n"}], "css": "/* ============================================================\n   ui/SectionHead — marketing section header. Emits `.section-head` wrapping a\n   `.eyebrow-pill` + <h2> + `<p class=\"lede\">`.\n   Two surfaces define these; the component renders `<p class=\"lede\">`, which\n   matches verify.css (`.section-head .lede`). landing.css styles `.section-head p`\n   (a fuller marketing restyle). Both are reproduced; pick per target surface.\n   Tokens: --green-50, --green-600, --green-100, --brand-teal, --brand-pink,\n   --fg-muted, --font-heading, --radius-pill.\n   ============================================================ */\n\n/* ---------- verify.css (matches `.lede` class) ---------- */\n.eyebrow-pill {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  background: var(--green-50);\n  color: var(--green-600);\n  border: 1px solid var(--green-100);\n  padding: 6px 12px;\n  border-radius: var(--radius-pill);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n}\n.section-head {\n  text-align: center;\n  max-width: 720px;\n  margin: 0 auto 40px;\n}\n.section-head h2 {\n  margin: 16px 0 10px;\n}\n.section-head .lede {\n  color: var(--fg-muted);\n  font-size: 18px;\n}\n\n/* ---------- landing.css (fuller marketing restyle — for reference) ----------\n.eyebrow-pill {\n  display:inline-flex; align-items:center; gap:8px;\n  background:var(--green-50); color:var(--green-600);\n  font-family:var(--font-heading); font-weight:800; font-size:12px;\n  letter-spacing:0.08em; text-transform:uppercase; padding:6px 14px;\n  border-radius:var(--radius-pill);\n}\n.eyebrow-pill::before { content:\"\"; width:6px; height:6px; border-radius:999px;\n  background:currentColor; }                              // a leading dot\n.section-head { text-align:center; max-width:720px; margin:0 auto 56px; }\n.section-head .eyebrow-pill { margin-bottom:18px; }\n.section-head h2 {\n  font-family:var(--font-heading); font-weight:800;\n  font-size:clamp(32px, 4.4vw, 52px); line-height:1.05;\n  letter-spacing:-0.025em; color:var(--brand-teal); margin:0 0 16px; text-wrap:balance;\n}\n.section-head p { font-size:18px; color:var(--fg-muted); margin:0; line-height:1.55; }\n.section-head h2 em { font-style:normal; color:var(--brand-pink); position:relative; }\n   -- NOTE: landing.css styles `.section-head p` (works since the component's lede\n      is a <p class=\"lede\">), adds an eyebrow-pill ::before dot, a larger clamped\n      h2, and accent <em> support. Heavier than the verify.css variant. */\n", "cssName": "ui-section-head.css", "source": "import type { ComponentChildren } from \"preact\";\n\ninterface Props {\n  eyebrow?: string;\n  title: string;\n  lede?: ComponentChildren;\n}\n\nexport function SectionHead({ eyebrow, title, lede }: Props) {\n  return (\n    <div class=\"section-head\">\n      {eyebrow ? <span class=\"eyebrow-pill\">{eyebrow}</span> : null}\n      <h2>{title}</h2>\n      {lede ? <p class=\"lede\">{lede}</p> : null}\n    </div>\n  );\n}\n", "sourceName": "SectionHead.tsx", "checklist": "# Capture checklist — ui/SectionHead\n\n**Theme:** light only.\n**Caveat:** UNUSED in the app (zero imports). No live route renders it — capture\nvia **isolate**. Load **landing.css** for the marketing look (clamped large h2 +\neyebrow dot) or **verify.css** for the lighter variant; shoot both if\ndocumenting the divergence.\n\n## Viewports\n- **1280px** and **640px** — to show the `clamp()` h2 scaling (landing.css\n  variant) and the 720px-max centered block.\n\n## Element(s) to crop\n- The full `.section-head` block (eyebrow pill + h2 + lede), centered.\n\n## States to drive\n1. **full** — eyebrow + title + lede.\n2. **no-eyebrow** — title + lede.\n3. **title-only** — just the h2.\n\n## Motion\n- None.\n"}], "pages": [{"name": "accounts-manager", "pageMd": "# Page — `/accounts-manager`\n\nSource route: `routes/accounts-manager/index.tsx` → `js/index.tsx` (39 lines).\n\n## Purpose — HONEST-EMPTY (stub)\nA **blank-slate** surface, separate from the contractor `/dashboard`. As of this\nsnapshot it renders the standard app-shell with **no content and no local\ncomponents**. The route's own doc comment says it plainly:\n\n> \"Uses the regular app shell — sidebar rail + topbar + main content — but the\n> sidebar carries NO nav tabs yet (showNav={false}) and the content area is a\n> blank slate. Build the accounts-manager surface out from the\n> `<div class=\"content\">` below, and add tabs back to the rail when there are\n> pages to point at.\"\n\nThere is **no island, no page-local component, and no extracted component CSS**\n— inventing any would misrepresent the source. This folder documents the shell\ncomposition only.\n\n## App-shell composition (order)\n```\n<Head> …Accounts Manager + /dashboard.css… </Head>\n<div class=\"app\">\n  <DashSidebar showNav={false} />     ← SHARED island, nav tabs OFF\n  <main class=\"main\">\n    <DashTopbar greetingName />       ← SHARED island, greeting only (NO greetingDate)\n    <div class=\"content\">\n      {/* Blank slate — build the accounts-manager view here. */}\n    </div>\n  </main>\n</div>\n```\nTwo deliberate shell differences vs `/settings` and `/admin`:\n- `DashSidebar` is mounted with **`showNav={false}`** → the nav rail renders no\n  tabs (rail chrome only). (See shared-components/dash-sidebar for the prop.)\n- `DashTopbar` receives **only `greetingName`** — no `greetingDate` is computed\n  or passed (the route doesn't build the weekday/month string the other two do).\n\n## `<Head>`\n- **Title:** literal `Accounts Manager` (NOT i18n'd, unlike settings/admin).\n- **CSS:** `<link rel=\"stylesheet\" href=\"/dashboard.css\" />` — shell + tokens\n  only; no content to style.\n\n## SSR data\n- `ctx.state.user` → `greetingName` and `lang` only. Nothing else fetched or\n  rendered.\n\n## Build order\n1. Mount the shared shell with `showNav={false}` and greeting-only topbar.\n2. Stop. The content area is intentionally empty — build future\n   accounts-manager components into `components/` and add them here, plus\n   restore sidebar nav tabs, when there are pages to point at.\n\n## Local components\n- **None.** Empty content area by design.\n", "components": []}, {"name": "admin", "pageMd": "# Page — `/admin`\n\nSource route: `routes/admin/index.tsx` → `js/index.tsx`.\nRoute guard: `routes/admin/_middleware.ts` → `js/_middleware.ts`.\n\n## Purpose\nSuper-admin operator console. Search users by phone, grant/revoke the\nsuper-admin flag, and impersonate a user (swaps the session cookie server-side,\nthen full-navigates to `/dashboard` as that user). The global\n`ImpersonationBanner` (shared) is the counterpart: once impersonating, it pins\nto the top of every page with a \"Return to your account\" button. English-only\non purpose — it does not touch the i18n dictionaries.\n\n## Auth / route gate (super-admin only)\n`_middleware.ts` resolves the user via `loadUser(ctx.req)` (`GET /me`) and:\n- no session → `302 /`,\n- logged in but `superAdmin !== true` → `302 /dashboard`,\n- else sets `ctx.state.user` and continues.\nThe flag is backend-resolved (can't be spoofed client-side), and every\n`/admin/*` endpoint re-checks it server-side. **Capture requires a super-admin\nsession** (dev master OTP `000000` logs in, but the account must carry the\nsuper-admin flag — see capture-checklist).\n\n## App-shell composition (order)\nIdentical shell to the other authed pages:\n```\n<Head> …Admin · {brand} + /dashboard.css… </Head>\n<div class=\"app\">\n  <DashSidebar active=\"admin\" />          ← SHARED island\n  <main class=\"main\">\n    <DashTopbar greetingDate greetingName />  ← SHARED island\n    <div class=\"content\">\n      <AdminPage />                       ← PAGE ISLAND (components/admin-page)\n    </div>\n  </main>\n</div>\n```\n\n## `<Head>`\n- **Title:** `Admin · {tFor(lang, \"brand.name\")}` (only the \"Admin\" prefix is a\n  literal; the brand name is i18n'd).\n- **CSS:** `<link rel=\"stylesheet\" href=\"/dashboard.css\" />` — used only for the\n  app-shell + tokens. AdminPage itself is entirely inline-styled (see\n  `components/admin-page/css/admin-page.css`).\n\n## SSR data\n- `ctx.state.user` (from the middleware) is used ONLY for the topbar greeting +\n  `lang`. **No admin data is server-rendered.** `AdminPage` fetches the user\n  list client-side on mount (empty query → server returns everyone, capped).\n\n## Sections (as AdminPage renders)\n1. **Heading** — \"Admin\" + one-line subtitle (English literals).\n2. **Search card** — `<form>` with a `type=\"search\"` phone box + Search button.\n3. **Error alert** (`role=\"alert\"`) — conditional.\n4. **Results card** (only when rows>0): an **Impersonate quick-select** row\n   (label + `<select>` of loaded users + Go) above a **users table** (Name /\n   Phone / Business / Super badge / Actions[Grant|Revoke + Impersonate]).\n5. **Loading** / **No users found** centered states.\n\n## Build order\n1. Shell + tokens (shared) + the `_middleware` super-admin gate.\n2. Build the `admin-page` island (see its `.md`): mount → list-all fetch →\n   table; search; per-row toggle/impersonate; quick-select; error/loading/empty.\n3. Verify against the shared `ImpersonationBanner` (the impersonate action's\n   visible counterpart on subsequent pages).\n\n## Local components\n- `components/admin-page/` ← `islands/AdminPage.tsx` (239 lines).\n", "components": [{"name": "admin-page", "component": "AdminPage", "spec": "# AdminPage\n\nThe whole `/admin` content area as a single island — a super-admin user console.\nSource: `islands/AdminPage.tsx` (239 lines). English-only operator tool;\nentirely inline-styled.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/AdminPage.tsx`).\n- **Interaction tier:** **whole-page island, client-only**, with three\n  server mutations. No form PRG (the search `<form onSubmit>` `preventDefault`s\n  and fetches), no Fresh Partial.\n- **Server actions + feedback:**\n  - **Search:** `adminClient.search(q.trim())` → `GET /admin/users?q=` →\n    `AdminUserView[]`. Submit button reads \"Searching…\" + `disabled` while\n    `loading`.\n  - **List-all on mount:** `useEffect` calls `search()` with empty `q` →\n    server returns everyone (capped). So the table is populated before any\n    typing.\n  - **Grant/Revoke:** `toggleSuper(u)` → `adminClient.grant(id)` /\n    `adminClient.revoke(id)` → `POST /admin/users/:id/{grant|revoke}` →\n    returns the updated `AdminUserView`, spliced into `rows` in place (no\n    refetch). Row button reads \"…\" while `busyId === u.id`.\n  - **Impersonate:** `impersonate(id)` → `adminClient.impersonate(id)` →\n    `POST /admin/impersonate/:id` (server swaps the `pm_session` cookie), then\n    `globalThis.location.href = \"/dashboard\"`.\n  - Errors from any action surface in a single `role=\"alert\"` banner above the\n    table (`error` state); the action's `busyId` is cleared on failure so the\n    UI re-enables.\n- **`location.href` flag (full navigation, NOT reload):** impersonate →\n  `/dashboard`. **Justified** — the session identity changed server-side, so the\n  whole app must rebuild as the target user. Same pattern (and same fix note) as\n  the shared `ImpersonationBanner`'s \"Return to your account\": the clean rebuild\n  is a server **PRG** (`303 → /dashboard`) instead of a client `location.href`,\n  avoiding a flash of the admin page as the new user. This is the impersonation\n  ENTRY; `ImpersonationBanner` is the EXIT — they are counterparts.\n- **Client state owned:** `q` (search), `rows: AdminUserView[]`, `loading`,\n  `busyId: string|null` (which row's action is in flight — gates ALL action\n  buttons), `error: string|null`, `impersonateId` (quick-select value).\n- **Data source + honest-empty:** single `GET /admin/users` (re-run on search).\n  Empty result → centered \"No users found.\" (and a separate \"Loading users…\"\n  while the first fetch is in flight with no rows yet).\n- **Liveness:** request-response only; no polling.\n- **Data-shape hazards:** `AdminUserView` is the search projection\n  (`id, name?, phoneNumber, businessName?, superAdmin`). Display name falls back\n  `name || businessName || \"Unnamed\"` (select) / `name || \"—\"` (table). Phones\n  run through `fmtPhone`. `busyId` is a single value, so **only one row action\n  can be in flight at a time** — every action button is disabled while any is\n  busy.\n\n## 2. Anatomy\n```\n<div style={wrap}>                                  ← .admin-wrap (max 980px)\n  <div>  <h1>Admin</h1>  <p>Search users, grant or revoke…</p>  ← .admin-head\n  <div style={card}>                                ← .admin-card (search)\n    <form onSubmit=search>\n      <input type=search inputMode=tel placeholder=\"Search by phone…\" />\n      <button type=submit> {loading?\"Searching…\":\"Search\"} </button>\n  { error && <div role=\"alert\">{error}</div> }      ← .admin-alert\n  { rows.length>0 && (\n    <div style={card}>                              ← .admin-card (results)\n      <div> Impersonate  <select>…users…</select>  <button>Go</button>  ← quick-select row\n      <div style=\"overflow-x:auto\">\n        <table>                                     ← .admin-table\n          <thead> Name | Phone | Business | Super | Actions\n          <tbody> per row:\n            name | fmtPhone | business | <badge yes|no> |\n              [Grant|Revoke (danger if super)]  [Impersonate]\n  ) }\n  { loading && rows.length===0 && \"Loading users…\" }   ← .admin-state\n  { !loading && rows.length===0 && \"No users found.\" } ← .admin-state\n</div>\n```\n- **All styling inline** (module consts: `wrap, card, inputStyle, btnPrimary,\n  btnSecondary, btnDanger`). No stylesheet classes — see\n  `css/admin-page.css` for the extracted rules. The super-badge \"yes\" pill is\n  green (`#e7f3e3`/`#2f6d29`); \"no\" is muted text.\n\n## 3. Props\nNone. The island fetches its own data; it only renders for a super-admin (route-\ngated).\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| — | — | — | — | — |\n\nFor isolation, drive via `_signals` (the `rows`/`loading`/`error`/`busyId`\nstate).\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | first fetch in flight, no rows yet → \"Loading users…\" | `cases/loading/loading.json` |\n| empty | fetch done, zero users → \"No users found.\" | `cases/empty/empty.json` |\n| list | populated table + quick-select | `cases/list/list.json` |\n| busy | a Grant/Revoke or Impersonate in flight (`busyId` set; all actions disabled, button reads \"…\") | `cases/busy/busy.json` |\n| error | a call failed → red `role=\"alert\"` banner above table | `cases/error/error.json` |\n| impersonate-select | a user chosen in the quick-select (Go enabled) | `cases/impersonate-select/impersonate-select.json` |\n\n## 5. Events\n- **Search submit:** `ev.expect(e => e.type === \"submit\" &&\n  e.source === \"form.admin-search\")` → `preventDefault` → `setLoading(true)` →\n  `search(q.trim())` → `setRows`.\n- **Search typing:** `input` on the search box → `setQ`.\n- **Grant/Revoke:** `ev.expect(e => e.type === \"click\" &&\n  e.source === \"button.admin-toggle-super\")` → `toggleSuper(u)` →\n  grant|revoke → splice updated row. (Disabled while `busyId !== null`.)\n- **Impersonate (row button):** `click` → `impersonate(u.id)` → redirect\n  `/dashboard`.\n- **Quick-select:** `change` on `<select#impersonate-select>` → `setImpersonateId`;\n  Go button `click` (disabled until a user is picked and nothing busy) →\n  `impersonate(impersonateId)`.\n\n## 6. Motion\n- **None.** No keyframes, no transitions — buttons swap text labels\n  (\"Search\"→\"Searching…\", \"Grant\"/\"Revoke\"→\"…\") to signal busy. Nothing to film.\n- **Reduced-motion:** N/A (no motion).\n\n## 7. Responsive\n- **No `@media` of its own.** The table is wrapped in\n  `style=\"overflow-x:auto\"` so on narrow viewports it scrolls horizontally\n  rather than reflowing. The `wrap` const is `max-width:980px;margin:0 auto`.\n  The impersonate quick-select row is `flex-wrap:wrap` so its label/select/Go\n  stack on narrow screens.\n- Verify at the shell breakpoint (`max-width: 640px`, where the sidebar becomes\n  an overlay) and at 980px (the content max-width).\n\n## 8. A11y\n- Search box has `aria-label=\"Search users by phone number\"`; the impersonate\n  `<select>` is associated to its `<label for=\"impersonate-select\">`.\n- The error banner is `role=\"alert\"` (assertive announce on failure).\n- All action controls are real `<button type=\"button\">`/`type=\"submit\"` with\n  `disabled` while busy.\n- **Gaps:** the \"Loading users…\"/\"No users found.\" states are plain text (no\n  `aria-live`/`aria-busy` on the table region), so SR users get no progress\n  announcement for the table itself.\n\n## 9. Used on\n`/admin` only — `routes/admin/index.tsx` mounts `<AdminPage />`, gated by\n`routes/admin/_middleware.ts` (super-admin). Single instance. Its impersonate\naction's runtime counterpart is the global `ImpersonationBanner`\n(shared-components/), shown on every page while impersonating.\n", "fixture": "{\n  \"category\": \"admin\",\n  \"folder\": \"admin-page\",\n  \"controls\": {}\n}\n", "cases": [{"name": "busy.json", "content": "{\n  \"_name\": \"Busy — Grant/Revoke or Impersonate in flight (all actions disabled)\",\n  \"_note\": \"busyId matches a row id → that row's primary action reads \\\"…\\\"; every Grant/Revoke/Impersonate/Go button is disabled while busyId !== null.\",\n  \"_signals\": {\n    \"q\": \"\",\n    \"rows\": [\n      { \"id\": \"usr_8f2a\", \"name\": \"Maria Lopez\", \"phoneNumber\": \"+15125550143\", \"businessName\": \"Lopez Landscaping\", \"superAdmin\": false },\n      { \"id\": \"usr_4d10\", \"name\": \"Dwayne Tucker\", \"phoneNumber\": \"+12145559981\", \"businessName\": \"Tucker Roofing\", \"superAdmin\": false }\n    ],\n    \"loading\": false,\n    \"busyId\": \"usr_8f2a\",\n    \"error\": null,\n    \"impersonateId\": \"\"\n  }\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — fetch done, zero users (\\\"No users found.\\\")\",\n  \"_signals\": {\n    \"q\": \"999\",\n    \"rows\": [],\n    \"loading\": false,\n    \"busyId\": null,\n    \"error\": null,\n    \"impersonateId\": \"\"\n  }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — action failed, role=\\\"alert\\\" banner above table\",\n  \"_signals\": {\n    \"q\": \"512 555\",\n    \"rows\": [\n      { \"id\": \"usr_8f2a\", \"name\": \"Maria Lopez\", \"phoneNumber\": \"+15125550143\", \"businessName\": \"Lopez Landscaping\", \"superAdmin\": false }\n    ],\n    \"loading\": false,\n    \"busyId\": null,\n    \"error\": \"Update failed\",\n    \"impersonateId\": \"\"\n  }\n}\n"}, {"name": "impersonate-select.json", "content": "{\n  \"_name\": \"Impersonate quick-select — a user chosen (Go enabled)\",\n  \"_signals\": {\n    \"q\": \"\",\n    \"rows\": [\n      { \"id\": \"usr_8f2a\", \"name\": \"Maria Lopez\", \"phoneNumber\": \"+15125550143\", \"businessName\": \"Lopez Landscaping\", \"superAdmin\": false },\n      { \"id\": \"usr_4d10\", \"name\": \"Dwayne Tucker\", \"phoneNumber\": \"+12145559981\", \"businessName\": \"Tucker Roofing\", \"superAdmin\": false }\n    ],\n    \"loading\": false,\n    \"busyId\": null,\n    \"error\": null,\n    \"impersonateId\": \"usr_8f2a\"\n  }\n}\n"}, {"name": "list.json", "content": "{\n  \"_name\": \"List — populated users table + quick-select\",\n  \"_signals\": {\n    \"q\": \"\",\n    \"rows\": [\n      { \"id\": \"usr_8f2a\", \"name\": \"Maria Lopez\", \"phoneNumber\": \"+15125550143\", \"businessName\": \"Lopez Landscaping\", \"superAdmin\": false },\n      { \"id\": \"usr_1b77\", \"name\": \"Raphael Castro\", \"phoneNumber\": \"+15125550100\", \"businessName\": \"RC Incorporated\", \"superAdmin\": true },\n      { \"id\": \"usr_4d10\", \"name\": \"Dwayne Tucker\", \"phoneNumber\": \"+12145559981\", \"businessName\": \"Tucker Roofing\", \"superAdmin\": false },\n      { \"id\": \"usr_9e3c\", \"phoneNumber\": \"+17135552207\", \"businessName\": \"Cobblestone Cafe\", \"superAdmin\": false },\n      { \"id\": \"usr_2af5\", \"name\": \"Priya Nair\", \"phoneNumber\": \"+15125557788\", \"superAdmin\": false }\n    ],\n    \"loading\": false,\n    \"busyId\": null,\n    \"error\": null,\n    \"impersonateId\": \"\"\n  }\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — first fetch in flight, no rows\",\n  \"_signals\": {\n    \"q\": \"\",\n    \"rows\": [],\n    \"loading\": true,\n    \"busyId\": null,\n    \"error\": null,\n    \"impersonateId\": \"\"\n  }\n}\n"}], "css": "/* ============================================================\n   AdminPage — extracted INLINE-style literals.\n   The island uses ZERO stylesheet classes; every wrapper, card, input, button,\n   table and badge is an inline `style=\"…\"` string (a handful hoisted to module\n   consts: wrap, card, inputStyle, btnPrimary, btnSecondary, btnDanger).\n   Reproduced here as ready-to-use rules so the console can be rebuilt without\n   re-deriving the literals. Off the Sabor semantic class system on purpose —\n   internal operator tool — but it DOES read the tokens (--border, --fg,\n   --fg-muted, --brand-green) with hard-coded hex fallbacks.\n   ============================================================ */\n\n/* ---- wrap const (page container) ---- */\n.admin-wrap {\n  max-width: 980px;\n  margin: 0 auto;\n  padding: 8px 4px 48px;\n}\n\n/* ---- card const (search card + table card) ---- */\n.admin-card {\n  background: #fff;\n  border: 1px solid var(--border, #e2e6dd);\n  border-radius: 14px;\n  padding: 18px;\n  margin-bottom: 18px;\n}\n\n/* ---- page heading block ---- */\n.admin-head { margin: 6px 4px 18px; }\n.admin-head__title {\n  margin: 0 0 4px;\n  font-size: 22px;\n  font-weight: 800;\n  color: var(--fg);\n}\n.admin-head__sub {\n  margin: 0;\n  color: var(--fg-muted, #6b7560);\n  font-size: 13.5px;\n}\n\n/* ---- inputStyle const (search box) ---- */\n.admin-input {\n  flex: 1;\n  min-width: 0;\n  box-sizing: border-box;\n  padding: 9px 12px;\n  border: 1px solid var(--border, #d8dcd5);\n  border-radius: 9px;\n  font: inherit;\n  font-size: 14px;\n  background: #fff;\n  color: var(--fg);\n}\n\n/* ---- buttons ---- */\n.admin-btn-primary {\n  padding: 9px 16px;\n  border-radius: 9px;\n  border: 0;\n  background: var(--brand-green, #519843);\n  color: #fff;\n  cursor: pointer;\n  font: inherit;\n  font-weight: 700;\n  white-space: nowrap;\n}\n.admin-btn-secondary {\n  padding: 7px 12px;\n  border-radius: 8px;\n  border: 1px solid var(--border, #d8dcd5);\n  background: #fff;\n  color: var(--fg);\n  cursor: pointer;\n  font: inherit;\n  font-weight: 600;\n  white-space: nowrap;\n}\n.admin-btn-danger {                /* Revoke (super-admin → off) */\n  padding: 7px 12px;\n  border-radius: 8px;\n  border: 1px solid #e3b7b7;\n  background: #fff;\n  color: #a83b3b;\n  cursor: pointer;\n  font: inherit;\n  font-weight: 600;\n  white-space: nowrap;\n}\n\n/* ---- error alert (role=\"alert\") ---- */\n.admin-alert {\n  background: #fdecec;\n  border: 1px solid #e3b7b7;\n  color: #a83b3b;\n  border-radius: 10px;\n  padding: 10px 14px;\n  margin-bottom: 14px;\n  font-size: 13.5px;\n}\n\n/* ---- impersonate quick-select row ---- */\n.admin-impersonate-row {\n  display: flex;\n  gap: 10px;\n  align-items: center;\n  flex-wrap: wrap;\n  margin-bottom: 14px;\n  padding-bottom: 14px;\n  border-bottom: 1px solid var(--border, #eef1ea);\n}\n.admin-impersonate-row__label {\n  font-size: 12px;\n  font-weight: 700;\n  letter-spacing: 0.05em;\n  text-transform: uppercase;\n  color: var(--fg-muted, #6b7560);\n}\n.admin-impersonate-row__select {\n  flex: 1;\n  min-width: 180px;\n  padding: 8px 10px;\n  border: 1px solid var(--border, #d8dcd5);\n  border-radius: 9px;\n  font: inherit;\n  font-size: 13.5px;\n  background: #fff;\n  color: var(--fg);\n}\n\n/* ---- users table ---- */\n.admin-table-scroll { overflow-x: auto; }\n.admin-table {\n  width: 100%;\n  border-collapse: collapse;\n  font-size: 13.5px;\n}\n.admin-table thead tr {\n  text-align: left;\n  color: var(--fg-muted, #6b7560);\n  font-size: 11.5px;\n  letter-spacing: 0.05em;\n  text-transform: uppercase;\n}\n.admin-table th { padding: 8px 10px; }\n.admin-table tbody tr { border-top: 1px solid var(--border, #eef1ea); }\n.admin-table td { padding: 10px; }\n.admin-table td--name { font-weight: 600; }\n.admin-table td--phone { font-variant-numeric: tabular-nums; }\n.admin-table td--biz { color: var(--fg-muted, #6b7560); }\n.admin-table td--actions { text-align: right; white-space: nowrap; }\n\n/* ---- super-admin badge ---- */\n.admin-badge-yes {\n  display: inline-block;\n  padding: 2px 8px;\n  border-radius: 999px;\n  background: #e7f3e3;\n  color: #2f6d29;\n  font-size: 11.5px;\n  font-weight: 700;\n}\n.admin-badge-no { color: var(--fg-muted, #9aa394); }\n\n/* ---- loading / empty states ---- */\n.admin-state {\n  text-align: center;\n  color: var(--fg-muted, #9aa394);\n  font-size: 13.5px;\n  padding: 24px;\n}\n", "cssName": "admin-page.css", "source": "/**\n * /admin data island — super-admin console.\n *\n * Search users by phone, grant/revoke super-admin, and impersonate. Every\n * action hits a server-guarded endpoint (clients/admin.ts → AdminController);\n * the page itself is also route-gated on the super-admin flag, so this island\n * only ever renders for an actual super admin.\n *\n * Copy here is intentionally English-only (internal operator tool) — it\n * deliberately doesn't touch the i18n dictionaries.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport { adminClient, type AdminUserView } from \"../clients/admin.ts\";\nimport { fmtPhone } from \"../lib/format.ts\";\n\nconst wrap = \"max-width:980px;margin:0 auto;padding:8px 4px 48px\";\nconst card =\n  \"background:#fff;border:1px solid var(--border,#e2e6dd);border-radius:14px;padding:18px;margin-bottom:18px\";\nconst inputStyle =\n  \"flex:1;min-width:0;box-sizing:border-box;padding:9px 12px;border:1px solid var(--border,#d8dcd5);border-radius:9px;font:inherit;font-size:14px;background:#fff;color:var(--fg)\";\nconst btnPrimary =\n  \"padding:9px 16px;border-radius:9px;border:0;background:var(--brand-green,#519843);color:#fff;cursor:pointer;font:inherit;font-weight:700;white-space:nowrap\";\nconst btnSecondary =\n  \"padding:7px 12px;border-radius:8px;border:1px solid var(--border,#d8dcd5);background:#fff;color:var(--fg);cursor:pointer;font:inherit;font-weight:600;white-space:nowrap\";\nconst btnDanger =\n  \"padding:7px 12px;border-radius:8px;border:1px solid #e3b7b7;background:#fff;color:#a83b3b;cursor:pointer;font:inherit;font-weight:600;white-space:nowrap\";\n\nexport default function AdminPage() {\n  const [q, setQ] = useState(\"\");\n  const [rows, setRows] = useState<AdminUserView[]>([]);\n  const [loading, setLoading] = useState(false);\n  const [busyId, setBusyId] = useState<string | null>(null);\n  const [error, setError] = useState<string | null>(null);\n  const [impersonateId, setImpersonateId] = useState(\"\");\n\n  async function search(e?: Event) {\n    e?.preventDefault();\n    setLoading(true);\n    setError(null);\n    try {\n      setRows(await adminClient.search(q.trim()));\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"Search failed\");\n    } finally {\n      setLoading(false);\n    }\n  }\n\n  // List all users on first load (empty query → the server returns everyone,\n  // capped). Searching then narrows it.\n  useEffect(() => {\n    search();\n  }, []);\n\n  async function toggleSuper(u: AdminUserView) {\n    setBusyId(u.id);\n    setError(null);\n    try {\n      const updated = u.superAdmin\n        ? await adminClient.revoke(u.id)\n        : await adminClient.grant(u.id);\n      setRows((rs) => rs.map((r) => (r.id === u.id ? updated : r)));\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"Update failed\");\n    } finally {\n      setBusyId(null);\n    }\n  }\n\n  async function impersonate(id: string) {\n    if (!id) return;\n    setBusyId(id);\n    setError(null);\n    try {\n      await adminClient.impersonate(id);\n      // The server swapped the pm_session cookie; reload as the target user.\n      globalThis.location.href = \"/dashboard\";\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"Impersonate failed\");\n      setBusyId(null);\n    }\n  }\n\n  return (\n    <div style={wrap}>\n      <div style=\"margin:6px 4px 18px\">\n        <h1 style=\"margin:0 0 4px;font-size:22px;font-weight:800;color:var(--fg)\">\n          Admin\n        </h1>\n        <p style=\"margin:0;color:var(--fg-muted,#6b7560);font-size:13.5px\">\n          Search users, grant or revoke super-admin, and impersonate.\n        </p>\n      </div>\n\n      <div style={card}>\n        <form\n          onSubmit={search}\n          style=\"display:flex;gap:10px;align-items:center\"\n        >\n          <input\n            style={inputStyle}\n            type=\"search\"\n            inputMode=\"tel\"\n            placeholder=\"Search by phone (e.g. 512 555 or +1512)\"\n            value={q}\n            onInput={(e) => setQ((e.target as HTMLInputElement).value)}\n            aria-label=\"Search users by phone number\"\n          />\n          <button type=\"submit\" style={btnPrimary} disabled={loading}>\n            {loading ? \"Searching…\" : \"Search\"}\n          </button>\n        </form>\n      </div>\n\n      {error && (\n        <div\n          role=\"alert\"\n          style=\"background:#fdecec;border:1px solid #e3b7b7;color:#a83b3b;border-radius:10px;padding:10px 14px;margin-bottom:14px;font-size:13.5px\"\n        >\n          {error}\n        </div>\n      )}\n\n      {rows.length > 0 && (\n        <div style={card}>\n          {\n            /* Impersonate dropdown — pick any loaded user, then go. Mirrors\n              the per-row Impersonate button for keyboard/quick selection. */\n          }\n          <div style=\"display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-bottom:14px;padding-bottom:14px;border-bottom:1px solid var(--border,#eef1ea)\">\n            <label\n              style=\"font-size:12px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;color:var(--fg-muted,#6b7560)\"\n              for=\"impersonate-select\"\n            >\n              Impersonate\n            </label>\n            <select\n              id=\"impersonate-select\"\n              style=\"flex:1;min-width:180px;padding:8px 10px;border:1px solid var(--border,#d8dcd5);border-radius:9px;font:inherit;font-size:13.5px;background:#fff;color:var(--fg)\"\n              value={impersonateId}\n              onChange={(e) =>\n                setImpersonateId((e.target as HTMLSelectElement).value)}\n            >\n              <option value=\"\">Select a user…</option>\n              {rows.map((u) => (\n                <option key={u.id} value={u.id}>\n                  {(u.name || u.businessName || \"Unnamed\") + \" · \" +\n                    fmtPhone(u.phoneNumber)}\n                </option>\n              ))}\n            </select>\n            <button\n              type=\"button\"\n              style={btnSecondary}\n              disabled={!impersonateId || busyId !== null}\n              onClick={() => impersonate(impersonateId)}\n            >\n              Go\n            </button>\n          </div>\n\n          <div style=\"overflow-x:auto\">\n            <table style=\"width:100%;border-collapse:collapse;font-size:13.5px\">\n              <thead>\n                <tr style=\"text-align:left;color:var(--fg-muted,#6b7560);font-size:11.5px;letter-spacing:.05em;text-transform:uppercase\">\n                  <th style=\"padding:8px 10px\">Name</th>\n                  <th style=\"padding:8px 10px\">Phone</th>\n                  <th style=\"padding:8px 10px\">Business</th>\n                  <th style=\"padding:8px 10px\">Super</th>\n                  <th style=\"padding:8px 10px;text-align:right\">Actions</th>\n                </tr>\n              </thead>\n              <tbody>\n                {rows.map((u) => (\n                  <tr\n                    key={u.id}\n                    style=\"border-top:1px solid var(--border,#eef1ea)\"\n                  >\n                    <td style=\"padding:10px;font-weight:600\">\n                      {u.name || \"—\"}\n                    </td>\n                    <td style=\"padding:10px;font-variant-numeric:tabular-nums\">\n                      {fmtPhone(u.phoneNumber)}\n                    </td>\n                    <td style=\"padding:10px;color:var(--fg-muted,#6b7560)\">\n                      {u.businessName || \"—\"}\n                    </td>\n                    <td style=\"padding:10px\">\n                      {u.superAdmin\n                        ? (\n                          <span style=\"display:inline-block;padding:2px 8px;border-radius:999px;background:#e7f3e3;color:#2f6d29;font-size:11.5px;font-weight:700\">\n                            yes\n                          </span>\n                        )\n                        : <span style=\"color:var(--fg-muted,#9aa394)\">no</span>}\n                    </td>\n                    <td style=\"padding:10px;text-align:right;white-space:nowrap\">\n                      <button\n                        type=\"button\"\n                        style={u.superAdmin ? btnDanger : btnSecondary}\n                        disabled={busyId !== null}\n                        onClick={() => toggleSuper(u)}\n                      >\n                        {busyId === u.id\n                          ? \"…\"\n                          : u.superAdmin\n                          ? \"Revoke\"\n                          : \"Grant\"}\n                      </button>\n                      <button\n                        type=\"button\"\n                        style={btnSecondary + \";margin-left:8px\"}\n                        disabled={busyId !== null}\n                        onClick={() => impersonate(u.id)}\n                      >\n                        Impersonate\n                      </button>\n                    </td>\n                  </tr>\n                ))}\n              </tbody>\n            </table>\n          </div>\n        </div>\n      )}\n\n      {loading && rows.length === 0 && (\n        <div style=\"text-align:center;color:var(--fg-muted,#9aa394);font-size:13.5px;padding:24px\">\n          Loading users…\n        </div>\n      )}\n      {!loading && rows.length === 0 && (\n        <div style=\"text-align:center;color:var(--fg-muted,#9aa394);font-size:13.5px;padding:24px\">\n          No users found.\n        </div>\n      )}\n    </div>\n  );\n}\n", "sourceName": "AdminPage.tsx", "checklist": "# Capture checklist — AdminPage\n\n**Theme:** light only.\n**Auth:** **SUPER-ADMIN session REQUIRED.** Dev master OTP `000000` logs in at\n`http://localhost:5280`, but the account must carry the `superAdmin` flag or\n`routes/admin/_middleware.ts` 302-redirects to `/dashboard` and you never reach\nthis page. (Grant the flag on the dev account first, or log in as a known\nsuper-admin.) **No fabricated screenshots** — capture only against the running\napp.\n\n## Route / URL\n- `http://localhost:5280/admin`.\n\n## Viewports (component has no own @media; uses shell's 640px cutover)\n- **1280px** — full table, all five columns; `wrap` max-width 980px centered.\n- **980px** — content max-width; confirm table fits.\n- **640px / 390px** — sidebar becomes a slide-in overlay (shell); the table sits\n  in an `overflow-x:auto` scroller (capture the horizontal scroll), and the\n  impersonate quick-select row wraps (label / select / Go stack).\n\n## Element(s) to crop\n1. Heading + search card (idle, then \"Searching…\" disabled state).\n2. Results card: the **Impersonate quick-select** row (label + select + Go).\n3. The **users table** — show a `superAdmin: true` row (green \"yes\" pill +\n   red \"Revoke\" button) AND a normal row (muted \"no\" + \"Grant\").\n4. A row with NO name (`name` absent) → cell shows \"—\"; select option falls back\n   to businessName / \"Unnamed\".\n\n## Transient states to drive\n1. **loading** — hard-reload `/admin`; capture the centered \"Loading users…\"\n   before the list-all fetch resolves.\n2. **empty** — search a phone with no match → centered \"No users found.\".\n3. **list** — default populated table.\n4. **busy** — click Grant/Revoke (or Impersonate); capture the \"…\" label +\n   every action button disabled (drive by stalling the\n   `/admin/users/:id/grant` response).\n5. **error** — force an action to fail → red `role=\"alert\"` banner above the\n   table.\n6. **impersonate** — pick a user in the quick-select (or click a row's\n   Impersonate); the Go button enables. Clicking navigates to `/dashboard` as\n   that user — to also capture the resulting global ImpersonationBanner, follow\n   through and screenshot `/dashboard` (see shared-components/impersonation-banner).\n\n## Motion\n- None — no transitions/animations; busy is signalled by label swaps only.\n  Nothing to film.\n"}]}, {"name": "assistant", "pageMd": "# Page: `/assistant` (and `/assistant/:threadId`)\n\n**Routes:** `routes/assistant/index.tsx` (new/empty chat) and\n`routes/assistant/[threadId].tsx` (a specific conversation). **Sources copied\nto:** `js/index.tsx`, `js/[threadId].tsx`. Components in `components/`.\n\n> **Consolidation:** `routes/messages/index.tsx` is a **302 → `/assistant?from=messages`**.\n> \"Messages\" and \"Assistant\" are the same workspace; the sidebar's\n> **`active=\"messages\"`** highlights this page. `<RedirectToast>` shows a\n> one-line explainer when `?from=messages` is present. There is **no separate\n> messages page** — do not build one.\n\n## Purpose\nThe AI assistant (\"Bossie\") chat workspace where the contractor drafts quotes,\nsets terms, and manages a job conversation end-to-end. This is the app's\nlargest, most interactive surface.\n\n## Two route variants\n| Variant | Handler behavior |\n|---|---|\n| **`/assistant`** (index) | `?onboard=1` → `POST /agents/conversations/onboarding-start` (in-process SSR), then **302 → `/assistant/:id?onboard=1`**; on any failure **302 → `/dashboard`** (never strand in an empty chat). Otherwise: SSR-fetch thread list (`GET /agents/conversations?limit=50`) + profile; render an empty `AsstChat` (`initialMessages={[]}`). |\n| **`/assistant/:threadId`** | SSR-fetch in parallel: conversation **detail** (`GET /agents/conversations/:id`, in-process — also clears `hasUnreadEvent` server-side), thread **list**, and **profile**. Computes `headerTitle`/`headerStatus` from `detail.conversation`, and an **onboarding banner gate**: `showOnboardBanner = isOnboard && initialStep<4 && !hasActivity` where `initialStep` = count of {name, biz, state, address} filled and `hasActivity` = bound customer / sent contract / phase===\"terms\". |\n\n## Classification & data flow\n- **Page tier:** `define.page` SSR shell + **multiple islands**. SSR reads are\n  **authed in-process** (`ssrBackendGetAuthed` / `ssrBackendPostAuthed`) because\n  the plain HTTP client 500s/508s SSR-side on Deno Deploy — note this for the\n  rebuild's SSR data layer.\n- **Data:** `Conversation[]` (list), `ConversationDetail` (messages + customer +\n  contract), `ProfileSnapshot` (initials, comms languages, language). See\n  `data-model.md` §1.9.\n- **Liveness:** `AsstChat` opens a **websocket `/api/voice/stream`** (proxied to\n  the backend `/voice/stream` → AssemblyAI streaming) for voice dictation —\n  **pushed**. Message send/receive is request-response over `clients/assistant.ts`.\n- **Initials:** `deriveUserInitials({name, businessName, phoneNumber})` exported\n  from `AsstChat.tsx`, used to render the user disc consistently with the sidebar.\n\n## `<Head>`\n- index: `<title>{assistantPage.docTitle}</title>`; thread:\n  `<title>{assistantThread.pageTitle}</title>`\n- `<link rel=\"stylesheet\" href=\"/assistant-page.css\">` (the 180 KB feature\n  sheet; 28 keyframes — components extract only their own subset).\n\n## Layout / composition order (both variants)\n```\n<RedirectToast/>                         (index only; ?from=messages explainer)\n.app\n  <DashSidebar active=\"messages\" />      [SHARED]\n  main.main\n    <DashTopbar greetingName greetingOverride={assistant…} />   [SHARED]\n    .asst                                (two-pane: threads rail | chat)\n      <AsstThreads initialThreads [activeId] />    ← ISLAND (left rail)\n      .asst__chat-wrap\n        section.chat\n          <OnboardingProgress initialStep />       ← ISLAND ([threadId] only, when showOnboardBanner)\n          <ChatHeaderLive initialClient initialStatus />  ← ISLAND\n          <AsstChat … />                            ← ISLAND (the main chat)\n```\n`AsstChat` props differ by variant: index passes `initialMessages=[]`,\n`userInitials`, `sendLanguages`; thread also passes `conversationId`,\n`initialMessages` (from detail), `initialCustomer`, `initialContract`, and\n`from={business,name,phone,email}`.\n\n## Components\n| Component | Folder | Tier (one-line) |\n|---|---|---|\n| `AsstChat` | `components/asst-chat/` | island — the chat (message list, typed bubbles, inline composer, voice ws, quote/terms phases, job-option cards, embeds MoneyInput; renders its **own** `action_card` quote-card variant — NOT the QuoteCard island). VERY large (8450 LOC) — spec is a faithful macro decomposition. |\n| `AsstThreads` | `components/asst-threads/` | island — conversation list rail; SSR-seeded then client-updated. |\n| `ChatHeaderLive` | `components/chat-header-live/` | island — live chat header (client name + phase/status chip). |\n| `MoneyInput` | `components/money-input/` | island — currency entry used inside AsstChat money prompts. |\n| `OnboardingProgress` | `components/onboarding-progress/` | island — 4-step onboarding strip, server-seeded `initialStep`. |\n| `RedirectToast` | `components/redirect-toast/` | island — `?from=messages` consolidation toast. |\n\nShared (referenced, not re-specced here): `DashSidebar`, `DashTopbar`,\n`Skeletons`, `MobileViewport`, `QuoteTrack`, `ui/*`. NOTE: AsstChat does **not**\nrender the shared `QuoteCard` island — it has its own local `action_card`\nquote-card message variant (different prop shape). Verified by grep; the\n`QuoteCard` island is rendered only by `QuotesPage`.\n\n## Dead code orphaned by this feature (do NOT build)\n`islands/AsstComposer.tsx`, `islands/Composer.tsx`, `islands/DocTabs.tsx`,\n`components/AssistantSections.tsx`, `components/MessageBubble.tsx` — all\nzero live references (AsstChat has its own inline composer/bubbles).\n\n## Capture checklist (auth-gated)\n- URLs: `/assistant`, `/assistant/<threadId>` (need a seeded conversation),\n  `/assistant?onboard=1` (onboarding banner). Auth via dev master OTP `000000`.\n- Viewports: real `assistant-page.css` `@media` widths (two-pane collapses to a\n  single column + thread drawer on mobile — verify the exact width).\n- Transient states: composer focus, mic/voice active (ws), a money prompt,\n  job-option cards, onboarding strip steps 0–4, the `?from=messages` toast.\n- Light theme only.\n\n## Build order\ndesign tokens → app shell (DashSidebar/DashTopbar) → `redirect-toast`,\n`chat-header-live`, `asst-threads`, `onboarding-progress`, `money-input` →\n`asst-chat` (depends on MoneyInput) → page compositions.\n", "components": [{"name": "asst-chat", "component": "AsstChat", "spec": "# AsstChat\n\nThe assistant chat surface — message list with typed bubbles, an inline\ntext+voice composer, two conversation phases (`quote` → `terms`), the\njob-options picker, the embedded `MoneyInput` price screen, and the in-chat\nQuote+Agreement (`action-card`) card. This is the app's largest island.\n\n> **This spec is a FAITHFUL PARTIAL (macro) decomposition.** `islands/AsstChat.tsx`\n> is **8450 LOC** with ~40 `useState`/`useRef` cells, 7 local sub-components,\n> a voice/STT websocket path, and a branching render tree. It is **not**\n> mechanically reproducible from this doc. What this captures: the message\n> **bubble TYPES**, the inline composer, the two phases, the job-option cards,\n> the embedded MoneyInput + Quote card, the props, the ~18 most important\n> states/cases, the real keyframes, and the data/liveness model. Micro-states\n> (every wizard sub-picker, recovery branches, dev-only buttons, the per-term\n> editing flows) are noted but not exhaustively enumerated. The source IS the\n> source of truth — see `js/AsstChat.tsx`.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/AsstChat.tsx`). **Assistant-local** (no shared use).\n- **Interaction tier:** `island` (client-only state) **with a voice websocket**.\n  Message send/receive is request-response; voice transcription is **pushed**.\n- **Server action + flash:** none. AsstChat does NOT submit a `<form>` and has\n  **no Fresh server action / flash**. Every mutation goes through the typed\n  `clients/assistant.ts` (`assistantClient.chat`, `.conversation`,\n  `.lockQuote`, `.transitionToTerms`, `.acceptContract`, `.sendContract`,\n  `.bindCustomer`) or `lib/api.ts` (`/quotes`, `/invoices/:id/email|text`,\n  `/agents/job-details/*`). Optimistic-append + re-fetch the conversation\n  detail; no full-page reload.\n- **Client state owned (the load-bearing subset):**\n  - `convoId`, `messages: Message[]` — seeded from `conversationId` /\n    `initialMessages` (SSR), then appended optimistically and reconciled by\n    `assistantClient.conversation(convoId)` after each turn.\n  - `customer`, `contract`, `quote`, `quoteId` — bound entities; `quote` is\n    lazily fetched when the conversation has a `quoteId`.\n  - `draft: string` — composer text. `sending`, `error` — turn lifecycle.\n  - Phase/flow flags: `priceCaptureOpen`, `priceCents`, `suggestPricing`,\n    `priceSuggestions`, `awaitingJobDetails`, `submittedJobDetails`,\n    `jobOptionsOpen`, `jobOptions`, `selectedOptionId`, `pickerMode`\n    (`\"polish\"|\"confirm\"`), `previewCtaId`, `previewLang`, `channelMenuOpen`,\n    `invoiceFlow`, `invoiceCustomerOpen`, `invoiceResult`, `customerPickerOpen`.\n  - Voice/STT: `recording`, `recElapsed`, `audioLevel`, `liveInterim`,\n    `liveFinal` + refs `recorderRef` (MediaRecorder), `audioCtxRef`,\n    `sttSocketRef` (WebSocket), `recStreamRef`, `finalSoFarRef`.\n  - **View-history stack** (`historyStackRef: ViewSnapshot[]`): a UI-only\n    undo stack of `{priceCaptureOpen, awaitingJobDetails, submittedJobDetails,\n    pendingJobDetailsRaw, pendingPriceCents, priceCents, jobOptionsOpen}`.\n    `pushHistory()` before a view change; `popHistory()` on the\n    `pm:asst-back` event from ChatHeaderLive. Depth is broadcast via\n    `pm:asst-history`.\n  - `lang` = `langSignal.value` (the `lang` prop is an ignored SSR seed).\n- **Cross-island PUSH (it's the publisher):** dispatches `pm:asst-header`\n  (`{client, status}`) → **ChatHeaderLive** updates the header title/status in\n  place; dispatches `pm:asst-history` (`{depth}`) → the header's back button\n  appears; listens `pm:asst-back` to pop its stack. Also reads the dash cache\n  (`readCached`/`subscribeDash`/`refreshDash` from `lib/dash-cache.ts`) for the\n  overdue-invoice gate.\n- **Data source:** `clients/assistant.ts` (+ `quotesClient`, `clientsClient`,\n  `contractsClient`, `filesClient`, `api`). See `data-model.md` §1.9\n  (`Conversation`/`Message`/`JobOption`/`ConversationDetail`).\n- **Liveness — voice is PUSHED via websocket `/api/voice/stream`:**\n  `openSttSocket(sampleRate)` opens `ws(s)://{host}/api/voice/stream?sample_rate=…`\n  — a Fresh SSR proxy bridging to **AssemblyAI v3 streaming** (key kept\n  server-side). It resolves on the `Begin` frame; `Turn` frames stream\n  `transcript` (interim → `liveInterim`; `end_of_turn` → appended to `liveFinal`);\n  `Termination`/`error` close it. A parallel `MediaRecorder` captures the\n  authoritative blob (uploaded as the fallback / image-of-record). If the ws\n  never comes up it resolves `null` and the path falls back to backend-only\n  transcription. **Message chat itself is NOT live** — it's request-response.\n- **Honest-empty:** `empty = messages.length === 0` → `.chat__empty` with the\n  monster logo, a title, and four start prompts (known-price / help-me-price /\n  quick-quote / invoice). No blank-screen on an empty thread.\n- **Anti-patterns:** **no `location.reload()`** (verified — grep clean). The\n  cross-island state lives in `globalThis` CustomEvents rather than a shared\n  store (acceptable, but couples AsstChat ↔ ChatHeaderLive by event-name\n  contract). The history undo stack is a ref of UI flags (snapshot drift risk\n  if new flow flags are added but not added to `ViewSnapshot`).\n- **Data-shape hazards:**\n  - `Message.createdAt` is **epoch-ms number** (not ISO) — `fmtTime` handles\n    both but cases must use numbers (e.g. `1750247400000`).\n  - Money everywhere is **integer cents** (`totalCents`, `priceCents`,\n    `estimatedTotal`, line-item `amountCents`). `fmtUSD(cents)` divides by 100.\n    The embedded MoneyInput emits cents.\n  - `action_card`/`continue_cta`/`phase_divider`/`wizard` carry an untyped\n    `m.payload` object the render narrows per-kind (`ActionCardPayload`,\n    `{toPhase,summary,contractId}`, divider `{label,channel,emailedTo,\n    emailFailureReason,…}`, `{stepId,options,hint}`). Older threads can miss\n    fields (e.g. divider `channel`) — the render infers them defensively.\n  - A DRAFT `action_card` is marked **superseded** (dimmed, buttons removed)\n    when a later card for the same `quoteId` has advanced — earlier cards stay\n    in history but can't re-fire.\n\n## 2. Anatomy\n```\n<>\n  <div class=\"chat__scroll\" ref=scrollRef>\n    (empty || jobOptionsOpen)\n      ? <div class=\"chat__empty\">\n          [first-screen logo + title]                         ← when no sub-flow open\n          jobOptionsOpen   → .chat__jobopts (job-option cards, back, edit titles/bullets)\n          : awaitingJobDetails → details entry (textarea routes to /job-details)\n          : invoiceResult  → invoice-sent result card\n          : priceCaptureOpen → .chat__price-capture { back, title, [price tiers], <MoneyInput/>, Continue }\n          : <div class=\"chat__empty-prompts\"> 4× .chat__empty-prompt\n          [localhost+?dev → .chat__empty-debug \"Seed phase 2\"]\n        </div>\n      : visible.map(m =>                                       ← the message list, by m.kind\n          m.role===\"user\" && payload.wizardStepId → .wiz-log (compact pick line)\n          m.kind===\"phase_divider\" → .phase-divider [+ .recovery-card if send failed]\n          m.kind===\"continue_cta\"  → .msg > .continue-cta [--done] (terms/send/invoice CTA)\n          m.kind===\"wizard\"        → .msg > .wiz (question + <CustomerStepPanel>/option buttons)\n          m.kind===\"action_card\"   → .msg > .action-card (Quote+Agreement: head/chip, details,\n                                       line items, total, Lock-in/Edit or Re-open)\n          m.kind===\"text\" && content===\"PM_ONBOARDING_DEMO_CTA\" → sample-quote link chip\n          else → .msg[.msg--user] { .msg__avatar, [.msg__image], .msg__bubble (pre-wrap), .msg__time })\n    [sending && last msg is user] → .msg__bubble--typing (3 typing-dots, aria-live)\n  </div>\n\n  {composerHidden ? null :                                    ← hidden on tap-only screens (see §4)\n    <div class=\"composer [composer--flash]\">\n      [error → .composer__err]\n      recording\n        ? <RecordingPanel elapsed level finalText interimText onStop onCancel/>\n        : <>\n            <div class=\"composer__inner\">\n              <textarea class=\"composer__input\" ref=taRef placeholder=composerPlaceholder(...)/>\n              <div class=\"composer__tools\">\n                <button class=\"composer__mic\" onClick=toggleRecord/>     ← brand-pink, PRIMARY\n                <button class=\"composer__send\" onClick=onSendClick disabled=!draft.trim/>\n              </div>\n            </div>\n            <div class=\"composer__hint\">{tap-to-talk / ⌘↵ hint}</div>   ← composerHintPulse\n          </>\n    </div>}\n</>\n```\n- **Sub-components (local, same file):** `RecordingPanel` (voice orb +\n  transcript), `CustomerStepPanel` (business/person customer picker),\n  `WizardFollowUpForm`, `CustomDatePickerForm`, `CustomDurationPickerForm`,\n  `CustomWarrantyPickerForm`, `CustomPaymentPickerForm`.\n- **Embedded islands/components:** `<MoneyInput/>` (price screen), and the\n  in-chat `action-card` reproduces the Quote card shape (canonical `QuoteCard`\n  lives in `pages/quotes/components/`).\n- **Helpers:** `deriveUserInitials` (exported; mirrors backend initials),\n  `fmtUSD`, `fmtTime`, `composerPlaceholder` (echoes the onboarding question),\n  `statusChipLabel`, `buildPaymentMilestones` (via `lib/payment-split.ts`).\n- **Icon dependency:** `I` + `ICN.*` from `lib/dash-icons.tsx` (+ inline `<path>`\n  back chevrons).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `conversationId` | string | `undefined` | text | no — seeds `convoId` |\n| `initialMessages` | `Message[]` (required) | — | (json) | no — seeds `messages` |\n| `initialCustomer` | `CustomerLite` | `undefined` | (json) | no |\n| `initialContract` | `ContractLite` | `undefined` | (json) | no |\n| `userInitials` | string | `\"?\"` | text | no — user-avatar disc |\n| `from` | `{business?,name?,phone?,email?}` | `undefined` | (json) | no — contractor \"FROM\" for preview |\n| `sendLanguages` | `string[]` | `[\"en\"]` | (json) | no — drives the preview-lang toggle |\n| `lang` | `Lang` | (ignored) | select | **ignored SSR seed** — reads `langSignal.value` |\n\n> Callbacks aren't props — AsstChat owns its flow; the only cross-component\n> wiring is the `pm:asst-header` / `pm:asst-history` / `pm:asst-back`\n> CustomEvent contract with ChatHeaderLive.\n\n## 4. States → cases\nThe ~18 load-bearing states. Heavy children (MoneyInput, RecordingPanel,\nCustomerStepPanel) are stubbed via `_mocks` in cases so the still renders.\n\n| state | meaning | case |\n|---|---|---|\n| empty | `initialMessages=[]` → first-screen logo + 4 prompts | `cases/empty/empty.json` |\n| conversation | a real back-and-forth (user + assistant text bubbles) | `cases/conversation/conversation.json` |\n| typing | last msg is user + `sending` → typing-dots | `cases/typing/typing.json` |\n| job-options | `jobOptionsOpen` → three scope-of-work cards (one selected) | `cases/job-options/job-options.json` |\n| price-capture | `priceCaptureOpen` → MoneyInput screen (mocked) | `cases/price-capture/price-capture.json` |\n| price-suggest | help-me-price → three price tiers + custom MoneyInput | `cases/price-suggest/price-suggest.json` |\n| action-card-draft | in-chat Quote card, status=draft → Lock-in/Edit | `cases/action-card-draft/action-card-draft.json` |\n| action-card-sent | Quote card, status=sent → Re-open | `cases/action-card-sent/action-card-sent.json` |\n| continue-terms | `continue_cta` toPhase=terms → Business/Person buttons | `cases/continue-terms/continue-terms.json` |\n| continue-send | `continue_cta` toPhase=send → Review button | `cases/continue-send/continue-send.json` |\n| wizard-step | a `wizard` bubble (config step, option buttons) | `cases/wizard-step/wizard-step.json` |\n| phase-divider | a `phase_divider` separator (\"Contract terms\") | `cases/phase-divider/phase-divider.json` |\n| recovery | failed-send divider → contact-recovery card | `cases/recovery/recovery.json` |\n| recording | mic hot → RecordingPanel takes over composer (mocked) | `cases/recording/recording.json` |\n| composer-flash | `awaitingJobDetails` + empty draft → bounce cue | `cases/composer-flash/composer-flash.json` |\n| image-bubble | a `kind:\"image\"` message bubble | `cases/image-bubble/image-bubble.json` |\n| es | Spanish chrome (langSignal flipped) | `cases/es/es.json` |\n\n> **Isolate note.** AsstChat fires no-backend network calls (`assistantClient`,\n> `api`, the voice ws, `refreshDash`) on mount; in isolate these 404/throw and\n> are swallowed, so the island stays on its seeded props. The composer is\n> **hidden** whenever `priceCaptureOpen || jobOptionsOpen || invoiceCustomerOpen\n> || invoiceResult || hasUnansweredWizard || previewCtaId` — so the\n> price/jobopts/wizard cases will NOT show a composer (expected). Cases use REAL\n> seed values from `lib/asst-seed.ts` (t1–t7: \"Tom & Linda K.\", \"Marcus Lin\",\n> \"Hilltop Diner\", \"Sarah Chen\", \"Greenleaf HOA\", \"Cobblestone Cafe\", \"Bayside\n> Properties\") mapped onto `Message`/`Conversation`. `_mocks` stubs `MoneyInput`,\n> `RecordingPanel`, `CustomerStepPanel`. `createdAt` is epoch-ms.\n\n## 5. Events\n- `ev.expect(e => e.source === \"button.composer__send\" && e.type === \"click\")`\n  → `onSendClick` (optimistic append + `assistantClient.chat`).\n- `ev.expect(e => e.source === \"textarea.composer__input\" && e.type === \"keydown\" && e.key === \"Enter\" && !e.shiftKey)`\n  → send (Shift+Enter = newline).\n- `ev.expect(e => e.source === \"button.composer__mic\" && e.type === \"click\")`\n  → `toggleRecord` → opens the voice ws + MediaRecorder (mic permission).\n- `ev.expect(e => e.source === \"button.chat__empty-prompt\")` → opens a flow\n  (`startKnownPriceFlow` / `startHelpMePriceFlow` / `startInvoiceFlow`).\n- `ev.expect(e => e.source === \"button.chat__price-continue\")` → `onPriceContinue`.\n- `ev.expect(e => e.source === \"div.chat__jobopt\")` → `setSelectedOptionId`.\n- `ev.expect(e => e.source === \"button.action-card__btn--primary\")` →\n  `lockActionCard` (locks the quote, then transitions).\n- `ev.expect(e => e.source === \"button.continue-cta__btn\")` → `submitContinueCta`.\n- Incoming (not user events): `pm:asst-back` from ChatHeaderLive → `popHistory`.\n- Outgoing dispatches: `pm:asst-header`, `pm:asst-history`.\n\n## 6. Motion (extracted — see css/asst-chat.css)\n- **Message in:** `@keyframes msg-in` (`opacity 0→1`, `translateY 8px→0`,\n  `scale .98→1`) `220ms cubic-bezier(.22,1,.36,1) both` — guarded\n  component-locally (`.msg { animation: none }` under reduced-motion).\n- **Typing dots:** `@keyframes typing-bounce` (`translateY 0→-4px`, staggered\n  150/300ms) `1.2s infinite`.\n- **Composer flash:** `@keyframes composer-bounce` — a real bouncy-ball jump\n  (apex −200px) + damped jell-o rotation wobble, `1.6s 250ms 1 both`,\n  `transform-origin: 50% 100%`, `will-change: transform`. **Jank finding:** the\n  −200px translate + `will-change` is GPU-promoted and smooth; the multi-stop\n  rotation is the single most expensive animation in the surface — acceptable as\n  a one-shot, would be janky if looped. Relies on the global reduced-motion clamp.\n- **Composer hint:** `@keyframes composerHintPulse` (`opacity 1→.45`) `1.8s\n  infinite` — guarded component-locally.\n- **Voice panel:** `rec-panel-enter` (240ms), `rec-panel-bg-drift` (14s drift),\n  `rec-ring` (audio-threshold ripples), `rec-live-pulse` (1.05s), `rec-final-fade`\n  (per finalized chunk), `rec-caret-blink` (1s steps) — all rely on the global clamp.\n- **Flows:** `chat-price-in` / `chat-details-in` (entry), `chat-details-pulse`\n  (writing dots), `spin` (button spinner), `quote-menu-in` / `custDdReveal`\n  (channel/customer dropdowns).\n- **Reduced motion:** ONLY `.msg` + `.composer__hint` are guarded\n  component-locally; **everything else relies on the GLOBAL tokens clamp**\n  (`animation-duration: 0.01ms !important`). The rebuild must keep both. Verify\n  the composer bounce and the voice orb still under reduced-motion.\n\n## 7. Responsive\n- **No own `@media`.** AsstChat is laid out by the page's `.asst` grid (the\n  chat column is `minmax(0, 1fr)`):\n  - `≥1100px`: `280px | 1fr | 360px` (threads | chat | docpane, when a docpane\n    exists; `.asst:not(:has(.docpane))` drops the third track).\n  - `≤1099px`: `260px | 1fr` (docpane hidden).\n  - `≤880px`: single `1fr` column — threads collapse to a drawer (page CSS).\n  - `≤480px`: the `RecordingPanel` tightens (orb 44px, 12px padding) — the only\n    AsstChat-relevant `@media` and it lives in the page sheet, not this island.\n- The composer's `padding-bottom: calc(16px + env(safe-area-inset-bottom))` +\n  `--app-vh` (from the `MobileViewport` island) keep the input above the iOS\n  keyboard / home indicator. `.composer__input` is `font-size:16px` to defeat\n  iOS Safari focus-zoom.\n\n## 8. A11y\n- Composer: `<textarea>` + real `<button>`s with `aria-label`/`title`\n  (mic = voiceMemo, send = send). The typing indicator has `aria-live=\"polite\"`\n  + an `aria-label`. Good.\n- **Gaps:** job-option cards are `<div role=\"button\" tabIndex=0>` (acceptable\n  but a real `<button>` is cleaner); the voice transcript has no `aria-live`\n  region (silent to AT during dictation); the MoneyInput screen hides the\n  composer entirely (tap-only) which is fine but means no text alternative; the\n  `kind:\"image\"` bubble uses `filename` as `alt` (falls back to a generic\n  string). Phase dividers are decorative `<div>`s, not landmarks.\n\n## 9. Used on\nBoth assistant route variants: `routes/assistant/index.tsx`\n(`initialMessages=[]`, `userInitials`, `sendLanguages`) and\n`routes/assistant/[threadId].tsx` (full set incl. `conversationId`,\n`initialMessages` from detail, `initialCustomer`, `initialContract`, `from`).\nEmbeds `MoneyInput`. CSS lives in `static/assistant-page.css` (the `.chat__*`,\n`.msg*`, `.composer*`, `.action-card*`, `.continue-cta*`, `.phase-divider*`,\n`.wiz*`, `.rec-panel*`, `.chat__price*`, `.chat__jobopts*` families) — this\nfolder's `css/asst-chat.css` is a representative extract of that subset.\n", "fixture": "{\n  \"category\": \"assistant\",\n  \"folder\": \"asst-chat\",\n  \"controls\": {\n    \"conversationId\": { \"type\": \"text\", \"value\": \"t2\" },\n    \"initialMessages\": {\n      \"type\": \"json\",\n      \"value\": [\n        {\n          \"id\": \"m1\",\n          \"conversationId\": \"t2\",\n          \"role\": \"user\",\n          \"kind\": \"text\",\n          \"content\": \"Need a quote for kitchen backsplash, ~30 sqft, white subway tile.\",\n          \"createdAt\": 1750247400000\n        },\n        {\n          \"id\": \"m2\",\n          \"conversationId\": \"t2\",\n          \"role\": \"assistant\",\n          \"kind\": \"text\",\n          \"content\": \"Got it — a 30 sqft white subway-tile backsplash. Do you already have a price in mind, or should I help you price it?\",\n          \"createdAt\": 1750247430000\n        }\n      ]\n    },\n    \"userInitials\": { \"type\": \"text\", \"value\": \"ML\" },\n    \"sendLanguages\": { \"type\": \"json\", \"value\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "action-card-draft.json", "content": "{\n  \"_name\": \"Action card (draft) — in-chat Quote+Agreement, Lock-in/Edit\",\n  \"_note\": \"kind=action_card, payload.status='draft' → .action-card with the Draft chip and the Lock-in + Edit CTAs (needs payload.quoteId for Lock-in to be enabled).\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialMessages\": [\n    {\n      \"id\": \"ac1\",\n      \"conversationId\": \"t2\",\n      \"role\": \"assistant\",\n      \"kind\": \"action_card\",\n      \"content\": \"Subway Tile Backsplash\",\n      \"createdAt\": 1750247600000,\n      \"payload\": {\n        \"actionType\": \"quote\",\n        \"status\": \"draft\",\n        \"quoteId\": \"q_abc\",\n        \"description\": \"Remove existing backsplash and prep wall.\\nInstall ~30 sqft white subway tile.\\nGrout, seal, and clean up.\",\n        \"lineItems\": [\n          { \"description\": \"Labor — tile install (30 sqft)\", \"amountCents\": 90000 },\n          { \"description\": \"Materials — subway tile, grout, sealant\", \"amountCents\": 35000 }\n        ],\n        \"totalCents\": 125000\n      }\n    }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "action-card-sent.json", "content": "{\n  \"_name\": \"Action card (sent) — Quote card with Sent chip + Re-open\",\n  \"_note\": \"payload.status='sent' → Sent chip, no Lock-in; shows the Re-open CTA.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialMessages\": [\n    {\n      \"id\": \"ac1\",\n      \"conversationId\": \"t2\",\n      \"role\": \"assistant\",\n      \"kind\": \"action_card\",\n      \"content\": \"Subway Tile Backsplash\",\n      \"createdAt\": 1750247600000,\n      \"payload\": {\n        \"actionType\": \"quote\",\n        \"status\": \"sent\",\n        \"quoteId\": \"q_abc\",\n        \"description\": \"Remove existing backsplash and prep wall.\\nInstall ~30 sqft white subway tile.\\nGrout, seal, and clean up.\",\n        \"lineItems\": [\n          { \"description\": \"Labor — tile install (30 sqft)\", \"amountCents\": 90000 },\n          { \"description\": \"Materials — subway tile, grout, sealant\", \"amountCents\": 35000 }\n        ],\n        \"totalCents\": 125000\n      }\n    }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "composer-flash.json", "content": "{\n  \"_name\": \"Composer flash — bounce attention cue (awaiting job details)\",\n  \"_note\": \"awaitingJobDetails=true with no submittedJobDetails and an empty draft → composer gets .composer--flash (the one-shot composer-bounce). Drive via _signals; the bounce plays once on mount.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"_signals\": { \"awaitingJobDetails\": true, \"submittedJobDetails\": null, \"draft\": \"\" },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"assistant\", \"kind\": \"text\", \"content\": \"Great — now tell me what the job involves and I'll write it up.\", \"createdAt\": 1750247400000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "continue-send.json", "content": "{\n  \"_name\": \"Continue CTA (send) — Review button\",\n  \"_note\": \"kind=continue_cta, payload.toPhase='send' → .continue-cta with the Review CTA (transitions the card into a 'Drafted/Sent ✓' state when clicked).\",\n  \"conversationId\": \"t1\",\n  \"userInitials\": \"TK\",\n  \"initialContract\": { \"id\": \"ct_1\", \"status\": \"draft\", \"totalAmount\": 125000 },\n  \"initialMessages\": [\n    {\n      \"id\": \"cta1\",\n      \"conversationId\": \"t1\",\n      \"role\": \"assistant\",\n      \"kind\": \"continue_cta\",\n      \"content\": \"Your contract is drafted. Review and send it to Tom & Linda K.\",\n      \"createdAt\": 1750247800000,\n      \"payload\": { \"toPhase\": \"send\", \"summary\": \"Garage epoxy floor — $1,250\", \"contractId\": \"ct_1\" }\n    }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "continue-terms.json", "content": "{\n  \"_name\": \"Continue CTA (terms) — Business / Person buttons\",\n  \"_note\": \"kind=continue_cta, payload.toPhase='terms' → .continue-cta with the PHASE 2 eyebrow and the two-button Business/Person split (the customer-type pick that kicks off the terms wizard).\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialMessages\": [\n    {\n      \"id\": \"cta1\",\n      \"conversationId\": \"t2\",\n      \"role\": \"assistant\",\n      \"kind\": \"continue_cta\",\n      \"content\": \"Quote locked in. Ready to set the contract terms?\",\n      \"createdAt\": 1750247700000,\n      \"payload\": { \"toPhase\": \"terms\", \"summary\": \"Subway Tile Backsplash — $1,250\" }\n    }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "conversation.json", "content": "{\n  \"_name\": \"Conversation — user + assistant text bubbles (Marcus Lin / t2)\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialCustomer\": { \"id\": \"c2\", \"name\": \"Marcus Lin\", \"email\": \"marcus@example.com\", \"phoneNumber\": \"+15125550142\" },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"Need a quote for kitchen backsplash, ~30 sqft, white subway tile.\", \"createdAt\": 1750247400000 },\n    { \"id\": \"m2\", \"conversationId\": \"t2\", \"role\": \"assistant\", \"kind\": \"text\", \"content\": \"Got it — a 30 sqft white subway-tile backsplash. Do you already have a price in mind, or should I help you price it?\", \"createdAt\": 1750247430000 },\n    { \"id\": \"m3\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"I'm thinking around $1,250 all-in.\", \"createdAt\": 1750247490000 },\n    { \"id\": \"m4\", \"conversationId\": \"t2\", \"role\": \"assistant\", \"kind\": \"text\", \"content\": \"$1,250 it is. I'll draft a clean scope of work — give me a sec.\", \"createdAt\": 1750247520000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — first-screen logo + 4 start prompts\",\n  \"_note\": \"messages.length===0 → .chat__empty. Composer is visible (no tap-only flow open).\",\n  \"conversationId\": \"new\",\n  \"initialMessages\": [],\n  \"userInitials\": \"ML\",\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Español — chrome localized (langSignal flipped to es)\",\n  \"_note\": \"AsstChat reads langSignal.value, NOT the lang prop. Flip the language signal via _signals so the empty-prompts, composer hint/placeholder, and CTA labels render in Spanish. Free-text message content stays verbatim.\",\n  \"conversationId\": \"new\",\n  \"userInitials\": \"ML\",\n  \"_signals\": { \"lang\": \"es\" },\n  \"initialMessages\": [],\n  \"sendLanguages\": [\"es\", \"en\"]\n}\n"}, {"name": "image-bubble.json", "content": "{\n  \"_name\": \"Image bubble — kind:image attachment + caption\",\n  \"_note\": \"A user message with kind='image' and payload.fileId → .msg__image (links to /api/files/:id) above the text bubble. In isolate the image src 404s; the bubble + alt still render.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialMessages\": [\n    {\n      \"id\": \"img1\",\n      \"conversationId\": \"t2\",\n      \"role\": \"user\",\n      \"kind\": \"image\",\n      \"content\": \"Here's the wall I need tiled.\",\n      \"createdAt\": 1750247400000,\n      \"payload\": { \"fileId\": \"f_kitchen\", \"filename\": \"kitchen-wall.jpg\" }\n    },\n    { \"id\": \"m2\", \"conversationId\": \"t2\", \"role\": \"assistant\", \"kind\": \"text\", \"content\": \"Got it — looks like about 30 sqft. I'll factor that into the quote.\", \"createdAt\": 1750247430000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "job-options.json", "content": "{\n  \"_name\": \"Job options — three scope-of-work cards, option 1 selected\",\n  \"_note\": \"jobOptionsOpen + jobOptions populated → .chat__jobopts. Composer is HIDDEN (tap-only screen). Drive `jobOptionsOpen`/`jobOptions`/`selectedOptionId`/`optionsLoading` via _signals since they're set by the flow, not props.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"_signals\": {\n    \"jobOptionsOpen\": true,\n    \"optionsLoading\": false,\n    \"pickerMode\": \"polish\",\n    \"selectedOptionId\": \"o1\",\n    \"jobOptions\": [\n      { \"id\": \"o1\", \"jobName\": \"Subway Tile Backsplash\", \"summary\": \"30 sqft white subway tile install\", \"bullets\": [\n        { \"id\": \"b1\", \"text\": \"Remove existing backsplash and prep wall surface\" },\n        { \"id\": \"b2\", \"text\": \"Install ~30 sqft white subway tile, level and spaced\" },\n        { \"id\": \"b3\", \"text\": \"Grout, seal, and clean up\" }\n      ] },\n      { \"id\": \"o2\", \"jobName\": \"Backsplash Refresh\", \"summary\": \"Standard tile refresh\", \"bullets\": [\n        { \"id\": \"b1\", \"text\": \"Set white subway tile over prepped wall\" },\n        { \"id\": \"b2\", \"text\": \"Grout and seal\" }\n      ] },\n      { \"id\": \"o3\", \"jobName\": \"Premium Tile Job\", \"summary\": \"Detailed install with trim\", \"bullets\": [\n        { \"id\": \"b1\", \"text\": \"Full prep, waterproofing, and edge trim\" },\n        { \"id\": \"b2\", \"text\": \"Install, grout, seal, and haul-away\" }\n      ] }\n    ]\n  },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"Kitchen backsplash, ~30 sqft, white subway tile.\", \"createdAt\": 1750247400000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "phase-divider.json", "content": "{\n  \"_name\": \"Phase divider — full-width labelled separator\",\n  \"_note\": \"kind=phase_divider with a clean label (no failure reasons) → .phase-divider only (no recovery card).\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialMessages\": [\n    { \"id\": \"u1\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"Looks good, lock it in.\", \"createdAt\": 1750247700000 },\n    { \"id\": \"pd1\", \"conversationId\": \"t2\", \"role\": \"system\", \"kind\": \"phase_divider\", \"content\": \"Contract terms\", \"createdAt\": 1750247720000, \"payload\": { \"label\": \"Phase 2 — Contract terms\" } }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "price-capture.json", "content": "{\n  \"_name\": \"Price capture — MoneyInput screen (heavy child mocked)\",\n  \"_note\": \"priceCaptureOpen=true, suggestPricing=false → .chat__price-capture with the embedded MoneyInput. Composer HIDDEN. MoneyInput is stubbed via _mocks so the still renders without its odometer/ResizeObserver.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"_signals\": { \"priceCaptureOpen\": true, \"suggestPricing\": false, \"priceCents\": 125000 },\n  \"_mocks\": { \"MoneyInput\": { \"stub\": \"<div class='mi-stub' style='border:1px dashed #cfd6cb;border-radius:28px;padding:26px;text-align:center;font:700 32px Inter,sans-serif;color:#0F3036'>$1,250.00</div>\" } },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"Kitchen backsplash, ~30 sqft, white subway tile.\", \"createdAt\": 1750247400000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "price-suggest.json", "content": "{\n  \"_name\": \"Price suggest — three LLM price tiers + custom MoneyInput\",\n  \"_note\": \"priceCaptureOpen + suggestPricing=true → renders the .chat__price-tiers list above the (mocked) MoneyInput. Composer HIDDEN.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"_signals\": {\n    \"priceCaptureOpen\": true,\n    \"suggestPricing\": true,\n    \"priceCents\": null,\n    \"priceSuggestions\": [\n      { \"tier\": \"low\", \"label\": \"Budget\", \"rationale\": \"Materials-only markup\", \"priceCents\": 95000 },\n      { \"tier\": \"mid\", \"label\": \"Standard\", \"rationale\": \"Typical for this scope in your area\", \"priceCents\": 125000 },\n      { \"tier\": \"high\", \"label\": \"Premium\", \"rationale\": \"Includes trim + waterproofing\", \"priceCents\": 165000 }\n    ]\n  },\n  \"_mocks\": { \"MoneyInput\": { \"stub\": \"<div class='mi-stub' style='border:1px dashed #cfd6cb;border-radius:28px;padding:26px;text-align:center;font:700 28px Inter,sans-serif;color:#6E8689'>$0.00</div>\" } },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"Help me price it.\", \"createdAt\": 1750247400000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "recording.json", "content": "{\n  \"_name\": \"Recording — voice panel takes over the composer (mocked)\",\n  \"_note\": \"recording=true → RecordingPanel replaces the composer's text row. The real panel needs a live AudioContext/level + transcript; stub it via _mocks so the still renders the panel shell with a sample transcript.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"_signals\": { \"recording\": true, \"recElapsed\": 4, \"audioLevel\": 0.42, \"liveFinal\": \"Need a quote for a kitchen backsplash,\", \"liveInterim\": \"about thirty square feet\" },\n  \"_mocks\": { \"RecordingPanel\": { \"stub\": \"<div class='rec-panel'><div class='rec-panel__transcript-text'>Need a quote for a kitchen backsplash, <span class='interim'>about thirty square feet</span></div><button class='rec-panel__cancel'>✕</button><button class='rec-panel__stop'><span class='rec-panel__stop-square'></span></button></div>\" } },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"assistant\", \"kind\": \"text\", \"content\": \"Tap the mic and tell me about the job.\", \"createdAt\": 1750247400000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "recovery.json", "content": "{\n  \"_name\": \"Recovery — failed-send divider → contact-recovery card\",\n  \"_note\": \"A phase_divider whose payload carries an emailFailureReason and contractId, with a bound customer that has NO email → renders .recovery-card under the divider (add-email + Save & resend). Requires this divider's id to equal lastRecoveryDividerId — drive via _signals.\",\n  \"conversationId\": \"t1\",\n  \"userInitials\": \"TK\",\n  \"initialCustomer\": { \"id\": \"c1\", \"name\": \"Tom & Linda K.\", \"phoneNumber\": \"+15125550101\" },\n  \"_signals\": { \"lastRecoveryDividerId\": \"pd1\" },\n  \"initialMessages\": [\n    {\n      \"id\": \"pd1\",\n      \"conversationId\": \"t1\",\n      \"role\": \"system\",\n      \"kind\": \"phase_divider\",\n      \"content\": \"Couldn't email the contract\",\n      \"createdAt\": 1750247900000,\n      \"payload\": {\n        \"label\": \"Contract not delivered\",\n        \"contractId\": \"ct_1\",\n        \"channel\": \"email\",\n        \"emailFailureReason\": \"no email on file\"\n      }\n    }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "typing.json", "content": "{\n  \"_name\": \"Typing — last msg is user + sending → 3 typing-dots\",\n  \"_note\": \"The typing indicator shows when `sending` and the last message is from the user. Drive `sending` via _signals; isolate can't trigger a real send, so seed it.\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"_signals\": { \"sending\": true },\n  \"initialMessages\": [\n    { \"id\": \"m1\", \"conversationId\": \"t2\", \"role\": \"assistant\", \"kind\": \"text\", \"content\": \"Do you already have a price in mind, or should I help you price it?\", \"createdAt\": 1750247430000 },\n    { \"id\": \"m2\", \"conversationId\": \"t2\", \"role\": \"user\", \"kind\": \"text\", \"content\": \"Help me price it.\", \"createdAt\": 1750247490000 }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}, {"name": "wizard-step.json", "content": "{\n  \"_name\": \"Wizard step — config question + option buttons\",\n  \"_note\": \"kind=wizard, payload.stepId='config' (NOT 'customer', which renders its own panel) → .wiz with the question and clickable option buttons. Composer HIDDEN (unanswered wizard).\",\n  \"conversationId\": \"t2\",\n  \"userInitials\": \"ML\",\n  \"initialContract\": { \"id\": \"ct_2\", \"status\": \"draft\" },\n  \"initialMessages\": [\n    {\n      \"id\": \"w1\",\n      \"conversationId\": \"t2\",\n      \"role\": \"assistant\",\n      \"kind\": \"wizard\",\n      \"content\": \"What kind of job is this?\",\n      \"createdAt\": 1750247900000,\n      \"payload\": {\n        \"stepId\": \"config\",\n        \"stepIdx\": 0,\n        \"hint\": \"This sets the default contract language.\",\n        \"options\": [\n          { \"id\": \"residential\", \"label\": \"Residential\", \"sub\": \"Homeowner project\" },\n          { \"id\": \"commercial\", \"label\": \"Commercial\", \"sub\": \"Business / property mgmt\" },\n          { \"id\": \"blank\", \"label\": \"Start blank\", \"sub\": \"No preset terms\" }\n        ]\n      }\n    }\n  ],\n  \"sendLanguages\": [\"en\"]\n}\n"}], "css": "/* ============================================================\n   AsstChat — REPRESENTATIVE extract from static/assistant-page.css.\n   This is a FAITHFUL PARTIAL: AsstChat is an 8450-LOC island whose\n   styles live across ~700 rules in assistant-page.css (the .chat__*,\n   .msg*, .composer*, .action-card*, .continue-cta*, .phase-divider*,\n   .wiz*, .chat__price*, .chat__jobopts*, .rec-panel* families) plus the\n   MoneyInput island's own inline STYLES (see ../../money-input/css).\n   Extracted here: the chat shell, message-bubble TYPES, the inline\n   composer (incl. flash bounce), the recording/voice panel essentials,\n   the action-card (quote card) / continue-cta / phase-divider / wizard\n   step shells, and the price-capture screen — i.e. everything the cases\n   exercise. The 13 keyframes this component actually uses are included.\n   Class names are verbatim so a rebuild can diff against the source.\n   ============================================================ */\n\n/* ---- chat shell (the chat column owned by AsstChat's render) ---- */\n.chat {\n  flex: 1;\n  min-height: 0;\n  display: flex;\n  flex-direction: column;\n}\n.chat__scroll {\n  flex: 1;\n  overflow-y: auto;\n  padding: 18px 24px 12px;\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n}\n.chat__scroll::-webkit-scrollbar { width: 0; }\n\n/* honest-empty / first-screen */\n.chat__empty {\n  flex: 1;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  gap: 16px;\n  padding: 24px;\n  text-align: center;\n}\n.chat__empty-icon img { width: 64px; height: 64px; object-fit: contain; }\n.chat__empty-title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 22px;\n  color: var(--brand-teal);\n  margin: 0;\n}\n.chat__empty-prompts {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n  width: 100%;\n  max-width: 460px;\n}\n.chat__empty-prompt {\n  padding: 12px 16px;\n  border: 1px solid var(--border);\n  border-radius: 12px;\n  background: #fff;\n  color: var(--brand-teal);\n  font: inherit;\n  font-weight: 600;\n  text-align: left;\n  cursor: pointer;\n  transition: border-color 140ms, transform 140ms;\n}\n.chat__empty-prompt:hover {\n  border-color: var(--brand-green);\n  transform: translateY(-1px);\n}\n\n/* ============================================================\n   Message bubbles — the bubble TYPES rendered by visible.map().\n   Order in source: wiz-log → phase_divider → continue_cta → wizard\n   → action_card → onboarding demo CTA → plain text/voice/image.\n   ============================================================ */\n@keyframes msg-in {\n  from { opacity: 0; transform: translateY(8px) scale(0.98); }\n  to   { opacity: 1; transform: translateY(0) scale(1); }\n}\n.msg {\n  display: flex;\n  gap: 10px;\n  max-width: 88%;\n  animation: msg-in 220ms cubic-bezier(0.22, 1, 0.36, 1) both;\n}\n.msg--user { align-self: flex-end; flex-direction: row-reverse; }\n.msg__avatar {\n  width: 30px; height: 30px;\n  border-radius: 10px;\n  background: var(--brand-green);\n  display: grid; place-items: center;\n  flex-shrink: 0; overflow: hidden; margin-top: 2px;\n}\n.msg__avatar img { width: 22px; height: 22px; object-fit: contain; }\n.msg--user .msg__avatar {\n  background: var(--brand-teal); color: #fff;\n  font-family: var(--font-heading); font-weight: 800; font-size: 11px;\n}\n.msg__bubble {\n  padding: 10px 14px;\n  border-radius: 14px;\n  font-size: 13.5px;\n  line-height: 1.45;\n  color: var(--brand-teal);\n  border: 1px solid var(--border);\n  background: var(--coffee-50);\n}\n.msg--user .msg__bubble {\n  background: var(--brand-teal); color: #fff; border-color: var(--brand-teal);\n}\n.msg__time { font-size: 10.5px; color: var(--fg-subtle); font-weight: 600; margin-top: 4px; }\n.msg--user .msg__time { text-align: right; }\n.msg__image img { max-width: 240px; border-radius: 12px; display: block; }\n\n/* \"Bossie is thinking\" typing indicator (shown while sending) */\n.msg__bubble--typing { display: inline-flex; align-items: center; gap: 5px; padding: 12px 16px; }\n.typing-dot {\n  width: 7px; height: 7px; border-radius: 50%;\n  background: rgba(26, 83, 92, 0.55);\n  animation: typing-bounce 1.2s infinite ease-in-out;\n}\n.typing-dot:nth-child(2) { animation-delay: 0.15s; }\n.typing-dot:nth-child(3) { animation-delay: 0.30s; }\n@keyframes typing-bounce {\n  0%, 60%, 100% { transform: translateY(0);    opacity: 0.55; }\n  30%           { transform: translateY(-4px); opacity: 1; }\n}\n\n/* compact wizard pick-log line (user answer to a wizard step) */\n.wiz-log { display: flex; align-items: center; gap: 10px; padding: 6px 0; font-size: 14px; color: var(--fg); line-height: 1.45; }\n.wiz-log__check {\n  flex: 0 0 auto; width: 18px; height: 18px; border-radius: 50%;\n  background: var(--brand-green); color: #fff;\n  display: inline-flex; align-items: center; justify-content: center;\n}\n\n/* phase divider — full-width labelled separator */\n.phase-divider { display: flex; align-items: center; gap: 12px; margin: 8px 0; }\n.phase-divider__line { flex: 1; height: 1px; background: var(--border); }\n.phase-divider__label {\n  display: inline-flex; align-items: center; gap: 6px;\n  font-size: 11px; font-weight: 800; letter-spacing: 0.06em; text-transform: uppercase;\n  color: var(--fg-muted);\n}\n\n/* contact-recovery card (rendered inline under a failed send divider) */\n.recovery-card {\n  border: 1px solid var(--border); border-radius: 14px;\n  background: #fff; padding: 14px; margin-top: 10px;\n}\n.recovery-card__head { display: flex; flex-direction: column; gap: 4px; margin-bottom: 10px; }\n.recovery-card__hint { font-size: 12px; color: var(--fg-muted); }\n.recovery-card__fields { display: flex; flex-direction: column; gap: 8px; }\n.recovery-card__input { padding: 10px 12px; border: 1px solid var(--border); border-radius: 10px; font: inherit; }\n.recovery-card__save {\n  align-self: flex-start; padding: 9px 16px; border: 0; border-radius: 999px;\n  background: var(--brand-green); color: #fff; font-weight: 700; cursor: pointer;\n}\n.recovery-card__save:disabled { opacity: 0.6; cursor: not-allowed; }\n\n/* continue-CTA — clickable phase-transition card (terms/send/invoice) */\n.continue-cta {\n  display: flex; align-items: center; gap: 12px;\n  border: 1px solid var(--border); border-radius: 16px;\n  background: #fff; padding: 14px 16px;\n}\n.continue-cta--done { background: var(--green-50); border-color: var(--green-200); }\n.continue-cta__icon {\n  width: 36px; height: 36px; border-radius: 12px;\n  background: var(--brand-green); color: #fff;\n  display: grid; place-items: center; flex-shrink: 0;\n}\n.continue-cta__txt { flex: 1; min-width: 0; }\n.continue-cta__title { font-weight: 800; color: var(--brand-teal); }\n.continue-cta__sub { font-size: 12.5px; color: var(--fg-muted); margin-top: 2px; }\n.continue-cta__btn {\n  border: 0; border-radius: 999px; padding: 9px 16px;\n  background: var(--brand-green); color: #fff; font-weight: 700; cursor: pointer;\n  display: inline-flex; align-items: center; gap: 6px;\n}\n.continue-cta__btn:disabled { opacity: 0.6; cursor: not-allowed; }\n\n/* action-card — the in-chat Quote + Agreement card (Draft→Sent→Approved) */\n.action-card { border: 1px solid var(--border); border-radius: 16px; background: #fff; overflow: hidden; }\n.action-card__head { display: flex; align-items: center; gap: 10px; padding: 14px 16px; border-bottom: 1px solid var(--border); }\n.action-card__icon { width: 32px; height: 32px; border-radius: 10px; background: var(--coffee-50); color: var(--brand-teal); display: grid; place-items: center; flex-shrink: 0; }\n.action-card__title { font-weight: 800; color: var(--brand-teal); }\n.action-card__chip { font-size: 11px; font-weight: 800; padding: 3px 10px; border-radius: 999px; background: var(--coffee-50); color: var(--fg-muted); }\n.action-card__body { padding: 12px 16px; display: flex; flex-direction: column; gap: 6px; }\n.action-card__details-label { font-size: 11px; font-weight: 800; letter-spacing: 0.05em; text-transform: uppercase; color: var(--fg-subtle); }\n.action-card__details-list { margin: 4px 0 0; padding-left: 18px; font-size: 13px; color: var(--brand-teal); }\n.action-card__row { display: flex; justify-content: space-between; font-size: 13px; color: var(--brand-teal); }\n.action-card__cta { display: flex; gap: 8px; padding: 0 16px 14px; }\n.action-card__btn { border: 1px solid var(--border); border-radius: 999px; padding: 8px 14px; background: #fff; font-weight: 700; cursor: pointer; display: inline-flex; align-items: center; gap: 6px; }\n.action-card__btn--primary { background: var(--brand-green); color: #fff; border-color: var(--brand-green); }\n.action-card__btn:disabled { opacity: 0.6; cursor: not-allowed; }\n\n/* wizard step card — question + clickable option buttons */\n.wiz { border: 1px solid var(--border); border-radius: 16px; background: #fff; padding: 16px; }\n.wiz__step-q { margin: 0 0 4px; font-size: 15px; font-weight: 800; color: var(--brand-teal); }\n.wiz__step-hint { font-size: 12px; color: var(--fg-muted); margin-bottom: 10px; }\n\n/* ============================================================\n   Inline composer — text + mic/voice. Hidden during tap-only screens.\n   ============================================================ */\n.composer {\n  border-top: 1px solid var(--border);\n  padding: 14px 18px calc(16px + env(safe-area-inset-bottom, 0px));\n  background: #fff;\n}\n.composer__inner {\n  background: var(--coffee-50);\n  border: 1px solid var(--border);\n  border-radius: 16px;\n  padding: 8px 8px 8px 14px;\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  transition: border-color 160ms;\n}\n.composer__inner:focus-within { border-color: var(--brand-green); background: #fff; }\n.composer__input {\n  flex: 1; border: none; outline: none; background: transparent;\n  font-family: var(--font-body);\n  /* must be >=16px to stop iOS Safari focus-zoom */\n  font-size: 16px; color: var(--brand-teal);\n  resize: none; padding: 8px 0; line-height: 1.4;\n  max-height: 120px; min-height: 22px;\n}\n.composer__input::placeholder { color: var(--fg-subtle); }\n.composer__tools { display: flex; gap: 4px; align-items: center; }\n/* mic is the PRIMARY affordance — brand-pink, larger than send */\n.composer__mic {\n  width: 44px; height: 44px;\n  border: 1px solid rgba(20, 72, 82, 0.14); border-radius: 999px;\n  background: #fff; color: var(--brand-pink);\n  display: grid; place-items: center; cursor: pointer;\n  transition: all 140ms;\n}\n.composer__mic:hover { transform: translateY(-1px); }\n.composer__send {\n  width: 36px; height: 36px; border: none; border-radius: 10px;\n  background: var(--brand-green); color: #fff; cursor: pointer;\n  display: grid; place-items: center;\n  box-shadow: 0 4px 10px rgba(81, 152, 67, 0.35);\n}\n.composer__send:hover { transform: translateY(-1px); }\n.composer__send:disabled { opacity: 0.5; cursor: not-allowed; box-shadow: none; }\n.composer__hint {\n  font-size: 10.5px; color: var(--brand-green); font-weight: 700;\n  margin-top: 8px; display: flex; align-items: center; gap: 6px; padding: 0 4px;\n  animation: composerHintPulse 1.8s ease-in-out infinite;\n}\n.composer__hint kbd {\n  font-family: var(--font-mono); background: var(--coffee-100);\n  padding: 1px 6px; border-radius: 4px; font-size: 10px; font-weight: 700; color: var(--brand-teal);\n}\n.composer__err { color: var(--danger); font-size: 12.5px; margin-bottom: 8px; }\n@keyframes composerHintPulse {\n  0%, 100% { opacity: 1; }\n  50%      { opacity: 0.45; }\n}\n/* attention cue: a real bouncy-ball jump + damped jell-o wobble (full\n   18-stop keyframe lives in source ~7007; condensed here) */\n@keyframes composer-bounce {\n  0%   { transform: translate3d(0, 0, 0) rotate(0deg); animation-timing-function: cubic-bezier(.165,.84,.44,1); }\n  20%  { transform: translate3d(0, -200px, 0) rotate(-1.4deg); animation-timing-function: cubic-bezier(.55,0,.95,.4); }\n  40%  { transform: translate3d(2px, 0, 0) rotate(1.7deg); animation-timing-function: cubic-bezier(.165,.84,.44,1); }\n  56%  { transform: translate3d(0, -80px, 0) rotate(-0.9deg); animation-timing-function: cubic-bezier(.55,0,.95,.4); }\n  72%  { transform: translate3d(-1px, 0, 0) rotate(0.8deg); }\n  82%  { transform: translate3d(0, -30px, 0) rotate(-0.4deg); }\n  91%  { transform: translate3d(0, 0, 0) rotate(-0.35deg); }\n  96%  { transform: translate3d(0, 0, 0) rotate(-0.12deg); }\n  100% { transform: translate3d(0, 0, 0) rotate(0deg); }\n}\n.composer--flash .composer__inner {\n  transform-origin: 50% 100%;\n  animation: composer-bounce 1.6s 250ms 1 both;\n  will-change: transform;\n}\n\n/* ============================================================\n   Recording / voice panel — replaces the composer while mic is hot.\n   (Orb + filters + extra stops live in source ~7196–7497; the panel\n   shell + the keyframes are extracted here.)\n   ============================================================ */\n.rec-panel {\n  border: 1px solid var(--border); border-radius: 18px;\n  background: #fff; padding: 14px 16px;\n  display: flex; align-items: center; gap: 14px;\n  position: relative; overflow: hidden;\n  animation: rec-panel-enter 240ms cubic-bezier(0.2, 0.9, 0.25, 1.05);\n}\n.rec-panel__bg { position: absolute; inset: 0; z-index: 0; animation: rec-panel-bg-drift 14s ease-in-out infinite alternate; }\n.rec-panel__transcript-text { font-size: 15px; line-height: 1.5; color: var(--brand-teal); }\n.rec-panel__transcript-text .interim { color: var(--fg-muted); font-style: italic; }\n.rec-panel__final-chunk { animation: rec-final-fade 280ms ease-out both; }\n.rec-panel__caret { animation: rec-caret-blink 1s steps(2, start) infinite; }\n.rec-panel__stop {\n  width: 48px; height: 48px; display: grid; place-items: center;\n  background: linear-gradient(135deg, #5fb352, #4a8c3d); color: #fff;\n  border: 0; border-radius: 999px; cursor: pointer; flex-shrink: 0;\n  transition: transform 140ms ease, box-shadow 140ms ease, filter 140ms ease;\n}\n.rec-panel__stop-square { display: block; width: 12px; height: 12px; background: #fff; border-radius: 3px; }\n.rec-panel__cancel { width: 36px; height: 36px; border-radius: 999px; border: 1px solid var(--border); background: #fff; cursor: pointer; flex-shrink: 0; }\n@keyframes rec-panel-enter    { from { opacity: 0; transform: translateY(8px) scale(0.98); } to { opacity: 1; transform: none; } }\n@keyframes rec-panel-bg-drift { from { transform: translate3d(-2%, -1%, 0); } to { transform: translate3d(2%, 1%, 0); } }\n@keyframes rec-ring           { 0% { transform: scale(0.6); opacity: 0.55; } 100% { transform: scale(2.2); opacity: 0; } }\n@keyframes rec-live-pulse     { 0% { box-shadow: 0 0 0 0 rgba(255,107,107,0.45); } 100% { box-shadow: 0 0 0 12px rgba(255,107,107,0); } }\n@keyframes rec-final-fade     { from { opacity: 0; transform: translateY(2px); } to { opacity: 1; transform: none; } }\n@keyframes rec-caret-blink    { 0%, 49% { opacity: 1; } 50%, 100% { opacity: 0; } }\n\n/* \"Bossie is writing your details/options\" loading dots */\n.chat__details-dots { display: inline-flex; gap: 4px; }\n.chat__details-dots span { width: 6px; height: 6px; border-radius: 50%; background: var(--brand-green); animation: chat-details-pulse 1.1s infinite ease-in-out; }\n.chat__details-dots span:nth-child(2) { animation-delay: 0.15s; }\n.chat__details-dots span:nth-child(3) { animation-delay: 0.30s; }\n@keyframes chat-details-in    { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: none; } }\n@keyframes chat-details-pulse { 0%, 100% { opacity: 0.4; } 50% { opacity: 1; } }\n\n/* ============================================================\n   Price-capture screen — wraps the embedded <MoneyInput/> island.\n   ============================================================ */\n@keyframes chat-price-in { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: translateY(0); } }\n.chat__price-capture {\n  width: 100%; max-width: 460px;\n  display: flex; flex-direction: column; gap: 14px;\n  animation: chat-price-in 320ms cubic-bezier(0.2, 0.7, 0.2, 1);\n}\n.chat__price-capture-head { display: flex; flex-direction: column; gap: 4px; text-align: left; position: relative; padding-top: 4px; }\n.chat__price-back {\n  position: absolute; top: 0; left: -2px;\n  display: inline-flex; align-items: center; gap: 4px;\n  appearance: none; border: 0; background: transparent;\n  color: var(--fg-muted); font-size: 12px; font-weight: 600; cursor: pointer;\n  padding: 4px 6px; border-radius: 6px;\n  transition: color 0.12s, background 0.12s;\n}\n.chat__price-back:hover { color: var(--brand-teal); background: rgba(26, 83, 92, 0.06); }\n.chat__price-title { margin: 22px 0 0; font-size: 18px; font-weight: 700; color: var(--brand-teal); letter-spacing: -0.01em; }\n.chat__price-sub { margin: 0; font-size: 13px; color: var(--fg-muted); }\n.chat__price-continue {\n  align-self: flex-end; appearance: none; border: 0;\n  background: var(--brand-green); color: #fff;\n  font-weight: 700; font-size: 14px; padding: 11px 22px; border-radius: 999px;\n  cursor: pointer; box-shadow: 0 6px 18px -6px rgba(81, 152, 67, 0.55);\n  transition: background 0.14s, transform 0.14s, box-shadow 0.14s, opacity 0.14s;\n}\n.chat__price-continue:hover:not(:disabled) { background: #437b37; transform: translateY(-1px); }\n.chat__price-continue:disabled { background: #cfd6cb; box-shadow: none; cursor: not-allowed; opacity: 0.7; }\n\n/* job-options picker (three LLM scope-of-work cards) */\n.chat__jobopts { width: 100%; max-width: 560px; display: flex; flex-direction: column; gap: 8px; animation: chat-price-in 320ms cubic-bezier(0.2, 0.7, 0.2, 1); }\n.chat__jobopt { border: 1px solid var(--border); border-radius: 14px; background: #fff; padding: 14px; cursor: pointer; }\n.chat__jobopt.is-selected { border-color: var(--brand-green); box-shadow: 0 0 0 2px rgba(81,152,67,0.18); }\n.chat__jobopt-bullets { margin: 8px 0 0; padding-left: 18px; font-size: 13px; color: var(--brand-teal); }\n.chat__jobopt-bullet.is-deleted { text-decoration: line-through; opacity: 0.5; }\n\n/* generic spinner used by inline busy buttons */\n@keyframes spin { to { transform: rotate(360deg); } }\n.spinner { display: inline-block; width: 13px; height: 13px; border: 2px solid rgba(255,255,255,0.4); border-top-color: #fff; border-radius: 50%; animation: spin 0.7s linear infinite; }\n\n/* ============================================================\n   Responsive — AsstChat is laid out by the PAGE's .asst grid, not by\n   its own @media. The chat column is `minmax(0, 1fr)` between the\n   280px threads rail and the (conditional) 360px docpane:\n     ≥1100px : 280px | 1fr | 360px  (docpane present)\n     ≤1099px : 260px | 1fr          (docpane hidden)\n     ≤880px  : 1fr                  (single column, threads → drawer)\n     ≤480px  : recording panel tightens (orb 44px, 12px padding)\n   The composer's safe-area padding + `--app-vh` (from the MobileViewport\n   island) keep the input above the iOS keyboard/home indicator.\n   ============================================================ */\n\n/* ---- reduced motion ----\n   AsstChat owns NO component-local reduced-motion guard except the .msg\n   and .composer__hint blocks below; everything else (composer-bounce,\n   rec-*, chat-price-in, typing-bounce, spin, …) relies on the GLOBAL\n   tokens clamp (animation-duration: 0.01ms !important). Preserve both. */\n@media (prefers-reduced-motion: reduce) {\n  .msg { animation: none; }\n  .composer__hint { animation: none; }\n}\n", "cssName": "asst-chat.css", "source": "import { useEffect, useRef, useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { detailLines } from \"../lib/format.ts\";\nimport {\n  computePaymentSplit,\n  type MilestoneRole,\n} from \"../lib/payment-split.ts\";\nimport { api } from \"../lib/api.ts\";\nimport {\n  assistantClient,\n  type ContractLite,\n  type CustomerLite,\n  type JobOption,\n  type Message,\n} from \"../clients/assistant.ts\";\nimport { filesClient } from \"../clients/files.ts\";\nimport { quotesClient } from \"../clients/quotes.ts\";\nimport { clientsClient } from \"../clients/clients.ts\";\nimport { contractsClient } from \"../clients/contracts.ts\";\nimport { readCached, refreshDash, subscribeDash } from \"../lib/dash-cache.ts\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\nimport { localizeTermValue } from \"../lib/term-i18n.ts\";\nimport MoneyInput from \"./MoneyInput.tsx\";\n\ntype WizardFieldType = \"percent\" | \"number\" | \"currency\" | \"days\" | \"text\";\n\ninterface WizardFollowUpField {\n  id: string;\n  label: string;\n  type: WizardFieldType;\n  default?: number | string;\n  min?: number;\n  max?: number;\n}\n\ninterface WizardOption {\n  id: string;\n  label: string;\n  sub?: string;\n  isCustom?: boolean;\n  followUp?: { fields: WizardFollowUpField[] };\n}\n\n/**\n * Term-edit picker fallback: a static map of stepId → preset options for\n * the contract-terms wizard. Used when the term row is being edited\n * inline and no matching wizard message is in chat scope (older threads,\n * or threads where the wizard messages were pruned). Mirrors\n * `CONTRACT_TERMS_WIZARD_V1` in the backend — keep in sync if that spec\n * grows new steps. The `customer` step is intentionally excluded since\n * its picker has its own dedicated panel.\n */\nconst TERM_OPTIONS_FALLBACK: Record<\n  string,\n  { labelKey: string; subKey?: string }[]\n> = {\n  config: [\n    {\n      labelKey: \"asstChat.terms.config.residential\",\n      subKey: \"asstChat.terms.config.residentialSub\",\n    },\n    {\n      labelKey: \"asstChat.terms.config.commercial\",\n      subKey: \"asstChat.terms.config.commercialSub\",\n    },\n    {\n      labelKey: \"asstChat.terms.config.blank\",\n      subKey: \"asstChat.terms.config.blankSub\",\n    },\n  ],\n  start_date: [\n    { labelKey: \"asstChat.terms.startDate.rightAway\" },\n    { labelKey: \"asstChat.terms.startDate.nextWeek\" },\n    { labelKey: \"asstChat.terms.startDate.nextMonth\" },\n  ],\n  wraps: [\n    { labelKey: \"asstChat.duration.preset.oneDay\" },\n    { labelKey: \"asstChat.duration.preset.twoThreeDays\" },\n    { labelKey: \"asstChat.duration.preset.oneWeek\" },\n    { labelKey: \"asstChat.duration.preset.twoWeeks\" },\n  ],\n  payment_terms: [\n    {\n      labelKey: \"asstChat.payment.preset.onCompletion\",\n      subKey: \"asstChat.terms.payment.onCompletionSub\",\n    },\n    {\n      labelKey: \"asstChat.payment.preset.fiftyFifty\",\n      subKey: \"asstChat.terms.payment.fiftyFiftySub\",\n    },\n    {\n      labelKey: \"asstChat.payment.preset.threeThreeForty\",\n      subKey: \"asstChat.terms.payment.threeThreeFortySub\",\n    },\n    {\n      labelKey: \"asstChat.payment.preset.depositBalance\",\n      subKey: \"asstChat.terms.payment.depositBalanceSub\",\n    },\n  ],\n  warranty: [\n    { labelKey: \"asstChat.warranty.preset.none\" },\n    { labelKey: \"asstChat.warranty.preset.sixMonths\" },\n    { labelKey: \"asstChat.warranty.preset.twelveMonths\" },\n    { labelKey: \"asstChat.warranty.preset.twentyFourMonths\" },\n  ],\n  termination: [\n    { labelKey: \"asstChat.terms.termination.sevenDays\" },\n    { labelKey: \"asstChat.terms.termination.fourteenDays\" },\n    { labelKey: \"asstChat.terms.termination.thirtyDays\" },\n  ],\n  dispute: [\n    {\n      labelKey: \"asstChat.terms.dispute.mediation\",\n      subKey: \"asstChat.terms.dispute.mediationSub\",\n    },\n    {\n      labelKey: \"asstChat.terms.dispute.arbitration\",\n      subKey: \"asstChat.terms.dispute.arbitrationSub\",\n    },\n    {\n      labelKey: \"asstChat.terms.dispute.court\",\n      subKey: \"asstChat.terms.dispute.courtSub\",\n    },\n  ],\n  governing_state: [\n    { labelKey: \"asstChat.terms.governingState.business\" },\n    { labelKey: \"asstChat.terms.governingState.jobSite\" },\n  ],\n  state_notices: [\n    {\n      labelKey: \"asstChat.terms.stateNotices.yes\",\n      subKey: \"asstChat.terms.stateNotices.yesSub\",\n    },\n    {\n      labelKey: \"asstChat.terms.stateNotices.no\",\n      subKey: \"asstChat.terms.stateNotices.noSub\",\n    },\n    {\n      labelKey: \"asstChat.terms.stateNotices.review\",\n      subKey: \"asstChat.terms.stateNotices.reviewSub\",\n    },\n  ],\n};\n\n/** One editable bullet on the \"Job Details\" picker screen. `deleted`\n *  is a soft toggle (the \"x\"/restore affordance) so the row can be\n *  brought back without losing its text. */\ninterface BulletDraft {\n  id: string;\n  text: string;\n  deleted: boolean;\n}\n\n/** Editable mirror of a server-returned JobOption — the picker lets the\n *  contractor delete/restore/edit bullets and append a custom one. */\ninterface JobOptionDraft {\n  id: string;\n  jobName: string;\n  summary: string;\n  bullets: BulletDraft[];\n  /** Per-language content of the ORIGINAL (pre-edit) option, used to fill the\n   *  quote's descriptionByLang on pick without re-translating unedited bullets. */\n  byLang?: Record<\n    string,\n    { jobName: string; summary: string; bullets: string[] }\n  >;\n}\n\n/** Sentinel id for the \"write it myself\" card — a free-text job description\n *  the contractor types verbatim instead of picking a generated option. */\nconst CUSTOM_OPTION_ID = \"__custom__\";\n\nlet bulletSeq = 0;\nfunction toOptionDrafts(options: JobOption[]): JobOptionDraft[] {\n  return options.map((o) => ({\n    id: o.id,\n    jobName: o.jobName,\n    summary: o.summary,\n    bullets: o.bullets.map((text) => ({\n      id: `b${++bulletSeq}`,\n      text,\n      deleted: false,\n    })),\n    byLang: o.byLang,\n  }));\n}\n\n/** Client-side last resort if the options endpoint itself errors (network\n *  / 4xx). The backend already returns a heuristic fallback on LLM failure,\n *  so this only fires when the request never completed. Mirrors that\n *  heuristic so the picker still renders three usable options. */\nfunction localFallbackOptions(raw: string, lang: Lang): JobOption[] {\n  const lines = raw\n    .split(/[\\n.;]+/)\n    .map((l) => l.trim().replace(/\\s+/g, \" \"))\n    .filter(Boolean);\n  const base = (lines.length > 0 ? lines : [raw.trim()]).slice(0, 4);\n  const summary = base[0]?.split(/\\s+/).slice(0, 8).join(\" \") ||\n    tFor(lang, \"asstChat.newJob\");\n  const jobName = summary.split(/\\s+/).slice(0, 3).join(\" \");\n  return [\n    { id: \"opt1\", jobName, summary, bullets: base },\n    { id: \"opt2\", jobName, summary, bullets: base.slice(0, 3) },\n    {\n      id: \"opt3\",\n      jobName,\n      summary,\n      bullets: [...base.slice(0, 3), tFor(lang, \"asstChat.jobsiteCleanup\")]\n        .slice(0, 4),\n    },\n  ];\n}\n\ninterface ActionCardLineItem {\n  description: string;\n  amountCents: number;\n}\n\ninterface ActionCardPayload {\n  actionType?: string;\n  status?: \"draft\" | \"sent\" | \"viewed\" | \"approved\" | \"void\" | string;\n  quoteId?: string;\n  customerId?: string;\n  /** Polished narrative produced from the user's raw job-details input. */\n  description?: string;\n  lineItems?: ActionCardLineItem[];\n  totalCents?: number;\n}\n\n/** Compose the composer placeholder. During onboarding we want it to\n *  echo the question Bossie is asking rather than the generic job-mode\n *  slab example, otherwise new users wonder if they're in the right\n *  surface. After onboarding hands off, revert to the job example. */\nfunction composerPlaceholder(msgs: Message[], lang: Lang): string {\n  const lastAssistant = [...msgs].reverse().find((m) =>\n    m.role === \"assistant\" && m.kind === \"text\"\n  );\n  const text = (lastAssistant?.content ?? \"\").toLowerCase();\n  if (/what should i call you|what.s your (first )?name/.test(text)) {\n    return tFor(lang, \"asstChat.composer.firstName\");\n  }\n  if (/what.s your business called|business name/.test(text)) {\n    return tFor(lang, \"asstChat.composer.businessName\");\n  }\n  if (/looks like you.re in|which state|right state/.test(text)) {\n    return tFor(lang, \"asstChat.composer.stateCode\");\n  }\n  if (/business address|paste it on one line/.test(text)) {\n    return tFor(lang, \"asstChat.composer.address\");\n  }\n  if (/email/.test(text)) return tFor(lang, \"asstChat.composer.email\");\n  if (/payment|venmo|zelle|cash app|how.*get paid/.test(text)) {\n    return tFor(lang, \"asstChat.composer.payment\");\n  }\n  return tFor(lang, \"asstChat.composer.default\");\n}\n\n/** Map a Quote/Contract status to the human-facing chip label on the\n *  in-chat Quote+Agreement card. Keeps the chip in sync with the doc's\n *  lifecycle: Draft → Sent → Viewed → Approved. */\nfunction statusChipLabel(status: string | undefined, lang: Lang): string {\n  switch ((status ?? \"draft\").toLowerCase()) {\n    case \"sent\":\n      return tFor(lang, \"status.sent\");\n    case \"opened\":\n    case \"viewed\":\n      return tFor(lang, \"status.viewed\");\n    case \"won\":\n    case \"accepted\":\n    case \"approved\":\n    case \"signed\":\n      return tFor(lang, \"asstChat.statusChip.approved\");\n    case \"void\":\n    case \"declined\":\n    case \"lost\":\n      return tFor(lang, \"status.declined\");\n    default:\n      return tFor(lang, \"status.draft\");\n  }\n}\n\nfunction fmtUSD(cents: number): string {\n  if (!Number.isFinite(cents)) return \"$0\";\n  const dollars = cents / 100;\n  return dollars.toLocaleString(\"en-US\", {\n    style: \"currency\",\n    currency: \"USD\",\n    maximumFractionDigits: 2,\n  });\n}\n\ninterface Props {\n  conversationId?: string;\n  initialMessages: Message[];\n  /** Customer bound to this conversation (only present in phase 2). */\n  initialCustomer?: CustomerLite;\n  /** Contract bound to this conversation (only present once the wizard completes). */\n  initialContract?: ContractLite;\n  /** 1-2 letter user-avatar string. Pre-derived on the server so we don't\n   *  flash a stale or default value while hydrating. */\n  userInitials?: string;\n  /** Contractor \"FROM\" details for the quote/agreement preview card\n   *  (roadmap p.5 — Quote & Agreement Preview.docx). */\n  from?: {\n    business?: string;\n    name?: string;\n    phone?: string;\n    email?: string;\n  };\n  /** Languages the contractor can send in (from Settings checkboxes). Drives\n   *  the quote-review \"Preview in\" language toggle. Defaults to [\"en\"]. */\n  sendLanguages?: string[];\n  /** The contractor's own UI language — drives all contractor-facing chrome.\n   *  Defaults to \"en\". (The customer-facing quote preview uses its own\n   *  `previewLang` toggle, sourced from `sendLanguages`.) */\n  lang?: Lang;\n}\n\n/** Derive a stable 1-2 letter avatar string. Mirrors the backend\n *  `computeInitials(name, businessName)` so the chat bubble matches the\n *  sidebar disc. Single-token name + a business → first letter of each\n *  (\"Diego\" + \"Riley Roofing Co.\" → \"DR\"). No name and no business →\n *  \"👤\". Phone digits are never used. */\nexport function deriveUserInitials(input: {\n  name?: string;\n  businessName?: string;\n  phoneNumber?: string;\n}): string {\n  const name = input.name?.trim();\n  const biz = input.businessName?.trim();\n  if (name) {\n    const parts = name.split(/\\s+/).filter(Boolean);\n    if (parts.length === 1) {\n      if (biz) {\n        const bizParts = biz.split(/\\s+/).filter(Boolean);\n        if (bizParts.length >= 1) {\n          return (parts[0][0] + bizParts[0][0]).toUpperCase();\n        }\n      }\n      return parts[0].slice(0, 2).toUpperCase();\n    }\n    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();\n  }\n  if (biz) {\n    const bizParts = biz.split(/\\s+/).filter(Boolean);\n    if (bizParts.length >= 2) {\n      return (bizParts[0][0] + bizParts[1][0]).toUpperCase();\n    }\n    if (bizParts.length === 1) return bizParts[0].slice(0, 2).toUpperCase();\n  }\n  return \"👤\";\n}\n\nfunction fmtTime(ts: number | string): string {\n  const d = typeof ts === \"string\" ? new Date(ts) : new Date(ts);\n  return d.toLocaleTimeString([], { hour: \"numeric\", minute: \"2-digit\" });\n}\n\nfunction fmtKB(bytes: number): string {\n  return bytes < 1024 ? `${bytes} B` : `${(bytes / 1024).toFixed(1)} KB`;\n}\n\ninterface PaymentMilestone {\n  label: string;\n  /** Percentage of the total. Omitted for absolute milestones (e.g. Net 15). */\n  pct?: number;\n  amountCents: number;\n}\n\n/**\n * Translate the wizard's picked Payment terms into a milestone schedule.\n * Inputs come straight from the wizard option labels (e.g. \"30 / 30 / 40\",\n * \"50 / 50\", \"Net 15 — full\", \"Deposit + balance\") or a custom free-text\n * answer the user typed. Returns null when we can't confidently parse —\n * the UI then falls back to the single Total Due number.\n *\n * Rounding: amounts are rounded to whole cents; the LAST milestone\n * absorbs the rounding remainder so the sum equals the total exactly.\n */\nfunction buildPaymentMilestones(\n  value: string,\n  totalCents: number,\n  lang: Lang,\n): PaymentMilestone[] | null {\n  // All money comes from the shared #payment-split source of truth so this\n  // preview matches the signed contract, the PDF, and the actual invoices.\n  // Only the display labels live here.\n  const parts = computePaymentSplit(value, totalCents);\n  // A single full payment → return null so the caller shows the plain\n  // Total Due (unchanged behavior for \"net X\" / unrecognized terms).\n  if (parts.length === 0 || (parts.length === 1 && parts[0].role === \"full\")) {\n    return null;\n  }\n  const labelFor: Record<MilestoneRole, string> = {\n    deposit: tFor(lang, \"asstChat.milestone.deposit\"),\n    midpoint: tFor(lang, \"asstChat.milestone.midpoint\"),\n    milestone: tFor(lang, \"asstChat.milestone.milestone\"),\n    completion: tFor(lang, \"asstChat.milestone.completion\"),\n    full: tFor(lang, \"asstChat.milestone.full\"),\n  };\n  return parts.map((p) => ({\n    label: labelFor[p.role],\n    pct: p.pct,\n    amountCents: p.amountCents,\n  }));\n}\n\n/** Labels for the quote-review PREVIEW language toggle, and Spanish copy for\n *  the card chrome. Mirrors the customer-facing public quote/contract pages\n *  (routes/q/[id].tsx, contract-doc) so the contractor's preview matches what\n *  the customer actually receives. Free-text the contractor typed (term\n *  values, line-item names) stays verbatim — exactly like the public pages. */\n/** Language endonym labels for the preview toggle. Keyed by send-language\n *  code; the value (endonym) is identical across UI languages, so the\n *  i18n key resolves to the same string regardless of `previewLang`. */\nconst SEND_LANG_LABEL_KEYS: Record<string, string> = {\n  en: \"asstChat.previewLang.en\",\n  es: \"asstChat.previewLang.es\",\n};\n/** Term-row label keys by wizard stepId. The preview resolves these in the\n *  selected preview language (so the row label matches the agreement the\n *  customer receives), falling back to the stored label for unknown steps. */\nconst TERM_LABEL_KEYS: Record<string, string> = {\n  config: \"asstChat.preview.termLabel.config\",\n  start_date: \"asstChat.preview.termLabel.startDate\",\n  wraps: \"asstChat.preview.termLabel.wraps\",\n  time_to_complete: \"asstChat.preview.termLabel.timeToComplete\",\n  payment_terms: \"asstChat.preview.termLabel.payment\",\n  warranty: \"asstChat.preview.termLabel.warranty\",\n};\n\nexport default function AsstChat({\n  conversationId,\n  initialMessages,\n  initialCustomer,\n  initialContract,\n  userInitials = \"?\",\n  from,\n  sendLanguages,\n}: Props) {\n  // Self-source the reactive UI language so this island re-renders live when\n  // the contractor flips the language in Settings. `lang?: Lang` remains on\n  // Props as an optional SSR seed but is intentionally ignored here.\n  const lang = langSignal.value;\n  // Languages offered in the quote-review \"Preview in\" toggle. Start from the\n  // contractor's configured send languages, but ALWAYS include their app\n  // language (first → the default) so e.g. a Spanish-app contractor can always\n  // preview/send in Spanish even when their send languages are English-only.\n  const previewLangOptions =\n    (sendLanguages && sendLanguages.length ? sendLanguages : [\"en\"]).filter((\n      l,\n    ) => l in SEND_LANG_LABEL_KEYS);\n  const sendLangs = Array.from(new Set([lang, ...previewLangOptions]))\n    .filter((l) => l in SEND_LANG_LABEL_KEYS);\n  const [convoId, setConvoId] = useState<string | undefined>(conversationId);\n  const [messages, setMessages] = useState<Message[]>(initialMessages);\n  const [customer, setCustomer] = useState<CustomerLite | undefined>(\n    initialCustomer,\n  );\n  const [contract, setContract] = useState<ContractLite | undefined>(\n    initialContract,\n  );\n  /** Per-user \"see what your customer sees\" sample quote URL. Minted\n   *  lazily — eagerly fetched on mount when the synthetic onboarding-\n   *  handoff CTA is in the thread so cmd+click works without a round-\n   *  trip. The click handler falls back to inline mint+open if this\n   *  isn't ready yet. */\n  const [sampleQuoteUrl, setSampleQuoteUrl] = useState<string | undefined>(\n    undefined,\n  );\n  /** Swap-customer pencil in the quote-review hero. Lazily loads the\n   *  user's saved customers on first open. */\n  const [customerPickerOpen, setCustomerPickerOpen] = useState(false);\n  const [customerPickerList, setCustomerPickerList] = useState<\n    CustomerLite[] | null\n  >(null);\n  const [customerPickerSearch, setCustomerPickerSearch] = useState(\"\");\n  const [customerPickerBusy, setCustomerPickerBusy] = useState(false);\n  const [quoteId, setQuoteId] = useState<string | undefined>();\n  /** Full quote row, fetched lazily when the conversation has a quoteId.\n   *  Used by the post-wizard quote-review preview to render `description`\n   *  + `lineItems` without depending on an in-thread action_card. */\n  const [quote, setQuote] = useState<\n    | {\n      id: string;\n      summary?: string;\n      /** ≤3-word job title — the platform-wide identifier (roadmap p.10). */\n      jobName?: string;\n      description?: string;\n      descriptionByLang?: Record<string, string>;\n      lineItems?: {\n        description: string;\n        quantity?: number;\n        unit?: string;\n        price?: number;\n      }[];\n      estimatedTotal?: number;\n    }\n    | undefined\n  >();\n  const [draft, setDraft] = useState(\"\");\n  /** Inline price-capture flow opened by the \"I already have my price\"\n   *  empty-state prompt. When set, renders the MoneyInput card in place\n   *  of the three preset prompts. `priceCents` tracks the live value so\n   *  Continue can hand the value to the phase-2 kickoff. */\n  const [priceCaptureOpen, setPriceCaptureOpen] = useState(false);\n  const [priceCents, setPriceCents] = useState<number | null>(null);\n  /** Set after the user clicks Continue on the price step. While true,\n   *  the chat input no longer routes to the LLM chat — instead its\n   *  next submission is treated as raw job-details, sent through the\n   *  polish endpoint, then used to seed the new quote + phase 2 wizard. */\n  const [awaitingJobDetails, setAwaitingJobDetails] = useState(false);\n  const [pendingPriceCents, setPendingPriceCents] = useState<number | null>(\n    null,\n  );\n  /** First-button flow is now \"details first, price second.\" When the user\n   *  taps \"I know my price, write it up.\" we ask for the job details up\n   *  front and stash the raw text here. After the user types it and\n   *  submits, we flip to the price-capture screen with this populated,\n   *  and the price-Continue handler combines both pieces to build the\n   *  quote. (The other two flows ignore this — they go through the LLM.) */\n  const [pendingJobDetailsRaw, setPendingJobDetailsRaw] = useState<\n    string | null\n  >(null);\n  /** Captures the raw text the user submitted at the job-details step\n   *  so we can render an optimistic user bubble + \"Polishing…\" indicator\n   *  while the polish + create-quote + transition chain runs. */\n  const [submittedJobDetails, setSubmittedJobDetails] = useState<string | null>(\n    null,\n  );\n  /** \"Job Details\" picker screen (the LLM-generated scope-of-work options).\n   *  Shown at the END of phase 2, right before the quote review. */\n  const [jobOptionsOpen, setJobOptionsOpen] = useState(false);\n  // Roadmap p.10: \"I know the job, help me price it.\" — when set, the\n  // price-capture screen shows three LLM-suggested tiers (+ custom entry).\n  const [suggestPricing, setSuggestPricing] = useState(false);\n  const [priceSuggestions, setPriceSuggestions] = useState<\n    Array<\n      { tier: string; label: string; priceCents: number; rationale: string }\n    > | null\n  >(null);\n  const [optionsLoading, setOptionsLoading] = useState(false);\n  const [jobOptions, setJobOptions] = useState<JobOptionDraft[] | null>(null);\n  const [selectedOptionId, setSelectedOptionId] = useState<string | null>(null);\n  /** Which bullet is open for inline edit, keyed by option+bullet id. */\n  const [editingBullet, setEditingBullet] = useState<\n    { optionId: string; bulletId: string } | null\n  >(null);\n  /** Which option's TITLE is open for inline edit (null = none). */\n  const [editingTitleId, setEditingTitleId] = useState<string | null>(null);\n  /** When set, renders the \"Want me to professionalize that?\" popup for\n   *  the bullet that was just edited or added. */\n  const [proPopup, setProPopup] = useState<\n    { optionId: string; bulletId: string } | null\n  >(null);\n  const [proBusy, setProBusy] = useState(false);\n  /** In-flight options generation, kicked off the moment the user submits\n   *  the raw job-details bubble so the LLM runs while they type the price. */\n  const optionsInFlightRef = useRef<\n    Promise<{ options: JobOption[] } | null> | null\n  >(null);\n  /** Snapshot of a bullet's text at edit-start, so commit can tell whether\n   *  it actually changed (and only then offer to professionalize). */\n  const editOriginalRef = useRef<string>(\"\");\n  /** Title text at edit-start, so commit can skip no-op renames. */\n  const titleOriginalRef = useRef<string>(\"\");\n  /** Uncontrolled \"write it myself\" textarea value — kept in a ref (not state)\n   *  so typing doesn't re-render this whole island, and persists across the\n   *  card unmount/remount when the contractor toggles the selection. */\n  const customDraftRef = useRef<string>(\"\");\n  /** Set once the user starts editing the picker (delete/edit/add). Blocks\n   *  the silent heuristic→LLM upgrade so we never clobber their changes. */\n  const optionsTouchedRef = useRef(false);\n  /** Inline-edit + \"add your own\" inputs are UNCONTROLLED (defaultValue,\n   *  commit on blur/Enter). Re-rendering this whole island on every keystroke\n   *  was dropping characters and lagging — so we read values from the DOM at\n   *  commit time instead of tracking them in state. */\n  const addInputRef = useRef<HTMLInputElement | null>(null);\n  const editEscapedRef = useRef(false);\n  /** True when this conversation entered phase 2 via the \"I know my price\"\n   *  flow and still owes the Job Details picker before the quote review.\n   *  Driven by a sessionStorage marker so it survives the page load that\n   *  hands off into the terms wizard. */\n  const [needsJobPolish, setNeedsJobPolish] = useState(false);\n  /** Raw job-details text carried from phase 1, used to (re)generate the\n   *  scope-of-work options in phase 2. */\n  const jobPolishRawRef = useRef<string | null>(null);\n  /** The \"Ready to send\" CTA whose review we deferred until the picker is\n   *  done. Set when we intercept it; consumed by applyJobOption. */\n  const pendingReviewCtaRef = useRef<string | null>(null);\n  /** Inline contact-recovery inputs keyed by the failure phase_divider id.\n   *  When SendContract reports a missing/invalid email or phone, we let\n   *  the user type it right under the divider; saving patches the\n   *  customer profile so we have it next time too. */\n  const [recoveryDraft, setRecoveryDraft] = useState<\n    Record<string, { email?: string; phone?: string }>\n  >({});\n  const [recoverySavingId, setRecoverySavingId] = useState<string | null>(null);\n  /** Selected channel for the quote-review send action. Smart-defaults\n   *  from customer.email/phoneNumber: both → both, email-only → email,\n   *  phone-only → sms. Overridable via the split-button chevron menu. */\n  const [sendChannel, setSendChannel] = useState<\"email\" | \"sms\" | \"both\">(\n    \"both\",\n  );\n  const [channelMenuOpen, setChannelMenuOpen] = useState(false);\n  /** Transient \"Link copied!\" feedback for the send menu's copy-link action\n   *  (roadmap: copy link alongside email / text / text+email). */\n  const [linkCopied, setLinkCopied] = useState(false);\n  /** The pick popped by the last wizard rewind — highlighted on the\n   *  re-asked step's matching option so Back restores the prior selection\n   *  (roadmap p.8). Cleared on the next answer. */\n  const [rewindAnswer, setRewindAnswer] = useState<\n    { stepId: string; optionId: string; customValue?: string } | null\n  >(null);\n  /** Job-details picker mode. \"polish\" = the classic end-of-phase-2 pass\n   *  that patches the existing quote. \"confirm\" = the roadmap p.18 order for\n   *  \"I know the job, help me price it.\": details → CONFIRM → pricing →\n   *  wizard — the picker runs pre-quote and its pick feeds quote creation. */\n  const [pickerMode, setPickerMode] = useState<\"polish\" | \"confirm\">(\"polish\");\n  /** The option confirmed in \"confirm\" mode — consumed by startQuoteFromRaw\n   *  so the quote is created with the confirmed title/summary/bullets (and\n   *  the end-of-flow picker is skipped). */\n  const confirmedOptionRef = useRef<JobOptionDraft | null>(null);\n  /** Doc type for the finished-flow review (roadmap: \"swap between quote and\n   *  invoice\"). \"invoice\" sends a standalone invoice — no signature, no\n   *  terms — instead of the Quote + Agreement. */\n  const [reviewDocType, setReviewDocType] = useState<\"quote\" | \"invoice\">(\n    \"quote\",\n  );\n  /** Set once the swapped invoice is created+sent: drives the success panel\n   *  with the public /i/:id link. The ref keeps the create idempotent if\n   *  dispatch fails and the user retries. */\n  const [swapInvoiceSent, setSwapInvoiceSent] = useState<string | null>(null);\n  const swapInvoiceIdRef = useRef<string | null>(null);\n  const [swapLinkCopied, setSwapLinkCopied] = useState(false);\n  /** \"Job done, need to invoice.\" starter (roadmap p.3): marks the pre-quote\n   *  details→price capture as invoice-bound, then collects the customer\n   *  through the standard wizard CustomerStepPanel and creates a standalone\n   *  invoice. */\n  const [invoiceFlow, setInvoiceFlow] = useState(false);\n  const [invoiceCustomerOpen, setInvoiceCustomerOpen] = useState(false);\n  const [invoiceResult, setInvoiceResult] = useState<\n    { id: string; customerEmail?: string; customerPhone?: string } | null\n  >(null);\n  const [invLinkCopied, setInvLinkCopied] = useState(false);\n  /** Language the quote-review card is PREVIEWED in (and sent in). The\n   *  contractor flips it with the \"Preview in\" toggle; defaults to their app\n   *  language until they explicitly pick one. */\n  const [previewLang, setPreviewLang] = useState<\"en\" | \"es\">(\n    (sendLangs[0] as \"en\" | \"es\") ?? \"en\",\n  );\n  // Until the contractor explicitly taps a \"Preview in\" pill, keep the preview\n  // following their app language — so a Spanish-app contractor sees the quote\n  // in Spanish even if the app language only resolved after first render\n  // (e.g. profile/impersonation loaded late).\n  const previewLangPickedRef = useRef(false);\n  useEffect(() => {\n    if (!previewLangPickedRef.current) {\n      setPreviewLang(lang === \"es\" ? \"es\" : \"en\");\n    }\n  }, [lang]);\n  /**\n   * Tracks `continue_cta` messages whose Review button has been clicked.\n   * Drives the inline \"Drafted ✓\" confirmation state — replaces the\n   * placeholder alert() that used to fire when the user reached the end\n   * of the wizard. Real send flow (email + signature) is still pending,\n   * but the user gets a clean acknowledgement instead of a system popup.\n   */\n  const [reviewedCtas, setReviewedCtas] = useState<Set<string>>(new Set());\n  /**\n   * Kind (\"business\" | \"person\") the user picked on the lock-quote CTA.\n   * Recorded when they click Business or Person on the post-lock CTA so\n   * the wizard's customer step can skip its own kind picker and jump\n   * straight to the existing-or-new picker for that kind.\n   */\n  const [precommittedKind, setPrecommittedKind] = useState<\n    \"business\" | \"person\" | null\n  >(null);\n  /**\n   * stepId of the contract term currently being re-edited inline. Driving\n   * a `null → stepId → null` cycle expands the term row into the wizard's\n   * option buttons for that step, lets the user pick a new value, and\n   * collapses back. Picking PUTs the contract directly (no rewinding the\n   * wizard state) — the contract IS the source of truth post-wizard.\n   */\n  const [editingTermStepId, setEditingTermStepId] = useState<string | null>(\n    null,\n  );\n  /** When the user clicks \"Custom\" inside a term picker, we swap the\n   *  options out for a single free-text input. Tracks (stepId, draft). */\n  const [customTermDraft, setCustomTermDraft] = useState<\n    {\n      stepId: string;\n      value: string;\n    } | null\n  >(null);\n  /**\n   * Set when the user clicks \"Review\" on the wizard's send CTA. Drives the\n   * inline contract preview card (total/customer/dates) so the user can\n   * actually look the contract over before clicking \"Send to client\".\n   */\n  const [previewCtaId, setPreviewCtaId] = useState<string | null>(null);\n\n  // Lazily fill quote.descriptionByLang[lang] so the preview (and the sent\n  // agreement, which reads the same field) render the job-details description\n  // in the reader's language. Translates once per language, cached on the\n  // quote, and only when a language is actually previewed/sent.\n  const descLangInFlight = useRef<Set<string>>(new Set<string>());\n  async function ensureDescriptionLang(targetLang: \"en\" | \"es\") {\n    const id = quoteId ?? quote?.id;\n    const base = (quote?.description ?? \"\").trim();\n    if (!id || !base) return;\n    if (quote?.descriptionByLang?.[targetLang]?.trim()) return;\n    if (descLangInFlight.current.has(targetLang)) return;\n    descLangInFlight.current.add(targetLang);\n    try {\n      const lines = base.split(\"\\n\").map((l) => l.trim()).filter(Boolean);\n      const res = await assistantClient.translate(lines, targetLang);\n      const translated =\n        (res?.texts && res.texts.length === lines.length ? res.texts : lines)\n          .join(\"\\n\");\n      const merged = {\n        ...(quote?.descriptionByLang ?? {}),\n        [targetLang]: translated,\n      };\n      setQuote((q) => q ? { ...q, descriptionByLang: merged } : q);\n      quotesClient.update(id, { descriptionByLang: merged }).catch(() => {});\n    } catch {\n      /* keep the base description */\n    } finally {\n      descLangInFlight.current.delete(targetLang);\n    }\n  }\n\n  // When the preview is open, make sure the current preview language has a\n  // translated description (keyed on description, not descriptionByLang, so\n  // filling the cache doesn't re-trigger this effect).\n  useEffect(() => {\n    if (previewCtaId) ensureDescriptionLang(previewLang);\n  }, [previewCtaId, previewLang, quote?.id, quote?.description]);\n  /**\n   * When the user picks a wizard option that carries a `followUp`, we stash\n   * the (messageId, optionId) here and render the inline form instead of\n   * firing the answer. Submitting clears it; cancelling clears it too.\n   */\n  const [followUpPick, setFollowUpPick] = useState<\n    {\n      messageId: string;\n      optionId: string;\n    } | null\n  >(null);\n  // start_date \"Pick a date\" — when armed, renders an inline date picker\n  // in place of the option grid for that wizard message.\n  const [customDatePick, setCustomDatePick] = useState<\n    {\n      messageId: string;\n      optionId: string;\n    } | null\n  >(null);\n  // wraps \"Custom\" — structured number + unit picker so the contract gets a\n  // clean duration string (\"3 weeks\") without relying on free-text parsing.\n  const [customDurationPick, setCustomDurationPick] = useState<\n    {\n      messageId: string;\n      optionId: string;\n    } | null\n  >(null);\n  // warranty \"Custom\" — same two-phase Bossie chat → verify pattern as the\n  // duration picker, but tuned for warranty language (months/years/lifetime)\n  // so the contract reads cleanly (\"12 months\", \"2 years\", \"Lifetime\").\n  const [customWarrantyPick, setCustomWarrantyPick] = useState<\n    {\n      messageId: string;\n      optionId: string;\n    } | null\n  >(null);\n  // payment_terms \"Custom\" — chat-with-verify Bossie flow that produces a\n  // clean payment string (\"Net 30\", \"30 / 30 / 40\") that buildPaymentMilestones\n  // can parse. Free-text never lands on the contract directly.\n  const [customPaymentPick, setCustomPaymentPick] = useState<\n    {\n      messageId: string;\n      optionId: string;\n    } | null\n  >(null);\n  // #27 — gates the third empty-state chip (\"Nudge an overdue invoice\").\n  // null = unknown / not yet loaded → don't render the chip yet (avoids\n  // flashing it on then yanking it away). Sourced from the shared dash\n  // cache so we don't fire a third copy of /analytics/dashboard.\n  const [, setOverdueCount] = useState<number | null>(() => {\n    const snap = readCached();\n    return snap?.stats?.invoices.overdue ?? null;\n  });\n  const [sending, setSending] = useState(false);\n  const [recording, setRecording] = useState(false);\n  const [recElapsed, setRecElapsed] = useState(0);\n  /** Live transcript surfaced from the Web Speech API. `interim` updates\n   *  as the user keeps speaking; `final` accumulates each finalised chunk\n   *  and is what we send when the user taps Stop. */\n  const [liveInterim, setLiveInterim] = useState(\"\");\n  const [liveFinal, setLiveFinal] = useState(\"\");\n  /** Smoothed audio level 0..1 driven by an AnalyserNode — used to\n   *  animate the visualizer bars so the user has unambiguous feedback\n   *  that the mic is hearing them. */\n  const [audioLevel, setAudioLevel] = useState(0);\n  const [error, setError] = useState<string | undefined>();\n\n  const recorderRef = useRef<MediaRecorder | null>(null);\n  const recChunksRef = useRef<Blob[]>([]);\n  const recStartRef = useRef<number>(0);\n  const recTickRef = useRef<number | null>(null);\n  const recStreamRef = useRef<MediaStream | null>(null);\n  /** Accumulated final transcript from AssemblyAI's `Turn` frames with\n   *  `end_of_turn=true`. Lives in a ref because the audio-process and\n   *  WS callbacks fire too fast for React state to be authoritative. */\n  const finalSoFarRef = useRef<string>(\"\");\n  // Web Audio plumbing — one AudioContext drives both the visualizer\n  // (AnalyserNode) and the STT pipe (ScriptProcessor → WS).\n  const audioCtxRef = useRef<AudioContext | null>(null);\n  const sttSourceRef = useRef<MediaStreamAudioSourceNode | null>(null);\n  const sttProcessorRef = useRef<ScriptProcessorNode | null>(null);\n  const sttSocketRef = useRef<WebSocket | null>(null);\n  const analyserRef = useRef<AnalyserNode | null>(null);\n  const levelRafRef = useRef<number | null>(null);\n  const scrollRef = useRef<HTMLDivElement | null>(null);\n  const taRef = useRef<HTMLTextAreaElement | null>(null);\n\n  // ── Assistant history stack ──────────────────────────────────────\n  // Snapshots of UI-only state pushed before every user-initiated\n  // view change. The back button (chat__head-btn) pops the latest.\n  interface ViewSnapshot {\n    priceCaptureOpen: boolean;\n    awaitingJobDetails: boolean;\n    submittedJobDetails: string | null;\n    pendingJobDetailsRaw: string | null;\n    pendingPriceCents: number | null;\n    priceCents: number | null;\n    jobOptionsOpen: boolean;\n  }\n  const historyStackRef = useRef<ViewSnapshot[]>([]);\n\n  function broadcastHistoryDepth() {\n    globalThis.dispatchEvent(\n      new CustomEvent(\"pm:asst-history\", {\n        detail: { depth: historyStackRef.current.length },\n      }),\n    );\n  }\n\n  function pushHistory() {\n    historyStackRef.current.push({\n      priceCaptureOpen,\n      awaitingJobDetails,\n      submittedJobDetails,\n      pendingJobDetailsRaw,\n      pendingPriceCents,\n      priceCents,\n      jobOptionsOpen,\n    });\n    broadcastHistoryDepth();\n  }\n\n  function popHistory() {\n    const snap = historyStackRef.current.pop();\n    if (!snap) return;\n    setPriceCaptureOpen(snap.priceCaptureOpen);\n    setAwaitingJobDetails(snap.awaitingJobDetails);\n    setSubmittedJobDetails(snap.submittedJobDetails);\n    setPendingJobDetailsRaw(snap.pendingJobDetailsRaw);\n    setPendingPriceCents(snap.pendingPriceCents);\n    setPriceCents(snap.priceCents);\n    setJobOptionsOpen(snap.jobOptionsOpen);\n    broadcastHistoryDepth();\n  }\n\n  // Listen for the back-button click event from ChatHeaderLive.\n  useEffect(() => {\n    function onBack() {\n      popHistory();\n    }\n    globalThis.addEventListener(\"pm:asst-back\", onBack);\n    return () => globalThis.removeEventListener(\"pm:asst-back\", onBack);\n  });\n  // ── end history stack ───────────────────────────────────────────\n\n  useEffect(() => {\n    setConvoId(conversationId);\n    setMessages(initialMessages);\n    setCustomer(initialCustomer);\n    setContract(initialContract);\n    // Seed quoteId from the bound contract (when present) so the\n    // quote-fetch effect below kicks in without waiting for a CTA click.\n    const seedQuoteId = (initialContract as { quoteId?: string } | undefined)\n      ?.quoteId;\n    if (seedQuoteId) setQuoteId(seedQuoteId);\n  }, [conversationId]);\n\n  // If we still don't have a quoteId on the route (terms phase but no\n  // contract bound yet — the \"I know my price → job details\" flow lands\n  // here before the wizard finalizes), pull it from the conversation.\n  useEffect(() => {\n    if (!convoId || quoteId) return;\n    let cancelled = false;\n    assistantClient\n      .conversation(convoId)\n      .then((detail) => {\n        if (cancelled) return;\n        const qId =\n          (detail.conversation as { quoteId?: string } | undefined)?.quoteId ??\n            (detail.contract as { quoteId?: string } | undefined)?.quoteId;\n        if (qId) setQuoteId(qId);\n      })\n      .catch(() => {/* preview falls back to action_card */});\n    return () => {\n      cancelled = true;\n    };\n  }, [convoId, quoteId]);\n\n  // Close the send-channel menu on outside click / Esc.\n  useEffect(() => {\n    if (!channelMenuOpen) return;\n    const onDown = (e: MouseEvent) => {\n      const t = e.target as HTMLElement | null;\n      if (!t) return;\n      if (t.closest(\".quote-review__send-split\")) return;\n      setChannelMenuOpen(false);\n    };\n    const onKey = (e: KeyboardEvent) => {\n      if (e.key === \"Escape\") setChannelMenuOpen(false);\n    };\n    document.addEventListener(\"mousedown\", onDown);\n    document.addEventListener(\"keydown\", onKey);\n    return () => {\n      document.removeEventListener(\"mousedown\", onDown);\n      document.removeEventListener(\"keydown\", onKey);\n    };\n  }, [channelMenuOpen]);\n\n  // Pick the send channel based on what contact info we actually have\n  // for this customer. The user can still override via the chevron menu.\n  useEffect(() => {\n    const hasEmail = !!customer?.email;\n    const hasPhone = !!customer?.phoneNumber;\n    if (hasEmail && hasPhone) setSendChannel(\"both\");\n    else if (hasEmail) setSendChannel(\"email\");\n    else if (hasPhone) setSendChannel(\"sms\");\n    else setSendChannel(\"both\");\n  }, [customer?.email, customer?.phoneNumber]);\n\n  // Whenever the active quoteId changes, fetch the full quote so the\n  // post-wizard preview can render description + lineItems without\n  // depending on a synthesized in-thread action_card.\n  useEffect(() => {\n    if (!quoteId) {\n      setQuote(undefined);\n      return;\n    }\n    let cancelled = false;\n    quotesClient\n      .get(quoteId)\n      .then((q) => {\n        if (!cancelled) {\n          setQuote({\n            id: q.id,\n            summary: q.summary,\n            jobName: q.jobName,\n            description: q.description,\n            lineItems: q.lineItems,\n            estimatedTotal: q.estimatedTotal,\n          });\n        }\n      })\n      .catch(() => {/* silent — preview falls back to action_card */});\n    return () => {\n      cancelled = true;\n    };\n  }, [quoteId]);\n\n  // Phase-2 entry from the \"I know my price\" flow: a sessionStorage marker\n  // (written just before the hand-off page load) carries the raw job\n  // details. Kick off the scope-of-work generation NOW so it runs in the\n  // background the whole time the user answers wizard questions — by the\n  // time the wizard finishes, the Job Details picker opens instantly. The\n  // marker is cleared once the picker is applied (see applyJobOption).\n  useEffect(() => {\n    if (!convoId) return;\n    let raw: string | null = null;\n    try {\n      raw = globalThis.sessionStorage?.getItem(`pm:jobpolish:${convoId}`) ??\n        null;\n    } catch { /* sessionStorage unavailable — skip */ }\n    if (!raw || !raw.trim()) return;\n    jobPolishRawRef.current = raw;\n    setNeedsJobPolish(true);\n    if (!optionsInFlightRef.current) {\n      optionsInFlightRef.current = assistantClient\n        .generateJobOptions(raw)\n        .catch((err) => {\n          console.warn(\"[asst] phase-2 job-options generation failed:\", err);\n          return null;\n        });\n    }\n  }, [convoId]);\n\n  // ?seed=… pre-fills the composer from a deeplink (e.g. hero CTAs on\n  // /payments / /invoices / /contracts → \"Ask Bossie to record a payment\").\n  // We strip the param after seeding so a refresh doesn't re-seed.\n  useEffect(() => {\n    if (typeof globalThis.window === \"undefined\") return;\n    const url = new URL(globalThis.location.href);\n    const seed = url.searchParams.get(\"seed\");\n    if (!seed) return;\n    setDraft(seed);\n    url.searchParams.delete(\"seed\");\n    globalThis.history.replaceState({}, \"\", url.toString());\n    // Focus on next paint so the composer expands and the user can edit/send.\n    queueMicrotask(() => taRef.current?.focus());\n  }, []);\n\n  // #27 — subscribe to the shared dash cache so the empty-state chip\n  // gating stays fresh even when the dashboard sidebar (the usual driver\n  // of the cache) is the one that triggers the refresh.\n  useEffect(() => {\n    let alive = true;\n    const unsub = subscribeDash((snap) => {\n      if (!alive) return;\n      setOverdueCount(snap.stats?.invoices.overdue ?? 0);\n    });\n    refreshDash().then((snap) => {\n      if (!alive) return;\n      setOverdueCount(snap.stats?.invoices.overdue ?? 0);\n    });\n    return () => {\n      alive = false;\n      unsub();\n    };\n  }, []);\n\n  // P6.12: keep the chat header fresh while the conversation evolves. The\n  // SSR-rendered header on /assistant (no threadId) starts as\n  // \"New conversation\" and never updates. Broadcast a CustomEvent whenever\n  // the bound customer or contract status changes so a sibling island can\n  // swap the title in place — no page reload needed.\n  useEffect(() => {\n    if (typeof globalThis.window === \"undefined\") return;\n    const client = customer?.name?.trim();\n    const contractStatus = contract?.status;\n    // Find the most recent quote action_card to derive a meaningful\n    // status. \"Drafting…\" used to fire as soon as a conversation existed,\n    // which mis-labelled the header on the literal first turn (audit #13).\n    const lastQuoteCard = [...messages].reverse().find((m) => {\n      if (m.kind !== \"action_card\") return false;\n      const p = m.payload as ActionCardPayload | undefined;\n      return p?.actionType === \"quote\" || p?.actionType == null;\n    });\n    const quoteStatus = (\n      lastQuoteCard?.payload as ActionCardPayload | undefined\n    )?.status;\n    // The most recent phase_divider tells us where the conversation is in\n    // the wizard timeline. Phase 2 (terms) lands the moment the user\n    // clicks Continue, BEFORE a contract row exists — without this hook\n    // the header sat at \"Quote sent\" through the entire wizard, which\n    // broke #15 (chip didn't update on transition).\n    const lastDivider = [...messages]\n      .reverse()\n      .find((m) => m.kind === \"phase_divider\");\n    const dividerPhase = (\n      lastDivider?.payload as { phase?: number } | undefined\n    )?.phase;\n    let status = tFor(lang, \"asstChat.header.default\");\n    if (contractStatus === \"signed\") {\n      status = tFor(lang, \"asstChat.header.contractSigned\");\n    } else if (contractStatus === \"sent\") {\n      status = tFor(lang, \"asstChat.header.contractOutForSignature\");\n    } else if (contract) {\n      status = tFor(lang, \"asstChat.header.contractDrafting\");\n    } else if (dividerPhase === 4) {\n      status = tFor(lang, \"asstChat.header.contractAccepted\");\n    } else if (dividerPhase === 3) {\n      status = tFor(lang, \"asstChat.header.contractSent\");\n    } else if (dividerPhase === 2) {\n      status = tFor(lang, \"asstChat.header.gatheringInfo\");\n    } else if (quoteStatus === \"accepted\") {\n      status = tFor(lang, \"asstChat.header.quoteAccepted\");\n    } else if (quoteStatus === \"sent\") {\n      status = tFor(lang, \"asstChat.header.quoteSent\");\n    } else if (lastQuoteCard) {\n      status = tFor(lang, \"asstChat.header.quoteDrafted\");\n    }\n    // No status chip at all on a brand-new thread — \"Drafting…\" before\n    // anything has been drafted reads as broken state.\n    const headerClient = client ??\n      (lastQuoteCard\n        ? tFor(lang, \"asstChat.header.conversation\")\n        : tFor(lang, \"asstChat.header.newConversation\"));\n    globalThis.window.dispatchEvent(\n      new CustomEvent(\"pm:asst-header\", {\n        detail: { client: headerClient, status },\n      }),\n    );\n  }, [\n    customer?.name,\n    contract?.id,\n    contract?.status,\n    convoId,\n    messages.length,\n    lang,\n  ]);\n\n  // Keep the composer focused: on first mount (so users can just start typing)\n  // and again whenever the assistant finishes a turn (sending: true → false).\n  // Without this the textarea was getting blurred whenever it was disabled,\n  // forcing a click back into the input between every message.\n  useEffect(() => {\n    if (!sending) taRef.current?.focus();\n  }, [sending]);\n\n  // Auto-focus the composer when the \"awaiting job details\" view appears\n  // (user clicked \"I know my price, write it up.\"). Without this the user\n  // has to manually click the textarea before typing.\n  useEffect(() => {\n    if (awaitingJobDetails && !submittedJobDetails) {\n      queueMicrotask(() => taRef.current?.focus());\n    }\n  }, [awaitingJobDetails, submittedJobDetails]);\n\n  // Onboarding banner quick-reply chips (Yes / Different state / Skip /\n  // Skip setup) dispatch this event with the reply text. AsstChat owns\n  // the chat transport, so we forward the text through sendText so it\n  // hits the same onboarding handler as a manually typed answer.\n  useEffect(() => {\n    function onQuickReply(e: Event) {\n      const text = (e as CustomEvent<{ text?: string }>).detail?.text;\n      if (typeof text === \"string\" && text.trim().length > 0) {\n        void sendText(text);\n      }\n    }\n    globalThis.addEventListener(\"pm:onboard-send-text\", onQuickReply);\n    return () =>\n      globalThis.removeEventListener(\"pm:onboard-send-text\", onQuickReply);\n  }, []);\n\n  // Eagerly mint the per-user sample quote URL once the synthetic\n  // onboarding-handoff CTA appears in the thread. Cheap (server-side\n  // idempotent) and ensures cmd+click on the link opens the right URL\n  // without an inline round-trip.\n  useEffect(() => {\n    if (sampleQuoteUrl) return;\n    const hasCta = messages.some(\n      (m) => m.kind === \"text\" && m.content === \"PM_ONBOARDING_DEMO_CTA\",\n    );\n    if (!hasCta) return;\n    let cancelled = false;\n    assistantClient\n      .ensureSampleQuote()\n      .then((r) => {\n        if (!cancelled) setSampleQuoteUrl(`/q/${r.quoteId}`);\n      })\n      .catch(() => {});\n    return () => {\n      cancelled = true;\n    };\n  }, [messages, sampleQuoteUrl]);\n\n  /**\n   * Auto-scroll: pin the chat to the bottom whenever the content height\n   * grows AND the user was already near the bottom (within 120px). The\n   * messages.length-based effect was too narrow — clicking Review opens\n   * an inline preview, the customer picker expands inline, the wizard\n   * answer log appends a row… all of those grow the scroll height\n   * without adding a message. A ResizeObserver on the inner content\n   * catches every height change in one place. We only auto-scroll when\n   * the user is near the bottom so we don't yank scroll while they're\n   * reading history.\n   */\n  useEffect(() => {\n    const scroller = scrollRef.current;\n    if (!scroller) return;\n    const inner = scroller.firstElementChild as HTMLElement | null;\n    if (!inner) return;\n    let nearBottom = true;\n    const STICK_THRESHOLD = 120;\n    function updateNearBottom() {\n      const distFromBottom = scroller!.scrollHeight - scroller!.scrollTop -\n        scroller!.clientHeight;\n      nearBottom = distFromBottom <= STICK_THRESHOLD;\n    }\n    function pin() {\n      scroller!.scrollTo({ top: scroller!.scrollHeight, behavior: \"smooth\" });\n    }\n    updateNearBottom();\n    scroller.addEventListener(\"scroll\", updateNearBottom, { passive: true });\n    const ro = new ResizeObserver(() => {\n      if (nearBottom) requestAnimationFrame(pin);\n    });\n    ro.observe(inner);\n    // First paint — drop straight to bottom (no smooth on initial load).\n    scroller.scrollTop = scroller.scrollHeight;\n    return () => {\n      ro.disconnect();\n      scroller.removeEventListener(\"scroll\", updateNearBottom);\n    };\n  }, []);\n\n  // Belt-and-suspenders: explicit scroll on message arrivals (for the\n  // \"user clicked away then a reply lands\" case, where they expect to\n  // be brought back regardless of where they were). The ResizeObserver\n  // above won't kick in if the user scrolled up to read history.\n  useEffect(() => {\n    const el = scrollRef.current;\n    if (el) el.scrollTo({ top: el.scrollHeight, behavior: \"smooth\" });\n  }, [messages.length]);\n\n  // Pin to bottom when the inline quote-review preview opens — it's a\n  // big card and the trigger button often sits at the bottom of the\n  // viewport, so we want to lift the new card fully into view.\n  useEffect(() => {\n    if (previewCtaId === null) return;\n    const el = scrollRef.current;\n    if (el) {\n      requestAnimationFrame(() =>\n        el.scrollTo({ top: el.scrollHeight, behavior: \"smooth\" })\n      );\n    }\n  }, [previewCtaId]);\n\n  // While the inline quote-review preview is open and the bound contract\n  // hasn't reached a terminal state (signed / declined), poll its row\n  // every 8s so the status chip ticks forward as the customer interacts:\n  //   Sent → Viewed (first public GET) → Approved (sign).\n  // No SSE channel exists today; polling here is scoped to the preview\n  // surface to keep traffic minimal.\n  useEffect(() => {\n    if (previewCtaId === null) return;\n    const cid = contract?.id;\n    if (!cid) return;\n    const TERMINAL = new Set([\n      \"signed\",\n      \"accepted\",\n      \"approved\",\n      \"declined\",\n      \"void\",\n      \"lost\",\n    ]);\n    if (TERMINAL.has((contract?.status ?? \"\").toLowerCase())) return;\n    let cancelled = false;\n    const tick = async () => {\n      try {\n        const fresh = await contractsClient.get(cid);\n        if (cancelled) return;\n        setContract((cur) => {\n          if (!cur || cur.id !== cid) return cur;\n          if (cur.status === fresh.status) return cur;\n          return { ...cur, ...fresh } as typeof cur;\n        });\n      } catch {\n        /* swallow — next tick will retry */\n      }\n    };\n    const id = globalThis.setInterval(tick, 8_000);\n    return () => {\n      cancelled = true;\n      globalThis.clearInterval(id);\n    };\n  }, [previewCtaId, contract?.id, contract?.status]);\n\n  // Auto-open the editable quote-review when the wizard emits its\n  // \"Ready to send\" CTA. The CTA banner itself is suppressed below\n  // (felt redundant), so this effect is the single entry point. Tracked\n  // per-message-id in a ref so cancelling the preview doesn't re-trigger.\n  const autoOpenedCtasRef = useRef<Set<string>>(new Set());\n  useEffect(() => {\n    if (previewCtaId !== null) return;\n    for (const m of messages) {\n      if (m.kind !== \"continue_cta\") continue;\n      const p = (m.payload ?? {}) as { toPhase?: string };\n      if (p.toPhase !== \"send\") continue;\n      if (reviewedCtas.has(m.id)) continue;\n      if (autoOpenedCtasRef.current.has(m.id)) continue;\n      autoOpenedCtasRef.current.add(m.id);\n      submitContinueCta(m).catch(() => {});\n      break;\n    }\n  }, [messages, previewCtaId, reviewedCtas]);\n\n  function autosize() {\n    const ta = taRef.current;\n    if (!ta) return;\n    ta.style.height = \"auto\";\n    // Floor at ~2 lines so the long example placeholder (\"Ex: Customer wants a\n    // 10'x10' slab, what should I charge?\") wraps and stays readable on mobile\n    // instead of clipping to a single line (problems.md #5).\n    ta.style.height = Math.min(Math.max(ta.scrollHeight, 56), 120) + \"px\";\n  }\n\n  /**\n   * Common send pipeline. Inserts an optimistic user bubble, calls the\n   * provided `submit` to talk to the backend, then either replaces the\n   * stub with the authoritative messages from the server or rolls back\n   * on error. `submit` returns the chat response shape.\n   */\n  async function submitTurn(\n    optimistic: {\n      role: \"user\" | \"assistant\";\n      kind: \"text\" | \"voice\" | \"image\";\n      content: string;\n    },\n    submit: () => Promise<{\n      conversation?: { id: string };\n      newMessages?: Message[];\n      message?: Message;\n      conversationId?: string;\n    }>,\n    onError?: () => void,\n  ) {\n    if (sending) return;\n    setError(undefined);\n    setSending(true);\n    const tmpId = `tmp-${Date.now()}`;\n    const stub: Message = {\n      id: tmpId,\n      conversationId: convoId ?? \"\",\n      role: optimistic.role,\n      kind: optimistic.kind,\n      content: optimistic.content,\n      createdAt: Date.now(),\n    };\n    setMessages((m) => [...m, stub]);\n\n    try {\n      const res = await submit();\n      const newConvoId = res.conversation?.id ?? res.conversationId;\n      if (newConvoId && newConvoId !== convoId) {\n        setConvoId(newConvoId);\n        if (!convoId && typeof globalThis.history !== \"undefined\") {\n          globalThis.history.replaceState(null, \"\", `/assistant/${newConvoId}`);\n        }\n      }\n      if (Array.isArray(res.newMessages) && res.newMessages.length > 0) {\n        setMessages((m) => [\n          ...m.filter((msg) => msg.id !== tmpId),\n          ...res.newMessages!,\n        ]);\n        // If this turn produced an onboarding ack/handoff, the user just\n        // saved a profile field (name / business / state / address) —\n        // refresh the dash cache so the sidebar's identity card rebuilds\n        // in real time. Cheap pattern match: the strings are stable\n        // server-side ack copy.\n        const onboardingHit = res.newMessages.some((msg) => {\n          if (msg.role !== \"assistant\" || msg.kind !== \"text\") return false;\n          const c = (msg.content ?? \"\").trim();\n          return (\n            c.startsWith(\"Nice to meet you,\") ||\n            c.startsWith(\"Almost there.\") ||\n            c.startsWith(\"Last one,\") ||\n            c.startsWith(\"Awesome — we're set,\")\n          );\n        });\n        const handoffFired = res.newMessages.some((msg) => {\n          if (msg.role !== \"assistant\" || msg.kind !== \"text\") return false;\n          return (msg.content ?? \"\").trim().startsWith(\"Awesome — we're set,\");\n        });\n        if (onboardingHit) {\n          refreshDash().catch(() => {\n            /* best-effort */\n          });\n          if (typeof globalThis.window !== \"undefined\") {\n            globalThis.window.dispatchEvent(\n              new CustomEvent(\"pm:profile-updated\"),\n            );\n          }\n        }\n        // Right after the handoff, surface the \"see what your customer\n        // sees\" demo chip as a synthetic local-only message. We don't\n        // persist it server-side — it's a one-time UX cue that goes\n        // away on reload. The msg id prefix lets the renderer recognize\n        // it and swap in the chip UI.\n        if (handoffFired) {\n          const demoMsg = {\n            id: `local-onboard-demo-${Date.now()}`,\n            conversationId: convoId ?? \"\",\n            role: \"assistant\",\n            kind: \"text\",\n            content: \"PM_ONBOARDING_DEMO_CTA\",\n            createdAt: Date.now(),\n          } as unknown as Message;\n          setMessages((m) => [...m, demoMsg]);\n        }\n      } else if (res.message) {\n        setMessages((m) => [\n          ...m.filter((msg) => msg.id !== tmpId),\n          res.message as Message,\n        ]);\n      }\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"send failed\");\n      setMessages((m) => m.filter((msg) => msg.id !== tmpId));\n      onError?.();\n    } finally {\n      setSending(false);\n    }\n  }\n\n  async function sendText(content: string) {\n    const trimmed = content.trim();\n    if (!trimmed) return;\n    // Empty-state job-details capture: the chat input is acting as\n    // the answer surface for \"tell me the job details\", not a chat turn.\n    // Intercept here so we don't fire a generic LLM call. This fires\n    // The chat input is acting as the job-details answer surface: stash the\n    // text and open the price screen (the quote + terms hand-off happens on\n    // price Continue).\n    if (awaitingJobDetails) {\n      setDraft(\"\");\n      autosize();\n      submitJobDetails(trimmed);\n      return;\n    }\n    setDraft(\"\");\n    autosize();\n    await submitTurn(\n      { role: \"user\", kind: \"text\", content: trimmed },\n      () =>\n        assistantClient.chat({\n          conversationId: convoId,\n          content: trimmed,\n          kind: \"text\",\n        }) as Promise<{\n          conversation?: { id: string };\n          newMessages?: Message[];\n          message?: Message;\n          conversationId?: string;\n        }>,\n      () => setDraft(trimmed),\n    );\n  }\n\n  /**\n   * Voice path: upload the recorded blob to /files, then post a chat\n   * turn with kind=voice + payload.fileId. The backend re-transcribes\n   * via its own pipeline (authoritative) and returns the persisted\n   * user message with the transcript as content — that replaces our\n   * optimistic bubble.\n   *\n   * `liveTranscript` is the realtime transcript captured client-side\n   * via the AssemblyAI streaming proxy. We use it as the optimistic\n   * bubble copy so the user sees their words land immediately rather\n   * than a generic \"Transcribing…\" placeholder.\n   */\n  async function sendVoice(\n    blob: Blob,\n    elapsedSec: number,\n    liveTranscript?: string,\n  ) {\n    const optimisticContent = liveTranscript && liveTranscript.length > 0\n      ? liveTranscript\n      : `🎙️ ${\n        tFor(lang, \"asstChat.voiceMemo.optimistic\", {\n          sec: elapsedSec,\n          size: fmtKB(blob.size),\n        })\n      }`;\n    await submitTurn(\n      {\n        role: \"user\",\n        kind: \"voice\",\n        content: optimisticContent,\n      },\n      async () => {\n        const file = await filesClient.uploadBlob(\n          blob,\n          `voice-${Date.now()}.webm`,\n        );\n        return (await assistantClient.chat({\n          conversationId: convoId,\n          kind: \"voice\",\n          payload: {\n            fileId: file.id,\n            ...(liveTranscript ? { transcript: liveTranscript } : {}),\n          },\n        })) as {\n          conversation?: { id: string };\n          newMessages?: Message[];\n          message?: Message;\n          conversationId?: string;\n        };\n      },\n    );\n  }\n\n  function onSendClick() {\n    sendText(draft);\n  }\n\n  function onKeyDown(e: KeyboardEvent) {\n    if (e.key === \"Enter\" && !e.shiftKey) {\n      e.preventDefault();\n      sendText(draft);\n    }\n  }\n\n  /**\n   * Dev-only: spin up a phase-2 conversation in one shot. Bypasses the\n   * LLM (which may be in stub mode) by creating a quote directly, binding\n   * it to a fresh conversation, transitioning to terms, and answering the\n   * config step — leaving the user on the customer step.\n   */\n  /**\n   * Continue handler for the inline \"I already have my price\" flow.\n   * Stashes the captured price and flips into the \"tell me the job details\"\n   * mode — the chat input becomes the answer surface for that step.\n   * The next chat-input submission routes through `submitJobDetails`,\n   * which polishes the raw text via LLM and kicks off the phase-2 flow.\n   */\n  function onPriceContinue(cents: number) {\n    if (sending || cents <= 0) return;\n    setError(undefined);\n    setPendingPriceCents(cents);\n    if (invoiceFlow) {\n      // \"Job done, need to invoice.\" — no quote/wizard: go pick the customer\n      // and mint the standalone invoice (roadmap p.3).\n      pushHistory();\n      openInvoiceCustomerStep();\n      return;\n    }\n    if (pendingJobDetailsRaw && pendingJobDetailsRaw.trim().length > 0) {\n      // Price + details in hand → create the quote now and hand off into the\n      // terms wizard. The Job Details picker is deferred to the END of phase\n      // 2 (right before the quote review): generation runs in the background\n      // while the user answers wizard questions, so they never wait on it.\n      // Keep the price screen mounted (its Continue flips to \"Setting up…\"\n      // via `sending`) so we don't flash the prompts before navigating.\n      void startQuoteFromRaw(pendingJobDetailsRaw, cents);\n    } else {\n      setPriceCaptureOpen(false);\n      setAwaitingJobDetails(true);\n    }\n  }\n\n  /**\n   * Opens the \"Job Details\" picker at the end of phase 2. The generation\n   * was kicked off on phase-2 mount and has been running the whole time the\n   * user answered wizard questions, so it's almost always already resolved\n   * → the picker opens instantly. We still render a local heuristic first\n   * (so the screen is never blank) and silently swap in the LLM options when\n   * they land, unless the user has already started editing.\n   */\n  async function openJobPicker() {\n    const raw = (jobPolishRawRef.current ?? quote?.description ?? \"\").trim();\n    optionsTouchedRef.current = false;\n    setJobOptionsOpen(true);\n    setOptionsLoading(false);\n    const heuristic = toOptionDrafts(\n      localFallbackOptions(raw || tFor(lang, \"asstChat.newJob\"), lang),\n    );\n    setJobOptions(heuristic);\n    setSelectedOptionId(heuristic[0]?.id ?? null);\n\n    const inflight = optionsInFlightRef.current ?? assistantClient\n      .generateJobOptions(raw)\n      .catch((err) => {\n        console.warn(\n          \"[asst] job-options generation failed, keeping heuristic:\",\n          err,\n        );\n        return null;\n      });\n    optionsInFlightRef.current = null;\n    const res = await inflight;\n    if (res?.options && res.options.length > 0 && !optionsTouchedRef.current) {\n      const drafts = toOptionDrafts(res.options);\n      setJobOptions(drafts);\n      setSelectedOptionId((prev) =>\n        prev && drafts.some((d) => d.id === prev)\n          ? prev\n          : (drafts[0]?.id ?? null)\n      );\n    }\n  }\n\n  /**\n   * Opens the Job Details picker as the CONFIRM step of the \"help me price\n   * it\" flow (roadmap p.18: details → confirm → pricing → wizard). Runs\n   * pre-quote: the pick is stashed on confirmedOptionRef and consumed by\n   * startQuoteFromRaw, so the end-of-phase-2 polish pass is skipped.\n   */\n  async function openJobPickerForConfirm(raw: string) {\n    setPickerMode(\"confirm\");\n    optionsTouchedRef.current = false;\n    setJobOptionsOpen(true);\n    setOptionsLoading(false);\n    const heuristic = toOptionDrafts(\n      localFallbackOptions(raw || tFor(lang, \"asstChat.newJob\"), lang),\n    );\n    setJobOptions(heuristic);\n    setSelectedOptionId(heuristic[0]?.id ?? null);\n    const res = await assistantClient.generateJobOptions(raw).catch((err) => {\n      console.warn(\n        \"[asst] confirm-step option generation failed, keeping heuristic:\",\n        err,\n      );\n      return null;\n    });\n    if (res?.options && res.options.length > 0 && !optionsTouchedRef.current) {\n      const drafts = toOptionDrafts(res.options);\n      setJobOptions(drafts);\n      setSelectedOptionId((prev) =>\n        prev && drafts.some((d) => d.id === prev)\n          ? prev\n          : (drafts[0]?.id ?? null)\n      );\n    }\n  }\n\n  // ── Job Details picker handlers ──────────────────────────────────\n  function toggleBulletDeleted(optionId: string, bulletId: string) {\n    optionsTouchedRef.current = true;\n    setJobOptions((prev) =>\n      prev?.map((o) =>\n        o.id === optionId\n          ? {\n            ...o,\n            bullets: o.bullets.map((b) =>\n              b.id === bulletId ? { ...b, deleted: !b.deleted } : b\n            ),\n          }\n          : o\n      ) ?? prev\n    );\n  }\n\n  function setBulletText(optionId: string, bulletId: string, text: string) {\n    setJobOptions((prev) =>\n      prev?.map((o) =>\n        o.id === optionId\n          ? {\n            ...o,\n            bullets: o.bullets.map((\n              b,\n            ) => (b.id === bulletId ? { ...b, text } : b)),\n          }\n          : o\n      ) ?? prev\n    );\n  }\n\n  function startBulletEdit(\n    optionId: string,\n    bulletId: string,\n    current: string,\n  ) {\n    optionsTouchedRef.current = true;\n    editOriginalRef.current = current;\n    setEditingBullet({ optionId, bulletId });\n  }\n\n  function setOptionName(optionId: string, name: string) {\n    setJobOptions((prev) =>\n      prev?.map((o) => (o.id === optionId ? { ...o, jobName: name } : o)) ??\n        prev\n    );\n  }\n\n  function startTitleEdit(optionId: string, current: string) {\n    optionsTouchedRef.current = true;\n    titleOriginalRef.current = current;\n    setEditingTitleId(optionId);\n  }\n\n  /** Commit an inline title edit (uncontrolled input → value read on blur).\n   *  Empty or unchanged input is a no-op so a stray click never blanks a\n   *  heading. */\n  function commitTitleEdit(optionId: string, value: string) {\n    setEditingTitleId(null);\n    const next = value.trim();\n    if (!next || next === titleOriginalRef.current.trim()) return;\n    setOptionName(optionId, next);\n  }\n\n  /** Commit an inline bullet edit (uncontrolled input → value read on blur).\n   *  Updates state once; offers to professionalize only when the text\n   *  actually changed. Empty input reverts to the original (no blank rows). */\n  function commitBulletEdit(optionId: string, bulletId: string, value: string) {\n    setEditingBullet(null);\n    const next = value.trim();\n    if (!next || next === editOriginalRef.current.trim()) return;\n    setBulletText(optionId, bulletId, next);\n    setProPopup({ optionId, bulletId });\n  }\n\n  /** Append the trailing \"add your own\" bullet, then offer to professionalize. */\n  function addCustomBullet(optionId: string, raw: string) {\n    const text = (raw ?? \"\").trim();\n    if (!text) return;\n    optionsTouchedRef.current = true;\n    const id = `b${++bulletSeq}`;\n    setJobOptions((prev) =>\n      prev?.map((o) =>\n        o.id === optionId\n          ? { ...o, bullets: [...o.bullets, { id, text, deleted: false }] }\n          : o\n      ) ?? prev\n    );\n    setProPopup({ optionId, bulletId: id });\n  }\n\n  async function confirmProfessionalize() {\n    if (!proPopup || proBusy) return;\n    const { optionId, bulletId } = proPopup;\n    const b = jobOptions?.find((o) => o.id === optionId)?.bullets.find((x) =>\n      x.id === bulletId\n    );\n    if (!b) {\n      setProPopup(null);\n      return;\n    }\n    setProBusy(true);\n    try {\n      const res = await assistantClient.professionalizeBullet(b.text);\n      if (res?.text) setBulletText(optionId, bulletId, res.text);\n    } catch (err) {\n      console.warn(\"[asst] professionalize failed, keeping text:\", err);\n    } finally {\n      setProBusy(false);\n      setProPopup(null);\n    }\n  }\n\n  function dismissProfessionalize() {\n    if (proBusy) return;\n    setProPopup(null);\n  }\n\n  /**\n   * Create the quote + conversation from the raw job-details text and hand\n   * off into the terms wizard (phase 2). The Job Details picker is NOT shown\n   * here — it's deferred to the end of phase 2. We stash the raw text in\n   * sessionStorage (keyed by the new conversation id) so the phase-2 mount\n   * can kick off generation in the background and re-open the picker before\n   * the quote review. Navigation is a full page load, hence sessionStorage\n   * rather than an in-memory promise.\n   */\n  async function startQuoteFromRaw(raw: string, cents: number) {\n    if (sending) return;\n    setError(undefined);\n    setSubmittedJobDetails(raw.trim());\n    setSending(true);\n    try {\n      // Roadmap p.18: when the details were CONFIRMED via the pre-quote\n      // picker (\"help me price it\" flow), seed the quote from the confirmed\n      // option — title, summary, bullets, and any pre-generated translations\n      // — and skip the end-of-phase-2 polish pass entirely.\n      const confirmed = confirmedOptionRef.current;\n      const firstLine = raw.split(\"\\n\")[0].trim();\n      const summary = confirmed?.summary ||\n        firstLine.split(/\\s+/).slice(0, 8).join(\" \") ||\n        tFor(lang, \"asstChat.newJob\");\n      const jobName = confirmed?.jobName ||\n        summary.split(/\\s+/).slice(0, 3).join(\" \");\n      const byLang = confirmed?.byLang;\n      const byLangFields = byLang\n        ? {\n          jobNameByLang: Object.fromEntries(\n            Object.entries(byLang).map((\n              [l, v],\n            ) => [l, l === lang ? jobName : v.jobName]),\n          ),\n          summaryByLang: Object.fromEntries(\n            Object.entries(byLang).map((\n              [l, v],\n            ) => [l, l === lang ? summary : v.summary]),\n          ),\n          descriptionByLang: Object.fromEntries(\n            Object.entries(byLang).map((\n              [l, v],\n            ) => [l, l === lang ? raw.trim() : v.bullets.join(\"\\n\")]),\n          ),\n        }\n        : {};\n      // Use the api helper (same base as quotesClient/assistantClient) — NOT\n      // a raw /api/* fetch. In prod the api helper hits the standalone backend\n      // directly while /api/* bounces through the Fresh proxy; mixing the two\n      // creates the quote on one backend and reads/updates it on another, so\n      // the later GET/PUT /quotes/:id 404s. Keep all quote ops on one path.\n      const quote = await api.post<{ id?: string }>(\"/quotes\", {\n        summary,\n        jobName,\n        description: raw.trim(),\n        ...byLangFields,\n        lineItems: [{\n          description: summary,\n          quantity: 1,\n          unit: \"ea\",\n          price: cents,\n        }],\n        estimatedTotal: cents,\n        status: \"sent\",\n      });\n      if (!quote?.id) throw new Error(\"failed to create quote\");\n\n      const conv = await api.post<{ id?: string }>(\"/agents/conversations\", {\n        quoteId: quote.id,\n      });\n      if (!conv?.id) throw new Error(\"failed to start conversation\");\n\n      await api.post(`/agents/conversations/${conv.id}/transition-to-terms`);\n\n      if (!confirmed) {\n        try {\n          globalThis.sessionStorage?.setItem(\n            `pm:jobpolish:${conv.id}`,\n            raw.trim(),\n          );\n        } catch { /* sessionStorage unavailable — picker just won't auto-open */ }\n      }\n\n      globalThis.location.href = `/assistant/${conv.id}`;\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"couldn't start\");\n      setSubmittedJobDetails(null);\n      setSending(false);\n    }\n  }\n\n  /**\n   * Apply the picked option at the end of phase 2: surviving bullets become\n   * the quote description, the option's summary/jobName seed the quote and\n   * its price line. Patches the existing quote (created in phase 1), then\n   * opens the deferred quote review.\n   */\n  async function applyJobOption() {\n    if (sending) return;\n    let opt = jobOptions?.find((o) => o.id === selectedOptionId);\n    // \"Write it myself\" — synthesize a draft from the free-text box: each\n    // non-empty line becomes a bullet, the first line seeds the title/summary.\n    // No byLang, so the customer-language copy is filled lazily on preview.\n    if (selectedOptionId === CUSTOM_OPTION_ID) {\n      const lines = customDraftRef.current\n        .split(\"\\n\")\n        .map((l) => l.trim())\n        .filter(Boolean);\n      if (lines.length === 0) {\n        setError(tFor(lang, \"asstChat.jobOpts.customEmpty\"));\n        return;\n      }\n      opt = {\n        id: CUSTOM_OPTION_ID,\n        // Job Names are ≤3 words platform-wide (roadmap p.10) — match the\n        // LLM-side clampJobName instead of the old 5-word slice.\n        jobName: lines[0].split(/\\s+/).slice(0, 3).join(\" \"),\n        summary: lines[0],\n        bullets: lines.map((text, i) => ({\n          id: `c${i}`,\n          text,\n          deleted: false,\n        })),\n      };\n    }\n    if (!opt) {\n      setError(tFor(lang, \"asstChat.error.pickOption\"));\n      return;\n    }\n    const live = opt.bullets.filter((b) =>\n      !b.deleted && b.text.trim().length > 0\n    );\n    const liveTexts = live.map((b) => b.text.trim());\n    const description = liveTexts.join(\"\\n\");\n    const summary =\n      (opt.summary || opt.jobName || tFor(lang, \"asstChat.newJob\"))\n        .trim();\n    const jobName = (opt.jobName || summary).trim();\n    // CONFIRM mode (roadmap p.18): no quote exists yet — stash the confirmed\n    // option and advance to the pricing screen. Suggestions are computed on\n    // the CONFIRMED scope, so the three tiers price what was actually agreed.\n    if (pickerMode === \"confirm\") {\n      confirmedOptionRef.current = {\n        ...opt,\n        jobName,\n        summary,\n        bullets: live,\n      };\n      setPendingJobDetailsRaw(description || summary);\n      setJobOptionsOpen(false);\n      setPickerMode(\"polish\");\n      setPriceCaptureOpen(true);\n      setPriceSuggestions(null);\n      void fetchPriceSuggestions(description || summary);\n      return;\n    }\n    setError(undefined);\n    setSending(true);\n    try {\n      // Store the picked job details in EVERY language the option was generated\n      // in, so the quote/agreement renders in the customer's language. Reuse the\n      // pre-generated translation for unedited bullets; translate edited ones.\n      const appLang = lang;\n      const descByLang: Record<string, string> = {};\n      if (liveTexts.length > 0) {\n        descByLang[appLang] = description;\n        const origApp = (opt.byLang?.[appLang]?.bullets ?? []).map((b) =>\n          b.trim()\n        );\n        const unedited = liveTexts.length === origApp.length &&\n          liveTexts.every((t, i) => t === origApp[i]);\n        for (const ol of Object.keys(opt.byLang ?? {})) {\n          if (ol === appLang) continue;\n          const pre = opt.byLang?.[ol]?.bullets;\n          if (unedited && pre && pre.length) {\n            descByLang[ol] = pre.map((b) => b.trim()).join(\"\\n\");\n          } else {\n            try {\n              const res = await assistantClient.translate(\n                liveTexts,\n                ol as \"en\" | \"es\",\n              );\n              descByLang[ol] = (res?.texts &&\n                  res.texts.length === liveTexts.length\n                ? res.texts\n                : liveTexts).join(\"\\n\");\n            } catch {\n              descByLang[ol] = description;\n            }\n          }\n        }\n      }\n      const hasByLang = Object.keys(descByLang).length > 0;\n      // Per-language title + summary so the customer's agreement heading renders\n      // in their language. The summary isn't editable, so it always reuses the\n      // pre-generated translation. The TITLE is now editable — when the\n      // contractor renames it, the pre-generated translations are stale, so we\n      // re-translate the new title (and the app-lang entry always reflects the\n      // edit so it shows for the contractor too).\n      const nameByLang: Record<string, string> = {};\n      const summByLang: Record<string, string> = {};\n      const origAppName = opt.byLang?.[appLang]?.jobName?.trim() ?? \"\";\n      const nameEdited = jobName !== origAppName;\n      if (jobName) nameByLang[appLang] = jobName;\n      for (const ol of Object.keys(opt.byLang ?? {})) {\n        const b = opt.byLang?.[ol];\n        if (b?.summary?.trim()) summByLang[ol] = b.summary.trim();\n        if (ol === appLang) continue;\n        if (!nameEdited && b?.jobName?.trim()) {\n          nameByLang[ol] = b.jobName.trim();\n        } else if (jobName) {\n          try {\n            const res = await assistantClient.translate(\n              [jobName],\n              ol as \"en\" | \"es\",\n            );\n            nameByLang[ol] = (res?.texts?.[0] ?? jobName).trim();\n          } catch {\n            nameByLang[ol] = jobName;\n          }\n        }\n      }\n      const hasName = Object.keys(nameByLang).length > 0;\n      const hasSumm = Object.keys(summByLang).length > 0;\n      if (quoteId) {\n        const items = quote?.lineItems && quote.lineItems.length > 0\n          ? quote.lineItems.map((\n            li,\n            i,\n          ) => (i === 0 ? { ...li, description: summary } : li))\n          : undefined;\n        await quotesClient.update(quoteId, {\n          description: description || summary,\n          ...(hasByLang ? { descriptionByLang: descByLang } : {}),\n          ...(hasName ? { jobNameByLang: nameByLang } : {}),\n          ...(hasSumm ? { summaryByLang: summByLang } : {}),\n          summary,\n          jobName,\n          ...(items ? { lineItems: items } : {}),\n        });\n        setQuote((q) =>\n          q\n            ? {\n              ...q,\n              summary,\n              description: description || summary,\n              ...(hasByLang ? { descriptionByLang: descByLang } : {}),\n              lineItems: items ?? q.lineItems,\n            }\n            : q\n        );\n      }\n      try {\n        globalThis.sessionStorage?.removeItem(`pm:jobpolish:${convoId}`);\n      } catch { /* ignore */ }\n      setNeedsJobPolish(false);\n      setJobOptionsOpen(false);\n      const ctaId = pendingReviewCtaRef.current;\n      pendingReviewCtaRef.current = null;\n      if (ctaId) setPreviewCtaId(ctaId);\n    } catch (err) {\n      setError(\n        err instanceof Error ? err.message : \"couldn't save job details\",\n      );\n    } finally {\n      setSending(false);\n    }\n  }\n\n  /**\n   * \"Details-first\" step of the \"I know my price\" flow: the user typed the\n   * raw job description into the chat input. Stash it and open the price\n   * screen. On price Continue, `startQuoteFromRaw` creates the quote and\n   * hands off into the terms wizard; the Job Details picker is deferred to\n   * the end of phase 2.\n   */\n  function submitJobDetails(raw: string) {\n    const trimmed = raw.trim();\n    if (sending || !trimmed) return;\n    setError(undefined);\n    pushHistory();\n    setPendingJobDetailsRaw(trimmed);\n    setSubmittedJobDetails(trimmed);\n    setAwaitingJobDetails(false);\n    if (suggestPricing) {\n      // \"Help me price it\" (roadmap p.18): confirm the job details FIRST,\n      // then price the confirmed scope. The picker's Continue advances to\n      // the pricing screen via applyJobOption's confirm branch.\n      void openJobPickerForConfirm(trimmed);\n    } else {\n      setPriceCaptureOpen(true);\n    }\n  }\n\n  async function seedPhase2() {\n    if (sending) return;\n    setError(undefined);\n    setSending(true);\n    try {\n      const quote = await fetch(\"/api/quotes\", {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        credentials: \"include\",\n        body: JSON.stringify({\n          summary: \"Kitchen backsplash — 30 sqft\",\n          lineItems: [\n            {\n              description: \"Backsplash tile install (30 sqft)\",\n              quantity: 1,\n              unit: \"ea\",\n              price: 120000,\n            },\n          ],\n          estimatedTotal: 120000,\n          status: \"sent\",\n        }),\n      }).then((r) => r.json());\n      if (!quote?.id) throw new Error(\"seed: failed to create stub quote\");\n\n      const conv = await fetch(\"/api/agents/conversations\", {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        credentials: \"include\",\n        body: JSON.stringify({ quoteId: quote.id }),\n      }).then((r) => r.json());\n      if (!conv?.id) throw new Error(\"seed: failed to start conversation\");\n\n      await fetch(`/api/agents/conversations/${conv.id}/transition-to-terms`, {\n        method: \"POST\",\n        credentials: \"include\",\n      });\n\n      globalThis.location.href = `/assistant/${conv.id}`;\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"seed failed\");\n      setSending(false);\n    }\n  }\n\n  /**\n   * Click handler for the assistant's `continue_cta` cards. Two flavors:\n   *   - toPhase = \"terms\" → fires POST transition-to-terms (the quote→\n   *     wizard hand-off); the response brings the phase_divider + first\n   *     wizard message.\n   *   - toPhase = \"send\"  → wizard is complete; for now we just surface\n   *     a confirmation since the send-to-client UI is out of scope.\n   * The CTA is removed client-side after click so the user can't fire it\n   * twice while the request is in flight.\n   */\n  async function submitContinueCta(\n    message: Message,\n    kind?: \"business\" | \"person\",\n  ) {\n    if (sending) return;\n    const payload = (message.payload ?? {}) as {\n      toPhase?: string;\n      contractId?: string;\n    };\n    if (payload.toPhase === \"terms\") {\n      if (!convoId) return;\n      // Stash the kind picked on the CTA so CustomerStepPanel can skip its\n      // own kind picker. Cleared when the panel consumes it.\n      if (kind) setPrecommittedKind(kind);\n      setError(undefined);\n      setSending(true);\n      // Optimistically drop the CTA so it doesn't linger after the click.\n      setMessages((m) => m.filter((x) => x.id !== message.id));\n      try {\n        const res = (await assistantClient.transitionToTerms(convoId)) as {\n          conversation?: { id: string };\n          newMessages?: Message[];\n        };\n        if (Array.isArray(res.newMessages) && res.newMessages.length > 0) {\n          setMessages((m) => [...m, ...res.newMessages!]);\n        }\n      } catch (err) {\n        setError(\n          err instanceof Error ? err.message : \"couldn't advance to terms\",\n        );\n        // Restore the CTA so the user can retry.\n        setMessages((m) => [...m, message]);\n      } finally {\n        setSending(false);\n      }\n      return;\n    }\n    if (payload.toPhase === \"send\") {\n      // Wizard is complete — open the inline preview card so the user can\n      // look the contract over before sending. Hydrate contract+customer\n      // state if we don't already have them (mid-session wizard runs land\n      // here without page-load contract data).\n      const contractId = payload.contractId ?? contract?.id;\n      if (!convoId || !contractId) {\n        setError(\"contract is not ready yet\");\n        return;\n      }\n      if (!contract || contract.id !== contractId || !customer) {\n        setError(undefined);\n        setSending(true);\n        try {\n          const detail = await assistantClient.conversation(convoId);\n          if (detail.contract) setContract(detail.contract);\n          if (detail.customer) setCustomer(detail.customer);\n          const qId = (detail.conversation as { quoteId?: string } | undefined)\n            ?.quoteId ??\n            (detail.contract as { quoteId?: string } | undefined)?.quoteId;\n          if (qId) setQuoteId(qId);\n        } catch (err) {\n          setError(\n            err instanceof Error ? err.message : \"couldn't load the contract\",\n          );\n          return;\n        } finally {\n          setSending(false);\n        }\n      }\n      // End of phase 2 — right before the quote review. If this conversation\n      // came in through the \"I know my price\" flow, show the Job Details\n      // picker first (its options were generated in the background while the\n      // user answered wizard questions). applyJobOption patches the quote and\n      // then re-fires the review for this CTA.\n      if (needsJobPolish) {\n        pendingReviewCtaRef.current = message.id;\n        void openJobPicker();\n        return;\n      }\n      setPreviewCtaId(message.id);\n      return;\n    }\n    if (payload.toPhase === \"invoice\") {\n      // Closing handoff: contract is signed, draft + send the invoice.\n      // SendInvoice is idempotent server-side, so a double-click just\n      // re-renders the same action_card. The CTA stays in the chat\n      // history (don't drop it) so the user has a record of the click.\n      if (!convoId) return;\n      setError(undefined);\n      setSending(true);\n      try {\n        const res = await assistantClient.sendInvoice(convoId);\n        setReviewedCtas((prev) => {\n          const next = new Set(prev);\n          next.add(message.id);\n          return next;\n        });\n        if (res.newMessages?.length) {\n          setMessages((m) => [...m, ...res.newMessages]);\n        }\n      } catch (err) {\n        setError(\n          err instanceof Error ? err.message : \"couldn't send the invoice\",\n        );\n      } finally {\n        setSending(false);\n      }\n    }\n  }\n\n  /**\n   * Dev-only: flip the contract to \"accepted\" via AcceptContract — the\n   * single customer-facing acceptance event in the chain. The server\n   * appends a phase_divider (\"Contract accepted by client\") + a\n   * \"Continue to invoice\" CTA, sets hasUnreadEvent, and bumps preview;\n   * we splice those into the chat in-place so the user can see the\n   * progression without a navigation jump.\n   */\n  async function simulateCustomerAccept(contractId: string | undefined) {\n    if (sending || !convoId || !contractId) return;\n    setError(undefined);\n    setSending(true);\n    try {\n      const res = await assistantClient.acceptContract(convoId, contractId);\n      if (res.newMessages?.length) {\n        setMessages((m) => [...m, ...res.newMessages]);\n      }\n    } catch (err) {\n      setError(\n        err instanceof Error ? err.message : \"couldn't simulate acceptance\",\n      );\n    } finally {\n      setSending(false);\n    }\n  }\n\n  /**\n   * Save inline-typed customer email/phone to the customer profile,\n   * then re-fire SendContract on the same channel so the doc actually\n   * reaches the client. Used by the recovery form rendered below a\n   * delivery-failure phase_divider.\n   */\n  async function saveContactAndRetry(\n    dividerId: string,\n    args: { contractId: string; channel: \"email\" | \"sms\" | \"both\" },\n  ) {\n    if (!convoId || !customer?.id) return;\n    const draft = recoveryDraft[dividerId] ?? {};\n    const email = draft.email?.trim();\n    const phone = draft.phone?.trim();\n    if (!email && !phone) return;\n\n    setRecoverySavingId(dividerId);\n    setError(undefined);\n    try {\n      const patch: { email?: string; phoneNumber?: string } = {};\n      if (email) patch.email = email;\n      if (phone) patch.phoneNumber = phone;\n      const updated = await clientsClient.update(customer.id, patch);\n      setCustomer((c) => (c ? { ...c, ...patch } as typeof c : c));\n      // Local optimistic patch for downstream renders that read the\n      // customer block straight off our state (the assistant header,\n      // the quote-review hero, etc.). We still trust the server row\n      // for canonical values via updated.\n      void updated;\n      const res = await assistantClient.sendContract(\n        convoId,\n        args.contractId,\n        args.channel,\n      );\n      if (res.newMessages?.length) {\n        setMessages((m) => [...m, ...res.newMessages]);\n      }\n      setRecoveryDraft((prev) => {\n        const next = { ...prev };\n        delete next[dividerId];\n        return next;\n      });\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"couldn't save & resend\");\n    } finally {\n      setRecoverySavingId(null);\n    }\n  }\n\n  /**\n   * Fire the post-wizard \"Ready to send\" CTA: dispatches the assembled\n   * contract to the customer via the SendContract coordinator on the\n   * requested channel (text, email, or both). Idempotent server-side\n   * (re-clicks redeliver), so flipping local state optimistically is safe.\n   */\n  async function confirmSendContract(\n    message: Message,\n    channel: \"email\" | \"sms\" | \"both\" = \"email\",\n    language?: \"en\" | \"es\",\n  ) {\n    if (sending || !convoId) return;\n    const payload = (message.payload ?? {}) as { contractId?: string };\n    let id = payload.contractId ?? contract?.id;\n    setError(undefined);\n    setSending(true);\n    try {\n      if (!id) {\n        const detail = await assistantClient.conversation(convoId);\n        id = detail.contract?.id ??\n          (detail.conversation as { contractId?: string } | undefined)\n            ?.contractId;\n        if (detail.contract) setContract(detail.contract);\n      }\n      if (!id) throw new Error(\"no contract bound to this conversation\");\n      const res = await assistantClient.sendContract(\n        convoId,\n        id,\n        channel,\n        language,\n      );\n      setReviewedCtas((prev) => {\n        const next = new Set(prev);\n        next.add(message.id);\n        return next;\n      });\n      setContract((c) => (c ? { ...c, status: \"sent\" } : c));\n      setPreviewCtaId(null);\n      if (res.newMessages?.length) {\n        setMessages((m) => [...m, ...res.newMessages]);\n      }\n    } catch (err) {\n      setError(\n        err instanceof Error ? err.message : \"couldn't send the contract\",\n      );\n    } finally {\n      setSending(false);\n    }\n  }\n\n  /**\n   * Save an inline edit to the grand total. The user types a money value\n   * into the `.quote-review__total-amt` span; strip non-numeric chars and\n   * convert to cents. Persistence:\n   *   - PUT /quotes/:id  — `estimatedTotal` so the canonical record matches\n   *   - PUT /contracts/:id (when a contract exists) — `totalAmount` is what\n   *     the preview reads from (`totalCentsForBreakdown`), so updating it\n   *     refreshes the displayed total + recomputes payment milestones.\n   *   - When no contract is bound, patch the action_card payload's\n   *     `totalCents` so the on-screen total reflects the edit.\n   * Line items are NOT touched — the Subtotal row keeps showing the line\n   * sum so the user sees the override delta.\n   */\n  async function onEditTotal(\n    quoteId: string | undefined,\n    actionCardId: string | undefined,\n    contractId: string | undefined,\n    originalCents: number,\n    el: HTMLElement,\n  ) {\n    const cleaned = (el.innerText ??\n\n… [truncated for viewer — open the file on disk for the rest] …", "sourceName": "AsstChat.tsx", "checklist": "# Capture checklist — AsstChat\n\n**Theme:** light only. **Auth:** dev master OTP `000000`.\n**No fabricated screenshots** — every shot is the live app at a real width/state.\n\n> AsstChat is the chat surface itself (message list + composer + flows). It is\n> the second column of the `.asst` grid, below `ChatHeaderLive`. Capture it in\n> context on the real routes; crop to the chat column.\n\n## Routes / URLs\n- `http://localhost:5280/assistant` — empty chat: monster logo, title, the four\n  start prompts, composer with the pink mic.\n- `http://localhost:5280/assistant/<threadId>` — a seeded conversation (need a\n  thread with messages; seed one or open an existing). Shows text bubbles +\n  composer; if the thread has a quote, the in-chat `action-card`.\n- `http://localhost:5280/assistant?onboard=1` — onboarding chat (the composer\n  placeholder echoes Bossie's question).\n- `http://localhost:5280/assistant/<threadId>?dev` (localhost only) — exposes\n  the \"Seed phase 2 wizard\" debug button + the \"Simulate customer accepted\"\n  button on a reviewed send-CTA; use these to drive the wizard/divider states.\n\n## Viewports (real `.asst` grid breakpoints from assistant-page.css)\n- **1280px** — three-column shell (threads 280 | chat 1fr | docpane 360, when a\n  docpane is present; otherwise two-column).\n- **1099px** — docpane hidden (`260 | 1fr`).\n- **880px** — single column; threads collapse to a drawer — chat is full-width.\n- **480px** — the RecordingPanel tightens (orb 44px). Shoot a recording state here.\n\n## Element(s) to crop\n- The chat column: `.chat__scroll` (message list) + the `.composer` below it.\n- Individual bubble crops: a `.msg__bubble` pair (user + assistant), the\n  `.action-card` (Quote+Agreement), a `.continue-cta`, a `.phase-divider`, a\n  `.wiz` step card, the `.chat__jobopts` picker, the `.chat__price-capture`\n  screen (with the MoneyInput hero), the `.rec-panel` voice surface.\n\n## Transient states to drive\n1. **composer focus** — click into `.composer__input`; capture the\n   `:focus-within` border (green) + the `.composer__hint` (`⌘↵` / tap-to-talk).\n2. **mic / voice active (ws)** — tap `.composer__mic`; allow the mic; the\n   `RecordingPanel` replaces the composer. Speak so `liveInterim`/`liveFinal`\n   stream in (interim words italic/muted, final words teal). Film the orb\n   pulsing with `audioLevel`. (This is the **pushed** `/api/voice/stream` path.)\n3. **job-option cards** — run a quote flow to the job-details step; capture the\n   three `.chat__jobopt` cards, one `.is-selected`, plus an inline title/bullet\n   edit input.\n4. **money prompt** — pick \"I already have my price\" → the `.chat__price-capture`\n   screen with the embedded MoneyInput hero (odometer + chips); and\n   \"Help me price it\" → the three `.chat__price-tiers` above the MoneyInput.\n5. **action-card lifecycle** — capture the same Quote card as **draft**\n   (Lock-in/Edit) and after locking → **sent** (Sent chip, Re-open); plus a\n   **superseded** earlier draft (dimmed) when a later card exists.\n6. **continue-cta** — both `toPhase=terms` (Business/Person buttons) and\n   `toPhase=send` (Review), then its `--done` state after Review is clicked.\n7. **phase divider + recovery** — a clean `Contract terms` divider, and a\n   failed-send divider with the inline `.recovery-card` (drive by sending a\n   contract to a customer with no email on file).\n8. **typing indicator** — send a message; capture the `.msg__bubble--typing`\n   three-dot state before the reply lands.\n9. **composer flash** — the `awaitingJobDetails` bounce cue (one-shot) on entry\n   to the job-details step.\n10. **Spanish** — flip Settings → language to Español; re-shoot the empty chat,\n    prompts, composer hint, and a CTA. (Free-text message bodies stay verbatim.)\n\n## Motion to film\n- `composer-bounce` (the flash, ~1.6s one-shot) — film the full jump+wobble.\n- `msg-in` (220ms per bubble) on a new reply; `typing-bounce` (1.2s loop).\n- The RecordingPanel orb + `rec-ring`/`rec-live-pulse`/`rec-final-fade`.\n- `chat-price-in` on the price screen entry; the MoneyInput odometer roll.\n- Re-shoot key states with `prefers-reduced-motion: reduce` — verify `.msg` and\n  `.composer__hint` still (component-local guards) AND the global clamp stills\n  the bounce/voice/price animations.\n"}, {"name": "asst-threads", "component": "AsstThreads", "spec": "# AsstThreads\n\nThe conversation-list sidebar (left column) of the assistant workspace —\nrecency-grouped thread rows, an \"unread event\" pulse, a status chip per row, a\n\"New conversation\" CTA, and a QuickBooks-style collapse-to-rail toggle.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/AsstThreads.tsx`).\n- **Interaction tier:** `island` (client-only state) **with live polling**.\n- **Client state owned:**\n  - `threads: Conversation[]` — seeded from the `initialThreads` prop (SSR), then\n    replaced on every poll/focus/mount-sync by `assistantClient.conversations(50)`.\n  - `collapsed: boolean` — rail/expanded; seeded from\n    `localStorage[\"pm:threads-collapsed\"]` (`\"1\"`/`\"0\"`), persisted on toggle.\n    Side-effect: toggles `asst--threads-collapsed` on the **parent `.asst` grid**\n    (via `asideRef.current.parentElement.classList.toggle`) so the grid's first\n    column narrows to 64px — a DOM-reach-up, documented hazard below.\n  - `lang` — read from `langSignal.value` during render (NOT the `lang` prop,\n    which is an ignored SSR seed) so the list re-localizes live on a Settings\n    language flip.\n- **Data source:** `GET /agents/conversations?limit=50` via\n  `assistantClient.conversations(50)`. The list re-fetches on a timer, on tab\n  `visibilitychange→visible`, and once on mount.\n- **Liveness — POLLING (flag):** `setInterval(refresh, POLL_MS)` where\n  `POLL_MS = 8_000` (8s). This is how a customer accept-contract event (which sets\n  `hasUnreadEvent` + bumps `updatedAt` server-side) surfaces here without a hard\n  reload — the unread dot/badge appears within ≤8s. **No websocket** (the voice\n  push path lives in AsstChat, not here). On a fetch error it keeps the last good\n  list rather than blanking.\n- **Honest-empty:** when `groups.length === 0` it renders\n  `.threads__empty` (\"No conversations yet\" via `asstThreads.empty`). The\n  `.threads__count` always shows `threads.length` (can be 0).\n- **Anti-patterns:**\n  - **8s poll** — acceptable for a low-frequency sidebar but a fixed-interval\n    poll; a rebuild could move it to the same websocket the chat already holds.\n  - **DOM reach-up:** mutates `parentElement.classList` instead of the parent\n    owning the collapsed class. Brittle if the DOM nesting under `.asst` changes.\n  - No `location.reload()`.\n- **Data-shape hazards:**\n  - `updatedAt` is normally an ISO-8601 string; `tsOf()` defensively also accepts\n    a numeric epoch string and disambiguates seconds vs ms by magnitude\n    (`<1e12 ⇒ ×1000`) so a seconds-epoch value doesn't render as \"1970 → Nd ago\".\n  - `deriveChip` walks the chain **backwards** (invoice → contract → quote →\n    phase) so the chip reflects the latest stage reached. It relies on the\n    DENORMALIZED `quoteStatus` / `contractStatus` / `invoiceStatus` fields on\n    Conversation (no N+1). Quote only ever reaches `\"sent\"` in this flow (customer\n    acceptance is a contract event), so there is no quote-accepted branch.\n\n## 2. Anatomy\n```\n<aside class=\"threads [threads--collapsed]\" ref=asideRef>\n  <div class=\"threads__head\">\n    <button class=\"threads__toggle\" onClick=toggleCollapsed>   ← hamburger / collapse-arrow icon swaps on state\n    <h3 class=\"threads__title\">{t conversations}</h3>\n    <span class=\"threads__count\">{threads.length}</span>\n  </div>\n  <a href=\"/assistant\" class=\"threads__new\">                   ← New conversation (full SSR nav, not an island action)\n    <I plus/> <span class=\"threads__new-label\"/> <span class=\"threads__new-kbd\">{⌘N}</span>\n  </a>\n  <div class=\"threads__list\">\n    EMPTY → <div class=\"threads__empty\"/>\n    else groups.map(group =>                                    ← Today / Yesterday / This Week / Earlier\n      <div class=\"threads__group-label\">{group.label}</div>\n      group.items.map(c =>\n        <a href=\"/assistant/{c.id}\" class=\"thread [thread--active] [thread--unread]\">\n          <div class=\"thread__head\">\n            [hasUnreadEvent → <span class=\"thread__unread-dot\"/>]\n            <span class=\"thread__client\">{customerName||title||t newConversation}</span>\n            <span class=\"thread__time\">{fmtTime(updatedAt)}</span>\n          </div>\n          <div class=\"thread__preview\">{preview ?? \"—\"}</div>\n          <div class=\"thread__chips\"><span class=\"thread__chip thread__chip--{kind}\">{label}</span></div>\n        </a>))\n  </div>\n</aside>\n```\n- **Icon dependency:** `I` + `ICN.plus` from `lib/dash-icons.tsx` (copied to\n  `js/dash-icons.tsx`); the toggle hamburger paths are inline `<path>`.\n- **Recency grouping:** `groupByRecency()` buckets by `updatedAt` into Today /\n  Yesterday (≥ today-1d) / This Week (≥ today-7d) / Earlier; empty buckets drop.\n- **Time format:** `fmtTime()` → `\"just now\"` (<1m) · `\"Nm\"` (<1h) · `\"Nh\"`\n  (<1d) · localized weekday (<7d) · `M/D` (older).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `initialThreads` | `Conversation[]` (required) | — | (json) | no — SSR seed, then replaced by poll |\n| `activeId` | `string` | `undefined` | text | no |\n| `lang` | `\"en\"\\|\"es\"` | (ignored) | select | **ignored SSR seed** — island reads `langSignal.value` |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | grouped list, one row active | `cases/default/default.json` |\n| unread | a thread with `hasUnreadEvent` → pulsing dot + bold client | `cases/unread/unread.json` |\n| collapsed | `threads--collapsed` rail (labels/list hidden) | `cases/collapsed/collapsed.json` |\n| empty | `initialThreads=[]` → `.threads__empty` | `cases/empty/empty.json` |\n| chip-variants | rows exercising draft/sent/paid/needs chips | `cases/chip-variants/chip-variants.json` |\n\n> Isolate note: the poll will try to fetch `/agents/conversations` with no\n> backend; in isolate it 404s/throws and `refresh()` swallows it, so the rows\n> stay on the seeded `initialThreads`. Cases use real seed shapes from\n> `lib/asst-seed.ts` (t1–t7) mapped onto the `Conversation` shape.\n\n## 5. Events\n- `ev.expect(e => e.source === \"button.threads__toggle\" && e.type === \"click\")`\n  → toggles `collapsed`; writes `localStorage[\"pm:threads-collapsed\"]`; toggles\n  `asst--threads-collapsed` on the parent grid.\n- Row click is a real `<a href=\"/assistant/{id}\">` navigation (no island handler).\n- \"New conversation\" is `<a href=\"/assistant\">` navigation.\n\n## 6. Motion (extracted)\n- **Unread dot:** `@keyframes thread-pulse` — `box-shadow 0 0 0 0 → 0 0 0 6px`\n  (green, fading alpha) over `1.6s ease-out infinite`. The only keyframe this\n  component owns.\n- **New CTA:** `transform: translateY(-1px)` on hover, `180ms`.\n- **Row:** `background 140ms` on hover/active.\n- **Reduced motion:** no component-local guard — relies on the global tokens\n  reduced-motion clamp (`animation-duration: 0.01ms !important`). Verify the\n  pulse stills.\n\n## 7. Responsive\n- The column is sized by the parent `.asst` grid (`280px` first column; `64px`\n  when `asst--threads-collapsed` at ≥1201px). Below the threads-hidden\n  breakpoint the column is removed by the page CSS, not by this island. Verify\n  against assistant-page.css (`.asst` grid ~7806) + assistant.css (collapse\n  ~3733). No `@media` queries are owned by this component itself.\n\n## 8. A11y\n- Toggle is a real `<button>` with `aria-label`/`title` that swap on state — good.\n- Rows are `<a>` links — keyboard-operable.\n- Unread dot has `aria-label` (`asstThreads.newEvent`).\n- **Gap:** the recency group labels are plain `<div>`s, not list semantics; the\n  list is not a `role=\"list\"`/`listitem` structure. Minor.\n\n## 9. Used on\nBoth assistant route variants: `routes/assistant/index.tsx` (no `activeId`) and\n`routes/assistant/[threadId].tsx` (`activeId={threadId}`). CSS in\n`static/assistant-page.css` (column/rows) + `static/assistant.css` (collapse).\n", "fixture": "{\n  \"category\": \"assistant\",\n  \"folder\": \"asst-threads\",\n  \"controls\": {\n    \"initialThreads\": {\n      \"type\": \"json\",\n      \"value\": [\n        {\n          \"id\": \"t1\",\n          \"userId\": \"u_dev\",\n          \"customerName\": \"Maria Gonzalez\",\n          \"preview\": \"Estimate sent — kitchen backsplash + under-cabinet lighting.\",\n          \"currentPhase\": \"terms\",\n          \"contractStatus\": \"sent\",\n          \"hasUnreadEvent\": false,\n          \"updatedAt\": \"2026-06-18T12:30:00.000Z\",\n          \"createdAt\": \"2026-06-18T11:00:00.000Z\"\n        },\n        {\n          \"id\": \"t2\",\n          \"userId\": \"u_dev\",\n          \"customerName\": \"Davis Remodel\",\n          \"preview\": \"Drafted quote for bathroom tile re-grout.\",\n          \"currentPhase\": \"quote\",\n          \"quoteStatus\": \"draft\",\n          \"updatedAt\": \"2026-06-18T11:30:00.000Z\",\n          \"createdAt\": \"2026-06-18T11:00:00.000Z\"\n        },\n        {\n          \"id\": \"t3\",\n          \"userId\": \"u_dev\",\n          \"customerName\": \"The Lakers\",\n          \"preview\": \"Nudged customer — waiting on a reply.\",\n          \"currentPhase\": \"quote\",\n          \"quoteStatus\": \"sent\",\n          \"hasUnreadEvent\": true,\n          \"updatedAt\": \"2026-06-18T09:30:00.000Z\",\n          \"createdAt\": \"2026-06-18T08:00:00.000Z\"\n        }\n      ]\n    },\n    \"activeId\": { \"type\": \"text\", \"value\": \"t1\" }\n  }\n}\n", "cases": [{"name": "chip-variants.json", "content": "{\n  \"_name\": \"Chip variants — draft / sent / signed / invoiced / paid / terms\",\n  \"activeId\": \"t1\",\n  \"_note\": \"Exercises every branch of deriveChip() (walks invoice→contract→quote→phase backwards).\",\n  \"initialThreads\": [\n    { \"id\": \"p\", \"userId\": \"u_dev\", \"customerName\": \"Paid Job\", \"preview\": \"Invoice paid in full.\", \"currentPhase\": \"terms\", \"invoiceStatus\": \"paid\", \"updatedAt\": \"2026-06-18T12:50:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" },\n    { \"id\": \"iv\", \"userId\": \"u_dev\", \"customerName\": \"Invoiced Job\", \"preview\": \"Invoice sent.\", \"currentPhase\": \"terms\", \"invoiceStatus\": \"sent\", \"updatedAt\": \"2026-06-18T12:40:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" },\n    { \"id\": \"sg\", \"userId\": \"u_dev\", \"customerName\": \"Signed Job\", \"preview\": \"Contract signed.\", \"currentPhase\": \"terms\", \"contractStatus\": \"accepted\", \"updatedAt\": \"2026-06-18T12:30:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" },\n    { \"id\": \"cs\", \"userId\": \"u_dev\", \"customerName\": \"Contract Sent\", \"preview\": \"Contract out for signature.\", \"currentPhase\": \"terms\", \"contractStatus\": \"sent\", \"updatedAt\": \"2026-06-18T12:20:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" },\n    { \"id\": \"qs\", \"userId\": \"u_dev\", \"customerName\": \"Quote Sent\", \"preview\": \"Estimate sent.\", \"currentPhase\": \"quote\", \"quoteStatus\": \"sent\", \"updatedAt\": \"2026-06-18T12:10:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" },\n    { \"id\": \"dr\", \"userId\": \"u_dev\", \"customerName\": \"Drafting\", \"preview\": \"Still gathering details.\", \"currentPhase\": \"quote\", \"updatedAt\": \"2026-06-18T12:00:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" }\n  ]\n}\n"}, {"name": "collapsed.json", "content": "{\n  \"_name\": \"Collapsed — rail (localStorage pm:threads-collapsed=1)\",\n  \"activeId\": \"t1\",\n  \"_localStorage\": { \"pm:threads-collapsed\": \"1\" },\n  \"_note\": \"Island seeds `collapsed` from localStorage on mount. The rail also relies on the parent .asst grid getting `asst--threads-collapsed`; in isolate the aside has no .asst parent, so the column-narrow won't show — only the in-aside `.threads--collapsed` hiding (title/count/list hidden, centered head/new) is visible.\",\n  \"initialThreads\": [\n    { \"id\": \"t1\", \"userId\": \"u_dev\", \"customerName\": \"Maria Gonzalez\", \"preview\": \"Estimate sent.\", \"currentPhase\": \"terms\", \"contractStatus\": \"sent\", \"updatedAt\": \"2026-06-18T12:30:00.000Z\", \"createdAt\": \"2026-06-18T11:00:00.000Z\" }\n  ]\n}\n"}, {"name": "default.json", "content": "{\n  \"_name\": \"Default — grouped list, t1 active\",\n  \"activeId\": \"t1\",\n  \"initialThreads\": [\n    { \"id\": \"t1\", \"userId\": \"u_dev\", \"customerName\": \"Maria Gonzalez\", \"preview\": \"Estimate sent — kitchen backsplash + under-cabinet lighting.\", \"currentPhase\": \"terms\", \"contractStatus\": \"sent\", \"updatedAt\": \"2026-06-18T12:30:00.000Z\", \"createdAt\": \"2026-06-18T11:00:00.000Z\" },\n    { \"id\": \"t2\", \"userId\": \"u_dev\", \"customerName\": \"Davis Remodel\", \"preview\": \"Drafted quote for bathroom tile re-grout.\", \"currentPhase\": \"quote\", \"quoteStatus\": \"draft\", \"updatedAt\": \"2026-06-18T11:30:00.000Z\", \"createdAt\": \"2026-06-18T11:00:00.000Z\" },\n    { \"id\": \"t4\", \"userId\": \"u_dev\", \"customerName\": \"Carter Family\", \"preview\": \"Contract signed — deck rebuild.\", \"currentPhase\": \"terms\", \"contractStatus\": \"accepted\", \"updatedAt\": \"2026-06-17T15:00:00.000Z\", \"createdAt\": \"2026-06-17T09:00:00.000Z\" }\n  ]\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — no conversations yet\",\n  \"initialThreads\": []\n}\n"}, {"name": "unread.json", "content": "{\n  \"_name\": \"Unread — pulsing dot + bold client\",\n  \"activeId\": \"t1\",\n  \"initialThreads\": [\n    { \"id\": \"t3\", \"userId\": \"u_dev\", \"customerName\": \"The Lakers\", \"preview\": \"Customer signed the contract.\", \"currentPhase\": \"terms\", \"contractStatus\": \"accepted\", \"hasUnreadEvent\": true, \"updatedAt\": \"2026-06-18T12:55:00.000Z\", \"createdAt\": \"2026-06-18T08:00:00.000Z\" },\n    { \"id\": \"t1\", \"userId\": \"u_dev\", \"customerName\": \"Maria Gonzalez\", \"preview\": \"Estimate sent — kitchen backsplash.\", \"currentPhase\": \"terms\", \"contractStatus\": \"sent\", \"updatedAt\": \"2026-06-18T12:30:00.000Z\", \"createdAt\": \"2026-06-18T11:00:00.000Z\" }\n  ]\n}\n"}], "css": "/* ============================================================\n   AsstThreads — extracted from static/assistant-page.css (column +\n   thread rows + thread-pulse keyframe) AND static/assistant.css\n   (the collapse/rail rules, which live in assistant.css only).\n   Tokens: --border, --brand-teal, --brand-green, --fg-muted, --fg-subtle,\n   --coffee-50/600, --green-50/600, --pink-50/700, --mint-100,\n   --font-heading, --font-mono.\n   NOTE: the live route layout for `.asst` is the 3-column GRID defined\n   LATER in assistant-page.css (line ~7806): `280px minmax(0,1fr) 360px`,\n   collapsing to 2 cols via `.asst:not(:has(.docpane))`. The earlier flex\n   `.asst` (line ~4433) is overridden by cascade. See assistant.md §7.\n   ============================================================ */\n\n/* ----- Threads column (assistant-page.css ~4454) ----- */\n.threads {\n  background: rgba(255, 255, 255, 0.7);\n  border: 1px solid var(--border);\n  border-radius: 18px;\n  display: flex;\n  flex-direction: column;\n  min-height: 0;\n  overflow: hidden;\n}\n.threads__head {\n  padding: 14px 16px 12px;\n  border-bottom: 1px solid var(--border);\n  display: flex;\n  align-items: center;\n  gap: 10px;\n}\n.threads__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  margin: 0;\n  flex: 1;\n}\n.threads__count {\n  font-size: 11px;\n  color: var(--fg-muted);\n  background: var(--coffee-50);\n  padding: 3px 8px;\n  border-radius: 999px;\n  font-weight: 700;\n}\n.threads__new {\n  padding: 12px 14px;\n  margin: 12px;\n  background: var(--brand-green);\n  color: #fff;\n  border: none;\n  border-radius: 12px;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  cursor: pointer;\n  box-shadow: 0 6px 16px rgba(81, 152, 67, 0.35);\n  transition: transform 180ms;\n}\n.threads__new:hover {\n  transform: translateY(-1px);\n}\n.threads__new-kbd {\n  margin-left: auto;\n  background: rgba(255, 255, 255, 0.2);\n  padding: 2px 8px;\n  border-radius: 6px;\n  font-family: var(--font-mono);\n  font-size: 10px;\n  font-weight: 700;\n}\n.threads__list {\n  flex: 1;\n  overflow-y: auto;\n  padding: 0 8px 12px;\n}\n.threads__list::-webkit-scrollbar {\n  width: 0;\n}\n.threads__group-label {\n  font-size: 10px;\n  font-weight: 700;\n  letter-spacing: 0.1em;\n  text-transform: uppercase;\n  color: var(--fg-subtle);\n  padding: 12px 10px 6px;\n}\n\n/* ----- Thread row ----- */\n.thread {\n  display: block;\n  width: 100%;\n  text-align: left;\n  background: transparent;\n  border: none;\n  padding: 10px 10px;\n  border-radius: 12px;\n  cursor: pointer;\n  margin-bottom: 2px;\n  transition: background 140ms;\n}\n.thread:hover {\n  background: rgba(255, 255, 255, 0.6);\n}\n.thread--active {\n  background: #fff;\n  box-shadow:\n    0 4px 14px rgba(20, 72, 82, 0.06),\n    0 0 0 1px rgba(20, 72, 82, 0.06);\n}\n.thread__head {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n}\n.thread__client {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  color: var(--brand-teal);\n  flex: 1;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.thread__time {\n  font-size: 10.5px;\n  color: var(--fg-subtle);\n  font-weight: 600;\n  flex-shrink: 0;\n}\n.thread__preview {\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin-top: 2px;\n  line-height: 1.35;\n  display: -webkit-box;\n  -webkit-line-clamp: 2;\n  -webkit-box-orient: vertical;\n  overflow: hidden;\n}\n.thread__chips {\n  display: flex;\n  gap: 4px;\n  margin-top: 6px;\n  flex-wrap: wrap;\n}\n.thread__chip {\n  font-size: 10px;\n  font-weight: 700;\n  padding: 2px 7px;\n  border-radius: 999px;\n  letter-spacing: 0.02em;\n}\n.thread__chip--draft {\n  background: var(--coffee-50);\n  color: var(--coffee-600);\n}\n.thread__chip--sent {\n  background: var(--green-50);\n  color: var(--green-600);\n}\n.thread__chip--paid {\n  background: var(--green-50);\n  color: var(--green-600);\n}\n.thread__chip--needs {\n  background: var(--pink-50);\n  color: var(--pink-700);\n}\n\n/* Unread pulse dot (set by hasUnreadEvent; cleared on next load) */\n.thread__unread-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 999px;\n  background: var(--green-600, #2f7d3a);\n  box-shadow: 0 0 0 0 rgba(47, 125, 58, 0.55);\n  animation: thread-pulse 1.6s ease-out infinite;\n  flex-shrink: 0;\n}\n.thread--unread .thread__client {\n  color: var(--brand-teal);\n  font-weight: 900;\n}\n@keyframes thread-pulse {\n  0% {\n    box-shadow: 0 0 0 0 rgba(47, 125, 58, 0.55);\n  }\n  100% {\n    box-shadow: 0 0 0 6px rgba(47, 125, 58, 0);\n  }\n}\n.threads__empty {\n  padding: 14px 10px;\n  color: var(--fg-subtle);\n  font-size: 12px;\n}\n\n/* ----- Conversation-list collapse (roadmap p.4) -----\n   These rules live in static/assistant.css ONLY (~3733), not in\n   assistant-page.css. QuickBooks-style minimize: a hamburger/arrow narrows\n   the rail to an icon strip; the grid override is gated to >1200px (the\n   width where the threads column is actually shown). */\n.threads__toggle {\n  width: 28px;\n  height: 28px;\n  border-radius: 8px;\n  border: none;\n  background: var(--coffee-50);\n  color: var(--brand-teal);\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n  transition: background 160ms;\n}\n.threads__toggle:hover {\n  background: var(--mint-100);\n}\n.threads__toggle svg {\n  display: block;\n}\n.threads--collapsed .threads__title,\n.threads--collapsed .threads__count,\n.threads--collapsed .threads__new-label,\n.threads--collapsed .threads__new-kbd,\n.threads--collapsed .threads__list {\n  display: none;\n}\n.threads--collapsed .threads__head {\n  justify-content: center;\n}\n.threads--collapsed .threads__new {\n  margin: 12px auto;\n  padding: 12px;\n  justify-content: center;\n}\n@media (min-width: 1201px) {\n  .asst.asst--threads-collapsed {\n    grid-template-columns: 64px minmax(0, 1fr) 360px;\n  }\n}\n", "cssName": "asst-threads.css", "source": "import { useEffect, useRef, useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { assistantClient, type Conversation } from \"../clients/assistant.ts\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  initialThreads: Conversation[];\n  activeId?: string;\n  lang?: Lang;\n}\n\ntype Chip = \"sent\" | \"draft\" | \"needs\" | \"paid\";\n\nconst POLL_MS = 8_000;\nconst HOUR = 3_600_000;\nconst DAY = 24 * HOUR;\n\nexport default function AsstThreads(\n  { initialThreads, activeId }: Props,\n) {\n  // Self-source the live UI language so this island re-renders when the\n  // language flips (SettingsPage), rather than freezing on the SSR seed.\n  const lang = langSignal.value;\n  const [threads, setThreads] = useState<Conversation[]>(initialThreads);\n  // Roadmap p.4: QuickBooks-style minimize for the conversation list. We\n  // toggle a class on the parent .asst grid so its first column narrows to a\n  // rail, and a class on the aside so the inner content collapses to icons.\n  const [collapsed, setCollapsed] = useState<boolean>(() => {\n    if (typeof globalThis.localStorage === \"undefined\") return false;\n    return globalThis.localStorage.getItem(\"pm:threads-collapsed\") === \"1\";\n  });\n  const asideRef = useRef<HTMLElement | null>(null);\n\n  useEffect(() => {\n    const parent = asideRef.current?.parentElement;\n    if (parent) parent.classList.toggle(\"asst--threads-collapsed\", collapsed);\n  }, [collapsed]);\n\n  function toggleCollapsed() {\n    setCollapsed((c) => {\n      const next = !c;\n      try {\n        globalThis.localStorage?.setItem(\n          \"pm:threads-collapsed\",\n          next ? \"1\" : \"0\",\n        );\n      } catch { /* SSR-safe */ }\n      return next;\n    });\n  }\n\n  // Live-refresh: poll on an interval so a customer accept (which flips\n  // hasUnreadEvent + bumps updatedAt server-side) shows up here without\n  // a hard reload, and re-fetch on tab focus so coming back from another\n  // tab is snappy.\n  useEffect(() => {\n    let cancelled = false;\n    async function refresh() {\n      try {\n        const next = await assistantClient.conversations(50);\n        if (!cancelled) setThreads(next);\n      } catch {\n        // Stay on the last good list rather than blanking the sidebar\n        // on a transient network blip.\n      }\n    }\n    const interval = setInterval(refresh, POLL_MS);\n    function onVis() {\n      if (document.visibilityState === \"visible\") refresh();\n    }\n    document.addEventListener(\"visibilitychange\", onVis);\n    // Sync once on mount in case SSR's snapshot is already stale.\n    refresh();\n    return () => {\n      cancelled = true;\n      clearInterval(interval);\n      document.removeEventListener(\"visibilitychange\", onVis);\n    };\n  }, []);\n\n  const sorted = [...threads].sort((a, b) =>\n    tsOf(b.updatedAt) - tsOf(a.updatedAt)\n  );\n  const groups = groupByRecency(sorted, lang);\n  const total = threads.length;\n\n  return (\n    <aside\n      ref={asideRef}\n      class={`threads ${collapsed ? \"threads--collapsed\" : \"\"}`}\n    >\n      <div class=\"threads__head\">\n        <button\n          type=\"button\"\n          class=\"threads__toggle\"\n          onClick={toggleCollapsed}\n          aria-label={collapsed\n            ? tFor(lang, \"asstThreads.expandConversations\")\n            : tFor(lang, \"asstThreads.collapseConversations\")}\n          title={collapsed\n            ? tFor(lang, \"asstThreads.expand\")\n            : tFor(lang, \"asstThreads.collapse\")}\n        >\n          <I\n            d={collapsed\n              ? (\n                <>\n                  <path d=\"M3 6h18M3 12h18M3 18h18\" />\n                </>\n              )\n              : (\n                <>\n                  {/* QuickBooks-style hamburger + collapse arrow */}\n                  <path d=\"M3 6h13M3 12h13M3 18h13\" />\n                  <path d=\"M21 9l-3 3 3 3\" />\n                </>\n              )}\n            size={16}\n          />\n        </button>\n        <h3 class=\"threads__title\">{tFor(lang, \"asstThreads.conversations\")}</h3>\n        <span class=\"threads__count\">{total}</span>\n      </div>\n      <a\n        href=\"/assistant\"\n        class=\"threads__new\"\n        style=\"text-decoration:none\"\n        title={tFor(lang, \"asstThreads.newConversation\")}\n      >\n        <I d={ICN.plus} size={14} sw={2.5} />\n        <span class=\"threads__new-label\">\n          {tFor(lang, \"asstThreads.newConversation\")}\n        </span>\n        <span class=\"threads__new-kbd\">{tFor(lang, \"asstThreads.newKbd\")}</span>\n      </a>\n      <div class=\"threads__list\">\n        {groups.length === 0\n          ? (\n            <div class=\"threads__empty\">\n              {tFor(lang, \"asstThreads.empty\")}\n            </div>\n          )\n          : groups.map((group) => (\n            <div key={group.label}>\n              <div class=\"threads__group-label\">{group.label}</div>\n              {group.items.map((c) => {\n                const chip = deriveChip(c, lang);\n                return (\n                  <a\n                    key={c.id}\n                    href={`/assistant/${c.id}`}\n                    class={`thread ${\n                      c.id === activeId ? \"thread--active\" : \"\"\n                    } ${c.hasUnreadEvent ? \"thread--unread\" : \"\"}`}\n                    style=\"text-decoration:none;text-align:left;width:100%;display:block\"\n                  >\n                    <div class=\"thread__head\">\n                      {c.hasUnreadEvent\n                        ? (\n                          <span\n                            class=\"thread__unread-dot\"\n                            aria-label={tFor(lang, \"asstThreads.newEvent\")}\n                          />\n                        )\n                        : null}\n                      <span class=\"thread__client\">{titleFor(c, lang)}</span>\n                      <span class=\"thread__time\">\n                        {fmtTime(c.updatedAt, lang)}\n                      </span>\n                    </div>\n                    <div class=\"thread__preview\">{c.preview ?? \"—\"}</div>\n                    <div class=\"thread__chips\">\n                      <span class={`thread__chip thread__chip--${chip.kind}`}>\n                        {chip.label}\n                      </span>\n                    </div>\n                  </a>\n                );\n              })}\n            </div>\n          ))}\n      </div>\n    </aside>\n  );\n}\n\nfunction titleFor(c: Conversation, lang: Lang): string {\n  return c.customerName?.trim() || c.title?.trim() ||\n    tFor(lang, \"asstThreads.newConversation\");\n}\n\n/**\n * Map the most-advanced known status to one of the four chip CSS\n * variants (draft / sent / paid / needs). Walks the chain backwards —\n * invoice → contract → quote — so the chip reflects the latest stage\n * the conversation has reached, not the earliest.\n */\nfunction deriveChip(\n  c: Conversation,\n  lang: Lang,\n): { kind: Chip; label: string } {\n  // Walk the chain backwards (latest stage wins). Customer acceptance\n  // is a single event on the contract — quoteStatus only ever reaches\n  // \"sent\" in this flow, so no quote-accepted branch is needed.\n  if (c.invoiceStatus === \"paid\") {\n    return { kind: \"paid\", label: tFor(lang, \"status.paid\") };\n  }\n  if (c.invoiceStatus === \"sent\") {\n    return { kind: \"sent\", label: tFor(lang, \"asstThreads.chip.invoiced\") };\n  }\n  if (c.contractStatus === \"accepted\") {\n    return { kind: \"paid\", label: tFor(lang, \"status.signed\") };\n  }\n  if (c.contractStatus === \"sent\") {\n    return { kind: \"sent\", label: tFor(lang, \"asstThreads.chip.contractSent\") };\n  }\n  if (c.contractStatus === \"draft\") {\n    return { kind: \"needs\", label: tFor(lang, \"asstThreads.chip.contract\") };\n  }\n  if (c.quoteStatus === \"sent\") {\n    return { kind: \"sent\", label: tFor(lang, \"asstThreads.chip.quoteSent\") };\n  }\n  if (c.currentPhase === \"terms\") {\n    return { kind: \"needs\", label: tFor(lang, \"asstThreads.chip.terms\") };\n  }\n  return { kind: \"draft\", label: tFor(lang, \"asstThreads.chip.drafting\") };\n}\n\nfunction tsOf(iso: string): number {\n  if (!iso) return 0;\n  // ISO-8601 string is the normal shape.\n  const parsed = Date.parse(iso);\n  if (Number.isFinite(parsed)) return parsed;\n  // Fallback: a numeric epoch delivered as a string. Disambiguate seconds vs\n  // milliseconds by magnitude (10-digit ≈ seconds, 13-digit ≈ ms) so a\n  // seconds-epoch value doesn't render as 1970 → bogus \"Nd ago\".\n  const n = Number(iso);\n  if (Number.isFinite(n) && n > 0) return n < 1e12 ? n * 1000 : n;\n  return 0;\n}\n\nconst WEEKDAY_KEYS = [\n  \"asstThreads.weekday.sun\",\n  \"asstThreads.weekday.mon\",\n  \"asstThreads.weekday.tue\",\n  \"asstThreads.weekday.wed\",\n  \"asstThreads.weekday.thu\",\n  \"asstThreads.weekday.fri\",\n  \"asstThreads.weekday.sat\",\n];\n\nfunction fmtTime(iso: string, lang: Lang): string {\n  const t = tsOf(iso);\n  if (!t) return \"\";\n  const diff = Date.now() - t;\n  if (diff < 60_000) return tFor(lang, \"asstThreads.justNow\");\n  if (diff < HOUR) return `${Math.floor(diff / 60_000)}m`;\n  if (diff < DAY) return `${Math.floor(diff / HOUR)}h`;\n  if (diff < 7 * DAY) return tFor(lang, WEEKDAY_KEYS[new Date(t).getDay()]);\n  const d = new Date(t);\n  return `${d.getMonth() + 1}/${d.getDate()}`;\n}\n\nfunction groupByRecency(\n  convs: Conversation[],\n  lang: Lang,\n): { label: string; items: Conversation[] }[] {\n  const todayStart = new Date();\n  todayStart.setHours(0, 0, 0, 0);\n  const yesterdayStart = todayStart.getTime() - DAY;\n  const weekStart = todayStart.getTime() - 7 * DAY;\n\n  const today: Conversation[] = [];\n  const yesterday: Conversation[] = [];\n  const week: Conversation[] = [];\n  const older: Conversation[] = [];\n  for (const c of convs) {\n    const t = tsOf(c.updatedAt);\n    if (t >= todayStart.getTime()) today.push(c);\n    else if (t >= yesterdayStart) yesterday.push(c);\n    else if (t >= weekStart) week.push(c);\n    else older.push(c);\n  }\n\n  return [\n    { label: tFor(lang, \"asstThreads.group.today\"), items: today },\n    { label: tFor(lang, \"asstThreads.group.yesterday\"), items: yesterday },\n    { label: tFor(lang, \"asstThreads.group.thisWeek\"), items: week },\n    { label: tFor(lang, \"asstThreads.group.earlier\"), items: older },\n  ].filter((g) => g.items.length > 0);\n}\n", "sourceName": "AsstThreads.tsx", "checklist": "# Capture checklist — AsstThreads\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/assistant` (no active row) and\n  `http://localhost:5280/assistant/<threadId>` (one `.thread--active`).\n- The left column is hidden by the page grid below its breakpoint — capture the\n  threads column at a width where it's shown (≥980/1201px).\n\n## Viewports\n- **1280px** (full 3-col grid, threads = 280px column) and the **collapsed rail**\n  (≥1201px after clicking the toggle → 64px column).\n- Optionally 1100px to confirm the column drops out (page CSS, not this island).\n\n## Element(s) to crop\n- The whole `<aside class=\"threads\">`: head (toggle + title + count), the green\n  \"New conversation\" CTA, and the grouped list (group labels + thread rows).\n- A row with `thread--unread` (pulsing dot + bold client) and a row that is\n  `thread--active`.\n\n## Transient states to drive\n1. **default** — grouped Today / Yesterday rows with status chips.\n2. **unread** — needs a thread where the customer signed the contract\n   (`hasUnreadEvent`); the green dot pulses and the client name goes bold. With no\n   backend, force via the dev sim accept-contract path, or rely on isolate.\n3. **collapsed** — click `.threads__toggle`; labels/list hide, the parent `.asst`\n   grid narrows the column to 64px. Reload → confirms `localStorage\n   [\"pm:threads-collapsed\"]` persisted.\n4. **empty** — a brand-new account with zero conversations → `.threads__empty`.\n5. **chip-variants** — threads at draft / quote-sent / contract-sent / signed /\n   invoiced / paid stages to capture all four chip colors.\n\n## Motion to film\n- The unread `thread-pulse` ring (1.6s ease-out) — film one cycle; re-shoot with\n  reduced-motion (global clamp).\n- The collapse toggle: column narrow + label fade (no dedicated keyframe; CSS\n  display swap + grid template change).\n\n## Liveness note\n- The list polls `/agents/conversations` every 8s and on tab focus. To see a live\n  update, trigger a customer accept in another context and watch the unread dot\n  appear within ≤8s (no reload).\n"}, {"name": "chat-header-live", "component": "ChatHeaderLive", "spec": "# ChatHeaderLive\n\nThe live chat header strip (title + live status chip + back/share/more tools)\nabove the message list. Renders the same `.chat__head` DOM the static SSR\n`<ChatHeader/>` would, but subscribes to window events so AsstChat can update the\ntitle and status string **in place** as a conversation forms — no reload.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ChatHeaderLive.tsx`).\n- **Interaction tier:** `island` (client-only state) — event-driven, no fetch.\n- **Client state owned:**\n  - `client: string` — header title; seeded from `initialClient` (SSR), then\n    overwritten by `pm:asst-header` events.\n  - `status: string` — sub-status text; seeded from `initialStatus`, then\n    overwritten by `pm:asst-header`.\n  - `historyDepth: number` — back-stack depth; driven by `pm:asst-history`. The\n    back button renders only when `historyDepth > 0`.\n  - `lang` — `langSignal.value` (the `lang` prop is an ignored SSR seed).\n- **Data source:** none of its own. Title/status are **pushed** from the\n  sibling `AsstChat` island via `globalThis` CustomEvents.\n- **Liveness — window-event PUSH (not polling, not websocket):**\n  - listens `pm:asst-header` → `{ client, status }` → `setClient`/`setStatus`\n    (only when the string is non-empty, so it never blanks).\n  - listens `pm:asst-history` → `{ depth }` → `setHistoryDepth`.\n  - on back-button click it **dispatches** `pm:asst-back` (no detail); AsstChat\n    pops its view-snapshot stack.\n- **Honest-empty:** always renders title+status (seeds are non-empty t-strings\n  like \"New conversation\" / \"Assistant ready to help\").\n- **Anti-patterns:** none. No `location.reload()`, no polling, no frozen-prop\n  hazard (the SSR seeds are intentionally superseded by live events).\n\n## 2. Anatomy\n```\n<div class=\"chat__head\">\n  [historyDepth>0 → <a class=\"chat__head-btn\" onClick=dispatch pm:asst-back><I back/></a>]\n  <div class=\"chat__head-info\">\n    <div class=\"chat__head-title\">{client}</div>\n    <div class=\"chat__head-sub\"><span class=\"chat__head-dot\"/>{status}</div>   ← dot pulses (tickerPulse)\n  </div>\n  <div class=\"chat__head-tools\">\n    <button class=\"chat__head-btn\" title=shareThread><I send/></button>\n    <button class=\"chat__head-btn\" title=more><I more/></button>\n  </div>\n</div>\n```\n- **Icon dependency:** `I` + `ICN.back`/`ICN.send`/`ICN.more` from\n  `lib/dash-icons.tsx` (copied to `js/dash-icons.tsx`).\n- Share/more buttons are **inert** (title-only, no onClick) — placeholder tools.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `initialClient` | string (required) | — | text | no — SSR seed, overwritten by `pm:asst-header` |\n| `initialStatus` | string (required) | — | text | no — SSR seed, overwritten by `pm:asst-header` |\n| `lang` | `\"en\"\\|\"es\"` | (ignored) | select | **ignored SSR seed** — reads `langSignal.value` |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| new | empty thread seed (\"New conversation\" / help line) | `cases/new/new.json` |\n| named | a customer-bound thread (title=name, phase status) | `cases/named/named.json` |\n| with-back | history depth > 0 → back button visible | `cases/with-back/with-back.json` |\n\n> Isolate note: drive the live updates via `_signals` (`client`, `status`,\n> `historyDepth`) since the real values arrive over `pm:asst-header` /\n> `pm:asst-history` from AsstChat, which isn't mounted in isolation. Back-button\n> click → assert the `pm:asst-back` dispatch in Events.\n\n## 5. Events\n- `ev.expect(e => e.source === \"a.chat__head-btn\" && e.type === \"click\")` (the\n  back anchor, only present when `historyDepth>0`) → `e.preventDefault()` +\n  dispatches `pm:asst-back`.\n- Incoming (not user events): `pm:asst-header`, `pm:asst-history` from AsstChat.\n\n## 6. Motion (extracted)\n- **Live dot:** `.chat__head-dot` runs `@keyframes tickerPulse`\n  (`box-shadow 0 0 0 0 → 0 0 0 6px → 0`, green, fading) `1.6s ease-in-out\n  infinite`. Shared keyframe (also used by the topbar ticker / chat header).\n- **Tool buttons:** `transition: all 160ms` (color + border on hover).\n- **Reduced motion:** no component-local guard — relies on the global tokens\n  reduced-motion clamp.\n\n## 7. Responsive\n- No own `@media`. It's a flex row inside the `.chat` column; `.chat__head-info`\n  is `min-width:0` so the title ellipsizes. Verify within the page's\n  assistant-page.css `.asst` grid breakpoints.\n\n## 8. A11y\n- Back/share/more are real `<button>`/`<a>` with `title` text (no\n  `aria-label`); the back anchor is `href=\"#\"` + `preventDefault` (rebuild fix:\n  prefer a `<button>`).\n- The live status has no `aria-live` — title/status changes are silent to AT.\n  Rebuild fix: wrap `.chat__head-sub` in `aria-live=\"polite\"`.\n\n## 9. Used on\nBoth assistant route variants. On `index.tsx` seeded with\n`assistantPage.newConversation` / `assistantPage.assistantHelp`; on\n`[threadId].tsx` seeded with the resolved customer/title + phase-prefixed status.\nCSS in `static/assistant-page.css` (`.chat__head*`).\n", "fixture": "{\n  \"category\": \"assistant\",\n  \"folder\": \"chat-header-live\",\n  \"controls\": {\n    \"initialClient\": { \"type\": \"text\", \"value\": \"New conversation\" },\n    \"initialStatus\": { \"type\": \"text\", \"value\": \"Bossie is ready to help\" }\n  }\n}\n", "cases": [{"name": "named.json", "content": "{\n  \"_name\": \"Named — customer-bound, terms phase\",\n  \"initialClient\": \"New conversation\",\n  \"initialStatus\": \"Bossie is ready to help\",\n  \"_signals\": { \"client\": \"Maria Gonzalez\", \"status\": \"Phase: terms\" }\n}\n"}, {"name": "new.json", "content": "{\n  \"_name\": \"New — empty thread seed\",\n  \"initialClient\": \"New conversation\",\n  \"initialStatus\": \"Bossie is ready to help\"\n}\n"}, {"name": "with-back.json", "content": "{\n  \"_name\": \"With back — historyDepth>0 shows back button\",\n  \"initialClient\": \"Maria Gonzalez\",\n  \"initialStatus\": \"Reviewing quote\",\n  \"_signals\": { \"client\": \"Maria Gonzalez\", \"status\": \"Reviewing quote\", \"historyDepth\": 2 }\n}\n"}], "css": "/* ============================================================\n   ChatHeaderLive — extracted from static/assistant-page.css (~4651).\n   Renders the same `.chat__head` DOM as the static SSR <ChatHeader/>.\n   Tokens: --border, --brand-teal, --brand-green, --green-600, --fg-muted,\n   --font-heading.\n   The pulsing live dot reuses the `tickerPulse` keyframe (shared, defined\n   in assistant-page.css ~2955 / ~741 — included here for portability).\n   ============================================================ */\n\n.chat__head {\n  padding: 14px 18px;\n  border-bottom: 1px solid var(--border);\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  background: linear-gradient(\n    180deg,\n    rgba(254, 237, 228, 0.5),\n    rgba(254, 237, 228, 0)\n  );\n}\n.chat__head-info {\n  flex: 1;\n  min-width: 0;\n}\n.chat__head-title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  color: var(--brand-teal);\n  line-height: 1.1;\n}\n.chat__head-sub {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  font-size: 11.5px;\n  color: var(--green-600);\n  font-weight: 700;\n  margin-top: 2px;\n}\n.chat__head-dot {\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: var(--brand-green);\n  box-shadow: 0 0 0 0 rgba(81, 152, 67, 0.5);\n  animation: tickerPulse 1.6s ease-in-out infinite;\n}\n.chat__head-tools {\n  display: flex;\n  gap: 4px;\n}\n.chat__head-btn {\n  width: 32px;\n  height: 32px;\n  border: 1px solid var(--border);\n  border-radius: 10px;\n  background: #fff;\n  display: grid;\n  place-items: center;\n  color: var(--fg-muted);\n  cursor: pointer;\n  transition: all 160ms;\n}\n.chat__head-btn:hover {\n  color: var(--brand-teal);\n  border-color: var(--brand-teal);\n}\n\n/* Shared live-dot pulse (defined once in assistant-page.css; reproduced\n   here so the component renders standalone in isolate). */\n@keyframes tickerPulse {\n  0% {\n    box-shadow: 0 0 0 0 rgba(81, 152, 67, 0.55);\n  }\n  70% {\n    box-shadow: 0 0 0 6px rgba(81, 152, 67, 0);\n  }\n  100% {\n    box-shadow: 0 0 0 0 rgba(81, 152, 67, 0);\n  }\n}\n", "cssName": "chat-header-live.css", "source": "/**\n * Live chat header for /assistant. Renders the same DOM as the static\n * <ChatHeader /> SSR component, but subscribes to the `pm:asst-header`\n * window event so AsstChat can update the title + status string in place\n * as a conversation forms (P6.12).\n *\n * Also listens for `pm:asst-history` events from AsstChat to\n * conditionally show/hide the back button. When history depth > 0 the\n * back anchor pops the most recent snapshot via `pm:asst-back`.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  initialClient: string;\n  initialStatus: string;\n  lang?: \"en\" | \"es\";\n}\n\ninterface HeaderEvent {\n  client: string;\n  status: string;\n}\n\nexport default function ChatHeaderLive(\n  { initialClient, initialStatus }: Props,\n) {\n  // Self-source the UI language so the header re-renders live when the\n  // language flips (Settings). The optional `lang` prop is an ignored SSR seed.\n  const lang = langSignal.value;\n  const [client, setClient] = useState(initialClient);\n  const [status, setStatus] = useState(initialStatus);\n  const [historyDepth, setHistoryDepth] = useState(0);\n\n  useEffect(() => {\n    function onUpdate(e: Event) {\n      const detail = (e as CustomEvent<HeaderEvent>).detail;\n      if (!detail) return;\n      if (typeof detail.client === \"string\" && detail.client.length > 0) {\n        setClient(detail.client);\n      }\n      if (typeof detail.status === \"string\" && detail.status.length > 0) {\n        setStatus(detail.status);\n      }\n    }\n    function onHistory(e: Event) {\n      const detail = (e as CustomEvent<{ depth: number }>).detail;\n      if (detail && typeof detail.depth === \"number\") {\n        setHistoryDepth(detail.depth);\n      }\n    }\n    globalThis.addEventListener(\"pm:asst-header\", onUpdate);\n    globalThis.addEventListener(\"pm:asst-history\", onHistory);\n    return () => {\n      globalThis.removeEventListener(\"pm:asst-header\", onUpdate);\n      globalThis.removeEventListener(\"pm:asst-history\", onHistory);\n    };\n  }, []);\n\n  return (\n    <div class=\"chat__head\">\n      {historyDepth > 0\n        ? (\n          <a\n            href=\"#\"\n            class=\"chat__head-btn\"\n            title={tFor(lang, \"common.back\")}\n            style=\"text-decoration:none\"\n            onClick={(e) => {\n              e.preventDefault();\n              globalThis.dispatchEvent(new CustomEvent(\"pm:asst-back\"));\n            }}\n          >\n            <I d={ICN.back} size={15} />\n          </a>\n        )\n        : null}\n      <div class=\"chat__head-info\">\n        <div class=\"chat__head-title\">{client}</div>\n        <div class=\"chat__head-sub\">\n          <span class=\"chat__head-dot\" />\n          {status}\n        </div>\n      </div>\n      <div class=\"chat__head-tools\">\n        <button\n          type=\"button\"\n          class=\"chat__head-btn\"\n          title={tFor(lang, \"chatHeader.shareThread\")}\n        >\n          <I d={ICN.send} size={15} />\n        </button>\n        <button\n          type=\"button\"\n          class=\"chat__head-btn\"\n          title={tFor(lang, \"chatHeader.more\")}\n        >\n          <I d={ICN.more} size={15} />\n        </button>\n      </div>\n    </div>\n  );\n}\n", "sourceName": "ChatHeaderLive.tsx", "checklist": "# Capture checklist — ChatHeaderLive\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/assistant` — header seeded \"New conversation\" / help line.\n- `http://localhost:5280/assistant/<threadId>` — header resolves to the bound\n  customer name + phase-prefixed status; on a thread with view-history (after\n  opening the quote review then any sub-screen) the **back** button appears.\n\n## Viewports\n- **1280px** (full 3-col shell) and **720px** (mobile cutover, header still\n  full-width above the chat).\n\n## Element(s) to crop\n- The single `.chat__head` strip (title + pulsing dot + sub-status + the\n  share/more tool buttons). Capture once without and once with the back button.\n\n## Transient states to drive\n1. **new** — land on `/assistant`; title \"New conversation\".\n2. **named** — open a thread / start a conversation that binds a customer; watch\n   the title swap in place (driven by `pm:asst-header`, no reload).\n3. **with-back** — inside a thread, drill into the quote review or a wizard\n   sub-screen so AsstChat pushes history → the back chevron fades in.\n\n## Motion to film\n- The `.chat__head-dot` `tickerPulse` ring (1.6s) — film one cycle.\n- Re-shoot with `prefers-reduced-motion: reduce` (global clamp — verify it\n  stills).\n- The title/status live-swap is a state replace (no transition of its own).\n"}, {"name": "money-input", "component": "MoneyInput", "spec": "# MoneyInput\n\nA hero currency field — Cash-App/Stripe-style type-and-replace amount entry with\na per-digit odometer roll, an aurora-mesh background, mouse-tracked 3D tilt,\nquick-amount chips, a log-scale magnitude bar, and a spelled-out value sub-line.\nUsed inside AsstChat's price-capture screen.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/MoneyInput.tsx`). **Assistant-local** — only\n  consumed by AsstChat.\n- **Interaction tier:** `island` (client-only, controlled input). No fetch.\n- **Client state owned:**\n  - `display: string` — the formatted typing string (e.g. `\"1,250.00\"`).\n  - `cents: number | null` — the parsed value in cents (the canonical output).\n  - `focused`, `ready` (gates the odometer roll transition until after first\n    paint), `stageWidth` (measured via `ResizeObserver` for hero-font\n    auto-shrink), `isTouchOnly` (swaps the keyboard-nudge hint for a touch hint).\n  - `lang` — `langSignal.value` (the `lang` prop is an ignored SSR seed); drives\n    the spelled-out value words + eyebrow/hints.\n- **Data source:** none — pure controlled input. Emits via callbacks:\n  `onChange(cents | null)` on every change; `onSubmit(cents)` on Enter (when\n  `cents > 0`).\n- **Liveness:** none.\n- **Honest-empty:** with no value the int reads \"0\" / dec \"00\" dimmed\n  (`is-empty`), the clear button is hidden, and the words line shows a hint\n  (keyboard vs touch).\n- **Anti-patterns:** none. No reload, no polling. Uses a deprecated-but-fine\n  pattern: a transparent `.mi__input` layered over the odometer captures keyboard\n  /paste/selection while the odometer renders the visible number (caret hidden,\n  type-and-replace UX).\n- **Data-shape hazards:**\n  - Value is **cents (integer)**, not dollars — the parent must treat the\n    callback arg as cents. `parseToCents` clamps decimals to 2 places.\n  - Hero font auto-shrinks (clamped 32–88px) based on digit count + measured\n    stage width so 11-digit amounts ($99,999,999.99) still fit; relies on the\n    `ResizeObserver` firing — verify in narrow mounts.\n\n## 2. Anatomy\n```\n<style>{STYLES}</style>                                   ← injected inline (see css/money-input.css)\n<div class=\"mi [is-focused] [has-value] [is-ready]\" style=\"--mi-hero:{heroSize}px\"\n     onMouseMove=tilt onMouseLeave=resetTilt onClick=focusInput>\n  <div class=\"mi__aurora\" aria-hidden/>                    ← blurred radial mesh\n  <div class=\"mi__shine\" aria-hidden/>                     ← mouse-tracked specular\n  <div class=\"mi__eyebrow\">{t eyebrow}</div>\n  <div class=\"mi__stage\">\n    <span class=\"mi__glyph\">$</span>\n    <div class=\"mi__amount\">\n      <Odometer class=\"mi__int\" text={intStr||\"0\"}/>        ← per-digit rolling columns\n      <Odometer class=\"mi__dec\" text={decStr||\"00\"}/>\n    </div>\n    <input class=\"mi__input\" inputMode=decimal/>            ← transparent, layered, captures input\n    <button class=\"mi__clear\">✕</button>                    ← fades in when has-value\n  </div>\n  <div class=\"mi__bar\"><div class=\"mi__bar-fill\" style=\"width:{magnitude%}\"/></div>   ← log-scale, ref $100M\n  <div class=\"mi__words\" key=cents>{words || hint}</div>    ← spelled-out, animates per change\n  <div class=\"mi__chips\" role=group>                        ← $50 / $100 / $500 / $1k / $5k presets\n    {CHIP_PRESETS_CENTS.map(c => <button class=\"mi__chip [is-active]\">${shortMoney(c)}</button>)}\n  </div>\n</div>\n```\n- **Odometer:** each digit cell is a 10-tall column translated `-n*10%`; commas/\n  dot are static cells. The roll transition (`460ms`) is gated on `.mi.is-ready`.\n- **Sub-components:** `Odometer` + `OdoCell` (local, in the same file).\n- **Helpers (local):** `parseToCents`, `formatTyping`, `shortMoney`,\n  `centsToWords` / `numToWords` (i18n number-to-words, en + es via `tFor`).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `initialCents` | number | `0` | number | no |\n| `onChange` | `(cents:number\\|null)=>void` | `undefined` | (callback → Events) | no |\n| `onSubmit` | `(cents:number)=>void` | `undefined` | (callback → Events) | no |\n| `autoFocus` | boolean | `undefined` | boolean | no |\n| `name` | string | `undefined` | text | no |\n| `lang` | `\"en\"\\|\"es\"` | (ignored) | select | **ignored SSR seed** — reads `langSignal.value` |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| empty | no value — dimmed 0/00 + hint, no clear, no active chip | `cases/empty/empty.json` |\n| value | a typed amount (`initialCents=125000` → \"$1,250\") | `cases/value/value.json` |\n| chip-active | a preset selected (`initialCents=100000` → $1k chip active) | `cases/chip-active/chip-active.json` |\n| large | 8-digit amount → hero font auto-shrinks | `cases/large/large.json` |\n| es | Spanish words line (\"mil doscientos cincuenta dólares\") | `cases/es/es.json` |\n\n> Isolate note: cases are bare props. `onChange`/`onSubmit` are callbacks →\n> assert in Events. The odometer roll only animates after `ready` (1 rAF) — for a\n> still capture it settles immediately.\n\n## 5. Events\n- `ev.expect(e => e.source === \"input.mi__input\" && e.type === \"input\")` →\n  reformats, updates `cents`, fires `onChange(cents)`.\n- `ev.expect(e => e.source === \"input.mi__input\" && e.type === \"keydown\" && e.key === \"Enter\")`\n  → `onSubmit(cents)` when `cents>0`.\n- `ev.expect(e => /ArrowUp|ArrowDown/.test(e.key))` → nudge ±$10 (±$100 with\n  Shift).\n- `ev.expect(e => e.source === \"button.mi__chip\")` → `setValue(presetCents)` →\n  `onChange`.\n- `ev.expect(e => e.source === \"button.mi__clear\")` → resets to null, refocuses.\n\n## 6. Motion (extracted, all in the inline STYLES)\n- **Odometer roll:** `.mi.is-ready .odo__col { transition: transform 460ms\n  cubic-bezier(.22,.85,.25,1) }` — digit columns slide to the new value.\n- **Magnitude bar:** `width` transition `540ms cubic-bezier(.22,.85,.25,1)`.\n- **Words line:** `@keyframes mi-words-in` (`opacity 0→1` + `translateY 4px→0`)\n  `320ms`, re-keyed per `cents` so it replays on each change.\n- **3D tilt:** `transform: perspective(1100px) rotateX/Y(--tilt-x/y)`,\n  `280ms`; `--tilt-*` set via JS on mousemove (±3deg), reset on leave.\n- **Glyph / clear / chips:** color/scale/shadow transitions (160–280ms).\n- **Reduced motion:** NO component-local guard — relies entirely on the GLOBAL\n  tokens reduced-motion clamp. Verify the odometer, tilt, bar and words all still\n  under reduced-motion (the JS tilt becomes a no-transition snap).\n\n## 7. Responsive\n- No `@media`. Self-adapts: `ResizeObserver` measures the card and recomputes\n  `stageWidth`, shrinking the hero font for narrow mounts; `isTouchOnly`\n  (`matchMedia(\"(hover:none) and (pointer:coarse)\")`) swaps the hint copy.\n\n## 8. A11y\n- `aria-label` on the input + clear button; chips in a labeled `role=\"group\"`;\n  the aurora/shine/glyph are `aria-hidden`.\n- **Gaps:** the visible value is the odometer (decorative spans) while the real\n  value lives in a transparent input — AT reads the input's `value`, which is OK,\n  but the caret is hidden (`caret-color:transparent`) which can disorient some\n  users. Keyboard nudge keys are undiscoverable to AT (only a visual hint).\n\n## 9. Used on\nInside `islands/AsstChat.tsx` only — the price-capture screen (one instance,\n`autoFocus` unless price-suggestions are showing; `onChange→setPriceCents`,\n`onSubmit→onPriceContinue`). Not mounted directly by any route. CSS is the\nisland's own inline `STYLES` (no assistant.css entry).\n", "fixture": "{\n  \"category\": \"assistant\",\n  \"folder\": \"money-input\",\n  \"controls\": {\n    \"initialCents\": { \"type\": \"number\", \"value\": 0 },\n    \"autoFocus\": { \"type\": \"boolean\", \"value\": false },\n    \"name\": { \"type\": \"text\", \"value\": \"price\" }\n  }\n}\n", "cases": [{"name": "chip-active.json", "content": "{\n  \"_name\": \"Chip active — $1k preset selected (cents match a CHIP_PRESET)\",\n  \"_note\": \"initialCents=100000 matches the $1k entry in CHIP_PRESETS_CENTS ([50,100,500,1k,5k]·100), so that chip renders is-active. Display '1,000.00'.\",\n  \"initialCents\": 100000\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — no value: dimmed 0/00, hint, no clear, no active chip\",\n  \"_note\": \"initialCents=0 → display empty, cents=null. Shows is-empty (dimmed 0 . 00), the keyboard/touch hint on the words line, no clear button, no active chip. onChange/onSubmit are callbacks → assert in Events.\",\n  \"initialCents\": 0\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Español — spelled-out words line in Spanish (langSignal=es)\",\n  \"_note\": \"MoneyInput reads langSignal.value (the lang prop is an ignored SSR seed). Flip the language signal via _signals so the words line + eyebrow/hints localize: $1,250.00 → 'mil doscientos cincuenta dólares'. Numeral formatting stays en-US grouping.\",\n  \"initialCents\": 125000,\n  \"_signals\": { \"lang\": \"es\" }\n}\n"}, {"name": "large.json", "content": "{\n  \"_name\": \"Large — 8-digit amount → hero font auto-shrinks\",\n  \"_note\": \"initialCents=1234567800 → display '12,345,678.00'. The hero font shrinks (clamped 32-88px) based on digit count + measured stage width via ResizeObserver so the long number still fits the card. Verify the shrink fires in the isolate mount (the RO must observe a sized stage).\",\n  \"initialCents\": 1234567800\n}\n"}, {"name": "value.json", "content": "{\n  \"_name\": \"Value — a typed amount ($1,250.00)\",\n  \"_note\": \"initialCents=125000 → display '1,250.00', cents=125000. has-value: odometer shows the number, clear button (✕) visible, words line reads 'one thousand two hundred fifty dollars', magnitude bar filled to the log-scale position. No preset chip matches 125000, so no chip is active.\",\n  \"initialCents\": 125000\n}\n"}], "css": "/* ============================================================\n   MoneyInput — extracted VERBATIM from the inline `STYLES` const in\n   islands/MoneyInput.tsx (the island injects this via a <style> tag; there\n   is NO entry in assistant.css / assistant-page.css). Self-contained: it\n   declares its own local --mi-* color vars (mirroring Sabor tokens) so it\n   renders standalone. One keyframe: mi-words-in.\n   ============================================================ */\n\n.mi {\n  --mi-teal: #0F3036;\n  --mi-teal-2: #1A535C;\n  --mi-green: #519843;\n  --mi-green-2: #7BB568;\n  --mi-pink: #FF6B6B;\n  --mi-line: rgba(26,83,92,0.10);\n  --mi-ink: #0F3036;\n  --mi-ink-mute: #6E8689;\n  --tilt-x: 0;\n  --tilt-y: 0;\n  --shine-x: 50%;\n  --shine-y: 50%;\n\n  position: relative;\n  isolation: isolate;\n  border-radius: 28px;\n  padding: 26px 28px 22px;\n  background: linear-gradient(180deg, #ffffff 0%, #fafaf5 100%);\n  border: 1px solid var(--mi-line);\n  box-shadow:\n    0 1px 0 rgba(255,255,255,0.9) inset,\n    0 24px 60px -20px rgba(15,48,54,0.18),\n    0 6px 18px -6px rgba(15,48,54,0.10);\n  transform: perspective(1100px) rotateX(calc(var(--tilt-x) * 1deg)) rotateY(calc(var(--tilt-y) * 1deg));\n  transition: transform 280ms cubic-bezier(.2,.7,.2,1), box-shadow 280ms;\n  font-family: \"Inter\", system-ui, -apple-system, sans-serif;\n  color: var(--mi-ink);\n  cursor: text;\n  overflow: hidden;\n}\n.mi.is-focused {\n  box-shadow:\n    0 1px 0 rgba(255,255,255,0.9) inset,\n    0 0 0 3px rgba(81,152,67,0.18),\n    0 30px 70px -20px rgba(15,48,54,0.22),\n    0 8px 22px -6px rgba(15,48,54,0.12);\n}\n\n/* aurora mesh in background */\n.mi__aurora {\n  position: absolute;\n  inset: -20%;\n  z-index: 0;\n  background:\n    radial-gradient(40% 40% at 18% 22%, rgba(81,152,67,0.18), transparent 70%),\n    radial-gradient(35% 35% at 82% 18%, rgba(255,107,107,0.10), transparent 70%),\n    radial-gradient(45% 45% at 70% 90%, rgba(26,83,92,0.13), transparent 70%);\n  filter: blur(28px) saturate(115%);\n  opacity: 0.55;\n  transition: opacity 320ms;\n  pointer-events: none;\n}\n.mi.is-focused .mi__aurora { opacity: 0.95; }\n\n/* mouse-tracked specular shine */\n.mi__shine {\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(220px 160px at var(--shine-x) var(--shine-y),\n    rgba(255,255,255,0.7) 0%, rgba(255,255,255,0) 65%);\n  mix-blend-mode: overlay;\n  opacity: 0;\n  transition: opacity 220ms;\n}\n.mi:hover .mi__shine,\n.mi.is-focused .mi__shine { opacity: 0.9; }\n\n.mi__eyebrow {\n  position: relative;\n  z-index: 1;\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: .18em;\n  text-transform: uppercase;\n  color: var(--mi-ink-mute);\n  margin-bottom: 6px;\n}\n\n.mi__stage {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  align-items: flex-end;\n  gap: 8px;\n  padding: 4px 0 14px;\n}\n.mi__glyph {\n  font-size: calc(var(--mi-hero, 88px) * 0.55);\n  font-weight: 700;\n  color: var(--mi-ink-mute);\n  line-height: 1;\n  letter-spacing: -0.02em;\n  margin-bottom: 6px;\n  transition: color 260ms cubic-bezier(.2,.7,.2,1), transform 260ms, font-size 260ms;\n  transform-origin: 50% 80%;\n}\n.mi.is-focused .mi__glyph,\n.mi.has-value .mi__glyph {\n  color: var(--mi-green);\n  transform: translateY(-2px) scale(1.04);\n  text-shadow: 0 8px 28px rgba(81,152,67,0.25);\n}\n\n.mi__amount {\n  flex: 1;\n  display: flex;\n  align-items: flex-end;\n  font-variant-numeric: tabular-nums;\n  font-feature-settings: \"tnum\" 1, \"lnum\" 1;\n  letter-spacing: -0.035em;\n  line-height: 1;\n  min-height: var(--mi-hero, 88px);\n  white-space: nowrap;\n  overflow: visible;\n  padding-bottom: 4px;\n  margin-right: 12px;\n}\n.mi__int {\n  font-size: var(--mi-hero, 88px);\n  font-weight: 800;\n  color: var(--mi-teal);\n  transition: color 260ms, opacity 220ms, font-size 260ms;\n}\n.mi__dec {\n  font-size: calc(var(--mi-hero, 88px) * 0.36);\n  font-weight: 700;\n  color: rgba(15,48,54,0.32);\n  margin-left: 4px;\n  letter-spacing: -0.02em;\n  align-self: flex-start;\n  transition: color 260ms, font-size 260ms;\n}\n.mi.has-value .mi__dec { color: rgba(26,83,92,0.55); }\n.mi__int.is-empty,\n.mi__dec.is-empty { color: rgba(15,48,54,0.18); text-shadow: none; }\n\n/* odometer cells */\n.odo {\n  display: inline-flex;\n  align-items: baseline;\n}\n.odo__cell {\n  position: relative;\n  display: inline-block;\n  width: 1ch;\n  height: 1em;\n  overflow: hidden;\n  vertical-align: baseline;\n}\n.odo__col {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  display: flex;\n  flex-direction: column;\n}\n.mi.is-ready .odo__col {\n  transition: transform 460ms cubic-bezier(.22,.85,.25,1);\n}\n.odo__digit {\n  display: block;\n  height: 1em;\n  line-height: 1;\n  text-align: right;\n}\n.odo__static { display: inline-block; }\n.odo__static--comma { width: 0.32ch; }\n.odo__static--dot { width: 0.32ch; }\n\n/* hidden but focusable input layered behind */\n.mi__input {\n  position: absolute;\n  inset: 0;\n  z-index: 2;\n  width: 100%;\n  height: 100%;\n  border: 0;\n  outline: 0;\n  background: transparent;\n  color: transparent;\n  caret-color: transparent;\n  font: inherit;\n  font-size: 16px;\n  padding: 0;\n  margin: 0;\n  cursor: text;\n}\n.mi__input::selection { background: transparent; }\n\n.mi__clear {\n  position: relative;\n  z-index: 3;\n  appearance: none;\n  border: 0;\n  background: rgba(15,48,54,0.06);\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 30px;\n  height: 30px;\n  border-radius: 50%;\n  color: var(--mi-ink-mute);\n  cursor: pointer;\n  align-self: flex-start;\n  margin-top: 4px;\n  opacity: 0;\n  transform: scale(0.7);\n  transition:\n    opacity 220ms cubic-bezier(.2,.7,.2,1),\n    transform 220ms cubic-bezier(.2,.7,.2,1),\n    background 160ms, color 160ms;\n}\n.mi.has-value .mi__clear { opacity: 1; transform: scale(1); }\n.mi__clear:hover { background: rgba(255,107,107,0.14); color: var(--mi-pink); }\n.mi__clear:focus-visible { outline: 2px solid rgba(81,152,67,0.45); outline-offset: 2px; }\n\n/* magnitude bar */\n.mi__bar {\n  position: relative;\n  z-index: 1;\n  height: 4px;\n  border-radius: 999px;\n  background: rgba(15,48,54,0.07);\n  overflow: hidden;\n}\n.mi__bar-fill {\n  height: 100%;\n  border-radius: 999px;\n  background: linear-gradient(90deg, var(--mi-green) 0%, var(--mi-green-2) 60%, #ffd166 100%);\n  box-shadow: 0 0 12px rgba(81,152,67,0.45);\n  transition: width 540ms cubic-bezier(.22,.85,.25,1);\n}\n\n/* spelled-out value */\n@keyframes mi-words-in {\n  from { opacity: 0; transform: translateY(4px); }\n  to   { opacity: 1; transform: translateY(0); }\n}\n.mi__words {\n  position: relative;\n  z-index: 1;\n  margin-top: 14px;\n  font-size: 12.5px;\n  font-weight: 500;\n  letter-spacing: .005em;\n  color: var(--mi-ink-mute);\n  line-height: 1.4;\n  min-height: 18px;\n  animation: mi-words-in 320ms cubic-bezier(.22,.85,.25,1);\n}\n.mi.has-value .mi__words {\n  color: var(--mi-teal-2);\n  font-weight: 600;\n}\n.mi__words::first-letter { text-transform: capitalize; }\n\n/* quick chips */\n.mi__chips {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  margin-top: 16px;\n}\n.mi__chip {\n  appearance: none;\n  border: 1px solid rgba(15,48,54,0.12);\n  background: rgba(255,255,255,0.7);\n  backdrop-filter: blur(6px);\n  color: var(--mi-teal-2);\n  font: inherit;\n  font-weight: 700;\n  font-size: 13px;\n  letter-spacing: -0.005em;\n  padding: 7px 13px;\n  border-radius: 999px;\n  cursor: pointer;\n  transition: transform 160ms cubic-bezier(.2,.7,.2,1), background 160ms, border-color 160ms, color 160ms, box-shadow 160ms;\n}\n.mi__chip:hover {\n  background: #fff;\n  border-color: rgba(81,152,67,0.45);\n  color: var(--mi-green);\n  transform: translateY(-1px);\n  box-shadow: 0 4px 12px -4px rgba(81,152,67,0.35);\n}\n.mi__chip:active { transform: translateY(0); }\n.mi__chip.is-active {\n  background: var(--mi-green);\n  border-color: var(--mi-green);\n  color: #fff;\n  box-shadow: 0 6px 16px -6px rgba(81,152,67,0.6);\n}\n\n/* NOTE: there is NO component-local prefers-reduced-motion guard in source.\n   The odometer roll, tilt, aurora, bar fill and words fade rely on the GLOBAL\n   tokens reduced-motion clamp (animation/transition-duration → 0.01ms). The\n   3D mouse-tilt is driven by JS setProperty on --tilt-x/--tilt-y (pointer\n   only); verify it sits flat under reduced-motion via the transition clamp. */\n", "cssName": "money-input.css", "source": "/**\n * MoneyInput — hero currency field.\n *\n *   • Hero typography: 88px integer · 44px cents (Stripe-style demphasis)\n *   • Per-digit odometer roll on every value change (CSS-only, no rAF)\n *   • Aurora mesh background that subtly responds to focus\n *   • Mouse-tracked 3D tilt for a premium tactile feel\n *   • Quick-amount chips that animate the number rolling to a target\n *   • Magnitude bar (log scale, anchored at $10k) under the input\n *   • Caret hidden — type-and-replace UX (à la Cash App / Stripe)\n *   • Spelled-out value sub-line, animates in on each change\n *\n * The visible \"amount\" is rendered by the odometer; a transparent input\n * underneath captures keyboard, paste, and selection. The two share the\n * same formatted string so widths line up via tabular-nums.\n */\nimport { useEffect, useRef, useState } from \"preact/hooks\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface MoneyInputProps {\n  initialCents?: number;\n  onChange?: (cents: number | null) => void;\n  onSubmit?: (cents: number) => void;\n  autoFocus?: boolean;\n  name?: string;\n  /** Optional SSR seed only; the live language is self-sourced from langSignal. */\n  lang?: Lang;\n}\n\nconst CHIP_PRESETS_CENTS = [50_00, 100_00, 500_00, 1_000_00, 5_000_00];\nconst MAGNITUDE_REF_CENTS = 100_000_000_00; // bar fills at $100M (log scale)\n\nexport default function MoneyInput(\n  { initialCents = 0, onChange, onSubmit, autoFocus, name }: MoneyInputProps,\n) {\n  // Self-source the live UI language; reading .value during render makes this\n  // island re-render whenever the language flips (SettingsPage).\n  const lang = langSignal.value;\n  const inputRef = useRef<HTMLInputElement>(null);\n  const cardRef = useRef<HTMLDivElement>(null);\n\n  const [display, setDisplay] = useState<string>(\n    initialCents > 0 ? formatTyping((initialCents / 100).toString()) : \"\",\n  );\n  const [cents, setCents] = useState<number | null>(\n    initialCents > 0 ? initialCents : null,\n  );\n  const [focused, setFocused] = useState(false);\n  const [ready, setReady] = useState(false);\n  const [stageWidth, setStageWidth] = useState(380);\n  // Touch-only devices have no arrow keys / Shift, so the keyboard-nudge hint\n  // is noise there — swap it for a touch-appropriate one.\n  const [isTouchOnly, setIsTouchOnly] = useState(false);\n\n  useEffect(() => {\n    const id = requestAnimationFrame(() => setReady(true));\n    return () => cancelAnimationFrame(id);\n  }, []);\n\n  useEffect(() => {\n    if (typeof globalThis.matchMedia !== \"function\") return;\n    const mq = globalThis.matchMedia(\"(hover: none) and (pointer: coarse)\");\n    const apply = () => setIsTouchOnly(mq.matches);\n    apply();\n    mq.addEventListener(\"change\", apply);\n    return () => mq.removeEventListener(\"change\", apply);\n  }, []);\n\n  useEffect(() => {\n    if (!autoFocus) return;\n    const id = requestAnimationFrame(() => inputRef.current?.focus());\n    return () => cancelAnimationFrame(id);\n  }, [autoFocus]);\n\n  // Measure available stage width so the auto-shrink formula adapts to\n  // wherever the card is mounted (story shell, chat panel, sidebar…).\n  useEffect(() => {\n    const card = cardRef.current;\n    if (!card) return;\n    const update = () => {\n      const w = card.clientWidth;\n      // Card padding (28px each side) + $ glyph (~heroSize*0.55) + gaps\n      // ≈ ~110px chrome. Subtract for amount stage budget.\n      setStageWidth(Math.max(180, w - 110));\n    };\n    update();\n    const ro = new ResizeObserver(update);\n    ro.observe(card);\n    return () => ro.disconnect();\n  }, []);\n\n  useEffect(() => {\n    onChange?.(cents);\n  }, [cents]);\n\n  function setValue(c: number | null) {\n    if (c === null || c === 0) {\n      setDisplay(\"\");\n      setCents(null);\n      return;\n    }\n    const asString = (c / 100).toFixed(2).replace(/\\.00$/, \"\");\n    setDisplay(formatTyping(asString));\n    setCents(c);\n  }\n\n  function handleInput(e: Event) {\n    const el = e.currentTarget as HTMLInputElement;\n    const oldVal = el.value;\n    const oldCaret = el.selectionStart ?? oldVal.length;\n    const tokensBeforeCaret = oldVal\n      .slice(0, oldCaret)\n      .replace(/[^\\d.]/g, \"\")\n      .length;\n\n    const formatted = formatTyping(oldVal);\n    setDisplay(formatted);\n    setCents(parseToCents(formatted));\n\n    queueMicrotask(() => {\n      const node = inputRef.current;\n      if (!node) return;\n      let pos = 0;\n      let counted = 0;\n      for (let i = 0; i < formatted.length; i++) {\n        if (counted >= tokensBeforeCaret) break;\n        if (/[\\d.]/.test(formatted[i])) counted++;\n        pos = i + 1;\n      }\n      node.setSelectionRange(pos, pos);\n    });\n  }\n\n  function handleKeyDown(e: KeyboardEvent) {\n    if (e.key === \"ArrowUp\" || e.key === \"ArrowDown\") {\n      e.preventDefault();\n      const step = e.shiftKey ? 100_00 : 10_00;\n      const dir = e.key === \"ArrowUp\" ? 1 : -1;\n      const next = Math.max(0, (cents ?? 0) + step * dir);\n      setValue(next === 0 ? null : next);\n      return;\n    }\n    if (e.key === \"Enter\" && onSubmit && (cents ?? 0) > 0) {\n      e.preventDefault();\n      onSubmit(cents!);\n    }\n  }\n\n  function handleMouseMove(e: MouseEvent) {\n    const card = cardRef.current;\n    if (!card) return;\n    const r = card.getBoundingClientRect();\n    const x = (e.clientX - r.left) / r.width - 0.5; // -0.5 .. 0.5\n    const y = (e.clientY - r.top) / r.height - 0.5;\n    card.style.setProperty(\"--tilt-x\", String(-y * 3));\n    card.style.setProperty(\"--tilt-y\", String(x * 3));\n    card.style.setProperty(\"--shine-x\", `${(x + 0.5) * 100}%`);\n    card.style.setProperty(\"--shine-y\", `${(y + 0.5) * 100}%`);\n  }\n  function handleMouseLeave() {\n    const card = cardRef.current;\n    if (!card) return;\n    card.style.setProperty(\"--tilt-x\", \"0\");\n    card.style.setProperty(\"--tilt-y\", \"0\");\n  }\n\n  function clear() {\n    setValue(null);\n    inputRef.current?.focus();\n  }\n\n  const hasValue = (cents ?? 0) > 0;\n  const words = hasValue ? centsToWords(cents!, lang) : \"\";\n\n  // Auto-shrink hero font so big numbers always fit. Counts the rendered\n  // chars (digits + commas + \".00\" tail + glyph buffer) and divides the\n  // available stage width to get a per-char budget. Clamped 32..88px so\n  // tiny numbers stay hero and 11-digit numbers ($99,999,999.99) still\n  // clear with a safe gutter.\n  const intDigitCount =\n    ((display.split(\".\")[0]) || \"0\").replace(/[^\\d]/g, \"\").length;\n  const totalChars = intDigitCount +\n    Math.max(0, Math.floor((intDigitCount - 1) / 3)) + // commas\n    3 + // \".00\"\n    2; // safety gutter\n  const PX_PER_CH = 0.58;\n  const heroSize = Math.max(\n    32,\n    Math.min(88, Math.floor(stageWidth / (totalChars * PX_PER_CH))),\n  );\n\n  // Magnitude bar fill: log-scaled so $50 reads, $5k still has headroom\n  const magnitude = (cents ?? 0) === 0 ? 0 : Math.min(\n    1,\n    Math.log10(1 + (cents ?? 0)) / Math.log10(1 + MAGNITUDE_REF_CENTS),\n  );\n\n  // Split display into integer / decimal halves so the cents render smaller\n  const dotIdx = display.indexOf(\".\");\n  const intStr = dotIdx === -1 ? display : display.slice(0, dotIdx);\n  const decStr = dotIdx === -1 ? \"\" : display.slice(dotIdx + 1); // bare cents, no dot\n\n  return (\n    <>\n      <style\n        // Static component stylesheet (no user input).\n        // deno-lint-ignore react-no-danger\n        dangerouslySetInnerHTML={{ __html: STYLES }}\n      />\n      <div\n        ref={cardRef}\n        class={`mi ${focused ? \"is-focused\" : \"\"} ${\n          hasValue ? \"has-value\" : \"\"\n        } ${ready ? \"is-ready\" : \"\"}`}\n        style={`--mi-hero:${heroSize}px`}\n        onMouseMove={handleMouseMove}\n        onMouseLeave={handleMouseLeave}\n        onClick={() => inputRef.current?.focus()}\n      >\n        <div class=\"mi__aurora\" aria-hidden=\"true\" />\n        <div class=\"mi__shine\" aria-hidden=\"true\" />\n\n        <div class=\"mi__eyebrow\">{tFor(lang, \"moneyInput.eyebrow\")}</div>\n\n        <div class=\"mi__stage\">\n          <span class=\"mi__glyph\" aria-hidden=\"true\">$</span>\n          <div class=\"mi__amount\">\n            <Odometer\n              text={intStr || \"0\"}\n              className=\"mi__int\"\n              empty={!hasValue}\n            />\n            <Odometer\n              text={decStr || \"00\"}\n              className=\"mi__dec\"\n              empty={!hasValue}\n            />\n          </div>\n          <input\n            ref={inputRef}\n            type=\"text\"\n            inputMode=\"decimal\"\n            autoComplete=\"off\"\n            spellcheck={false}\n            name={name}\n            class=\"mi__input\"\n            value={display}\n            onInput={handleInput}\n            onKeyDown={handleKeyDown}\n            onFocus={() => setFocused(true)}\n            onBlur={() => setFocused(false)}\n            aria-label={tFor(lang, \"moneyInput.amountAriaLabel\")}\n          />\n          <button\n            type=\"button\"\n            class=\"mi__clear\"\n            aria-label={tFor(lang, \"moneyInput.clearAriaLabel\")}\n            onClick={(e) => {\n              e.stopPropagation();\n              clear();\n            }}\n            tabIndex={hasValue ? 0 : -1}\n          >\n            <svg viewBox=\"0 0 16 16\" width=\"14\" height=\"14\" aria-hidden=\"true\">\n              <path\n                d=\"M3 3l10 10M13 3L3 13\"\n                stroke=\"currentColor\"\n                stroke-width=\"2.2\"\n                stroke-linecap=\"round\"\n              />\n            </svg>\n          </button>\n        </div>\n\n        <div class=\"mi__bar\" aria-hidden=\"true\">\n          <div\n            class=\"mi__bar-fill\"\n            style={`width:${(magnitude * 100).toFixed(2)}%`}\n          />\n        </div>\n\n        <div class=\"mi__words\" key={`w-${cents ?? 0}`}>\n          {hasValue\n            ? words\n            : isTouchOnly\n            ? tFor(lang, \"moneyInput.hintTouch\")\n            : tFor(lang, \"moneyInput.hintKeyboard\")}\n        </div>\n\n        <div\n          class=\"mi__chips\"\n          role=\"group\"\n          aria-label={tFor(lang, \"moneyInput.quickAmountsAriaLabel\")}\n        >\n          {CHIP_PRESETS_CENTS.map((c) => (\n            <button\n              key={`chip-${c}`}\n              type=\"button\"\n              class={`mi__chip ${cents === c ? \"is-active\" : \"\"}`}\n              onClick={(e) => {\n                e.stopPropagation();\n                setValue(c);\n              }}\n            >\n              ${shortMoney(c)}\n            </button>\n          ))}\n        </div>\n      </div>\n    </>\n  );\n}\n\n/* --------------------------------------------------------------------- */\n/* Odometer — renders a string char-by-char; digits roll on change.      */\n/* --------------------------------------------------------------------- */\nfunction Odometer(\n  { text, className, empty }: {\n    text: string;\n    className?: string;\n    empty?: boolean;\n  },\n) {\n  return (\n    <span class={`odo ${className ?? \"\"} ${empty ? \"is-empty\" : \"\"}`}>\n      {Array.from(text).map((ch, i) => <OdoCell key={`${i}-${ch}`} ch={ch} />)}\n    </span>\n  );\n}\n\nfunction OdoCell({ ch }: { ch: string }) {\n  if (/\\d/.test(ch)) {\n    const n = parseInt(ch, 10);\n    return (\n      <span class=\"odo__cell\">\n        <span\n          class=\"odo__col\"\n          style={`transform: translateY(${-n * 10}%)`}\n        >\n          {Array.from(\n            { length: 10 },\n            (_, i) => <span key={`d-${i}`} class=\"odo__digit\">{i}</span>,\n          )}\n        </span>\n      </span>\n    );\n  }\n  return (\n    <span\n      class={`odo__static odo__static--${\n        ch === \",\" ? \"comma\" : ch === \".\" ? \"dot\" : \"other\"\n      }`}\n    >\n      {ch}\n    </span>\n  );\n}\n\n/* --------------------------------------------------------------------- */\n/* Helpers                                                                */\n/* --------------------------------------------------------------------- */\n\nfunction parseToCents(raw: string): number | null {\n  const cleaned = raw.replace(/[^\\d.]/g, \"\");\n  if (cleaned === \"\" || cleaned === \".\") return null;\n  const firstDot = cleaned.indexOf(\".\");\n  let intPart = firstDot === -1 ? cleaned : cleaned.slice(0, firstDot);\n  let decPart = firstDot === -1\n    ? \"\"\n    : cleaned.slice(firstDot + 1).replace(/\\./g, \"\");\n  if (intPart === \"\") intPart = \"0\";\n  decPart = (decPart + \"00\").slice(0, 2);\n  const cents = parseInt(intPart, 10) * 100 + parseInt(decPart, 10);\n  return Number.isFinite(cents) ? cents : null;\n}\n\nfunction formatTyping(raw: string): string {\n  let s = raw.replace(/[^\\d.]/g, \"\");\n  const firstDot = s.indexOf(\".\");\n  if (firstDot !== -1) {\n    s = s.slice(0, firstDot + 1) + s.slice(firstDot + 1).replace(/\\./g, \"\");\n  }\n  if (firstDot !== -1) {\n    const dec = s.slice(firstDot + 1, firstDot + 3);\n    s = s.slice(0, firstDot + 1) + dec;\n  }\n  const dot2 = s.indexOf(\".\");\n  let intPart = dot2 === -1 ? s : s.slice(0, dot2);\n  const decPart = dot2 === -1 ? \"\" : s.slice(dot2);\n  if (intPart.length > 1) intPart = intPart.replace(/^0+/, \"\") || \"0\";\n  const intFmt = intPart === \"\" ? \"\" : Number(intPart).toLocaleString(\"en-US\");\n  return intFmt + decPart;\n}\n\nfunction shortMoney(cents: number): string {\n  const d = cents / 100;\n  if (d >= 1000) return (d / 1000) + \"k\";\n  return String(d);\n}\n\nfunction centsToWords(cents: number, lang: Lang): string {\n  if (!Number.isFinite(cents) || cents < 0) return \"\";\n  if (cents === 0) return tFor(lang, \"moneyInput.words.zeroDollars\");\n  const dollars = Math.floor(cents / 100);\n  const c = cents % 100;\n  const dStr = numToWords(dollars, lang);\n  const dLabel = tFor(\n    lang,\n    dollars === 1 ? \"moneyInput.words.dollar\" : \"moneyInput.words.dollars\",\n  );\n  if (c === 0) return `${dStr} ${dLabel}`;\n  const cStr = numToWords(c, lang);\n  const cLabel = tFor(\n    lang,\n    c === 1 ? \"moneyInput.words.cent\" : \"moneyInput.words.cents\",\n  );\n  return `${dStr} ${dLabel} ${tFor(lang, \"moneyInput.words.and\")} ${cStr} ${cLabel}`;\n}\nfunction ones(lang: Lang, i: number): string {\n  return tFor(lang, `moneyInput.ones.${i}`);\n}\nfunction tens(lang: Lang, i: number): string {\n  return tFor(lang, `moneyInput.tens.${i}`);\n}\nfunction numToWords(n: number, lang: Lang): string {\n  if (n < 0 || !Number.isFinite(n)) return \"\";\n  if (n < 20) return ones(lang, n);\n  if (n < 100) {\n    const t = Math.floor(n / 10);\n    const o = n % 10;\n    return o === 0 ? tens(lang, t) : `${tens(lang, t)}-${ones(lang, o)}`;\n  }\n  if (n < 1000) {\n    const h = Math.floor(n / 100);\n    const r = n % 100;\n    return r === 0\n      ? `${ones(lang, h)} ${tFor(lang, \"moneyInput.words.hundred\")}`\n      : `${ones(lang, h)} ${tFor(lang, \"moneyInput.words.hundred\")} ${\n        numToWords(r, lang)\n      }`;\n  }\n  if (n < 1_000_000) {\n    const k = Math.floor(n / 1000);\n    const r = n % 1000;\n    return r === 0\n      ? `${numToWords(k, lang)} ${tFor(lang, \"moneyInput.words.thousand\")}`\n      : `${numToWords(k, lang)} ${tFor(lang, \"moneyInput.words.thousand\")} ${\n        numToWords(r, lang)\n      }`;\n  }\n  const m = Math.floor(n / 1_000_000);\n  const r = n % 1_000_000;\n  return r === 0\n    ? `${numToWords(m, lang)} ${tFor(lang, \"moneyInput.words.million\")}`\n    : `${numToWords(m, lang)} ${tFor(lang, \"moneyInput.words.million\")} ${\n      numToWords(r, lang)\n    }`;\n}\n\n/* --------------------------------------------------------------------- */\n/* Styles                                                                 */\n/* --------------------------------------------------------------------- */\n\nconst STYLES = `\n.mi {\n  --mi-teal: #0F3036;\n  --mi-teal-2: #1A535C;\n  --mi-green: #519843;\n  --mi-green-2: #7BB568;\n  --mi-pink: #FF6B6B;\n  --mi-line: rgba(26,83,92,0.10);\n  --mi-ink: #0F3036;\n  --mi-ink-mute: #6E8689;\n  --tilt-x: 0;\n  --tilt-y: 0;\n  --shine-x: 50%;\n  --shine-y: 50%;\n\n  position: relative;\n  isolation: isolate;\n  border-radius: 28px;\n  padding: 26px 28px 22px;\n  background: linear-gradient(180deg, #ffffff 0%, #fafaf5 100%);\n  border: 1px solid var(--mi-line);\n  box-shadow:\n    0 1px 0 rgba(255,255,255,0.9) inset,\n    0 24px 60px -20px rgba(15,48,54,0.18),\n    0 6px 18px -6px rgba(15,48,54,0.10);\n  transform: perspective(1100px) rotateX(calc(var(--tilt-x) * 1deg)) rotateY(calc(var(--tilt-y) * 1deg));\n  transition: transform 280ms cubic-bezier(.2,.7,.2,1), box-shadow 280ms;\n  font-family: \"Inter\", system-ui, -apple-system, sans-serif;\n  color: var(--mi-ink);\n  cursor: text;\n  overflow: hidden;\n}\n.mi.is-focused {\n  box-shadow:\n    0 1px 0 rgba(255,255,255,0.9) inset,\n    0 0 0 3px rgba(81,152,67,0.18),\n    0 30px 70px -20px rgba(15,48,54,0.22),\n    0 8px 22px -6px rgba(15,48,54,0.12);\n}\n\n/* aurora mesh in background */\n.mi__aurora {\n  position: absolute;\n  inset: -20%;\n  z-index: 0;\n  background:\n    radial-gradient(40% 40% at 18% 22%, rgba(81,152,67,0.18), transparent 70%),\n    radial-gradient(35% 35% at 82% 18%, rgba(255,107,107,0.10), transparent 70%),\n    radial-gradient(45% 45% at 70% 90%, rgba(26,83,92,0.13), transparent 70%);\n  filter: blur(28px) saturate(115%);\n  opacity: 0.55;\n  transition: opacity 320ms;\n  pointer-events: none;\n}\n.mi.is-focused .mi__aurora { opacity: 0.95; }\n\n/* mouse-tracked specular shine */\n.mi__shine {\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(220px 160px at var(--shine-x) var(--shine-y),\n    rgba(255,255,255,0.7) 0%, rgba(255,255,255,0) 65%);\n  mix-blend-mode: overlay;\n  opacity: 0;\n  transition: opacity 220ms;\n}\n.mi:hover .mi__shine,\n.mi.is-focused .mi__shine { opacity: 0.9; }\n\n.mi__eyebrow {\n  position: relative;\n  z-index: 1;\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: .18em;\n  text-transform: uppercase;\n  color: var(--mi-ink-mute);\n  margin-bottom: 6px;\n}\n\n.mi__stage {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  align-items: flex-end;\n  gap: 8px;\n  padding: 4px 0 14px;\n}\n.mi__glyph {\n  font-size: calc(var(--mi-hero, 88px) * 0.55);\n  font-weight: 700;\n  color: var(--mi-ink-mute);\n  line-height: 1;\n  letter-spacing: -0.02em;\n  margin-bottom: 6px;\n  transition: color 260ms cubic-bezier(.2,.7,.2,1), transform 260ms, font-size 260ms;\n  transform-origin: 50% 80%;\n}\n.mi.is-focused .mi__glyph,\n.mi.has-value .mi__glyph {\n  color: var(--mi-green);\n  transform: translateY(-2px) scale(1.04);\n  text-shadow: 0 8px 28px rgba(81,152,67,0.25);\n}\n\n.mi__amount {\n  flex: 1;\n  display: flex;\n  align-items: flex-end;\n  font-variant-numeric: tabular-nums;\n  font-feature-settings: \"tnum\" 1, \"lnum\" 1;\n  letter-spacing: -0.035em;\n  line-height: 1;\n  min-height: var(--mi-hero, 88px);\n  white-space: nowrap;\n  overflow: visible;\n  padding-bottom: 4px;\n  /* Reserve room for the clear (✕) button so the cents (.mi__dec) never\n     crowd or overlap it at larger amounts. */\n  margin-right: 12px;\n}\n.mi__int {\n  font-size: var(--mi-hero, 88px);\n  font-weight: 800;\n  color: var(--mi-teal);\n  transition: color 260ms, opacity 220ms, font-size 260ms;\n}\n.mi__dec {\n  font-size: calc(var(--mi-hero, 88px) * 0.36);\n  font-weight: 700;\n  color: rgba(15,48,54,0.32);\n  margin-left: 4px;\n  letter-spacing: -0.02em;\n  align-self: flex-start;\n  transition: color 260ms, font-size 260ms;\n}\n.mi.has-value .mi__dec { color: rgba(26,83,92,0.55); }\n.mi__int.is-empty,\n.mi__dec.is-empty { color: rgba(15,48,54,0.18); text-shadow: none; }\n\n/* odometer cells */\n.odo {\n  display: inline-flex;\n  align-items: baseline;\n}\n.odo__cell {\n  position: relative;\n  display: inline-block;\n  width: 1ch;\n  height: 1em;\n  overflow: hidden;\n  vertical-align: baseline;\n}\n.odo__col {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  display: flex;\n  flex-direction: column;\n}\n.mi.is-ready .odo__col {\n  transition: transform 460ms cubic-bezier(.22,.85,.25,1);\n}\n.odo__digit {\n  display: block;\n  height: 1em;\n  line-height: 1;\n  text-align: right;\n}\n.odo__static { display: inline-block; }\n.odo__static--comma { width: 0.32ch; }\n.odo__static--dot { width: 0.32ch; }\n\n/* hidden but focusable input layered behind */\n.mi__input {\n  position: absolute;\n  inset: 0;\n  z-index: 2;\n  width: 100%;\n  height: 100%;\n  border: 0;\n  outline: 0;\n  background: transparent;\n  color: transparent;\n  caret-color: transparent;\n  font: inherit;\n  font-size: 16px;\n  padding: 0;\n  margin: 0;\n  cursor: text;\n}\n.mi__input::selection { background: transparent; }\n\n.mi__clear {\n  position: relative;\n  z-index: 3;\n  appearance: none;\n  border: 0;\n  background: rgba(15,48,54,0.06);\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 30px;\n  height: 30px;\n  border-radius: 50%;\n  color: var(--mi-ink-mute);\n  cursor: pointer;\n  align-self: flex-start;\n  margin-top: 4px;\n  opacity: 0;\n  transform: scale(0.7);\n  transition:\n    opacity 220ms cubic-bezier(.2,.7,.2,1),\n    transform 220ms cubic-bezier(.2,.7,.2,1),\n    background 160ms, color 160ms;\n}\n.mi.has-value .mi__clear { opacity: 1; transform: scale(1); }\n.mi__clear:hover { background: rgba(255,107,107,0.14); color: var(--mi-pink); }\n.mi__clear:focus-visible { outline: 2px solid rgba(81,152,67,0.45); outline-offset: 2px; }\n\n/* magnitude bar */\n.mi__bar {\n  position: relative;\n  z-index: 1;\n  height: 4px;\n  border-radius: 999px;\n  background: rgba(15,48,54,0.07);\n  overflow: hidden;\n}\n.mi__bar-fill {\n  height: 100%;\n  border-radius: 999px;\n  background: linear-gradient(90deg, var(--mi-green) 0%, var(--mi-green-2) 60%, #ffd166 100%);\n  box-shadow: 0 0 12px rgba(81,152,67,0.45);\n  transition: width 540ms cubic-bezier(.22,.85,.25,1);\n}\n\n/* spelled-out value */\n@keyframes mi-words-in {\n  from { opacity: 0; transform: translateY(4px); }\n  to   { opacity: 1; transform: translateY(0); }\n}\n.mi__words {\n  position: relative;\n  z-index: 1;\n  margin-top: 14px;\n  font-size: 12.5px;\n  font-weight: 500;\n  letter-spacing: .005em;\n  color: var(--mi-ink-mute);\n  line-height: 1.4;\n  min-height: 18px;\n  animation: mi-words-in 320ms cubic-bezier(.22,.85,.25,1);\n}\n.mi.has-value .mi__words {\n  color: var(--mi-teal-2);\n  font-weight: 600;\n}\n.mi__words::first-letter { text-transform: capitalize; }\n\n/* quick chips */\n.mi__chips {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  margin-top: 16px;\n}\n.mi__chip {\n  appearance: none;\n  border: 1px solid rgba(15,48,54,0.12);\n  background: rgba(255,255,255,0.7);\n  backdrop-filter: blur(6px);\n  color: var(--mi-teal-2);\n  font: inherit;\n  font-weight: 700;\n  font-size: 13px;\n  letter-spacing: -0.005em;\n  padding: 7px 13px;\n  border-radius: 999px;\n  cursor: pointer;\n  transition: transform 160ms cubic-bezier(.2,.7,.2,1), background 160ms, border-color 160ms, color 160ms, box-shadow 160ms;\n}\n.mi__chip:hover {\n  background: #fff;\n  border-color: rgba(81,152,67,0.45);\n  color: var(--mi-green);\n  transform: translateY(-1px);\n  box-shadow: 0 4px 12px -4px rgba(81,152,67,0.35);\n}\n.mi__chip:active { transform: translateY(0); }\n.mi__chip.is-active {\n  background: var(--mi-green);\n  border-color: var(--mi-green);\n  color: #fff;\n  box-shadow: 0 6px 16px -6px rgba(81,152,67,0.6);\n}\n`;\n", "sourceName": "MoneyInput.tsx", "checklist": "# Capture checklist — MoneyInput\n\n- **Where:** rendered inside `AsstChat`'s price-capture screen — live route\n  `/assistant/<threadId>` (auth: dev master OTP `000000`) when Bossie asks for a\n  price; or in isolate at `/components/assistant/money-input/<case>`.\n- **Viewports:** 390 (mobile, primary), 768, 1280.\n- **Crop targets:** the money field + the preset chips (`CHIP_PRESETS_CENTS`) +\n  the keypad/submit.\n- **States to drive:**\n  - `empty` — no value, autofocus.\n  - `value` — a typed amount.\n  - `chip-active` — a preset chip selected (e.g. $1,000).\n  - `large` — 8-digit amount triggering the auto-shrink type scale.\n  - `es` — Spanish (drive via `_signals.lang` — the `lang` prop is an ignored\n    SSR seed; the island reads `langSignal`).\n- **Theme:** light only. Money is **integer cents**. **No fabricated screenshots.**\n"}, {"name": "onboarding-progress", "component": "OnboardingProgress", "spec": "# OnboardingProgress\n\nThe \"Quick setup\" progress strip shown at the top of the chat on\n`/assistant/[threadId]?onboard=1` while the conversation is still\nonboarding-shaped. Four dots fill as the user answers (name → business → state →\naddress); the current dot pulses; a confetti burst fires at completion; then the\nstrip fades itself out so the chat takes over.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/OnboardingProgress.tsx`).\n- **Interaction tier:** `island` (client-only state) **with event-driven\n  re-fetch**.\n- **Client state owned:**\n  - `step: Step` (`0|1|2|3|4`) — seeded from `initialStep` (computed SSR-side from\n    the profile so the dots never flash empty), recomputed on every\n    `pm:profile-updated` event by fetching `/api/profile` and counting filled\n    fields (name, business||legal, state, postal).\n  - `done: boolean` — `step >= 4`.\n  - `hidden: boolean` — set true ~4.5s after completion → component returns null.\n  - `lang` — `langSignal.value` (the `lang` prop is an ignored SSR seed).\n  - refs: `canvasRef` (confetti), `lastConfettiAt` (5s throttle).\n- **Data source:** `GET /api/profile` (Fresh route → backend). Counts 4 fields\n  into the step.\n- **Liveness — event PUSH + targeted re-fetch (no polling):** listens for the\n  `pm:profile-updated` CustomEvent (dispatched by AsstChat after each successful\n  onboarding turn) → re-fetches `/api/profile` → recomputes `step`. Also computes\n  once on mount in case the SSR `initialStep` is stale.\n- **Honest-empty:** always renders (with `step` dots) while visible; there is no\n  empty variant — it's a progress affordance, not a data list.\n- **Anti-patterns:**\n  - **`globalThis.location.assign(\"/dashboard\")`** in `skipSetup()` — a hard\n    full-page navigation (acceptable: it's an explicit \"skip\" exit, not a\n    refresh). FLAG: not a `location.reload()`, but a frozen-state-dropping nav.\n  - The \"skip\" affordance does **not** persist a skip flag (acknowledged in a\n    source comment) — the user will see onboarding again on the next\n    `?onboard=1` visit. Known gap.\n  - No `location.reload()`.\n- **Gating (in the route, not the island):** `[threadId].tsx` only mounts this\n  when `showOnboardBanner = isOnboard && initialStep < 4 && !hasActivity`\n  (hasActivity = bound customer || contract || phase===\"terms\"). So a thread with\n  real work never shows \"One left.\"\n\n## 2. Anatomy\n```\nhidden===true → null\nelse:\n<div style=\"…banner…; opacity:{hidden?0:1}\">              ← fade-out container\n  <canvas ref=canvasRef aria-hidden/>                      ← confetti layer (absolute, full-bleed)\n  <div row>\n    <span avatar>{done ? \"✓\" : \"👋\"}</span>                ← green when done, pink otherwise\n    <div col>\n      <div><strong eyebrow>{done?\"DONE\":\"QUICK SETUP\"}</strong>{message}</div>   ← step-specific copy\n      <div dots-row>\n        {[1..4].map(i → <span dot [pm-onb-pulse if current]/>)}\n        <div role=progressbar aria-valuenow=step><div fill width={pct}/></div>\n        <span>{step}/4</span>\n      </div>\n      {!done && <div quick-replies>                          ← step-specific chips + Skip setup\n        step===2 → \"Yes\" / \"different state\"\n        step===3 → \"skip\"\n        always   → \"Skip setup\" (ghost, → /dashboard)\n      </div>}\n    </div>\n  </div>\n  <style>{@keyframes pm-onb-pulse + reduced-motion guard}</style>\n</div>\n```\n- **Quick-reply chips** dispatch `pm:onboard-send-text` (`{ text }`) which AsstChat\n  listens for and forwards into the composer send — non-blocking (you can still\n  type your own answer).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `initialStep` | `0\\|1\\|2\\|3\\|4` | `0` | select | no — SSR seed, then synced from `/api/profile` |\n| `lang` | `\"en\"\\|\"es\"` | (ignored) | select | **ignored SSR seed** — reads `langSignal.value` |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| step0 | nothing filled — \"let's get you set up\" | `cases/step0/step0.json` |\n| step2 | 2 of 4 — shows Yes / different-state chips | `cases/step2/step2.json` |\n| step3 | 3 of 4 — \"One left.\" + skip chip | `cases/step3/step3.json` |\n| done | 4/4 — green ✓, confetti, then fades out | `cases/done/done.json` |\n\n> Isolate note: `initialStep` seeds the visible state directly, so cases are bare\n> props. The mount effect will try to fetch `/api/profile` (no backend → throws,\n> swallowed) and keep the seeded step. The `done` case fires confetti on mount\n> (suppressed under reduced-motion) and hides itself after ~4.5s — capture\n> quickly.\n\n## 5. Events\n- `ev.expect(e => e.source.includes(\"button\") && /Yes|different state|skip/.test(e.text))`\n  → `quickReply(text)` → dispatches `pm:onboard-send-text` `{ text }`.\n- `ev.expect(e => e.text === \"Skip setup\")` → `skipSetup()` →\n  `location.assign(\"/dashboard\")`.\n- Incoming (not user): `pm:profile-updated` → re-fetch + recompute step.\n\n## 6. Motion (extracted, all inline)\n- **Current-step dot:** `@keyframes pm-onb-pulse` — `scale(1)→1.35` + expanding\n  `box-shadow` ring, `1.4s ease-in-out infinite` (pink).\n- **Progress-bar fill:** `width` transition `480ms cubic-bezier(.34,1.56,.64,1)`\n  (overshoot/bounce).\n- **Dots:** `transition: all 280ms ease` (fill color).\n- **Container:** `transition: opacity 600ms ease-out` → fades to 0 ~4.5s after\n  completion, then unmounts (`hidden`).\n- **Confetti:** canvas `requestAnimationFrame`, 80 particles, gravity+drift,\n  ~1.7s; **suppressed when `prefers-reduced-motion: reduce`** (early-returns).\n- **Reduced motion:** component-local guard `[style*=\"pm-onb-pulse\"]{animation:\n  none}` + the confetti `matchMedia` early-return. Verify both.\n\n## 7. Responsive\n- The row is `flex-wrap:wrap` with `min-width:160px` on the text column, so it\n  reflows on narrow widths. No `@media` of its own.\n\n## 8. A11y\n- The bar is a real `role=\"progressbar\"` with `aria-valuemin/max/now` + label —\n  good.\n- Confetti canvas is `aria-hidden`.\n- Quick-reply / skip are real `<button>`s.\n- **Gap:** the step copy itself isn't in an `aria-live` region, so a step advance\n  isn't announced; minor.\n\n## 9. Used on\nOnly `routes/assistant/[threadId].tsx`, conditionally (`showOnboardBanner`). Not\non `index.tsx`. Pairs with the `?onboard=1` entry flow seeded by\n`/assistant?onboard=1` → `onboarding-start` → redirect to the new thread.\n", "fixture": "{\n  \"category\": \"assistant\",\n  \"folder\": \"onboarding-progress\",\n  \"controls\": {\n    \"initialStep\": { \"type\": \"select\", \"value\": 2, \"options\": [0, 1, 2, 3, 4] }\n  }\n}\n", "cases": [{"name": "done.json", "content": "{\n  \"_name\": \"Done — 4/4 green check + confetti (then fades out ~4.5s)\",\n  \"initialStep\": 4,\n  \"_note\": \"On mount, done=true fires confetti (suppressed under reduced-motion) and schedules setHidden(true) after ~4.5s → component unmounts. Capture immediately.\"\n}\n"}, {"name": "step0.json", "content": "{\n  \"_name\": \"Step 0 — nothing filled\",\n  \"initialStep\": 0\n}\n"}, {"name": "step2.json", "content": "{\n  \"_name\": \"Step 2 — Yes / different-state chips\",\n  \"initialStep\": 2\n}\n"}, {"name": "step3.json", "content": "{\n  \"_name\": \"Step 3 — One left + skip chip\",\n  \"initialStep\": 3\n}\n"}], "css": "/* ============================================================\n   OnboardingProgress — NO external stylesheet in source. The island is\n   entirely inline-styled and ships its own <style> block (verbatim below)\n   carrying the pulse keyframe + a reduced-motion guard. The dot/bar colors\n   are inline JS literals (PINK #FF6B6B, PINK_DARK #d94e4e, GREEN #519843,\n   TEAL #144852, INK #1c2c30) — they mirror the Sabor tokens but are\n   hard-coded in the TSX, not `var(--…)`. Reproduced here for the rebuild.\n   ============================================================ */\n\n/* The component's own injected keyframe (from the inline <style>): */\n@keyframes pm-onb-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    box-shadow: 0 0 0 0 rgba(255, 107, 107, 0.5);\n  }\n  50% {\n    transform: scale(1.35);\n    box-shadow: 0 0 0 6px rgba(255, 107, 107, 0);\n  }\n}\n@media (prefers-reduced-motion: reduce) {\n  [style*=\"pm-onb-pulse\"] {\n    animation: none !important;\n  }\n}\n\n/* Container (inline style in source) — banner across the top of the chat: */\n.onb-progress {\n  position: relative;\n  padding: 14px 22px 16px;\n  background: linear-gradient(\n    135deg,\n    rgba(255, 107, 107, 0.08) 0%,\n    rgba(255, 107, 107, 0.02) 100%\n  );\n  border-bottom: 1px solid rgba(255, 107, 107, 0.2);\n  overflow: hidden;\n  transition: opacity 600ms ease-out; /* fade-out 4.5s after step 4 */\n}\n\n/* Progress-bar fill easing (inline) — bouncy fill on each step: */\n/* width transition: 480ms cubic-bezier(0.34,1.56,0.64,1) */\n/* Dot transitions: all 280ms ease; current step gets pm-onb-pulse 1.4s. */\n/* Confetti (step-4 completion) is canvas-drawn via requestAnimationFrame,\n   80 particles, 1.7s, suppressed under prefers-reduced-motion. */\n", "cssName": "onboarding-progress.css", "source": "import { useEffect, useRef, useState } from \"preact/hooks\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\nconst PINK = \"#FF6B6B\";\nconst PINK_DARK = \"#d94e4e\";\nconst GREEN = \"#519843\";\nconst TEAL = \"#144852\";\nconst INK = \"#1c2c30\";\n\ntype Step = 0 | 1 | 2 | 3 | 4;\nconst TOTAL_STEPS: Step = 4;\n\ninterface Props {\n  /** Server-rendered initial step so the dots don't flash empty. */\n  initialStep?: Step;\n  /** Optional SSR seed for the UI language. Ignored at runtime — the island\n   * self-sources the reactive language from `langSignal`. */\n  lang?: Lang;\n}\n\n/**\n * Onboarding progress strip — replaces the static \"Quick setup\" banner.\n *\n * - Shows 4 dots that fill as the user answers (name, business, state,\n *   address). The current step pulses.\n * - Fires a confetti burst once the user reaches step 4 (handoff).\n * - Fades itself out a few seconds after completion so the chat takes\n *   over the visual focus.\n *\n * Subscribes to the `pm:profile-updated` CustomEvent (dispatched by\n * AsstChat after every successful onboarding turn) and refetches\n * /api/profile to count filled fields. Self-gating means SSR can render\n * the initial step from the server-resolved profile and the island\n * just keeps it in sync as the chat progresses.\n */\nexport default function OnboardingProgress(\n  { initialStep = 0 }: Props,\n) {\n  // Self-source the reactive UI language so the island re-renders live when\n  // the language flips (e.g. via SettingsPage). Ignores any `lang` prop seed.\n  const lang = langSignal.value;\n  const [step, setStep] = useState<Step>(initialStep);\n  const [done, setDone] = useState(initialStep >= TOTAL_STEPS);\n  const [hidden, setHidden] = useState(false);\n  const canvasRef = useRef<HTMLCanvasElement | null>(null);\n  const lastConfettiAt = useRef<number>(0);\n\n  useEffect(() => {\n    if (typeof globalThis.window === \"undefined\") return;\n    const computeStep = async () => {\n      try {\n        const r = await fetch(\"/api/profile\", { credentials: \"include\" });\n        if (!r.ok) return;\n        const j = await r.json() as {\n          user?: { name?: string };\n          identity?: { businessName?: string; legalName?: string };\n          address?: { state?: string; postal?: string };\n        };\n        let s: Step = 0;\n        if (j.user?.name?.trim()) s = (s + 1) as Step;\n        if (\n          (j.identity?.businessName?.trim() || j.identity?.legalName?.trim())\n        ) s = (s + 1) as Step;\n        if (j.address?.state?.trim()) s = (s + 1) as Step;\n        if (j.address?.postal?.trim()) s = (s + 1) as Step;\n        setStep(s);\n        if (s >= TOTAL_STEPS) {\n          setDone(true);\n          // Throttle: only confetti once per 5s in case multiple events fire.\n          if (Date.now() - lastConfettiAt.current > 5000) {\n            lastConfettiAt.current = Date.now();\n            fireConfetti();\n            globalThis.setTimeout(() => setHidden(true), 4500);\n          }\n        }\n      } catch { /* network blip — ignore */ }\n    };\n    const onEvt = () => computeStep();\n    globalThis.addEventListener(\"pm:profile-updated\", onEvt as EventListener);\n    // First compute on mount in case server-rendered initialStep is stale.\n    computeStep();\n    return () =>\n      globalThis.removeEventListener(\n        \"pm:profile-updated\",\n        onEvt as EventListener,\n      );\n  }, []);\n\n  function fireConfetti() {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n    const ctx = canvas.getContext(\"2d\");\n    if (!ctx) return;\n    const reduced = globalThis.matchMedia?.(\"(prefers-reduced-motion: reduce)\")\n      .matches;\n    if (reduced) return;\n    const dpr = globalThis.devicePixelRatio || 1;\n    const rect = canvas.getBoundingClientRect();\n    canvas.width = Math.round(rect.width * dpr);\n    canvas.height = Math.round(rect.height * dpr);\n    ctx.scale(dpr, dpr);\n    const colors = [PINK, GREEN, TEAL, \"#f9c74f\", \"#f9844a\"];\n    interface Particle {\n      x: number;\n      y: number;\n      vx: number;\n      vy: number;\n      rot: number;\n      vrot: number;\n      color: string;\n      size: number;\n    }\n    const N = 80;\n    const parts: Particle[] = [];\n    for (let i = 0; i < N; i++) {\n      parts.push({\n        x: rect.width / 2 + (Math.random() - 0.5) * 60,\n        y: 18,\n        vx: (Math.random() - 0.5) * 6,\n        vy: -Math.random() * 7 - 2,\n        rot: Math.random() * Math.PI,\n        vrot: (Math.random() - 0.5) * 0.4,\n        color: colors[Math.floor(Math.random() * colors.length)],\n        size: 4 + Math.random() * 4,\n      });\n    }\n    const start = performance.now();\n    const tick = (now: number) => {\n      const t = now - start;\n      ctx.clearRect(0, 0, rect.width, rect.height);\n      for (const p of parts) {\n        // Gravity + drift\n        p.vy += 0.25;\n        p.x += p.vx;\n        p.y += p.vy;\n        p.rot += p.vrot;\n        ctx.save();\n        ctx.translate(p.x, p.y);\n        ctx.rotate(p.rot);\n        ctx.fillStyle = p.color;\n        ctx.fillRect(-p.size / 2, -p.size / 2, p.size, p.size * 0.6);\n        ctx.restore();\n      }\n      if (t < 1700) {\n        globalThis.requestAnimationFrame(tick);\n      } else {\n        ctx.clearRect(0, 0, rect.width, rect.height);\n      }\n    };\n    globalThis.requestAnimationFrame(tick);\n  }\n\n  if (hidden) return null;\n\n  function quickReply(text: string) {\n    globalThis.dispatchEvent(\n      new CustomEvent(\"pm:onboard-send-text\", { detail: { text } }),\n    );\n  }\n\n  function skipSetup() {\n    // Mark \"skip\" so the gate doesn't bounce them back on next visit\n    // (best-effort — the FE doesn't currently persist a skip flag, so the\n    // user will see onboarding again on next ?onboard=1 visit. Acceptable\n    // for now; the dashboard checklist will catch them).\n    globalThis.location.assign(\"/dashboard\");\n  }\n\n  const pct = (step / TOTAL_STEPS) * 100;\n  const message = done\n    ? tFor(lang, \"onboardingProgress.message.done\")\n    : step === 0\n    ? tFor(lang, \"onboardingProgress.message.step0\")\n    : step === 1\n    ? tFor(lang, \"onboardingProgress.message.step1\")\n    : step === 2\n    ? tFor(lang, \"onboardingProgress.message.step2\")\n    : step === 3\n    ? tFor(lang, \"onboardingProgress.message.step3\")\n    : tFor(lang, \"onboardingProgress.message.last\");\n\n  return (\n    <div\n      style={`position:relative;padding:14px 22px 16px;background:linear-gradient(135deg,rgba(255,107,107,0.08) 0%,rgba(255,107,107,0.02) 100%);border-bottom:1px solid rgba(255,107,107,0.20);overflow:hidden;transition:opacity 600ms ease-out;opacity:${\n        hidden ? 0 : 1\n      }`}\n    >\n      <canvas\n        ref={canvasRef}\n        aria-hidden=\"true\"\n        style=\"position:absolute;inset:0;pointer-events:none;width:100%;height:100%\"\n      />\n      <div style=\"display:flex;align-items:center;gap:14px;flex-wrap:wrap;position:relative;z-index:1\">\n        <span\n          aria-hidden=\"true\"\n          style={`display:inline-flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:50%;background:${\n            done ? GREEN : PINK\n          };color:#fff;font-weight:800;flex-shrink:0;font-size:15px;transition:background 280ms`}\n        >\n          {done ? \"✓\" : \"👋\"}\n        </span>\n        <div style=\"flex:1;min-width:160px\">\n          <div\n            style={`font-size:13.5px;color:${INK};font-weight:600;line-height:1.35`}\n          >\n            <strong\n              style={`color:${\n                done ? GREEN : PINK_DARK\n              };letter-spacing:.04em;text-transform:uppercase;font-size:11px;font-weight:800;margin-right:6px`}\n            >\n              {done\n                ? tFor(lang, \"onboardingProgress.label.done\")\n                : tFor(lang, \"onboardingProgress.label.quickSetup\")}\n            </strong>\n            {message}\n          </div>\n          {/* Dots + thin progress bar */}\n          <div style=\"display:flex;align-items:center;gap:10px;margin-top:8px\">\n            {[1, 2, 3, 4].map((i) => {\n              const filled = step >= i;\n              const pulsing = step === i - 1 && !done;\n              return (\n                <span\n                  key={i}\n                  style={`width:10px;height:10px;border-radius:50%;flex-shrink:0;transition:all 280ms ease;background:${\n                    filled ? (done ? GREEN : PINK) : \"rgba(255,107,107,0.20)\"\n                  };${\n                    pulsing\n                      ? `animation:pm-onb-pulse 1.4s ease-in-out infinite;`\n                      : \"\"\n                  }`}\n                />\n              );\n            })}\n            <div\n              role=\"progressbar\"\n              aria-label={tFor(lang, \"onboardingProgress.ariaLabel\")}\n              aria-valuemin={0}\n              aria-valuemax={TOTAL_STEPS}\n              aria-valuenow={step}\n              style=\"flex:1;height:3px;border-radius:999px;background:rgba(255,107,107,0.15);overflow:hidden;margin-left:4px\"\n            >\n              <div\n                style={`height:100%;background:${\n                  done\n                    ? GREEN\n                    : `linear-gradient(90deg,${PINK} 0%,${PINK_DARK} 100%)`\n                };width:${pct}%;transition:width 480ms cubic-bezier(0.34,1.56,0.64,1);border-radius:999px`}\n              />\n            </div>\n            <span\n              style={`font-size:11px;font-weight:800;color:${\n                done ? GREEN : PINK_DARK\n              };letter-spacing:.06em;min-width:30px;text-align:right`}\n            >\n              {step}/{TOTAL_STEPS}\n            </span>\n          </div>\n          {\n            /* Step-specific quick replies. Banner-level so they're discoverable\n              regardless of which step the assistant is currently on, and\n              non-blocking (you can still type your own answer below). */\n          }\n          {!done && (\n            <div style=\"display:flex;flex-wrap:wrap;gap:6px;margin-top:8px\">\n              {step === 2 && (\n                <>\n                  <button\n                    type=\"button\"\n                    onClick={() => quickReply(\"Yes\")}\n                    style={`appearance:none;font:inherit;font-weight:700;font-size:11.5px;padding:5px 11px;border-radius:999px;border:1px solid ${GREEN};background:#fff;color:${GREEN};cursor:pointer`}\n                  >\n                    {tFor(lang, \"onboardingProgress.reply.yes\")}\n                  </button>\n                  <button\n                    type=\"button\"\n                    onClick={() => quickReply(\"different state\")}\n                    style={`appearance:none;font:inherit;font-weight:700;font-size:11.5px;padding:5px 11px;border-radius:999px;border:1px solid rgba(20,72,82,0.20);background:#fff;color:${TEAL};cursor:pointer`}\n                  >\n                    {tFor(lang, \"onboardingProgress.reply.differentState\")}\n                  </button>\n                </>\n              )}\n              {step === 3 && (\n                <button\n                  type=\"button\"\n                  onClick={() => quickReply(\"skip\")}\n                  style={`appearance:none;font:inherit;font-weight:700;font-size:11.5px;padding:5px 11px;border-radius:999px;border:1px solid rgba(20,72,82,0.20);background:#fff;color:${TEAL};cursor:pointer`}\n                >\n                  {tFor(lang, \"onboardingProgress.reply.skip\")}\n                </button>\n              )}\n              <button\n                type=\"button\"\n                onClick={skipSetup}\n                style={`appearance:none;font:inherit;font-weight:600;font-size:11.5px;padding:5px 11px;border-radius:999px;border:1px dashed rgba(20,72,82,0.20);background:transparent;color:${INK};opacity:0.7;cursor:pointer;margin-left:auto`}\n              >\n                {tFor(lang, \"onboardingProgress.skipSetup\")}\n              </button>\n            </div>\n          )}\n        </div>\n      </div>\n      <style>\n        {`\n        @keyframes pm-onb-pulse {\n          0%, 100% { transform: scale(1);   box-shadow: 0 0 0 0 rgba(255,107,107,0.5); }\n          50%      { transform: scale(1.35); box-shadow: 0 0 0 6px rgba(255,107,107,0); }\n        }\n        @media (prefers-reduced-motion: reduce) {\n          [style*=\"pm-onb-pulse\"] { animation: none !important; }\n        }\n      `}\n      </style>\n    </div>\n  );\n}\n", "sourceName": "OnboardingProgress.tsx", "checklist": "# Capture checklist — OnboardingProgress\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- Only appears on `http://localhost:5280/assistant/<threadId>?onboard=1` AND only\n  while the gate passes (`initialStep < 4 && !hasActivity`).\n- Reach it via the entry flow: `http://localhost:5280/assistant?onboard=1` →\n  server seeds an onboarding thread → 302 to `/assistant/<id>?onboard=1`.\n\n## Viewports\n- **1280px** (full chat column) and **720px** (mobile — the row wraps; chips wrap\n  to a second line).\n\n## Element(s) to crop\n- The full pink banner strip across the top of the `.chat` column: 👋/✓ avatar,\n  the QUICK SETUP eyebrow + step copy, the 4 dots + thin progress bar + `N/4`,\n  and the quick-reply chip row.\n\n## Transient states to drive\n1. **step0** — fresh onboarding thread, nothing answered (dots empty, first dot\n   pulsing).\n2. **step2** — after answering name + business; the \"Yes\" / \"different state\"\n   chips appear (the state question).\n3. **step3** — after the state answer; \"One left.\" copy + the \"skip\" chip for the\n   address question.\n4. **done** — answer the address question → all 4 dots green, ✓ avatar, confetti\n   burst, then the banner fades out after ~4.5s. Capture mid-confetti AND the\n   faded-out (gone) end state.\n\n## Motion to film\n- `pm-onb-pulse` on the current-step dot (1.4s).\n- The progress-bar `width` fill on each step advance (480ms bounce ease).\n- The completion confetti (canvas, ~1.7s) and the 600ms opacity fade-out.\n- Re-shoot with `prefers-reduced-motion: reduce`: the dot pulse stills (local\n  guard) and the confetti does NOT fire (matchMedia early-return) — verify both.\n"}, {"name": "redirect-toast", "component": "RedirectToast", "spec": "# RedirectToast\n\nA tiny one-shot toast that reads a known query parameter on mount and surfaces a\nsingle explanatory line for ~6 seconds, then strips the param. Used by the\n`/messages → /assistant?from=messages` consolidation (P6.14) so the silent jump\nis no longer confusing.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/RedirectToast.tsx`).\n- **Interaction tier:** `island` (client-only, one-shot) — no fetch, no events.\n- **Client state owned:**\n  - `key: string | null` — the i18n message key to show. `null` = hidden.\n  - `lang` — `langSignal.value` (the `lang` prop is an ignored SSR seed) so a\n    live language flip re-localizes the visible toast.\n- **Mount logic (effect):** reads `URLSearchParams.get(\"from\")`; maps it through\n  `MESSAGE_KEYS = { messages: \"redirectToast.messagesConsolidated\" }`. If a known\n  key matches: `setKey(k)`, then **`history.replaceState`** to drop `?from=` (so\n  reloads don't re-fire), and `setTimeout(() => setKey(null), 6000)`.\n- **Data source:** the URL query string only. No backend.\n- **Liveness:** none.\n- **Honest-empty:** returns `null` (renders nothing) unless a recognized `from`\n  value is present — so on a normal `/assistant` visit it's invisible.\n- **Anti-patterns:** none. `history.replaceState` is the correct param-strip; no\n  reload. (The 6s auto-dismiss has no manual close button — minor.)\n- **Styling:** fully **inline-styled** (a pill: teal bg `#1A535C`, white text,\n  fixed top-center, `z-index:9999`). No external CSS class — nothing to extract;\n  the `css/redirect-toast.css` here only documents the inline values.\n\n## 2. Anatomy\n```\nkey===null → renders null\nelse:\n<div role=\"status\" style=\"position:fixed;top:18px;left:50%;translateX(-50%);\n     background:var(--brand-teal,#1A535C);color:#fff;padding:10px 18px;\n     border-radius:999px;font-size:13.5px;font-weight:500;\n     box-shadow:0 4px 14px rgba(0,0,0,.18);z-index:9999;max-width:90vw\">\n  {tFor(lang, key)}\n</div>\n```\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `lang` | `\"en\"\\|\"es\"` | (ignored) | select | **ignored SSR seed** — reads `langSignal.value` |\n\n(No other props — the trigger is the URL, not a prop.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| from-messages | `?from=messages` present → consolidation line visible | `cases/from-messages/from-messages.json` |\n| hidden | no recognized `from` → renders nothing | `cases/hidden/hidden.json` |\n\n> Isolate note: the trigger is `location.search`, which isolate can't set per\n> case. Drive via `_signals.key` (set to `\"redirectToast.messagesConsolidated\"`\n> to show, `null` to hide), bypassing the URL read. The 6s auto-dismiss timer is\n> harmless in isolate.\n\n## 5. Events\n- No user events (no buttons). The only \"event\" is the mount-time URL read and\n  the 6s self-dismiss timer.\n\n## 6. Motion (extracted)\n- **None.** The toast appears/disappears via conditional render — no entrance or\n  exit transition is defined (it pops in and pops out). A rebuild could add a\n  fade/slide; the current source has no animation.\n- **Reduced motion:** N/A (no animation).\n\n## 7. Responsive\n- `max-width:90vw` keeps it on-screen on narrow viewports; otherwise fixed\n  top-center at all widths. No `@media`.\n\n## 8. A11y\n- `role=\"status\"` — announced politely by AT. Good.\n- **Gap:** no dismiss control and no `aria-live` region wrapper beyond the\n  implicit `role=\"status\"`; acceptable for a 6s informational toast.\n\n## 9. Used on\nOnly `routes/assistant/index.tsx` (mounted unconditionally; self-gates on the\n`?from=` param). Not on `[threadId].tsx`. This is the visible half of the\n`/messages` → `/assistant?from=messages` 302 redirect\n(`routes/messages/index.tsx`).\n", "fixture": "{\n  \"category\": \"assistant\",\n  \"folder\": \"redirect-toast\",\n  \"controls\": {}\n}\n", "cases": [{"name": "from-messages.json", "content": "{\n  \"_name\": \"From messages — consolidation toast visible\",\n  \"_note\": \"Real trigger is location.search `?from=messages`; isolate can't set the URL per case, so force the resolved state via _signals.key.\",\n  \"_signals\": { \"key\": \"redirectToast.messagesConsolidated\" }\n}\n"}, {"name": "hidden.json", "content": "{\n  \"_name\": \"Hidden — no recognized ?from, renders nothing\",\n  \"_signals\": { \"key\": null }\n}\n"}], "css": "/* ============================================================\n   RedirectToast — NO external CSS in source. The island is fully\n   inline-styled. This file documents those inline values verbatim\n   (as a `.redirect-toast` class) for the rebuild; the source applies\n   them via the element `style` attribute, not a class.\n   ============================================================ */\n\n.redirect-toast {\n  position: fixed;\n  top: 18px;\n  left: 50%;\n  transform: translateX(-50%);\n  background: var(--brand-teal, #1a535c);\n  color: #fff;\n  padding: 10px 18px;\n  border-radius: 999px;\n  font-size: 13.5px;\n  font-weight: 500;\n  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.18);\n  z-index: 9999;\n  max-width: 90vw;\n}\n", "cssName": "redirect-toast.css", "source": "/**\n * Tiny one-shot toast island. Reads a known query parameter on mount and\n * surfaces an explanatory line for ~6 seconds. Used by /messages →\n * /assistant redirect (P6.14) so the silent jump is no longer confusing.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\nconst MESSAGE_KEYS: Record<string, string> = {\n  messages: \"redirectToast.messagesConsolidated\",\n};\n\n// `lang` prop kept as an optional SSR seed but ignored; the island\n// self-sources the live UI language from `langSignal`.\nexport default function RedirectToast(_props: { lang?: Lang }) {\n  // Read langSignal.value during render so the toast resolves in (and\n  // re-renders on) the live UI language.\n  const lang = langSignal.value;\n  const [key, setKey] = useState<string | null>(null);\n\n  useEffect(() => {\n    if (typeof globalThis.location === \"undefined\") return;\n    const params = new URLSearchParams(globalThis.location.search);\n    const from = params.get(\"from\");\n    const k = from ? MESSAGE_KEYS[from] : undefined;\n    if (!k) return;\n    setKey(k);\n\n    // Strip the param so reloads don't keep firing the toast.\n    if (typeof globalThis.history !== \"undefined\") {\n      params.delete(\"from\");\n      const qs = params.toString();\n      const url = globalThis.location.pathname + (qs ? `?${qs}` : \"\") +\n        globalThis.location.hash;\n      globalThis.history.replaceState(null, \"\", url);\n    }\n\n    const timer = setTimeout(() => setKey(null), 6000);\n    return () => clearTimeout(timer);\n  }, []);\n\n  if (!key) return null;\n  // Resolve against the reactive `lang` so a live language flip while the\n  // toast is visible re-localizes the line.\n  const text = tFor(lang, key);\n\n  return (\n    <div\n      role=\"status\"\n      style=\"position:fixed;top:18px;left:50%;transform:translateX(-50%);background:var(--brand-teal,#1A535C);color:#fff;padding:10px 18px;border-radius:999px;font-size:13.5px;font-weight:500;box-shadow:0 4px 14px rgba(0,0,0,0.18);z-index:9999;max-width:90vw\"\n    >\n      {text}\n    </div>\n  );\n}\n", "sourceName": "RedirectToast.tsx", "checklist": "# Capture checklist — RedirectToast\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- Trigger by navigating to **`http://localhost:5280/messages`** — it 302s to\n  `/assistant?from=messages`, and the toast fires on mount.\n- Equivalently hit `http://localhost:5280/assistant?from=messages` directly.\n\n## Viewports\n- **1280px** and **390px** (mobile) — confirm the pill stays top-center and\n  respects `max-width:90vw`.\n\n## Element(s) to crop\n- The fixed top-center teal pill with the consolidation line\n  (`redirectToast.messagesConsolidated`). Crop the toast and a sliver of the\n  page behind it for context.\n\n## Transient states to drive\n1. **from-messages** — load `/messages` (or `/assistant?from=messages`); capture\n   within the 6-second window before it self-dismisses. Note the URL bar: the\n   `?from=` param is stripped immediately via `history.replaceState`, so a reload\n   will NOT re-show it.\n2. **hidden** — load a plain `/assistant`; confirm no toast renders.\n\n## Motion to film\n- None — the toast has no entrance/exit animation (conditional render only). No\n  reduced-motion concern.\n"}]}, {"name": "clients", "pageMd": "# Page: /clients\n\n## Purpose\nThe contractor's client roster, framed editorially rather than as a CRM table.\nAn on-mount fetch pulls a rollup-enriched `CustomerCard[]` plus two analytics\naggregates (leaderboard + segment mix); the page renders an editorial hero with\nlive counts, a dark \"Today's loop\" check-in ribbon, a searchable/filterable grid\nof mood-colored client cards with tap-to-open contact panels, and a sticky right\nrail (top clients + who's-on-your-books). A contractor can add a client via an\ninline modal (`POST /customers`).\n\n## App-shell composition (order)\nRoute `routes/clients/index.tsx` (`define.page`) renders:\n```\n<Head>…</Head>\n<div class=\"app\">\n  <DashSidebar active=\"clients\" />        ← SHARED (shared-components/dash-sidebar)\n  <main class=\"main\">\n    <DashTopbar greetingDate greetingName /> ← SHARED (shared-components/dash-topbar)\n    <div class=\"content\">\n      <ClientsPage />                      ← island (components/clients-page)\n    </div>\n  </main>\n</div>\n```\n`ClientsPage` then composes (build order): `ClientsHero` → `LoopBar` →\n`ClientsBoard` (with `TopClients` + `ClientsSegments` as rail children) → inline\nAdd-Client modal. The shared shell, `Skeletons`, `MobileViewport`,\n`ImpersonationBanner` are NOT re-spec'd here (see `shared-components/`).\n\n## `<Head>`\n- **Title:** `tFor(lang, \"clientsRoute.docTitle\")` → **\"Clients · Paperwork\n  Monster\"** (`es` variant from the dict).\n- **CSS (two stylesheets, order matters):**\n  - `<link rel=\"stylesheet\" href=\"/dashboard.css\" key=\"css-dashboard\" />` — the\n    token + shell base (`--brand-*`, `--fg-*`, `.app`, `.main`, `.content`).\n  - `<link rel=\"stylesheet\" href=\"/clients.css\" key=\"css-clients\" />` — this\n    page's component styles (copied to `css/clients.css`; per-component extracts\n    under `components/*/css/`).\n\n## SSR data\n- The route SSRs **no page-body data**. It only computes shell props from\n  `ctx.state.user`:\n  - `lang` = `user.language === \"es\" ? \"es\" : \"en\"`.\n  - `greetingName` = first word of `user.name` (or localized `there` fallback).\n  - `greetingDate` = `\"{weekday} · {month} {date}\"` localized via `WEEKDAY_KEYS`\n    / `MONTH_KEYS` against `new Date()`.\n- All page content is fetched **client-side** by the `ClientsPage` island on\n  mount: `Promise.all([ GET /clients, GET /analytics/clients/top?limit=5,\n  GET /analytics/clients/segments ])`. See data-model.md §4 (ClientsPage →\n  `CustomerCard[]`, `TopClient[]`, `ClientSegmentRow[]`) and hazards #3/#6.\n\n## Sections\n1. **ClientsHero** (`.ph2`) — editorial header: pulsing crumb (\"Clients · N on\n   the books\"), big headline (`The N people who keep the lights on.` / empty\n   variant), a stat sub (jobs in flight · $ owed · quiet clients), and the pink\n   **Add a client** CTA. → `components/clients-sections` (`ClientsHero`).\n2. **LoopBar** (`.loopbar`) — dark teal ribbon of up to 3 drafted check-ins with\n   stacked avatars + \"Open the loop\" CTA; honest-empty when no picks. →\n   `components/clients-sections` (`LoopBar`).\n3. **ClientsBoard** (`.ctoolbar2` + `.clay2`) — the interactive core: search +\n   six filter chips (with per-status counts) + a static \"Warmth\" sort, above the\n   `.ccards2` mood-card grid; each card opens an in-place contact panel.\n   → `components/clients-board` (island).\n4. **Right rail** (`.cside2`, slotted into the board) — **TopClients** (`.ctop2`\n   leaderboard) + **ClientsSegments** (`.csegment2` segment-mix bars). →\n   `components/clients-sections` (`TopClients`, `ClientsSegments`).\n5. **Add-Client modal** — inline-styled overlay form (`POST /customers`), owned\n   by the `ClientsPage` island.\n\n## Local components\n| folder | source | bucket / tier | note |\n|---|---|---|---|\n| `components/clients-page` | `islands/ClientsPage.tsx` (242) | island | whole-page data island; on-mount fetch + inline add modal |\n| `components/clients-sections` | `components/ClientsSections.tsx` (268) | page-composition (static; Hero has an island-owned `onAdd` callback) | 4 exports: Hero, LoopBar, TopClients, ClientsSegments |\n| `components/clients-board` | `islands/ClientsBoard.tsx` (371) | island | toolbar + card grid + slide-up panel; URL-synced `?segment=` filter |\n\n**Shared (reference only — not re-spec'd):** `DashSidebar`, `DashTopbar`,\n`Skeletons` (`ShimmerStyle`/`PageHeaderSkeleton`/`CardGridSkeleton`).\n**Shared helpers copied into component `js/`:** `lib/clients-display.ts`\n(mood/status/since/balance/story/cta/segment/address derivation +\n`dollars`/`initialsOf`/`numberWord`) and `lib/dash-icons.tsx` (`I` + `ICN`).\n\n## Dead code on this surface\n- **`islands/ContactForm.tsx` (85)** — **DEAD.** Zero imports anywhere in the\n  repo (verified via grep over `islands/`, `routes/`, `components/`, and a\n  repo-wide `*.tsx` sweep). It is a landing/auth OTP capture form\n  (`landingClient.sendOtp` → redirect to `/verify`), NOT a clients component, and\n  is **not** mounted by `ClientsBoard` (the add-client form on `/clients` is the\n  inline modal inside `ClientsPage`). Per spec policy, no folder is written for\n  it. Belongs to the auth/landing surface if anything (data-model.md lists it\n  under `LoginForm/LandingScripts/ContactForm → /auth/send-otp`).\n- **`@keyframes ccard2-editorial-in`** (clients.css) — declared but never\n  applied; the cards' staggered entrance never plays (see clients-board.md §6).\n- **`.cpage-error`, `.ctop2__empty`, `.csegment2__empty`, `.ccards2__empty`** —\n  classes emitted in JSX with **no CSS rule** in `static/*.css` (unstyled flow).\n\n## Build order\n1. Add `/clients.css` (+ ensure `/dashboard.css` tokens) and the route shell\n   (`DashSidebar active=\"clients\"` + `DashTopbar` + `<ClientsPage/>` in\n   `.content`), `<Head>` title + both stylesheets.\n2. Port `lib/clients-display.ts` + `lib/dash-icons.tsx` (shared deps).\n3. Build `ClientsSections` (static presentational; verify all honest-empty\n   branches) — Hero, LoopBar, TopClients, ClientsSegments.\n4. Build `ClientsBoard` island (toolbar + card grid + panel; URL `?segment=`\n   sync via pushState/popstate; outside-click/Escape close).\n5. Build `ClientsPage` island (on-mount `Promise.all` fetch + skeleton/error\n   gates; compose 3+4; inline Add-Client modal `POST /customers` + refresh).\n6. Fix-list to carry forward: apply the dead `ccard2-editorial-in` animation;\n   style `.cpage-error`; refresh `top`+`segments` after add (stale-rail bug);\n   promote the modal to a real dialog; lift inline modal styles to tokens.\n", "components": [{"name": "clients-board", "component": "ClientsBoard", "spec": "# ClientsBoard\n\nThe interactive heart of `/clients`: a toolbar (search + filter chips + sort)\nabove a responsive grid of editorial \"client cards\", each with a tap-to-open\nslide-up detail panel. A 280px right rail (leaderboard + segment mix) is slotted\nin as `children`.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ClientsBoard.tsx`).\n- **Interaction tier:** `island` (client-only state). Pure client-side filter +\n  search + open/close over a frozen `cards` prop — no fetch of its own.\n- **Client state owned:**\n  - `filter: FilterId` (`\"all\" | ClientStatus`) — active chip. **URL-synced:**\n    seeded from `?segment=` on mount (`filterFromSearch`), written back via\n    `history.pushState({segment}, \"\", url)` on chip click (omits the param for\n    `\"all\"`), and re-read on `popstate` (browser back/forward re-applies it).\n    This is real routing-via-querystring, NOT a reload — good.\n  - `query: string` — live search text (substring match across name /\n    businessName / email / phoneNumber / address / notes).\n  - `openId: string | null` — which card's detail panel is expanded (one at a\n    time). Closed on outside-click (document listener) and on `Escape`.\n- **Data source:** the backend's `CustomerCard[]` via the `cards` prop — passed\n  down from the `ClientsPage` island (which fetched `/clients`). ClientsBoard\n  does **no fetching, no seed, no derivation the backend already owns**\n  (`status`, `balanceCents`, `daysSinceContact` arrive precomputed). All\n  display mapping (gradient palette, status chip text, story line, CTA verb,\n  since-badge tier, balance formatting, address fallback) is done through the\n  shared **`lib/clients-display.ts`** helpers (copied to `js/clients-display.ts`):\n  `moodFor`, `initialsOf`, `segmentLabel`, `storyLineFor`, `ctaFor`,\n  `balanceDisplay`, `addressFor`, `sinceBadge`.\n- **Honest-empty:** when `rows.length === 0` it renders `.ccards2__empty` with\n  either `clientsBoard.empty.noClients` (the roster is genuinely empty) or\n  `clientsBoard.empty.noMatches` (filter/search hides everything) — the branch\n  is chosen by `cards.length === 0`. Filter chip counts always render (incl. 0).\n- **Liveness:** none. No polling/websocket. The `cards` prop is a frozen\n  snapshot from the parent's single on-mount fetch.\n- **Anti-patterns:** none in this island. (The whole-page-island /\n  `location.reload` concern lives one level up in `ClientsPage`, not here.) It\n  correctly uses `pushState`/`popstate` rather than reloading on filter change.\n- **Data-shape hazards:**\n  - **Per-status filter counts** (`filterCounts`, a `useMemo` that loops `cards`\n    and increments `counts[c.status]++`) are recomputed client-side from the\n    frozen array. Cheap here (≤ a few dozen cards), but the SAME counts on a\n    real backend are status-bucket aggregates over the whole roster — see\n    data-model.md hazard #6 (status-bucketed counters; don't scan per visit).\n    The seed encodes the identical pattern as `CLIENTS.filter(...).length`.\n  - `c.status` is assumed to be exactly one of the six `FilterId`s; any other\n    value silently fails to increment any bucket (the `counts[c.status]++` would\n    write an untracked key) — relies on the backend honoring the union.\n  - Search is a plain client-side `.includes` over the already-loaded array\n    (fine), so it can't find clients beyond the fetched page if pagination is\n    ever added.\n\n## 2. Anatomy\n```\n<>  (fragment — no wrapper element)\n  <div class=\"ctoolbar2\">\n    <div class=\"ctoolbar2__search\">\n      <I search/> <input placeholder=… value=query onInput=setQuery/>\n    </div>\n    <div class=\"ctoolbar2__filters\">\n      {FILTER_DEFS.map(f =>\n        <button class=\"ctoolbar2__filter [--active]\" aria-pressed onClick=selectFilter>\n          {label} <span class=\"ctoolbar2__filter-count\">{count}</span>\n        </button>)}\n    </div>\n    <button class=\"ctoolbar2__sort\">Warmth <I chevDown/></button>   ← STATIC (no handler)\n  </div>\n\n  <div class=\"clay2\">                                ← two-pane grid (1fr + 280px)\n    <div class=\"ccards2\">                            ← auto-fill minmax(300px,1fr)\n      {rows.map((c,i) => <ClientCard .../>)}\n      {rows.length===0 && <div class=\"ccards2__empty\">{empty copy}</div>}\n    </div>\n    <div class=\"cside2\">{children}</div>             ← right-rail slot (TopClients + ClientsSegments)\n  </div>\n</>\n\nClientCard (local sub-component):\n<div class=\"ccard2 [ccard2--open]\" style=\"--mood-*; animation-delay:{i*35}ms\" onClick=onCardClick>\n  <div class=\"ccard2__mood\">                          ← gradient banner\n    <div class=\"ccard2__mood-tex\"/>\n    <SinceBadge days unit/>                            ← giant ghost number\n    <div class=\"ccard2__status\"><dot/> {mood.label}</div>\n    {vip && <div class=\"ccard2__crown\"><I crown/></div>}\n  </div>\n  <div class=\"ccard2__av\">{initials}</div>             ← overlapping avatar tile\n  <div class=\"ccard2__body\">\n    <h3 class=\"ccard2__name\"/> [<div class=\"ccard2__biz\"/>]\n    <div class=\"ccard2__seg\">{seg} · {lastWhenRel}</div>\n    <p class=\"ccard2__story\">{storyLine}</p>           ← clamped to 4 lines\n  </div>\n  <div class=\"ccard2__foot\">\n    <button class=\"ccard2__nudge\">{cta} <arrow→></button>   ← STATIC (no handler)\n    <div class=\"ccard2__bal-wrap\"><lbl>Balance</lbl><val class=…>{balance}</val></div>\n  </div>\n\n  <div class=\"ccard2__panel\" onClick=stopPropagation>   ← absolute, slides up when --open\n    <div class=\"ccard2__panel-head\"> av + name/biz/seg·mood + <button x onClick=onClose/> </div>\n    <div class=\"ccard2__panel-rows\">\n      [phone <a tel:>] [email <a mailto:>] [address (always; static div)]\n    </div>\n    <div class=\"ccard2__panel-actions\">\n      <button>Message</button> <button --pink>Open card</button>   ← both STATIC\n    </div>\n  </div>\n</div>\n```\n- **Icon dependency:** `I` + `ICN.{search,crown,x,phone,mail,pin,arrow,msg,eye}`\n  from `lib/dash-icons.tsx` (copied to `js/dash-icons.tsx`). The sort caret uses\n  an inline `<path d=\"m6 9 6 6 6-6\"/>`.\n- **Sub-components:** `ClientCard` and `SinceBadge` are defined in the same file\n  (not exported) — spec'd here, not as separate folders.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `cards` | `CustomerCard[]` (required) | — | (data) | no |\n| `children` | `ComponentChildren` | `undefined` | (slot — right rail) | no |\n| `lang` | `\"en\" \\| \"es\"` | `\"en\"` | select | no |\n\n`ClientCard` (internal) props: `c: CustomerCard`, `idx: number`,\n`isOpen: boolean`, `onOpen()`, `onClose()`, `lang`. `SinceBadge` (internal):\n`days: number`, `lang`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | full roster (12 seed cards), filter \"all\", nothing open | `cases/default/default.json` |\n| filtered-owes | \"Owe you\" chip active → only `status:\"owes\"` rows | `cases/filtered-owes/filtered-owes.json` |\n| search | query narrows the grid to matches | `cases/search/search.json` |\n| card-open | one card's detail panel expanded (`ccard2--open`) | `cases/card-open/card-open.json` |\n| empty-no-clients | `cards=[]` → \"No clients yet…\" empty cell | `cases/empty-no-clients/empty-no-clients.json` |\n| empty-no-matches | non-empty roster but filter/search hides all | `cases/empty-no-matches/empty-no-matches.json` |\n| es | Spanish UI language | `cases/es/es.json` |\n\n## 5. Events\n- `capture(page)`: `e.source === \"button.ctoolbar2__filter\" && e.type === \"click\"`\n  → `selectFilter(id)`; updates `filter`, pushes `?segment=` (or deletes it for\n  `all`).\n- `capture(page)`: `e.source === \"input\" (in .ctoolbar2__search) && e.type === \"input\"`\n  → `setQuery`; live-filters the grid.\n- `capture(page)`: `e.source === \"div.ccard2\" && e.type === \"click\"` (not on\n  `.ccard2__foot`/`.ccard2__panel`) → `onOpen` → sets `openId`.\n- `capture(page)`: `e.source === \"button.ccard2__panel-x\" && e.type === \"click\"`\n  → `onClose`.\n- `capture(page)`: `document click outside .ccard2` OR `keydown Escape` →\n  `setOpenId(null)`.\n- `capture(page)`: `window popstate` → re-applies `filter` from URL.\n- **STATIC / no handler (decorative for now):** `.ctoolbar2__sort` (\"Warmth\"),\n  `.ccard2__nudge` (the CTA verb), `.ccard2__panel-act` ×2 (\"Message\",\n  \"Open card\"). FLAG: these read as actions but are inert — wire them on rebuild.\n\n## 6. Motion (extracted from clients.css)\n- **Card hover:** `transform: translateY(-4px)` + deepened mood-tinted shadow\n  over `320ms` (`transform` uses `--ease-bounce`, shadow `--ease-out`). Disabled\n  when `--open` (`.ccard2--open:hover { transform:none }`).\n- **Detail panel slide-up:** `transform: translateY(8% → 0)` over `380ms\n  var(--ease-bounce)` + `opacity 0 → 1` over `240ms var(--ease-out)`; toggled by\n  the `.ccard2--open` class. `pointer-events` flips none→auto.\n- **Status dot:** `pulse-dot 2.4s infinite` (opacity 1↔0.4).\n- **Close (x) button:** `transform: rotate(90deg)` on hover (`--dur-fast`).\n- **Nudge arrow / panel-row arrow:** `translateX(2px)` on hover (`--dur-fast`).\n- **DEAD KEYFRAME — `@keyframes ccard2-editorial-in`:** declared in clients.css\n  but **never applied**. The card markup sets only `animation-delay:{i*35}ms`\n  inline with no `animation-name`, so the staggered entrance never plays (a\n  delay alone is inert). **Jank/fix:** add\n  `animation: ccard2-editorial-in 420ms var(--ease-out) backwards;` to `.ccard2`\n  so the inline per-card delay produces the intended cascade; gate it behind the\n  global reduced-motion clamp. (Until then the cards just appear instantly.)\n- **Reduced motion:** clients.css has **no** component-local reduced-motion block\n  — it relies on the global tokens-CSS clamp (`animation/transition-duration →\n  0.01ms !important`). Verify hover lift, panel slide, and pulse all still under\n  the clamp.\n\n## 7. Responsive (this component's own @media in clients.css)\n- **`max-width:1100px`:** `.clay2` collapses to a single column (the right rail\n  drops below the cards).\n- **`max-width:768px`:** `.ccards2` → 1 column; `.ctoolbar2` → 1 column (search,\n  filters, sort stack); `.ctoolbar2__filters` becomes horizontally scrollable\n  (`overflow-x:auto`).\n- Card grid itself is intrinsically responsive via\n  `repeat(auto-fill, minmax(300px, 1fr))` above those breakpoints.\n\n## 8. A11y\n- Filter chips are real `<button>`s with `aria-pressed={active}` — good.\n- **Gaps:**\n  - The card is a `<div onClick>` (not a button) with no role/tabindex/key\n    handler — the open interaction is mouse-only and not keyboard-operable; the\n    expanded panel is not announced. Rebuild: make the card (or a header button\n    inside it) keyboard-activatable with `aria-expanded`/`aria-controls`.\n  - The detail panel has no focus trap and isn't a dialog; Escape closes it\n    (good) but focus isn't moved in/out.\n  - Search `<input>` has a `placeholder` but no associated `<label>`.\n  - The since-badge giant number is decorative (`pointer-events:none;\n    user-select:none`) — fine, but its day/week meaning isn't exposed to AT.\n\n## 9. Used on\n- `/clients` only. Mounted by the `ClientsPage` island (`islands/ClientsPage.tsx`)\n  via `<ClientsBoard cards={cards} lang={lang}>…rail…</ClientsBoard>`. Evidence:\n  sole import of `ClientsBoard` is in `islands/ClientsPage.tsx`. CSS lives in\n  `static/clients.css` (loaded by `routes/clients/index.tsx` `<Head>`).\n", "fixture": "{\n  \"category\": \"clients\",\n  \"folder\": \"clients-board\",\n  \"controls\": {\n    \"cards\": { \"type\": \"json\", \"value\": [] },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "card-open.json", "content": "{\n  \"_name\": \"Card open — Maple Grove detail panel expanded\",\n  \"lang\": \"en\",\n  \"_signals\": { \"openId\": \"c2\" },\n  \"cards\": [\n    { \"id\": \"c2\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\", \"businessName\": \"Maple Grove PM\", \"email\": \"janet@maplegrove.co\", \"phoneNumber\": \"(415) 555-0124\", \"address\": \"1200 Maple Grove Ln, Oakland CA\", \"segment\": \"property_mgmt\", \"vip\": true, \"createdAt\": \"2025-02-10T10:00:00.000Z\", \"updatedAt\": \"2026-06-18T10:00:00.000Z\", \"lastWhen\": \"2026-06-18T10:00:00.000Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 420000, \"balanceSub\": \"Invoice #204\", \"activeJobs\": 2, \"jobsSub\": \"Roof + gutters\", \"status\": \"active\", \"temp\": 92, \"daysSinceContact\": 0, \"revenue12moCents\": 2480000 },\n    { \"id\": \"c5\", \"userId\": \"u1\", \"name\": \"Sarah Chen\", \"email\": \"sarah.chen@gmail.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-04-02T10:00:00.000Z\", \"updatedAt\": \"2026-06-15T10:00:00.000Z\", \"lastWhen\": \"2026-06-15T10:00:00.000Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Deck stain bal.\", \"activeJobs\": 1, \"jobsSub\": \"Deck\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 480000 }\n  ]\n}\n"}, {"name": "default.json", "content": "{\n  \"_name\": \"Default — full seed roster (12), filter all\",\n  \"lang\": \"en\",\n  \"cards\": [\n    { \"id\": \"c1\", \"userId\": \"u1\", \"name\": \"Greenleaf HOA\", \"email\": \"board@greenleaf-hoa.org\", \"phoneNumber\": \"(415) 555-0145\", \"segment\": \"hoa\", \"vip\": true, \"createdAt\": \"2025-01-04T10:00:00.000Z\", \"updatedAt\": \"2026-06-16T10:00:00.000Z\", \"lastWhen\": \"2026-06-16T10:00:00.000Z\", \"lastWhenRel\": \"2 days ago\", \"lastTone\": \"warm\", \"balanceCents\": 0, \"balanceSub\": \"Paid in full\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"regular\", \"temp\": 78, \"daysSinceContact\": 2, \"revenue12moCents\": 3240000 },\n    { \"id\": \"c2\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\", \"businessName\": \"Maple Grove PM\", \"email\": \"janet@maplegrove.co\", \"phoneNumber\": \"(415) 555-0124\", \"segment\": \"property_mgmt\", \"vip\": true, \"createdAt\": \"2025-02-10T10:00:00.000Z\", \"updatedAt\": \"2026-06-18T10:00:00.000Z\", \"lastWhen\": \"2026-06-18T10:00:00.000Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 420000, \"balanceSub\": \"Invoice #204\", \"activeJobs\": 2, \"jobsSub\": \"Roof + gutters\", \"status\": \"active\", \"temp\": 92, \"daysSinceContact\": 0, \"revenue12moCents\": 2480000 },\n    { \"id\": \"c3\", \"userId\": \"u1\", \"name\": \"Cobblestone Cafe\", \"email\": \"hello@cobblestone.cafe\", \"phoneNumber\": \"(415) 555-0117\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2025-03-01T10:00:00.000Z\", \"updatedAt\": \"2026-06-17T10:00:00.000Z\", \"lastWhen\": \"2026-06-17T10:00:00.000Z\", \"lastWhenRel\": \"yesterday\", \"lastTone\": \"hot\", \"balanceCents\": 0, \"balanceSub\": \"Settled\", \"activeJobs\": 1, \"jobsSub\": \"Patio repair\", \"status\": \"regular\", \"temp\": 71, \"daysSinceContact\": 1, \"revenue12moCents\": 1592000 },\n    { \"id\": \"c4\", \"userId\": \"u1\", \"name\": \"Marshall & Sons\", \"email\": \"mike@marshall-sons.com\", \"phoneNumber\": \"(415) 555-0166\", \"segment\": \"property_mgmt\", \"vip\": false, \"createdAt\": \"2025-03-12T10:00:00.000Z\", \"updatedAt\": \"2026-06-14T10:00:00.000Z\", \"lastWhen\": \"2026-06-14T10:00:00.000Z\", \"lastWhenRel\": \"4 days ago\", \"lastTone\": \"warm\", \"balanceCents\": -50000, \"balanceSub\": \"Credit on file\", \"activeJobs\": 1, \"jobsSub\": \"Unit turnover\", \"status\": \"active\", \"temp\": 64, \"daysSinceContact\": 4, \"revenue12moCents\": 1145000 },\n    { \"id\": \"c5\", \"userId\": \"u1\", \"name\": \"Sarah Chen\", \"email\": \"sarah.chen@gmail.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-04-02T10:00:00.000Z\", \"updatedAt\": \"2026-06-15T10:00:00.000Z\", \"lastWhen\": \"2026-06-15T10:00:00.000Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Deck stain bal.\", \"activeJobs\": 1, \"jobsSub\": \"Deck\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 480000 },\n    { \"id\": \"c6\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\", \"email\": \"manager@hilltopdiner.com\", \"phoneNumber\": \"(415) 555-0102\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2025-04-20T10:00:00.000Z\", \"updatedAt\": \"2026-06-07T10:00:00.000Z\", \"lastWhen\": \"2026-06-07T10:00:00.000Z\", \"lastWhenRel\": \"11 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 116000, \"balanceSub\": \"Past due 14d\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"owes\", \"temp\": 22, \"daysSinceContact\": 11, \"revenue12moCents\": 320000 },\n    { \"id\": \"c7\", \"userId\": \"u1\", \"name\": \"Tom & Linda Kowalski\", \"email\": \"linda.k@outlook.com\", \"phoneNumber\": \"(415) 555-0193\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2026-06-18T09:00:00.000Z\", \"updatedAt\": \"2026-06-18T09:00:00.000Z\", \"lastWhen\": \"2026-06-18T09:00:00.000Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 0, \"balanceSub\": \"—\", \"activeJobs\": 0, \"jobsSub\": \"Requested quote\", \"status\": \"lead\", \"temp\": 86, \"daysSinceContact\": 0, \"revenue12moCents\": 0 },\n    { \"id\": \"c8\", \"userId\": \"u1\", \"name\": \"Jana Patel\", \"email\": \"jana.patel@hey.com\", \"phoneNumber\": \"(415) 555-0142\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-05-09T10:00:00.000Z\", \"updatedAt\": \"2026-06-13T10:00:00.000Z\", \"lastWhen\": \"2026-06-13T10:00:00.000Z\", \"lastWhenRel\": \"5 days ago\", \"lastTone\": \"warm\", \"balanceCents\": -82500, \"balanceSub\": \"Overpaid\", \"activeJobs\": 1, \"jobsSub\": \"Bathroom\", \"status\": \"active\", \"temp\": 68, \"daysSinceContact\": 5, \"revenue12moCents\": 640000 },\n    { \"id\": \"c9\", \"userId\": \"u1\", \"name\": \"Marcus Lin\", \"email\": \"marcus.lin@gmail.com\", \"phoneNumber\": \"(415) 555-0151\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2026-06-17T10:00:00.000Z\", \"updatedAt\": \"2026-06-17T10:00:00.000Z\", \"lastWhen\": \"2026-06-17T10:00:00.000Z\", \"lastWhenRel\": \"yesterday\", \"lastTone\": \"warm\", \"balanceCents\": 0, \"balanceSub\": \"—\", \"activeJobs\": 0, \"jobsSub\": \"Kitchen inquiry\", \"status\": \"lead\", \"temp\": 52, \"daysSinceContact\": 1, \"revenue12moCents\": 0 },\n    { \"id\": \"c10\", \"userId\": \"u1\", \"name\": \"Bayside Properties\", \"email\": \"ops@bayside-pm.com\", \"phoneNumber\": \"(415) 555-0179\", \"segment\": \"property_mgmt\", \"vip\": false, \"createdAt\": \"2025-01-30T10:00:00.000Z\", \"updatedAt\": \"2026-06-14T10:00:00.000Z\", \"lastWhen\": \"2026-06-14T10:00:00.000Z\", \"lastWhenRel\": \"4 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 0, \"balanceSub\": \"Settled\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"cold\", \"temp\": 28, \"daysSinceContact\": 4, \"revenue12moCents\": 0 },\n    { \"id\": \"c11\", \"userId\": \"u1\", \"name\": \"Riverside Yoga\", \"email\": \"studio@riversideyoga.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2024-12-01T10:00:00.000Z\", \"updatedAt\": \"2026-05-07T10:00:00.000Z\", \"lastWhen\": \"2026-05-07T10:00:00.000Z\", \"lastWhenRel\": \"6 weeks ago\", \"lastTone\": \"cold\", \"balanceCents\": 0, \"balanceSub\": \"Settled\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"cold\", \"temp\": 24, \"daysSinceContact\": 42, \"revenue12moCents\": 0 },\n    { \"id\": \"c12\", \"userId\": \"u1\", \"name\": \"Ortega Rentals\", \"email\": \"mateo@ortegarentals.com\", \"phoneNumber\": \"(415) 555-0163\", \"segment\": \"property_mgmt\", \"vip\": false, \"createdAt\": \"2024-10-15T10:00:00.000Z\", \"updatedAt\": \"2026-04-18T10:00:00.000Z\", \"lastWhen\": \"2026-04-18T10:00:00.000Z\", \"lastWhenRel\": \"9 weeks ago\", \"lastTone\": \"cold\", \"balanceCents\": 0, \"balanceSub\": \"Settled\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"cold\", \"temp\": 18, \"daysSinceContact\": 61, \"revenue12moCents\": 0 }\n  ]\n}\n"}, {"name": "empty-no-clients.json", "content": "{\n  \"_name\": \"Empty roster — 'No clients yet…' (cards=[])\",\n  \"lang\": \"en\",\n  \"cards\": []\n}\n"}, {"name": "empty-no-matches.json", "content": "{\n  \"_name\": \"No matches — non-empty roster but search hides all ('zzzz')\",\n  \"lang\": \"en\",\n  \"_signals\": { \"query\": \"zzzz\" },\n  \"cards\": [\n    { \"id\": \"c3\", \"userId\": \"u1\", \"name\": \"Cobblestone Cafe\", \"email\": \"hello@cobblestone.cafe\", \"phoneNumber\": \"(415) 555-0117\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2025-03-01T10:00:00.000Z\", \"updatedAt\": \"2026-06-17T10:00:00.000Z\", \"lastWhen\": \"2026-06-17T10:00:00.000Z\", \"lastWhenRel\": \"yesterday\", \"lastTone\": \"hot\", \"balanceCents\": 0, \"balanceSub\": \"Settled\", \"activeJobs\": 1, \"jobsSub\": \"Patio repair\", \"status\": \"regular\", \"temp\": 71, \"daysSinceContact\": 1, \"revenue12moCents\": 1592000 }\n  ]\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — toolbar chips + card chrome localized\",\n  \"lang\": \"es\",\n  \"cards\": [\n    { \"id\": \"c2\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\", \"businessName\": \"Maple Grove PM\", \"email\": \"janet@maplegrove.co\", \"phoneNumber\": \"(415) 555-0124\", \"segment\": \"property_mgmt\", \"vip\": true, \"createdAt\": \"2025-02-10T10:00:00.000Z\", \"updatedAt\": \"2026-06-18T10:00:00.000Z\", \"lastWhen\": \"2026-06-18T10:00:00.000Z\", \"lastWhenRel\": \"hoy\", \"lastTone\": \"hot\", \"balanceCents\": 420000, \"balanceSub\": \"Factura #204\", \"activeJobs\": 2, \"jobsSub\": \"Techo + canaletas\", \"status\": \"active\", \"temp\": 92, \"daysSinceContact\": 0, \"revenue12moCents\": 2480000 },\n    { \"id\": \"c6\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\", \"email\": \"manager@hilltopdiner.com\", \"phoneNumber\": \"(415) 555-0102\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2025-04-20T10:00:00.000Z\", \"updatedAt\": \"2026-06-07T10:00:00.000Z\", \"lastWhen\": \"2026-06-07T10:00:00.000Z\", \"lastWhenRel\": \"hace 11 días\", \"lastTone\": \"cold\", \"balanceCents\": 116000, \"balanceSub\": \"Vencida 14d\", \"activeJobs\": 0, \"jobsSub\": \"Sin trabajos\", \"status\": \"owes\", \"temp\": 22, \"daysSinceContact\": 11, \"revenue12moCents\": 320000 }\n  ]\n}\n"}, {"name": "filtered-owes.json", "content": "{\n  \"_name\": \"Filter: Owe you — only status=owes (Sarah Chen, Hilltop Diner)\",\n  \"lang\": \"en\",\n  \"_signals\": { \"filter\": \"owes\" },\n  \"cards\": [\n    { \"id\": \"c5\", \"userId\": \"u1\", \"name\": \"Sarah Chen\", \"email\": \"sarah.chen@gmail.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-04-02T10:00:00.000Z\", \"updatedAt\": \"2026-06-15T10:00:00.000Z\", \"lastWhen\": \"2026-06-15T10:00:00.000Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Deck stain bal.\", \"activeJobs\": 1, \"jobsSub\": \"Deck\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 480000 },\n    { \"id\": \"c6\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\", \"email\": \"manager@hilltopdiner.com\", \"phoneNumber\": \"(415) 555-0102\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2025-04-20T10:00:00.000Z\", \"updatedAt\": \"2026-06-07T10:00:00.000Z\", \"lastWhen\": \"2026-06-07T10:00:00.000Z\", \"lastWhenRel\": \"11 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 116000, \"balanceSub\": \"Past due 14d\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"owes\", \"temp\": 22, \"daysSinceContact\": 11, \"revenue12moCents\": 320000 }\n  ]\n}\n"}, {"name": "search.json", "content": "{\n  \"_name\": \"Search 'maple' — query narrows grid to one match\",\n  \"lang\": \"en\",\n  \"_signals\": { \"query\": \"maple\" },\n  \"cards\": [\n    { \"id\": \"c2\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\", \"businessName\": \"Maple Grove PM\", \"email\": \"janet@maplegrove.co\", \"phoneNumber\": \"(415) 555-0124\", \"segment\": \"property_mgmt\", \"vip\": true, \"createdAt\": \"2025-02-10T10:00:00.000Z\", \"updatedAt\": \"2026-06-18T10:00:00.000Z\", \"lastWhen\": \"2026-06-18T10:00:00.000Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 420000, \"balanceSub\": \"Invoice #204\", \"activeJobs\": 2, \"jobsSub\": \"Roof + gutters\", \"status\": \"active\", \"temp\": 92, \"daysSinceContact\": 0, \"revenue12moCents\": 2480000 },\n    { \"id\": \"c5\", \"userId\": \"u1\", \"name\": \"Sarah Chen\", \"email\": \"sarah.chen@gmail.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-04-02T10:00:00.000Z\", \"updatedAt\": \"2026-06-15T10:00:00.000Z\", \"lastWhen\": \"2026-06-15T10:00:00.000Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Deck stain bal.\", \"activeJobs\": 1, \"jobsSub\": \"Deck\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 480000 }\n  ]\n}\n"}], "css": "/* ============================================================\n   ClientsBoard — extracted from static/clients.css.\n   Owns: the editorial card grid (.ccards2 / .ccard2*), the slide-up\n   detail panel (.ccard2__panel*), the toolbar (.ctoolbar2*) and the\n   two-pane layout grid (.clay2). The right-rail content (.cside2 /\n   .ctop2 / .csegment2) is slotted in as `children` and styled by\n   clients-sections.css — .clay2 here only positions the rail column.\n\n   Tokens (--brand-*, --pink-*, --teal-*, --green-*, --coffee-*,\n   --mint-*, --bg-*, --fg-*, --radius-*, --dur-*, --ease-*, --font-*,\n   --shadow-*) come from /dashboard.css — link it alongside /clients.css.\n   ============================================================ */\n\n/* ============== EDITORIAL CARD GRID ============== */\n.ccards2 {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));\n  gap: 16px;\n}\n.ccard2 {\n  position: relative;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  overflow: hidden;\n  cursor: pointer;\n  box-shadow:\n    0 1px 0 rgba(26, 83, 92, 0.04),\n    0 8px 22px -14px rgba(26, 83, 92, 0.18);\n  transition:\n    transform 320ms var(--ease-bounce),\n    box-shadow 320ms var(--ease-out);\n  display: flex;\n  flex-direction: column;\n}\n/* DEAD KEYFRAME: declared but never applied. The card markup sets only\n   `animation-delay:{idx*35}ms` inline (no `animation-name`), so this\n   entrance never plays. Rebuild fix: add\n   `animation: ccard2-editorial-in 420ms var(--ease-out) backwards;`\n   to `.ccard2` (the inline delay then staggers it), guarded by the\n   global reduced-motion clamp. */\n@keyframes ccard2-editorial-in {\n  from {\n    opacity: 0;\n    transform: translateY(10px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n.ccard2:hover {\n  transform: translateY(-4px);\n  box-shadow:\n    0 2px 0 rgba(26, 83, 92, 0.05),\n    0 22px 42px -18px var(--mood-shadow, rgba(26, 83, 92, 0.28));\n}\n.ccard2--open {\n  cursor: default;\n}\n.ccard2--open:hover {\n  transform: none;\n}\n\n.ccard2__mood {\n  position: relative;\n  height: 138px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n}\n.ccard2__mood-tex {\n  position: absolute;\n  inset: 0;\n  overflow: hidden;\n  border-radius: var(--radius-xl) var(--radius-xl) 0 0;\n  pointer-events: none;\n}\n.ccard2__mood-tex::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(\n      circle at 18% 22%,\n      rgba(255, 255, 255, 0.18) 0,\n      transparent 35%\n    ),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n}\n.ccard2__since {\n  position: absolute;\n  bottom: -22px;\n  right: -6px;\n  text-align: right;\n  pointer-events: none;\n  user-select: none;\n  font-family: var(--font-heading);\n  line-height: 0.85;\n  color: rgba(255, 255, 255, 0.18);\n  letter-spacing: -0.03em;\n}\n.ccard2__since-num {\n  font-weight: 900;\n  font-size: 96px;\n  display: block;\n}\n.ccard2__since-unit {\n  display: block;\n  font-size: 16px;\n  font-weight: 700;\n  letter-spacing: 0.18em;\n  text-transform: uppercase;\n  margin-top: 4px;\n  opacity: 0.85;\n}\n.ccard2__since--warm {\n  color: rgba(255, 255, 255, 0.16);\n}\n.ccard2__since--steady {\n  color: rgba(255, 255, 255, 0.22);\n}\n.ccard2__since--cool {\n  color: rgba(255, 255, 255, 0.32);\n}\n.ccard2__since--cold {\n  color: rgba(255, 255, 255, 0.45);\n}\n\n.ccard2__status {\n  position: absolute;\n  top: 14px;\n  left: 14px;\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 5px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.92);\n  backdrop-filter: blur(4px);\n  color: var(--mood-status, var(--brand-teal));\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  z-index: 2;\n}\n.ccard2__status-dot {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: currentColor;\n  animation: pulse-dot 2.4s infinite;\n}\n@keyframes pulse-dot {\n  0%, 100% {\n    opacity: 1;\n  }\n  50% {\n    opacity: 0.4;\n  }\n}\n.ccard2__crown {\n  position: absolute;\n  top: 14px;\n  right: 14px;\n  width: 28px;\n  height: 28px;\n  border-radius: 9px;\n  background: linear-gradient(135deg, #ffd56b, #e8a33d);\n  display: grid;\n  place-items: center;\n  color: #5a3d11;\n  box-shadow: 0 6px 14px rgba(232, 163, 61, 0.4);\n  z-index: 2;\n}\n\n.ccard2__av {\n  position: absolute;\n  left: 22px;\n  top: 92px;\n  width: 86px;\n  height: 86px;\n  border-radius: 22px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 28px;\n  letter-spacing: 0.02em;\n  z-index: 4;\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.28),\n    0 12px 24px -8px rgba(0, 0, 0, 0.32);\n}\n\n.ccard2__body {\n  padding: 50px 22px 0;\n  flex: 1 1 auto;\n  display: flex;\n  flex-direction: column;\n}\n.ccard2__name {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 19px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  line-height: 1.15;\n  margin: 0 0 4px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ccard2__biz {\n  font-family: var(--font-body);\n  font-size: 13px;\n  font-weight: 600;\n  color: var(--fg-muted);\n  line-height: 1.2;\n  margin: 0 0 4px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ccard2__seg {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  font-family: var(--font-body);\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin-bottom: 14px;\n}\n.ccard2__seg-dot {\n  width: 3px;\n  height: 3px;\n  border-radius: 999px;\n  background: var(--fg-subtle);\n}\n.ccard2__story {\n  font-family: var(--font-body);\n  font-size: 14.5px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  text-wrap: pretty;\n  margin: 0;\n  height: calc(14.5px * 1.5 * 4);\n  overflow: hidden;\n  display: -webkit-box;\n  -webkit-line-clamp: 4;\n  -webkit-box-orient: vertical;\n}\n\n.ccard2__foot {\n  margin-top: 18px;\n  padding: 14px 22px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  display: grid;\n  grid-template-columns: 1fr auto;\n  gap: 12px;\n  align-items: center;\n}\n.ccard2__nudge {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 0;\n  border: none;\n  background: transparent;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--brand-pink);\n  cursor: pointer;\n  text-align: left;\n  line-height: 1.3;\n  transition: gap var(--dur-fast) var(--ease-out);\n}\n.ccard2__nudge:hover {\n  gap: 9px;\n}\n.ccard2__nudge-arrow {\n  display: inline-block;\n  transition: transform var(--dur-fast) var(--ease-out);\n}\n.ccard2__nudge:hover .ccard2__nudge-arrow {\n  transform: translateX(2px);\n}\n.ccard2__bal-wrap {\n  text-align: right;\n}\n.ccard2__bal-lbl {\n  font-family: var(--font-heading);\n  font-size: 9.5px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 1px;\n}\n.ccard2__bal-val {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  letter-spacing: -0.01em;\n}\n.ccard2__bal-val--owe {\n  color: var(--pink-700);\n}\n.ccard2__bal-val--cred {\n  color: var(--green-700);\n}\n.ccard2__bal-val--zero {\n  color: var(--brand-teal);\n}\n\n/* ---- Detail panel: slides up from inside the card ---- */\n.ccard2__panel {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(180deg, #fffdf8 0%, #faf5eb 100%);\n  border-radius: var(--radius-xl);\n  padding: 22px 22px 18px;\n  display: flex;\n  flex-direction: column;\n  z-index: 10;\n  transform: translateY(8%);\n  opacity: 0;\n  pointer-events: none;\n  transition: transform 380ms var(--ease-bounce), opacity 240ms var(--ease-out);\n  box-shadow: inset 0 0 0 1px rgba(26, 83, 92, 0.06);\n}\n.ccard2--open .ccard2__panel {\n  transform: translateY(0);\n  opacity: 1;\n  pointer-events: auto;\n}\n.ccard2__panel-head {\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  margin-bottom: 14px;\n}\n.ccard2__panel-av {\n  width: 44px;\n  height: 44px;\n  border-radius: 12px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  flex-shrink: 0;\n  box-shadow: 0 4px 10px -4px rgba(0, 0, 0, 0.2);\n}\n.ccard2__panel-name {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  color: var(--brand-teal);\n  line-height: 1.2;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ccard2__panel-biz {\n  font-size: 12px;\n  font-weight: 600;\n  color: var(--fg-muted);\n  margin-top: 2px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ccard2__panel-seg {\n  font-size: 11.5px;\n  color: var(--fg-muted);\n  margin-top: 2px;\n}\n.ccard2__panel-x {\n  margin-left: auto;\n  width: 28px;\n  height: 28px;\n  border-radius: 999px;\n  border: none;\n  background: rgba(26, 83, 92, 0.06);\n  color: var(--brand-teal);\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n  transition:\n    background var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n.ccard2__panel-x:hover {\n  background: rgba(26, 83, 92, 0.12);\n  transform: rotate(90deg);\n}\n\n.ccard2__panel-rows {\n  display: flex;\n  flex-direction: column;\n  gap: 1px;\n  background: rgba(26, 83, 92, 0.08);\n  border-radius: var(--radius-md);\n  overflow: hidden;\n  margin-bottom: 12px;\n}\n.ccard2__panel-row {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  padding: 10px 12px;\n  background: #fff;\n  text-decoration: none;\n  color: var(--brand-teal);\n  font-size: 13px;\n  line-height: 1.3;\n  transition: background var(--dur-fast) var(--ease-out);\n  cursor: pointer;\n}\n.ccard2__panel-row:hover {\n  background: var(--mint-100);\n}\n.ccard2__panel-row-icon {\n  width: 28px;\n  height: 28px;\n  border-radius: 8px;\n  background: var(--mint-200);\n  color: var(--brand-teal);\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n}\n.ccard2__panel-row-text {\n  min-width: 0;\n  flex: 1;\n}\n.ccard2__panel-row-lbl {\n  font-family: var(--font-heading);\n  font-size: 9.5px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 1px;\n}\n.ccard2__panel-row-val {\n  font-weight: 600;\n  color: var(--brand-teal);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ccard2__panel-row-arrow {\n  color: var(--fg-subtle);\n  transition: transform var(--dur-fast) var(--ease-out);\n}\n.ccard2__panel-row:hover .ccard2__panel-row-arrow {\n  transform: translateX(2px);\n  color: var(--brand-pink);\n}\n\n.ccard2__panel-actions {\n  margin-top: auto;\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: 8px;\n}\n.ccard2__panel-act {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 6px;\n  padding: 10px 12px;\n  border: none;\n  border-radius: var(--radius-md);\n  background: var(--brand-teal);\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  cursor: pointer;\n  transition: transform var(--dur-fast) var(--ease-out);\n}\n.ccard2__panel-act--pink {\n  background: var(--brand-pink);\n}\n.ccard2__panel-act:hover {\n  transform: translateY(-1px);\n}\n\n/* ============== TOOLBAR (search + filter chips + sort) ============== */\n.ctoolbar2 {\n  display: grid;\n  grid-template-columns: 1fr auto auto;\n  gap: 8px;\n  align-items: center;\n  background: #fff;\n  border: 1px solid var(--border);\n  border-radius: var(--radius-lg);\n  padding: 8px;\n  margin-bottom: 14px;\n}\n.ctoolbar2__search {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  padding: 9px 12px;\n  background: var(--bg-sunken);\n  border-radius: var(--radius-md);\n  color: var(--fg-muted);\n}\n.ctoolbar2__search input {\n  flex: 1;\n  border: none;\n  background: transparent;\n  outline: none;\n  font-family: var(--font-body);\n  font-size: 14px;\n  color: var(--brand-teal);\n}\n.ctoolbar2__search input::placeholder {\n  color: var(--fg-subtle);\n}\n.ctoolbar2__filters {\n  display: flex;\n  gap: 4px;\n  padding: 2px;\n  background: var(--bg-sunken);\n  border-radius: var(--radius-md);\n}\n.ctoolbar2__filter {\n  border: none;\n  background: transparent;\n  padding: 7px 12px;\n  border-radius: 8px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--fg-muted);\n  cursor: pointer;\n  transition: all var(--dur-fast) var(--ease-out);\n  white-space: nowrap;\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n}\n.ctoolbar2__filter--active {\n  background: #fff;\n  color: var(--brand-teal);\n  box-shadow: var(--shadow-sm);\n}\n.ctoolbar2__filter-count {\n  font-size: 10px;\n  padding: 1px 6px;\n  border-radius: 999px;\n  background: var(--mint-200);\n  color: var(--brand-teal);\n}\n.ctoolbar2__filter--active .ctoolbar2__filter-count {\n  background: var(--green-50);\n  color: var(--green-700);\n}\n.ctoolbar2__sort {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  padding: 8px 12px;\n  border-radius: var(--radius-md);\n  background: var(--bg-sunken);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--brand-teal);\n  border: none;\n  cursor: pointer;\n}\n\n/* ============== TWO-PANE LAYOUT GRID (cards + right rail slot) ====== */\n.clay2 {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) 280px;\n  gap: 18px;\n  align-items: start;\n}\n\n/* Empty-state cell inside the card grid (no clients / no matches). The\n   source has no dedicated rule for `.ccards2__empty`; it inherits flow\n   layout. Rebuild may want to center it across the grid. */\n\n/* ============== RESPONSIVE (own breakpoints, from clients.css) ====== */\n@media (max-width: 1100px) {\n  .clay2 {\n    grid-template-columns: 1fr;\n  }\n}\n@media (max-width: 768px) {\n  .ccards2 {\n    grid-template-columns: 1fr;\n  }\n  .ctoolbar2 {\n    grid-template-columns: 1fr;\n  }\n  .ctoolbar2__filters {\n    overflow-x: auto;\n  }\n}\n", "cssName": "clients-board.css", "source": "/**\n * Interactive heart of /clients: toolbar (search + filter chips) + the\n * editorial card grid + the slide-up contact panel.\n *\n * Receives the backend's `CustomerCard[]` from the SSR route — no seed data,\n * no derivations the backend already owns (status, balance, days-since-contact\n * all come pre-computed via BuildCustomerCards).\n */\nimport type { ComponentChildren } from \"preact\";\nimport { useEffect, useMemo, useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { tFor } from \"../lib/i18n.ts\";\nimport type { ClientStatus, CustomerCard } from \"../clients/clients.ts\";\nimport {\n  addressFor,\n  balanceDisplay,\n  ctaFor,\n  initialsOf,\n  moodFor,\n  segmentLabel,\n  sinceBadge,\n  storyLineFor,\n} from \"../lib/clients-display.ts\";\n\n// Chip set matches the canonical reference (Paperwork Monster Clients.html)\n// — six entries with the exact labels from the design source. URL\n// bookmarking via ?segment= is additive: it doesn't change the visual\n// chip set, just lets a filtered view be reloaded / shared / back-buttoned.\ntype FilterId = ClientStatus | \"all\";\n\ninterface FilterEntry {\n  id: FilterId;\n  labelKey: string;\n}\nconst FILTER_DEFS: FilterEntry[] = [\n  { id: \"all\", labelKey: \"clientsBoard.filter.all\" },\n  { id: \"active\", labelKey: \"clientsBoard.filter.activeJobs\" },\n  { id: \"lead\", labelKey: \"clientsBoard.filter.leads\" },\n  { id: \"owes\", labelKey: \"clientsBoard.filter.oweYou\" },\n  { id: \"regular\", labelKey: \"clientsBoard.filter.regulars\" },\n  { id: \"cold\", labelKey: \"clientsBoard.filter.quiet\" },\n];\n\nconst FILTER_IDS: ReadonlyArray<string> = FILTER_DEFS.map((f) => f.id);\nfunction isFilterId(s: string | null | undefined): s is FilterId {\n  return !!s && FILTER_IDS.includes(s);\n}\nfunction filterFromSearch(search: string | null | undefined): FilterId {\n  if (!search) return \"all\";\n  const raw = new URLSearchParams(search).get(\"segment\");\n  return isFilterId(raw) ? raw : \"all\";\n}\n\nfunction SinceBadge({ days, lang }: { days: number; lang: \"en\" | \"es\" }) {\n  const { tier, num, unit } = sinceBadge(days, lang);\n  return (\n    <div class={`ccard2__since ccard2__since--${tier}`}>\n      <span class=\"ccard2__since-num\">{num}</span>\n      <span class=\"ccard2__since-unit\">{unit}</span>\n    </div>\n  );\n}\n\ninterface CardProps {\n  c: CustomerCard;\n  idx: number;\n  isOpen: boolean;\n  onOpen: () => void;\n  onClose: () => void;\n  lang: \"en\" | \"es\";\n}\n\nfunction ClientCard({ c, idx, isOpen, onOpen, onClose, lang }: CardProps) {\n  const mood = moodFor(c, lang);\n  const initials = initialsOf(c.name);\n  const seg = segmentLabel(c.segment, lang);\n  const story = storyLineFor(c, lang);\n  const cta = ctaFor(c, lang);\n  const balance = balanceDisplay(c, lang);\n  const address = addressFor(c, lang);\n\n  function onCardClick(e: Event) {\n    const target = e.target as HTMLElement;\n    if (target.closest(\".ccard2__foot\") || target.closest(\".ccard2__panel\")) {\n      return;\n    }\n    if (!isOpen) onOpen();\n  }\n\n  const styleStr = `--mood-from:${mood.from};` +\n    `--mood-to:${mood.to};` +\n    `--mood-shadow:${mood.shadow};` +\n    `--mood-status:${mood.statusFg};` +\n    `animation-delay:${idx * 35}ms`;\n\n  return (\n    <div\n      class={`ccard2 ${isOpen ? \"ccard2--open\" : \"\"}`}\n      style={styleStr}\n      onClick={onCardClick}\n    >\n      <div class=\"ccard2__mood\">\n        <div class=\"ccard2__mood-tex\" />\n        <SinceBadge days={c.daysSinceContact} lang={lang} />\n        <div class=\"ccard2__status\">\n          <span class=\"ccard2__status-dot\" /> {mood.label}\n        </div>\n        {c.vip && (\n          <div class=\"ccard2__crown\">\n            <I d={ICN.crown} size={13} sw={2.5} />\n          </div>\n        )}\n      </div>\n      <div class=\"ccard2__av\">{initials}</div>\n      <div class=\"ccard2__body\">\n        <h3 class=\"ccard2__name\">{c.name}</h3>\n        {c.businessName && <div class=\"ccard2__biz\">{c.businessName}</div>}\n        <div class=\"ccard2__seg\">\n          <span>{seg}</span>\n          <span class=\"ccard2__seg-dot\" />\n          <span>{c.lastWhenRel}</span>\n        </div>\n        <p class=\"ccard2__story\">{story}</p>\n      </div>\n      <div class=\"ccard2__foot\">\n        <button class=\"ccard2__nudge\" type=\"button\">\n          {cta} <span class=\"ccard2__nudge-arrow\">→</span>\n        </button>\n        <div class=\"ccard2__bal-wrap\">\n          <div class=\"ccard2__bal-lbl\">{tFor(lang, \"clientsBoard.balance\")}</div>\n          <div class={`ccard2__bal-val ${balance.cls}`}>{balance.text}</div>\n        </div>\n      </div>\n\n      <div class=\"ccard2__panel\" onClick={(e) => e.stopPropagation()}>\n        <div class=\"ccard2__panel-head\">\n          <div class=\"ccard2__panel-av\">{initials}</div>\n          <div style=\"min-width:0;flex:1\">\n            <div class=\"ccard2__panel-name\">{c.name}</div>\n            {c.businessName && (\n              <div class=\"ccard2__panel-biz\">{c.businessName}</div>\n            )}\n            <div class=\"ccard2__panel-seg\">{seg} · {mood.label}</div>\n          </div>\n          <button\n            class=\"ccard2__panel-x\"\n            type=\"button\"\n            onClick={onClose}\n            aria-label={tFor(lang, \"common.close\")}\n          >\n            <I d={ICN.x} size={14} sw={2.5} />\n          </button>\n        </div>\n        <div class=\"ccard2__panel-rows\">\n          {c.phoneNumber && (\n            <a class=\"ccard2__panel-row\" href={`tel:${c.phoneNumber}`}>\n              <span class=\"ccard2__panel-row-icon\">\n                <I d={ICN.phone} size={13} sw={2.2} />\n              </span>\n              <span class=\"ccard2__panel-row-text\">\n                <div class=\"ccard2__panel-row-lbl\">\n                  {tFor(lang, \"clientsBoard.panel.phone\")}\n                </div>\n                <div class=\"ccard2__panel-row-val\">{c.phoneNumber}</div>\n              </span>\n              <span class=\"ccard2__panel-row-arrow\">\n                <I d={ICN.arrow} size={12} sw={2.4} />\n              </span>\n            </a>\n          )}\n          {c.email && (\n            <a class=\"ccard2__panel-row\" href={`mailto:${c.email}`}>\n              <span class=\"ccard2__panel-row-icon\">\n                <I d={ICN.mail} size={13} sw={2.2} />\n              </span>\n              <span class=\"ccard2__panel-row-text\">\n                <div class=\"ccard2__panel-row-lbl\">\n                  {tFor(lang, \"clientsBoard.panel.email\")}\n                </div>\n                <div class=\"ccard2__panel-row-val\">{c.email}</div>\n              </span>\n              <span class=\"ccard2__panel-row-arrow\">\n                <I d={ICN.arrow} size={12} sw={2.4} />\n              </span>\n            </a>\n          )}\n          <div class=\"ccard2__panel-row\">\n            <span class=\"ccard2__panel-row-icon\">\n              <I d={ICN.pin} size={13} sw={2.2} />\n            </span>\n            <span class=\"ccard2__panel-row-text\">\n              <div class=\"ccard2__panel-row-lbl\">\n                {tFor(lang, \"clientsBoard.panel.address\")}\n              </div>\n              <div class=\"ccard2__panel-row-val\">{address}</div>\n            </span>\n            <span class=\"ccard2__panel-row-arrow\">\n              <I d={ICN.arrow} size={12} sw={2.4} />\n            </span>\n          </div>\n        </div>\n        <div class=\"ccard2__panel-actions\">\n          <button class=\"ccard2__panel-act\" type=\"button\">\n            <I d={ICN.msg} size={12} sw={2.4} />{\" \"}\n            {tFor(lang, \"clientsBoard.panel.message\")}\n          </button>\n          <button\n            class=\"ccard2__panel-act ccard2__panel-act--pink\"\n            type=\"button\"\n          >\n            <I d={ICN.eye} size={12} sw={2.4} />{\" \"}\n            {tFor(lang, \"clientsBoard.panel.openCard\")}\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n}\n\ninterface BoardProps {\n  cards: CustomerCard[];\n  children?: ComponentChildren;\n  lang?: \"en\" | \"es\";\n}\n\nexport default function ClientsBoard(\n  { cards, children, lang = \"en\" }: BoardProps,\n) {\n  // Filter id (\"all\" | ClientStatus). Seeded from URL ?segment= so a\n  // reload / shared link / back-button navigates to the same filtered\n  // view. Defaults to \"all\" (the leftmost chip) when the param is absent\n  // or unrecognized.\n  const [filter, setFilter] = useState<FilterId>(() => {\n    if (typeof globalThis.location === \"undefined\") return \"all\";\n    return filterFromSearch(globalThis.location.search);\n  });\n  const [query, setQuery] = useState(\"\");\n  const [openId, setOpenId] = useState<string | null>(null);\n\n  useEffect(() => {\n    if (!openId) return;\n    function onDocClick(e: MouseEvent) {\n      const target = e.target as HTMLElement;\n      if (!target.closest(\".ccard2\")) setOpenId(null);\n    }\n    function onKey(e: KeyboardEvent) {\n      if (e.key === \"Escape\") setOpenId(null);\n    }\n    document.addEventListener(\"click\", onDocClick);\n    document.addEventListener(\"keydown\", onKey);\n    return () => {\n      document.removeEventListener(\"click\", onDocClick);\n      document.removeEventListener(\"keydown\", onKey);\n    };\n  }, [openId]);\n\n  // Browser back / forward should re-apply the URL's filter state.\n  useEffect(() => {\n    function onPop() {\n      setFilter(filterFromSearch(globalThis.location.search));\n    }\n    globalThis.addEventListener(\"popstate\", onPop);\n    return () => globalThis.removeEventListener(\"popstate\", onPop);\n  }, []);\n\n  function selectFilter(next: FilterId) {\n    setFilter(next);\n    if (typeof globalThis.location === \"undefined\") return;\n    const url = new URL(globalThis.location.href);\n    // \"all\" is the default — represent it by absence of the param.\n    if (next === \"all\") url.searchParams.delete(\"segment\");\n    else url.searchParams.set(\"segment\", next);\n    globalThis.history.pushState({ segment: next }, \"\", url.toString());\n  }\n\n  const filterCounts = useMemo(() => {\n    const counts: Record<FilterId, number> = {\n      all: cards.length,\n      active: 0,\n      lead: 0,\n      owes: 0,\n      regular: 0,\n      cold: 0,\n    };\n    for (const c of cards) counts[c.status]++;\n    return counts;\n  }, [cards]);\n\n  const rows = cards.filter((c) => {\n    if (filter !== \"all\" && c.status !== filter) return false;\n    if (query.trim()) {\n      const q = query.toLowerCase();\n      return (\n        c.name.toLowerCase().includes(q) ||\n        (c.businessName ?? \"\").toLowerCase().includes(q) ||\n        (c.email ?? \"\").toLowerCase().includes(q) ||\n        (c.phoneNumber ?? \"\").includes(q) ||\n        (c.address ?? \"\").toLowerCase().includes(q) ||\n        (c.notes ?? \"\").toLowerCase().includes(q)\n      );\n    }\n    return true;\n  });\n\n  return (\n    <>\n      <div class=\"ctoolbar2\">\n        <div class=\"ctoolbar2__search\">\n          <I d={ICN.search} size={14} />\n          <input\n            placeholder={tFor(lang, \"clientsBoard.searchPlaceholder\")}\n            value={query}\n            onInput={(e) =>\n              setQuery((e.currentTarget as HTMLInputElement).value)}\n          />\n        </div>\n        <div class=\"ctoolbar2__filters\">\n          {FILTER_DEFS.map((f) => {\n            const active = filter === f.id;\n            return (\n              <button\n                key={f.id}\n                type=\"button\"\n                aria-pressed={active}\n                class={`ctoolbar2__filter ${\n                  active ? \"ctoolbar2__filter--active\" : \"\"\n                }`}\n                onClick={() => selectFilter(f.id)}\n              >\n                {tFor(lang, f.labelKey)}\n                <span class=\"ctoolbar2__filter-count\">\n                  {filterCounts[f.id]}\n                </span>\n              </button>\n            );\n          })}\n        </div>\n        <button class=\"ctoolbar2__sort\" type=\"button\">\n          {tFor(lang, \"clientsBoard.sort.warmth\")}{\" \"}\n          <I d={<path d=\"m6 9 6 6 6-6\" />} size={12} sw={2.5} />\n        </button>\n      </div>\n\n      <div class=\"clay2\">\n        <div class=\"ccards2\">\n          {rows.map((c, i) => (\n            <ClientCard\n              key={c.id}\n              c={c}\n              idx={i}\n              isOpen={openId === c.id}\n              onOpen={() => setOpenId(c.id)}\n              onClose={() => setOpenId(null)}\n              lang={lang}\n            />\n          ))}\n          {rows.length === 0 && (\n            <div class=\"ccards2__empty\">\n              {cards.length === 0\n                ? tFor(lang, \"clientsBoard.empty.noClients\")\n                : tFor(lang, \"clientsBoard.empty.noMatches\")}\n            </div>\n          )}\n        </div>\n        <div class=\"cside2\">\n          {children}\n        </div>\n      </div>\n    </>\n  );\n}\n", "sourceName": "ClientsBoard.tsx", "checklist": "# Capture checklist — ClientsBoard\n\n**No backend running → NO fabricated screenshots.** Capture only against the\nlive app once it's served. Light theme only (the app has no dark mode).\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000` (phone login → 000000).\n\n## Route / URL\n- `http://localhost:5280/clients` (frontend :5280; backend :4280 must be up so\n  the island's on-mount `/clients`, `/analytics/clients/top`, `/segments`\n  fetches resolve — otherwise the board renders its empty branch).\n- Filter deep-links: `…/clients?segment=owes`, `?segment=active`,\n  `?segment=lead`, `?segment=regular`, `?segment=cold` (each pre-selects a chip\n  via `filterFromSearch`).\n\n## Viewports (this component's real @media in clients.css)\n- **1280px** — full two-pane (`.clay2` 1fr + 280px rail; multi-column card grid).\n- **1100px** — boundary: `.clay2` collapses to one column (rail drops below).\n- **768px** — `.ccards2` → 1 column; `.ctoolbar2` stacks; `.ctoolbar2__filters`\n  scrolls horizontally. (Also capture just-above at 769px to show the toolbar\n  still in its 3-column grid.)\n\n## Element(s) to crop\n- The `.ctoolbar2` row (search + 6 filter chips with counts + Warmth sort).\n- A single `.ccard2` (mood banner + ghost since-number + status chip + avatar +\n  body + footer) — closed.\n- The same card with `.ccard2--open` (slide-up `.ccard2__panel` visible).\n- The empty cell (`.ccards2__empty`).\n\n## Transient states to drive\n1. **filter** — click each filter chip; confirm `aria-pressed`, the active chip\n   pill, the count badges, and that the URL gains/loses `?segment=`. Use browser\n   Back to confirm `popstate` re-applies the filter.\n2. **search** — type in `.ctoolbar2__search` (e.g. \"maple\"); grid narrows live.\n3. **card open** — click a card body → panel slides up. Then: click outside →\n   closes; press Escape → closes; click the `.ccard2__panel-x` → closes.\n4. **no-matches** — type a non-matching query with a non-empty roster →\n   `clientsBoard.empty.noMatches`.\n5. **es** — toggle UI language to Spanish (topbar) → chips + card chrome\n   localized.\n\n## Motion to film (from clients.css)\n- **Card hover lift** — `translateY(-4px)` + shadow over 320ms (bounce/out).\n- **Panel slide-up** — `translateY(8%→0)` 380ms bounce + opacity 240ms (toggle a\n  card open).\n- **Status dot pulse** — `pulse-dot 2.4s` (opacity 1↔0.4), film one cycle.\n- **Close-button rotate** — hover `.ccard2__panel-x` → 90deg.\n- **Dead entrance (do NOT expect motion):** `ccard2-editorial-in` is declared but\n  unapplied — confirm cards appear instantly with NO staggered fade (documents\n  the bug). Re-film after the rebuild fix applies the animation.\n- Re-shoot any of the above with `prefers-reduced-motion: reduce` (relies on the\n  global tokens clamp) — verify hover/slide/pulse go instant.\n"}, {"name": "clients-page", "component": "ClientsPage", "spec": "# ClientsPage\n\nThe top-level data island for `/clients`. The SSR route renders only the shell\n(sidebar + topbar); this island fetches the full payload on mount and composes\nthe editorial board: hero → loop ribbon → board (with right-rail children) →\nan inline Add-Client modal.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ClientsPage.tsx`).\n- **Interaction tier:** `island` (client-only state + on-mount fetch). This is a\n  **whole-page island** — the route SSRs nothing of the page body; everything\n  below the topbar hydrates and then fetches client-side. So the first paint is\n  the skeleton, not content.\n- **Client state owned:**\n  - `s: State` — `{ loading, error, cards, top, segments }`. Set once by an\n    on-mount `Promise.all([list(), top(5), segments()])` (each `.catch`-guarded\n    to a benign empty value). `alive` flag guards against setting state after\n    unmount.\n  - Add-client modal state: `addOpen`, `adding`, `addErr`, and the four field\n    signals `addName` / `addBusiness` / `addPhone` / `addEmail`.\n- **Data source per region + honest-empty:**\n  - `cards` ← `GET /clients` (`CustomerCard[]`). Drives hero counts, loop picks,\n    and the board. Empty → hero/board render their honest-empty branches.\n  - `top` ← `GET /analytics/clients/top?limit=5`. `segments` ← `GET\n    /analytics/clients/segments`. Each independently honest-empty in its section.\n- **Server action + flash:** the Add-Client modal does a real mutation —\n  `clientsClient.create({ name, businessName?, phoneNumber?, email? })`\n  (`POST /customers`). It is **client-side fetch, not a Fresh form/PRG**: on\n  success it clears the fields, closes the modal, and calls `refreshClients()`\n  (re-fetches `/clients` and merges `cards` into state). On error it sets\n  `addErr` (shown inline as a `role=\"alert\"` paragraph). No flash cookie, no\n  navigation.\n- **Island client-state + refresh — `location.reload()` FLAG:**\n  - **No `location.reload()` is used** — refresh after add is a proper\n    `refreshClients()` re-fetch + `setS`. Good; do NOT introduce a reload here.\n  - **BUT a real frozen-SSR-prop / stale-rollup hazard remains:** `top` and\n    `segments` are fetched **once on mount and never refreshed**. After adding a\n    client, `refreshClients()` updates only `cards` — the leaderboard and\n    segment mix keep their stale on-mount snapshot until a full navigation. A new\n    customer with no revenue wouldn't change `top`, but `segments` counts (and\n    the hero's editorial copy) can drift. Rebuild fix: have `refreshClients`\n    also re-pull `top`+`segments`, or recompute the hero stats from the refreshed\n    `cards` (the hero counts ARE derived from `cards`, so those do update; only\n    the two analytics rails go stale).\n  - `langSignal.value` is read for `lang` (reactive) — the prop `lang` is\n    ignored (`{ lang: _lang }`), so the language toggle re-renders correctly.\n- **Liveness:** none. Single on-mount fetch; no polling/websocket. Everything\n  downstream sees a frozen snapshot until an add or a navigation.\n- **Anti-patterns / scrutiny (per task brief):**\n  - **Whole-page island:** acceptable here but means no SSR content + a skeleton\n    flash on every visit. If the rollup ever moves server-side, prefer SSR-ing\n    the first `cards` payload through the route and hydrating from it.\n  - **Stale analytics rails after mutation** (above) — the concrete\n    frozen-prop bug to flag and fix.\n  - **Add-Client modal is 100% inline-styled** with hard-coded hex fallbacks\n    (`#144852`, `#6b7560`, `#d8dcd5`, `#a83b3b`) bypassing the token system —\n    lift into real classes on rebuild (see `css/clients-page.css`).\n- **Data-shape hazards:** inherits data-model.md hazard #3 (`/clients` is N\n  per-customer rollups) and #6 (filter/status counts). The three fetches fan out\n  three separate whole-account/per-customer aggregate reads on each mount.\n\n## 2. Anatomy\n```\nClientsPage (no own wrapper)\n├─ loading → <ShimmerStyle/> <PageHeaderSkeleton/> <CardGridSkeleton rows={3}/>   [shared Skeletons]\n├─ error   → <div class=\"cpage-error\">{loadError}</div>                            [UNSTYLED — flag]\n└─ ready   → <>\n     <ClientsHero totalClients activeJobs owedTotal quietCount lang onAdd/>        [ClientsSections]\n     <LoopBar picks={pickLoop(cards)} lang/>                                       [ClientsSections]\n     <ClientsBoard cards lang>                                                     [ClientsBoard island]\n       <TopClients rows={top.results} lang/>                                       [ClientsSections, rail]\n       <ClientsSegments rows={segments.segments} lang/>                            [ClientsSections, rail]\n     </ClientsBoard>\n     {addOpen && <div overlay onClick=close>                                       [INLINE modal]\n        <form onSubmit=submitAddClient>\n          <h2>Add a client</h2>\n          <label>Name <input autoFocus required/></label>\n          <label>Business name <input/></label>\n          <label>Phone <input type=tel/></label>\n          <label>Email <input type=email/></label>\n          {addErr && <p role=alert>{addErr}</p>}\n          <div actions> <button cancel/> <button submit disabled={!name||adding}>{Add|…}</button> </div>\n        </form></div>}\n   </>\n```\n- **Composition (build order):** Skeletons gate → Hero → LoopBar → Board(rail\n  children) → modal portal.\n- **Derived in island:** `activeJobs = cards.filter(status===\"active\").length`;\n  `owedTotal = Σ(balanceCents>0)/100` (DOLLARS); `quietCount =\n  cards.filter(status===\"cold\").length`; `loopPicks = pickLoop(cards)` (lead/owes,\n  sorted by daysSinceContact desc, top 3).\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `\"en\" \\| \"es\"` | (ignored — reads `langSignal.value`) | select | reads `langSignal` |\n\nThe island takes essentially no real props (the route mounts `<ClientsPage />`\nwith none). `lang` in the signature is dead (`_lang`).\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | on-mount skeletons | `cases/loading/loading.json` |\n| ready | full board with seed roster | `cases/ready/ready.json` |\n| error | fetch threw → `.cpage-error` | `cases/error/error.json` |\n| add-modal | Add-Client modal open over the board | `cases/add-modal/add-modal.json` |\n| empty | fetch returned `[]` everywhere → empty hero/board | `cases/empty/empty.json` |\n\nBecause this island fetches on mount, isolate cases drive the rendered output via\n`_signals` (loading/error/addOpen) and `_mocks` (the three client methods) rather\nthan props.\n\n## 5. Events\n- `capture(page)`: `e.source === \"button.ph2__cta\" && e.type === \"click\"` →\n  `onAdd` → `setAddOpen(true)` (opens modal).\n- `capture(page)`: `e.source === \"form\" (modal) && e.type === \"submit\"` →\n  `submitAddClient` → `POST /customers` → on success refetch `/clients`.\n- `capture(page)`: `e.source === \"div\" (overlay) && e.type === \"click\"` →\n  close modal (unless `adding`).\n- `capture(page)`: `e.source === \"button\" (cancel) && e.type === \"click\"` →\n  close modal.\n- Field inputs (`onInput`) update the four add-field signals.\n\n## 6. Motion\n- The island contributes no keyframes of its own. The modal appears/disappears by\n  conditional mount (no transition in source — it pops in instantly). The visible\n  motion all comes from its children (`ClientsSections`/`ClientsBoard` keyframes:\n  `ph2-pulse`, `pulse-dot`, panel slide, the dead `ccard2-editorial-in`).\n- **Reduced motion:** nothing island-local; relies on the global clamp via the\n  children.\n\n## 7. Responsive\n- No own `@media`. Layout responsiveness is entirely the children's\n  (`.ph2`, `.loopbar`, `.clay2`, `.ccards2` at 1100px / 768px). The inline modal\n  uses `padding:20px` on the scrim + `max-width:440px; width:100%` so it's\n  fluid on small screens.\n\n## 8. A11y\n- Modal error uses `role=\"alert\"` — good.\n- **Gaps:** the modal is a plain `<div>`/`<form>`, **not** `role=\"dialog\"` /\n  `aria-modal`; no focus trap (only `autoFocus` on the name field); Escape does\n  NOT close it (only overlay click / Cancel). Skeleton/error states have no\n  `aria-live`. Rebuild: promote the modal to a proper dialog with focus\n  management + Escape, and announce load/error.\n\n## 9. Used on\n- `/clients` only. Mounted by `routes/clients/index.tsx` as `<ClientsPage />`\n  inside `.content`. Sole importer. It in turn owns the only mounts of\n  `ClientsBoard` and `ClientsSections`.\n", "fixture": "{\n  \"category\": \"clients\",\n  \"folder\": \"clients-page\",\n  \"controls\": {}\n}\n", "cases": [{"name": "add-modal.json", "content": "{\n  \"_name\": \"Add-Client modal open over the board — inline-styled scrim + white card (Name required + autoFocus, Business name, Phone tel, Email), Cancel + Add. Submit disabled until name non-empty.\",\n  \"_mocks\": {\n    \"clientsClient.list\": [\n      { \"id\": \"cus_maplegrove\", \"userId\": \"u_demo\", \"name\": \"Maple Grove Apartments\", \"businessName\": \"Maple Grove Apartments\", \"segment\": \"property_mgmt\", \"vip\": true, \"createdAt\": \"2024-08-01T00:00:00Z\", \"updatedAt\": \"2026-06-18T00:00:00Z\", \"lastWhen\": \"2026-06-18T00:00:00Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 420000, \"balanceSub\": \"Owes you\", \"activeJobs\": 2, \"jobsSub\": \"2 active jobs\", \"status\": \"active\", \"temp\": 92, \"daysSinceContact\": 0, \"revenue12moCents\": 2480000 },\n      { \"id\": \"cus_sarahchen\", \"userId\": \"u_demo\", \"name\": \"Sarah Chen\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-01-01T00:00:00Z\", \"updatedAt\": \"2026-06-15T00:00:00Z\", \"lastWhen\": \"2026-06-15T00:00:00Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Owes you\", \"activeJobs\": 1, \"jobsSub\": \"1 job\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 0 }\n    ],\n    \"clientsClient.top\": { \"results\": [] },\n    \"clientsClient.segments\": { \"segments\": [] }\n  },\n  \"_signals\": {\n    \"addOpen\": true,\n    \"adding\": false,\n    \"addErr\": null,\n    \"addName\": \"Delgado Residence\",\n    \"addBusiness\": \"\",\n    \"addPhone\": \"(415) 555-0200\",\n    \"addEmail\": \"maria.delgado@gmail.com\"\n  },\n  \"_note\": \"Modal is a bare <div>/<form> (NOT role=dialog, no focus trap, Escape does not close — flagged in §8). With addName filled the green Add submit is enabled. Set addErr to a string + adding:false to capture the inline role='alert' error; set adding:true to capture the '…' submitting label with both buttons disabled.\"\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — all three fetches returned empty ([] / no results / no segments); hero + board + rails render their honest-empty branches (zero counts, no client cards, no leaderboard, no segment mix).\",\n  \"_mocks\": {\n    \"clientsClient.list\": [],\n    \"clientsClient.top\": { \"results\": [] },\n    \"clientsClient.segments\": { \"segments\": [] }\n  },\n  \"_signals\": { \"addOpen\": false },\n  \"_note\": \"Derived counts all collapse to 0: totalClients=0, activeJobs=0, owedTotal=0, quietCount=0, loopPicks=[]. This is the brand-new-account first run. The 'Add a client' CTA (button.ph2__cta) is the only meaningful affordance.\"\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — the on-mount Promise.all rejected at the .then/.catch boundary; renders <div class='cpage-error'> with clientsPage.loadError. UNSTYLED in source (no rule in static/*.css) — FLAG.\",\n  \"_signals\": {\n    \"s\": { \"loading\": false, \"error\": \"Network request failed\", \"cards\": [], \"top\": { \"results\": [] }, \"segments\": { \"segments\": [] } },\n    \"addOpen\": false\n  },\n  \"_note\": \"Each individual fetch is .catch-guarded to a benign empty, so this branch only fires if the outer .then itself throws (e.g. setS or a render-time exception). Drive in isolate via the error signal directly. The rendered text is the literal tFor(lang,'clientsPage.loadError',{error}) string.\"\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — on-mount skeletons (ShimmerStyle + PageHeaderSkeleton + CardGridSkeleton rows=3). First paint before the three fetches resolve.\",\n  \"_signals\": {\n    \"s\": { \"loading\": true, \"error\": null, \"cards\": [], \"top\": { \"results\": [] }, \"segments\": { \"segments\": [] } },\n    \"addOpen\": false\n  },\n  \"_note\": \"The route SSRs no page body, so this skeleton is the literal first paint on every /clients visit. Drive by stalling the three /clients + /analytics/clients/* mocks (never resolving).\"\n}\n"}, {"name": "ready.json", "content": "{\n  \"_name\": \"Ready — full editorial board off the 12-client seed roster (lib/clients-seed.ts → CustomerCard[]). Hero counts derive from cards; LoopBar picks lead/owes by daysSinceContact desc top 3; board + Top-clients + Segments rails.\",\n  \"_mocks\": {\n    \"clientsClient.list\": [\n      { \"id\": \"cus_greenleaf\", \"userId\": \"u_demo\", \"name\": \"Greenleaf HOA\", \"businessName\": \"Greenleaf HOA\", \"email\": \"board@greenleaf-hoa.org\", \"phoneNumber\": \"(415) 555-0145\", \"segment\": \"hoa\", \"vip\": true, \"createdAt\": \"2024-06-01T00:00:00Z\", \"updatedAt\": \"2026-06-16T00:00:00Z\", \"lastWhen\": \"2026-06-16T00:00:00Z\", \"lastWhenRel\": \"2 days ago\", \"lastTone\": \"warm\", \"balanceCents\": 0, \"balanceSub\": \"Paid in full\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"regular\", \"temp\": 78, \"daysSinceContact\": 2, \"revenue12moCents\": 3240000 },\n      { \"id\": \"cus_maplegrove\", \"userId\": \"u_demo\", \"name\": \"Maple Grove Apartments\", \"businessName\": \"Maple Grove Apartments\", \"email\": \"janet@maplegrove.co\", \"phoneNumber\": \"(415) 555-0124\", \"segment\": \"property_mgmt\", \"vip\": true, \"createdAt\": \"2024-08-01T00:00:00Z\", \"updatedAt\": \"2026-06-18T00:00:00Z\", \"lastWhen\": \"2026-06-18T00:00:00Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 420000, \"balanceSub\": \"Owes you\", \"activeJobs\": 2, \"jobsSub\": \"2 active jobs\", \"status\": \"active\", \"temp\": 92, \"daysSinceContact\": 0, \"revenue12moCents\": 2480000 },\n      { \"id\": \"cus_cobblestone\", \"userId\": \"u_demo\", \"name\": \"Cobblestone Cafe\", \"businessName\": \"Cobblestone Cafe\", \"email\": \"hello@cobblestone.cafe\", \"phoneNumber\": \"(415) 555-0117\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2024-09-01T00:00:00Z\", \"updatedAt\": \"2026-06-17T00:00:00Z\", \"lastWhen\": \"2026-06-17T00:00:00Z\", \"lastWhenRel\": \"1 day ago\", \"lastTone\": \"hot\", \"balanceCents\": 0, \"balanceSub\": \"Paid in full\", \"activeJobs\": 0, \"jobsSub\": \"1 job\", \"status\": \"regular\", \"temp\": 71, \"daysSinceContact\": 1, \"revenue12moCents\": 1592000 },\n      { \"id\": \"cus_marshall\", \"userId\": \"u_demo\", \"name\": \"Marshall & Sons\", \"businessName\": \"Marshall & Sons\", \"email\": \"mike@marshall-sons.com\", \"phoneNumber\": \"(415) 555-0166\", \"segment\": \"property_mgmt\", \"vip\": false, \"createdAt\": \"2024-10-01T00:00:00Z\", \"updatedAt\": \"2026-06-14T00:00:00Z\", \"lastWhen\": \"2026-06-14T00:00:00Z\", \"lastWhenRel\": \"4 days ago\", \"lastTone\": \"warm\", \"balanceCents\": -50000, \"balanceSub\": \"Credit on file\", \"activeJobs\": 1, \"jobsSub\": \"1 active job\", \"status\": \"active\", \"temp\": 64, \"daysSinceContact\": 4, \"revenue12moCents\": 1145000 },\n      { \"id\": \"cus_sarahchen\", \"userId\": \"u_demo\", \"name\": \"Sarah Chen\", \"email\": \"sarah.chen@gmail.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-01-01T00:00:00Z\", \"updatedAt\": \"2026-06-15T00:00:00Z\", \"lastWhen\": \"2026-06-15T00:00:00Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Owes you\", \"activeJobs\": 1, \"jobsSub\": \"1 job\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_hilltop\", \"userId\": \"u_demo\", \"name\": \"Hilltop Diner\", \"businessName\": \"Hilltop Diner\", \"email\": \"manager@hilltopdiner.com\", \"phoneNumber\": \"(415) 555-0102\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2024-05-01T00:00:00Z\", \"updatedAt\": \"2026-06-07T00:00:00Z\", \"lastWhen\": \"2026-06-07T00:00:00Z\", \"lastWhenRel\": \"11 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 116000, \"balanceSub\": \"Owes you\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"owes\", \"temp\": 22, \"daysSinceContact\": 11, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_kowalski\", \"userId\": \"u_demo\", \"name\": \"Tom & Linda Kowalski\", \"email\": \"linda.k@outlook.com\", \"phoneNumber\": \"(415) 555-0193\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2026-06-18T00:00:00Z\", \"updatedAt\": \"2026-06-18T00:00:00Z\", \"lastWhen\": \"2026-06-18T00:00:00Z\", \"lastWhenRel\": \"today\", \"lastTone\": \"hot\", \"balanceCents\": 0, \"balanceSub\": \"New lead\", \"activeJobs\": 0, \"jobsSub\": \"No jobs yet\", \"status\": \"lead\", \"temp\": 86, \"daysSinceContact\": 0, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_janapatel\", \"userId\": \"u_demo\", \"name\": \"Jana Patel\", \"email\": \"jana.patel@hey.com\", \"phoneNumber\": \"(415) 555-0142\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-03-01T00:00:00Z\", \"updatedAt\": \"2026-06-13T00:00:00Z\", \"lastWhen\": \"2026-06-13T00:00:00Z\", \"lastWhenRel\": \"5 days ago\", \"lastTone\": \"warm\", \"balanceCents\": -82500, \"balanceSub\": \"Credit on file\", \"activeJobs\": 1, \"jobsSub\": \"1 active job\", \"status\": \"active\", \"temp\": 68, \"daysSinceContact\": 5, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_marcuslin\", \"userId\": \"u_demo\", \"name\": \"Marcus Lin\", \"email\": \"marcus.lin@gmail.com\", \"phoneNumber\": \"(415) 555-0151\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2026-06-17T00:00:00Z\", \"updatedAt\": \"2026-06-17T00:00:00Z\", \"lastWhen\": \"2026-06-17T00:00:00Z\", \"lastWhenRel\": \"1 day ago\", \"lastTone\": \"warm\", \"balanceCents\": 0, \"balanceSub\": \"New lead\", \"activeJobs\": 0, \"jobsSub\": \"No jobs yet\", \"status\": \"lead\", \"temp\": 52, \"daysSinceContact\": 1, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_bayside\", \"userId\": \"u_demo\", \"name\": \"Bayside Properties\", \"businessName\": \"Bayside Properties\", \"email\": \"ops@bayside-pm.com\", \"phoneNumber\": \"(415) 555-0179\", \"segment\": \"property_mgmt\", \"vip\": false, \"createdAt\": \"2024-03-01T00:00:00Z\", \"updatedAt\": \"2026-06-14T00:00:00Z\", \"lastWhen\": \"2026-06-14T00:00:00Z\", \"lastWhenRel\": \"4 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 0, \"balanceSub\": \"Paid in full\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"cold\", \"temp\": 28, \"daysSinceContact\": 4, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_riverside\", \"userId\": \"u_demo\", \"name\": \"Riverside Yoga\", \"businessName\": \"Riverside Yoga\", \"email\": \"studio@riversideyoga.com\", \"phoneNumber\": \"(415) 555-0188\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2024-02-01T00:00:00Z\", \"updatedAt\": \"2026-05-07T00:00:00Z\", \"lastWhen\": \"2026-05-07T00:00:00Z\", \"lastWhenRel\": \"42 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 0, \"balanceSub\": \"Paid in full\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"cold\", \"temp\": 24, \"daysSinceContact\": 42, \"revenue12moCents\": 0 },\n      { \"id\": \"cus_ortega\", \"userId\": \"u_demo\", \"name\": \"Ortega Rentals\", \"businessName\": \"Ortega Rentals\", \"email\": \"mateo@ortegarentals.com\", \"phoneNumber\": \"(415) 555-0163\", \"segment\": \"property_mgmt\", \"vip\": false, \"createdAt\": \"2024-01-01T00:00:00Z\", \"updatedAt\": \"2026-04-18T00:00:00Z\", \"lastWhen\": \"2026-04-18T00:00:00Z\", \"lastWhenRel\": \"61 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 0, \"balanceSub\": \"Paid in full\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"cold\", \"temp\": 18, \"daysSinceContact\": 61, \"revenue12moCents\": 1872000 }\n    ],\n    \"clientsClient.top\": {\n      \"results\": [\n        { \"customerId\": \"cus_greenleaf\", \"name\": \"Greenleaf HOA\", \"revenue12moCents\": 3240000, \"rank\": 1, \"barPct\": 100 },\n        { \"customerId\": \"cus_maplegrove\", \"name\": \"Maple Grove Apartments\", \"revenue12moCents\": 2480000, \"rank\": 2, \"barPct\": 76 },\n        { \"customerId\": \"cus_ortega\", \"name\": \"Ortega Rentals\", \"revenue12moCents\": 1872000, \"rank\": 3, \"barPct\": 58 },\n        { \"customerId\": \"cus_cobblestone\", \"name\": \"Cobblestone Cafe\", \"revenue12moCents\": 1592000, \"rank\": 4, \"barPct\": 49 },\n        { \"customerId\": \"cus_marshall\", \"name\": \"Marshall & Sons\", \"revenue12moCents\": 1145000, \"rank\": 5, \"barPct\": 35 }\n      ]\n    },\n    \"clientsClient.segments\": {\n      \"segments\": [\n        { \"key\": \"property_mgmt\", \"label\": \"Property mgmt\", \"count\": 4, \"pct\": 80 },\n        { \"key\": \"homeowner\", \"label\": \"Homeowners\", \"count\": 5, \"pct\": 60 },\n        { \"key\": \"small_biz\", \"label\": \"Small biz\", \"count\": 2, \"pct\": 40 },\n        { \"key\": \"hoa\", \"label\": \"HOAs\", \"count\": 1, \"pct\": 18 }\n      ]\n    }\n  },\n  \"_signals\": { \"addOpen\": false },\n  \"_note\": \"Derived in-island from cards: totalClients=12, activeJobs=3 (status==='active'), owedTotal = (420000+192000+116000)/100 = $7,280 (only balanceCents>0), quietCount=4 (status==='cold'). LoopBar picks = lead/owes sorted by daysSinceContact desc, top 3 → Hilltop Diner(11), Jana Patel? no (active) → Sarah Chen(3)/Marcus Lin(1 lead)/Tom & Linda(0 lead): actually owes/lead members are Hilltop(11,owes), Sarah(3,owes), Marcus(1,lead), Tom&Linda(0,lead) → top 3 by days desc = Hilltop, Sarah, Marcus.\"\n}\n"}], "css": "/* ============================================================\n   ClientsPage — the top-level /clients data island.\n   It owns NO dedicated CSS classes in static/clients.css:\n     - The loading branch uses the shared Skeletons (ShimmerStyle +\n       PageHeaderSkeleton + CardGridSkeleton) — styled by the shared\n       Skeletons component (see shared-components/skeletons/).\n     - The error branch emits `<div class=\"cpage-error\">…</div>` which has\n       NO rule anywhere in static/*.css — it renders as unstyled flow text.\n       FLAG: give it a real error style on rebuild (token --danger / --danger-bg).\n     - The Add-Client modal is rendered with 100% INLINE styles (no classes)\n       directly in islands/ClientsPage.tsx (the overlay scrim, the white\n       card, every field/label/button). Those literals are reproduced below\n       as a reference block — NOT a stylesheet the app links. On rebuild,\n       lift them into real classes backed by tokens.\n   The hero/loop/board/rail visuals all live in their own component CSS\n   (clients-sections.css, clients-board.css). This file is essentially a\n   marker that the island contributes only inline + skeleton styling.\n   ============================================================ */\n\n/* ---- error branch (currently UNSTYLED in source) ---- */\n.cpage-error {\n  /* No source rule. Suggested rebuild target: */\n  /* color: var(--danger); background: var(--danger-bg);\n     padding: 14px 16px; border-radius: var(--radius-md);\n     font-weight: 600; */\n}\n\n/* ---- Add-Client modal — inline-style literals from ClientsPage.tsx ----\n   (reference only; the source applies these via `style=\"…\"` attributes)\n\n   overlay scrim:\n     position:fixed; inset:0; background:rgba(20,40,45,0.45);\n     display:flex; align-items:center; justify-content:center;\n     z-index:1000; padding:20px;\n   modal card:\n     background:#fff; border-radius:16px; padding:24px 26px;\n     max-width:440px; width:100%;\n     box-shadow:0 24px 64px rgba(20,72,82,0.22);\n     display:flex; flex-direction:column; gap:14px;\n   title (h2):\n     margin:0; font-size:20px; font-weight:800; color:var(--fg,#144852);\n   label:\n     display:flex; flex-direction:column; gap:5px; font-size:13px;\n     font-weight:700; color:var(--fg-muted,#6b7560);\n   input:\n     padding:11px 13px; border:1px solid var(--border,#d8dcd5);\n     border-radius:10px; font:inherit; font-size:15px; font-weight:400;\n     color:var(--fg);\n   error <p role=alert>:\n     margin:0; color:#a83b3b; font-size:13.5px; font-weight:600;\n   actions row:\n     display:flex; gap:10px; justify-content:flex-end; margin-top:6px;\n   cancel button (ghost):\n     padding:10px 16px; border:0; background:transparent;\n     color:var(--fg-muted,#6b7560); font:inherit; font-weight:700;\n     cursor:pointer; border-radius:10px;\n   submit button (solid green primary):\n     padding:10px 20px; border:0; border-radius:10px;\n     background:var(--brand-green,#519843); color:#fff; font:inherit;\n     font-weight:800; cursor:pointer; opacity:1;\n*/\n", "cssName": "clients-page.css", "source": "/**\n * Top-level data island for /clients. The SSR route renders only the page\n * shell (sidebar + topbar); this island fetches the full payload from the\n * backend on mount and renders the editorial board.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport {\n  clientsClient,\n  type ClientSegmentsResponse,\n  type CustomerCard,\n  type TopClientsResponse,\n} from \"../clients/clients.ts\";\nimport {\n  ClientsHero,\n  ClientsSegments,\n  LoopBar,\n  TopClients,\n} from \"../components/ClientsSections.tsx\";\nimport ClientsBoard from \"./ClientsBoard.tsx\";\nimport {\n  CardGridSkeleton,\n  PageHeaderSkeleton,\n  ShimmerStyle,\n} from \"../components/Skeletons.tsx\";\nimport { langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  cards: CustomerCard[];\n  top: TopClientsResponse;\n  segments: ClientSegmentsResponse;\n}\n\nconst INITIAL: State = {\n  loading: true,\n  error: null,\n  cards: [],\n  top: { results: [] },\n  segments: { segments: [] },\n};\n\nfunction pickLoop(cards: CustomerCard[]): CustomerCard[] {\n  const candidates = cards.filter((c) =>\n    c.status === \"lead\" || c.status === \"owes\"\n  );\n  candidates.sort((a, b) => b.daysSinceContact - a.daysSinceContact);\n  return candidates.slice(0, 3);\n}\n\nexport default function ClientsPage({ lang: _lang }: { lang?: \"en\" | \"es\" }) {\n  const lang = langSignal.value;\n  const [s, setS] = useState<State>(INITIAL);\n  const [addOpen, setAddOpen] = useState(false);\n  const [adding, setAdding] = useState(false);\n  const [addErr, setAddErr] = useState<string | null>(null);\n  const [addName, setAddName] = useState(\"\");\n  const [addBusiness, setAddBusiness] = useState(\"\");\n  const [addPhone, setAddPhone] = useState(\"\");\n  const [addEmail, setAddEmail] = useState(\"\");\n\n  async function refreshClients() {\n    const cards = await clientsClient.list().catch(() => null);\n    if (cards) setS((prev) => ({ ...prev, cards }));\n  }\n\n  async function submitAddClient(e: Event) {\n    e.preventDefault();\n    if (!addName.trim() || adding) return;\n    setAdding(true);\n    setAddErr(null);\n    try {\n      await clientsClient.create({\n        name: addName.trim(),\n        ...(addBusiness.trim() ? { businessName: addBusiness.trim() } : {}),\n        ...(addPhone.trim() ? { phoneNumber: addPhone.trim() } : {}),\n        ...(addEmail.trim() ? { email: addEmail.trim() } : {}),\n      });\n      setAddName(\"\");\n      setAddBusiness(\"\");\n      setAddPhone(\"\");\n      setAddEmail(\"\");\n      setAddOpen(false);\n      await refreshClients();\n    } catch (err) {\n      setAddErr(err instanceof Error ? err.message : \"Could not add client\");\n    } finally {\n      setAdding(false);\n    }\n  }\n\n  useEffect(() => {\n    let alive = true;\n    Promise.all([\n      clientsClient.list().catch(() => [] as CustomerCard[]),\n      clientsClient.top(5).catch(() => ({ results: [] } as TopClientsResponse)),\n      clientsClient.segments().catch(\n        () => ({ segments: [] } as ClientSegmentsResponse),\n      ),\n    ]).then(([cards, top, segments]) => {\n      if (!alive) return;\n      setS({ loading: false, error: null, cards, top, segments });\n    }).catch((err: Error) => {\n      if (!alive) return;\n      setS({ ...INITIAL, loading: false, error: err.message });\n    });\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  if (s.loading) {\n    return (\n      <>\n        <ShimmerStyle />\n        <PageHeaderSkeleton />\n        <CardGridSkeleton rows={3} />\n      </>\n    );\n  }\n  if (s.error) {\n    return (\n      <div class=\"cpage-error\">\n        {tFor(lang, \"clientsPage.loadError\", { error: s.error })}\n      </div>\n    );\n  }\n\n  const { cards: rawCards, top, segments } = s;\n  const cards = Array.isArray(rawCards) ? rawCards : [];\n  const activeJobs = cards.filter((c) => c.status === \"active\").length;\n  const owedTotal = cards.reduce(\n    (sum, c) => sum + (c.balanceCents > 0 ? c.balanceCents : 0),\n    0,\n  ) / 100;\n  const quietCount = cards.filter((c) => c.status === \"cold\").length;\n  const loopPicks = pickLoop(cards);\n\n  return (\n    <>\n      <ClientsHero\n        totalClients={cards.length}\n        activeJobs={activeJobs}\n        owedTotal={owedTotal}\n        quietCount={quietCount}\n        lang={lang}\n        onAdd={() => {\n          setAddErr(null);\n          setAddOpen(true);\n        }}\n      />\n      <LoopBar picks={loopPicks} lang={lang} />\n      <ClientsBoard cards={cards} lang={lang}>\n        <TopClients rows={top.results} lang={lang} />\n        <ClientsSegments rows={segments.segments} lang={lang} />\n      </ClientsBoard>\n\n      {addOpen && (\n        <div\n          onClick={() => !adding && setAddOpen(false)}\n          style=\"position:fixed;inset:0;background:rgba(20,40,45,0.45);display:flex;align-items:center;justify-content:center;z-index:1000;padding:20px\"\n        >\n          <form\n            onClick={(e) => e.stopPropagation()}\n            onSubmit={submitAddClient}\n            style=\"background:#fff;border-radius:16px;padding:24px 26px;max-width:440px;width:100%;box-shadow:0 24px 64px rgba(20,72,82,0.22);display:flex;flex-direction:column;gap:14px\"\n          >\n            <h2 style=\"margin:0;font-size:20px;font-weight:800;color:var(--fg,#144852)\">\n              {tFor(lang, \"clientsHero.addClient\")}\n            </h2>\n            <label style=\"display:flex;flex-direction:column;gap:5px;font-size:13px;font-weight:700;color:var(--fg-muted,#6b7560)\">\n              {tFor(lang, \"settings.name\")}\n              <input\n                type=\"text\"\n                autoFocus\n                required\n                value={addName}\n                onInput={(e) => setAddName((e.target as HTMLInputElement).value)}\n                style=\"padding:11px 13px;border:1px solid var(--border,#d8dcd5);border-radius:10px;font:inherit;font-size:15px;font-weight:400;color:var(--fg)\"\n              />\n            </label>\n            <label style=\"display:flex;flex-direction:column;gap:5px;font-size:13px;font-weight:700;color:var(--fg-muted,#6b7560)\">\n              {tFor(lang, \"clientsPage.businessName\")}\n              <input\n                type=\"text\"\n                value={addBusiness}\n                onInput={(e) =>\n                  setAddBusiness((e.target as HTMLInputElement).value)}\n                style=\"padding:11px 13px;border:1px solid var(--border,#d8dcd5);border-radius:10px;font:inherit;font-size:15px;font-weight:400;color:var(--fg)\"\n              />\n            </label>\n            <label style=\"display:flex;flex-direction:column;gap:5px;font-size:13px;font-weight:700;color:var(--fg-muted,#6b7560)\">\n              {tFor(lang, \"settings.phone\")}\n              <input\n                type=\"tel\"\n                value={addPhone}\n                onInput={(e) =>\n                  setAddPhone((e.target as HTMLInputElement).value)}\n                style=\"padding:11px 13px;border:1px solid var(--border,#d8dcd5);border-radius:10px;font:inherit;font-size:15px;font-weight:400;color:var(--fg)\"\n              />\n            </label>\n            <label style=\"display:flex;flex-direction:column;gap:5px;font-size:13px;font-weight:700;color:var(--fg-muted,#6b7560)\">\n              {tFor(lang, \"settings.email\")}\n              <input\n                type=\"email\"\n                value={addEmail}\n                onInput={(e) =>\n                  setAddEmail((e.target as HTMLInputElement).value)}\n                style=\"padding:11px 13px;border:1px solid var(--border,#d8dcd5);border-radius:10px;font:inherit;font-size:15px;font-weight:400;color:var(--fg)\"\n              />\n            </label>\n            {addErr && (\n              <p\n                role=\"alert\"\n                style=\"margin:0;color:#a83b3b;font-size:13.5px;font-weight:600\"\n              >\n                {addErr}\n              </p>\n            )}\n            <div style=\"display:flex;gap:10px;justify-content:flex-end;margin-top:6px\">\n              <button\n                type=\"button\"\n                disabled={adding}\n                onClick={() => setAddOpen(false)}\n                style=\"padding:10px 16px;border:0;background:transparent;color:var(--fg-muted,#6b7560);font:inherit;font-weight:700;cursor:pointer;border-radius:10px\"\n              >\n                {tFor(lang, \"common.cancel\")}\n              </button>\n              <button\n                type=\"submit\"\n                disabled={adding || !addName.trim()}\n                style=\"padding:10px 20px;border:0;border-radius:10px;background:var(--brand-green,#519843);color:#fff;font:inherit;font-weight:800;cursor:pointer;opacity:1\"\n              >\n                {adding ? \"…\" : tFor(lang, \"common.add\")}\n              </button>\n            </div>\n          </form>\n        </div>\n      )}\n    </>\n  );\n}\n", "sourceName": "ClientsPage.tsx", "checklist": "# Capture checklist — ClientsPage\n\n- **Route:** `/clients` (auth-gated — dev master OTP `000000`).\n- **Viewports** (clients.css real `@media`): 390, 768, 980, 1280. Shell drawer\n  cutover at 640/641.\n- **Crop targets:** the `.content` island; the ClientsSections analytics rails\n  (top clients, segments); the ClientsBoard rows; the Add-client modal overlay.\n- **States to drive:**\n  - `loading` — skeletons (initial `Promise.all`).\n  - `ready` — populated from `lib/clients-seed.ts` (12 customers; derived\n    status/segment/lastTone via `lib/clients-display.ts`).\n  - `empty` — no customers.\n  - `error` — only if the outer `.then` throws (each fetch is `.catch`-guarded).\n  - `add-modal` — open the inline Add-client modal (note: not `role=dialog`, no\n    focus trap, Esc doesn't close — capture for the a11y rebuild).\n- **Theme:** light only. **No fabricated screenshots** — capture against a seeded dev backend.\n"}, {"name": "clients-sections", "component": "ClientsSections", "spec": "# ClientsSections\n\nFour pure-presentation sections for `/clients`, exported from one module\n(`components/ClientsSections.tsx`): `ClientsHero` (editorial page header + Add\nCTA), `LoopBar` (dark \"Today's loop\" ribbon), `TopClients` (right-rail\nleaderboard), `ClientsSegments` (right-rail segment-mix bars). They render\nbackend data passed in as props — no fetch, no state.\n\n## 1. Classification & behavior\n- **Bucket:** `page-composition` — these live in `components/` (not `islands/`),\n  are imported by the `ClientsPage` island, and are server-renderable pure\n  functions. The module header says so: *\"Pure presentation — feed them backend\n  data.\"*\n- **Interaction tier:** `static` for three of them (no state/events). The one\n  exception is **`ClientsHero`'s `onAdd` callback** — it is a closure created\n  *inside* the `ClientsPage` island and passed down, so it hydrates with that\n  island (not a cross-island prop). The button itself is plain markup; the\n  behavior (open the add-client modal) belongs to the parent island.\n- **Server action / flash / PRG:** none. No forms here (the add-client form is\n  the parent island's inline modal).\n- **Data source per region + honest-empty:**\n  - `ClientsHero` — derived counts (`totalClients`, `activeJobs`, `owedTotal`,\n    `quietCount`) computed in the parent island from `CustomerCard[]`. **Honest\n    empty:** `totalClients === 0` swaps to a distinct \"Let's add your *first\n    client*\" headline + sub.\n  - `LoopBar` — `picks: CustomerCard[]` (top 3 lead/owes by days-since-contact,\n    chosen by the parent's `pickLoop`). **Honest empty:** `picks.length === 0`\n    renders a \"No check-ins drafted yet\" ribbon with an \"Open the assistant\" CTA.\n  - `TopClients` — `rows: TopClient[]` from `/analytics/clients/top`. **Honest\n    empty:** `rows.length === 0` → \"No paid invoices in the last year yet.\"\n  - `ClientsSegments` — `rows: ClientSegmentRow[]` from\n    `/analytics/clients/segments`. **Honest empty:** `rows.length === 0` → \"No\n    clients yet.\"\n- **Liveness:** none (no polling/websocket). Props are a frozen snapshot from\n  the parent's on-mount fetch.\n- **Anti-patterns:** none. No `location.reload`, no fetch, no frozen-SSR-prop\n  trap (these are children of the island, re-rendered when the parent's state\n  updates).\n- **Data-shape hazards:**\n  - `TopClient.revenue12moCents` and `ClientSegmentRow` are **separate\n    whole-account aggregates** from the per-customer `/clients` rollup — two\n    extra analytics scans per page load (data-model.md hazard #3). `barPct` /\n    `pct` arrive precomputed (no client math beyond `width:${pct}%`).\n  - `ClientsHero.owedTotal` is passed as **dollars** (the parent divides\n    `balanceCents / 100` before passing) — note the unit break from the\n    cents-everywhere convention; `TopClients` instead receives raw cents and\n    formats via `dollars()`. Keep the boundary straight on rebuild.\n  - `numberWord(totalClients)` only has words 0–20 (`clients-display.ts`); above\n    20 it falls back to digits — the editorial \"*twelve* people\" headline\n    degrades gracefully.\n  - `LoopBar` avatar gradients cycle a fixed 3-entry palette (`LOOP_AV_BG`) — by\n    design `picks` is capped at 3.\n\n## 2. Anatomy\n```\nClientsHero → <div class=\"ph2\">\n  <div>\n    <div class=\"ph2__crumb\"><dot/> Clients · {n} on the books</div>\n    {empty\n      ? <h1 class=\"ph2__title\">Let's add your <em>first client</em>.<br>They'll keep…</h1>\n        <p class=\"ph2__sub\">Once a quote ships…</p>\n      : <h1 class=\"ph2__title\">The <em>{word} people</em><br>who keep the lights on.</h1>\n        <p class=\"ph2__sub\"><strong>{n} jobs in flight</strong> · <strong>${owed}</strong> currently owed … · <strong>{n} quiet</strong> clients worth a hello.</p>}\n  </div>\n  <button class=\"ph2__cta\" onClick={onAdd}><I plus/> Add a client</button>\n\nLoopBar → <div class=\"loopbar\">\n  <div class=\"loopbar__title\"><span class=\"loopbar__lbl\"><dot/> Today's loop</span>\n    <span class=\"loopbar__h\">{n} friendly check-ins…  | empty heading}</span></div>\n  [<div class=\"loopbar__avs\">{picks.map → .loopbar__av {initials}} <div class=\"loopbar__av-meta\">{names}<br><strong>~{n*30} seconds</strong> to send …</div></div>]\n  <a class=\"loopbar__cta\" href=\"/assistant\"><I send/> Open the loop | Open the assistant</a>\n\nTopClients → <div class=\"ctop2\">\n  <div class=\"ctop2__head\"><div class=\"ctop2__title\">Top of the leaderboard</div><div class=\"ctop2__period\">last 12 mo</div></div>\n  {empty ? <div class=\"ctop2__empty\">…</div>\n         : <div class=\"ctop2__list\">{rows.map → <div class=\"ctop2__item\"><div class=\"ctop2__rank [--1]\">{rank}</div><div class=\"ctop2__name\"/><div class=\"ctop2__amt\">{dollars}</div></div><div class=\"ctop2__bar-wrap\"><div class=\"ctop2__bar\" style=width:{barPct}%/></div>}</div>}\n\nClientsSegments → <div class=\"csegment2\">\n  <div class=\"csegment2__title\">Who's on your books</div>\n  {empty ? <div class=\"csegment2__empty\">No clients yet.</div>\n         : rows.map → <div class=\"cseg2-row\"><div class=\"cseg2-row__lbl\">{plural label}</div><div class=\"cseg2-row__bar\"><div class=\"cseg2-row__fill\" style=width:{pct}%;background:{SEGMENT_COLOR[key]}/></div><div class=\"cseg2-row__num\">{count}</div></div>}\n```\n- **Icon dependency:** `I` + `ICN.{plus,send}` from `lib/dash-icons.tsx`\n  (copied to `js/dash-icons.tsx`).\n- **Helper dependency:** `dollars`, `initialsOf`, `numberWord` from\n  `lib/clients-display.ts` (copied to `js/clients-display.ts`).\n- **`SEGMENT_COLOR` map** (in-file): `property_mgmt`→green, `homeowner`→pink,\n  `small_biz`→teal, `hoa`→coffee-500, `unsorted`→coffee-300.\n\n## 3. Props\n**ClientsHero**\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `totalClients` | number | — | number | no |\n| `activeJobs` | number | — | number | no |\n| `owedTotal` | number (DOLLARS) | — | number | no |\n| `quietCount` | number | — | number | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n| `onAdd` | `() => void` | `undefined` | (Events — island closure) | no |\n\n**LoopBar** — `picks: CustomerCard[]`, `lang`.\n**TopClients** — `rows: TopClient[]`, `lang`.\n**ClientsSegments** — `rows: ClientSegmentRow[]`, `lang`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| hero-populated | non-empty editorial headline + stat sub | `cases/hero-populated/hero-populated.json` |\n| hero-empty | `totalClients=0` → \"first client\" headline | `cases/hero-empty/hero-empty.json` |\n| loopbar-picks | 3 drafted check-ins with avatars | `cases/loopbar-picks/loopbar-picks.json` |\n| loopbar-empty | no check-ins → \"Open the assistant\" | `cases/loopbar-empty/loopbar-empty.json` |\n| topclients-rows | 5 leaderboard rows (rank 1 gold) | `cases/topclients-rows/topclients-rows.json` |\n| segments-rows | 4 segment bars | `cases/segments-rows/segments-rows.json` |\n| es | Spanish UI language | `cases/es/es.json` |\n\nBecause this is a **page-composition** module (multiple exports), the fixture's\n`controls` target one component at a time via `_name`; cases name which export\nthey exercise. The fixture lists all four under `components`.\n\n## 5. Events\n- `ClientsHero`: `capture(page)`: `e.source === \"button.ph2__cta\" && e.type === \"click\"`\n  → calls `onAdd` (parent island opens the add-client modal). Callback prop —\n  not serializable; documented here, not in the isolate fixture.\n- `LoopBar` / `TopClients` / `ClientsSegments`: no JS events; the loop CTAs are\n  plain `<a href=\"/assistant\">` navigations.\n\n## 6. Motion (extracted from clients.css)\n- `.ph2__crumb-dot` and `.loopbar__lbl-dot`: `ph2-pulse 2.4s infinite` (animated\n  expanding box-shadow ring — real keyframe).\n- `.ph2__cta:hover`: `translateY(-2px)` + deeper shadow (`--dur-fast`, bounce).\n- `.loopbar__cta:hover`: `translateY(-1px)` (`--dur-fast`).\n- `.cseg2-row__fill`: `transition: width 1s var(--ease-bounce)` — the segment\n  bars animate their width when `pct` changes (bouncy 1s fill). **Jank note:**\n  animating `width` triggers layout, but on a thin 6px bar with few rows it's\n  negligible; could swap to `transform: scaleX()` for compositor-only animation.\n- **Reduced motion:** no component-local block; relies on the global tokens\n  clamp. Verify pulse + the 1s segment fill go instant.\n\n## 7. Responsive (own @media in clients.css)\n- **`max-width:1100px`:** `.ph2` stacks (`flex-direction:column;\n  align-items:flex-start`); `.loopbar` → single column (`grid-template-columns:\n  1fr`); `.cside2` loses `position:sticky`.\n- **`max-width:768px`:** `.ph2__title` 44px → 28px; `.ph2__cta` full-width\n  centered.\n- `.ctop2` / `.csegment2` have no own breakpoints (they reflow with the rail\n  column collapsing under the cards at 1100px).\n\n## 8. A11y\n- Emits a single `<h1>` (`.ph2__title`) — correct page-level heading; the rail\n  section titles are plain `<div>`s, not headings (consider `<h2>` for\n  \"leaderboard\" / \"Who's on your books\").\n- The Add CTA is a real `<button type=\"button\">` — good.\n- Loop/avatar visuals are decorative; initials are text (readable).\n- Bars (`ctop2__bar`, `cseg2-row__fill`) are purely visual with the numeric\n  value shown alongside — acceptable; no `role=\"progressbar\"`/`aria-valuenow`.\n\n## 9. Used on\n- `/clients` only. Imported by `islands/ClientsPage.tsx`\n  (`ClientsHero`, `LoopBar` rendered directly; `TopClients` + `ClientsSegments`\n  passed as `ClientsBoard` children for the right rail). Evidence: sole importer\n  of `components/ClientsSections.tsx`. CSS in `static/clients.css`.\n", "fixture": "{\n  \"category\": \"clients\",\n  \"folder\": \"clients-sections\",\n  \"components\": [\"ClientsHero\", \"LoopBar\", \"TopClients\", \"ClientsSegments\"],\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"totalClients\": { \"type\": \"number\", \"value\": 12 },\n    \"activeJobs\": { \"type\": \"number\", \"value\": 4 },\n    \"owedTotal\": { \"type\": \"number\", \"value\": 7280 },\n    \"quietCount\": { \"type\": \"number\", \"value\": 3 }\n  }\n}\n", "cases": [{"name": "es.json", "content": "{\n  \"_name\": \"Spanish — ClientsHero populated, localized headline + stats\",\n  \"_component\": \"ClientsHero\",\n  \"lang\": \"es\",\n  \"totalClients\": 12,\n  \"activeJobs\": 4,\n  \"owedTotal\": 7280,\n  \"quietCount\": 3\n}\n"}, {"name": "hero-empty.json", "content": "{\n  \"_name\": \"ClientsHero — empty ('Let's add your first client')\",\n  \"_component\": \"ClientsHero\",\n  \"lang\": \"en\",\n  \"totalClients\": 0,\n  \"activeJobs\": 0,\n  \"owedTotal\": 0,\n  \"quietCount\": 0\n}\n"}, {"name": "hero-populated.json", "content": "{\n  \"_name\": \"ClientsHero — populated (12 clients, 4 jobs, $7,280 owed, 3 quiet)\",\n  \"_component\": \"ClientsHero\",\n  \"lang\": \"en\",\n  \"totalClients\": 12,\n  \"activeJobs\": 4,\n  \"owedTotal\": 7280,\n  \"quietCount\": 3\n}\n"}, {"name": "loopbar-empty.json", "content": "{\n  \"_name\": \"LoopBar — empty ('No check-ins drafted yet')\",\n  \"_component\": \"LoopBar\",\n  \"lang\": \"en\",\n  \"picks\": []\n}\n"}, {"name": "loopbar-picks.json", "content": "{\n  \"_name\": \"LoopBar — 3 drafted check-ins (lead/owes by days-since-contact)\",\n  \"_component\": \"LoopBar\",\n  \"lang\": \"en\",\n  \"picks\": [\n    { \"id\": \"c6\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\", \"segment\": \"small_biz\", \"vip\": false, \"createdAt\": \"2025-04-20T10:00:00.000Z\", \"updatedAt\": \"2026-06-07T10:00:00.000Z\", \"lastWhen\": \"2026-06-07T10:00:00.000Z\", \"lastWhenRel\": \"11 days ago\", \"lastTone\": \"cold\", \"balanceCents\": 116000, \"balanceSub\": \"Past due 14d\", \"activeJobs\": 0, \"jobsSub\": \"No open jobs\", \"status\": \"owes\", \"temp\": 22, \"daysSinceContact\": 11, \"revenue12moCents\": 320000 },\n    { \"id\": \"c9\", \"userId\": \"u1\", \"name\": \"Marcus Lin\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2026-06-17T10:00:00.000Z\", \"updatedAt\": \"2026-06-17T10:00:00.000Z\", \"lastWhen\": \"2026-06-17T10:00:00.000Z\", \"lastWhenRel\": \"yesterday\", \"lastTone\": \"warm\", \"balanceCents\": 0, \"balanceSub\": \"—\", \"activeJobs\": 0, \"jobsSub\": \"Kitchen inquiry\", \"status\": \"lead\", \"temp\": 52, \"daysSinceContact\": 1, \"revenue12moCents\": 0 },\n    { \"id\": \"c5\", \"userId\": \"u1\", \"name\": \"Sarah Chen\", \"segment\": \"homeowner\", \"vip\": false, \"createdAt\": \"2025-04-02T10:00:00.000Z\", \"updatedAt\": \"2026-06-15T10:00:00.000Z\", \"lastWhen\": \"2026-06-15T10:00:00.000Z\", \"lastWhenRel\": \"3 days ago\", \"lastTone\": \"hot\", \"balanceCents\": 192000, \"balanceSub\": \"Deck stain bal.\", \"activeJobs\": 1, \"jobsSub\": \"Deck\", \"status\": \"owes\", \"temp\": 58, \"daysSinceContact\": 3, \"revenue12moCents\": 480000 }\n  ]\n}\n"}, {"name": "segments-rows.json", "content": "{\n  \"_name\": \"ClientsSegments — 4 segment bars (seed SEGMENTS)\",\n  \"_component\": \"ClientsSegments\",\n  \"lang\": \"en\",\n  \"rows\": [\n    { \"key\": \"property_mgmt\", \"label\": \"Property mgmt\", \"count\": 4, \"pct\": 80 },\n    { \"key\": \"homeowner\", \"label\": \"Homeowners\", \"count\": 5, \"pct\": 60 },\n    { \"key\": \"small_biz\", \"label\": \"Small biz\", \"count\": 2, \"pct\": 40 },\n    { \"key\": \"hoa\", \"label\": \"HOAs\", \"count\": 1, \"pct\": 18 }\n  ]\n}\n"}, {"name": "topclients-rows.json", "content": "{\n  \"_name\": \"TopClients — 5 leaderboard rows (seed TOP_CLIENTS; rank 1 gold)\",\n  \"_component\": \"TopClients\",\n  \"lang\": \"en\",\n  \"rows\": [\n    { \"customerId\": \"c1\", \"name\": \"Greenleaf HOA\", \"revenue12moCents\": 3240000, \"rank\": 1, \"barPct\": 100 },\n    { \"customerId\": \"c2\", \"name\": \"Maple Grove Apartments\", \"revenue12moCents\": 2480000, \"rank\": 2, \"barPct\": 76 },\n    { \"customerId\": \"c12\", \"name\": \"Ortega Rentals\", \"revenue12moCents\": 1872000, \"rank\": 3, \"barPct\": 58 },\n    { \"customerId\": \"c3\", \"name\": \"Cobblestone Cafe\", \"revenue12moCents\": 1592000, \"rank\": 4, \"barPct\": 49 },\n    { \"customerId\": \"c4\", \"name\": \"Marshall & Sons\", \"revenue12moCents\": 1145000, \"rank\": 5, \"barPct\": 35 }\n  ]\n}\n"}], "css": "/* ============================================================\n   ClientsSections — extracted from static/clients.css.\n   Owns the four server-rendered presentational sections:\n     ClientsHero      → .ph2*           (editorial page header + Add CTA)\n     LoopBar          → .loopbar*        (dark \"Today's loop\" ribbon)\n     TopClients       → .ctop2*          (right-rail leaderboard)\n     ClientsSegments  → .csegment2 / .cseg2-row*  (right-rail segment mix)\n   Plus the rail container .cside2 (these sections are slotted into\n   ClientsBoard's `.clay2` right column as children).\n\n   Tokens come from /dashboard.css — link it alongside /clients.css.\n   ============================================================ */\n\n/* ============== HERO (ClientsHero) ============== */\n.ph2 {\n  display: flex;\n  align-items: flex-end;\n  justify-content: space-between;\n  gap: 24px;\n  padding: 8px 4px 0;\n  margin-bottom: 24px;\n}\n.ph2__crumb {\n  font-family: var(--font-body);\n  font-size: 12px;\n  color: var(--fg-subtle);\n  letter-spacing: 0.06em;\n  text-transform: uppercase;\n  font-weight: 700;\n  margin-bottom: 10px;\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n}\n.ph2__crumb-dot {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: var(--brand-pink);\n  animation: ph2-pulse 2.4s infinite;\n}\n@keyframes ph2-pulse {\n  0%, 100% {\n    box-shadow: 0 0 0 0 rgba(255, 107, 107, 0.55);\n  }\n  50% {\n    box-shadow: 0 0 0 6px rgba(255, 107, 107, 0);\n  }\n}\n.ph2__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 44px;\n  color: var(--brand-teal);\n  line-height: 1.0;\n  letter-spacing: -0.02em;\n  margin: 0 0 12px;\n  text-wrap: balance;\n}\n.ph2__title em {\n  font-style: normal;\n  color: var(--brand-pink);\n}\n.ph2__sub {\n  color: var(--fg-muted);\n  font-size: 15px;\n  line-height: 1.5;\n  max-width: 520px;\n}\n.ph2__sub strong {\n  color: var(--brand-teal);\n  font-weight: 700;\n}\n.ph2__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  padding: 12px 18px;\n  border-radius: var(--radius-pill);\n  background: var(--brand-pink);\n  color: #fff;\n  border: none;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  cursor: pointer;\n  flex-shrink: 0;\n  box-shadow: 0 8px 18px -6px rgba(255, 107, 107, 0.5);\n  transition:\n    transform var(--dur-fast) var(--ease-bounce),\n    box-shadow var(--dur-fast) var(--ease-out);\n}\n.ph2__cta:hover {\n  transform: translateY(-2px);\n  box-shadow: 0 12px 22px -6px rgba(255, 107, 107, 0.6);\n}\n\n/* ============== LOOP RIBBON (LoopBar) ============== */\n.loopbar {\n  background: linear-gradient(135deg, #0f3a40 0%, #1a535c 50%, #0a2a2f 100%);\n  color: #fff;\n  border-radius: var(--radius-xl);\n  padding: 16px 20px;\n  margin-bottom: 18px;\n  position: relative;\n  overflow: hidden;\n  display: grid;\n  grid-template-columns: auto 1fr auto;\n  gap: 18px;\n  align-items: center;\n}\n.loopbar::before {\n  content: \"\";\n  position: absolute;\n  top: -30px;\n  right: -30px;\n  width: 180px;\n  height: 180px;\n  background: radial-gradient(circle, rgba(95, 163, 79, 0.32), transparent 70%);\n  border-radius: 999px;\n  pointer-events: none;\n}\n.loopbar__title {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n}\n.loopbar__lbl {\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: rgba(255, 255, 255, 0.7);\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n}\n.loopbar__lbl-dot {\n  width: 7px;\n  height: 7px;\n  border-radius: 999px;\n  background: var(--brand-pink);\n  box-shadow: 0 0 0 3px rgba(255, 107, 107, 0.25);\n  animation: ph2-pulse 2.4s infinite;\n}\n.loopbar__h {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 18px;\n  color: #fff;\n  letter-spacing: -0.01em;\n}\n.loopbar__avs {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  align-items: center;\n}\n.loopbar__av {\n  width: 40px;\n  height: 40px;\n  border-radius: 12px;\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 12px;\n  border: 2px solid #0a2a2f;\n  margin-left: -10px;\n}\n.loopbar__av:first-child {\n  margin-left: 0;\n}\n.loopbar__av-meta {\n  margin-left: 16px;\n  font-size: 12.5px;\n  color: rgba(255, 255, 255, 0.85);\n  line-height: 1.4;\n}\n.loopbar__av-meta strong {\n  color: #fff;\n  font-weight: 700;\n}\n.loopbar__cta {\n  position: relative;\n  z-index: 1;\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  padding: 11px 16px;\n  border-radius: var(--radius-pill);\n  background: #fff;\n  color: #0f3a40;\n  border: none;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  cursor: pointer;\n  transition: transform var(--dur-fast) var(--ease-out);\n}\n.loopbar__cta:hover {\n  transform: translateY(-1px);\n}\n\n/* ============== RIGHT RAIL CONTAINER (.cside2 — slotted as children) === */\n.cside2 {\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n  position: sticky;\n  top: 16px;\n}\n\n/* ============== LEADERBOARD (TopClients) ============== */\n.ctop2 {\n  background: var(--brand-teal);\n  color: #fff;\n  border-radius: var(--radius-xl);\n  padding: 18px;\n  position: relative;\n  overflow: hidden;\n}\n.ctop2::before {\n  content: \"\";\n  position: absolute;\n  top: -30px;\n  right: -30px;\n  width: 140px;\n  height: 140px;\n  background: radial-gradient(\n    circle,\n    rgba(255, 107, 107, 0.35),\n    transparent 70%\n  );\n  border-radius: 999px;\n}\n.ctop2__head {\n  position: relative;\n  z-index: 1;\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  margin-bottom: 14px;\n}\n.ctop2__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n}\n.ctop2__period {\n  font-size: 11px;\n  opacity: 0.7;\n  font-weight: 600;\n}\n.ctop2__list {\n  display: flex;\n  flex-direction: column;\n  gap: 11px;\n  position: relative;\n  z-index: 1;\n}\n.ctop2__item {\n  display: grid;\n  grid-template-columns: 22px 1fr auto;\n  gap: 10px;\n  align-items: center;\n}\n.ctop2__rank {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 11px;\n  color: rgba(255, 255, 255, 0.5);\n  letter-spacing: 0.05em;\n}\n.ctop2__rank--1 {\n  color: #ffd56b;\n}\n.ctop2__name {\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 13px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ctop2__bar-wrap {\n  grid-column: 2 / -1;\n  height: 4px;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.12);\n  margin-top: 4px;\n  overflow: hidden;\n}\n.ctop2__bar {\n  height: 100%;\n  background: linear-gradient(90deg, var(--brand-pink), #ffb3b3);\n  border-radius: 999px;\n}\n.ctop2__amt {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n}\n/* NOTE: source has no `.ctop2__empty` rule — the empty state inherits\n   default flow text inside .ctop2. The TopClients empty branch renders\n   <div class=\"ctop2__empty\"> with no styling. */\n\n/* ============== SEGMENT MIX (ClientsSegments) ============== */\n.csegment2 {\n  background: #fff;\n  border: 1px solid var(--border);\n  border-radius: var(--radius-xl);\n  padding: 18px;\n}\n.csegment2__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  margin-bottom: 14px;\n}\n.cseg2-row {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  margin-bottom: 10px;\n}\n.cseg2-row:last-child {\n  margin-bottom: 0;\n}\n.cseg2-row__lbl {\n  font-size: 12px;\n  color: var(--brand-teal);\n  font-weight: 600;\n  min-width: 100px;\n}\n.cseg2-row__bar {\n  flex: 1;\n  height: 6px;\n  border-radius: 999px;\n  background: var(--bg-sunken);\n  overflow: hidden;\n}\n.cseg2-row__fill {\n  height: 100%;\n  border-radius: 999px;\n  transition: width 1s var(--ease-bounce);\n}\n.cseg2-row__num {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 12px;\n  color: var(--brand-teal);\n  min-width: 24px;\n  text-align: right;\n}\n/* NOTE: source has no `.csegment2__empty` rule — inherits default flow. */\n\n/* ============== RESPONSIVE (own breakpoints, from clients.css) ====== */\n@media (max-width: 1100px) {\n  .cside2 {\n    position: static;\n  }\n  .loopbar {\n    grid-template-columns: 1fr;\n    gap: 12px;\n  }\n  .ph2 {\n    flex-direction: column;\n    align-items: flex-start;\n  }\n}\n@media (max-width: 768px) {\n  .ph2__title {\n    font-size: 28px;\n  }\n  .ph2__cta {\n    width: 100%;\n    justify-content: center;\n  }\n}\n", "cssName": "clients-sections.css", "source": "/**\n * Server-rendered sections for /clients: editorial header, today's loop strip,\n * leaderboard, and segment-mix bar. Pure presentation — feed them backend data.\n */\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport type {\n  ClientSegmentRow,\n  CustomerCard,\n  TopClient,\n} from \"../clients/clients.ts\";\nimport { dollars, initialsOf, numberWord } from \"../lib/clients-display.ts\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\n\ninterface ClientsHeroProps {\n  totalClients: number;\n  activeJobs: number;\n  owedTotal: number;\n  quietCount: number;\n  lang?: Lang;\n  /** Open the add-client modal. Rendered inside the ClientsPage island, so\n   *  this closure hydrates with it (not a cross-island prop). */\n  onAdd?: () => void;\n}\n\nexport function ClientsHero(\n  { totalClients, activeJobs, owedTotal, quietCount, lang = \"en\", onAdd }:\n    ClientsHeroProps,\n) {\n  const owedFmt = owedTotal.toLocaleString(\"en-US\", {\n    maximumFractionDigits: 0,\n  });\n  const empty = totalClients === 0;\n  return (\n    <div class=\"ph2\">\n      <div>\n        <div class=\"ph2__crumb\">\n          <span class=\"ph2__crumb-dot\" />{\" \"}\n          {tFor(lang, \"clientsHero.crumb\", { n: totalClients })}\n        </div>\n        {empty\n          ? (\n            <>\n              <h1 class=\"ph2__title\">\n                {tFor(lang, \"clientsHero.emptyTitlePre\")}{\" \"}\n                <em>{tFor(lang, \"clientsHero.emptyTitleEm\")}</em>.<br />\n                {tFor(lang, \"clientsHero.emptyTitlePost\")}\n              </h1>\n              <p class=\"ph2__sub\">\n                {tFor(lang, \"clientsHero.emptySub\")}\n              </p>\n            </>\n          )\n          : (\n            <>\n              <h1 class=\"ph2__title\">\n                {tFor(lang, \"clientsHero.titlePre\")}{\" \"}\n                <em>\n                  {tFor(\n                    lang,\n                    `clientsHero.people.${totalClients === 1 ? \"one\" : \"other\"}`,\n                    { word: numberWord(totalClients, lang) },\n                  )}\n                </em>\n                <br />{tFor(lang, \"clientsHero.titlePost\")}\n              </h1>\n              <p class=\"ph2__sub\">\n                <strong>\n                  {tFor(\n                    lang,\n                    `clientsHero.jobsInFlight.${\n                      activeJobs === 1 ? \"one\" : \"other\"\n                    }`,\n                    { n: activeJobs },\n                  )}\n                </strong>{\" \"}\n                · <strong>${owedFmt}</strong>{\" \"}\n                {tFor(lang, \"clientsHero.owed\")} ·{\" \"}\n                <strong>\n                  {tFor(lang, \"clientsHero.quiet\", { n: quietCount })}\n                </strong>{\" \"}\n                {tFor(\n                  lang,\n                  `clientsHero.quietClients.${\n                    quietCount === 1 ? \"one\" : \"other\"\n                  }`,\n                  { n: quietCount },\n                )}\n              </p>\n            </>\n          )}\n      </div>\n      <button class=\"ph2__cta\" type=\"button\" onClick={onAdd}>\n        <I d={ICN.plus} size={14} /> {tFor(lang, \"clientsHero.addClient\")}\n      </button>\n    </div>\n  );\n}\n\ninterface LoopBarProps {\n  picks: CustomerCard[];\n  lang?: Lang;\n}\n\nconst LOOP_AV_BG = [\n  \"linear-gradient(135deg, var(--coffee-300), var(--coffee-500))\",\n  \"linear-gradient(135deg, var(--coffee-400), #4F362A)\",\n  \"linear-gradient(135deg, var(--pink-300), var(--brand-pink))\",\n];\n\nexport function LoopBar({ picks, lang = \"en\" }: LoopBarProps) {\n  if (picks.length === 0) {\n    return (\n      <div class=\"loopbar\">\n        <div class=\"loopbar__title\">\n          <span class=\"loopbar__lbl\">\n            <span class=\"loopbar__lbl-dot\" /> {tFor(lang, \"loopBar.label\")}\n          </span>\n          <span class=\"loopbar__h\">\n            {tFor(lang, \"loopBar.empty\")}\n          </span>\n        </div>\n        <a class=\"loopbar__cta\" href=\"/assistant\">\n          <I d={ICN.send} size={13} /> {tFor(lang, \"loopBar.openAssistant\")}\n        </a>\n      </div>\n    );\n  }\n  const names = picks.map((p) => p.name.split(\" \")[0]).join(\" · \");\n  return (\n    <div class=\"loopbar\">\n      <div class=\"loopbar__title\">\n        <span class=\"loopbar__lbl\">\n          <span class=\"loopbar__lbl-dot\" /> {tFor(lang, \"loopBar.label\")}\n        </span>\n        <span class=\"loopbar__h\">\n          {tFor(\n            lang,\n            `loopBar.heading.${picks.length === 1 ? \"one\" : \"other\"}`,\n            { n: picks.length },\n          )}\n        </span>\n      </div>\n      <div class=\"loopbar__avs\">\n        {picks.map((p, i) => (\n          <div\n            key={p.id}\n            class=\"loopbar__av\"\n            style={`background:${LOOP_AV_BG[i % LOOP_AV_BG.length]}`}\n          >\n            {initialsOf(p.name)}\n          </div>\n        ))}\n        <div class=\"loopbar__av-meta\">\n          {names}\n          <br />\n          <strong>\n            {tFor(lang, \"loopBar.seconds\", { n: picks.length * 30 })}\n          </strong>{\" \"}\n          {tFor(lang, \"loopBar.toSend\")}{\" \"}\n          {picks.length === 1\n            ? tFor(lang, \"loopBar.sendIt\")\n            : tFor(lang, \"loopBar.sendAll\", {\n              word: numberWord(picks.length, lang),\n            })}\n        </div>\n      </div>\n      <a class=\"loopbar__cta\" href=\"/assistant\">\n        <I d={ICN.send} size={13} /> {tFor(lang, \"loopBar.openLoop\")}\n      </a>\n    </div>\n  );\n}\n\ninterface TopClientsProps {\n  rows: TopClient[];\n  lang?: Lang;\n}\n\nexport function TopClients({ rows, lang = \"en\" }: TopClientsProps) {\n  if (rows.length === 0) {\n    return (\n      <div class=\"ctop2\">\n        <div class=\"ctop2__head\">\n          <div class=\"ctop2__title\">{tFor(lang, \"topClients.title\")}</div>\n          <div class=\"ctop2__period\">{tFor(lang, \"topClients.period\")}</div>\n        </div>\n        <div class=\"ctop2__empty\">{tFor(lang, \"topClients.empty\")}</div>\n      </div>\n    );\n  }\n  return (\n    <div class=\"ctop2\">\n      <div class=\"ctop2__head\">\n        <div class=\"ctop2__title\">{tFor(lang, \"topClients.title\")}</div>\n        <div class=\"ctop2__period\">{tFor(lang, \"topClients.period\")}</div>\n      </div>\n      <div class=\"ctop2__list\">\n        {rows.map((t, i) => (\n          <div key={t.customerId}>\n            <div class=\"ctop2__item\">\n              <div class={`ctop2__rank ${i === 0 ? \"ctop2__rank--1\" : \"\"}`}>\n                {String(t.rank).padStart(2, \"0\")}\n              </div>\n              <div class=\"ctop2__name\">{t.name}</div>\n              <div class=\"ctop2__amt\">{dollars(t.revenue12moCents)}</div>\n            </div>\n            <div class=\"ctop2__bar-wrap\">\n              <div class=\"ctop2__bar\" style={`width: ${t.barPct}%`} />\n            </div>\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n}\n\ninterface ClientsSegmentsProps {\n  rows: ClientSegmentRow[];\n  lang?: Lang;\n}\n\nconst SEGMENT_COLOR: Record<string, string> = {\n  property_mgmt: \"var(--brand-green)\",\n  homeowner: \"var(--brand-pink)\",\n  small_biz: \"var(--brand-teal)\",\n  hoa: \"var(--coffee-500)\",\n  unsorted: \"var(--coffee-300)\",\n};\n\nexport function ClientsSegments({ rows, lang = \"en\" }: ClientsSegmentsProps) {\n  if (rows.length === 0) {\n    return (\n      <div class=\"csegment2\">\n        <div class=\"csegment2__title\">\n          {tFor(lang, \"clientsSegments.title\")}\n        </div>\n        <div class=\"csegment2__empty\">{tFor(lang, \"clientsSegments.empty\")}</div>\n      </div>\n    );\n  }\n  // Plural-ize labels for the section\n  const PLURAL: Record<string, string> = {\n    \"property_mgmt\": tFor(lang, \"clientsSegments.label.property_mgmt\"),\n    \"homeowner\": tFor(lang, \"clientsSegments.label.homeowner\"),\n    \"small_biz\": tFor(lang, \"clientsSegments.label.small_biz\"),\n    \"hoa\": tFor(lang, \"clientsSegments.label.hoa\"),\n    \"unsorted\": tFor(lang, \"clientsSegments.label.unsorted\"),\n  };\n  return (\n    <div class=\"csegment2\">\n      <div class=\"csegment2__title\">{tFor(lang, \"clientsSegments.title\")}</div>\n      {rows.map((s) => (\n        <div class=\"cseg2-row\" key={s.key}>\n          <div class=\"cseg2-row__lbl\">{PLURAL[s.key] ?? s.label}</div>\n          <div class=\"cseg2-row__bar\">\n            <div\n              class=\"cseg2-row__fill\"\n              style={`width:${s.pct}%; background:${\n                SEGMENT_COLOR[s.key] ?? \"var(--coffee-300)\"\n              }`}\n            />\n          </div>\n          <div class=\"cseg2-row__num\">{s.count}</div>\n        </div>\n      ))}\n    </div>\n  );\n}\n", "sourceName": "ClientsSections.tsx", "checklist": "# Capture checklist — ClientsSections\n\n**No backend running → NO fabricated screenshots.** Light theme only.\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/clients` (these four sections render in-page; there is\n  no standalone route). Backend :4280 up so counts/leaderboard/segments are real.\n\n## Viewports (own @media in clients.css)\n- **1280px** — hero `.ph2` as a row (title left, Add CTA right); loop ribbon as a\n  3-column grid; rail sections at 280px wide.\n- **1100px** — hero stacks (`column`); `.loopbar` → 1 column; rail un-sticks and\n  drops below the cards.\n- **768px** — `.ph2__title` 44→28px; `.ph2__cta` full-width.\n\n## Element(s) to crop\n- `.ph2` populated hero (crumb + headline + stat sub + Add CTA).\n- `.ph2` empty hero (\"Let's add your *first client*\").\n- `.loopbar` with 3 avatars + meta + \"Open the loop\"; and the empty ribbon.\n- `.ctop2` leaderboard (rank 01 gold + bars + amounts); and its empty state.\n- `.csegment2` segment bars (4 colored fills + counts); and its empty state.\n\n## Transient states to drive\n1. **hero populated vs empty** — populated needs ≥1 client; empty needs a wiped\n   account (or mock `/clients` → `[]`).\n2. **loop picks vs empty** — picks need ≥1 lead/owes client; empty otherwise.\n3. **leaderboard / segments empty** — fresh account with no paid invoices /\n   no clients.\n4. **es** — Spanish UI language: headline \"*doce* personas\", section titles,\n   loop copy.\n\n## Motion to film (from clients.css)\n- `.ph2__crumb-dot` / `.loopbar__lbl-dot` — `ph2-pulse 2.4s` expanding ring.\n- `.ph2__cta:hover` — `translateY(-2px)` + shadow (bounce).\n- `.cseg2-row__fill` — `width` transition 1s bounce: film the segment bars\n  filling on first paint / when `pct` changes (note: `width` animation = layout;\n  flag a `scaleX` swap).\n- Re-shoot with `prefers-reduced-motion: reduce` (global clamp) — pulse + 1s\n  fill should go instant.\n"}]}, {"name": "contracts", "pageMd": "# Page — /contracts\n\nThe contractor's signed-work board: \"everything you've committed to.\" Hero\n(Σ committed value + deposits line), 4 KPI cards, a dark-teal 30-day **schedule\nstrip** (Gantt-style lane chart), and up to four collapsible pipeline **tracks**\n(In progress / Starting soon / Wrapping up / Drafts) of flippable contract\ncards. Cards are grouped by a server-derived `mood`.\n\nSource route: `routes/contracts/index.tsx` → `js/index.tsx`.\n\n## App-shell composition (island order)\n\n```\n<div class=\"app\">\n  <DashSidebar active=\"contracts\" />            ← shared island\n  <main class=\"main\">\n    <DashTopbar greetingDate={…} greetingName={…} />  ← shared island\n    <div class=\"content\">\n      <ContractsPage />                          ← page island (whole body)\n    </div>\n  </main>\n</div>\n```\n\n- Same shell as /quotes. Difference: this route passes **both** `greetingDate`\n  (localized \"weekday · month day\", computed SSR from `new Date()`) **and**\n  `greetingName` to DashTopbar; /quotes passes only `greetingName`.\n- `ContractsPage` is the only page-local island.\n\n## `<Head>`\n\n- **title:** `tFor(lang, \"contractsPage.docTitle\")` — **localized** (contrast\n  with /quotes' hard-coded title).\n- **CSS:** two `<link>` tags, in order:\n  1. `/dashboard.css` (key `css-dashboard`) — tokens + shell + skeletons.\n  2. `/contracts.css` (key `css-contracts`) — all contracts classes (copied to\n     `css/contracts.css`).\n\n## SSR data\n\n**None for the body.** The route computes `greetingName` + `greetingDate` from\n`ctx.state.user` + the current date. All contract/customer/quote data is fetched\n**client-side** by `ContractsPage` on mount; the server ships shell + skeleton.\n\n## Sections (top → bottom, all inside ContractsPage)\n\n1. **ContractsHero** (`.kph`) — eyebrow (\"Work in flight · N contracts\"), big\n   headline (Σ committed), deposits sub-line, \"Schedule a job\" CTA (an `<a>` to\n   `/assistant?seed=…`). Empty variant when nothing live.\n2. **ContractsKpis** (`.kkpi`) — 4 cards: In progress (accent), Starting soon,\n   Wrapping up, Closed this month.\n3. **ScheduleStrip** (`.csched`) — dark gradient panel, 5 week rows, lane-packed\n   contract bars positioned by day-of-month coordinate, \"TODAY\" marker, legend.\n   Honest-empty slate when no bars in range.\n4. **Tracks** — single stacked column (NO sidebar, unlike /quotes):\n   - 01 In progress → `.kcards` of `ContractCard`, or `.kempty` slate.\n   - 02 Starting soon → `.kcards` / `.kempty`.\n   - 03 Wrapping up → `.kcards` / `.kempty`.\n   - 04 Drafts → `.kcards` — **only rendered when `drafts.length > 0`** (no track\n     element at all otherwise; no empty slate).\n\n## Build order\n\n1. Tokens + `/dashboard.css` shell, shared Skeletons.\n2. `lib/contracts-shape.ts` adapter (`toContractCard`, `moodFor`) — the data\n   projection every card/strip consumes. (Not a UI component; a dependency.)\n3. `contract-track/` (island; collapse mechanic — a contracts-specific twin of\n   shared QuoteTrack).\n4. `contracts-sections/` (page-composition; hero/kpi/schedule-strip + `.kempty`).\n5. `contract-card/` (island; flip card with milestone synthesis).\n6. `contracts-page/` (island; fetch 3 endpoints + group by mood + compose).\n7. Route shell `index.tsx`.\n\n## Local components (this folder)\n\n| folder | source | bucket |\n|---|---|---|\n| `contracts-page/` | `islands/ContractsPage.tsx` | island (data/orchestrator) |\n| `contracts-sections/` | `components/ContractsSections.tsx` | page-composition (SSR) |\n| `contract-card/` | `islands/ContractCard.tsx` | island (client-only flip) |\n| `contract-track/` | `islands/ContractTrack.tsx` | island (collapse) |\n\nShared (reference only, do NOT re-spec): `DashSidebar`, `DashTopbar`,\n`Skeletons`, `dash-icons`. **`components/contract-doc.tsx`** is a shared\npublic-surface component owned by another agent — NOT spec'd here (the\n`/c/[id]` public route, not /contracts).\n\n## Page-level anti-pattern (see contracts-page/ for detail)\n\n`ContractsPage` is a **whole-page island**: it fetches `/contracts` + `/clients`\n+ `/quotes` on mount, derives moods via `toContractCard`, and freezes the result.\nThe card CTAs mutate navigation via `globalThis.location.assign(...)` (full\nnavigations, not client routing). No `location.reload()` on this page (contracts\nhave no delete-from-list button), so it is *less* exposed than /quotes — but the\nfrozen-SSR-props pattern still applies. Detail + Partial fix in\n`contracts-page/contracts-page.md` §1.\n", "components": [{"name": "contract-card", "component": "ContractCard", "spec": "# ContractCard\n\nPer-contract pipeline card with a **flip-to-back** milestone timeline. The front\nface shows a mood-colored gradient header (status pill + relative-time pill +\ngiant ghost numeral), an initials avatar, client/title/story, a progress bar\n(paid / left), and a primary CTA + contract value. Clicking the card flips it to\na back face: a synthesized milestone checklist + 3 action buttons.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ContractCard.tsx`).\n- **Interaction tier:** `island` (client-only state) — a local flip toggle plus\n  three full-navigation CTAs.\n- **Client state owned + refresh:**\n  - `flipped: boolean` (`useState(false)`) — front ↔ back face. **Not** persisted\n    (resets on remount). No data refresh: the card consumes a frozen\n    `ContractCard` prop (`c`) built once by the parent's `toContractCard()`. The\n    card never re-fetches.\n- **Server action + flash:** **none.** ContractCard performs no mutations. All\n  \"actions\" are navigations, not writes — no PRG, no optimistic update, no flash.\n- **`location.reload()` flag:** **no `reload()`** — but five hard navigations via\n  `globalThis.location.assign(...)`:\n  - front CTA → `c.ctaHref` (`/invoices` for active/wrapping-up/completed, else\n    `/c/{id}`).\n  - back \"Invoice\" → `/invoices`.\n  - back \"Text client\" → `/assistant?seed=…` (encoded prompt).\n  - back \"View contract\" → `/c/{id}`.\n  These are **full page navigations**, not SPA routing. In a Fresh 2 rebuild they\n  should be real `<a href>` anchors (the front CTA and the 3 back buttons are\n  `<button onClick={location.assign(...)}>` today — anti-pattern: a link styled as\n  a button, breaking middle-click / open-in-new-tab / right-click and needing JS\n  to navigate). **Fix:** render anchors; keep the flip on the card surface only.\n  No Partial/PRG fix applies (no server mutation happens).\n- **Data source per region:** **none of its own.** Every visible value\n  (`client`, `initials`, `title`, `story`, `status`, `when`, `pct`, `paid`,\n  `left`, `total`, `cta`, `ctaHref`, the mood gradient custom-props) is a field\n  on the `c: ContractCard` prop, produced by `lib/contracts-shape.ts`\n  `toContractCard()` from the backend `Contract` (+ customer-name + quote-summary\n  lookups) in the parent `ContractsPage`. No `clients/contracts.ts` /\n  `ssrBackendGet` call here.\n- **Honest-empty:** N/A at card level — the parent never renders a card without a\n  contract. Missing dates collapse to `\"—\"` in `when`/milestone dates; missing\n  customer → `\"contractsShape.untitledCustomer\"`; missing quote summary →\n  `\"contractsShape.signedContract\"` (both resolved upstream in `toContractCard`).\n- **Liveness:** request-response only. The status-dot has a **cosmetic** CSS\n  pulse (`q-pulse-dot`, 2.4s) — it is **not** live data; it is decoration on every\n  card regardless of real \"liveness\". No polling/websocket.\n- **Reactivity / i18n:** `lang` is a prop (default `\"en\"`), threaded from the\n  parent's `langSignal.value`. The card calls `tFor(lang, …)` for the static\n  chrome labels (progress/paid/left/contract/close + the 3 action button labels +\n  the assistant seed prompt). The data-derived strings (`status`, `when`, `story`,\n  `cta`) are **already localized** inside `toContractCard()` and passed in as\n  plain strings — the card does not re-localize them, so a language flip must\n  re-run the parent's projection to fully re-localize.\n- **Data-shape hazards:**\n  - **Milestones are synthesized, not stored.** `buildMilestones()` fabricates a\n    timeline: if no `startDate`/`completionDate`, three generic mood-keyed stops;\n    else five fixed-offset stops (0 / 0.25 / 0.55 / 0.85 / 1.0 of the span) with\n    `done = stop date < now`, and the first not-done stop after a done one tagged\n    `current`. **This is presentational fiction** — when the backend grows a real\n    Milestone shape, swap to consuming it. Do not treat these dates as real.\n  - **`pct` / `paid` / `left` are interpolated, not actual ledger values.** In\n    `toContractCard`: `completed`→100%, both dates present→linear days-elapsed,\n    `wrapping-up`→90%, `starting-soon`→0%; `paid = total * pct/100`. These are\n    *estimates of schedule progress dressed as payment progress* — they do NOT\n    reflect real `Payment` rows. Flag for rebuild: bind to actual invoices/payments.\n  - **Money unit boundary:** the card receives `total`/`paid`/`left` as\n    pre-formatted dollar **strings** (`fmtMoney` already applied upstream from\n    INTEGER CENTS). The card itself never does money math — keep it that way.\n  - **`#{c.id.slice(0,8)}`** assumes an id ≥8 chars; fine for backend ids.\n\n## 2. Anatomy\n```\n<div class=\"kcard [kcard--flipped]\" style=\"--mood-from/--mood-to/--mood-shadow/--mood-status\" onClick=onCardClick>\n  <div class=\"kcard__mood\">                                  ← gradient header, 138px, overflow:hidden\n    <span class=\"kcard__status\"><span dot/>{c.status}</span> ← pill, top-left (pulsing dot)\n    <span class=\"kcard__when\">{c.when}</span>                ← pill, top-right\n    <span class=\"kcard__numeral\">{(idx+1) zero-pad 2}</span> ← 96px ghost numeral, bottom-right (CLIP BUG, see §6)\n  </div>\n  <div class=\"kcard__av\">{c.initials}</div>                  ← 86px avatar, overlaps mood/body seam\n  <div class=\"kcard__body\">                                  ← column flex\n    <div class=\"kcard__client-name\">{c.client} · #{id8}</div>\n    <h3 class=\"kcard__title\">{c.title}</h3>                  ← 2-line clamp\n    <p class=\"kcard__story\">{c.story}</p>                    ← 3-line clamp\n    <div class=\"kcard__prog\">                                ← margin-top:auto (pins to bottom)\n      <div class=\"kcard__prog-row\"><span lbl>Progress</span><span pct>{c.pct}%</span></div>\n      <div class=\"kcard__prog-bar\"><div class=\"kcard__prog-fill\" style=\"width:{pct}%\"/></div>\n      <div class=\"kcard__prog-meta\"><span>Paid {c.paid}</span><span>Left {c.left}</span></div>\n    </div>\n  </div>\n  <div class=\"kcard__foot\">                                  ← border-top, grid 1fr auto\n    <button class=\"kcard__cta\" onClick=stop+assign(ctaHref)>{c.cta} <I arrow/></button>\n    <div class=\"kcard__val-wrap\"><div lbl>Contract</div><div num>{c.total}</div></div>\n  </div>\n\n  <div class=\"kcard__back\" aria-hidden={!flipped} onClick=stopPropagation>   ← absolute inset:0, z:10\n    <div class=\"kcard__back-head\">                           ← gradient\n      <button class=\"kcard__back-close\" onClick=stop+setFlipped(false)><I x/></button>\n      <div class=\"kcard__back-eyebrow\">#{id8} · {c.client}</div>\n      <h4 class=\"kcard__back-big\">{c.title}<small>{c.total} · {c.when}</small></h4>\n    </div>\n    <div class=\"kcard__back-body\">                           ← scrollable milestone list\n      {milestones.map → <div class=\"kcard__mile [--done][--current]\">\n         <span check><I check/></span><span name>{m.name}</span><span date>{m.date}</span></div>}\n    </div>\n    <div class=\"kcard__back-foot\">                           ← 3 equal buttons\n      <button onClick=assign('/invoices')><I invoice/> Invoice</button>\n      <button onClick=assign('/assistant?seed=…')><I send/> Text client</button>\n      <button onClick=assign('/c/{id}')><I contract/> View contract</button>\n    </div>\n  </div>\n</div>\n```\n- **Icon dependency:** `I` + `ICN.{arrow,x,check,invoice,send,contract}` from\n  `lib/dash-icons.tsx` (copied to `js/dash-icons.tsx`).\n- **Mood custom-props:** set inline on the root from `c.moodFrom/moodTo/\n  moodShadow/statusColor`; the gradient header, avatar, prog-fill, back-head,\n  done-milestone check, and hover shadow all read them.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `c` | `ContractCard` (object, required) | — | (object — built by `toContractCard`) | no |\n| `idx` | number (required) | — | number | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n> `c` is the rich card shape (`lib/contracts-shape.ts`). In isolate it is supplied\n> as a full object literal in each case (real values per data-model `Contract` →\n> `toContractCard`); the control widget is the JSON object editor, not a scalar.\n\n## 4. States → cases\nDriven by `c.mood` (the dominant axis — it sets palette, status label, when,\nstory, cta, ctaHref, pct) plus the flip toggle.\n| state | meaning | case |\n|---|---|---|\n| active | `mood=\"active\"`, front face, day-of-N progress, pink/green/orange variant | `cases/active/active.json` |\n| starting-soon | `mood=\"starting-soon\"`, 0% pct, \"Starts in N days\", scheduled | `cases/starting-soon/starting-soon.json` |\n| wrapping-up | `mood=\"wrapping-up\"`, 90% pct, \"Wraps in N days\" | `cases/wrapping-up/wrapping-up.json` |\n| completed | `mood=\"completed\"`, 100% pct, \"Closed\", green | `cases/completed/completed.json` |\n| draft | `mood=\"draft\"`, no dates, \"Draft — not sent\", finish CTA | `cases/draft/draft.json` |\n| stale | `mood=\"stale\"`, \"Stale draft\", re-engage CTA | `cases/stale/stale.json` |\n| flipped | back face open (milestone list + 3 actions) | `cases/flipped/flipped.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"div.kcard\" && e.type === \"click\")` →\n  `onCardClick`: if not already flipped AND the click target is **not** inside\n  `.kcard__cta` or `.kcard__back`, set `flipped = true`.\n- `ev.expect(e => e.source === \"button.kcard__cta\" && e.type === \"click\")` →\n  `e.stopPropagation()` then `location.assign(c.ctaHref)` (does NOT flip).\n- `ev.expect(e => e.source === \"button.kcard__back-close\" && e.type === \"click\")`\n  → `e.stopPropagation()` then `setFlipped(false)`.\n- `ev.expect(e => e.source === \"div.kcard__back\" && e.type === \"click\")` →\n  `e.stopPropagation()` (back face swallows clicks so they don't re-flip).\n- `ev.expect(e => e.source === \"button.kcard__back-foot >> nth(0)\" && e.type === \"click\")`\n  → `stopPropagation` + `location.assign(\"/invoices\")`.\n- `ev.expect(e => e.source === \"button.kcard__back-foot >> nth(1)\" && e.type === \"click\")`\n  → `stopPropagation` + `location.assign(\"/assistant?seed=…\")`.\n- `ev.expect(e => e.source === \"button.kcard__back-foot >> nth(2)\" && e.type === \"click\")`\n  → `stopPropagation` + `location.assign(\"/c/{id}\")`.\n\n## 6. Motion (extracted from contract-card.css)\n- **Card hover:** `.kcard` `transform: translateY(-4px)` + deeper mood-tinted\n  shadow, over `transform 320ms var(--ease-bounce)` + `box-shadow 320ms\n  var(--ease-out)`.\n- **Flip-in:** `.kcard__back` rests at `translateY(8px) scale(.98)` `opacity:0`\n  `pointer-events:none`; `.kcard--flipped .kcard__back` → `translateY(0) scale(1)`\n  `opacity:1` `pointer-events:auto`, over `transform 380ms var(--ease-bounce),\n  opacity 240ms var(--ease-out)`. (It is a fade+rise+scale \"card lifts to front,\"\n  NOT a 3D rotateY flip — despite the \"flip\" name.)\n- **Progress fill:** `.kcard__prog-fill` `transition: width 480ms var(--ease-out)`\n  — animates when `pct` changes / on mount.\n- **Status dot:** `q-pulse-dot` keyframe (opacity 1→.4→1) `2.4s infinite`. This\n  is the single keyframe shared with quotes.css; **cosmetic, not live**.\n- **CTA hover:** `.kcard__cta:hover { gap: 6px → 9px }` (arrow nudges) over\n  `var(--dur-fast)`. Back-foot buttons: border/color/translateY(-1px) on hover.\n- **Back-close hover:** background alpha `.22 → .38` over `var(--dur-fast)`.\n- **Jank / clip BUG (real, flagged in CSS):** `.kcard__numeral` is `bottom:-18px`\n  while `.kcard__mood` is `overflow:hidden` → the 96px numeral's descender is\n  **clipped**. The sibling `.qcard__numeral` was fixed to `bottom:4px` (audit #30)\n  but the fix was **not ported back here**. Rebuild: set `.kcard__numeral`\n  `bottom:4px`. Not animation jank — a static clipping bug; capture it.\n- **Reduced motion:** **no component-local block** — relies on the global tokens\n  clamp. Verify hover-lift, flip, prog-fill, and pulse all still/snap under\n  `prefers-reduced-motion: reduce`.\n\n## 7. Responsive\n- **No own `@media` queries.** The card is fixed-internal-layout; it reflows only\n  via the host `.kcards` grid (`repeat(auto-fill, minmax(300px, 1fr))`, defined in\n  this CSS for isolate context) — i.e. it goes 1-up below ~300px effective column\n  width and N-up above, with no card-internal breakpoint. The mood header height\n  (138px), avatar (86px), numeral (96px) are all fixed px. Verify legibility of\n  the 2-line title clamp + 3-line story clamp at the narrowest single-column width.\n\n## 8. A11y\n- **Whole-card click without keyboard:** `.kcard` is a `<div onClick>` that flips\n  — not focusable/operable by keyboard, no role/`aria-expanded`. **Fix:** the flip\n  affordance should be a real control (or the card a `<button>` with\n  `aria-expanded={flipped}` `aria-controls` → back-face id). Today only the inner\n  `<button>`s are reachable.\n- **Links-as-buttons:** front CTA + 3 back-foot actions are `<button>`s that call\n  `location.assign` — they should be `<a href>` for native link semantics\n  (focus, middle-click, screen-reader \"link\" role). Back-close is a real\n  `<button>` with `aria-label` (good).\n- **Back face hidden state:** `aria-hidden={!flipped}` is set on `.kcard__back`\n  (good) — but the back content is still in the DOM and `opacity:0`\n  `pointer-events:none` only; confirm focus can't tab into it while hidden\n  (add `inert`/`tabindex=-1` management in rebuild).\n- **Status pill** relies on color + the uppercase text label (text present —\n  acceptable). The pulsing dot is decorative.\n\n## 9. Used on\n**Only** `islands/ContractsPage.tsx` (`/contracts`), rendered inside each\n`ContractTrack` body as `<ContractCard c={…} idx={i} lang={lang} />`. Evidence:\ngrep of `ContractCard` import. It is the contracts twin of the shared `QuoteCard`\n(`islands/QuoteCard.tsx`, owned separately) — similar `.k*`/`.q*` class lineage,\nbut a distinct component with the mood-palette + milestone-synthesis logic.\nCSS: `static/contracts.css` `.kcard*` rules (extracted to `css/contract-card.css`).\n", "fixture": "{\n  \"category\": \"contracts\",\n  \"folder\": \"contract-card\",\n  \"controls\": {\n    \"c\": {\n      \"type\": \"select\",\n      \"value\": \"active\",\n      \"options\": [\"active\", \"starting-soon\", \"wrapping-up\", \"completed\", \"draft\", \"stale\"],\n      \"_note\": \"Selects a prebuilt ContractCard object (the `c` prop) by mood. Each option mirrors lib/contracts-shape.ts toContractCard() output: mood drives moodFrom/moodTo/moodShadow/statusColor, status label, when, story, cta, ctaHref, pct, paid/left/total (fmtMoney dollar strings from INTEGER CENTS). The full object literal lives in the matching cases/<mood>.json.\"\n    },\n    \"idx\": { \"type\": \"number\", \"value\": 0 },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "active.json", "content": "{\n  \"_name\": \"Active — in progress, Day 6 of 14\",\n  \"idx\": 0,\n  \"lang\": \"en\",\n  \"c\": {\n    \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n    \"client\": \"Maria Delgado\",\n    \"initials\": \"MD\",\n    \"title\": \"Backyard Deck Build\",\n    \"story\": \"Maria Delgado's job is underway — keep momentum and bill the progress.\",\n    \"status\": \"IN PROGRESS\",\n    \"mood\": \"active\",\n    \"moodFrom\": \"#FF6B6B\",\n    \"moodTo\": \"#C84A4A\",\n    \"moodShadow\": \"rgba(255,107,107,0.45)\",\n    \"statusColor\": \"#C84A4A\",\n    \"when\": \"Day 6 of 14\",\n    \"pct\": 43,\n    \"paid\": \"$6,235\",\n    \"left\": \"$8,265\",\n    \"total\": \"$14,500\",\n    \"cta\": \"Send progress invoice\",\n    \"ctaHref\": \"/invoices\",\n    \"scheduleStart\": 3,\n    \"scheduleEnd\": 17,\n    \"scheduleScheduled\": false,\n    \"scheduleColor\": [\"#FF6B6B\", \"#C84A4A\"],\n    \"startDate\": \"2026-06-13\",\n    \"completionDate\": \"2026-06-27\"\n  }\n}\n"}, {"name": "completed.json", "content": "{\n  \"_name\": \"Completed — 100%, all paid, view receipt\",\n  \"idx\": 3,\n  \"lang\": \"en\",\n  \"c\": {\n    \"id\": \"ctr_7c0182befa334510\",\n    \"client\": \"Greenwood HOA\",\n    \"initials\": \"GH\",\n    \"title\": \"Clubhouse Fence Replace\",\n    \"story\": \"Wrapped and paid in full — pull the receipt any time.\",\n    \"status\": \"COMPLETED\",\n    \"mood\": \"completed\",\n    \"moodFrom\": \"#5FA34F\",\n    \"moodTo\": \"#3F7A33\",\n    \"moodShadow\": \"rgba(81,152,67,0.4)\",\n    \"statusColor\": \"#2F5A26\",\n    \"when\": \"Closed\",\n    \"pct\": 100,\n    \"paid\": \"$12,300\",\n    \"left\": \"$0\",\n    \"total\": \"$12,300\",\n    \"cta\": \"View receipt\",\n    \"ctaHref\": \"/invoices\",\n    \"scheduleStart\": 1,\n    \"scheduleEnd\": 6,\n    \"scheduleScheduled\": false,\n    \"scheduleColor\": [\"#5FA34F\", \"#3F7A33\"],\n    \"startDate\": \"2026-05-20\",\n    \"completionDate\": \"2026-06-04\"\n  }\n}\n"}, {"name": "draft.json", "content": "{\n  \"_name\": \"Draft — not sent, no dates, finish & send\",\n  \"idx\": 0,\n  \"lang\": \"en\",\n  \"c\": {\n    \"id\": \"ctr_0a5512deccb73f81\",\n    \"client\": \"Sam Rivera\",\n    \"initials\": \"SR\",\n    \"title\": \"Garage Epoxy Floor\",\n    \"story\": \"This contract is still a draft — finish it up and send it for signature.\",\n    \"status\": \"DRAFT\",\n    \"mood\": \"draft\",\n    \"moodFrom\": \"#C8B89A\",\n    \"moodTo\": \"#7E5A3F\",\n    \"moodShadow\": \"rgba(126,90,63,0.4)\",\n    \"statusColor\": \"#4A2F1E\",\n    \"when\": \"Draft — not sent\",\n    \"pct\": 0,\n    \"paid\": \"$0\",\n    \"left\": \"$3,200\",\n    \"total\": \"$3,200\",\n    \"cta\": \"Finish & send\",\n    \"ctaHref\": \"/c/ctr_0a5512deccb73f81\",\n    \"scheduleStart\": 8,\n    \"scheduleEnd\": 12,\n    \"scheduleScheduled\": true,\n    \"scheduleColor\": [\"#C8B89A\", \"#7E5A3F\"]\n  }\n}\n"}, {"name": "flipped.json", "content": "{\n  \"_name\": \"Flipped — back face: milestone list + 3 actions\",\n  \"idx\": 0,\n  \"lang\": \"en\",\n  \"_signals\": { \"flipped\": true },\n  \"c\": {\n    \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n    \"client\": \"Maria Delgado\",\n    \"initials\": \"MD\",\n    \"title\": \"Backyard Deck Build\",\n    \"story\": \"Maria Delgado's job is underway — keep momentum and bill the progress.\",\n    \"status\": \"IN PROGRESS\",\n    \"mood\": \"active\",\n    \"moodFrom\": \"#FF6B6B\",\n    \"moodTo\": \"#C84A4A\",\n    \"moodShadow\": \"rgba(255,107,107,0.45)\",\n    \"statusColor\": \"#C84A4A\",\n    \"when\": \"Day 6 of 14\",\n    \"pct\": 43,\n    \"paid\": \"$6,235\",\n    \"left\": \"$8,265\",\n    \"total\": \"$14,500\",\n    \"cta\": \"Send progress invoice\",\n    \"ctaHref\": \"/invoices\",\n    \"scheduleStart\": 3,\n    \"scheduleEnd\": 17,\n    \"scheduleScheduled\": false,\n    \"scheduleColor\": [\"#FF6B6B\", \"#C84A4A\"],\n    \"startDate\": \"2026-06-13\",\n    \"completionDate\": \"2026-06-27\"\n  }\n}\n"}, {"name": "stale.json", "content": "{\n  \"_name\": \"Stale — old draft, re-engage\",\n  \"idx\": 1,\n  \"lang\": \"en\",\n  \"c\": {\n    \"id\": \"ctr_e1937740bb2ac056\",\n    \"client\": \"Lakeside Cafe\",\n    \"initials\": \"LC\",\n    \"title\": \"Patio Pergola Install\",\n    \"story\": \"This one's gone quiet — a quick nudge could bring it back to life.\",\n    \"status\": \"STALE\",\n    \"mood\": \"stale\",\n    \"moodFrom\": \"#B89D90\",\n    \"moodTo\": \"#785544\",\n    \"moodShadow\": \"rgba(120,85,68,0.4)\",\n    \"statusColor\": \"#5A3C2A\",\n    \"when\": \"Stale draft\",\n    \"pct\": 0,\n    \"paid\": \"$0\",\n    \"left\": \"$9,750\",\n    \"total\": \"$9,750\",\n    \"cta\": \"Re-engage\",\n    \"ctaHref\": \"/c/ctr_e1937740bb2ac056\",\n    \"scheduleStart\": 8,\n    \"scheduleEnd\": 12,\n    \"scheduleScheduled\": false,\n    \"scheduleColor\": [\"#B89D90\", \"#785544\"]\n  }\n}\n"}, {"name": "starting-soon.json", "content": "{\n  \"_name\": \"Starting soon — scheduled, 0% progress\",\n  \"idx\": 1,\n  \"lang\": \"en\",\n  \"c\": {\n    \"id\": \"ctr_b21c77aa401de9f0\",\n    \"client\": \"Northgate Property Mgmt\",\n    \"initials\": \"NP\",\n    \"title\": \"Unit 4B Bathroom Reno\",\n    \"story\": \"Northgate Property Mgmt is locked in — confirm the start time and you're set.\",\n    \"status\": \"SCHEDULED\",\n    \"mood\": \"starting-soon\",\n    \"moodFrom\": \"#9DBDC6\",\n    \"moodTo\": \"#4F7A88\",\n    \"moodShadow\": \"rgba(157,189,198,0.5)\",\n    \"statusColor\": \"#3A5C68\",\n    \"when\": \"Starts in 5 days\",\n    \"pct\": 0,\n    \"paid\": \"$0\",\n    \"left\": \"$8,900\",\n    \"total\": \"$8,900\",\n    \"cta\": \"Confirm start time\",\n    \"ctaHref\": \"/c/ctr_b21c77aa401de9f0\",\n    \"scheduleStart\": 13,\n    \"scheduleEnd\": 22,\n    \"scheduleScheduled\": true,\n    \"scheduleColor\": [\"#9DBDC6\", \"#4F7A88\"],\n    \"startDate\": \"2026-06-23\",\n    \"completionDate\": \"2026-07-02\"\n  }\n}\n"}, {"name": "wrapping-up.json", "content": "{\n  \"_name\": \"Wrapping up — 90%, draft final invoice\",\n  \"idx\": 2,\n  \"lang\": \"en\",\n  \"c\": {\n    \"id\": \"ctr_44e9013ab8c1207d\",\n    \"client\": \"Hans Hansen\",\n    \"initials\": \"HH\",\n    \"title\": \"Front Porch Repair\",\n    \"story\": \"Almost done — get the final invoice out while the work is fresh.\",\n    \"status\": \"WRAPPING UP\",\n    \"mood\": \"wrapping-up\",\n    \"moodFrom\": \"#D9886F\",\n    \"moodTo\": \"#A85A3A\",\n    \"moodShadow\": \"rgba(217,136,111,0.45)\",\n    \"statusColor\": \"#7A3D24\",\n    \"when\": \"Wraps in 2 days\",\n    \"pct\": 90,\n    \"paid\": \"$5,400\",\n    \"left\": \"$600\",\n    \"total\": \"$6,000\",\n    \"cta\": \"Draft final invoice\",\n    \"ctaHref\": \"/invoices\",\n    \"scheduleStart\": 1,\n    \"scheduleEnd\": 10,\n    \"scheduleScheduled\": false,\n    \"scheduleColor\": [\"#D9886F\", \"#A85A3A\"],\n    \"startDate\": \"2026-06-08\",\n    \"completionDate\": \"2026-06-20\"\n  }\n}\n"}], "css": "/* ============================================================\n   ContractCard — extracted from static/contracts.css (.kcard* rules).\n   Mood colors arrive as inline custom props on the .kcard element:\n   --mood-from / --mood-to / --mood-shadow / --mood-status (set by\n   moodFor() in lib/contracts-shape.ts; \"active\" picks one of 4 deterministic\n   variants by hashId).\n   Tokens: --brand-teal, --brand-pink, --fg-muted, --radius-xl, --radius-pill,\n   --font-heading, --font-body, --dur-fast, --ease-out, --ease-bounce.\n   ============================================================ */\n\n/* grid the page slots cards into (defined here for isolate context) */\n.kcards {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));\n  gap: 16px;\n}\n\n/* live status-dot pulse (shared keyframe name with quotes.css) */\n@keyframes q-pulse-dot {\n  0%, 100% { opacity: 1; }\n  50% { opacity: 0.4; }\n}\n\n.kcard {\n  position: relative;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  overflow: visible;\n  cursor: pointer;\n  box-shadow:\n    0 1px 0 rgba(26, 83, 92, 0.04),\n    0 8px 22px -14px rgba(26, 83, 92, 0.18);\n  transition:\n    transform 320ms var(--ease-bounce),\n    box-shadow 320ms var(--ease-out);\n  display: flex;\n  flex-direction: column;\n}\n.kcard:hover {\n  transform: translateY(-4px);\n  box-shadow:\n    0 2px 0 rgba(26, 83, 92, 0.05),\n    0 22px 42px -18px var(--mood-shadow, rgba(26, 83, 92, 0.28));\n}\n\n/* mood block */\n.kcard__mood {\n  position: relative;\n  height: 138px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  border-radius: var(--radius-xl) var(--radius-xl) 0 0;\n  overflow: hidden;\n}\n.kcard__mood::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 18% 22%, rgba(255, 255, 255, 0.18) 0, transparent 35%),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n  pointer-events: none;\n}\n.kcard__numeral {\n  position: absolute;\n  /* JANK/CLIP BUG: bottom:-18px + overflow:hidden on .kcard__mood clips the\n     descender of the 96px numeral (#30 fixed it on .qcard__numeral to\n     bottom:4px but it was NOT ported back here). Rebuild: bottom:4px. */\n  bottom: -18px;\n  right: -6px;\n  font-family: var(--font-heading);\n  font-weight: 900;\n  font-size: 96px;\n  line-height: 1;\n  color: rgba(255, 255, 255, 0.16);\n  letter-spacing: -0.04em;\n  pointer-events: none;\n  user-select: none;\n}\n.kcard__status {\n  position: absolute;\n  top: 14px;\n  left: 14px;\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 5px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.92);\n  backdrop-filter: blur(4px);\n  color: var(--mood-status, var(--brand-teal));\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  z-index: 2;\n}\n.kcard__status-dot {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: currentColor;\n  animation: q-pulse-dot 2.4s infinite;\n}\n.kcard__when {\n  position: absolute;\n  top: 14px;\n  right: 14px;\n  display: inline-flex;\n  align-items: center;\n  gap: 5px;\n  padding: 5px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.18);\n  color: rgba(255, 255, 255, 0.95);\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.06em;\n  text-transform: uppercase;\n  backdrop-filter: blur(3px);\n  z-index: 2;\n}\n\n/* avatar */\n.kcard__av {\n  position: absolute;\n  left: 22px;\n  top: 92px;\n  width: 86px;\n  height: 86px;\n  border-radius: 22px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 28px;\n  letter-spacing: 0.02em;\n  z-index: 4;\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.28),\n    0 12px 24px -8px rgba(0, 0, 0, 0.32);\n}\n\n/* body — column flex so the progress block pins to the bottom (margin-top:auto) */\n.kcard__body {\n  padding: 50px 22px 0;\n  flex: 1 1 auto;\n  display: flex;\n  flex-direction: column;\n}\n.kcard__client-name {\n  font-family: var(--font-body);\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin: 0 0 4px;\n  letter-spacing: 0.02em;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.kcard__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 19px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  line-height: 1.2;\n  margin: 0 0 14px;\n  display: -webkit-box;\n  -webkit-line-clamp: 2;\n  -webkit-box-orient: vertical;\n  overflow: hidden;\n}\n.kcard__story {\n  font-family: var(--font-body);\n  font-size: 14.5px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  text-wrap: pretty;\n  margin: 0 0 14px;\n  display: -webkit-box;\n  -webkit-line-clamp: 3;\n  -webkit-box-orient: vertical;\n  overflow: hidden;\n}\n\n/* progress block (front face) */\n.kcard__prog { margin-top: auto; padding: 0 0 4px; }\n.kcard__prog-row {\n  display: flex;\n  justify-content: space-between;\n  align-items: baseline;\n  margin-bottom: 6px;\n  font-family: var(--font-heading);\n}\n.kcard__prog-lbl {\n  font-size: 9.5px;\n  font-weight: 800;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n}\n.kcard__prog-pct {\n  font-size: 12px;\n  font-weight: 800;\n  color: var(--brand-teal);\n}\n.kcard__prog-bar {\n  height: 8px;\n  border-radius: 999px;\n  background: rgba(26, 83, 92, 0.08);\n  overflow: hidden;\n  position: relative;\n}\n.kcard__prog-fill {\n  height: 100%;\n  border-radius: 999px;\n  background: linear-gradient(90deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  transition: width 480ms var(--ease-out);\n}\n.kcard__prog-meta {\n  margin-top: 6px;\n  font-family: var(--font-body);\n  font-size: 11.5px;\n  color: var(--fg-muted);\n  display: flex;\n  justify-content: space-between;\n  gap: 8px;\n}\n\n/* foot */\n.kcard__foot {\n  margin-top: 16px;\n  padding: 14px 22px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  display: grid;\n  grid-template-columns: 1fr auto;\n  gap: 12px;\n  align-items: center;\n}\n.kcard__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 0;\n  border: none;\n  background: transparent;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--brand-pink);\n  cursor: pointer;\n  text-align: left;\n  line-height: 1.3;\n  transition: gap var(--dur-fast) var(--ease-out);\n}\n.kcard__cta:hover { gap: 9px; }\n.kcard__val-wrap { text-align: right; }\n.kcard__val-lbl {\n  font-family: var(--font-heading);\n  font-size: 9.5px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 1px;\n}\n.kcard__val-num {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  letter-spacing: -0.01em;\n  color: var(--brand-teal);\n}\n\n/* back face (flip target) */\n.kcard__back {\n  position: absolute;\n  inset: 0;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  z-index: 10;\n  display: flex;\n  flex-direction: column;\n  overflow: hidden;\n  transform: translateY(8px) scale(0.98);\n  opacity: 0;\n  pointer-events: none;\n  transition: transform 380ms var(--ease-bounce), opacity 240ms var(--ease-out);\n  box-shadow: 0 22px 50px -22px var(--mood-shadow, rgba(26, 83, 92, 0.4));\n}\n.kcard--flipped .kcard__back {\n  transform: translateY(0) scale(1);\n  opacity: 1;\n  pointer-events: auto;\n}\n.kcard__back-head {\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  color: #fff;\n  padding: 18px 22px 16px;\n  position: relative;\n  overflow: hidden;\n}\n.kcard__back-head::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 18% 22%, rgba(255, 255, 255, 0.18) 0, transparent 35%),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n  pointer-events: none;\n}\n.kcard__back-eyebrow {\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.1em;\n  text-transform: uppercase;\n  opacity: 0.78;\n  margin-bottom: 6px;\n}\n.kcard__back-big {\n  font-family: var(--font-heading);\n  font-weight: 900;\n  font-size: 30px;\n  line-height: 1.1;\n  letter-spacing: -0.02em;\n  margin: 0;\n}\n.kcard__back-big small {\n  font-size: 13px;\n  font-weight: 700;\n  opacity: 0.8;\n  margin-left: 4px;\n  letter-spacing: 0;\n  display: block;\n  margin-top: 4px;\n}\n.kcard__back-close {\n  position: absolute;\n  top: 12px;\n  right: 12px;\n  width: 28px;\n  height: 28px;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.22);\n  color: #fff;\n  border: none;\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  z-index: 2;\n  transition: background var(--dur-fast) var(--ease-out);\n}\n.kcard__back-close:hover { background: rgba(255, 255, 255, 0.38); }\n.kcard__back-body {\n  padding: 18px 22px;\n  flex: 1 1 auto;\n  overflow-y: auto;\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n}\n\n/* milestones */\n.kcard__mile {\n  display: grid;\n  grid-template-columns: 22px 1fr auto;\n  gap: 10px;\n  align-items: center;\n  padding: 8px 0;\n  position: relative;\n}\n.kcard__mile-check {\n  width: 18px;\n  height: 18px;\n  border-radius: 6px;\n  border: 2px solid rgba(26, 83, 92, 0.18);\n  background: #fff;\n  display: grid;\n  place-items: center;\n  color: transparent;\n}\n.kcard__mile--done .kcard__mile-check {\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  border-color: transparent;\n  color: #fff;\n}\n.kcard__mile--current .kcard__mile-check {\n  border-color: var(--brand-pink);\n  box-shadow: 0 0 0 3px rgba(255, 107, 107, 0.18);\n}\n.kcard__mile-name {\n  font-family: var(--font-heading);\n  font-size: 13px;\n  font-weight: 700;\n  color: var(--brand-teal);\n  letter-spacing: -0.005em;\n}\n.kcard__mile--done .kcard__mile-name {\n  color: var(--fg-muted);\n  text-decoration: line-through;\n  text-decoration-color: rgba(26, 83, 92, 0.3);\n}\n.kcard__mile-date {\n  font-family: var(--font-body);\n  font-size: 11px;\n  color: var(--fg-muted);\n}\n\n/* back foot — 3 action buttons */\n.kcard__back-foot {\n  display: grid;\n  grid-template-columns: 1fr 1fr 1fr;\n  gap: 6px;\n  padding: 12px 14px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  background: rgba(26, 83, 92, 0.02);\n}\n.kcard__back-foot button {\n  background: #fff;\n  border: 1px solid rgba(26, 83, 92, 0.12);\n  border-radius: 10px;\n  padding: 9px 6px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 11px;\n  color: var(--brand-teal);\n  cursor: pointer;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 5px;\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    color var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n.kcard__back-foot button:hover {\n  border-color: var(--brand-pink);\n  color: var(--brand-pink);\n  transform: translateY(-1px);\n}\n\n/* No component-local reduced-motion block — global tokens clamp applies. */\n", "cssName": "contract-card.css", "source": "/**\n * Pipeline contract card with flip-to-back milestone timeline.\n * Mirrors the prototype's ContractCard component.\n *\n * Milestones aren't persisted server-side yet, so the back face renders a\n * deterministic synthesis from start / completion dates. When the backend\n * gains a Milestone shape this component will switch to consume it directly.\n */\nimport { useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\nimport type { ContractCard as Card } from \"../lib/contracts-shape.ts\";\n\ninterface Props {\n  c: Card;\n  idx: number;\n  lang?: Lang;\n}\n\ninterface Milestone {\n  name: string;\n  date: string;\n  done: boolean;\n  current?: boolean;\n}\n\nfunction fmtDate(iso: string | undefined): string {\n  if (!iso) return \"—\";\n  const d = new Date(iso);\n  if (Number.isNaN(d.getTime())) return iso;\n  return d.toLocaleDateString(undefined, { month: \"short\", day: \"numeric\" });\n}\n\nfunction buildMilestones(c: Card, lang: Lang, now = new Date()): Milestone[] {\n  const start = c.startDate ? new Date(c.startDate) : undefined;\n  const end = c.completionDate ? new Date(c.completionDate) : undefined;\n  if (!start && !end) {\n    return [\n      {\n        name: tFor(lang, \"contractCard.milestone.contractSigned\"),\n        date: \"—\",\n        done: c.mood !== \"draft\",\n      },\n      {\n        name: tFor(lang, \"contractCard.milestone.workInProgress\"),\n        date: \"—\",\n        done: c.mood === \"completed\" || c.mood === \"wrapping-up\",\n        current: c.mood === \"active\",\n      },\n      {\n        name: tFor(lang, \"contractCard.milestone.finalInvoice\"),\n        date: \"—\",\n        done: c.mood === \"completed\",\n        current: c.mood === \"wrapping-up\",\n      },\n    ];\n  }\n  const s = start ?? end!;\n  const e = end ?? start!;\n  const span = Math.max(\n    1,\n    Math.round((e.getTime() - s.getTime()) / 86_400_000),\n  );\n  const stops = [\n    { offset: 0, name: tFor(lang, \"contractCard.milestone.siteWalk\") },\n    { offset: 0.25, name: tFor(lang, \"contractCard.milestone.first\") },\n    { offset: 0.55, name: tFor(lang, \"contractCard.milestone.midPoint\") },\n    { offset: 0.85, name: tFor(lang, \"contractCard.milestone.punchList\") },\n    { offset: 1, name: tFor(lang, \"contractCard.milestone.finalWalk\") },\n  ];\n  return stops.map((stop) => {\n    const dt = new Date(s.getTime() + span * stop.offset * 86_400_000);\n    const done = dt.getTime() < now.getTime();\n    return {\n      name: stop.name,\n      date: fmtDate(dt.toISOString()),\n      done,\n    };\n  }).map((m, i, arr) => {\n    const prev = arr[i - 1];\n    const isCurrent = !m.done && (!prev || prev.done);\n    return isCurrent ? { ...m, current: true } : m;\n  });\n}\n\nexport default function ContractCard({ c, idx, lang = \"en\" }: Props) {\n  const [flipped, setFlipped] = useState(false);\n  const milestones = buildMilestones(c, lang);\n\n  const styleStr = `--mood-from:${c.moodFrom};` +\n    `--mood-to:${c.moodTo};` +\n    `--mood-shadow:${c.moodShadow};` +\n    `--mood-status:${c.statusColor};`;\n\n  function onCardClick(e: MouseEvent) {\n    if (flipped) return;\n    const target = e.target as HTMLElement;\n    if (target.closest(\".kcard__cta\") || target.closest(\".kcard__back\")) return;\n    setFlipped(true);\n  }\n\n  return (\n    <div\n      class={`kcard ${flipped ? \"kcard--flipped\" : \"\"}`}\n      style={styleStr}\n      onClick={onCardClick}\n    >\n      <div class=\"kcard__mood\">\n        <span class=\"kcard__status\">\n          <span class=\"kcard__status-dot\" />\n          {c.status}\n        </span>\n        <span class=\"kcard__when\">{c.when}</span>\n        <span class=\"kcard__numeral\">{String(idx + 1).padStart(2, \"0\")}</span>\n      </div>\n\n      <div class=\"kcard__av\">{c.initials}</div>\n\n      <div class=\"kcard__body\">\n        <div class=\"kcard__client-name\">{c.client} · #{c.id.slice(0, 8)}</div>\n        <h3 class=\"kcard__title\">{c.title}</h3>\n        <p class=\"kcard__story\">{c.story}</p>\n\n        <div class=\"kcard__prog\">\n          <div class=\"kcard__prog-row\">\n            <span class=\"kcard__prog-lbl\">{tFor(lang, \"contractCard.progress\")}</span>\n            <span class=\"kcard__prog-pct\">{c.pct}%</span>\n          </div>\n          <div class=\"kcard__prog-bar\">\n            <div class=\"kcard__prog-fill\" style={`width:${c.pct}%`} />\n          </div>\n          <div class=\"kcard__prog-meta\">\n            <span>{tFor(lang, \"contractCard.paid\", { amount: c.paid })}</span>\n            <span>{tFor(lang, \"contractCard.left\", { amount: c.left })}</span>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"kcard__foot\">\n        <button\n          class=\"kcard__cta\"\n          type=\"button\"\n          onClick={(e) => {\n            e.stopPropagation();\n            globalThis.location.assign(c.ctaHref);\n          }}\n        >\n          {c.cta} <I d={ICN.arrow} size={11} sw={2.5} />\n        </button>\n        <div class=\"kcard__val-wrap\">\n          <div class=\"kcard__val-lbl\">{tFor(lang, \"contractCard.contractLabel\")}</div>\n          <div class=\"kcard__val-num\">{c.total}</div>\n        </div>\n      </div>\n\n      <div\n        class=\"kcard__back\"\n        aria-hidden={!flipped}\n        onClick={(e) => e.stopPropagation()}\n      >\n        <div class=\"kcard__back-head\">\n          <button\n            class=\"kcard__back-close\"\n            type=\"button\"\n            onClick={(e) => {\n              e.stopPropagation();\n              setFlipped(false);\n            }}\n            aria-label={tFor(lang, \"common.close\")}\n          >\n            <I d={ICN.x} size={14} sw={2.5} />\n          </button>\n          <div class=\"kcard__back-eyebrow\">\n            #{c.id.slice(0, 8)} · {c.client}\n          </div>\n          <h4 class=\"kcard__back-big\">\n            {c.title}\n            <small>{c.total} · {c.when}</small>\n          </h4>\n        </div>\n        <div class=\"kcard__back-body\">\n          {milestones.map((m, mi) => (\n            <div\n              key={mi}\n              class={`kcard__mile ${m.done ? \"kcard__mile--done\" : \"\"} ${\n                m.current ? \"kcard__mile--current\" : \"\"\n              }`}\n            >\n              <span class=\"kcard__mile-check\">\n                {m.done && <I d={ICN.check} size={12} sw={3} />}\n              </span>\n              <span class=\"kcard__mile-name\">{m.name}</span>\n              <span class=\"kcard__mile-date\">{m.date}</span>\n            </div>\n          ))}\n        </div>\n        <div class=\"kcard__back-foot\">\n          <button\n            type=\"button\"\n            onClick={(e) => {\n              e.stopPropagation();\n              globalThis.location.assign(\"/invoices\");\n            }}\n          >\n            <I d={ICN.invoice} size={13} /> {tFor(lang, \"contractCard.action.invoice\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={(e) => {\n              e.stopPropagation();\n              globalThis.location.assign(\n                `/assistant?seed=${\n                  encodeURIComponent(\n                    tFor(lang, \"contractCard.assistantSeed\", {\n                      client: c.client,\n                      title: c.title,\n                    }),\n                  )\n                }`,\n              );\n            }}\n          >\n            <I d={ICN.send} size={13} /> {tFor(lang, \"contractCard.action.textClient\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={(e) => {\n              e.stopPropagation();\n              globalThis.location.assign(`/c/${c.id}`);\n            }}\n          >\n            <I d={ICN.contract} size={13} /> {tFor(lang, \"contractCard.action.viewContract\")}\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n}\n", "sourceName": "ContractCard.tsx", "checklist": "# Capture checklist — ContractCard\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/contracts` — the only host. Cards live inside the\n  `ContractTrack` bands. To see every mood you need an account whose contracts\n  span the moods (active → Track 01, starting-soon → Track 02, wrapping-up →\n  Track 03, draft/stale → Track 04). Track 01 is open by default; expand 02/03/04\n  (click `.ktrack__head`) to reveal their cards.\n\n## Viewports\ncontract-card.css has **no media query** — the card is fixed-internal, reflowing\nonly via the `.kcards` grid. Shoot at the host-page breakpoints:\n- **1280px** (`--container-product`) — cards 3-up.\n- **1100px** (contracts-sections KPI reflow point) — grid still auto-fill.\n- **700px / ~360px** — single-column; verify the 2-line title clamp + 3-line\n  story clamp + the foot grid (CTA / value) stay legible at the narrowest column.\n\n## Element(s) to crop\n- A single `<div class=\"kcard\">` front face: gradient `.kcard__mood` (status pill,\n  when pill, ghost numeral), `.kcard__av` avatar, body, progress bar, foot.\n- The same card **flipped** (`.kcard--flipped`): `.kcard__back` head + milestone\n  list + 3-button `.kcard__back-foot`.\n- One card per mood (6 palettes) so the deterministic mood colors are documented.\n\n## Transient states to drive\n1. **mood faces (×6)** — active / starting-soon / wrapping-up / completed / draft\n   / stale. Each has a distinct gradient + status pill + when label + CTA.\n   (active picks 1 of 4 accent variants by `hashId(id)` — capture a couple ids.)\n2. **hover** — pointer over a card → `translateY(-4px)` lift + deeper mood shadow.\n3. **flip** — click the card surface (NOT the CTA, NOT the back) → back face rises\n   in (`translateY(8px)→0`, `scale(.98)→1`, fade). Capture the back: milestone\n   checklist (done = struck through + filled check; current = pink ring) + the\n   3 action buttons.\n4. **close** — click `.kcard__back-close` (×) → returns to front.\n5. **CTA isolation** — click `.kcard__cta` → should NOT flip (it stops\n   propagation and navigates). Verify it doesn't flip the card.\n\n## Crop targets — bug to capture (do NOT fix in the shot, document it)\n- **`.kcard__numeral` clip bug:** the 96px ghost numeral is `bottom:-18px` under\n  `overflow:hidden` → its descender is clipped at the mood-header bottom edge.\n  Crop the mood header bottom-right corner to show the clipped numeral. (Sibling\n  `.qcard__numeral` was fixed to `bottom:4px`; this was not ported. Flag in spec.)\n\n## Motion to film\n- **Flip:** `transform 380ms var(--ease-bounce)` + `opacity 240ms var(--ease-out)`\n  (rise + scale + fade — NOT a 3D rotate). Film one flip + close.\n- **Hover lift:** `transform 320ms var(--ease-bounce)`.\n- **Progress fill:** `width 480ms var(--ease-out)` (films on mount / pct change).\n- **Status-dot pulse:** `q-pulse-dot` 2.4s (cosmetic — note it is NOT live data).\n- Re-shoot the flip + hover with `prefers-reduced-motion: reduce` — **no\n  component-local guard**, so verify the global tokens clamp makes them instant.\n\n## NO fabricated screenshots\nAll frames must come from the live `/contracts` route. Isolate cases cover the\nsix moods + flipped state for state-completeness; the deliverable motion filmstrip\nand the numeral-clip crop come from the running app.\n"}, {"name": "contract-track", "component": "ContractTrack", "spec": "# ContractTrack\n\nCollapsible \"track\" group (numbered section header + count + collapsible body)\nthat groups the contracts pipeline into stacked bands (In progress / Starting\nsoon / Wrapping up / Drafts) on `/contracts`. The contracts-specific twin of the\nshared `QuoteTrack` — same collapse mechanic, **separate component** with a\nnarrower prop surface.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ContractTrack.tsx`).\n- **Interaction tier:** `island` (client-only state) — a controlled disclosure.\n- **Client state owned + refresh:**\n  - `open: boolean` — section expanded/collapsed. Seeded lazily from\n    `localStorage[storageKey]` (`\"1\"`/`\"0\"`) when a `storageKey` is given, else\n    `defaultOpen`. Written back to that key in a `useEffect` on every change (so\n    a contractor's per-track open/closed preference survives reloads). No data\n    refresh — the body content is frozen props passed by the parent.\n- **Server action + flash:** none. This is a pure layout/disclosure shell; it\n  performs no mutations and renders no flash.\n- **`location.reload()` flag:** none — state is local + `localStorage`-persisted.\n  No reload, no PRG, no Partial needed. (Note: `count` and `children` are frozen\n  props — if the parent `ContractsPage` re-fetches, it must re-render\n  ContractTrack with a new `count` string and new children; ContractTrack never\n  fetches on its own.)\n- **Data source per region:** none of its own. `num`, `title`, `count` are\n  **props** supplied by the parent page island (`ContractsPage`), which has\n  already pre-formatted `count` into a localized string\n  (`plural(\"contractsPage.inProgress.count\", n)`). The body comes via `children`.\n  No `clients/contracts.ts` or `ssrBackendGet` call here — the parent owns the\n  fetch (see `contracts-page/`).\n- **Honest-empty:** ContractTrack does **not** own an empty state. The parent\n  passes either a `.kcards` grid of `ContractCard`s **or** a `.kempty` slate as\n  `children`; the count label (e.g. \"0 contracts\") always renders from the prop.\n  (Track 04 Drafts is the exception — the parent omits the whole `<ContractTrack>`\n  element when `drafts.length === 0`, so no empty slate ever shows for drafts.)\n- **Liveness:** none (request-response via parent). No polling/websocket.\n- **Reactivity / i18n:** **no `lang` prop** (unlike QuoteTrack). `count` arrives\n  pre-localized as a string; `title` arrives pre-translated. The component does\n  not call `tFor` — it re-localizes only when the parent re-renders it.\n- **Data-shape hazards:**\n  - `count` is a **frozen pre-formatted string** (\"3 contracts\"), not a\n    `number`+`unit` pair. ContractTrack cannot re-pluralize; if the parent passes\n    a stale string the count desyncs from the body. (QuoteTrack takes a `number`\n    and localizes itself — this twin pushed that responsibility up to the parent.)\n  - `storageKey` collisions: the parent must pass distinct keys per track\n    (`contracts:track:01`…`04`). Reusing a key across tracks would share\n    open-state. Reusing the `pm:qtrack:*` keys would collide with QuoteTrack.\n\n## 2. Anatomy\n```\n<section class=\"ktrack [ktrack--collapsed]\">\n  <header class=\"ktrack__head\" onClick=toggle>\n    <span class=\"ktrack__chev\"><I chev size=14 sw=2.5/></span>   ← rotate(90deg) open / rotate(0deg) collapsed\n    <span class=\"ktrack__num\">{num}</span>                       ← e.g. \"01\" (pink, tracking-wide)\n    <h2 class=\"ktrack__title\">{title}</h2>                       ← e.g. \"In progress\" (QuoteTrack uses a <span>)\n    <span class=\"ktrack__count\">{count}</span>                   ← pre-formatted string, margin-left:auto\n  </header>\n  <div class=\"ktrack__body\">\n    <div class=\"ktrack__body-inner\">{children}</div>             ← .kcards grid OR .kempty slate\n  </div>\n</section>\n```\n- **Slots/children:** yes — `children` render inside `.ktrack__body-inner` (the\n  parent passes the track's cards/empty slate here).\n- **Icon dependency:** `I` + `ICN.chev` from `lib/dash-icons.tsx` (copied to\n  `js/dash-icons.tsx`).\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `num` | string (required) | — | text | no |\n| `title` | string (required) | — | text | no |\n| `count` | string (required, pre-formatted) | — | text | no |\n| `defaultOpen` | boolean | `true` | boolean | no |\n| `storageKey` | string | `undefined` (no persistence) | text | no |\n| `children` | ComponentChildren | — | (slot) | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| open | expanded body (`defaultOpen=true`, Track 01) | `cases/open/open.json` |\n| collapsed | `ktrack--collapsed`, body rows 0fr (Track 02) | `cases/collapsed/collapsed.json` |\n| empty | `count=\"0 contracts\"` + `.kempty` slate as children | `cases/empty/empty.json` |\n| drafts | Track 04 \"Drafts\" wording, collapsed | `cases/drafts/drafts.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"header.ktrack__head\" && e.type === \"click\")` →\n  toggles `open`; the `useEffect` writes `localStorage[storageKey]` (`\"1\"`/`\"0\"`)\n  when `storageKey` is set.\n\n## 6. Motion (extracted from contract-track.css)\n- **Chevron:** `.ktrack__chev` is `transform: rotate(90deg)` open (points down);\n  `.ktrack--collapsed .ktrack__chev` is `rotate(0deg)` (points right), over\n  `transform 240ms var(--ease-bounce)` (bouncy overshoot).\n- **Body:** `.ktrack__body` animates `grid-template-rows: 1fr → 0fr` **and**\n  `margin-top: 14px → 0`, both `320ms var(--ease-out)`. The inner\n  `.ktrack__body-inner` is `overflow:hidden; min-height:0`, so it clips smoothly\n  as the row track collapses — the height-animation-without-fixed-px technique.\n- **Header border:** `.ktrack__head` border-color transition `var(--dur-fast)\n  var(--ease-out)` on hover (trivial).\n- **Jank finding:** animating `grid-template-rows` is smooth in modern engines;\n  older Safari ignores it (snap to final, not jank — degrades acceptably). No\n  layout thrash since the row track, not a measured pixel height, is animated.\n- **Reduced motion:** **no component-local guard** — relies on the global tokens\n  reduced-motion clamp (`animation/transition-duration → 0.01ms !important`).\n  Verify the collapse becomes instant under `prefers-reduced-motion: reduce`.\n\n## 7. Responsive\n- **No own `@media` queries.** The header is a flex baseline row; the body\n  inherits the host page's `.kcards` grid responsiveness (the grid itself has no\n  media query — `repeat(auto-fill, minmax(300px,1fr))` reflows fluidly). The KPI\n  and hero breakpoints (1100px, 700px) live in contracts-sections.css, not here.\n  Verify the track within `/contracts` at its own page breakpoints.\n\n## 8. A11y\n- **Gaps (same as QuoteTrack):** the disclosure header is a `<header>` with an\n  `onClick`, NOT a `<button>` — it is not keyboard-focusable or operable, and\n  exposes no `aria-expanded`/`aria-controls`. The title is correctly an `<h2>`\n  (good heading semantics) but the toggle target itself is not reachable by\n  keyboard. **Rebuild fix:** make the header (or a wrapping element) a `<button>`\n  (or `role=\"button\"` + `tabindex=0` + Enter/Space key handlers) with\n  `aria-expanded={open}` and `aria-controls` pointing at the `.ktrack__body` id.\n  `user-select:none` is set to avoid text-selection on click.\n\n## 9. Used on\n**Only** `islands/ContractsPage.tsx` (`/contracts`) — mounted up to 4× (Tracks\n01–04). Evidence: grep of `ContractTrack` import resolves solely to\n`ContractsPage`. CSS lives in `static/contracts.css` (`.ktrack*` rules, extracted\nto `css/contract-track.css`). It is a deliberate sibling of, not a reuse of, the\nshared `QuoteTrack` (`shared-components/quote-track/`) — see that spec for the\n`number`+`unit`+`lang` variant.\n", "fixture": "{\n  \"category\": \"contracts\",\n  \"folder\": \"contract-track\",\n  \"controls\": {\n    \"num\": { \"type\": \"text\", \"value\": \"01\" },\n    \"title\": { \"type\": \"text\", \"value\": \"In progress\" },\n    \"count\": { \"type\": \"text\", \"value\": \"3 contracts\" },\n    \"defaultOpen\": { \"type\": \"boolean\", \"value\": true },\n    \"storageKey\": { \"type\": \"text\", \"value\": \"contracts:track:01\" }\n  }\n}\n", "cases": [{"name": "collapsed.json", "content": "{\n  \"_name\": \"Collapsed — Track 02 Starting soon (body clipped)\",\n  \"num\": \"02\",\n  \"title\": \"Starting soon\",\n  \"count\": \"2 contracts\",\n  \"defaultOpen\": false,\n  \"storageKey\": \"contracts:track:02\",\n  \"_signals\": { \"open\": false },\n  \"_innerHtml\": \"<div class=\\\"kcards\\\">[ hidden body ]</div>\"\n}\n"}, {"name": "drafts.json", "content": "{\n  \"_name\": \"Drafts — Track 04 (only rendered when drafts > 0)\",\n  \"num\": \"04\",\n  \"title\": \"Drafts\",\n  \"count\": \"1 contract\",\n  \"defaultOpen\": false,\n  \"storageKey\": \"contracts:track:04\",\n  \"_signals\": { \"open\": false },\n  \"_innerHtml\": \"<div class=\\\"kcards\\\" style=\\\"opacity:.4\\\">[ draft ContractCard ]</div>\"\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — zero count + .kempty slate as children\",\n  \"num\": \"03\",\n  \"title\": \"Wrapping up\",\n  \"count\": \"0 contracts\",\n  \"defaultOpen\": true,\n  \"storageKey\": \"contracts:track:03\",\n  \"_signals\": { \"open\": true },\n  \"_innerHtml\": \"<div class=\\\"kempty\\\">No jobs wrapping up right now.</div>\"\n}\n"}, {"name": "open.json", "content": "{\n  \"_name\": \"Open — Track 01 In progress expanded\",\n  \"num\": \"01\",\n  \"title\": \"In progress\",\n  \"count\": \"3 contracts\",\n  \"defaultOpen\": true,\n  \"storageKey\": \"contracts:track:01\",\n  \"_signals\": { \"open\": true },\n  \"_innerHtml\": \"<div class=\\\"kcards\\\" style=\\\"opacity:.4\\\">[ ContractCard grid slot ]</div>\"\n}\n"}], "css": "/* ============================================================\n   ContractTrack — extracted from static/contracts.css (.ktrack* rules).\n   The contracts twin of the shared QuoteTrack (.qtrack), but a SEPARATE\n   component: it takes `count` as a pre-formatted STRING (not number+unit),\n   has no `lang` prop, and its title is an <h2> (QuoteTrack uses a <span>).\n   Same collapse mechanic (grid-template-rows 1fr→0fr + chevron rotate).\n   Tokens: --brand-teal, --brand-pink, --fg-muted, --font-heading,\n   --font-body, --dur-fast, --ease-out, --ease-bounce.\n   ============================================================ */\n\n.ktrack {\n  margin-bottom: 28px;\n}\n.ktrack__head {\n  display: flex;\n  align-items: baseline;\n  gap: 12px;\n  margin-bottom: 14px;\n  cursor: pointer;\n  user-select: none;\n  padding: 6px 0;\n  border-bottom: 1px solid rgba(26, 83, 92, 0.08);\n  transition: border-color var(--dur-fast) var(--ease-out);\n}\n.ktrack__head:hover {\n  border-color: rgba(26, 83, 92, 0.2);\n}\n.ktrack__chev {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 18px;\n  height: 18px;\n  color: var(--brand-teal);\n  transition: transform 240ms var(--ease-bounce);\n  transform: rotate(90deg);          /* open: chevron points down */\n}\n.ktrack--collapsed .ktrack__chev {\n  transform: rotate(0deg);           /* collapsed: chevron points right */\n}\n.ktrack__num {\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 800;\n  letter-spacing: 0.1em;\n  color: var(--brand-pink);\n}\n.ktrack__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 20px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  line-height: 1;\n  margin: 0;\n}\n.ktrack__count {\n  font-family: var(--font-body);\n  font-size: 13px;\n  color: var(--fg-muted);\n  margin-left: auto;\n}\n\n/* Collapsible body — animates via grid-template-rows 1fr -> 0fr */\n.ktrack__body {\n  display: grid;\n  grid-template-rows: 1fr;\n  transition:\n    grid-template-rows 320ms var(--ease-out),\n    margin-top 320ms var(--ease-out);\n  margin-top: 14px;\n}\n.ktrack__body-inner {\n  overflow: hidden;\n  min-height: 0;\n}\n.ktrack--collapsed .ktrack__body {\n  grid-template-rows: 0fr;\n  margin-top: 0;\n}\n\n/* No component-local reduced-motion block — relies on the global tokens\n   reduced-motion clamp for the chevron rotate + grid-rows collapse. */\n", "cssName": "contract-track.css", "source": "/**\n * Collapsible track group for the contracts pipeline. Track 1 (In progress)\n * defaults to open; tracks 2 + 3 collapsed. Open state persists per track in\n * localStorage so contractors who always want a specific track open get it.\n */\nimport type { ComponentChildren } from \"preact\";\nimport { useEffect, useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\n\ninterface Props {\n  num: string;\n  title: string;\n  count: string;\n  defaultOpen?: boolean;\n  storageKey?: string;\n  children?: ComponentChildren;\n}\n\nexport default function ContractTrack(\n  { num, title, count, defaultOpen = true, storageKey, children }: Props,\n) {\n  const [open, setOpen] = useState<boolean>(() => {\n    if (typeof globalThis.localStorage === \"undefined\" || !storageKey) {\n      return defaultOpen;\n    }\n    const raw = globalThis.localStorage.getItem(storageKey);\n    if (raw === \"1\") return true;\n    if (raw === \"0\") return false;\n    return defaultOpen;\n  });\n\n  useEffect(() => {\n    if (!storageKey) return;\n    try {\n      globalThis.localStorage?.setItem(storageKey, open ? \"1\" : \"0\");\n    } catch { /* ignore */ }\n  }, [open, storageKey]);\n\n  return (\n    <section class={`ktrack ${open ? \"\" : \"ktrack--collapsed\"}`}>\n      <header class=\"ktrack__head\" onClick={() => setOpen((v) => !v)}>\n        <span class=\"ktrack__chev\">\n          <I d={ICN.chev} size={14} sw={2.5} />\n        </span>\n        <span class=\"ktrack__num\">{num}</span>\n        <h2 class=\"ktrack__title\">{title}</h2>\n        <span class=\"ktrack__count\">{count}</span>\n      </header>\n      <div class=\"ktrack__body\">\n        <div class=\"ktrack__body-inner\">{children}</div>\n      </div>\n    </section>\n  );\n}\n", "sourceName": "ContractTrack.tsx", "checklist": "# Capture checklist — ContractTrack\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/contracts` — the only host. Track 01 \"In progress\"\n  defaults open; Tracks 02 \"Starting soon\" / 03 \"Wrapping up\" default collapsed;\n  Track 04 \"Drafts\" appears collapsed **only when** there is ≥1 draft/stale\n  contract.\n\n## Viewports\nRe-shoot at this component's own / host-page CSS breakpoints (contract-track.css\nhas no media query; the surrounding sections do):\n- **1280px** (`--container-product`).\n- **1100px** (contracts-sections.css KPI/hero reflow — header still single row).\n- **700px** (contracts-sections.css single-column — confirm the header baseline\n  row wraps cleanly: chevron + num + h2 title + right-aligned count).\n\n## Element(s) to crop\n- A single `<section class=\"ktrack\">` — the header row (`.ktrack__chev` +\n  `.ktrack__num` + `.ktrack__title` h2 + `.ktrack__count`) plus its body. Capture\n  one expanded instance (Track 01, body full of `.kcards`) and one\n  `ktrack--collapsed` instance (Track 02/03).\n\n## Transient states to drive\n1. **open** — Track 01 expanded with `ContractCard`s in the body.\n2. **collapsed** — click `.ktrack__head` to collapse (chevron rotates from\n   pointing down → right; body grid-rows clip to 0fr; margin-top → 0). **Reload**\n   to confirm `localStorage[\"contracts:track:02\"]` persisted the closed state.\n3. **empty** — a track whose parent passed a `.kempty` slate as children (drive\n   by an account with no contracts in that mood) → count reads \"0 contracts\" with\n   the dashed empty slate inside.\n4. **drafts** — Track 04 present (account with ≥1 draft/stale contract); confirm\n   it is absent entirely on an account with no drafts.\n\n## Motion to film\n- Collapse/expand toggle: chevron `rotate(90deg↔0deg)` over **240ms\n  var(--ease-bounce)** + body `grid-template-rows 1fr↔0fr` (+ `margin-top\n  14px↔0`) over **320ms var(--ease-out)**. Film one full toggle. Re-shoot with\n  `prefers-reduced-motion: reduce` — there is **no component-local guard**, so\n  verify the global tokens clamp makes the collapse instant.\n\n## NO fabricated screenshots\nAll frames must come from the live `/contracts` route. Do not synthesize the\ntrack from isolate alone for the deliverable filmstrip; isolate is for state\ncoverage, the live route is the source of truth for motion + layout.\n"}, {"name": "contracts-page", "component": "ContractsPage", "spec": "# ContractsPage\n\nThe top-level data island for `/contracts`. The SSR route ships only the app\nshell + a skeleton; this island fetches contracts + customers + quotes on mount,\nderives each contract's `mood`, groups the cards into pipeline buckets, and\ncomposes the whole page body (Hero → KPIs → ScheduleStrip → up to 4\nContractTracks of ContractCards).\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/ContractsPage.tsx`) — **whole-page island**. It\n  is also the page's only composition root: it renders `ContractsHero`,\n  `ContractsKpis`, `ScheduleStrip` (page-composition children),\n  `ContractTrack` + `ContractCard` (child islands), and the shared `Skeletons`.\n- **Per-interaction tier:**\n  - The **page body** is `island` (client-only). On mount it fires three GETs\n    (`/contracts`, `/clients`, `/quotes`), derives + groups, then **freezes** the\n    result in `useState` for the page lifetime.\n  - There are **no forms / no PRG / no Fresh Partials** on this page. All\n    \"actions\" are child-`ContractCard` navigations (`location.assign`) to other\n    routes; nothing mutates `/contracts` data in place.\n- **Island client-state owned + refresh:**\n  - `s: State = { loading, error, contracts, customers, quotes }` (`useState`,\n    seeded `INITIAL` with `loading:true`). Set **once** in the mount `useEffect`\n    `Promise.all([...])`; an `alive` flag guards against setState-after-unmount.\n  - **No refresh path:** there is no re-fetch, no interval, no subscription, no\n    refetch-on-focus. The data is fetched exactly once per mount and held.\n- **Server action + flash:** none here (no mutation on this page).\n- **`location.reload()` flag — FROZEN-SSR / single-fetch anti-pattern:**\n  - This page has **no `location.reload()`** of its own (unlike `/quotes`, which\n    reloads after a delete). The page-level note in `contracts.md` confirms it's\n    *less* exposed than /quotes. **However** the frozen-single-fetch pattern is\n    the real smell: the island fetches once and freezes; any data change made\n    elsewhere (e.g. the card CTAs that `location.assign('/invoices')` to confirm a\n    payment, then the contractor navigates back) is **invisible until a full page\n    reload** because the browser re-mounts the island only on hard nav. The child\n    `ContractCard` CTAs *are* full navigations, so coming back to `/contracts`\n    does re-mount and re-fetch — the freeze is masked by the fact that every exit\n    is a hard nav. If those CTAs were ever switched to SPA/Partial routing, stale\n    contract moods would surface.\n  - **The form+PRG / Partial fix (for the rebuild):** the contracts list +\n    derived moods should be **server-rendered** (SSR the route with the contract\n    data, derive mood server-side — the backend already does `deriveMood`), and\n    any mutation (confirm payment, send invoice) should be a **form → POST →\n    303 PRG redirect back to `/contracts`** so the freshly-derived list re-renders\n    server-side, OR a **Fresh Partial** (`f-client-nav` + a `<Partial>` wrapping\n    the tracks) that re-renders just the tracks after the action without a\n    whole-island remount or a `reload()`. Either removes the \"fetch-once-and-pray\"\n    client island entirely. The skeleton-then-fetch flash (the page shows\n    `PageHeaderSkeleton` + `CardGridSkeleton rows={3}` on every cold mount because\n    **nothing is SSR'd for the body**) also disappears once the body is SSR'd.\n- **Data source per region:**\n  - `/contracts` → `contractsClient.list()` → `Contract[]` (each with server-\n    derived `mood`). `.catch(() => [])` — 404/error-safe to empty.\n  - `/clients` → `clientsClient.list()` → `CustomerCard[]` (used **only** to build\n    a `customerId → name` map; the rollup fields are ignored here). `.catch([])`.\n  - `/quotes` → `quotesClient.list()` → `QuoteCard[]` (used **only** to build a\n    `quoteId → jobName||summary` map for card titles). `.catch([])`.\n  - These run via `Promise.all` (parallel). The whole island consumes\n    `clients/contracts.ts`, `clients/clients.ts`, `clients/quotes.ts` — NOT\n    `ssrBackendGet` (no SSR data for the body; see contracts.md \"SSR data: None\").\n  - Each raw `Contract` → `lib/contracts-shape.ts` `toContractCard({contract,\n    customerNames, quoteSummaries, now, index})` → the rich `ContractCard` the\n    children consume.\n- **Honest-empty:**\n  - Each track shows a `.kempty` slate when its mood bucket is empty (Tracks\n    01–03). **Track 04 Drafts is omitted entirely** when `drafts.length === 0` (no\n    element, no slate).\n  - Hero shows its `allZero` empty line; ScheduleStrip shows `.csched__empty` when\n    no bars fall in range. A fully-empty account therefore renders the hero +\n    4 zero-KPIs + empty schedule + three empty-slate tracks — coherent, not blank.\n  - **Defensive `Array.isArray(...)` guards** re-wrap each fetched list (in case a\n    client returns a non-array) before grouping.\n- **Liveness:** request-response, **once** per mount. No polling, no websocket.\n  (Cosmetic pulses live in the children, not here.)\n- **Reactivity / i18n:** reads `langSignal.value` at render → re-localizes the\n  whole page (titles, track counts via a local `plural()` helper, child labels)\n  when Settings flips language; the underlying data is not re-fetched on flip.\n- **Data-shape hazards:**\n  - **Mood grouping over an unpaginated list (THE flagged data-shape hazard).**\n    `contracts.map(toContractCard)` then five `.filter(c.mood === …)` passes run\n    over the **entire** `/contracts` response with **no pagination** — the\n    client-side face of data-model §5.9 (\"Contract list `mood` … full-list read\n    with no pagination\"). At demo scale (handful of contracts) it's free; with\n    hundreds of contracts the island fetches + derives + 5×-filters the whole set\n    on the client every mount. **Mitigation:** paginate or pre-bucket server-side\n    (the backend already derives `mood`; have it return mood-bucketed counts +\n    paged lists per bucket).\n  - **`active` is the catch-all fallback mood.** `toContractCard` defaults\n    `mood = contract.mood ?? \"active\"` — a contract with a missing/unknown server\n    mood silently lands in \"In progress\" (Track 01). Could over-count active.\n  - **`liveCards = active + starting-soon + wrapping-up`** drives the hero total\n    and the schedule strip — `completed` and `draft`/`stale` are **excluded** from\n    the headline \"$ committed\" figure by design.\n  - **Money 100×-low guard.** Card money fields are dollar strings; the island\n    re-parses them to **cents** (`Math.round(parseMoney(c[key]) * 100)`) before\n    handing to `fmtMoney`-consuming sections — the explicit fix for the\n    \"$10 of a $1,000 contract\" bug. Do not pass dollars to the sections.\n  - **`parseMoney` strips all non-`[0-9.]`** — fine for \"$1,000.00\" but a string\n    with two dots or thousands-dots in es-locale formatting could misparse;\n    upstream `fmtMoney` controls the format so it's currently safe.\n\n## 2. Anatomy\n```\nContractsPage (island)\n├─ loading → <><ShimmerStyle/><PageHeaderSkeleton/><CardGridSkeleton rows={3}/></>   ← shared Skeletons\n├─ error   → <div class=\"kpage-error\">{loadError}: {msg}</div>                        ← UNSTYLED class (hazard, see §6/css)\n└─ ready   → <>\n     <ContractsHero …/>                          ← page-composition\n     <ContractsKpis …/>                           ← page-composition\n     <ScheduleStrip cards={liveCards} …/>         ← page-composition\n     <ContractTrack num=\"01\" In progress …>       ← child island\n        {inProgress.length ? <div class=\"kcards\">{inProgress.map(ContractCard)}</div> : <div class=\"kempty\"/>}\n     <ContractTrack num=\"02\" Starting soon … defaultOpen={false}>  … </ContractTrack>\n     <ContractTrack num=\"03\" Wrapping up … defaultOpen={false}>    … </ContractTrack>\n     {drafts.length > 0 &&\n       <ContractTrack num=\"04\" Drafts … defaultOpen={false}>\n          <div class=\"kcards\">{drafts.map(ContractCard)}</div>     ← no empty slate (only rendered if >0)\n       </ContractTrack>}\n   </>\n```\n- **Grouping:** `inProgress=mood active`, `startingSoon=starting-soon`,\n  `wrappingUp=wrapping-up`, `closed=completed`, `drafts=draft||stale`.\n  `liveCards = inProgress + startingSoon + wrappingUp`.\n- **Dependency (not a UI component):** `lib/contracts-shape.ts`\n  (`toContractCard`, `moodFor`) — the projection every child consumes.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` (`\"en\"\\|\"es\"`) | (reads `langSignal.value`) | select | yes (`langSignal`) |\n\n> The only prop is the optional `lang`; in practice the island reads\n> `langSignal.value` directly, so `lang` is effectively signal-driven. All page\n> data is fetched, not passed — so isolate cases drive the **fetched data** via\n> `_mocks` (the three clients) + `_signals` for the language, not via props.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | mount before fetch resolves → Skeletons | `cases/loading/loading.json` |\n| error | a fetch rejects (not caught to []) → `.kpage-error` | `cases/error/error.json` |\n| populated | full board: hero + KPIs + strip + tracks 01–04 | `cases/populated/populated.json` |\n| empty | fetch resolves to []/[]/[] → zero hero + empty slates, no Track 04 | `cases/empty/empty.json` |\n| es | Spanish — whole page re-localized | `cases/es/es.json` |\n\n## 5. Events\nThe island owns no direct DOM events (no buttons of its own). Its observable\nbehavior is the **mount fetch** and the resulting render; child events live in\ntheir own specs. Capture the lifecycle as:\n- `ev.expect(e => e.source === \"island#ContractsPage\" && e.type === \"mount\")` →\n  fires `Promise.all([contracts.list, clients.list, quotes.list])`; on resolve\n  sets `{loading:false, contracts, customers, quotes}`; on reject sets `error`.\n- (Child events — track collapse, card flip, card CTA navigation — are specified\n  in `contract-track/`, `contract-card/`.)\n\n## 6. Motion (extracted)\n- **The island itself has no motion** — its CSS (`contracts-page.css`) styles only\n  the `.kpage-error` text and documents the loading state. All visible motion\n  belongs to the children: skeleton shimmer (shared `Skeletons`/`ShimmerStyle`),\n  hero pulse + CTA hover + schedule hover (contracts-sections), track collapse\n  (contract-track), card flip/hover/prog-fill (contract-card).\n- **`.kpage-error` HAZARD (real, flagged in css):** the error branch renders\n  `<div class=\"kpage-error\">` but **that class has no rule in `static/contracts.css`\n  or `static/dashboard.css`** — the error message renders as unstyled body text. A\n  minimal rule is reproduced in `css/contracts-page.css` for isolate legibility\n  only; the rebuild should add a real error-card style.\n- **Loading→ready transition:** there is no crossfade — skeletons are swapped for\n  content in one render (a hard pop). Rebuild could fade, but it's not jank.\n- **Reduced motion:** N/A at this level (no own animation); the children carry\n  the (global-clamp-only) reduced-motion behavior.\n\n## 7. Responsive\n- **No own `@media` queries.** `contracts-page.css` only styles the error text +\n  documents loading. The page body is a **single stacked column** (NO two-column\n  `.qlay` layout, unlike /quotes — see contracts.md). All responsiveness comes\n  from the children's CSS (contracts-sections @1100/700px; the `.kcards` auto-fill\n  grid). Verify the whole stack at 1280 / 1100 / 700px on the live route.\n\n## 8. A11y\n- **Loading state** is a visual skeleton with no `aria-busy`/live-region\n  announcement — a screen reader hears nothing while the page is empty then\n  suddenly hears the full board. **Rebuild:** add `aria-busy` / a polite\n  live-region status, or (better) SSR the body so there is no empty phase.\n- **Error state** is unstyled, unlabeled text (`.kpage-error`) — add `role=\"alert\"`\n  + a real error card so failures are announced and visible.\n- Heading order: the page emits `ContractsHero`'s `<h1>` then each track's `<h2>`\n  then each card's `<h3>` — a sane outline (good), provided the sections render in\n  source order (they do).\n- All deeper a11y (track keyboard operability, card flip focus) is owned by the\n  child specs.\n\n## 9. Used on\n**Only** `routes/contracts/index.tsx` (`/contracts`), as the single page-local\nisland inside the app shell (`DashSidebar` + `DashTopbar` + `<div class=\"content\">`\n— see contracts.md). Evidence: grep of `ContractsPage` import. It consumes\n`clients/contracts.ts`, `clients/clients.ts`, `clients/quotes.ts`,\n`lib/contracts-shape.ts`, the shared `Skeletons`, and mounts `ContractsHero` /\n`ContractsKpis` / `ScheduleStrip` / `ContractTrack` / `ContractCard`. CSS:\n`css/contracts-page.css` (error/loading only — all visible chrome is the children).\n", "fixture": "{\n  \"category\": \"contracts\",\n  \"folder\": \"contracts-page\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  },\n  \"components\": {\n    \"ContractsPage\": {\n      \"_note\": \"Whole-page island. Its only prop is `lang` (in practice it reads langSignal.value). All page DATA is fetched on mount — drive it in isolate via _mocks on the three clients, not props. It mounts ContractsHero/ContractsKpis/ScheduleStrip (page-composition) + ContractTrack + ContractCard (child islands) + shared Skeletons. _mocks keys stub the heavy children/clients so a case can pin loading / error / populated / empty deterministically.\",\n      \"_mocks\": {\n        \"contractsClient.list\": \"[] (override per case with a Contract[] carrying server `mood`)\",\n        \"clientsClient.list\": \"[] (CustomerCard[] — only customerId→name is used)\",\n        \"quotesClient.list\": \"[] (QuoteCard[] — only quoteId→jobName||summary is used)\",\n        \"ContractCard\": \"stub\",\n        \"ContractTrack\": \"passthrough (renders children)\",\n        \"ScheduleStrip\": \"stub\",\n        \"Skeletons\": \"real (loading state needs the shimmer)\"\n      }\n    }\n  }\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty — all three fetches resolve to [] (fresh account)\",\n  \"_note\": \"loading:false, no error, empty lists. Hero allZero line, KPIs all 0, .csched__empty strip, Tracks 01–03 each show a .kempty slate, Track 04 Drafts is omitted entirely (drafts.length === 0).\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": false, \"error\": null },\n  \"_mocks\": {\n    \"contractsClient.list\": [],\n    \"clientsClient.list\": [],\n    \"quotesClient.list\": []\n  }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — a fetch rejects past the .catch([]) guard\",\n  \"_note\": \"Per-client .catch(()=>[]) swallows individual failures; this case rejects the outer Promise.all chain (e.g. a throw before .then) so loading:false + error set → <div class=\\\"kpage-error\\\">. NOTE: .kpage-error has no real style in contracts.css/dashboard.css — renders as unstyled text (flagged hazard).\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": false, \"error\": \"Network request failed (503)\" },\n  \"_mocks\": {\n    \"contractsClient.list\": \"reject: Network request failed (503)\"\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — whole page re-localized via langSignal\",\n  \"_note\": \"Same populated data; langSignal flipped to es. Doc title, hero, KPI labels, track titles + counts (local plural helper), card chrome, schedule labels all re-localize. Underlying data is NOT re-fetched on flip.\",\n  \"lang\": \"es\",\n  \"_signals\": { \"loading\": false, \"error\": null, \"lang\": \"es\" },\n  \"_mocks\": {\n    \"contractsClient.list\": [\n      { \"id\": \"ctr_9f3a2b71c0d4e5f6\", \"userId\": \"u1\", \"quoteId\": \"qt_a1\", \"customerId\": \"cus_1\", \"status\": \"signed\", \"startDate\": \"2026-06-13\", \"estimatedCompletionDate\": \"2026-06-27\", \"totalAmount\": 1450000, \"createdAt\": \"2026-06-10T15:04:00Z\", \"updatedAt\": \"2026-06-13T09:00:00Z\", \"mood\": \"active\" },\n      { \"id\": \"ctr_b21c77aa401de9f0\", \"userId\": \"u1\", \"quoteId\": \"qt_c3\", \"customerId\": \"cus_3\", \"status\": \"signed\", \"startDate\": \"2026-06-23\", \"estimatedCompletionDate\": \"2026-07-02\", \"totalAmount\": 890000, \"createdAt\": \"2026-06-14T11:00:00Z\", \"updatedAt\": \"2026-06-15T11:00:00Z\", \"mood\": \"starting-soon\" }\n    ],\n    \"clientsClient.list\": [\n      { \"id\": \"cus_1\", \"userId\": \"u1\", \"name\": \"Maria Delgado\" },\n      { \"id\": \"cus_3\", \"userId\": \"u1\", \"name\": \"Northgate Property Mgmt\" }\n    ],\n    \"quotesClient.list\": [\n      { \"id\": \"qt_a1\", \"userId\": \"u1\", \"jobName\": \"Terraza de Cedro\", \"createdAt\": \"2026-06-09T09:00:00Z\", \"updatedAt\": \"2026-06-09T09:00:00Z\", \"stage\": \"won\" },\n      { \"id\": \"qt_c3\", \"userId\": \"u1\", \"jobName\": \"Baño Unidad 4B\", \"createdAt\": \"2026-06-13T09:00:00Z\", \"updatedAt\": \"2026-06-13T09:00:00Z\", \"stage\": \"won\" }\n    ]\n  }\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — mount before fetch resolves (Skeletons)\",\n  \"_note\": \"loading:true initial state. ShimmerStyle + PageHeaderSkeleton + CardGridSkeleton rows={3}. Drive by holding the three client promises pending.\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": true },\n  \"_mocks\": {\n    \"contractsClient.list\": \"pending\",\n    \"clientsClient.list\": \"pending\",\n    \"quotesClient.list\": \"pending\",\n    \"Skeletons\": \"real\"\n  }\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — full board, tracks 01–04 with grouped cards\",\n  \"_note\": \"Real Contract[] (each with server `mood`) + CustomerCard[] (names) + QuoteCard[] (titles). toContractCard projects each → grouped by mood: active→Track01, starting-soon→Track02, wrapping-up→Track03, draft|stale→Track04. liveCards = active+starting-soon+wrapping-up drives hero/strip.\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": false, \"error\": null },\n  \"_mocks\": {\n    \"contractsClient.list\": [\n      { \"id\": \"ctr_9f3a2b71c0d4e5f6\", \"userId\": \"u1\", \"quoteId\": \"qt_a1\", \"customerId\": \"cus_1\", \"status\": \"signed\", \"startDate\": \"2026-06-13\", \"estimatedCompletionDate\": \"2026-06-27\", \"totalAmount\": 1450000, \"signedAt\": \"2026-06-11T12:00:00Z\", \"createdAt\": \"2026-06-10T15:04:00Z\", \"updatedAt\": \"2026-06-13T09:00:00Z\", \"mood\": \"active\" },\n      { \"id\": \"ctr_44e9013ab8c1207d\", \"userId\": \"u1\", \"quoteId\": \"qt_b2\", \"customerId\": \"cus_2\", \"status\": \"signed\", \"startDate\": \"2026-06-08\", \"estimatedCompletionDate\": \"2026-06-20\", \"totalAmount\": 600000, \"createdAt\": \"2026-06-05T10:00:00Z\", \"updatedAt\": \"2026-06-18T08:00:00Z\", \"mood\": \"wrapping-up\" },\n      { \"id\": \"ctr_b21c77aa401de9f0\", \"userId\": \"u1\", \"quoteId\": \"qt_c3\", \"customerId\": \"cus_3\", \"status\": \"signed\", \"startDate\": \"2026-06-23\", \"estimatedCompletionDate\": \"2026-07-02\", \"totalAmount\": 890000, \"createdAt\": \"2026-06-14T11:00:00Z\", \"updatedAt\": \"2026-06-15T11:00:00Z\", \"mood\": \"starting-soon\" },\n      { \"id\": \"ctr_7c0182befa334510\", \"userId\": \"u1\", \"quoteId\": \"qt_d4\", \"customerId\": \"cus_4\", \"status\": \"signed\", \"startDate\": \"2026-05-20\", \"estimatedCompletionDate\": \"2026-06-04\", \"totalAmount\": 1230000, \"signedAt\": \"2026-05-19T12:00:00Z\", \"createdAt\": \"2026-05-15T09:00:00Z\", \"updatedAt\": \"2026-06-05T09:00:00Z\", \"mood\": \"completed\" },\n      { \"id\": \"ctr_0a5512deccb73f81\", \"userId\": \"u1\", \"quoteId\": \"qt_e5\", \"customerId\": \"cus_5\", \"status\": \"draft\", \"totalAmount\": 320000, \"createdAt\": \"2026-06-16T09:00:00Z\", \"updatedAt\": \"2026-06-16T09:00:00Z\", \"mood\": \"draft\" }\n    ],\n    \"clientsClient.list\": [\n      { \"id\": \"cus_1\", \"userId\": \"u1\", \"name\": \"Maria Delgado\" },\n      { \"id\": \"cus_2\", \"userId\": \"u1\", \"name\": \"Hans Hansen\" },\n      { \"id\": \"cus_3\", \"userId\": \"u1\", \"name\": \"Northgate Property Mgmt\" },\n      { \"id\": \"cus_4\", \"userId\": \"u1\", \"name\": \"Greenwood HOA\" },\n      { \"id\": \"cus_5\", \"userId\": \"u1\", \"name\": \"Sam Rivera\" }\n    ],\n    \"quotesClient.list\": [\n      { \"id\": \"qt_a1\", \"userId\": \"u1\", \"jobName\": \"Backyard Deck Build\", \"summary\": \"build a 16x20 cedar deck\", \"createdAt\": \"2026-06-09T09:00:00Z\", \"updatedAt\": \"2026-06-09T09:00:00Z\", \"stage\": \"won\" },\n      { \"id\": \"qt_b2\", \"userId\": \"u1\", \"jobName\": \"Front Porch Repair\", \"createdAt\": \"2026-06-04T09:00:00Z\", \"updatedAt\": \"2026-06-04T09:00:00Z\", \"stage\": \"won\" },\n      { \"id\": \"qt_c3\", \"userId\": \"u1\", \"jobName\": \"Unit 4B Bathroom Reno\", \"createdAt\": \"2026-06-13T09:00:00Z\", \"updatedAt\": \"2026-06-13T09:00:00Z\", \"stage\": \"won\" },\n      { \"id\": \"qt_d4\", \"userId\": \"u1\", \"jobName\": \"Clubhouse Fence Replace\", \"createdAt\": \"2026-05-14T09:00:00Z\", \"updatedAt\": \"2026-05-14T09:00:00Z\", \"stage\": \"won\" },\n      { \"id\": \"qt_e5\", \"userId\": \"u1\", \"jobName\": \"Garage Epoxy Floor\", \"createdAt\": \"2026-06-16T09:00:00Z\", \"updatedAt\": \"2026-06-16T09:00:00Z\", \"stage\": \"draft\" }\n    ]\n  }\n}\n"}], "css": "/* ============================================================\n   ContractsPage — the island owns LOADING/ERROR shells only; all visible\n   chrome is styled by its children (ContractsSections, ContractCard,\n   ContractTrack). Unlike /quotes there is NO two-column .qlay layout — the\n   contracts body is a single stacked column (hero → kpi → schedule → tracks).\n   Tokens: --font-body, --fg.\n   ============================================================ */\n\n/* ERROR STATE — the island renders <div class=\"kpage-error\">…</div>.\n   HAZARD: this class has NO rule in static/contracts.css OR static/dashboard.css\n   — the error message renders as unstyled body text. Rebuild should add a real\n   error-card style. Reproduced minimal here for isolate legibility. */\n.kpage-error {\n  font-family: var(--font-body);\n  font-size: 14px;\n  color: var(--fg, #1a535c);\n  padding: 24px 0;\n}\n\n/* LOADING STATE — shared Skeletons: <ShimmerStyle/> + <PageHeaderSkeleton/>\n   + <CardGridSkeleton rows={3}/>. Styles owned by shared-components/skeletons. */\n", "cssName": "contracts-page.css", "source": "/**\n * Top-level data island for /contracts. The SSR route renders only the\n * page shell; this island fetches contracts + customers + quotes from the\n * backend on mount, derives the moods, and renders the schedule strip and\n * tracks.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport { type Contract, contractsClient } from \"../clients/contracts.ts\";\nimport { clientsClient, type CustomerCard } from \"../clients/clients.ts\";\nimport { type QuoteCard, quotesClient } from \"../clients/quotes.ts\";\nimport {\n  ContractsHero,\n  ContractsKpis,\n  ScheduleStrip,\n} from \"../components/ContractsSections.tsx\";\nimport ContractTrack from \"./ContractTrack.tsx\";\nimport ContractCard from \"./ContractCard.tsx\";\nimport { toContractCard } from \"../lib/contracts-shape.ts\";\nimport {\n  CardGridSkeleton,\n  PageHeaderSkeleton,\n  ShimmerStyle,\n} from \"../components/Skeletons.tsx\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  contracts: Contract[];\n  customers: CustomerCard[];\n  quotes: QuoteCard[];\n}\n\nconst INITIAL: State = {\n  loading: true,\n  error: null,\n  contracts: [],\n  customers: [],\n  quotes: [],\n};\n\nfunction parseMoney(s: string): number {\n  const n = Number(s.replace(/[^0-9.]/g, \"\"));\n  return Number.isFinite(n) ? n : 0;\n}\n\nexport default function ContractsPage(_props: { lang?: Lang } = {}) {\n  const lang = langSignal.value;\n  const [s, setS] = useState<State>(INITIAL);\n  const plural = (key: string, n: number) =>\n    tFor(lang, `${key}.${n === 1 ? \"one\" : \"other\"}`, { n });\n\n  useEffect(() => {\n    let alive = true;\n    Promise.all([\n      contractsClient.list().catch(() => [] as Contract[]),\n      clientsClient.list().catch(() => [] as CustomerCard[]),\n      quotesClient.list().catch(() => [] as QuoteCard[]),\n    ]).then(([contracts, customers, quotes]) => {\n      if (!alive) return;\n      setS({ loading: false, error: null, contracts, customers, quotes });\n    }).catch((err: Error) => {\n      if (!alive) return;\n      setS({ ...INITIAL, loading: false, error: err.message });\n    });\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  if (s.loading) {\n    return (\n      <>\n        <ShimmerStyle />\n        <PageHeaderSkeleton />\n        <CardGridSkeleton rows={3} />\n      </>\n    );\n  }\n  if (s.error) {\n    return (\n      <div class=\"kpage-error\">\n        {tFor(lang, \"contractsPage.loadError\")}: {s.error}\n      </div>\n    );\n  }\n\n  const {\n    contracts: rawContracts,\n    customers: rawCustomers,\n    quotes: rawQuotes,\n  } = s;\n  const contracts = Array.isArray(rawContracts) ? rawContracts : [];\n  const customers = Array.isArray(rawCustomers) ? rawCustomers : [];\n  const quotes = Array.isArray(rawQuotes) ? rawQuotes : [];\n  const customerNames = new Map(customers.map((c) => [c.id, c.name]));\n  // Card titles prefer the ≤3-word Job Name (roadmap p.10) so contracts read\n  // the same as the quote/invoice headings; summary is the fallback.\n  const quoteSummaries = new Map(\n    quotes\n      .filter((q) =>\n        (typeof q.jobName === \"string\" && q.jobName) ||\n        (typeof q.summary === \"string\" && q.summary)\n      )\n      .map((q) => [q.id, (q.jobName as string) || (q.summary as string)]),\n  );\n\n  const now = new Date();\n  const cards = contracts.map((contract, index) =>\n    toContractCard({ contract, customerNames, quoteSummaries, now, index })\n  );\n\n  const inProgress = cards.filter((c) => c.mood === \"active\");\n  const startingSoon = cards.filter((c) => c.mood === \"starting-soon\");\n  const wrappingUp = cards.filter((c) => c.mood === \"wrapping-up\");\n  const closed = cards.filter((c) => c.mood === \"completed\");\n  const drafts = cards.filter((c) => c.mood === \"draft\" || c.mood === \"stale\");\n\n  const liveCards = [...inProgress, ...startingSoon, ...wrappingUp];\n\n  // The KPI/hero formatter (fmtMoney) takes INTEGER CENTS, but the card\n  // strings are dollars (\"$1,000\"). Convert so totals don't read 100× low\n  // (the \"$10 of work\" bug for a $1,000 contract).\n  const centsOf = (xs: typeof cards, key: \"total\" | \"left\") =>\n    xs.reduce((sum, c) => sum + Math.round(parseMoney(c[key]) * 100), 0);\n  const dollarsOf = (xs: typeof cards) => centsOf(xs, \"total\");\n  const leftOf = (xs: typeof cards) => centsOf(xs, \"left\");\n\n  const totalCommitted = dollarsOf(liveCards);\n  const inProgressValue = dollarsOf(inProgress);\n  const startingSoonValue = dollarsOf(startingSoon);\n  const wrappingUpLeft = leftOf(wrappingUp);\n  const closedValue = dollarsOf(closed);\n  const pendingDeposits = leftOf(startingSoon);\n\n  return (\n    <>\n      <ContractsHero\n        lang={lang}\n        totalValue={totalCommitted}\n        contractCount={liveCards.length}\n        inFlightCount={inProgress.length}\n        inFlightValue={inProgressValue}\n        startingSoonCount={startingSoon.length}\n        pendingDeposits={pendingDeposits}\n      />\n      <ContractsKpis\n        lang={lang}\n        inProgressCount={inProgress.length}\n        inProgressValue={inProgressValue}\n        startingSoonCount={startingSoon.length}\n        startingSoonValue={startingSoonValue}\n        wrappingUpCount={wrappingUp.length}\n        wrappingUpLeft={wrappingUpLeft}\n        closedCount={closed.length}\n        closedValue={closedValue}\n      />\n\n      <ScheduleStrip lang={lang} cards={liveCards} />\n\n      <ContractTrack\n        num=\"01\"\n        title={tFor(lang, \"contractsPage.inProgress.title\")}\n        count={plural(\"contractsPage.inProgress.count\", inProgress.length)}\n        defaultOpen\n        storageKey=\"contracts:track:01\"\n      >\n        {inProgress.length > 0\n          ? (\n            <div class=\"kcards\">\n              {inProgress.map((c, i) => (\n                <ContractCard key={c.id} c={c} idx={i} lang={lang} />\n              ))}\n            </div>\n          )\n          : (\n            <div class=\"kempty\">\n              {tFor(lang, \"contractsPage.inProgress.empty\")}\n            </div>\n          )}\n      </ContractTrack>\n\n      <ContractTrack\n        num=\"02\"\n        title={tFor(lang, \"contractsPage.startingSoon.title\")}\n        count={plural(\"contractsPage.startingSoon.count\", startingSoon.length)}\n        defaultOpen={false}\n        storageKey=\"contracts:track:02\"\n      >\n        {startingSoon.length > 0\n          ? (\n            <div class=\"kcards\">\n              {startingSoon.map((c, i) => (\n                <ContractCard key={c.id} c={c} idx={i} lang={lang} />\n              ))}\n            </div>\n          )\n          : (\n            <div class=\"kempty\">\n              {tFor(lang, \"contractsPage.startingSoon.empty\")}\n            </div>\n          )}\n      </ContractTrack>\n\n      <ContractTrack\n        num=\"03\"\n        title={tFor(lang, \"contractsPage.wrappingUp.title\")}\n        count={plural(\"contractsPage.wrappingUp.count\", wrappingUp.length)}\n        defaultOpen={false}\n        storageKey=\"contracts:track:03\"\n      >\n        {wrappingUp.length > 0\n          ? (\n            <div class=\"kcards\">\n              {wrappingUp.map((c, i) => (\n                <ContractCard key={c.id} c={c} idx={i} lang={lang} />\n              ))}\n            </div>\n          )\n          : (\n            <div class=\"kempty\">\n              {tFor(lang, \"contractsPage.wrappingUp.empty\")}\n            </div>\n          )}\n      </ContractTrack>\n\n      {drafts.length > 0 && (\n        <ContractTrack\n          num=\"04\"\n          title={tFor(lang, \"contractsPage.drafts.title\")}\n          count={plural(\"contractsPage.drafts.count\", drafts.length)}\n          defaultOpen={false}\n          storageKey=\"contracts:track:04\"\n        >\n          <div class=\"kcards\">\n            {drafts.map((c, i) => (\n              <ContractCard key={c.id} c={c} idx={i} lang={lang} />\n            ))}\n          </div>\n        </ContractTrack>\n      )}\n    </>\n  );\n}\n", "sourceName": "ContractsPage.tsx", "checklist": "# Capture checklist — ContractsPage\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/contracts` — this island IS the page body. The route\n  ships the shell (`DashSidebar` + `DashTopbar` + `.content`) + skeleton; the\n  island fetches `/contracts` + `/clients` + `/quotes` on mount and renders\n  Hero → KPIs → ScheduleStrip → Tracks 01–04.\n\n## Viewports\nContractsPage has no own `@media`; the stack is a single column and all\nresponsiveness comes from the children. Shoot the whole page at:\n- **1280px** (`--container-product`).\n- **1100px** (KPIs reflow to 2×2, hero stacks — contracts-sections breakpoint).\n- **700px** (KPIs 1-up, full mobile stack).\n\n## Element(s) to crop / capture\n- **Whole page body** (below the topbar) at each viewport — the full\n  hero→KPI→schedule→tracks stack.\n- **Loading state** — the skeleton: `PageHeaderSkeleton` + `CardGridSkeleton\n  rows={3}` (shared Skeletons). Capture before the fetch resolves.\n- **Error state** — `<div class=\"kpage-error\">` (drive a fetch failure). Document\n  that it renders as **unstyled text** (the `.kpage-error` class has no real rule\n  in contracts.css/dashboard.css — flagged hazard).\n- **Empty state** — fresh account: hero allZero line, 4 zero-KPIs, `.csched__empty`\n  strip, Tracks 01–03 each with a `.kempty` slate, **Track 04 absent**.\n\n## Transient states to drive\n1. **loading** — hard-reload `/contracts` and capture the skeleton frame before\n   the three GETs resolve (throttle network to hold it if needed).\n2. **populated** — seeded account spanning all moods → Tracks 01 (open) + 02/03/04\n   (collapsed by default). Expand each track to show its cards.\n3. **empty** — fresh account → empty slates + no Track 04.\n4. **error** — block/500 the `/contracts` request → `.kpage-error` text.\n5. **es** — Settings → Spanish → whole page re-localizes without a re-fetch.\n6. **track expand** — click each `.ktrack__head` to expand 02/03/04 (the\n   transient track-expand state called out in the brief).\n7. **card mood states** — within the tracks, the 6 card moods + a flipped card\n   (the transient card mood/flip states called out in the brief).\n\n## Motion to film\n- The island itself has **no motion**; film the loading→ready swap (hard pop, no\n  crossfade) and defer all real motion to the child filmstrips (skeleton shimmer,\n  hero pulse/CTA, schedule hover, track collapse, card flip/hover/prog-fill).\n- Re-shoot loading + a track collapse + a card flip with `prefers-reduced-motion:\n  reduce` to verify the global tokens clamp (no component-local guards anywhere\n  in this page's tree).\n\n## NO fabricated screenshots\nEvery frame from the live `/contracts` route (drive loading via network throttle,\nempty via a fresh account, error via a blocked request, populated via a seeded\naccount). Isolate cases pin the five states deterministically for state-coverage;\nthey are not a substitute for the live page deliverable.\n"}, {"name": "contracts-sections", "component": "ContractsSections", "spec": "# ContractsSections\n\nThe three server-rendered presentational sections at the top of `/contracts`,\nexported from one module (`components/ContractsSections.tsx`):\n**`ContractsHero`** (`.kph`), **`ContractsKpis`** (`.kkpi`, 4 cards), and\n**`ScheduleStrip`** (`.csched`, a dark-teal Gantt-style 30-day lane chart). The\nempty-track slate (`.kempty`) CSS also lives in this folder (it is contracts-page\nchrome rendered by `ContractsPage`, not its own component).\n\n## 1. Classification & behavior\n- **Bucket:** `page-composition` (static section blocks). These are plain\n  functional components rendered **inside** the `ContractsPage` island's output —\n  they are not islands themselves (no `useState`/`useEffect`, no hydration of\n  their own) and have no client interactivity beyond CSS `:hover`.\n- **Per-interaction tier:**\n  - `ContractsHero` CTA: a static **`<a href=\"/assistant?seed=…\">`** — a real\n    anchor, full navigation. No form, no PRG, no island. Correct pattern.\n  - `ContractsKpis`: pure display, no interaction.\n  - `ScheduleStrip` bars: CSS `:hover` scale/brightness + a native `title`\n    tooltip; no click handler, no island.\n- **Server action + flash:** none — read-only presentation.\n- **Island client-state owned + refresh:** **none.** All three are stateless\n  functions of their props. They never re-fetch; the parent island\n  (`ContractsPage`) owns the single client-side fetch and re-renders these with\n  fresh props.\n- **`location.reload()` flag:** none in this module. (The hero CTA is a proper\n  `<a>`, not a JS navigation — contrast `ContractCard`'s button-as-link\n  anti-pattern.)\n- **Data source per region:**\n  - `ContractsHero`: all values are **props** (`totalValue`, `contractCount`,\n    `inFlightCount`, `inFlightValue`, `startingSoonCount`, `pendingDeposits`) —\n    INTEGER CENTS for money, run through `fmtMoney`. Computed by `ContractsPage`\n    from the `toContractCard` projection, NOT fetched here.\n  - `ContractsKpis`: 8 numeric/cents props, same origin.\n  - `ScheduleStrip`: a `cards: ContractCard[]` prop (the *live* cards =\n    active+starting-soon+wrapping-up) — it lays them out by\n    `scheduleStart`/`scheduleEnd` day coordinates. No fetch.\n  - None of these call `clients/contracts.ts` or `ssrBackendGet` — they are\n    downstream of the parent island's fetch.\n- **Honest-empty:**\n  - Hero: `allZero` (no in-flight, no deposits, no starting-soon) → renders the\n    `contractsHero.empty` sub-line and **hides** the active-value line.\n  - ScheduleStrip: when `placed.length === 0` (no bars in the 1..30 day window) →\n    `.csched__empty` slate (\"nothing scheduled\" copy). The grid + week labels\n    still render.\n  - `.kempty` slate (this folder's CSS, rendered by the parent): the dashed\n    empty-state box a `ContractTrack` shows when its mood bucket is empty.\n- **Liveness:** none. The pulsing eyebrow dot (`q-pulse-dot`, 2.4s) and the\n  \"TODAY\" marker are **cosmetic** — the TODAY line is a fixed coordinate\n  (`TODAY_INDEX = 8`), not a real-time tick.\n- **Reactivity / i18n:** `lang` is a prop (default `\"en\"`); every label uses\n  `tFor`/`tnFor(lang, …)`. Re-localizes only when the parent passes a new `lang`.\n- **Data-shape hazards:**\n  - **ScheduleStrip lane-packing (`packLanes`) is O(cards × lanes)** and runs on\n    every render over the *whole* live-card list with **no pagination** — fine at\n    demo scale, but it's the client-side face of the data-model hazard \"Contract\n    list `mood` … full-list read with no pagination\" (data-model §5.9). If a\n    contractor has hundreds of live contracts the strip packs them all into\n    overlapping lanes every render.\n  - **Schedule coordinates are a fixed 30-day window** (`RANGE_FROM=1`,\n    `RANGE_TO=30`, day 8 = today, anchored `now − 7 days`). Contracts outside\n    −7..+22 days are filtered out of the strip entirely (`c.scheduleEnd >= 1 &&\n    c.scheduleStart <= 30`) — a card can exist in a track but be invisible on the\n    strip. By design, but worth noting.\n  - **Money is INTEGER CENTS** into `fmtMoney`. The parent already converts the\n    card's dollar strings back to cents before passing here (the \"$10 vs $1,000\"\n    100×-low bug guard, see `ContractsPage` §1) — these components must receive\n    cents, not dollars.\n  - **Hero `subMid`/`depositsAmount`** interpolate `{n}`/`{money}` — the plural\n    of `startingSoonCount` is handled by the i18n key, not re-pluralized here.\n\n## 2. Anatomy\n```\nContractsHero — <section class=\"kph\"><div class=\"kph__inner\">\n  <div>\n    <div class=\"kph__eyebrow\"><span dot/> {eyebrow} · {N contracts}</div>\n    <h1 class=\"kph__title\"><em>{fmtMoney(totalValue)}</em> {titlePre}<br/>{titlePost}</h1>\n    <p class=\"kph__sub\">{allZero ? empty : (jobsRunning · <strong>{deposits}</strong> {subMid})}</p>\n    {!allZero && <p class=\"kph__sub\" style=\"…opacity:.75\">{activeValue}</p>}\n  </div>\n  <a class=\"kph__cta\" href=\"/assistant?seed=…\"><I plus/> {cta}</a>\n</section>\n\nContractsKpis — <div class=\"kkpi\"> (4 cards)\n  <div class=\"kkpi__card kkpi__card--accent\">  In progress  → .kkpi__num--pink {N jobs} / {active money}\n  <div class=\"kkpi__card\">                      Starting soon → {N jobs} / next-14-days money\n  <div class=\"kkpi__card\">                      Wrapping up   → {N jobs} / left-to-bill money\n  <div class=\"kkpi__card\">                      Closed (mo.)  → {N jobs} / all-paid money\n\nScheduleStrip — <section class=\"csched\">\n  <div class=\"csched__head\"> eyebrow + title + <div class=\"csched__legend\"> (in-progress / scheduled swatches)\n  <div class=\"csched__grid\">\n    WEEKS.map → <div class=\"csched__weekrow\"><div weeklbl/><div class=\"csched__weekbar\" style=\"--lanes-h\">\n        {showToday && <div class=\"csched__today\" style=\"left:…\"/>}    ← pink line + \"TODAY\" ::before\n        {bars.map → <div class=\"csched__bar [--scheduled]\" style=\"--bar-from/--bar-to;left;width;top;height\" title=\"client — when\">\n           {initials} · {firstName}</div>}\n    {placed.length===0 && <div class=\"csched__empty\">{empty}</div>}\n\n.kempty — dashed empty slate (rendered by ContractsPage into a ContractTrack body)\n```\n- **Layout math (ScheduleStrip):** `packLanes()` greedily assigns each card to the\n  first lane whose end < the card's start (else a new lane); `pos()`/`widthPct()`\n  map day coords to `%` within a week's `from..to`; `--lanes-h` sizes the row.\n- **Icon dependency:** `I` + `ICN.plus` (hero CTA) from `lib/dash-icons.tsx`\n  (copied to `js/dash-icons.tsx`).\n\n## 3. Props\nThree exported components; one row per prop. (Page-composition, so the isolate\nfixture uses a `\"components\"` map keyed by each export — see fixture.json.)\n\n**ContractsHero**\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `totalValue` | number (CENTS) | — | number | no |\n| `contractCount` | number | — | number | no |\n| `inFlightCount` | number | — | number | no |\n| `inFlightValue` | number (CENTS) | — | number | no |\n| `startingSoonCount` | number | — | number | no |\n| `pendingDeposits` | number (CENTS) | — | number | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n**ContractsKpis**\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `inProgressCount` | number | — | number | no |\n| `inProgressValue` | number (CENTS) | — | number | no |\n| `startingSoonCount` | number | — | number | no |\n| `startingSoonValue` | number (CENTS) | — | number | no |\n| `wrappingUpCount` | number | — | number | no |\n| `wrappingUpLeft` | number (CENTS) | — | number | no |\n| `closedCount` | number | — | number | no |\n| `closedValue` | number (CENTS) | — | number | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n**ScheduleStrip**\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `cards` | `ContractCard[]` | — | (array — built by `toContractCard`) | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| populated | hero + 4 KPIs + schedule strip with packed bars (typical board) | `cases/populated/populated.json` |\n| empty | hero `allZero` empty line + KPIs all 0 + `.csched__empty` slate | `cases/empty/empty.json` |\n| es | Spanish labels across all three sections | `cases/es/es.json` |\n| schedule-overlap | ScheduleStrip alone with overlapping bars → 2+ lanes packed + TODAY marker | `cases/schedule-overlap/schedule-overlap.json` |\n| kempty-slate | the `.kempty` dashed slate in isolation | `cases/kempty-slate/kempty-slate.json` |\n\n## 5. Events\nEffectively none (no JS handlers). Capture the only navigations / hover affordances:\n- `ev.expect(e => e.source === \"a.kph__cta\" && e.type === \"click\")` → native link\n  navigation to `/assistant?seed=…` (no preventDefault; full nav).\n- `ev.expect(e => e.source === \"div.csched__bar\" && e.type === \"mouseenter\")` →\n  CSS-only `scaleY(1.1)` + `brightness(1.1)` (no JS); native `title` tooltip\n  surfaces `\"{client} — {when}\"`.\n\n## 6. Motion (extracted from contracts-sections.css)\n- **Eyebrow dot:** `.kph__eyebrow-dot` + the schedule has no dot, but the hero dot\n  pulses via `q-pulse-dot` (opacity 1→.4→1) `2.4s infinite` — the single keyframe\n  in this file (contracts.css has 1 keyframe total, shared with quotes.css).\n- **Hero CTA hover:** `.kph__cta:hover { transform: translateY(-2px) }` over\n  `transform 240ms var(--ease-bounce)`.\n- **Schedule bar hover:** `.csched__bar:hover { transform: scaleY(1.1);\n  filter: brightness(1.1) }` over `transform 240ms var(--ease-out), filter 240ms`.\n- **TODAY marker:** static pink 2px line with a `::before` \"TODAY\" chip + glow\n  shadow — **no animation** (fixed coordinate).\n- **Scheduled-bar pattern:** `.csched__bar--scheduled` is a static diagonal\n  `repeating-linear-gradient` hatch over the mood gradient (the \"not started yet\"\n  affordance) — no motion.\n- **Jank finding:** `scaleY(1.1)` on a `position:absolute` bar is GPU-cheap and\n  smooth; no layout thrash (bars are absolutely positioned, so the scale doesn't\n  reflow siblings). No identified jank.\n- **Reduced motion:** **no component-local block** in contracts-sections.css —\n  relies on the global tokens reduced-motion clamp. Verify the eyebrow pulse stills\n  and the hover transforms snap under `prefers-reduced-motion: reduce`.\n\n## 7. Responsive (this module's own @media — from contracts-sections.css)\n- **`@media (max-width: 1100px)`:** `.kkpi` → `repeat(2, 1fr)` (4 cards become a\n  2×2 grid); `.kph__inner` → single column (`1fr`, `align-items:start`); `.kph__cta`\n  `justify-self:start` (CTA drops below the hero text, left-aligned).\n- **`@media (max-width: 700px)`:** `.kkpi` → `1fr` (KPI cards stack 1-up).\n- The hero title is fluid: `font-size: clamp(34px, 4.5vw, 56px)`.\n- ScheduleStrip has **no own media query** — the `60px 1fr` week-row grid + `%`\n  bar positioning fluidly squeeze; verify bar labels (`initials · firstName`)\n  truncate (`text-overflow:ellipsis`) rather than overflow at narrow widths.\n\n## 8. A11y\n- **Hero CTA** is a real `<a>` with text + icon — good (focusable, link role).\n- **ScheduleStrip bars** convey info via color + a `title` tooltip + truncated\n  inline text. **Gaps:** the lane chart has no table/list semantics, no text\n  alternative for the visual timeline, and `title` is not reliably exposed to\n  screen readers / not keyboard-reachable. The legend swatches are decorative\n  `<span>`s with adjacent text (acceptable). **Rebuild:** give the strip an\n  accessible name + per-bar `aria-label` (\"{client}, {status}, {when}\"), or a\n  visually-hidden list fallback.\n- **KPI cards** are plain `<div>`s — the label/number/sub are readable text\n  (acceptable); consider `<dl>`/`<dt>`/`<dd>` semantics in rebuild.\n- **Color-only legend:** in-progress vs scheduled is distinguished by a solid vs\n  dashed swatch — there is text beside each, so it is not color-only. Good.\n\n## 9. Used on\n**Only** `islands/ContractsPage.tsx` (`/contracts`), imported as\n`{ ContractsHero, ContractsKpis, ScheduleStrip }`. Evidence: grep of the import.\nThe `.kempty` slate styled here is rendered by `ContractsPage` directly into empty\n`ContractTrack` bodies. CSS: `static/contracts.css` (`.kph*`, `.kkpi*`, `.csched*`,\n`.kempty` — extracted to `css/contracts-sections.css`).\n", "fixture": "{\n  \"category\": \"contracts\",\n  \"folder\": \"contracts-sections\",\n  \"components\": {\n    \"ContractsHero\": {\n      \"_note\": \"<ContractsHero {...} />. Money props are INTEGER CENTS (fmtMoney). allZero = inFlightCount===0 && pendingDeposits===0 && startingSoonCount===0 → empty sub-line + hides active-value line. CTA is a static <a href=/assistant?seed=…>.\",\n      \"totalValue\": 2940000,\n      \"contractCount\": 6,\n      \"inFlightCount\": 3,\n      \"inFlightValue\": 2030000,\n      \"startingSoonCount\": 2,\n      \"pendingDeposits\": 445000,\n      \"lang\": \"en\"\n    },\n    \"ContractsKpis\": {\n      \"_note\": \"<ContractsKpis {...} />. 4 cards: In progress (accent/pink), Starting soon, Wrapping up, Closed this month. All *Value/*Left props are INTEGER CENTS.\",\n      \"inProgressCount\": 3,\n      \"inProgressValue\": 2030000,\n      \"startingSoonCount\": 2,\n      \"startingSoonValue\": 1080000,\n      \"wrappingUpCount\": 1,\n      \"wrappingUpLeft\": 60000,\n      \"closedCount\": 4,\n      \"closedValue\": 3870000,\n      \"lang\": \"en\"\n    },\n    \"ScheduleStrip\": {\n      \"_note\": \"<ScheduleStrip cards={ContractCard[]} />. Lays out live cards by scheduleStart/scheduleEnd (day-of-window coords, day 8 = today). packLanes() greedily stacks overlapping bars into lanes; bars outside 1..30 are filtered. scheduleScheduled=true → dashed bar.\",\n      \"cards\": [\n        { \"id\": \"ctr_a1\", \"client\": \"Maria Delgado\", \"initials\": \"MD\", \"when\": \"Day 6 of 14\", \"scheduleStart\": 3, \"scheduleEnd\": 17, \"scheduleScheduled\": false, \"scheduleColor\": [\"#FF6B6B\", \"#C84A4A\"] },\n        { \"id\": \"ctr_b2\", \"client\": \"Hans Hansen\", \"initials\": \"HH\", \"when\": \"Wraps in 2 days\", \"scheduleStart\": 1, \"scheduleEnd\": 10, \"scheduleScheduled\": false, \"scheduleColor\": [\"#D9886F\", \"#A85A3A\"] },\n        { \"id\": \"ctr_c3\", \"client\": \"Northgate Property Mgmt\", \"initials\": \"NP\", \"when\": \"Starts in 5 days\", \"scheduleStart\": 13, \"scheduleEnd\": 22, \"scheduleScheduled\": true, \"scheduleColor\": [\"#9DBDC6\", \"#4F7A88\"] },\n        { \"id\": \"ctr_d4\", \"client\": \"Sam Rivera\", \"initials\": \"SR\", \"when\": \"Starts in 9 days\", \"scheduleStart\": 17, \"scheduleEnd\": 28, \"scheduleScheduled\": true, \"scheduleColor\": [\"#5FA34F\", \"#335D2A\"] }\n      ],\n      \"lang\": \"en\"\n    }\n  }\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty — hero allZero line + KPIs all 0 + .csched__empty slate\",\n  \"components\": {\n    \"ContractsHero\": {\n      \"totalValue\": 0,\n      \"contractCount\": 0,\n      \"inFlightCount\": 0,\n      \"inFlightValue\": 0,\n      \"startingSoonCount\": 0,\n      \"pendingDeposits\": 0,\n      \"lang\": \"en\"\n    },\n    \"ContractsKpis\": {\n      \"inProgressCount\": 0,\n      \"inProgressValue\": 0,\n      \"startingSoonCount\": 0,\n      \"startingSoonValue\": 0,\n      \"wrappingUpCount\": 0,\n      \"wrappingUpLeft\": 0,\n      \"closedCount\": 0,\n      \"closedValue\": 0,\n      \"lang\": \"en\"\n    },\n    \"ScheduleStrip\": {\n      \"cards\": [],\n      \"lang\": \"en\"\n    }\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — all three sections localized\",\n  \"components\": {\n    \"ContractsHero\": {\n      \"totalValue\": 2940000,\n      \"contractCount\": 6,\n      \"inFlightCount\": 3,\n      \"inFlightValue\": 2030000,\n      \"startingSoonCount\": 2,\n      \"pendingDeposits\": 445000,\n      \"lang\": \"es\"\n    },\n    \"ContractsKpis\": {\n      \"inProgressCount\": 3,\n      \"inProgressValue\": 2030000,\n      \"startingSoonCount\": 2,\n      \"startingSoonValue\": 1080000,\n      \"wrappingUpCount\": 1,\n      \"wrappingUpLeft\": 60000,\n      \"closedCount\": 4,\n      \"closedValue\": 3870000,\n      \"lang\": \"es\"\n    },\n    \"ScheduleStrip\": {\n      \"cards\": [\n        { \"id\": \"ctr_a1\", \"client\": \"Maria Delgado\", \"initials\": \"MD\", \"when\": \"Día 6 de 14\", \"scheduleStart\": 3, \"scheduleEnd\": 17, \"scheduleScheduled\": false, \"scheduleColor\": [\"#FF6B6B\", \"#C84A4A\"] },\n        { \"id\": \"ctr_c3\", \"client\": \"Northgate Property Mgmt\", \"initials\": \"NP\", \"when\": \"Empieza en 5 días\", \"scheduleStart\": 13, \"scheduleEnd\": 22, \"scheduleScheduled\": true, \"scheduleColor\": [\"#9DBDC6\", \"#4F7A88\"] }\n      ],\n      \"lang\": \"es\"\n    }\n  }\n}\n"}, {"name": "kempty-slate.json", "content": "{\n  \"_name\": \".kempty slate — dashed empty-track box (rendered by parent)\",\n  \"_note\": \"Not its own export — the .kempty class lives in contracts-sections.css and is rendered by ContractsPage into an empty ContractTrack body. Isolated here via _innerHtml so the dashed slate style is captured.\",\n  \"_innerHtml\": \"<div class=\\\"kempty\\\">No jobs in progress right now. Quote a job to get one started.</div>\"\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — hero + 4 KPIs + schedule strip with bars\",\n  \"components\": {\n    \"ContractsHero\": {\n      \"totalValue\": 2940000,\n      \"contractCount\": 6,\n      \"inFlightCount\": 3,\n      \"inFlightValue\": 2030000,\n      \"startingSoonCount\": 2,\n      \"pendingDeposits\": 445000,\n      \"lang\": \"en\"\n    },\n    \"ContractsKpis\": {\n      \"inProgressCount\": 3,\n      \"inProgressValue\": 2030000,\n      \"startingSoonCount\": 2,\n      \"startingSoonValue\": 1080000,\n      \"wrappingUpCount\": 1,\n      \"wrappingUpLeft\": 60000,\n      \"closedCount\": 4,\n      \"closedValue\": 3870000,\n      \"lang\": \"en\"\n    },\n    \"ScheduleStrip\": {\n      \"cards\": [\n        { \"id\": \"ctr_a1\", \"client\": \"Maria Delgado\", \"initials\": \"MD\", \"when\": \"Day 6 of 14\", \"scheduleStart\": 3, \"scheduleEnd\": 17, \"scheduleScheduled\": false, \"scheduleColor\": [\"#FF6B6B\", \"#C84A4A\"] },\n        { \"id\": \"ctr_b2\", \"client\": \"Hans Hansen\", \"initials\": \"HH\", \"when\": \"Wraps in 2 days\", \"scheduleStart\": 1, \"scheduleEnd\": 10, \"scheduleScheduled\": false, \"scheduleColor\": [\"#D9886F\", \"#A85A3A\"] },\n        { \"id\": \"ctr_c3\", \"client\": \"Northgate Property Mgmt\", \"initials\": \"NP\", \"when\": \"Starts in 5 days\", \"scheduleStart\": 13, \"scheduleEnd\": 22, \"scheduleScheduled\": true, \"scheduleColor\": [\"#9DBDC6\", \"#4F7A88\"] }\n      ],\n      \"lang\": \"en\"\n    }\n  }\n}\n"}, {"name": "schedule-overlap.json", "content": "{\n  \"_name\": \"Schedule overlap — overlapping bars pack into 3 lanes + TODAY marker\",\n  \"_note\": \"Five live contracts whose day-windows overlap heavily so packLanes() is forced into multiple lanes; --lanes-h grows each week row. day 8 = today → the pink TODAY line + chip lands in week2 (days 8–14).\",\n  \"components\": {\n    \"ScheduleStrip\": {\n      \"cards\": [\n        { \"id\": \"ctr_o1\", \"client\": \"Maria Delgado\", \"initials\": \"MD\", \"when\": \"Day 6 of 14\", \"scheduleStart\": 2, \"scheduleEnd\": 16, \"scheduleScheduled\": false, \"scheduleColor\": [\"#FF6B6B\", \"#C84A4A\"] },\n        { \"id\": \"ctr_o2\", \"client\": \"Hans Hansen\", \"initials\": \"HH\", \"when\": \"Wraps in 2 days\", \"scheduleStart\": 1, \"scheduleEnd\": 11, \"scheduleScheduled\": false, \"scheduleColor\": [\"#D9886F\", \"#A85A3A\"] },\n        { \"id\": \"ctr_o3\", \"client\": \"Northgate Property Mgmt\", \"initials\": \"NP\", \"when\": \"Starts in 1 day\", \"scheduleStart\": 9, \"scheduleEnd\": 24, \"scheduleScheduled\": true, \"scheduleColor\": [\"#9DBDC6\", \"#4F7A88\"] },\n        { \"id\": \"ctr_o4\", \"client\": \"Sam Rivera\", \"initials\": \"SR\", \"when\": \"Starts in 6 days\", \"scheduleStart\": 14, \"scheduleEnd\": 27, \"scheduleScheduled\": true, \"scheduleColor\": [\"#5FA34F\", \"#335D2A\"] },\n        { \"id\": \"ctr_o5\", \"client\": \"Greenwood HOA\", \"initials\": \"GH\", \"when\": \"Day 2 of 9\", \"scheduleStart\": 6, \"scheduleEnd\": 20, \"scheduleScheduled\": false, \"scheduleColor\": [\"#E6A85C\", \"#B97A2E\"] }\n      ],\n      \"lang\": \"en\"\n    }\n  }\n}\n"}], "css": "/* ============================================================\n   ContractsSections — extracted from static/contracts.css.\n   Covers the 3 SSR sections: ContractsHero (.kph), ContractsKpis (.kkpi),\n   ScheduleStrip (.csched). The .kempty slate (rendered by ContractsPage when\n   a track is empty) also lives here since it is contracts-page chrome.\n   Tokens: --brand-teal, --brand-pink, --fg-muted, --radius-lg, --radius-xl,\n   --radius-pill, --font-heading, --font-body, --dur-fast, --ease-out,\n   --ease-bounce.\n   ============================================================ */\n\n@keyframes q-pulse-dot {\n  0%, 100% { opacity: 1; }\n  50% { opacity: 0.4; }\n}\n\n/* --- Hero (.kph) --- */\n.kph { padding: 20px 0 24px; position: relative; }\n.kph__inner {\n  display: grid;\n  grid-template-columns: 1fr auto;\n  gap: 20px;\n  align-items: end;\n}\n.kph__eyebrow {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 800;\n  letter-spacing: 0.12em;\n  text-transform: uppercase;\n  color: var(--brand-pink);\n  margin-bottom: 12px;\n}\n.kph__eyebrow-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 999px;\n  background: var(--brand-pink);\n  animation: q-pulse-dot 2.4s infinite;\n}\n.kph__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: clamp(34px, 4.5vw, 56px);\n  line-height: 0.96;\n  color: var(--brand-teal);\n  letter-spacing: -0.02em;\n  margin: 0 0 14px;\n  text-wrap: balance;\n}\n.kph__title em { font-style: normal; color: var(--brand-pink); }\n.kph__sub {\n  font-family: var(--font-body);\n  font-size: 16px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  max-width: 540px;\n  margin: 0;\n}\n.kph__sub strong { color: var(--brand-pink); font-weight: 700; }\n.kph__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  padding: 14px 22px;\n  border-radius: var(--radius-pill);\n  background: var(--brand-pink);\n  color: #fff;\n  border: none;\n  cursor: pointer;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 14px;\n  text-decoration: none;\n  box-shadow: 0 8px 18px -6px rgba(255, 107, 107, 0.55);\n  transition: transform 240ms var(--ease-bounce);\n  flex-shrink: 0;\n}\n.kph__cta:hover { transform: translateY(-2px); }\n\n/* --- KPI strip (.kkpi) --- */\n.kkpi {\n  display: grid;\n  grid-template-columns: repeat(4, 1fr);\n  gap: 12px;\n  margin-bottom: 28px;\n}\n.kkpi__card {\n  background: #fff;\n  border: 1px solid rgba(26, 83, 92, 0.08);\n  border-radius: var(--radius-lg);\n  padding: 16px 18px;\n}\n.kkpi__card--accent {\n  background: linear-gradient(135deg, rgba(255, 107, 107, 0.08) 0%, rgba(255, 107, 107, 0.02) 100%);\n  border-color: rgba(255, 107, 107, 0.2);\n}\n.kkpi__lbl {\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.1em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 8px;\n}\n.kkpi__num {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 24px;\n  color: var(--brand-teal);\n  letter-spacing: -0.02em;\n  line-height: 1;\n}\n.kkpi__num--pink { color: var(--brand-pink); }\n.kkpi__sub {\n  font-family: var(--font-body);\n  font-size: 11.5px;\n  color: var(--fg-muted);\n  margin-top: 4px;\n}\n\n/* --- Schedule strip (.csched) — dark teal Gantt-style 30-day lane chart --- */\n.csched {\n  background: linear-gradient(135deg, #0f3a40 0%, #1a535c 100%);\n  border-radius: var(--radius-xl);\n  padding: 20px 22px 22px;\n  color: #fff;\n  margin-bottom: 28px;\n  position: relative;\n  overflow: hidden;\n}\n.csched::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 12% 18%, rgba(255, 255, 255, 0.12) 0, transparent 30%),\n    radial-gradient(circle at 88% 82%, rgba(255, 107, 107, 0.18) 0, transparent 38%);\n  pointer-events: none;\n}\n.csched__head {\n  display: flex;\n  align-items: baseline;\n  gap: 14px;\n  margin-bottom: 14px;\n  position: relative;\n  z-index: 1;\n}\n.csched__eyebrow {\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.12em;\n  text-transform: uppercase;\n  color: var(--brand-pink);\n}\n.csched__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 19px;\n  color: #fff;\n  letter-spacing: -0.01em;\n}\n.csched__legend {\n  margin-left: auto;\n  display: flex;\n  gap: 14px;\n  font-family: var(--font-body);\n  font-size: 11px;\n  color: rgba(255, 255, 255, 0.7);\n}\n.csched__legend-dot {\n  display: inline-block;\n  width: 8px;\n  height: 8px;\n  border-radius: 999px;\n  margin-right: 5px;\n  vertical-align: middle;\n}\n.csched__grid { position: relative; z-index: 1; }\n.csched__weekrow {\n  display: grid;\n  grid-template-columns: 60px 1fr;\n  gap: 12px;\n  padding: 5px 0;\n  align-items: center;\n  border-top: 1px solid rgba(255, 255, 255, 0.08);\n}\n.csched__weekrow:first-child { border-top: none; }\n.csched__weeklbl {\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 11px;\n  color: rgba(255, 255, 255, 0.55);\n  letter-spacing: 0.04em;\n}\n.csched__weekbar {\n  position: relative;\n  height: var(--lanes-h, 22px);     /* set inline per row from packLanes() */\n  background: rgba(255, 255, 255, 0.06);\n  border-radius: 6px;\n}\n.csched__bar {\n  position: absolute;               /* left/width/top/height set inline */\n  height: 18px;\n  border-radius: 4px;\n  background: linear-gradient(135deg, var(--bar-from) 0%, var(--bar-to) 100%);\n  display: flex;\n  align-items: center;\n  padding: 0 8px;\n  font-family: var(--font-heading);\n  font-size: 10.5px;\n  font-weight: 700;\n  color: #fff;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.18);\n  cursor: default;\n  transition: transform 240ms var(--ease-out), filter 240ms;\n}\n.csched__bar:hover { transform: scaleY(1.1); filter: brightness(1.1); }\n.csched__bar--scheduled {\n  background:\n    repeating-linear-gradient(45deg,\n      rgba(255, 255, 255, 0.10),\n      rgba(255, 255, 255, 0.10) 5px,\n      rgba(255, 255, 255, 0.18) 5px,\n      rgba(255, 255, 255, 0.18) 10px),\n    linear-gradient(135deg, var(--bar-from) 0%, var(--bar-to) 100%);\n}\n.csched__today {\n  position: absolute;\n  top: -3px;\n  bottom: -3px;\n  width: 2px;\n  background: var(--brand-pink);\n  box-shadow: 0 0 8px rgba(255, 107, 107, 0.7);\n  z-index: 2;\n}\n.csched__today::before {\n  content: \"TODAY\";\n  position: absolute;\n  top: -16px;\n  left: 50%;\n  transform: translateX(-50%);\n  background: var(--brand-pink);\n  color: #fff;\n  font-family: var(--font-heading);\n  font-size: 8.5px;\n  font-weight: 800;\n  letter-spacing: 0.08em;\n  padding: 2px 5px;\n  border-radius: 3px;\n  white-space: nowrap;\n}\n.csched__empty {\n  padding: 28px 8px;\n  text-align: center;\n  color: rgba(255, 255, 255, 0.7);\n  font-family: var(--font-body);\n  font-size: 13.5px;\n}\n\n/* --- empty-track slate (ContractsPage renders <div class=\"kempty\">…) --- */\n.kempty {\n  padding: 22px 18px;\n  background: rgba(26, 83, 92, 0.03);\n  border: 1px dashed rgba(26, 83, 92, 0.16);\n  border-radius: var(--radius-lg);\n  color: var(--fg-muted);\n  font-family: var(--font-body);\n  font-size: 13.5px;\n  text-align: center;\n}\n\n/* Responsive (from static/contracts.css) */\n@media (max-width: 1100px) {\n  .kkpi { grid-template-columns: repeat(2, 1fr); }\n  .kph__inner { grid-template-columns: 1fr; align-items: start; }\n  .kph__cta { justify-self: start; }\n}\n@media (max-width: 700px) {\n  .kkpi { grid-template-columns: 1fr; }\n}\n", "cssName": "contracts-sections.css", "source": "/**\n * Server-rendered sections for /contracts. Ported from the prototype's\n * ContractsHero, ContractsKPIs, and ScheduleStrip.\n *\n * The interactive Track collapse + ContractCard flip live in islands/.\n */\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { fmtMoney } from \"../lib/format.ts\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\nimport type { ContractCard } from \"../lib/contracts-shape.ts\";\n\n/** Plural helper bound to an explicit SSR language. */\nfunction tnFor(lang: Lang, key: string, n: number, vars?: Record<string, string | number>): string {\n  return tFor(lang, `${key}.${n === 1 ? \"one\" : \"other\"}`, { n, ...vars });\n}\n\ninterface HeroProps {\n  totalValue: number;\n  contractCount: number;\n  inFlightCount: number;\n  inFlightValue: number;\n  startingSoonCount: number;\n  pendingDeposits: number;\n  lang?: Lang;\n}\n\nexport function ContractsHero({\n  totalValue,\n  contractCount,\n  inFlightCount,\n  inFlightValue,\n  startingSoonCount,\n  pendingDeposits,\n  lang = \"en\",\n}: HeroProps) {\n  const allZero = inFlightCount === 0 && pendingDeposits === 0 &&\n    startingSoonCount === 0;\n  return (\n    <section class=\"kph\">\n      <div class=\"kph__inner\">\n        <div>\n          <div class=\"kph__eyebrow\">\n            <span class=\"kph__eyebrow-dot\" />\n            {tFor(lang, \"contractsHero.eyebrow\")} ·{\" \"}\n            {tnFor(lang, \"contractsHero.contracts\", contractCount)}\n          </div>\n          <h1 class=\"kph__title\">\n            <em>{fmtMoney(totalValue)}</em> {tFor(lang, \"contractsHero.titlePre\")}<br />\n            {tFor(lang, \"contractsHero.titlePost\")}\n          </h1>\n          <p class=\"kph__sub\">\n            {allZero\n              ? <>{tFor(lang, \"contractsHero.empty\")}</>\n              : (\n                <>\n                  {tnFor(lang, \"contractsHero.jobsRunning\", inFlightCount)} ·{\" \"}\n                  <strong>\n                    {tFor(lang, \"contractsHero.depositsAmount\", {\n                      money: fmtMoney(pendingDeposits),\n                    })}\n                  </strong>{\" \"}\n                  {tFor(lang, \"contractsHero.subMid\", {\n                    n: startingSoonCount,\n                  })}\n                </>\n              )}\n          </p>\n          {!allZero && (\n            <p\n              class=\"kph__sub\"\n              style=\"margin-top:6px;font-size:12.5px;opacity:0.75\"\n            >\n              {tFor(lang, \"contractsHero.activeValue\", {\n                money: fmtMoney(inFlightValue),\n              })}\n            </p>\n          )}\n        </div>\n        <a\n          class=\"kph__cta\"\n          href={`/assistant?seed=${\n            encodeURIComponent(tFor(lang, \"contractsHero.seed\"))\n          }`}\n        >\n          <I d={ICN.plus} size={14} sw={2.5} /> {tFor(lang, \"contractsHero.cta\")}\n        </a>\n      </div>\n    </section>\n  );\n}\n\ninterface KpisProps {\n  inProgressCount: number;\n  inProgressValue: number;\n  startingSoonCount: number;\n  startingSoonValue: number;\n  wrappingUpCount: number;\n  wrappingUpLeft: number;\n  closedCount: number;\n  closedValue: number;\n  lang?: Lang;\n}\n\nexport function ContractsKpis({\n  inProgressCount,\n  inProgressValue,\n  startingSoonCount,\n  startingSoonValue,\n  wrappingUpCount,\n  wrappingUpLeft,\n  closedCount,\n  closedValue,\n  lang = \"en\",\n}: KpisProps) {\n  return (\n    <div class=\"kkpi\">\n      <div class=\"kkpi__card kkpi__card--accent\">\n        <div class=\"kkpi__lbl\">{tFor(lang, \"contractsKpi.inProgress\")}</div>\n        <div class=\"kkpi__num kkpi__num--pink\">\n          {tnFor(lang, \"contractsKpi.jobs\", inProgressCount)}\n        </div>\n        <div class=\"kkpi__sub\">\n          {tFor(lang, \"contractsKpi.active\", { money: fmtMoney(inProgressValue) })}\n        </div>\n      </div>\n      <div class=\"kkpi__card\">\n        <div class=\"kkpi__lbl\">{tFor(lang, \"contractsKpi.startingSoon\")}</div>\n        <div class=\"kkpi__num\">\n          {tnFor(lang, \"contractsKpi.jobs\", startingSoonCount)}\n        </div>\n        <div class=\"kkpi__sub\">\n          {tFor(lang, \"contractsKpi.next14Days\", {\n            money: fmtMoney(startingSoonValue),\n          })}\n        </div>\n      </div>\n      <div class=\"kkpi__card\">\n        <div class=\"kkpi__lbl\">{tFor(lang, \"contractsKpi.wrappingUp\")}</div>\n        <div class=\"kkpi__num\">\n          {tnFor(lang, \"contractsKpi.jobs\", wrappingUpCount)}\n        </div>\n        <div class=\"kkpi__sub\">\n          {tFor(lang, \"contractsKpi.leftToBill\", {\n            money: fmtMoney(wrappingUpLeft),\n          })}\n        </div>\n      </div>\n      <div class=\"kkpi__card\">\n        <div class=\"kkpi__lbl\">{tFor(lang, \"contractsKpi.closedThisMonth\")}</div>\n        <div class=\"kkpi__num\">\n          {tnFor(lang, \"contractsKpi.jobs\", closedCount)}\n        </div>\n        <div class=\"kkpi__sub\">\n          {tFor(lang, \"contractsKpi.allPaid\", { money: fmtMoney(closedValue) })}\n        </div>\n      </div>\n    </div>\n  );\n}\n\ninterface StripProps {\n  cards: ContractCard[];\n  lang?: Lang;\n}\n\nconst RANGE_FROM = 1;\nconst RANGE_TO = 30;\nconst TODAY_INDEX = 8;\nconst WEEKS = [\n  { labelKey: \"schedule.week1\", from: 1, to: 7 },\n  { labelKey: \"schedule.week2\", from: 8, to: 14 },\n  { labelKey: \"schedule.week3\", from: 15, to: 21 },\n  { labelKey: \"schedule.week4\", from: 22, to: 28 },\n  { labelKey: \"schedule.week5\", from: 29, to: 30 },\n];\n\nconst LANE_H = 22;\nconst LANE_GAP = 4;\n\ninterface PackedBar {\n  card: ContractCard;\n  lane: number;\n}\n\nfunction packLanes(\n  cards: ContractCard[],\n): { laneCount: number; placed: PackedBar[] } {\n  const sorted = [...cards].sort((a, b) => a.scheduleStart - b.scheduleStart);\n  const laneEnds: number[] = [];\n  const placed: PackedBar[] = [];\n  for (const c of sorted) {\n    let lane = -1;\n    for (let li = 0; li < laneEnds.length; li++) {\n      if (laneEnds[li] < c.scheduleStart) {\n        laneEnds[li] = c.scheduleEnd;\n        lane = li;\n        break;\n      }\n    }\n    if (lane === -1) {\n      lane = laneEnds.length;\n      laneEnds.push(c.scheduleEnd);\n    }\n    placed.push({ card: c, lane });\n  }\n  return { laneCount: laneEnds.length, placed };\n}\n\nfunction pos(d: number, from: number, to: number): string {\n  return `${((d - from) / (to - from + 1)) * 100}%`;\n}\nfunction widthPct(\n  start: number,\n  end: number,\n  from: number,\n  to: number,\n): string {\n  return `${((end - start + 1) / (to - from + 1)) * 100}%`;\n}\n\nexport function ScheduleStrip({ cards, lang = \"en\" }: StripProps) {\n  const visible = cards.filter((c) =>\n    c.scheduleEnd >= RANGE_FROM && c.scheduleStart <= RANGE_TO\n  );\n  const { laneCount, placed } = packLanes(visible);\n  const rowH = Math.max(1, laneCount) * LANE_H +\n    Math.max(0, laneCount - 1) * LANE_GAP + 6;\n  const lanesH = `${rowH}px`;\n\n  return (\n    <section class=\"csched\">\n      <div class=\"csched__head\">\n        <div>\n          <div class=\"csched__eyebrow\">{tFor(lang, \"schedule.eyebrow\")}</div>\n          <div class=\"csched__title\">\n            {tFor(lang, \"schedule.title\")}\n          </div>\n        </div>\n        <div class=\"csched__legend\">\n          <span>\n            <span class=\"csched__legend-dot\" style=\"background:#FF6B6B\" />\n            {tFor(lang, \"schedule.legendInProgress\")}\n          </span>\n          <span>\n            <span\n              class=\"csched__legend-dot\"\n              style=\"background:rgba(255,255,255,0.3);border:1px dashed rgba(255,255,255,0.6)\"\n            />\n            {tFor(lang, \"schedule.legendScheduled\")}\n          </span>\n        </div>\n      </div>\n      <div class=\"csched__grid\">\n        {WEEKS.map((w, wi) => {\n          const bars = placed.filter(({ card }) =>\n            card.scheduleEnd >= w.from && card.scheduleStart <= w.to\n          );\n          const showToday = TODAY_INDEX >= w.from && TODAY_INDEX <= w.to;\n          return (\n            <div key={wi} class=\"csched__weekrow\">\n              <div class=\"csched__weeklbl\">{tFor(lang, w.labelKey)}</div>\n              <div class=\"csched__weekbar\" style={`--lanes-h:${lanesH}`}>\n                {showToday && (\n                  <div\n                    class=\"csched__today\"\n                    style={`left:${pos(TODAY_INDEX + 0.5, w.from, w.to)}`}\n                  />\n                )}\n                {bars.map(({ card, lane }) => {\n                  const s = Math.max(card.scheduleStart, w.from);\n                  const e = Math.min(card.scheduleEnd, w.to);\n                  const top = 3 + lane * (LANE_H + LANE_GAP);\n                  const style = `--bar-from:${card.scheduleColor[0]};` +\n                    `--bar-to:${card.scheduleColor[1]};` +\n                    `left:${pos(s, w.from, w.to)};` +\n                    `width:${widthPct(s, e, w.from, w.to)};` +\n                    `top:${top}px;height:${LANE_H - 2}px`;\n                  return (\n                    <div\n                      key={card.id}\n                      class={`csched__bar ${\n                        card.scheduleScheduled ? \"csched__bar--scheduled\" : \"\"\n                      }`}\n                      style={style}\n                      title={`${card.client} — ${card.when}`}\n                    >\n                      {card.initials} · {card.client.split(\" \")[0]}\n                    </div>\n                  );\n                })}\n              </div>\n            </div>\n          );\n        })}\n        {placed.length === 0 && (\n          <div class=\"csched__empty\">\n            {tFor(lang, \"schedule.empty\")}\n          </div>\n        )}\n      </div>\n    </section>\n  );\n}\n", "sourceName": "ContractsSections.tsx", "checklist": "# Capture checklist — ContractsSections (Hero / KPIs / ScheduleStrip)\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/contracts` — the only host. These three sections render\n  at the top of the page body (above the tracks), in order: Hero → KPIs →\n  ScheduleStrip. They appear immediately after the island finishes its fetch\n  (before that, the page shows skeletons — see contracts-page/).\n\n## Viewports\nShoot at this module's own `@media` breakpoints (from contracts-sections.css):\n- **1280px** (`--container-product`) — KPIs 4-across; hero text left, CTA right.\n- **1100px** (the module's first breakpoint) — KPIs become **2×2**; hero\n  collapses to a single column with the CTA dropping below, left-aligned. Capture\n  the exact reflow at this width.\n- **700px** (the module's second breakpoint) — KPIs stack **1-up**.\n- Also ~360px to confirm hero title `clamp(34px,4.5vw,56px)` floors and schedule\n  bar labels truncate.\n\n## Element(s) to crop\n- `<section class=\"kph\">` — eyebrow (pulsing dot + \"Work in flight · N\n  contracts\"), `<em>$total</em>` headline, deposits sub-line, the `.kph__cta`\n  anchor. Also crop the **empty** variant (allZero sub-line, no active-value line).\n- `<div class=\"kkpi\">` — all 4 cards; the first (`--accent`) has the pink number.\n- `<section class=\"csched\">` — the dark-teal panel: head + legend, 5 week rows,\n  packed bars, the pink \"TODAY\" line + chip. Also crop the `.csched__empty` slate.\n- `<div class=\"kempty\">` — the dashed empty-track slate (rendered by the parent\n  into an empty track; or capture via isolate `kempty-slate` case).\n\n## Transient states to drive\n1. **populated** — account with live + scheduled + closed contracts → full hero\n   numbers, all KPIs non-zero, schedule bars packed across lanes.\n2. **empty** — fresh account (no contracts) → hero `allZero` line, KPIs all \"0\n   jobs\", `.csched__empty` slate inside the strip.\n3. **es** — Settings → Spanish → all labels re-localize (eyebrow, title pre/post,\n   KPI labels, schedule eyebrow/title/legend/week labels).\n4. **schedule-overlap** — many overlapping live contracts → 2+ lanes packed,\n   `--lanes-h` grows the week rows; the TODAY marker sits in the day-8 week.\n5. **bar hover** — pointer over a `.csched__bar` → `scaleY(1.1)` + brightness;\n   native `title` tooltip shows \"{client} — {when}\".\n\n## Motion to film\n- **Eyebrow pulse:** `.kph__eyebrow-dot` `q-pulse-dot` 2.4s (cosmetic).\n- **Hero CTA hover:** `translateY(-2px)` 240ms bounce.\n- **Schedule bar hover:** `scaleY(1.1)` + `brightness(1.1)` 240ms.\n- The \"TODAY\" marker and scheduled-bar diagonal hatch are static (no motion).\n- Re-shoot hover + pulse with `prefers-reduced-motion: reduce` — **no\n  component-local guard**; verify the global tokens clamp stills/snaps them.\n\n## NO fabricated screenshots\nAll frames from the live `/contracts` route (drive empty via a fresh account;\ndrive overlap via a seeded multi-contract account). Isolate cases exist for\nstate-completeness; do not pass off an isolate render as the live deliverable.\n"}]}, {"name": "dashboard", "pageMd": "# Page — /dashboard\n\nThe authenticated **home** route. An app-shell page: SSR renders only the\nshell + greeting; one big client island (`DashboardPage`) fans out all the\ndata fetches and assembles the page body. Two more self-gating overlay islands\n(`AssistantCoachmark`, `WelcomeBackToast`) sit outside the shell.\n\nSource route: `routes/dashboard/index.tsx` (copied verbatim to `js/index.tsx`).\n\n## Purpose\nFirst screen after login. Surfaces, top-to-bottom: a dominant **assistant\nbanner** CTA, a post-onboarding **setup checklist**, four **KPI** tiles, then two\ntwo-column rows — Active jobs + Quotes awaiting, and Activity + Money owed.\n\n## SSR / app-shell composition (mount order)\nThe route is `define.page(...)` — server-rendered shell. It computes the\ngreeting on the server and mounts islands in this DOM order:\n\n```\n<>\n  <Head> title + <link rel=stylesheet href=\"/dashboard.css\">\n  <div class=\"app\">                          ← app-shell flex container\n    <DashSidebar active=\"home\" />            ← SHARED island (see shared-components/dash-sidebar) — reference only\n    <main class=\"main\">\n      <DashTopbar greetingDate greetingName/>← SHARED island (see shared-components/dash-topbar) — reference only\n      <div class=\"content\">                  ← the scroll column (CSS in dashboard-page.css)\n        <DashboardPage />                     ← THIS PAGE's main island (page-local: components/dashboard-page)\n      </div>\n    </main>\n  </div>\n  <AssistantCoachmark />                      ← page-local island; one-shot overlay, self-gates on localStorage\n  <WelcomeBackToast />                        ← page-local island; self-gates on ?welcome=back\n</>\n```\n\nMount order matters: `DashSidebar` must hydrate **before** `AssistantCoachmark`\nruns, because the coachmark polls the DOM for the sidebar's `.sb__textus`\n(\"My Assistant\") button to position its spotlight hole (retries up to 30× @\n120ms). The two overlays render outside `.app` so they layer above the whole\nshell at `z-index:9999`.\n\n## `<Head>`\n- **Title:** `tFor(lang, \"dashboardPage.docTitle\", { brand })` → `Dashboard ·\n  Paperwork Monster` (es: localized).\n- **CSS:** `<link rel=\"stylesheet\" href=\"/dashboard.css\">` — the single feature\n  stylesheet (`static/dashboard.css`, ~2600 lines). It also defines the\n  sidebar/topbar/phone-preview classes (shared specs own those). Tokens come\n  from `static/_proto/colors_and_type.css` (loaded app-wide, not here).\n\n## SSR data (server-computed, passed as props)\nOnly the greeting is computed SSR — from `ctx.state.user` (the resolved\nsession `User`):\n- `lang` = `user.language === \"es\" ? \"es\" : \"en\"`.\n- `greetingName` = first token of `user.name` (trimmed) or `tFor(lang,\n  \"common.thereFallback\")` (`\"there\"`).\n- `greetingDate` = `\"{Weekday} · {Month} {date}\"` from `new Date()` via the\n  `WEEKDAY_KEYS` / `MONTH_KEYS` i18n arrays.\n\nThese two strings are passed to **DashTopbar** (shared). `DashboardPage` itself\nreceives NO SSR data props — it is a frozen-shell-then-client-fetch island\n(see the anti-pattern below).\n\n## Section layout (what DashboardPage renders, in order)\n1. `.assistant-cta` banner (green→teal gradient hero with monster art + \"My\n   Assistant\" + \"Call support\" buttons). **CSS owned by this page** →\n   `css/dashboard-page.css`.\n2. `<SetupChecklist/>` (page-local island) — pink onboarding checklist; renders\n   nothing once complete or dismissed.\n3. `<Kpis/>` — 4 tiles (Active jobs / Outstanding / Quotes pending / Avg paid job).\n4. `<div class=\"grid\">` → `<ActiveJobs/>` + `<QuotesAwaiting/>`.\n5. `<div class=\"grid\">` → `<Activity/>` + `<Outstanding/>`.\n\nItems 3–5 are exports of **DashSections** (page-composition; `components/\ndash-sections`). `Outstanding`'s money figure and the dead `Hero` use the\n**Ticker** island (`components/ticker`).\n\n## Cross-page note — QuoteCard\nThe breakdown task description claimed DashboardPage renders the **QuoteCard**\nisland. **It does not** (confirmed against source): DashboardPage imports only\n`ActiveJobs, Activity, Kpis, Outstanding, QuotesAwaiting` from DashSections,\n`SetupChecklist`, `Ticker` (transitively), and the shared Skeletons. There is\nno `QuoteCard` import on `/dashboard`. The \"Quotes awaiting\" strip is the\n`QuotesAwaiting` panel (its own simple `.quote-item` rows), **not** the\n`/quotes` `QuoteCard`. So: no `quote-card` folder is written here, and the only\nhonest cross-page note is that the dashboard's quote strip is a *separate, much\nsimpler* presentation than the canonical `QuoteCard` (specced under\n`pages/quotes/components/quote-card/`).\n\n## Build order (rebuild this page mechanically)\n1. Tokens + `dashboard.css` shell classes (`.app`,`.main`,`.content`) present.\n2. Shared **DashSidebar** + **DashTopbar** (reference specs) mounted in the shell.\n3. **Ticker** island (leaf; needed by DashSections).\n4. **DashSections** exports (`Kpis`,`ActiveJobs`,`QuotesAwaiting`,`Outstanding`,\n   `Activity`) — pure presentational, fed row arrays.\n5. **SetupChecklist** island (fetches `/profile`).\n6. **DashboardPage** island — wire the parallel fetches + the `dash-cache`\n   warm-start, map DTOs→rows, render assistant-cta + checklist + sections.\n   Use the shared **Skeletons** for the loading state.\n7. **AssistantCoachmark** + **WelcomeBackToast** overlays last (depend on the\n   hydrated sidebar / URL respectively).\n8. Route shell `index.tsx` composes everything + computes the greeting SSR.\n\n## Page-local components (full specs under `components/`)\n| folder | source | bucket |\n|---|---|---|\n| `dashboard-page` | `islands/DashboardPage.tsx` | island (data fan-out) |\n| `dash-sections` | `components/DashSections.tsx` | page-composition (static SSR sections) |\n| `ticker` | `islands/Ticker.tsx` | island (rAF count-up) |\n| `setup-checklist` | `islands/SetupChecklist.tsx` | island (onboarding checklist) |\n| `assistant-coachmark` | `islands/AssistantCoachmark.tsx` | island (one-shot overlay) |\n| `welcome-back-toast` | `islands/WelcomeBackToast.tsx` | island (URL-gated toast) |\n\nShared (reference only, NOT re-specced here): DashSidebar, DashTopbar,\nSkeletons (`ShimmerStyle`/`SkelBlock`), MobileViewport, ImpersonationBanner.\n\n## ANTI-PATTERN (the classic whole-page island) — FLAGGED\n`DashboardPage` is the textbook \"whole-page island\": the SSR route renders an\nempty shell, the island hydrates, then fetches **6 endpoints in parallel** on\nmount (`/analytics/dashboard`, `/jobs`, `/quotes?status=sent`,\n`/invoices?status=pending`, `/customers`, `/notifications`) and only THEN\nrenders the body. Until those resolve the user sees `DashboardSkeleton`.\n- **Mitigation already present:** `lib/dash-cache.ts` warm-starts the hero+KPIs\n  from a `sessionStorage` snapshot (`readCached()`), so navigating *back* to\n  `/dashboard` skips the full skeleton flash; the mount fetch becomes a silent\n  background refresh.\n- **No `location.reload()`** anywhere in this scope — good. (Checked all six\n  scope islands; the only full navigations are `<a href>`s and DashSidebar's\n  logout, which is in the shared spec.)\n- **The form+PRG / Fresh Partial fix:** the hero greeting + KPI tiles + jobs\n  row are all read-only projections of `DashboardStats`/`Job[]` — they have NO\n  interactivity that requires an island. They could be **server-rendered** in\n  the route handler (resolve the stats SSR, render the tiles as static HTML)\n  and only the genuinely-interactive bits (the Ticker count-up, the\n  nudge/quote buttons) kept as small islands or progressively-enhanced. That\n  removes the cold-load skeleton entirely and the whole-account rollup is\n  fetched once on the server instead of after hydration. The \"Nudge by text\" /\n  \"Nudge all\" buttons (currently inert — see DashSections spec) would become\n  `<form method=POST>` → server action → PRG redirect with a flash.\n\n## DATA-SHAPE HAZARD — dashboard stat tiles (FLAGGED)\nThe four KPI tiles + Money-owed bar are derived from `DashboardStats`, a\n**whole-account rollup** (`/analytics/dashboard`) that aggregates counts across\nall quotes/contracts/invoices/customers + `quotedValueCents` (Σ sent quotes) +\n`revenue.sparkline12mo` + `invoices.agingBuckets`. Because `DashSidebar` mounts\nthis same rollup on *every* authed page, the existing mitigation is\n`lib/dash-cache.ts` (single-flight + sessionStorage warm start + a pub/sub so\nsiblings share one fetch). See `data-model.md` §5.1. Rebuild target:\nprecompute these counters incrementally on the backend; never recompute by\nscanning per request.\n", "components": [{"name": "assistant-coachmark", "component": "AssistantCoachmark", "spec": "# AssistantCoachmark\n\nA one-shot, full-screen onboarding **overlay** shown on a user's FIRST dashboard\nvisit after onboarding. It darkens the whole app, cuts a spotlight hole over the\nsidebar's \"My Assistant\" button (`.sb__textus`), and floats a pink speech bubble\npointing at it — so the only lit affordance is the assistant CTA. Click anywhere\nto dismiss; it persists \"shown\" to `localStorage` and never returns.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (file lives in `islands/AssistantCoachmark.tsx`). Mounted\n  at the **root** of the dashboard route (sibling of `.app`, not inside it) so it\n  can overlay everything at `z-index: 9999`.\n- **Interaction tier:** `island` — client-only state, self-gating, no server\n  contact, no data fetch. Pure presentational overlay driven by viewport\n  measurement + timers.\n- **Self-gate (the headline behavior):** on mount it reads\n  `localStorage[\"pm:assistant-coachmark-shown\"]`; if `=== \"1\"` it returns early\n  and renders nothing. So SSR is unaffected (the island emits nothing on the\n  server / on repeat visits). First visit only.\n- **Client state owned (`useState`):**\n  - `visible: boolean` — gates the whole overlay (false until the target button\n    is found + measured).\n  - `box: {top,left,width,height} | null` — the measured\n    `getBoundingClientRect()` of the `.sb__textus` button (the hole geometry).\n  - `fadingOut: boolean` — drives the 320ms exit fade before unmount.\n  - `phase: 0 | 1 | 2` — entrance choreography stage (pre-mount → entering →\n    settled); gates which animations run.\n- **Target acquisition (fragile coupling — FLAG):** it `document.querySelector`s\n  `'a[href=\"/assistant\"].sb__textus, .sb__textus[href=\"/assistant\"]'` and\n  **retries every 120ms up to 30 times** (~3.6s) waiting for the DashSidebar\n  island to hydrate and paint the button. This is a **cross-island DOM\n  dependency** — if the sidebar markup/selector changes or the sidebar fails to\n  hydrate, the coachmark silently never shows (it just exhausts its retries). Fix:\n  have the sidebar expose the button position via a shared signal or a known\n  ref/anchor rather than string-querying its internal class.\n- **Data source:** none — no `clients/*`, no cache. Geometry comes from the live\n  DOM; copy from `tFor(lang, \"assistantCoachmark.*\")`.\n- **Reactive language:** reads `langSignal.value` at render; the optional `lang`\n  prop is an ignored SSR seed.\n- **Liveness:** request-response-free. Timers only: the rAF-deferred phase kick,\n  a `setTimeout(800ms)` to mark `phase=2`, the `setTimeout(320ms)` exit, plus a\n  `resize` listener that re-measures the hole. No polling beyond the 30×120ms\n  acquisition retry.\n- **Dismiss / persistence:** any click on the overlay → `dismiss()`:\n  `setFadingOut(true)`, write `localStorage[\"pm:assistant-coachmark-shown\"]=\"1\"`\n  (try/catch SSR-safe), then `setVisible(false)` after 320ms. **The flag is\n  written on dismiss, NOT on first show** — so a user who reloads before clicking\n  will see it again (acceptable: it's a one-interaction gate, not a one-render\n  gate).\n- **`location.reload()`:** none. ✅ Dismiss is in-place unmount, no nav.\n- **Data-shape hazards:** `box` is `null` until measured → early `return null`\n  guard before any geometry math. All `localStorage` access is try/catch-wrapped\n  (SSR / privacy-mode safe). The SVG mask geometry is computed from `box` +\n  fixed paddings — no external data to malform.\n\n## 2. Anatomy\n```\n(localStorage shown===\"1\") → never renders\n(!visible || !box) → null\nelse:\n<div role=\"dialog\" aria-label=\"Onboarding hint — click anywhere to dismiss\"\n     onClick=dismiss\n     style=\"position:fixed;inset:0;z-index:9999;cursor:pointer;\n            transition opacity+backdrop-filter 320ms; opacity 0|1; blur 0|2px\">\n  <svg width=100% height=100% aria-hidden style=\"position:absolute;inset:0;pointer-events:none\">\n    <defs>\n      <mask id=coachmark-mask>  ← white full-rect minus a black rounded-rect over the button (evenodd cut-out)\n      <radialGradient id=coachmark-glow>  ← pink → transparent\n    <rect fill=\"rgba(15,28,33,0.74)\" mask=url(#coachmark-mask)>   ← dark backdrop with the hole\n    <ellipse fill=url(#coachmark-glow)>                           ← ambient pink bloom under button\n    (phase>=1) <circle×2 animation:coach-ripple>                 ← two staggered sonar ripples\n    <rect stroke=PINK animation:coach-breathe(phase2)>           ← spotlight ring (scale .6→1 then breathe)\n    (phase===2) <circle×4 animation:coach-orbit>                 ← orbital sparkles\n  <div style=\"… pink gradient bubble, slides in from right\">     ← speech bubble\n    <div> 👋(coach-wave) + heading \"Click here to talk to your assistant\"\n    <div> body \"Bossie drafts quotes, sends contracts, chases invoices. Tap to start.\"\n    <div> ← arrow notch (coach-arrow-nudge, phase2) pointing left at the button\n  <div style=\"bottom hint\"> \"click anywhere to dismiss\"\n  <style> @keyframes coach-ripple|breathe|orbit|wave|arrow-nudge + reduced-motion guard\n```\n- **Slots/children:** none.\n- **Dependency:** the live DOM position of DashSidebar's `.sb__textus` button (see\n  the FLAG above). No `ICN`/icon import; the 👋 is an emoji, the arrow is a CSS\n  triangle, the ring/ripples are SVG.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `lang` | `\"en\"\\|\"es\"` | `undefined` (IGNORED) | — | superseded by `langSignal` |\n\nThe island is mounted bare (`<AssistantCoachmark />`). `lang` is a vestigial SSR\nseed; the live value is `langSignal.value`.\n\n## 4. States → cases\nThe whole thing is internal timer/measurement state; isolate models it via\n`_signals` (visible/box/phase/fadingOut) + `_mocks` for the localStorage gate +\nthe target button geometry. Because it positions itself off a real\n`.sb__textus`, isolate needs a stub anchor at a known rect (provide a `box`).\n\n| state | meaning | case |\n|---|---|---|\n| settled | first visit, fully landed (phase 2): backdrop + spotlight + bubble + sparkles | `cases/settled/settled.json` |\n| entering | mid-entrance (phase 1): ring springing in, ripples emitting, bubble sliding | `cases/entering/entering.json` |\n| already-shown | `localStorage` flag set → renders nothing | `cases/already-shown/already-shown.json` |\n| fading-out | post-click exit (phase 2 + fadingOut): backdrop/blur fading to 0 | `cases/fading-out/fading-out.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"div[role='dialog']\" && e.type === \"click\")` →\n  `dismiss()`: writes `localStorage[\"pm:assistant-coachmark-shown\"]=\"1\"`, fades\n  out over 320ms, unmounts. (The entire overlay is the click target — click\n  anywhere.)\n- External (received): `window` `resize` → re-`measure()` the button → updates\n  `box` so the hole tracks the button if layout shifts.\n- No emitted custom events; no nav; no fetch.\n\n## 6. Motion (extracted — self-injected <style> + inline transitions)\nTotal intro ~700–800ms before the bubble lands; timeline:\n- **t=0** backdrop fades in (wrapper `opacity` + `backdrop-filter: blur(2px)`,\n  320ms ease-out).\n- **t=120ms** spotlight ring springs `scale(0.6)→1` (`transform 520ms\n  cubic-bezier(.34,1.56,.64,1)`, overshoot) + opacity in.\n- **t=240ms** ambient glow ellipse fades in (480ms ease-out, +240ms delay); two\n  **sonar ripples** emit (`coach-ripple` 1.6s ease-out, delays 0.24s/0.48s, 2\n  iterations).\n- **t=320ms** speech bubble slides\n  `translateX(-24px) scale(.85) rotate(2deg) → 0/1/0` (460ms bounce, +320ms\n  delay).\n- **t=520ms** dismiss hint fades + rises in.\n- **after settle (phase 2):** spotlight ring **breathes**\n  (`coach-breathe` 2.6s ease-in-out infinite, +800ms delay), 4 **orbital\n  sparkles** (`coach-orbit` 5.2s linear infinite, staggered i×1.3s), the 👋\n  **waves** (`coach-wave` 2.8s), the arrow **nudges** (`coach-arrow-nudge` 1.6s).\n- **Keyframes (in css/assistant-coachmark.css + the island's inline <style>):**\n  `coach-ripple, coach-breathe, coach-orbit, coach-wave, coach-arrow-nudge`.\n- **Jank findings:**\n  - The dark backdrop is an SVG `<rect mask=url(#coachmark-mask)>`; SVG masking +\n    `backdrop-filter: blur(2px)` on a full-viewport `position:fixed` layer is\n    paint-heavy and can stutter on first frame on low-end devices. The blur in\n    particular forces an offscreen composite of everything behind it. Acceptable\n    for a one-shot overlay, but prefer a CSS box-shadow \"hole\" (a large inset\n    shadow on a transparent div positioned over the button) over SVG masking if\n    smoothness matters.\n  - `coach-breathe` animates `filter: drop-shadow(...)` which repaints each frame\n    — fine at 2.6s, but it's the most expensive of the loops.\n- **Reduced motion:** the island ships its OWN scoped guard\n  (`@media (prefers-reduced-motion: reduce)` → `[role=dialog] * { animation:none\n  !important; transition-duration: 80ms !important }`), in ADDITION to the global\n  tokens clamp. Good — every child animation is killed and transitions snap.\n\n## 7. Responsive (own @media)\n- **No width breakpoints.** It self-positions off the measured `.sb__textus`\n  rect and re-measures on `resize`, so it tracks the sidebar at any width.\n- **Caveat:** on mobile (≤640px) the sidebar is an off-canvas drawer — the\n  `.sb__textus` button may be translated off-screen / not laid out, so `box`\n  could measure a hidden/zero-area rect or the query may resolve to an element at\n  `translateX(-100%)`. The coachmark does not special-case mobile; verify the\n  hole lands sensibly (or that the overlay is suppressed) at ≤640px during\n  capture. Likely intended for desktop first-run.\n\n## 8. A11y\n- `role=\"dialog\"` with an `aria-label`. The decorative SVG layer is\n  `aria-hidden`.\n- **GAPS (modal hygiene):**\n  - No **focus trap** and focus is not moved into the dialog on open.\n  - No **`Esc` to close** (only click-anywhere) — a keyboard-only user with no\n    pointer can't dismiss it, and Tab can escape behind the overlay.\n  - The bubble copy is in a plain `<div>`, not wired as the dialog's\n    `aria-labelledby`/`aria-describedby` (the `aria-label` carries a generic\n    string instead of the heading/body).\n  - It's a click-to-dismiss `role=dialog` with `cursor:pointer` everywhere — fine\n    visually, but no keyboard-operable dismiss affordance.\n- Reduced motion is well handled (own guard + global).\n\n## 9. Used on\n**`/dashboard` only.** Imported by `routes/dashboard/index.tsx`\n(`import AssistantCoachmark from \"../../islands/AssistantCoachmark.tsx\"`),\nrendered once at the route root (outside `.app`). Not shared. Depends on\nDashSidebar (shared) for its anchor button — but is itself dashboard-only.\n", "fixture": "{\n  \"category\": \"dashboard\",\n  \"folder\": \"assistant-coachmark\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"], \"signal\": true }\n  }\n}\n", "cases": [{"name": "already-shown.json", "content": "{\n  \"_name\": \"Already shown — localStorage flag set → returns early, renders nothing\",\n  \"_signals\": {\n    \"lang\": \"en\",\n    \"visible\": false,\n    \"fadingOut\": false,\n    \"phase\": 0,\n    \"box\": null\n  },\n  \"_mocks\": {\n    \"localStorage\": { \"pm:assistant-coachmark-shown\": \"1\" },\n    \"_anchor\": { \"selector\": \"a.sb__textus[href='/assistant']\", \"rect\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 } }\n  }\n}\n"}, {"name": "entering.json", "content": "{\n  \"_name\": \"Entering — mid-entrance (phase 1): ring springing, ripples emitting, bubble sliding in\",\n  \"_signals\": {\n    \"lang\": \"en\",\n    \"visible\": true,\n    \"fadingOut\": false,\n    \"phase\": 1,\n    \"box\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 }\n  },\n  \"_mocks\": {\n    \"localStorage\": { \"pm:assistant-coachmark-shown\": null },\n    \"_anchor\": { \"selector\": \"a.sb__textus[href='/assistant']\", \"rect\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 } }\n  }\n}\n"}, {"name": "fading-out.json", "content": "{\n  \"_name\": \"Fading out — post-click exit: backdrop opacity + blur transitioning to 0 before unmount\",\n  \"_signals\": {\n    \"lang\": \"en\",\n    \"visible\": true,\n    \"fadingOut\": true,\n    \"phase\": 2,\n    \"box\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 }\n  },\n  \"_mocks\": {\n    \"localStorage\": { \"pm:assistant-coachmark-shown\": \"1\" },\n    \"_anchor\": { \"selector\": \"a.sb__textus[href='/assistant']\", \"rect\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 } }\n  }\n}\n"}, {"name": "settled.json", "content": "{\n  \"_name\": \"Settled — first visit, fully landed (phase 2): spotlight breathing, bubble, sparkles\",\n  \"_signals\": {\n    \"lang\": \"en\",\n    \"visible\": true,\n    \"fadingOut\": false,\n    \"phase\": 2,\n    \"box\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 }\n  },\n  \"_mocks\": {\n    \"localStorage\": { \"pm:assistant-coachmark-shown\": null },\n    \"_anchor\": { \"selector\": \"a.sb__textus[href='/assistant']\", \"rect\": { \"top\": 132, \"left\": 20, \"width\": 176, \"height\": 48 } }\n  }\n}\n"}], "css": "/* ===========================================================================\n * AssistantCoachmark — NO entries in static/dashboard.css.\n *\n * The island is fully INLINE-STYLED and ships its OWN <style> block with the\n * @keyframes below, embedded at the bottom of the rendered overlay. Reproduced\n * verbatim from js/AssistantCoachmark.tsx so a rebuild has the exact motion.\n *\n * The overlay is a position:fixed inset:0 z-index:9999 backdrop with an SVG\n * mask that cuts a rounded-rect hole over the sidebar's `.sb__textus`\n * (\"My Assistant\") button, a pink spotlight ring, sonar ripples, orbital\n * sparkles, and a speech bubble. Backdrop blur(2px) + rgba(15,28,33,0.74).\n * ======================================================================== */\n\n/* --- Self-injected keyframes (verbatim from the island's inline <style>) --- */\n@keyframes coach-ripple {\n  0%   { transform: scale(0.7); opacity: 0.55; }\n  80%  { opacity: 0; }\n  100% { transform: scale(2.6); opacity: 0; }\n}\n@keyframes coach-breathe {\n  0%, 100% { filter: drop-shadow(0 0 14px rgba(255, 107, 107, 0.75)); transform: scale(1); }\n  50%      { filter: drop-shadow(0 0 22px rgba(255, 107, 107, 0.95)); transform: scale(1.025); }\n}\n@keyframes coach-orbit {\n  0%   { transform: rotate(0deg)   translateY(0) scale(1);    opacity: 0; }\n  15%  { opacity: 0.95; }\n  50%  { transform: rotate(180deg) translateY(0) scale(1.15); opacity: 0.95; }\n  85%  { opacity: 0.95; }\n  100% { transform: rotate(360deg) translateY(0) scale(1);    opacity: 0; }\n}\n@keyframes coach-wave {\n  0%, 60%, 100% { transform: rotate(0deg); }\n  70%           { transform: rotate(14deg); }\n  80%           { transform: rotate(-8deg); }\n  90%           { transform: rotate(10deg); }\n}\n@keyframes coach-arrow-nudge {\n  0%, 100% { transform: translateY(-50%) translateX(0); }\n  50%      { transform: translateY(-50%) translateX(4px); }\n}\n\n/* --- Reduced motion (verbatim — the island's own guard, scoped to the\n * role=dialog overlay; kills all child animations + clamps transitions to\n * 80ms). This is in ADDITION to the global tokens clamp. --- */\n@media (prefers-reduced-motion: reduce) {\n  [role=dialog] *,\n  [role=dialog] *::before,\n  [role=dialog] *::after {\n    animation: none !important;\n    transition-duration: 80ms !important;\n  }\n}\n\n/* ---------------------------------------------------------------------------\n * Inline-style transition reference (NOT a stylesheet rule — documents the\n * timing carried in the TSX style attrs so motion is rebuildable):\n *\n *   backdrop wrapper : transition opacity 320ms ease-out,\n *                      backdrop-filter 320ms ease-out; blur 0↔2px\n *   spotlight ring   : transition transform 520ms cubic-bezier(.34,1.56,.64,1) 120ms,\n *                      opacity 320ms ease-out 120ms; scale .6→1;\n *                      animation coach-breathe 2.6s ease-in-out infinite 800ms (phase 2)\n *   sonar ripples ×2 : animation coach-ripple 1.6s ease-out 0.24s/0.48s 2 (phase>=1)\n *   ambient glow     : transition opacity 480ms ease-out 240ms\n *   orbital sparkles : animation coach-orbit 5.2s linear infinite, delay i*1.3s (phase 2)\n *   speech bubble    : transition transform 460ms cubic-bezier(.34,1.56,.64,1) 320ms,\n *                      opacity 320ms ease-out 320ms;\n *                      translateX(-24px) scale(.85) rotate(2deg) → 0/1/0\n *   bubble wave 👋   : animation coach-wave 2.8s ease-in-out infinite\n *   arrow notch      : animation coach-arrow-nudge 1.6s ease-in-out infinite 800ms (phase 2)\n *   dismiss hint     : transition opacity 320ms ease-out 520ms,\n *                      transform 460ms cubic-bezier(.34,1.56,.64,1) 520ms\n *   dismiss/fade-out : wrapper opacity→0 + blur→0 over 320ms, then unmount\n * ------------------------------------------------------------------------- */\n", "cssName": "assistant-coachmark.css", "source": "import { useEffect, useState } from \"preact/hooks\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\nconst STORAGE_KEY = \"pm:assistant-coachmark-shown\";\nconst PINK = \"#FF6B6B\";\nconst PINK_DARK = \"#d94e4e\";\n\n/**\n * One-shot dashboard coachmark — shows on the user's FIRST visit to the\n * dashboard after onboarding completes. Masks the rest of the app with a\n * dark backdrop and cuts a hole over the \"My assistant\" sidebar button so\n * it's the only visible affordance.\n *\n * Entrance choreography (timing tuned to read as a single beat, not a slow\n * staged reveal — total intro ~700ms before the bubble fully lands):\n *\n *   t=0      — backdrop starts fading in (opacity + slight backdrop blur)\n *   t=120ms  — spotlight ring spring-pops to 1.0 from scale(0.6)\n *   t=240ms  — two sonar ripples emit outward from the button\n *   t=320ms  — speech bubble slides in from the right + scales 0.85→1\n *              with a subtle rotate(2°→0°), arrow notch wiggles\n *   t=520ms  — \"click anywhere to dismiss\" fades in at bottom\n *\n * After mount: the spotlight halo breathes (continuous pulse), the bubble's\n * arrow nudges left/right, and 4 tiny pink sparkles orbit the button. All\n * paused under prefers-reduced-motion.\n *\n * Click anywhere → fade out + persist to localStorage. The coachmark never\n * re-appears.\n */\nexport default function AssistantCoachmark(\n  _props: { lang?: Lang } = {},\n) {\n  // Self-source the reactive UI language; reading langSignal.value during\n  // render makes this island re-render live when the language flips. The\n  // optional `lang` prop is kept as an ignored SSR seed only.\n  const lang = langSignal.value;\n  const [visible, setVisible] = useState(false);\n  const [box, setBox] = useState<\n    { top: number; left: number; width: number; height: number } | null\n  >(null);\n  const [fadingOut, setFadingOut] = useState(false);\n  const [phase, setPhase] = useState<0 | 1 | 2>(0); // 0 = pre-mount, 1 = entering, 2 = settled\n\n  useEffect(() => {\n    if (typeof globalThis.window === \"undefined\") return;\n    let alreadyShown = false;\n    try {\n      alreadyShown = globalThis.localStorage.getItem(STORAGE_KEY) === \"1\";\n    } catch { /* SSR-safe */ }\n    if (alreadyShown) return;\n    let tries = 0;\n    const find = () => {\n      tries++;\n      const el = document.querySelector(\n        'a[href=\"/assistant\"].sb__textus, .sb__textus[href=\"/assistant\"]',\n      );\n      if (el instanceof HTMLElement) {\n        measure(el);\n        setVisible(true);\n        // Kick off the entrance — defer one frame so the initial paint\n        // has the elements at their start state, then animate to settled.\n        globalThis.requestAnimationFrame(() => {\n          globalThis.requestAnimationFrame(() => setPhase(1));\n        });\n        // Mark \"settled\" after the staged entrance completes (matches\n        // the longest delay in the sequence, ~720ms total).\n        globalThis.setTimeout(() => setPhase(2), 800);\n        return;\n      }\n      if (tries < 30) globalThis.setTimeout(find, 120);\n    };\n    find();\n    const onResize = () => {\n      const el = document.querySelector(\n        'a[href=\"/assistant\"].sb__textus, .sb__textus[href=\"/assistant\"]',\n      );\n      if (el instanceof HTMLElement) measure(el);\n    };\n    globalThis.addEventListener(\"resize\", onResize);\n    return () => globalThis.removeEventListener(\"resize\", onResize);\n  }, []);\n\n  function measure(el: HTMLElement) {\n    const r = el.getBoundingClientRect();\n    setBox({ top: r.top, left: r.left, width: r.width, height: r.height });\n  }\n\n  function dismiss() {\n    setFadingOut(true);\n    try {\n      globalThis.localStorage.setItem(STORAGE_KEY, \"1\");\n    } catch { /* SSR-safe */ }\n    globalThis.setTimeout(() => setVisible(false), 320);\n  }\n\n  if (!visible || !box) return null;\n\n  const padding = 8;\n  const holeTop = box.top - padding;\n  const holeLeft = box.left - padding;\n  const holeW = box.width + padding * 2;\n  const holeH = box.height + padding * 2;\n  const holeCx = holeLeft + holeW / 2;\n  const holeCy = holeTop + holeH / 2;\n\n  // Tooltip position: to the right of the hole, vertically centered.\n  const tipLeft = holeLeft + holeW + 24;\n  const tipTop = holeTop + holeH / 2 - 50;\n\n  // Two sonar ripples staggered. Diameters scale up to ~3x the button\n  // height; opacity fades to 0 by the end. Pure CSS via keyframes so we\n  // don't need rAF.\n  const rippleR = Math.max(holeW, holeH) / 2;\n\n  return (\n    <div\n      onClick={dismiss}\n      style={`position:fixed;inset:0;z-index:9999;cursor:pointer;transition:opacity 320ms ease-out, backdrop-filter 320ms ease-out;opacity:${\n        fadingOut ? 0 : 1\n      };backdrop-filter:${\n        fadingOut ? \"blur(0px)\" : \"blur(2px)\"\n      };-webkit-backdrop-filter:${fadingOut ? \"blur(0px)\" : \"blur(2px)\"}`}\n      aria-label={tFor(lang, \"assistantCoachmark.ariaLabel\")}\n      role=\"dialog\"\n    >\n      {\n        /* SVG mask: dark overlay everywhere EXCEPT a rounded rect\n          around the assistant button. Uses evenodd fill rule so the\n          inner rectangle becomes a transparent cut-out. The whole SVG\n          fades in via the wrapper opacity. The spotlight ring scale\n          springs from 0.6 → 1.0 by tying transform-origin to the hole\n          center (set inline below on the ring rect group). */\n      }\n      <svg\n        width=\"100%\"\n        height=\"100%\"\n        style=\"position:absolute;inset:0;pointer-events:none\"\n        aria-hidden=\"true\"\n      >\n        <defs>\n          <mask id=\"coachmark-mask\">\n            <rect x=\"0\" y=\"0\" width=\"100%\" height=\"100%\" fill=\"white\" />\n            <rect\n              x={holeLeft}\n              y={holeTop}\n              width={holeW}\n              height={holeH}\n              rx=\"14\"\n              ry=\"14\"\n              fill=\"black\"\n            />\n          </mask>\n          <radialGradient id=\"coachmark-glow\" cx=\"50%\" cy=\"50%\" r=\"50%\">\n            <stop offset=\"0%\" stop-color=\"rgba(255,107,107,0.35)\" />\n            <stop offset=\"60%\" stop-color=\"rgba(255,107,107,0.05)\" />\n            <stop offset=\"100%\" stop-color=\"rgba(255,107,107,0)\" />\n          </radialGradient>\n        </defs>\n\n        {\n          /* Dark backdrop — fades in over 320ms via wrapper opacity.\n            Mask cuts the hole around the button. */\n        }\n        <rect\n          x=\"0\"\n          y=\"0\"\n          width=\"100%\"\n          height=\"100%\"\n          fill=\"rgba(15,28,33,0.74)\"\n          mask=\"url(#coachmark-mask)\"\n          style={`transition:opacity 320ms ease-out;opacity:${\n            phase === 0 ? 0 : 1\n          }`}\n        />\n\n        {\n          /* Soft radial bloom under the button — ambient glow that warms\n            up as the spotlight settles. */\n        }\n        <ellipse\n          cx={holeCx}\n          cy={holeCy}\n          rx={holeW * 1.2}\n          ry={holeH * 1.4}\n          fill=\"url(#coachmark-glow)\"\n          style={`transition:opacity 480ms ease-out 240ms;opacity:${\n            phase === 0 ? 0 : 1\n          }`}\n        />\n\n        {\n          /* Sonar ripples — two concentric circles spring outward from\n            the button. Pure CSS @keyframes scale + opacity. Staggered\n            240ms apart. */\n        }\n        {phase >= 1 && (\n          <>\n            <circle\n              cx={holeCx}\n              cy={holeCy}\n              r={rippleR}\n              fill=\"none\"\n              stroke={PINK}\n              stroke-width=\"2\"\n              style=\"transform-origin:center;transform-box:fill-box;animation:coach-ripple 1.6s ease-out 0.24s 2;opacity:0\"\n            />\n            <circle\n              cx={holeCx}\n              cy={holeCy}\n              r={rippleR}\n              fill=\"none\"\n              stroke={PINK}\n              stroke-width=\"2\"\n              style=\"transform-origin:center;transform-box:fill-box;animation:coach-ripple 1.6s ease-out 0.48s 2;opacity:0\"\n            />\n          </>\n        )}\n\n        {\n          /* Spotlight ring — the always-visible pink stroke around the\n            button. Scales from 0.6 → 1 with overshoot on entrance, then\n            breathes continuously to keep attention. transform-origin is\n            the button center (set via transform-box on the rect). */\n        }\n        <rect\n          x={holeLeft - 2}\n          y={holeTop - 2}\n          width={holeW + 4}\n          height={holeH + 4}\n          rx=\"16\"\n          ry=\"16\"\n          fill=\"none\"\n          stroke={PINK}\n          stroke-width=\"2.5\"\n          style={`filter:drop-shadow(0 0 14px rgba(255,107,107,0.75));transform-origin:${holeCx}px ${holeCy}px;transform-box:view-box;transition:transform 520ms cubic-bezier(0.34, 1.56, 0.64, 1) 120ms, opacity 320ms ease-out 120ms;transform:${\n            phase === 0 ? \"scale(0.6)\" : \"scale(1)\"\n          };opacity:${phase === 0 ? 0 : 1};animation:${\n            phase === 2\n              ? \"coach-breathe 2.6s ease-in-out infinite 800ms\"\n              : \"none\"\n          }`}\n        />\n\n        {\n          /* Orbital sparkles — 4 tiny pink dots that drift around the\n            button after settle. Subtle, not loud. */\n        }\n        {phase === 2 && (\n          <>\n            {[0, 1, 2, 3].map((i) => (\n              <circle\n                key={i}\n                cx={holeCx}\n                cy={holeCy - holeH / 2 - 14}\n                r=\"2.5\"\n                fill={PINK}\n                style={`transform-origin:${holeCx}px ${holeCy}px;animation:coach-orbit 5.2s linear infinite;animation-delay:${\n                  i * 1.3\n                }s;opacity:0.85;filter:drop-shadow(0 0 6px rgba(255,107,107,0.7))`}\n              />\n            ))}\n          </>\n        )}\n      </svg>\n\n      {\n        /* Speech bubble — slides in from the right of the button with a\n          subtle scale + tilt. After settle, the arrow nudges as a\n          breathing micro-animation. */\n      }\n      <div\n        style={`position:absolute;top:${tipTop}px;left:${tipLeft}px;background:linear-gradient(135deg, ${PINK} 0%, ${PINK_DARK} 100%);color:#fff;padding:14px 18px;border-radius:14px;max-width:280px;box-shadow:0 14px 40px rgba(255,107,107,0.45), 0 2px 8px rgba(0,0,0,0.18);font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;transition:transform 460ms cubic-bezier(0.34, 1.56, 0.64, 1) 320ms, opacity 320ms ease-out 320ms;transform-origin:left center;transform:${\n          phase === 0\n            ? \"translateX(-24px) scale(0.85) rotate(2deg)\"\n            : \"translateX(0) scale(1) rotate(0deg)\"\n        };opacity:${phase === 0 ? 0 : 1}`}\n      >\n        <div style=\"font-weight:800;font-size:14px;letter-spacing:.01em;display:flex;align-items:center;gap:6px\">\n          <span style=\"display:inline-block;animation:coach-wave 2.8s ease-in-out infinite;transform-origin:70% 80%\">\n            👋\n          </span>\n          <span>{tFor(lang, \"assistantCoachmark.heading\")}</span>\n        </div>\n        <div style=\"margin-top:6px;font-size:12px;opacity:0.92;line-height:1.4\">\n          {tFor(lang, \"assistantCoachmark.body\")}\n        </div>\n        {\n          /* Arrow notch pointing left toward the highlighted button —\n            subtle nudge animation while idle. */\n        }\n        <div\n          style={`position:absolute;left:-8px;top:50%;transform:translateY(-50%);width:0;height:0;border-top:8px solid transparent;border-bottom:8px solid transparent;border-right:8px solid ${PINK};animation:${\n            phase === 2\n              ? \"coach-arrow-nudge 1.6s ease-in-out infinite 800ms\"\n              : \"none\"\n          }`}\n        />\n      </div>\n\n      {/* Dismiss hint */}\n      <div\n        style={`position:absolute;bottom:32px;left:50%;color:#fff;font-size:12px;opacity:${\n          phase === 0 ? 0 : 0.65\n        };letter-spacing:.04em;text-align:center;transition:opacity 320ms ease-out 520ms, transform 460ms cubic-bezier(0.34, 1.56, 0.64, 1) 520ms;transform:translateX(-50%) translateY(${\n          phase === 0 ? \"8px\" : \"0\"\n        })`}\n      >\n        {tFor(lang, \"assistantCoachmark.dismissHint\")}\n      </div>\n\n      <style>\n        {`\n        @keyframes coach-ripple {\n          0%   { transform: scale(0.7); opacity: 0.55; }\n          80%  { opacity: 0; }\n          100% { transform: scale(2.6); opacity: 0; }\n        }\n        @keyframes coach-breathe {\n          0%, 100% { filter: drop-shadow(0 0 14px rgba(255,107,107,0.75)); transform: scale(1); }\n          50%      { filter: drop-shadow(0 0 22px rgba(255,107,107,0.95)); transform: scale(1.025); }\n        }\n        @keyframes coach-orbit {\n          0%   { transform: rotate(0deg)   translateY(0) scale(1); opacity: 0; }\n          15%  { opacity: 0.95; }\n          50%  { transform: rotate(180deg) translateY(0) scale(1.15); opacity: 0.95; }\n          85%  { opacity: 0.95; }\n          100% { transform: rotate(360deg) translateY(0) scale(1); opacity: 0; }\n        }\n        @keyframes coach-wave {\n          0%, 60%, 100% { transform: rotate(0deg); }\n          70%           { transform: rotate(14deg); }\n          80%           { transform: rotate(-8deg); }\n          90%           { transform: rotate(10deg); }\n        }\n        @keyframes coach-arrow-nudge {\n          0%, 100% { transform: translateY(-50%) translateX(0); }\n          50%      { transform: translateY(-50%) translateX(4px); }\n        }\n        @media (prefers-reduced-motion: reduce) {\n          [role=dialog] *,\n          [role=dialog] *::before,\n          [role=dialog] *::after {\n            animation: none !important;\n            transition-duration: 80ms !important;\n          }\n        }\n      `}\n      </style>\n    </div>\n  );\n}\n", "sourceName": "AssistantCoachmark.tsx", "checklist": "# Capture checklist — AssistantCoachmark\n\n**Theme:** light only.\n**Auth:** log in with dev master OTP `000000`.\n**Route:** `http://localhost:5280/dashboard`. The overlay is mounted at the route\nroot and only appears on a user's FIRST visit (self-gates on\n`localStorage[\"pm:assistant-coachmark-shown\"]`).\n\n## Driving the one-shot (it hides itself after one interaction)\n- **To make it appear:** clear the flag, then reload —\n  `localStorage.removeItem(\"pm:assistant-coachmark-shown\")` in the console (or use\n  a fresh/incognito session), then navigate to `/dashboard`. Wait for the\n  DashSidebar island to hydrate so the `.sb__textus` button exists (the coachmark\n  retries up to ~3.6s to find it).\n- **It will NOT reappear** once you click it (the flag is written on dismiss).\n  Clear the flag again between captures.\n\n## Element(s) to crop\n- The full-viewport overlay: the dark masked backdrop with the spotlight HOLE cut\n  over the sidebar \"My Assistant\" button, the pink ring + ripples, the speech\n  bubble to the right of the button, and the bottom \"click anywhere to dismiss\"\n  hint. Capture the whole viewport (it's a full-screen overlay).\n\n## Viewports\n- No own breakpoints; it tracks the measured button rect. Shoot:\n  - **1280px** (primary — desktop first-run, sidebar expanded, hole over the\n    expanded `.sb__textus`).\n  - **641px** (sidebar still a fixed rail — confirm the hole still lands).\n  - **390px** (sidebar is an off-canvas drawer — VERIFY where/whether the hole\n    lands; the button may be translated off-canvas. Document the actual behavior;\n    do not assume it shows.)\n\n## Transient states to drive (MOTION component — film the choreography)\n1. **entrance (filmstrip)** — film the staged intro on fresh appearance:\n   - t≈0: backdrop fades + blurs in.\n   - t≈120ms: spotlight ring springs scale .6→1 (overshoot).\n   - t≈240ms: ambient glow + two sonar ripples emit.\n   - t≈320ms: speech bubble slides in from the right (scale + slight rotate).\n   - t≈520ms: dismiss hint fades up.\n2. **settled (phase 2)** — the looping idle: ring breathing (2.6s), 4 orbital\n   sparkles (5.2s, staggered), 👋 waving (2.8s), arrow nudging (1.6s). Capture a\n   still + a short loop.\n3. **dismiss / fade-out** — click anywhere; film the 320ms backdrop opacity +\n   blur fade to 0, then unmount. Confirm `localStorage` flag becomes \"1\".\n4. **already-shown** — reload WITHOUT clearing the flag: overlay does not appear\n   (clean dashboard). Capture to evidence the one-shot gate.\n\n## Motion to film + reduced-motion\n- Film items 1–2 above, then re-shoot with **`prefers-reduced-motion: reduce`**:\n  the island's OWN scoped guard kills all child animations and snaps transitions\n  to 80ms — capture the static, no-loop version (backdrop + ring + bubble appear\n  near-instantly, nothing breathing/orbiting/waving).\n\n## Notes\n- This overlays the REAL DashSidebar — the spotlight hole geometry depends on the\n  live `.sb__textus` rect. Capture against the actual sidebar, not a stub.\n- NO fabricated screenshots — drive the real overlay via the localStorage flag.\n"}, {"name": "dash-sections", "component": "DashSections", "spec": "# DashSections\n\nThe **page-composition** module for `/dashboard`. A single SSR file\n(`components/DashSections.tsx`) that exports the section blocks\n`DashboardPage` assembles into the page body: `Kpis`, `ActiveJobs`,\n`QuotesAwaiting`, `Outstanding`, `Activity` — plus a now-dead `Hero` export.\nNone of these are islands themselves; they are pure presentational functions\nrendered server-side from rows the `DashboardPage` island has already mapped.\nThe only client-side motion inside them is the embedded `<Ticker/>` island\n(money count-up).\n\n## 1. Classification & behavior\n- **Bucket:** `page-composition` — a bag of section components, not a single\n  rendered unit. File lives at `components/DashSections.tsx` (a plain component\n  module, NOT under `islands/`).\n- **Interaction tier:** **static SSR** for every block. They take fully-mapped\n  props and emit markup. There is **no `useState`, no `useEffect`, no fetch**\n  here — `DashboardPage` (the island) owns all data + lifecycle and passes rows\n  down. The only interactive descendant is the `<Ticker/>` island\n  (`.money__amt` in `Outstanding`, `.hero__title em` in the dead `Hero`).\n- **Server mutations:** NONE. The \"Nudge by text\" / \"View quote\" / \"Nudge all\"\n  buttons are **inert** `<button type=\"button\">` with **no `onClick`** — visual\n  affordances only (roadmap TODO). FLAG: they look actionable but do nothing\n  yet; a rebuild must either wire them or down-rank them to avoid a dead-click.\n  No form, no PRG, no flash.\n- **`location.reload()`:** none. ✅\n- **Data source per region:** all data arrives as **props** from `DashboardPage`\n  (which fetches via `clients/dashboard.ts` + `clients/quotes.ts` and maps DTOs\n  to the row types below). DashSections never touches `clients/*` or\n  `lib/dash-cache.ts` directly. The static fallback values\n  (`lib/dash-seed.ts`: `SEED_KPIS`, `SEED_JOBS`, `SEED_QUOTES`,\n  `SEED_OUTSTANDING`, `SEED_ACTIVITY`) seed the same prop shapes when the\n  backend hasn't answered.\n- **Honest-empty (per block, all internal):**\n  - `ActiveJobs` — `jobs.length === 0` → empty copy\n    (`activeJobs.empty.text`) + a \"See pipeline →\" CTA to `/quotes`.\n  - `QuotesAwaiting` — `quotes.length === 0` → one-liner\n    (`quotesAwaiting.empty`), and the count pill is suppressed.\n  - `Outstanding` — `allZero` (`current+mid+overdue === 0 && owed === 0`) hides\n    the bar/legend and shows `outstanding.empty.noInvoices` (no rows) or\n    `outstanding.empty.allPaid` (rows exist but settled); the \"Nudge all\"\n    button is suppressed.\n  - `Activity` — `items.length === 0` swaps the header sub\n    (`activity.emptySub` vs `activity.busySub`); the feed simply renders no rows.\n  - `Kpis` — never structurally empty (always 4 tiles); `avgJob` shows\n    `kpis.avgJob.none` (\"No paid jobs yet\") instead of `$0`, and Outstanding is\n    clamped to `$0` by `DashboardPage.pickKpis` when `pending === 0`.\n- **Liveness:** request-response, render-once from props. No polling, no\n  `setInterval`, no websocket. (`<Ticker/>` runs one rAF count-up on mount.)\n- **i18n:** every string flows through `tFor(lang, …)`; a `plural()` helper\n  resolves `.one`/`.other` for SSR call sites (frontend `tn` is langSignal-only\n  and can't honor an explicit lang). `lang` is threaded as a prop from\n  `DashboardPage` (which reads `langSignal.value`).\n- **Data-shape hazards:**\n  - `QuotesAwaiting` derives its total by **string-parsing the formatted amount\n    back to a number** (`Number(q.amt.replace(/[^0-9.]/g,\"\"))`) — fragile if the\n    formatter ever emits non-`$1,234` shapes (e.g. `k` suffix); it would\n    mis-sum.\n  - `Outstanding` uses `total = realTotal || 1` to avoid divide-by-zero in the\n    bar segment widths.\n  - `Activity` injects `a.html` via `dangerouslySetInnerHTML` — trusted,\n    server-derived + `escapeHtml`'d in `DashboardPage` (the `· N×` dupe badge is\n    appended as trusted markup). No user input reaches it unescaped.\n  - `ActiveJobs` shows `total ?? jobs.length` as the header count so the badge\n    matches the \"Active jobs\" KPI even though only the top slice renders.\n\n## 2. Anatomy\n```\nDashSections.tsx exports (composed by DashboardPage in this order):\n  <Kpis>                                  ← section.kpis\n    .kpi × 4 { .kpi__icon, .kpi__label, .kpi__val, .kpi__delta[--up|warn|neutral] }\n  <div class=\"grid\">\n    <ActiveJobs>                          ← .panel\n      .panel__head { dot · .panel__title · .panel__count · a.panel__action \"See all →\" }\n      empty → copy + a.panel__action \"/quotes\"\n      else  → .job × n { .job__icon · title/meta/.job__progress · .job__amount }\n    <QuotesAwaiting>                       ← .panel\n      .panel__head { .panel__title · .panel__count(coffee) · a.panel__action \"/quotes\" }\n      empty → .quotesAwaiting.empty line\n      .quote-item × n { client/amt row · sub(hot 🔥 / cold) · .quote-item__cta(2 inert qbtn) }\n  <div class=\"grid\">\n    <Activity id=\"activity\">               ← .panel\n      .panel__head { title · busy/empty sub · a.panel__action \"/activity\" }\n      .activity-item × n { .activity-item__icon · dangerouslySetInnerHTML · time }\n    <Outstanding>                          ← .money (NOT .panel)\n      .money__head { .money__label · .money__amt → $<Ticker value={owed}/> · inert \"Nudge all\" }\n      allZero → empty line; else → .money__bar(3 segs) + .money__legend(3)\n      items → dashed-top list of {client · meta · amount}\n\nDEAD export <Hero> (.hero*, .btn*) — no importer renders it; replaced by the\n  .assistant-cta banner in DashboardPage. Still embeds <Ticker/> in .hero__title em.\n  The `.hero` box IS reused by the DashboardSkeleton (see dashboard-page.css).\n```\n- **Slots/children:** none — each block is self-composed from its row props.\n- **Icon dependency:** `lib/dash-icons.tsx` (`I` renderer + `ICN` map). KPIs use\n  `hardhat/invoice/quote/trend`; jobs use the row's `icon`\n  (`hardhat/wrench/truck/paint/ruler`); quotes/outstanding use `send/eye`;\n  activity uses `send/check/contract/card/invoice/msg/sparkle`. (Already copied\n  into `shared-components/dash-sidebar/js/dash-icons.tsx`.)\n- **Ticker dependency:** imports `islands/Ticker.tsx` — see the `ticker/` spec.\n\n## 3. Props\nPer exported block (all SSR, none are signals — `lang` is threaded, not read):\n\n**Kpis** `KpisProps`\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `activeJobs` | number | — | number | no |\n| `outstanding` | number (dollars) | — | number | no |\n| `outstandingCount` | number | — | number | no |\n| `outstandingOverdue` | number | — | number | no |\n| `pendingQuotes` | number | — | number | no |\n| `pendingTotal` | number (dollars) | — | number | no |\n| `avgJob` | number (dollars, 0 ⇒ \"none\") | — | number | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n**ActiveJobs** `{ jobs: JobRow[]; total?: number; lang? }`\n**QuotesAwaiting** `{ quotes: QuoteRow[]; lang? }`\n**Outstanding** `{ owed, current, mid, overdue: number; items: OutstandingRow[]; lang? }`\n**Activity** `{ items: ActivityEntry[]; lang? }`\n**Hero (DEAD)** `{ thisMonthBilled, pendingQuotes: number; outstandingOverdue?; lang? }`\n\nRow types (from source): `JobRow{client,task,amount,paid,pct,due,icon,color,status{kind,txt}}`,\n`QuoteRow{client,desc,amt,sent,hot?,cold?}`,\n`OutstandingRow{client,meta,metaColor,metaWeight,amount}`,\n`ActivityEntry{icon,bg,fg,html,time}`.\n\n## 4. States → cases\nModeled as a **composition fixture** (`\"components\"`) — each block is a row in\n`isolate`, fed the real seed shapes from `lib/dash-seed.ts`.\n\n| state | meaning | case |\n|---|---|---|\n| populated | all blocks filled from `SEED_*` (jobs, quotes, outstanding, activity, KPIs) | `cases/populated/populated.json` |\n| empty | brand-new account — every block's honest-empty branch | `cases/empty/empty.json` |\n| mobile | ≤640px: `.grid`→1-col, `.kpis`→2-up | `cases/mobile/mobile.json` |\n\n(The dead `Hero` is documented but not given a live case — it has no importer.)\n\n## 5. Events\n- `ev.expect(e => e.source === \"a.panel__action\" && e.type === \"click\")` →\n  nav (`#` for ActiveJobs header, `/quotes`, `/activity`).\n- `ev.expect(e => e.source === \"a.panel__action[href='/quotes']\" && e.type === \"click\")`\n  → ActiveJobs empty CTA → `/quotes`.\n- `ev.expect(e => e.source === \"button.qbtn--nudge\" && e.type === \"click\")` →\n  **NO-OP** (inert; flag). Same for `button.qbtn--view` and the Outstanding\n  \"Nudge all\" button.\n- No emitted custom events; no form submit. All real interactivity bubbles up to\n  navigation links; the count-up is internal to `<Ticker/>`.\n\n## 6. Motion (extracted — from dash-sections.css + dashboard.css)\n- **`.kpi:hover`** → `translateY(-2px)` + shadow grow over **200ms**\n  `cubic-bezier(.34,1.56,.64,1)` (bounce). Animates `transform` (cheap) +\n  `box-shadow` (paint). **Jank finding:** the shadow change repaints the tile on\n  hover; acceptable (hover, not scroll). Fix on low-end: layer a pseudo-element\n  shadow and fade its opacity instead of animating `box-shadow`.\n- **`.job__progress-bar`** — width set inline as a static `%`; no transition, so\n  it does NOT animate in (paints at final width). (A rebuild could ease it from\n  0 with `transition: width`.)\n- **`.hero__pill-dot`** (used by ActiveJobs header dot, reused from `.hero`) →\n  `ppulse` 2s infinite ring pulse (`@keyframes ppulse` in dash-sections.css).\n- **Money count-up** — `<Ticker value={owed}/>` rAF eases 0→owed over 1400ms\n  (see `ticker/` spec; **no reduced-motion guard** — a11y gap).\n- **DEAD Hero** `.btn:hover` → `translateY(-1px)` + `brightness(1.05)` 180ms\n  bounce; `.hero__title em` hosts the pink Ticker.\n- **Reduced motion:** no section-local override — the **global tokens `@media\n  (prefers-reduced-motion: reduce)`** clamps the kpi hover-lift transition and\n  `ppulse` to `0.01ms`. The `<Ticker/>` count-up is JS-driven and is NOT clamped\n  (flagged).\n\n## 7. Responsive (this module's own @media — dash-sections.css)\n- **Single breakpoint: `max-width: 640px`** (NOT the product default 720px —\n  verify here).\n  - `.grid` → `grid-template-columns: 1fr` (single column), gap 14px.\n  - `.kpis` → `repeat(2, 1fr)` (2-up), gap 10px; `.kpi` padding tightens,\n    `.kpi__val` → 22px, `.kpi__label`/`.kpi__delta-sub` allowed to wrap.\n- `> 640px`: `.grid` is `1.45fr 1fr`; `.kpis` is `repeat(4,1fr)`.\n- The `.panel` / `.money` cards themselves have no width media query — they fill\n  their grid cell.\n\n## 8. A11y\n- **No headings hierarchy issue:** each panel uses `<h3 class=\"panel__title\">`;\n  the KPI labels are plain `<div>` (not headings) — fine.\n- Section nav targets are real `<a>` (keyboard-operable). Icons are decorative\n  SVG (no labels — acceptable, text follows).\n- **GAP — inert buttons:** `.qbtn`/\"Nudge all\" are focusable `<button>`s with no\n  action; a screen-reader/keyboard user can focus and \"press\" them to nothing.\n  Either wire them or `disabled` + a \"coming soon\" affordance.\n- **GAP — Activity dupe badge** is injected via `dangerouslySetInnerHTML`; it is\n  trusted/escaped but the `· N×` count isn't announced as a separate unit.\n- `Outstanding`'s money figure is animated text (`<Ticker/>`) with no\n  `aria-live` and no reduced-motion guard — a number that visibly spins up for\n  SR/low-vision users (flag; see ticker/ §8).\n- Empty-state copy is real text (announced). Reduced motion handled globally.\n\n## 9. Used on\n**`/dashboard` only**, composed by the `DashboardPage` island\n(`islands/DashboardPage.tsx` imports `Kpis/ActiveJobs/QuotesAwaiting/Outstanding/\nActivity` from `components/DashSections.tsx`). The `Hero` export has **no\nimporter** (dead). `lib/dash-seed.ts` references the same row types for the\nstatic fallback. Not shared with any other route.\n", "fixture": "{\n  \"category\": \"dashboard\",\n  \"folder\": \"dash-sections\",\n  \"controls\": {\n    \"lang\": {\n      \"type\": \"select\",\n      \"value\": \"en\",\n      \"options\": [\n        \"en\",\n        \"es\"\n      ]\n    }\n  },\n  \"components\": {\n    \"Kpis\": {\n      \"controls\": {\n        \"lang\": {\n          \"type\": \"select\",\n          \"value\": \"en\",\n          \"options\": [\n            \"en\",\n            \"es\"\n          ]\n        }\n      }\n    },\n    \"ActiveJobs\": {\n      \"controls\": {\n        \"lang\": {\n          \"type\": \"select\",\n          \"value\": \"en\",\n          \"options\": [\n            \"en\",\n            \"es\"\n          ]\n        }\n      }\n    },\n    \"QuotesAwaiting\": {\n      \"controls\": {\n        \"lang\": {\n          \"type\": \"select\",\n          \"value\": \"en\",\n          \"options\": [\n            \"en\",\n            \"es\"\n          ]\n        }\n      }\n    },\n    \"Activity\": {\n      \"controls\": {\n        \"lang\": {\n          \"type\": \"select\",\n          \"value\": \"en\",\n          \"options\": [\n            \"en\",\n            \"es\"\n          ]\n        }\n      }\n    },\n    \"Outstanding\": {\n      \"controls\": {\n        \"lang\": {\n          \"type\": \"select\",\n          \"value\": \"en\",\n          \"options\": [\n            \"en\",\n            \"es\"\n          ]\n        }\n      }\n    }\n  }\n}", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty \\u2014 brand-new account; every block's honest-empty branch\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"Kpis\": {\n      \"props\": {\n        \"activeJobs\": 0,\n        \"outstanding\": 0,\n        \"outstandingCount\": 0,\n        \"outstandingOverdue\": 0,\n        \"pendingQuotes\": 0,\n        \"pendingTotal\": 0,\n        \"avgJob\": 0,\n        \"lang\": \"en\"\n      }\n    },\n    \"ActiveJobs\": {\n      \"props\": {\n        \"total\": 0,\n        \"lang\": \"en\",\n        \"jobs\": []\n      }\n    },\n    \"QuotesAwaiting\": {\n      \"props\": {\n        \"lang\": \"en\",\n        \"quotes\": []\n      }\n    },\n    \"Activity\": {\n      \"props\": {\n        \"lang\": \"en\",\n        \"items\": []\n      }\n    },\n    \"Outstanding\": {\n      \"props\": {\n        \"owed\": 0,\n        \"current\": 0,\n        \"mid\": 0,\n        \"overdue\": 0,\n        \"lang\": \"en\",\n        \"items\": []\n      }\n    }\n  }\n}"}, {"name": "mobile.json", "content": "{\n  \"_name\": \"Mobile (<=640px) \\u2014 .grid stacks 1-col, .kpis drops to 2-up\",\n  \"lang\": \"en\",\n  \"_viewport\": {\n    \"width\": 390,\n    \"height\": 844\n  },\n  \"_mocks\": {\n    \"Kpis\": {\n      \"props\": {\n        \"activeJobs\": 7,\n        \"outstanding\": 6420,\n        \"outstandingCount\": 4,\n        \"outstandingOverdue\": 1,\n        \"pendingQuotes\": 4,\n        \"pendingTotal\": 12800,\n        \"avgJob\": 2830,\n        \"lang\": \"en\"\n      }\n    },\n    \"ActiveJobs\": {\n      \"props\": {\n        \"total\": 7,\n        \"lang\": \"en\",\n        \"jobs\": [\n          {\n            \"client\": \"Maple Grove Apartments\",\n            \"task\": \"Re-roof building C\",\n            \"amount\": \"$4,800\",\n            \"paid\": \"$2,400 paid\",\n            \"pct\": 50,\n            \"due\": \"today\",\n            \"icon\": \"hardhat\",\n            \"color\": \"var(--brand-green)\",\n            \"status\": {\n              \"kind\": \"green\",\n              \"txt\": \"On track\"\n            }\n          },\n          {\n            \"client\": \"Sarah Chen\",\n            \"task\": \"Bathroom remodel\",\n            \"amount\": \"$8,200\",\n            \"paid\": \"$3,000 paid\",\n            \"pct\": 36,\n            \"due\": \"Wed\",\n            \"icon\": \"wrench\",\n            \"color\": \"var(--brand-pink)\",\n            \"status\": {\n              \"kind\": \"green\",\n              \"txt\": \"Crew onsite\"\n            }\n          }\n        ]\n      }\n    },\n    \"QuotesAwaiting\": {\n      \"props\": {\n        \"lang\": \"en\",\n        \"quotes\": [\n          {\n            \"client\": \"Tom & Linda K.\",\n            \"desc\": \"Garage epoxy floor\",\n            \"amt\": \"$3,400\",\n            \"sent\": \"Sent Mon \\u00b7 viewed 2\\u00d7\",\n            \"hot\": true\n          },\n          {\n            \"client\": \"Bayside Properties\",\n            \"desc\": \"Gutter cleaning, 4-unit\",\n            \"amt\": \"$1,680\",\n            \"sent\": \"Sent 4 days ago\",\n            \"hot\": false,\n            \"cold\": true\n          }\n        ]\n      }\n    },\n    \"Activity\": {\n      \"props\": {\n        \"lang\": \"en\",\n        \"items\": [\n          {\n            \"icon\": \"check\",\n            \"bg\": \"var(--green-50)\",\n            \"fg\": \"var(--green-600)\",\n            \"html\": \"<strong>Tom K.</strong> opened your quote \\u2014 2nd time\",\n            \"time\": \"2 min ago\"\n          },\n          {\n            \"icon\": \"send\",\n            \"bg\": \"var(--pink-50)\",\n            \"fg\": \"var(--pink-700)\",\n            \"html\": \"Texted quote to <strong>Greenleaf HOA</strong>\",\n            \"time\": \"1 hr ago\"\n          }\n        ]\n      }\n    },\n    \"Outstanding\": {\n      \"props\": {\n        \"owed\": 6420,\n        \"current\": 3340,\n        \"mid\": 1920,\n        \"overdue\": 1160,\n        \"lang\": \"en\",\n        \"items\": [\n          {\n            \"client\": \"Hilltop Diner\",\n            \"meta\": \"5 days overdue \\u00b7 #INV-204\",\n            \"metaColor\": \"var(--pink-700)\",\n            \"metaWeight\": 700,\n            \"amount\": \"$1,160\"\n          }\n        ]\n      }\n    }\n  }\n}"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated \\u2014 all blocks filled from lib/dash-seed.ts (SEED_*)\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"Kpis\": {\n      \"props\": {\n        \"activeJobs\": 7,\n        \"outstanding\": 6420,\n        \"outstandingCount\": 4,\n        \"outstandingOverdue\": 1,\n        \"pendingQuotes\": 4,\n        \"pendingTotal\": 12800,\n        \"avgJob\": 2830,\n        \"lang\": \"en\"\n      }\n    },\n    \"ActiveJobs\": {\n      \"props\": {\n        \"total\": 7,\n        \"lang\": \"en\",\n        \"jobs\": [\n          {\n            \"client\": \"Maple Grove Apartments\",\n            \"task\": \"Re-roof building C\",\n            \"amount\": \"$4,800\",\n            \"paid\": \"$2,400 paid\",\n            \"pct\": 50,\n            \"due\": \"today\",\n            \"icon\": \"hardhat\",\n            \"color\": \"var(--brand-green)\",\n            \"status\": {\n              \"kind\": \"green\",\n              \"txt\": \"On track\"\n            }\n          },\n          {\n            \"client\": \"Sarah Chen\",\n            \"task\": \"Bathroom remodel\",\n            \"amount\": \"$8,200\",\n            \"paid\": \"$3,000 paid\",\n            \"pct\": 36,\n            \"due\": \"Wed\",\n            \"icon\": \"wrench\",\n            \"color\": \"var(--brand-pink)\",\n            \"status\": {\n              \"kind\": \"green\",\n              \"txt\": \"Crew onsite\"\n            }\n          },\n          {\n            \"client\": \"Marshall & Sons\",\n            \"task\": \"Driveway re-pour\",\n            \"amount\": \"$2,950\",\n            \"paid\": \"Deposit due\",\n            \"pct\": 18,\n            \"due\": \"Fri\",\n            \"icon\": \"truck\",\n            \"color\": \"var(--coffee-500)\",\n            \"status\": {\n              \"kind\": \"warn\",\n              \"txt\": \"Awaiting permit\"\n            }\n          },\n          {\n            \"client\": \"Jana Patel\",\n            \"task\": \"Interior paint, 2BR\",\n            \"amount\": \"$1,650\",\n            \"paid\": \"Quoted\",\n            \"pct\": 0,\n            \"due\": \"Mon Apr 29\",\n            \"icon\": \"paint\",\n            \"color\": \"var(--brand-pink)\",\n            \"status\": {\n              \"kind\": \"teal\",\n              \"txt\": \"Scheduled\"\n            }\n          },\n          {\n            \"client\": \"Cobblestone Cafe\",\n            \"task\": \"Patio re-tile\",\n            \"amount\": \"$3,400\",\n            \"paid\": \"$1,000 paid\",\n            \"pct\": 30,\n            \"due\": \"Apr 30\",\n            \"icon\": \"ruler\",\n            \"color\": \"var(--brand-green)\",\n            \"status\": {\n              \"kind\": \"green\",\n              \"txt\": \"On track\"\n            }\n          }\n        ]\n      }\n    },\n    \"QuotesAwaiting\": {\n      \"props\": {\n        \"lang\": \"en\",\n        \"quotes\": [\n          {\n            \"client\": \"Tom & Linda K.\",\n            \"desc\": \"Garage epoxy floor\",\n            \"amt\": \"$3,400\",\n            \"sent\": \"Sent Mon \\u00b7 viewed 2\\u00d7\",\n            \"hot\": true\n          },\n          {\n            \"client\": \"Greenleaf HOA\",\n            \"desc\": \"Common-area repaint\",\n            \"amt\": \"$5,800\",\n            \"sent\": \"Sent Tue \\u00b7 viewed\",\n            \"hot\": false\n          },\n          {\n            \"client\": \"Marcus Lin\",\n            \"desc\": \"Kitchen backsplash\",\n            \"amt\": \"$1,920\",\n            \"sent\": \"Sent today\",\n            \"hot\": false\n          },\n          {\n            \"client\": \"Bayside Properties\",\n            \"desc\": \"Gutter cleaning, 4-unit\",\n            \"amt\": \"$1,680\",\n            \"sent\": \"Sent 4 days ago\",\n            \"hot\": false,\n            \"cold\": true\n          }\n        ]\n      }\n    },\n    \"Activity\": {\n      \"props\": {\n        \"lang\": \"en\",\n        \"items\": [\n          {\n            \"icon\": \"check\",\n            \"bg\": \"var(--green-50)\",\n            \"fg\": \"var(--green-600)\",\n            \"html\": \"<strong>Tom K.</strong> opened your quote \\u2014 2nd time\",\n            \"time\": \"2 min ago\"\n          },\n          {\n            \"icon\": \"send\",\n            \"bg\": \"var(--pink-50)\",\n            \"fg\": \"var(--pink-700)\",\n            \"html\": \"Texted quote to <strong>Greenleaf HOA</strong>\",\n            \"time\": \"1 hr ago\"\n          },\n          {\n            \"icon\": \"card\",\n            \"bg\": \"var(--green-50)\",\n            \"fg\": \"var(--green-600)\",\n            \"html\": \"<strong>Maple Grove</strong> paid invoice deposit\",\n            \"time\": \"3 hr ago\"\n          },\n          {\n            \"icon\": \"contract\",\n            \"bg\": \"var(--teal-50)\",\n            \"fg\": \"var(--teal-600)\",\n            \"html\": \"<strong>Sarah Chen</strong> e-signed her contract\",\n            \"time\": \"Yesterday\"\n          }\n        ]\n      }\n    },\n    \"Outstanding\": {\n      \"props\": {\n        \"owed\": 6420,\n        \"current\": 3340,\n        \"mid\": 1920,\n        \"overdue\": 1160,\n        \"lang\": \"en\",\n        \"items\": [\n          {\n            \"client\": \"Hilltop Diner\",\n            \"meta\": \"5 days overdue \\u00b7 #INV-204\",\n            \"metaColor\": \"var(--pink-700)\",\n            \"metaWeight\": 700,\n            \"amount\": \"$1,160\"\n          },\n          {\n            \"client\": \"Sarah Chen\",\n            \"meta\": \"Due in 3 days \\u00b7 #INV-208\",\n            \"metaColor\": \"var(--coffee-500)\",\n            \"metaWeight\": 600,\n            \"amount\": \"$1,920\"\n          },\n          {\n            \"client\": \"Maple Grove Apts.\",\n            \"meta\": \"Due Apr 30 \\u00b7 #INV-210\",\n            \"metaColor\": \"var(--green-600)\",\n            \"metaWeight\": 600,\n            \"amount\": \"$3,340\"\n          }\n        ]\n      }\n    }\n  }\n}"}], "css": "/* ===========================================================================\n * DashSections CSS — the section-card styles for every block this\n * page-composition renders: Kpis (.kpis/.kpi*), ActiveJobs (.panel + .job*),\n * QuotesAwaiting (.panel + .quote-item* + .qbtn*), Outstanding (.money*),\n * Activity (.panel + .activity-item*). Plus the DEAD `Hero` export's `.hero*`\n * + `.btn*` rules (kept for dead-code documentation).\n *\n * Source: static/dashboard.css. Tokens from _proto/colors_and_type.css.\n * The animated <Ticker/> rendered inside Hero/Outstanding has NO CSS of its\n * own — it is just a text node inside `.money__amt` / `.hero__title em`.\n * ======================================================================== */\n\n/* ------ KPI rail (Kpis) -------------------------------------------------- */\n.kpis {\n  display: grid;\n  grid-template-columns: repeat(4, 1fr);\n  gap: 12px;\n}\n.kpi {\n  background: #fff;\n  border: 1px solid var(--border);\n  border-radius: 18px;\n  padding: 16px 18px;\n  position: relative;\n  overflow: hidden;\n  transition:\n    transform 200ms cubic-bezier(0.34, 1.56, 0.64, 1),\n    box-shadow 200ms;\n}\n.kpi:hover {\n  transform: translateY(-2px);\n  box-shadow: 0 12px 24px rgba(100, 69, 54, 0.10);\n}\n.kpi__icon {\n  width: 36px;\n  height: 36px;\n  border-radius: 12px;\n  display: grid;\n  place-items: center;\n  margin-bottom: 10px;\n}\n.kpi__label {\n  font-size: 11px;\n  font-weight: 700;\n  letter-spacing: 0.04em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n}\n.kpi__val {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 26px;\n  color: var(--brand-teal);\n  letter-spacing: -0.02em;\n  line-height: 1;\n  margin: 4px 0 6px;\n}\n.kpi__delta {\n  font-size: 11px;\n  color: var(--fg-muted);\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  flex-wrap: wrap;\n  overflow: hidden;\n  min-width: 0;\n  line-height: 1.3;\n}\n.kpi__delta strong {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  display: inline-block;\n  padding: 2px 7px;\n  border-radius: 999px;\n  font-size: 10px;\n  flex-shrink: 0;\n  white-space: nowrap;\n}\n.kpi__delta-sub {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  min-width: 0;\n  flex: 1;\n}\n.kpi__delta--up strong {\n  background: var(--green-50);\n  color: var(--green-600);\n}\n.kpi__delta--neutral strong {\n  background: var(--coffee-50);\n  color: var(--coffee-600);\n}\n.kpi__delta--warn strong {\n  background: var(--pink-50);\n  color: var(--pink-700);\n}\n\n/* ------ Two-column section grid (wraps ActiveJobs+Quotes, Activity+Money) - */\n.grid {\n  display: grid;\n  grid-template-columns: 1.45fr 1fr;\n  gap: 18px;\n}\n\n/* ------ Panel shell (ActiveJobs, QuotesAwaiting, Activity) ---------------- */\n.panel {\n  background: #fff;\n  border: 1px solid var(--border);\n  border-radius: 20px;\n  overflow: hidden;\n  display: flex;\n  flex-direction: column;\n}\n.panel__head {\n  padding: 14px 18px;\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  border-bottom: 1px solid var(--border);\n  flex-wrap: nowrap;\n  min-width: 0;\n}\n.panel__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  margin: 0;\n  white-space: nowrap;\n  flex-shrink: 0;\n}\n.panel__count {\n  background: var(--green-50);\n  color: var(--green-600);\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 10px;\n  padding: 2px 8px;\n  border-radius: 999px;\n  white-space: nowrap;\n  flex-shrink: 0;\n}\n.panel__action {\n  margin-left: auto;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--brand-green);\n  text-decoration: none;\n  cursor: pointer;\n  white-space: nowrap;\n  flex-shrink: 0;\n}\n\n/* ------ Active jobs feed (ActiveJobs row) -------------------------------- */\n.job {\n  display: grid;\n  grid-template-columns: 44px 1fr auto;\n  gap: 14px;\n  padding: 14px 20px;\n  border-bottom: 1px dashed rgba(100, 69, 54, 0.10);\n  align-items: center;\n}\n.job:last-child {\n  border-bottom: none;\n}\n.job__icon {\n  width: 44px;\n  height: 44px;\n  border-radius: 14px;\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  flex-shrink: 0;\n}\n.job__title-row {\n  display: flex;\n  align-items: center;\n  flex-wrap: nowrap;\n  min-width: 0;\n  gap: 8px;\n}\n.job__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  margin: 0;\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  min-width: 0;\n  flex: 1;\n}\n/* .job__chip + .chip--* are defined but the live ActiveJobs row does NOT\n * render a chip element (status is shown via job__amount-sub / progress only).\n * Kept here as the source declares them. */\n.job__chip {\n  flex-shrink: 0;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 10px;\n  padding: 2px 8px;\n  border-radius: 999px;\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n}\n.chip--green { background: var(--green-50); color: var(--green-600); }\n.chip--pink  { background: var(--pink-50);  color: var(--pink-700); }\n.chip--coffee{ background: var(--coffee-50); color: var(--coffee-600); }\n.chip--teal  { background: var(--teal-50);  color: var(--teal-600); }\n.chip--warn  { background: #fff6e5;          color: #a57a1b; }\n\n.job__meta {\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin-top: 3px;\n  display: flex;\n  align-items: center;\n  gap: 8px;\n}\n.job__meta-dot {\n  width: 3px;\n  height: 3px;\n  background: var(--fg-subtle);\n  border-radius: 999px;\n  flex-shrink: 0;\n}\n.job__progress {\n  margin-top: 8px;\n  height: 5px;\n  background: var(--mint-200);\n  border-radius: 999px;\n  overflow: hidden;\n  max-width: 220px;\n}\n.job__progress-bar {\n  height: 100%;\n  border-radius: 999px;\n}\n.job__amount {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 16px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  text-align: right;\n  white-space: nowrap;\n}\n.job__amount-sub {\n  font-size: 10px;\n  color: var(--fg-muted);\n  font-weight: 600;\n  margin-top: 2px;\n  text-align: right;\n}\n\n/* ------ Quotes awaiting card (QuotesAwaiting) ---------------------------- */\n.quote-item {\n  padding: 14px 20px;\n  border-bottom: 1px dashed rgba(100, 69, 54, 0.10);\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.quote-item:last-child {\n  border-bottom: none;\n}\n.quote-item__row {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n  min-width: 0;\n}\n.quote-item__client {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  color: var(--brand-teal);\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  min-width: 0;\n  flex: 1;\n}\n.quote-item__amt {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  flex-shrink: 0;\n  white-space: nowrap;\n}\n.quote-item__sub {\n  font-size: 12px;\n  color: var(--fg-muted);\n  overflow: hidden;\n  min-width: 0;\n}\n.quote-item__sub > span:first-child {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  min-width: 0;\n  max-width: 50%;\n}\n.quote-item__cta {\n  display: flex;\n  gap: 8px;\n  margin-top: 4px;\n}\n.qbtn {\n  border: 1px solid var(--border);\n  background: #fff;\n  white-space: nowrap;\n  color: var(--brand-teal);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 11px;\n  padding: 5px 11px;\n  border-radius: 999px;\n  cursor: pointer;\n  display: inline-flex;\n  align-items: center;\n  gap: 5px;\n}\n.qbtn--nudge {\n  background: var(--brand-pink);\n  color: #fff;\n  border-color: var(--brand-pink);\n}\n.qbtn--view {\n  background: transparent;\n}\n\n/* ------ Money owed (Outstanding) ----------------------------------------- */\n.money {\n  background: linear-gradient(135deg, var(--coffee-50) 0%, #fff 100%);\n  border: 1px solid var(--border);\n  border-radius: 20px;\n  padding: 18px 20px;\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n}\n.money__head {\n  display: flex;\n  align-items: baseline;\n  justify-content: space-between;\n  gap: 10px;\n}\n.money__label {\n  font-size: 11px;\n  font-weight: 700;\n  letter-spacing: 0.05em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n}\n.money__amt {\n  /* hosts the animated <Ticker value={owed}/> as a text node */\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 32px;\n  color: var(--brand-teal);\n  letter-spacing: -0.02em;\n  line-height: 1;\n}\n.money__bar {\n  height: 8px;\n  background: rgba(100, 69, 54, 0.10);\n  border-radius: 999px;\n  overflow: hidden;\n  display: flex;\n}\n.money__bar-seg {\n  height: 100%;\n}\n.money__legend {\n  display: flex;\n  justify-content: space-between;\n  gap: 8px;\n  flex-wrap: wrap;\n  font-size: 11px;\n  color: var(--fg-muted);\n}\n.money__legend-item {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n}\n.money__legend-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 999px;\n}\n\n/* ------ Activity feed (Activity) ----------------------------------------- */\n.activity-item {\n  display: flex;\n  gap: 12px;\n  padding: 12px 20px;\n  align-items: flex-start;\n  border-bottom: 1px dashed rgba(100, 69, 54, 0.10);\n}\n.activity-item:last-child {\n  border-bottom: none;\n}\n.activity-item__icon {\n  width: 32px;\n  height: 32px;\n  border-radius: 10px;\n  display: grid;\n  place-items: center;\n  flex-shrink: 0;\n}\n.activity-item__text {\n  font-size: 13px;\n  color: var(--brand-teal);\n  flex: 1;\n  line-height: 1.4;\n}\n.activity-item__text strong {\n  font-family: var(--font-heading);\n  font-weight: 800;\n}\n.activity-item__time {\n  font-size: 11px;\n  color: var(--fg-subtle);\n  margin-top: 2px;\n}\n\n/* =========================================================================\n * DEAD CODE — `Hero` export (.hero*, .btn*). No importer renders <Hero/>\n * (replaced by the .assistant-cta banner in DashboardPage). Documented so a\n * rebuild can either restore or drop it deliberately. The `.hero` box itself\n * IS reused by the loading skeleton (see dashboard-page.css).\n * ======================================================================== */\n.hero__copy { position: relative; z-index: 2; min-width: 0; }\n.hero__art { position: relative; height: 200px; z-index: 1; }\n.hero__art-blob {\n  position: absolute;\n  inset: -10px -10px -10px 10px;\n  background: radial-gradient(ellipse at 60% 55%, rgba(81, 152, 67, 0.18) 0%, transparent 70%);\n  border-radius: 50%;\n  filter: blur(2px);\n}\n.hero__confetti { position: absolute; width: 10px; height: 10px; border-radius: 3px; opacity: 0.85; }\n.hero__confetti--1 { top: 12px; left: 18px; background: var(--brand-pink); transform: rotate(20deg); }\n.hero__confetti--2 { top: 38px; right: 32px; background: var(--brand-green); transform: rotate(-15deg); width: 8px; height: 8px; }\n.hero__confetti--3 { bottom: 24px; left: 0px; background: var(--coffee-300, #c9b7ab); transform: rotate(35deg); width: 6px; height: 6px; }\n.hero__stats { display: flex; gap: 10px; margin-top: 16px; flex-wrap: wrap; }\n.hero__stat {\n  display: inline-flex; align-items: center; gap: 8px; padding: 6px 12px;\n  background: rgba(255, 255, 255, 0.8); border: 1px solid var(--border);\n  border-radius: 999px; font-family: var(--font-heading); font-weight: 700;\n  font-size: 11px; color: var(--brand-teal); white-space: nowrap;\n}\n.hero__stat strong { color: var(--brand-green); font-weight: 800; }\n.hero__stat--pink strong { color: var(--brand-pink); }\n.hero__pill-dot {\n  width: 6px; height: 6px; background: var(--brand-green); border-radius: 999px;\n  box-shadow: 0 0 0 0 rgba(81, 152, 67, 0.6); animation: ppulse 2s infinite;\n}\n@keyframes ppulse {\n  0%   { box-shadow: 0 0 0 0 rgba(81, 152, 67, 0.55); }\n  70%  { box-shadow: 0 0 0 10px rgba(81, 152, 67, 0); }\n  100% { box-shadow: 0 0 0 0 rgba(81, 152, 67, 0); }\n}\n.hero__title {\n  font-family: var(--font-heading); font-weight: 800; font-size: 32px;\n  line-height: 1.1; letter-spacing: -0.02em; color: var(--brand-teal); margin: 0 0 8px;\n}\n.hero__title em { font-style: normal; color: var(--brand-pink); }\n.hero__sub { font-size: 14px; color: var(--fg-muted); margin: 0 0 18px; max-width: 540px; }\n.hero__cta-row { display: flex; gap: 12px; flex-wrap: wrap; }\n.btn {\n  border: none; border-radius: 14px; font-family: var(--font-heading);\n  font-weight: 800; font-size: 14px; padding: 12px 20px; cursor: pointer;\n  display: inline-flex; align-items: center; gap: 8px;\n  transition: transform 180ms cubic-bezier(0.34, 1.56, 0.64, 1), filter 180ms;\n}\n.btn:hover { transform: translateY(-1px); filter: brightness(1.05); }\n.btn--quote {\n  background: var(--brand-green); color: #fff; padding: 14px 22px; font-size: 15px;\n  box-shadow: 0 10px 24px rgba(81, 152, 67, 0.4);\n}\n.btn--quote .btn__lightning {\n  background: rgba(255, 255, 255, 0.22); border-radius: 8px; padding: 4px;\n  display: grid; place-items: center;\n}\n\n/* =========================================================================\n * Responsive (own breakpoints). At <=640px the grid stacks 1-col and kpis\n * drop to 2-up; everything else inherits the panel/card box widths.\n * ======================================================================== */\n@media (max-width: 640px) {\n  .grid {\n    grid-template-columns: 1fr;\n    gap: 14px;\n  }\n  .kpis {\n    grid-template-columns: repeat(2, 1fr);\n    gap: 10px;\n  }\n  .kpi {\n    padding: 14px 14px 13px;\n  }\n  .kpi__val {\n    font-size: 22px;\n  }\n  .kpi__label {\n    white-space: normal;\n  }\n  .kpi__delta-sub {\n    white-space: normal;\n    overflow: visible;\n    text-overflow: clip;\n  }\n}\n\n/* Reduced motion: handled globally by the tokens @media block (clamps the kpi\n * hover-lift transition + ppulse to 0.01ms). No section-local override. */\n", "cssName": "dash-sections.css", "source": "/**\n * Server-rendered Dashboard sections, ported verbatim from\n * Paperwork Monster Dashboard.html. Only the Ticker (animated number)\n * pieces are islands — everything else is static SSR.\n */\nimport { I, ICN, type IconName } from \"../lib/dash-icons.tsx\";\nimport Ticker from \"../islands/Ticker.tsx\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\n\n/** Plural resolver for SSR call sites (frontend `tn` is langSignal-reactive\n *  only, so it can't honor an explicit lang). */\nconst plural = (lang: Lang, key: string, n: number) =>\n  tFor(lang, `${key}.${n === 1 ? \"one\" : \"other\"}`, { n });\n\n/* ---------- Hero ---------- */\n\ninterface HeroProps {\n  thisMonthBilled: number;\n  pendingQuotes: number;\n  outstandingOverdue?: number;\n  lang?: Lang;\n}\n\nexport function Hero(\n  { thisMonthBilled, pendingQuotes, outstandingOverdue = 0, lang = \"en\" }:\n    HeroProps,\n) {\n  const fresh = thisMonthBilled === 0;\n  // CTA priority: overdue invoices > pending quotes > assistant fallback. The\n  // hero CTA used to always be \"My assistant\" which duplicated the sidebar;\n  // pick the most actionable next step instead.\n  const cta = outstandingOverdue > 0\n    ? {\n      label: plural(lang, \"dashHero.cta.nudgeOverdue\", outstandingOverdue),\n      href: \"/invoices\",\n    }\n    : pendingQuotes > 0\n    ? {\n      // Navigation intent only — this is an <a href>, not a bulk send. \"Send\n      // the N quotes\" read as a one-tap blast; \"Review\" matches what it does.\n      label: plural(lang, \"dashHero.cta.reviewPending\", pendingQuotes),\n      href: \"/quotes\",\n    }\n    : { label: tFor(lang, \"dashHero.cta.assistant\"), href: \"/assistant\" };\n  return (\n    <section class=\"hero\">\n      <div class=\"hero__copy\">\n        <h1 class=\"hero__title\">\n          {fresh ? <>{tFor(lang, \"dashHero.title.getQuotesOut\")}</> : (\n            <>\n              {tFor(lang, \"dashHero.title.billedLead\")}{\" \"}\n              <em>\n                $<Ticker value={thisMonthBilled} />\n              </em>{\" \"}\n              {tFor(lang, \"dashHero.title.billedTrail\")}<br />\n              {tFor(lang, \"dashHero.title.getQuotesOut\")}\n            </>\n          )}\n        </h1>\n        <p class=\"hero__sub\">\n          {pendingQuotes > 0\n            ? plural(lang, \"dashHero.sub.pending\", pendingQuotes)\n            : tFor(lang, \"dashHero.sub.empty\")}\n        </p>\n        {pendingQuotes > 0 && (\n          <div class=\"hero__stats\">\n            <span class=\"hero__stat hero__stat--pink\">\n              <strong>\n                {plural(lang, \"dashHero.stat.quotes\", pendingQuotes)}\n              </strong>{\" \"}\n              {tFor(lang, \"dashHero.stat.awaiting\")}\n            </span>\n          </div>\n        )}\n        <div class=\"hero__cta-row\" style=\"margin-top:18px\">\n          <a\n            class=\"btn btn--quote\"\n            href={cta.href}\n            style=\"text-decoration:none\"\n          >\n            <span class=\"btn__lightning\">\n              <I d={ICN.crown} size={14} />\n            </span>\n            {cta.label}\n          </a>\n          {\n            /* Roadmap p.10: the assistant must stay reachable above the fold\n              on phones. That entry now lives in the dominant .assistant-cta\n              banner above the hero, so the old secondary ghost link here is\n              redundant and was removed. */\n          }\n        </div>\n      </div>\n      <div class=\"hero__art\">\n        <span class=\"hero__confetti hero__confetti--1\" />\n        <span class=\"hero__confetti hero__confetti--2\" />\n        <span class=\"hero__confetti hero__confetti--3\" />\n        <div class=\"hero__art-blob\" />\n        <img src=\"/logo-monster.png\" alt=\"\" class=\"hero__monster\" />\n      </div>\n    </section>\n  );\n}\n\n/* ---------- KPIs ---------- */\n\ninterface KpisProps {\n  activeJobs: number;\n  outstanding: number;\n  outstandingCount: number;\n  outstandingOverdue: number;\n  pendingQuotes: number;\n  pendingTotal: number;\n  avgJob: number;\n  lang?: Lang;\n}\n\nexport function Kpis(props: KpisProps) {\n  const lang = props.lang ?? \"en\";\n  // \"Avg. paid job\" — only counts paid invoices. Reads as \"—\" with an\n  // honest sub when there's no paid history yet (was \"$0 / last 30 days\",\n  // which looked broken).\n  const avgJobVal = props.avgJob > 0\n    ? `$${props.avgJob.toLocaleString()}`\n    : tFor(lang, \"kpis.avgJob.none\");\n  const avgJobSub = props.avgJob > 0 ? tFor(lang, \"kpis.avgJob.trailingYear\") : \"\";\n  const items: Array<\n    {\n      icon: IconName;\n      ic_bg: string;\n      ic_fg: string;\n      label: string;\n      val: string;\n      sub: string;\n      delta: { kind: \"up\" | \"warn\" | \"neutral\"; txt: string } | null;\n    }\n  > = [\n    {\n      icon: \"hardhat\",\n      ic_bg: \"var(--green-50)\",\n      ic_fg: \"var(--green-600)\",\n      label: tFor(lang, \"kpis.activeJobs.label\"),\n      val: String(props.activeJobs),\n      sub: tFor(lang, \"kpis.activeJobs.sub\"),\n      delta: null,\n    },\n    {\n      icon: \"invoice\",\n      ic_bg: \"var(--pink-50)\",\n      ic_fg: \"var(--pink-700)\",\n      label: tFor(lang, \"kpis.outstanding.label\"),\n      val: `$${props.outstanding.toLocaleString()}`,\n      sub: plural(lang, \"kpis.outstanding.invoices\", props.outstandingCount),\n      delta: props.outstandingOverdue > 0\n        ? {\n          kind: \"warn\",\n          txt: tFor(lang, \"kpis.outstanding.overdue\", {\n            n: props.outstandingOverdue,\n          }),\n        }\n        : null,\n    },\n    {\n      icon: \"quote\",\n      ic_bg: \"var(--coffee-50)\",\n      ic_fg: \"var(--coffee-600)\",\n      label: tFor(lang, \"kpis.quotesPending.label\"),\n      val: String(props.pendingQuotes),\n      sub: props.pendingTotal > 0\n        ? tFor(lang, \"kpis.quotesPending.inFlight\", {\n          amt: (props.pendingTotal / 1000).toFixed(1),\n        })\n        : \"—\",\n      delta: null,\n    },\n    {\n      icon: \"trend\",\n      ic_bg: \"var(--teal-50)\",\n      ic_fg: \"var(--teal-600)\",\n      label: tFor(lang, \"kpis.avgJob.label\"),\n      val: avgJobVal,\n      sub: avgJobSub,\n      delta: null,\n    },\n  ];\n  return (\n    <section class=\"kpis\">\n      {items.map((k) => (\n        <div class=\"kpi\" key={k.label}>\n          <div\n            class=\"kpi__icon\"\n            style={`background:${k.ic_bg};color:${k.ic_fg}`}\n          >\n            <I d={ICN[k.icon]} size={18} />\n          </div>\n          <div class=\"kpi__label\">{k.label}</div>\n          <div class=\"kpi__val\">{k.val}</div>\n          {k.delta\n            ? (\n              <div class={`kpi__delta kpi__delta--${k.delta.kind}`}>\n                <strong>{k.delta.txt}</strong>\n                <span class=\"kpi__delta-sub\">{k.sub}</span>\n              </div>\n            )\n            : (\n              <div class=\"kpi__delta kpi__delta--neutral\">\n                <span class=\"kpi__delta-sub\" style=\"margin-left:0\">\n                  {k.sub}\n                </span>\n              </div>\n            )}\n        </div>\n      ))}\n    </section>\n  );\n}\n\n/* ---------- Active Jobs ---------- */\n\nexport interface JobRow {\n  client: string;\n  task: string;\n  amount: string;\n  paid: string;\n  pct: number;\n  due: string;\n  icon: IconName;\n  color: string;\n  status: { kind: \"green\" | \"warn\" | \"teal\"; txt: string };\n}\n\nexport function ActiveJobs(\n  { jobs, total, lang = \"en\" }: {\n    jobs: JobRow[];\n    total?: number;\n    lang?: Lang;\n  },\n) {\n  // `total` is the full active-job count (matches the KPI); `jobs` is only the\n  // top slice rendered below. Show the total so the header count and the\n  // \"Active jobs\" KPI never contradict each other.\n  const count = total ?? jobs.length;\n  return (\n    <div class=\"panel\">\n      <div class=\"panel__head\">\n        <div style=\"display:flex;align-items:center;gap:10px;min-width:0;overflow:hidden\">\n          <span\n            class=\"hero__pill-dot\"\n            style=\"position:static;width:8px;height:8px\"\n          />\n          <h3 class=\"panel__title\">{tFor(lang, \"kpis.activeJobs.label\")}</h3>\n          <span class=\"panel__count\">\n            {tFor(lang, \"activeJobs.count\", { n: count })}\n          </span>\n        </div>\n        <a class=\"panel__action\" href=\"#\">{tFor(lang, \"dashPanel.seeAll\")}</a>\n      </div>\n      {jobs.length === 0\n        ? (\n          <div style=\"padding:18px 4px;display:flex;flex-direction:column;gap:10px;align-items:flex-start\">\n            <p style=\"font-size:13.5px;color:var(--fg-muted, #6b7560);margin:0;line-height:1.5\">\n              {tFor(lang, \"activeJobs.empty.text\")}\n            </p>\n            <a\n              class=\"panel__action\"\n              href=\"/quotes\"\n              style=\"background:var(--pink-50);color:var(--pink-700);padding:6px 12px;border-radius:8px;text-decoration:none\"\n            >\n              {tFor(lang, \"activeJobs.empty.action\")}\n            </a>\n          </div>\n        )\n        : (\n          jobs.map((j, i) => (\n            <div class=\"job\" key={i}>\n              <div class=\"job__icon\" style={`background:${j.color}`}>\n                <I d={ICN[j.icon]} size={18} />\n              </div>\n              <div style=\"min-width:0\">\n                <div class=\"job__title-row\">\n                  <h4 class=\"job__title\">{j.client}</h4>\n                </div>\n                <div class=\"job__meta\">\n                  {j.task} <span class=\"job__meta-dot\" />{\" \"}\n                  {tFor(lang, \"activeJobs.due\", { due: j.due })}\n                </div>\n                <div class=\"job__progress\">\n                  <div\n                    class=\"job__progress-bar\"\n                    style={`width:${j.pct}%;background:${j.color}`}\n                  />\n                </div>\n              </div>\n              <div>\n                <div class=\"job__amount\">{j.amount}</div>\n                <div class=\"job__amount-sub\">{j.paid}</div>\n              </div>\n            </div>\n          ))\n        )}\n    </div>\n  );\n}\n\n/* ---------- Quotes Awaiting ---------- */\n\nexport interface QuoteRow {\n  client: string;\n  desc: string;\n  amt: string;\n  sent: string;\n  hot?: boolean;\n  cold?: boolean;\n}\n\nexport function QuotesAwaiting(\n  { quotes, lang = \"en\" }: { quotes: QuoteRow[]; lang?: Lang },\n) {\n  const total = quotes.reduce(\n    (s, q) => s + Number(q.amt.replace(/[^0-9.]/g, \"\")),\n    0,\n  );\n  const empty = quotes.length === 0;\n  return (\n    <div class=\"panel\">\n      <div class=\"panel__head\">\n        <h3 class=\"panel__title\">{tFor(lang, \"quotesAwaiting.title\")}</h3>\n        {!empty && (\n          <span\n            class=\"panel__count\"\n            style=\"background:var(--coffee-50);color:var(--coffee-600)\"\n          >\n            {tFor(lang, \"quotesAwaiting.count\", {\n              n: quotes.length,\n              total: total.toLocaleString(),\n            })}\n          </span>\n        )}\n        <a class=\"panel__action\" href=\"/quotes\" style=\"margin-left:auto\">\n          {tFor(lang, \"dashPanel.seeAll\")}\n        </a>\n      </div>\n      {empty && (\n        <div style=\"font-size:13px;color:var(--fg-muted);padding:8px 0 14px\">\n          {tFor(lang, \"quotesAwaiting.empty\")}\n        </div>\n      )}\n      {quotes.map((q, i) => (\n        <div class=\"quote-item\" key={i}>\n          <div class=\"quote-item__row\">\n            <span class=\"quote-item__client\">{q.client}</span>\n            <span class=\"quote-item__amt\">{q.amt}</span>\n          </div>\n          <div\n            class=\"quote-item__sub\"\n            style=\"display:flex;align-items:center;gap:8px\"\n          >\n            <span>{q.desc}</span>\n            <span class=\"job__meta-dot\" />\n            <span\n              style={`color:${\n                q.hot\n                  ? \"var(--brand-green)\"\n                  : q.cold\n                  ? \"var(--pink-700)\"\n                  : \"var(--fg-muted)\"\n              };font-weight:${q.hot || q.cold ? 700 : 500}`}\n            >\n              {q.sent} {q.hot ? \"🔥\" : \"\"}{\" \"}\n              {q.cold ? tFor(lang, \"quotesAwaiting.cold\") : \"\"}\n            </span>\n          </div>\n          <div class=\"quote-item__cta\">\n            <button type=\"button\" class=\"qbtn qbtn--nudge\">\n              <I d={ICN.send} size={11} /> {tFor(lang, \"quotesAwaiting.nudgeByText\")}\n            </button>\n            <button type=\"button\" class=\"qbtn qbtn--view\">\n              <I d={ICN.eye} size={11} /> {tFor(lang, \"quotesAwaiting.viewQuote\")}\n            </button>\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n}\n\n/* ---------- Outstanding ---------- */\n\nexport interface OutstandingRow {\n  client: string;\n  meta: string;\n  metaColor: string;\n  metaWeight: number;\n  amount: string;\n}\n\nexport function Outstanding(\n  { owed, current, mid, overdue, items, lang = \"en\" }: {\n    owed: number;\n    current: number;\n    mid: number;\n    overdue: number;\n    items: OutstandingRow[];\n    lang?: Lang;\n  },\n) {\n  const realTotal = current + mid + overdue;\n  const allZero = realTotal === 0 && owed === 0;\n  const total = realTotal || 1;\n  return (\n    <div class=\"money\">\n      <div class=\"money__head\">\n        <div>\n          <div class=\"money__label\">{tFor(lang, \"outstanding.label\")}</div>\n          <div class=\"money__amt\">\n            $<Ticker value={owed} />\n          </div>\n        </div>\n        {!allZero && (\n          <button\n            type=\"button\"\n            class=\"qbtn qbtn--nudge\"\n            style=\"padding:8px 14px;font-size:12px\"\n          >\n            <I d={ICN.send} size={12} /> {tFor(lang, \"outstanding.nudgeAll\")}\n          </button>\n        )}\n      </div>\n      {allZero\n        ? (\n          <div style=\"font-size:13px;color:var(--fg-muted);padding:6px 0 14px\">\n            {items.length === 0\n              ? tFor(lang, \"outstanding.empty.noInvoices\")\n              : tFor(lang, \"outstanding.empty.allPaid\")}\n          </div>\n        )\n        : (\n          <>\n            <div class=\"money__bar\">\n              <div\n                class=\"money__bar-seg\"\n                style={`width:${\n                  (current / total) * 100\n                }%;background:var(--brand-green)`}\n              />\n              <div\n                class=\"money__bar-seg\"\n                style={`width:${\n                  (mid / total) * 100\n                }%;background:var(--coffee-400)`}\n              />\n              <div\n                class=\"money__bar-seg\"\n                style={`width:${\n                  (overdue / total) * 100\n                }%;background:var(--brand-pink)`}\n              />\n            </div>\n            <div class=\"money__legend\">\n              <div class=\"money__legend-item\">\n                <span\n                  class=\"money__legend-dot\"\n                  style=\"background:var(--brand-green)\"\n                />{\" \"}\n                {tFor(lang, \"outstanding.legend.current\", {\n                  amt: current.toLocaleString(),\n                })}\n              </div>\n              <div class=\"money__legend-item\">\n                <span\n                  class=\"money__legend-dot\"\n                  style=\"background:var(--coffee-400)\"\n                />{\" \"}\n                {tFor(lang, \"outstanding.legend.mid\", {\n                  amt: mid.toLocaleString(),\n                })}\n              </div>\n              <div class=\"money__legend-item\">\n                <span\n                  class=\"money__legend-dot\"\n                  style=\"background:var(--brand-pink)\"\n                />{\" \"}\n                {tFor(lang, \"outstanding.legend.overdue\", {\n                  amt: overdue.toLocaleString(),\n                })}\n              </div>\n            </div>\n          </>\n        )}\n\n      {items.length > 0 && (\n        <div style=\"border-top:1px dashed rgba(100,69,54,0.15);padding-top:14px;display:flex;flex-direction:column;gap:10px\">\n          {items.map((item, i) => (\n            <div\n              key={i}\n              style=\"display:flex;justify-content:space-between;align-items:center\"\n            >\n              <div>\n                <div style=\"font-family:var(--font-heading);font-weight:800;font-size:13px;color:var(--brand-teal)\">\n                  {item.client}\n                </div>\n                <div\n                  style={`font-size:11px;color:${item.metaColor};font-weight:${item.metaWeight};margin-top:1px`}\n                >\n                  {item.meta}\n                </div>\n              </div>\n              <div style=\"font-family:var(--font-heading);font-weight:800;font-size:14px;color:var(--brand-teal)\">\n                {item.amount}\n              </div>\n            </div>\n          ))}\n        </div>\n      )}\n    </div>\n  );\n}\n\n/* ---------- Activity ---------- */\n\nexport interface ActivityEntry {\n  icon: IconName;\n  bg: string;\n  fg: string;\n  html: string;\n  time: string;\n}\n\nexport function Activity(\n  { items, lang = \"en\" }: { items: ActivityEntry[]; lang?: Lang },\n) {\n  const empty = items.length === 0;\n  return (\n    <div class=\"panel\" id=\"activity\">\n      <div class=\"panel__head\">\n        <h3 class=\"panel__title\">{tFor(lang, \"activity.title\")}</h3>\n        <span style=\"font-size:11px;color:var(--fg-muted);margin-left:4px\">\n          {empty\n            ? tFor(lang, \"activity.emptySub\")\n            : tFor(lang, \"activity.busySub\")}\n        </span>\n        <a class=\"panel__action\" href=\"/activity\" style=\"margin-left:auto\">\n          {tFor(lang, \"activity.fullLog\")}\n        </a>\n      </div>\n      {items.map((a, i) => (\n        <div class=\"activity-item\" key={i}>\n          <div\n            class=\"activity-item__icon\"\n            style={`background:${a.bg};color:${a.fg}`}\n          >\n            <I d={ICN[a.icon]} size={14} />\n          </div>\n          <div class=\"activity-item__text\">\n            <span\n              // Trusted server-derived activity markup (no user input).\n              // deno-lint-ignore react-no-danger\n              dangerouslySetInnerHTML={{ __html: a.html }}\n            />\n            <div class=\"activity-item__time\">{a.time}</div>\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n}\n", "sourceName": "DashSections.tsx", "checklist": "# Capture checklist — DashSections\n\n**Theme:** light only.\n**Auth:** log in with dev master OTP `000000` (any seeded phone).\n**Route:** `http://localhost:5280/dashboard`. These blocks are the lower half of\nthe dashboard body (below the `.assistant-cta` banner + SetupChecklist), composed\nby `DashboardPage`. No standalone route — capture in place on `/dashboard`.\n\n## Element(s) to crop\n- **Kpis** — the `.kpis` row (4 tiles).\n- **ActiveJobs** — the left `.panel` in the first `.grid`.\n- **QuotesAwaiting** — the right `.panel` in the first `.grid`.\n- **Activity** — the left `.panel` (`#activity`) in the second `.grid`.\n- **Outstanding** — the right `.money` card in the second `.grid` (gradient).\n- Also one full-width crop of each `.grid` row to verify the `1.45fr 1fr` split.\n\n## Viewports (this module's own @media — dash-sections.css)\n- **1280px** (desktop: KPIs 4-up, grids `1.45fr 1fr`).\n- **720px** (between cutovers — still desktop layout per this CSS; confirm).\n- **640px** (the module's only breakpoint: `.grid`→1-col, `.kpis`→2-up,\n  `.kpi__val`→22px, labels/delta-sub wrap). Shoot at **641** and **640** to catch\n  the cutover.\n- **390px** (mobile, iPhone-ish: stacked single column).\n\n## Transient / data states to drive\n1. **populated** — a seeded account so every block is full: 4 KPI tiles with a\n   pink \"1 overdue\" delta on Outstanding; ActiveJobs with progress bars + a\n   \"🔥\"/\"· cold\" quote; the money card with its 3-segment bar + legend +\n   per-invoice list.\n2. **empty** — a brand-new account (no jobs/quotes/invoices/activity):\n   - ActiveJobs → \"No jobs in flight yet…\" + \"See pipeline →\".\n   - QuotesAwaiting → \"No quotes out yet…\" (count pill suppressed).\n   - Outstanding → \"$0\" + \"No invoices yet…\" (bar/legend/Nudge-all hidden).\n   - Activity → \"Nothing yet…\" sub, no rows.\n   - Kpis → Avg paid job shows \"No paid jobs yet\"; Outstanding clamped to $0.\n3. **money count-up** — film the Outstanding `.money__amt` ticking 0 → owed over\n   ~1.4s on first paint (it's the embedded `<Ticker/>`). Re-shoot with\n   `prefers-reduced-motion: reduce` and NOTE: the count-up still runs (no guard).\n4. **kpi hover** — hover a `.kpi`; capture the `translateY(-2px)` lift + shadow.\n5. **inert buttons** — hover/focus a `.qbtn--nudge` / \"Nudge all\"; document that\n   clicking does nothing (no handler) — they look actionable but are no-ops.\n\n## Motion to film\n- KPI hover-lift (200ms bounce + shadow).\n- `.hero__pill-dot` `ppulse` ring on the ActiveJobs header dot (2s loop).\n- Outstanding money count-up (rAF, ~1400ms) — and the no-guard reduced-motion\n  case.\n- Re-shoot the hover/pulse with `prefers-reduced-motion: reduce` (global clamp to\n  0.01ms).\n\n## Notes\n- These are SSR blocks — no client interactivity to drive beyond hover. All\n  \"actions\" are `<a href>` navigation; the nudge/view/Nudge-all buttons are\n  inert.\n- NO fabricated screenshots — capture only what the live `/dashboard` renders.\n"}, {"name": "dashboard-page", "component": "DashboardPage", "spec": "# DashboardPage\n\nThe top-level **data island** for `/dashboard`. The SSR route renders an empty\nshell; this island hydrates, fans out the dashboard fetches in parallel, maps\nthe DTOs to presentational rows, and assembles the page body (assistant banner,\nsetup checklist, KPIs, jobs/quotes grid, activity/money grid).\n\n## 1. Classification & behavior\n- **Bucket:** `island` (file lives in `islands/DashboardPage.tsx`). 554 lines.\n- **Interaction tier:** `island` — client-only state via `useState`; renders\n  nothing interactive that mutates the server. All real interactivity is `<a\n  href>` navigation + the (currently inert) nudge buttons inside DashSections.\n- **Client state owned (`useState<State>`):**\n  `{ loading, error, stats, jobs, quoteCards, pendingInvoices, customers,\n  notifications }`. Seeded on mount from `readCached()` — if a `stats` snapshot\n  already exists in the shared dash-cache, the initial state is\n  `{ ...INITIAL, loading:false, stats:c.stats }` so the hero + KPIs paint\n  instantly (warm start); otherwise `INITIAL` (loading skeleton).\n- **Reactive language:** reads `langSignal.value` at render → re-renders live\n  when Settings flips language. The legacy `lang?` prop is ignored.\n- **Data source per region (all client-fetched on mount, `useEffect([])`):**\n  - KPIs / Money-owed / Outstanding totals ← `DashboardStats`\n    (`dashboardClient.stats()` → `/analytics/dashboard`). The whole-account\n    rollup — see DATA-SHAPE HAZARD below.\n  - Active jobs rows ← `Job[]` (`dashboardClient.jobs()` → `/jobs`), sliced to 5.\n  - Quotes-awaiting rows ← `QuoteCard[]`\n    (`quotesClient.list(\"sent\")` → `/quotes?status=sent`), de-duped + sorted by\n    `sentAt` desc, sliced to 4.\n  - Outstanding rows ← `Invoice[]`\n    (`dashboardClient.invoices(\"pending\")`), sorted by `dueDate` asc, sliced 5;\n    client names joined from `customers`.\n  - Activity rows ← `Notification[]` (`dashboardClient.notifications(10)`),\n    consecutive-duplicate-title-collapsed (`· N×`), sliced to 4.\n  - `customers` ← `dashboardClient.customers()` (only for the\n    `customerId→name` map used by Outstanding).\n  - Each fetch is individually `.catch()`-guarded to a safe empty\n    default, so a single endpoint failing degrades that region, not the page.\n  - It ALSO calls `refreshDash()` (fire-and-forget) to keep the shared cache\n    warm for sibling islands (sidebar badges).\n- **Honest-empty:** every section degrades honestly (see DashSections spec):\n  ActiveJobs shows an empty CTA, QuotesAwaiting a one-liner, Outstanding \"all\n  paid / no invoices\", Activity an empty sub. `pickKpis` clamps Outstanding to\n  exactly `$0` when `invoices.pending === 0` (avoids a nonsense `$0.09 · 0\n  invoices` from stray aging-bucket cents).\n- **Server mutations:** NONE in this island. (The nudge buttons in DashSections\n  are inert `<button type=button>` with no handler.)\n- **Liveness:** request-response, fetch-once-on-mount. No polling, no\n  websocket, no `setInterval`. The Ticker count-up is the only timed animation.\n- **`location.reload()`:** NONE. ✅ (Anti-pattern check passed.)\n- **Data-shape hazards:**\n  - `pickKpis` reads deep into `stats.invoices.agingBuckets.*` and\n    `stats.revenue.sparkline12mo` with `?? 0` fallbacks — tolerant of a partial\n    `DashboardStats`. Money fields are **cents**, divided by 100 here.\n  - `Array.isArray(...)` guards around `jobs`/`quoteCards`/`pendingInvoices`\n    before slicing — defends a non-array payload.\n  - `clientFromSummary` parses a client name out of a quote summary\n    (`/—\\s*(.+)$/`) only when `q.customerName` is null — fragile string parse.\n  - Activity HTML is server-derived + `escapeHtml`'d, then injected via\n    `dangerouslySetInnerHTML` in the `Activity` section (the `· N×` dupe badge\n    is appended as trusted markup).\n\n## 2. Anatomy\n```\nloading → <DashboardSkeleton/>      (ShimmerStyle + SkelBlocks shaped like the real layout)\nerror   → <div class=\"dashpage-error\">{loadError}: {msg}</div>   ← UNSTYLED\nelse:\n<>\n  <div class=\"assistant-cta\">                       ← green→teal gradient banner\n    <div class=\"assistant-cta__body\">\n      eyebrow / title / sub\n      <div class=\"assistant-cta__actions\">\n        <a.assistant-cta__btn href=\"/assistant\">    crown + \"My Assistant\" + arrow\n        <a.assistant-cta__call href=\"tel:+18667678399\">  phone + \"Call support\" + (866) 767-8399\n    <div class=\"assistant-cta__art\">                ← 3 confetti + blob + monster img\n  <SetupChecklist/>                                 ← page-local island\n  <Kpis .../>                                       ← DashSections\n  <div class=\"grid\"><ActiveJobs/><QuotesAwaiting/></div>\n  <div class=\"grid\"><Activity/><Outstanding/></div>\n</>\n```\n- **Support line:** `SUPPORT_PHONE = \"+18667678399\"` /\n  `SUPPORT_PHONE_DISPLAY = \"(866) 767-8399\"` — the toll-free 866 that forwards\n  to the owner cell via a Twilio Studio Flow. Public, safe to ship.\n- **Icon dependency:** `lib/dash-icons.tsx` (`I` renderer + `ICN` map). Uses\n  `crown`, `arrow`, `phone` directly; section icons (`hardhat/wrench/truck/\n  paint/ruler/send/check/contract/card/invoice/msg/sparkle/quote/trend/eye`)\n  flow through DashSections. (Already copied into the shared dash-sidebar spec\n  at `shared-components/dash-sidebar/js/dash-icons.tsx`.)\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `lang` | `\"en\"\\|\"es\"` | `undefined` (IGNORED) | — | superseded by `langSignal` |\n\nThe island takes **no functional props** — the route mounts it bare\n(`<DashboardPage />`). The `lang` prop is a vestigial SSR seed; the live value\nis `langSignal.value`.\n\n## 4. States → cases\nBecause all display data is client-fetched, isolate models it via `_mocks`\n(the client responses) rather than props. The `lang` reactive value is set via\n`_signals`.\n\n| state | meaning | case |\n|---|---|---|\n| populated | seeded account; all sections filled | `cases/populated/populated.json` |\n| loading | cold cache, fetches in flight → DashboardSkeleton | `cases/loading/loading.json` |\n| warm-start | dash-cache hit → hero+KPIs paint, no skeleton | `cases/warm-start/warm-start.json` |\n| empty | brand-new account; honest-empty everywhere | `cases/empty/empty.json` |\n| error | all fetches reject → `.dashpage-error` | `cases/error/error.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"a.assistant-cta__btn\" && e.type === \"click\")` →\n  nav `/assistant`.\n- `ev.expect(e => e.source === \"a.assistant-cta__call\" && e.type === \"click\")` →\n  `tel:+18667678399` (dials; on desktop opens the OS handler).\n- All other clicks bubble to DashSections children (See-all links, nudge\n  buttons) — modeled in `dash-sections` Events.\n- No emitted custom events; no form submit.\n\n## 6. Motion (extracted)\n- `.assistant-cta:hover` → `translateY(-2px)` + shadow grow over **220ms**\n  `cubic-bezier(.34,1.56,.64,1)`; the inner `.assistant-cta__btn` nudges\n  `translateX(2px)` + `brightness(1.05)` (180ms bounce).\n- KPI tiles + everything else: see DashSections / Ticker specs.\n- **Skeleton shimmer:** `pmShimmer` 1.4s linear infinite (from shared\n  Skeletons) while `loading`.\n- **Jank finding:** the assistant-cta hover animates `transform` (compositor-\n  friendly) + `box-shadow` (paint). The shadow change forces a repaint of the\n  large banner each frame on hover — acceptable (hover, not scroll), but on\n  low-end devices prefer animating an opacity-layered pseudo-shadow.\n- **Reduced motion:** no island-local guard; the global tokens `@media` clamps\n  the hover transition to `0.01ms`.\n\n## 7. Responsive (own @media widths)\n- The island has no `@media` of its own — it relies on `dashboard.css`:\n  `.assistant-cta` restacks at **720px**; `.grid`→1-col and `.kpis`→2-up at\n  **640px**; shell flex→body-scroll at **640/641px**. See css/dashboard-page.css.\n\n## 8. A11y\n- The assistant banner is two real `<a>` links — keyboard-operable, native\n  semantics. Icons inside are decorative SVG.\n- Skeleton blocks are decorative (no `aria-busy` on the region — gap; consider\n  `aria-busy=\"true\"` on `.content` while loading).\n- The error branch renders an unstyled, un-roled `<div>` — not announced to SR\n  (gap; add `role=\"alert\"`).\n- Section-level a11y lives in DashSections / SetupChecklist.\n\n## 9. Used on\n**`/dashboard` only.** Single import: `routes/dashboard/index.tsx`. Not shared.\n", "fixture": "{\n  \"category\": \"dashboard\",\n  \"folder\": \"dashboard-page\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"], \"signal\": true }\n  }\n}\n", "cases": [{"name": "populated.json", "content": "{\n  \"_name\": \"Populated — seeded account, all sections filled\",\n  \"_signals\": { \"lang\": \"en\" },\n  \"_mocks\": {\n    \"GET /analytics/dashboard\": {\n      \"customers\": 12,\n      \"quotes\": { \"total\": 15, \"draft\": 2, \"sent\": 9, \"accepted\": 4 },\n      \"contracts\": { \"total\": 6, \"draft\": 2, \"signed\": 4 },\n      \"invoices\": {\n        \"total\": 10, \"pending\": 4, \"paid\": 5, \"overdue\": 1,\n        \"agingBuckets\": { \"current\": 334000, \"aging1_14d\": 192000, \"overdue15_30d\": 116000, \"overdue30plus\": 0 }\n      },\n      \"quotedValueCents\": 1280000,\n      \"awaitingResponse\": 9,\n      \"revenue\": {\n        \"ytdCents\": 8420000, \"lastMonthCents\": 1500000, \"monthOverMonthPct\": 12,\n        \"sparkline12mo\": [600000,720000,810000,540000,930000,1010000,1200000,880000,1340000,1180000,1500000,1842000]\n      },\n      \"payments\": { \"receivedYtdCents\": 8420000, \"methodMixCents\": { \"check\": 5000000, \"zelle\": 3420000 }, \"topPayors\": [] }\n    },\n    \"GET /jobs\": [\n      { \"id\": \"job_1\", \"customer\": { \"id\": \"c1\", \"name\": \"Maple Grove Apartments\" }, \"quote\": { \"id\": \"q1\", \"summary\": \"Re-roof building C\", \"estimatedTotalCents\": 480000 }, \"contract\": { \"id\": \"ct1\", \"status\": \"signed\" }, \"totalCents\": 480000, \"paidCents\": 240000, \"pctPaid\": 50, \"nextDueDate\": \"2026-06-18\", \"status\": \"on_track\", \"statusLabel\": \"On track\" },\n      { \"id\": \"job_2\", \"customer\": { \"id\": \"c2\", \"name\": \"Sarah Chen\" }, \"quote\": { \"id\": \"q2\", \"summary\": \"Bathroom remodel\", \"estimatedTotalCents\": 820000 }, \"contract\": { \"id\": \"ct2\", \"status\": \"signed\" }, \"totalCents\": 820000, \"paidCents\": 300000, \"pctPaid\": 36, \"nextDueDate\": \"2026-06-24\", \"status\": \"on_track\", \"statusLabel\": \"Crew onsite\" },\n      { \"id\": \"job_3\", \"customer\": { \"id\": \"c3\", \"name\": \"Marshall & Sons\" }, \"quote\": { \"id\": \"q3\", \"summary\": \"Driveway re-pour\", \"estimatedTotalCents\": 295000 }, \"contract\": { \"id\": \"ct3\", \"status\": \"signed\" }, \"totalCents\": 295000, \"paidCents\": 0, \"pctPaid\": 18, \"nextDueDate\": \"2026-06-26\", \"status\": \"awaiting_permit\", \"statusLabel\": \"Awaiting permit\" }\n    ],\n    \"GET /quotes?status=sent\": [\n      { \"id\": \"q10\", \"userId\": \"u1\", \"customerName\": \"Tom & Linda K.\", \"summary\": \"Garage epoxy floor\", \"estimatedTotal\": 340000, \"status\": \"sent\", \"sentAt\": \"2026-06-16T15:00:00Z\", \"stage\": \"opened\", \"daysIn\": 2, \"opens\": 2, \"lastOpenAt\": \"2026-06-17T19:00:00Z\", \"sentDays\": 2, \"decidedDays\": null, \"createdAt\": \"2026-06-16T15:00:00Z\", \"updatedAt\": \"2026-06-17T19:00:00Z\" },\n      { \"id\": \"q11\", \"userId\": \"u1\", \"customerName\": \"Greenleaf HOA\", \"summary\": \"Common-area repaint\", \"estimatedTotal\": 580000, \"status\": \"sent\", \"sentAt\": \"2026-06-15T15:00:00Z\", \"stage\": \"sent\", \"daysIn\": 3, \"opens\": 1, \"lastOpenAt\": \"2026-06-16T10:00:00Z\", \"sentDays\": 3, \"decidedDays\": null, \"createdAt\": \"2026-06-15T15:00:00Z\", \"updatedAt\": \"2026-06-16T10:00:00Z\" },\n      { \"id\": \"q12\", \"userId\": \"u1\", \"customerName\": \"Bayside Properties\", \"summary\": \"Gutter cleaning, 4-unit\", \"estimatedTotal\": 168000, \"status\": \"sent\", \"sentAt\": \"2026-06-14T15:00:00Z\", \"stage\": \"cooling\", \"daysIn\": 4, \"opens\": 0, \"lastOpenAt\": null, \"sentDays\": 4, \"decidedDays\": null, \"createdAt\": \"2026-06-14T15:00:00Z\", \"updatedAt\": \"2026-06-14T15:00:00Z\" }\n    ],\n    \"GET /invoices?status=pending\": [\n      { \"id\": \"INV-204\", \"userId\": \"u1\", \"customerId\": \"c4\", \"amount\": 116000, \"dueDate\": \"2026-06-10\", \"status\": \"pending\", \"createdAt\": \"2026-05-10T00:00:00Z\", \"updatedAt\": \"2026-05-10T00:00:00Z\" },\n      { \"id\": \"INV-208\", \"userId\": \"u1\", \"customerId\": \"c2\", \"amount\": 192000, \"dueDate\": \"2026-06-21\", \"status\": \"pending\", \"createdAt\": \"2026-06-01T00:00:00Z\", \"updatedAt\": \"2026-06-01T00:00:00Z\" },\n      { \"id\": \"INV-210\", \"userId\": \"u1\", \"customerId\": \"c1\", \"amount\": 334000, \"dueDate\": \"2026-06-30\", \"status\": \"pending\", \"createdAt\": \"2026-06-05T00:00:00Z\", \"updatedAt\": \"2026-06-05T00:00:00Z\" }\n    ],\n    \"GET /customers\": [\n      { \"id\": \"c1\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n      { \"id\": \"c2\", \"userId\": \"u1\", \"name\": \"Sarah Chen\" },\n      { \"id\": \"c4\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\" }\n    ],\n    \"GET /notifications?limit=10\": [\n      { \"id\": \"n1\", \"userId\": \"u1\", \"type\": \"quote_accepted\", \"title\": \"Tom K. opened your quote — 2nd time\", \"read\": false, \"createdAt\": \"2026-06-18T15:58:00Z\" },\n      { \"id\": \"n2\", \"userId\": \"u1\", \"type\": \"quote_sent\", \"title\": \"Texted quote to Greenleaf HOA\", \"read\": false, \"createdAt\": \"2026-06-18T15:00:00Z\" },\n      { \"id\": \"n3\", \"userId\": \"u1\", \"type\": \"invoice_paid\", \"title\": \"Maple Grove paid invoice deposit\", \"read\": true, \"createdAt\": \"2026-06-18T13:00:00Z\" },\n      { \"id\": \"n4\", \"userId\": \"u1\", \"type\": \"contract_signed\", \"title\": \"Sarah Chen e-signed her contract\", \"read\": true, \"createdAt\": \"2026-06-17T16:00:00Z\" }\n    ]\n  }\n}\n"}], "css": null, "cssName": null, "source": "/**\n * Top-level data island for /dashboard. The SSR route renders only the\n * page shell; this island fans out the dashboard fetches in parallel and\n * assembles the hero, KPIs, jobs row, quotes row, activity feed, and\n * outstanding list.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport {\n  type Customer,\n  dashboardClient,\n  type DashboardStats,\n  type Invoice,\n  type Job,\n  type Notification,\n} from \"../clients/dashboard.ts\";\nimport { type QuoteCard, quotesClient } from \"../clients/quotes.ts\";\nimport {\n  ActiveJobs,\n  Activity,\n  type ActivityEntry,\n  type JobRow,\n  Kpis,\n  Outstanding,\n  type OutstandingRow,\n  type QuoteRow,\n  QuotesAwaiting,\n} from \"../components/DashSections.tsx\";\nimport { I, ICN, type IconName } from \"../lib/dash-icons.tsx\";\nimport { fmtMoney } from \"../lib/format.ts\";\nimport { readCached, refreshDash } from \"../lib/dash-cache.ts\";\nimport { ShimmerStyle, SkelBlock } from \"../components/Skeletons.tsx\";\nimport SetupChecklist from \"./SetupChecklist.tsx\";\nimport { langSignal, type Lang, tFor } from \"../lib/i18n.ts\";\n\n// Toll-free support line (TWILIO_SUPPORT_NUMBER). Public number, safe to ship.\n// A call here hits the Twilio Studio Flow that texts a heads-up, plays a brief\n// hold, then forwards to the support cell.\nconst SUPPORT_PHONE = \"+18667678399\";\nconst SUPPORT_PHONE_DISPLAY = \"(866) 767-8399\";\n\nfunction shortMonth(lang: Lang, monthIdx: number): string {\n  return tFor(lang, `common.monthShort.${monthIdx}`);\n}\nfunction shortDay(lang: Lang, dayIdx: number): string {\n  return tFor(lang, `common.dayShort.${dayIdx}`);\n}\n\nfunction fmtDue(iso: string | null, now: Date, lang: Lang): string {\n  if (!iso) return tFor(lang, \"dashboardPage.due.none\");\n  const due = new Date(iso + \"T00:00:00\");\n  if (Number.isNaN(due.getTime())) return iso;\n  const startOfToday = new Date(now);\n  startOfToday.setHours(0, 0, 0, 0);\n  const diffDays = Math.round(\n    (due.getTime() - startOfToday.getTime()) / 86_400_000,\n  );\n  if (diffDays === 0) return tFor(lang, \"dashboardPage.due.today\");\n  if (diffDays === 1) return tFor(lang, \"dashboardPage.due.tomorrow\");\n  if (diffDays === -1) return tFor(lang, \"dashboardPage.due.yesterday\");\n  if (diffDays > 1 && diffDays < 7) return shortDay(lang, due.getDay());\n  return `${shortMonth(lang, due.getMonth())} ${due.getDate()}`;\n}\n\nfunction fmtRel(iso: string, now: Date, lang: Lang): string {\n  const t = new Date(iso).getTime();\n  if (!Number.isFinite(t)) return \"\";\n  const m = Math.max(1, Math.floor((now.getTime() - t) / 60_000));\n  if (m < 60) return tFor(lang, \"dashboardPage.rel.minAgo\", { n: m });\n  const h = Math.floor(m / 60);\n  if (h < 24) return tFor(lang, \"dashboardPage.rel.hrAgo\", { n: h });\n  const d = Math.floor(h / 24);\n  if (d === 1) return tFor(lang, \"dashboardPage.rel.yesterday\");\n  if (d < 7) return tFor(lang, \"dashboardPage.rel.daysAgo\", { n: d });\n  return `${shortMonth(lang, new Date(iso).getMonth())} ${\n    new Date(iso).getDate()\n  }`;\n}\n\nconst JOB_ROTATION: { icon: IconName; color: string }[] = [\n  { icon: \"hardhat\", color: \"var(--brand-green)\" },\n  { icon: \"wrench\", color: \"var(--brand-pink)\" },\n  { icon: \"truck\", color: \"var(--coffee-500)\" },\n  { icon: \"paint\", color: \"var(--brand-pink)\" },\n  { icon: \"ruler\", color: \"var(--green-600)\" },\n];\n\nfunction jobToRow(j: Job, idx: number, now: Date, lang: Lang): JobRow {\n  const rot = JOB_ROTATION[idx % JOB_ROTATION.length];\n  const total = j.totalCents / 100;\n  const paid = j.paidCents / 100;\n  const paidLabel = paid > 0\n    ? tFor(lang, \"dashboardPage.job.amountPaid\", {\n      amount: `$${Math.round(paid).toLocaleString()}`,\n    })\n    : (j.contract?.status === \"signed\"\n      ? tFor(lang, \"dashboardPage.job.deposit\")\n      : tFor(lang, \"dashboardPage.job.quoted\"));\n  const statusKind: JobRow[\"status\"][\"kind\"] = j.status === \"overdue\"\n    ? \"warn\"\n    : j.status === \"awaiting\" || j.status === \"awaiting_permit\"\n    ? \"warn\"\n    : j.status === \"complete\"\n    ? \"teal\"\n    : \"green\";\n  return {\n    client: j.customer.name,\n    task: j.quote.summary,\n    amount: `$${Math.round(total).toLocaleString()}`,\n    paid: paidLabel,\n    pct: j.pctPaid,\n    due: fmtDue(j.nextDueDate, now, lang),\n    icon: rot.icon,\n    color: rot.color,\n    status: { kind: statusKind, txt: j.statusLabel },\n  };\n}\n\nfunction clientFromSummary(summary: string | null | undefined): string {\n  if (!summary) return \"—\";\n  const m = summary.match(/—\\s*(.+)$/);\n  return m ? m[1].trim() : \"—\";\n}\n\nfunction quoteToRow(q: QuoteCard, now: Date, lang: Lang): QuoteRow {\n  const sentLabel = q.sentAt\n    ? `${tFor(lang, \"dashboardPage.quote.sent\", {\n      rel: fmtRel(q.sentAt, now, lang),\n    })}${\n      q.opens > 0\n        ? ` · ${tFor(lang, \"dashboardPage.quote.viewed\")}${\n          q.opens > 1 ? ` ${q.opens}×` : \"\"\n        }`\n        : \"\"\n    }`\n    : tFor(lang, \"dashboardPage.quote.drafted\");\n  return {\n    client: q.customerName ?? clientFromSummary(q.summary),\n    desc: q.summary ?? \"\",\n    amt: fmtMoney(q.estimatedTotal ?? 0),\n    sent: sentLabel,\n    hot: q.stage === \"opened\" && q.opens >= 2,\n    cold: q.stage === \"stale\" || q.stage === \"cooling\",\n  };\n}\n\nfunction shortInv(id: string, idx: number): string {\n  const tail = id.replace(/[^0-9]/g, \"\").slice(-3);\n  return tail || String(200 + idx);\n}\n\nfunction invoiceToRow(\n  inv: Invoice,\n  customerNames: Map<string, string>,\n  now: Date,\n  idx: number,\n  lang: Lang,\n): OutstandingRow {\n  const due = new Date(inv.dueDate + \"T00:00:00\");\n  const today = new Date(now);\n  today.setHours(0, 0, 0, 0);\n  const days = Math.round((due.getTime() - today.getTime()) / 86_400_000);\n  const num = shortInv(inv.id, idx);\n  let meta: string;\n  let metaColor: string;\n  let metaWeight = 600;\n  if (days < 0) {\n    const overdueDays = -days;\n    meta = tFor(\n      lang,\n      `dashboardPage.invoice.overdue.${overdueDays === 1 ? \"one\" : \"other\"}`,\n      { n: overdueDays, num },\n    );\n    metaColor = \"var(--pink-700)\";\n    metaWeight = 700;\n  } else if (days <= 5) {\n    meta = tFor(\n      lang,\n      `dashboardPage.invoice.dueIn.${days === 1 ? \"one\" : \"other\"}`,\n      { n: days, num },\n    );\n    metaColor = \"var(--coffee-500)\";\n  } else {\n    meta = tFor(lang, \"dashboardPage.invoice.dueOn\", {\n      date: `${shortMonth(lang, due.getMonth())} ${due.getDate()}`,\n      num,\n    });\n    metaColor = \"var(--green-600)\";\n  }\n  const name = (inv.customerId && customerNames.get(inv.customerId)) || \"—\";\n  return {\n    client: name,\n    meta,\n    metaColor,\n    metaWeight,\n    amount: fmtMoney(inv.amount ?? 0),\n  };\n}\n\nconst NOTIF_ICON: Record<\n  Notification[\"type\"],\n  { icon: IconName; bg: string; fg: string }\n> = {\n  quote_sent: { icon: \"send\", bg: \"var(--pink-50)\", fg: \"var(--pink-700)\" },\n  quote_accepted: {\n    icon: \"check\",\n    bg: \"var(--green-50)\",\n    fg: \"var(--green-600)\",\n  },\n  contract_signed: {\n    icon: \"contract\",\n    bg: \"var(--teal-50)\",\n    fg: \"var(--teal-600)\",\n  },\n  invoice_claimed: {\n    icon: \"card\",\n    bg: \"var(--pink-50)\",\n    fg: \"var(--pink-700)\",\n  },\n  invoice_paid: { icon: \"card\", bg: \"var(--green-50)\", fg: \"var(--green-600)\" },\n  invoice_overdue: {\n    icon: \"invoice\",\n    bg: \"var(--pink-50)\",\n    fg: \"var(--pink-700)\",\n  },\n  customer_replied: {\n    icon: \"msg\",\n    bg: \"var(--coffee-50)\",\n    fg: \"var(--coffee-600)\",\n  },\n  generic: { icon: \"sparkle\", bg: \"var(--teal-50)\", fg: \"var(--teal-600)\" },\n};\n\nfunction notifToActivity(n: Notification, now: Date, lang: Lang): ActivityEntry {\n  const skin = NOTIF_ICON[n.type] ?? NOTIF_ICON.generic;\n  return {\n    icon: skin.icon,\n    bg: skin.bg,\n    fg: skin.fg,\n    html: escapeHtml(n.title),\n    time: fmtRel(n.createdAt, now, lang),\n  };\n}\n\nfunction escapeHtml(s: string): string {\n  return s.replace(\n    /[&<>\"']/g,\n    (c) =>\n      c === \"&\"\n        ? \"&amp;\"\n        : c === \"<\"\n        ? \"&lt;\"\n        : c === \">\"\n        ? \"&gt;\"\n        : c === '\"'\n        ? \"&quot;\"\n        : \"&#39;\",\n  );\n}\n\nfunction pickKpis(stats: DashboardStats | undefined) {\n  const buckets = stats?.invoices.agingBuckets;\n  const current = (buckets?.current ?? 0) / 100;\n  const mid = (buckets?.aging1_14d ?? 0) / 100;\n  const overdue =\n    ((buckets?.overdue15_30d ?? 0) + (buckets?.overdue30plus ?? 0)) / 100;\n  const owed = current + mid + overdue;\n  const sparkline = stats?.revenue.sparkline12mo ?? [];\n  const thisMonthBilled =\n    (sparkline.length > 0 ? sparkline[sparkline.length - 1] : 0) / 100;\n\n  const paidCount = stats?.invoices.paid ?? 0;\n  const ytd = (stats?.revenue.ytdCents ?? 0) / 100;\n  const avgJob = paidCount > 0 ? Math.round(ytd / paidCount) : 0;\n\n  // When there are no pending invoices, clamp Outstanding to exactly $0 — the\n  // aging buckets can carry stray sub-dollar cents that otherwise surface as a\n  // nonsense \"$0.09 · 0 invoices\".\n  const pending = stats?.invoices.pending ?? 0;\n\n  return {\n    thisMonthBilled,\n    activeJobs: 0,\n    outstanding: pending === 0 ? 0 : owed,\n    outstandingCount: pending,\n    outstandingOverdue: stats?.invoices.overdue ?? 0,\n    pendingQuotes: stats?.quotes.sent ?? 0,\n    pendingTotal: (stats?.quotedValueCents ?? 0) / 100,\n    avgJob,\n    owed,\n    current,\n    mid,\n    overdue,\n  };\n}\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  stats: DashboardStats | undefined;\n  jobs: Job[];\n  quoteCards: QuoteCard[];\n  pendingInvoices: Invoice[];\n  customers: Customer[];\n  notifications: Notification[];\n}\n\nconst INITIAL: State = {\n  loading: true,\n  error: null,\n  stats: undefined,\n  jobs: [],\n  quoteCards: [],\n  pendingInvoices: [],\n  customers: [],\n  notifications: [],\n};\n\nfunction DashboardSkeleton() {\n  return (\n    <>\n      <ShimmerStyle />\n      <section class=\"hero\" style=\"min-height:200px\">\n        <div class=\"hero__copy\">\n          <SkelBlock h={36} w=\"80%\" />\n          <SkelBlock h={16} w=\"60%\" mt={14} />\n          <SkelBlock h={16} w=\"45%\" mt={8} />\n          <SkelBlock h={44} w=\"220px\" r={12} mt={22} />\n        </div>\n      </section>\n      <section class=\"kpis\">\n        {[0, 1, 2, 3].map((i) => (\n          <div class=\"kpi\" key={i}>\n            <SkelBlock h={36} w=\"36px\" r={10} />\n            <SkelBlock h={11} w=\"60%\" mt={14} />\n            <SkelBlock h={28} w=\"55%\" mt={10} />\n            <SkelBlock h={12} w=\"70%\" mt={10} />\n          </div>\n        ))}\n      </section>\n      <div class=\"grid\">\n        <div class=\"panel\">\n          <SkelBlock h={20} w=\"40%\" />\n          <SkelBlock h={64} mt={18} />\n          <SkelBlock h={64} mt={12} />\n        </div>\n        <div class=\"panel\">\n          <SkelBlock h={20} w=\"40%\" />\n          <SkelBlock h={48} mt={18} />\n          <SkelBlock h={48} mt={12} />\n          <SkelBlock h={48} mt={12} />\n        </div>\n      </div>\n      <div class=\"grid\">\n        <div class=\"panel\">\n          <SkelBlock h={20} w=\"40%\" />\n          <SkelBlock h={40} mt={18} />\n          <SkelBlock h={40} mt={12} />\n        </div>\n        <div class=\"money\">\n          <SkelBlock h={28} w=\"50%\" />\n          <SkelBlock h={10} mt={14} />\n          <SkelBlock h={36} mt={18} />\n          <SkelBlock h={36} mt={12} />\n        </div>\n      </div>\n    </>\n  );\n}\n\nexport default function DashboardPage(_props: { lang?: Lang } = {}) {\n  // Reactive UI language — seeded from the profile cache, flipped live by\n  // Settings. (The old `lang` prop is superseded by this shared signal.)\n  const lang = langSignal.value;\n  // Warm-start from the shared dash cache: if we already have a stats snapshot\n  // (e.g. from the sidebar on a prior page), render the hero + KPIs instantly\n  // and treat the mount fetch as a background refresh instead of flashing the\n  // full skeleton every time the user navigates back here.\n  const [s, setS] = useState<State>(() => {\n    const c = readCached();\n    return c?.stats ? { ...INITIAL, loading: false, stats: c.stats } : INITIAL;\n  });\n\n  useEffect(() => {\n    let alive = true;\n    // Keep the shared cache fresh for other islands (sidebar badges, etc).\n    refreshDash().catch(() => {});\n    Promise.all([\n      dashboardClient.stats().catch(() =>\n        undefined as DashboardStats | undefined\n      ),\n      dashboardClient.jobs().catch(() => [] as Job[]),\n      quotesClient.list(\"sent\").catch(() => [] as QuoteCard[]),\n      dashboardClient.invoices(\"pending\").catch(() => [] as Invoice[]),\n      dashboardClient.customers().catch(() => [] as Customer[]),\n      dashboardClient.notifications(10).catch(() => [] as Notification[]),\n    ]).then(\n      (\n        [stats, jobs, quoteCards, pendingInvoices, customers, notifications],\n      ) => {\n        if (!alive) return;\n        setS({\n          loading: false,\n          error: null,\n          stats,\n          jobs,\n          quoteCards,\n          pendingInvoices,\n          customers,\n          notifications,\n        });\n      },\n    ).catch((err: Error) => {\n      if (!alive) return;\n      setS({ ...INITIAL, loading: false, error: err.message });\n    });\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  if (s.loading) return <DashboardSkeleton />;\n  if (s.error) {\n    return (\n      <div class=\"dashpage-error\">\n        {tFor(lang, \"dashboardPage.loadError\")}: {s.error}\n      </div>\n    );\n  }\n\n  const { stats, jobs, quoteCards, pendingInvoices, customers, notifications } =\n    s;\n  const now = new Date();\n\n  const kpis = { ...pickKpis(stats), activeJobs: jobs.length };\n  const customerNames = new Map(customers.map((c) => [c.id, c.name] as const));\n\n  const safeJobs = Array.isArray(jobs) ? jobs : [];\n  const jobRows: JobRow[] = safeJobs.slice(0, 5).map((j, i) =>\n    jobToRow(j, i, now, lang)\n  );\n\n  const seenQuoteIds = new Set<string>();\n  const safeQuoteCards = Array.isArray(quoteCards) ? quoteCards : [];\n  const safePendingInvoices = Array.isArray(pendingInvoices)\n    ? pendingInvoices\n    : [];\n  const quoteRows: QuoteRow[] = safeQuoteCards\n    .slice()\n    .sort((a, b) => (b.sentAt ?? \"\").localeCompare(a.sentAt ?? \"\"))\n    .filter((q) => {\n      if (!q.id || seenQuoteIds.has(q.id)) return false;\n      seenQuoteIds.add(q.id);\n      return true;\n    })\n    .slice(0, 4)\n    .map((q) => quoteToRow(q, now, lang));\n\n  const outstandingRows: OutstandingRow[] = safePendingInvoices\n    .slice()\n    .sort((a, b) => (a.dueDate ?? \"\").localeCompare(b.dueDate ?? \"\"))\n    .slice(0, 5)\n    .map((inv, i) => invoiceToRow(inv, customerNames, now, i, lang));\n\n  const grouped: Notification[] = [];\n  for (const n of notifications) {\n    const prev = grouped[grouped.length - 1];\n    if (prev && prev.title === n.title) {\n      grouped[grouped.length - 1] = { ...prev, title: prev.title };\n      (grouped[grouped.length - 1] as Notification & { _dupes?: number })\n        ._dupes = ((prev as Notification & { _dupes?: number })._dupes ?? 1) +\n          1;\n    } else {\n      grouped.push(n);\n    }\n  }\n  const activityRows: ActivityEntry[] = grouped.slice(0, 4).map((n) => {\n    const dupes = (n as Notification & { _dupes?: number })._dupes ?? 1;\n    const entry = notifToActivity(n, now, lang);\n    return dupes > 1\n      ? {\n        ...entry,\n        html:\n          `${entry.html} <span style=\"color:var(--fg-muted);font-weight:600\">· ${dupes}×</span>`,\n      }\n      : entry;\n  });\n\n  return (\n    <>\n      <div class=\"assistant-cta\">\n        <div class=\"assistant-cta__body\">\n          <span class=\"assistant-cta__eyebrow\">\n            {tFor(lang, \"dashHero.cta.assistant\")}\n          </span>\n          <span class=\"assistant-cta__title\">\n            {tFor(lang, \"dashAssistantCta.title\")}\n          </span>\n          <span class=\"assistant-cta__sub\">\n            {tFor(lang, \"dashAssistantCta.sub\")}\n          </span>\n          <div class=\"assistant-cta__actions\">\n            <a class=\"assistant-cta__btn\" href=\"/assistant\">\n              <span class=\"assistant-cta__crown\">\n                <I d={ICN.crown} size={16} />\n              </span>\n              {tFor(lang, \"dashHero.cta.assistant\")}\n              <I d={ICN.arrow} size={16} />\n            </a>\n            <a class=\"assistant-cta__call\" href={`tel:${SUPPORT_PHONE}`}>\n              <I d={ICN.phone} size={15} />\n              <span class=\"assistant-cta__call-label\">\n                {tFor(lang, \"dashAssistantCta.callSupport\")}\n              </span>\n              <span class=\"assistant-cta__call-num\">{SUPPORT_PHONE_DISPLAY}</span>\n            </a>\n          </div>\n        </div>\n        <div class=\"assistant-cta__art\">\n          <span class=\"assistant-cta__confetti assistant-cta__confetti--1\" />\n          <span class=\"assistant-cta__confetti assistant-cta__confetti--2\" />\n          <span class=\"assistant-cta__confetti assistant-cta__confetti--3\" />\n          <span class=\"assistant-cta__blob\" />\n          <img src=\"/logo-monster.png\" alt=\"\" class=\"assistant-cta__monster\" />\n        </div>\n      </div>\n      <SetupChecklist />\n      <Kpis\n        activeJobs={kpis.activeJobs}\n        outstanding={kpis.outstanding}\n        outstandingCount={kpis.outstandingCount}\n        outstandingOverdue={kpis.outstandingOverdue}\n        pendingQuotes={kpis.pendingQuotes}\n        pendingTotal={kpis.pendingTotal}\n        avgJob={kpis.avgJob}\n        lang={lang}\n      />\n      <div class=\"grid\">\n        <ActiveJobs jobs={jobRows} total={kpis.activeJobs} lang={lang} />\n        <QuotesAwaiting quotes={quoteRows} lang={lang} />\n      </div>\n      <div class=\"grid\">\n        <Activity items={activityRows} lang={lang} />\n        <Outstanding\n          owed={kpis.owed}\n          current={kpis.current}\n          mid={kpis.mid}\n          overdue={kpis.overdue}\n          items={outstandingRows}\n          lang={lang}\n        />\n      </div>\n    </>\n  );\n}\n", "sourceName": "DashboardPage.tsx", "checklist": "# Capture checklist — DashboardPage\n\n- **Route:** `/dashboard` (auth-gated — log in via dev master OTP `000000`).\n- **Viewports** (dashboard.css; shell cuts at **640/641px**): 390 (mobile,\n  KPIs 2-up, sidebar→drawer), 980 (tablet), 1280 (desktop, `--container-product`).\n- **Crop targets:** the `.content` region (the island); plus each section block\n  (KPIs row, Active jobs, Quotes awaiting, Outstanding, Activity).\n- **States to drive:**\n  - `loading` — skeletons on first mount (throttle network / use the loading case).\n  - `ready` — populated from `lib/dash-seed.ts` (`SEED_*`).\n  - `empty` — brand-new account (every block's honest-empty branch + SetupChecklist visible).\n  - `error` — fetch failure (`.dashpage-error` — note: **unstyled**, renders as plain text).\n- **Theme:** light only. **No fabricated screenshots** — capture against a seeded dev backend.\n"}, {"name": "setup-checklist", "component": "SetupChecklist", "spec": "# SetupChecklist\n\nThe post-onboarding \"finish setting up\" card on the dashboard. It lists the\nhigh-leverage profile items onboarding doesn't collect (logo, payment method,\ninsurance, address pieces, etc.), shows a completeness bar, and **self-hides**\nonce every item is done or the user dismisses it. Rendered between the\n`.assistant-cta` banner and the KPIs by `DashboardPage`.\n\n> **PROJECT FACT — no nav gate.** The product deliberately does NOT block\n> navigation on profile completeness. Profile is collected *here*, on the\n> dashboard, via this dismissible checklist — there is no `loadProfileGate`\n> redirect, no onboarding wall. A rebuild MUST NOT reintroduce a redirect that\n> forces the user to finish their profile before they can use the app. This card\n> is the entire mechanism: opt-in nudges, fully skippable.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (file lives in `islands/SetupChecklist.tsx`).\n- **Interaction tier:** `island` — client-only state + one client fetch on\n  mount. No server mutation here; the items are `<a href=\"/settings\">` links that\n  navigate to Settings where the actual editing happens.\n- **Client state owned (`useState`):**\n  - `snap: ProfileSnapshot | null` — the profile fetched on mount; `null` until\n    it lands (and the whole island renders `null` while `null`).\n  - `dismissed: boolean` — set `true` by the \"Hide\" button; **session-only, NOT\n    persisted** (it returns on the next page load). FLAG: a refresh re-shows the\n    card even after the user hid it — consider persisting to `localStorage`.\n- **Data source:** `clients/profile.ts` → `profileClient.get()` →\n  **`GET /profile`** (returns a `ProfileSnapshot`). Fetched in a mounted\n  `useEffect([])` with an `alive` guard; `.catch(() => {})` swallows errors\n  (a failed fetch leaves `snap` `null` → the island renders nothing, fails\n  closed). It does NOT use `lib/dash-cache.ts` — it owns this read so the\n  dashboard route stays SSR-clean and only pays for `/profile` when the checklist\n  might actually show.\n- **Honest-empty / self-hide (three render-null gates):**\n  1. `!snap || dismissed` → `null` (still loading, or user hid it).\n  2. After computing `items`, `remaining = items.filter(!done)`; if\n     `remaining.length === 0` → `null` (everything done — card disappears).\n  3. So the card only appears when the profile loaded AND at least one item is\n     incomplete AND the user hasn't dismissed it.\n- **Item completeness logic (per `ProfileSnapshot` field):**\n  - `name` ← `snap.user.name?.trim()`\n  - `biz` ← `snap.identity?.businessName?.trim()`\n  - `email` ← `snap.user.email?.trim()`\n  - `logo` ← `snap.identity?.logoFileId`\n  - `address` ← `snap.address?.postal?.trim() || snap.address?.street?.trim()`\n  - `payment` ← any `snap.identity?.acceptedPaymentMethods[*].enabled === true`\n  - `insurance` ← `snap.insurance?.provider?.trim()`\n  - `pct = round((done / total) * 100)` drives the bar + `aria-valuenow`.\n- **Reactive language:** reads `langSignal.value` at render → re-localizes live\n  on a Settings language flip. The `/profile` fetch is only for completeness\n  data, not language.\n- **Server mutations / PRG / flash:** none here. (Editing happens on `/settings`.)\n- **`location.reload()`:** none. ✅\n- **Liveness:** request-response, fetch-once-on-mount. No polling, no\n  `setInterval`, no websocket. It does NOT re-fetch when the user returns from\n  Settings — a freshly-completed item won't tick off until a full reload (the\n  page is a fresh island mount on navigation, so in practice it re-fetches per\n  visit).\n- **Data-shape hazards:** all field reads are optional-chained + `?? null`\n  guarded, tolerant of a partial `ProfileSnapshot` (`identity`/`address`/\n  `insurance` can each be `null`). `acceptedPaymentMethods` is iterated with\n  `Object.values(...).some(m => m?.enabled === true)` — tolerant of missing/\n  partial method entries.\n\n## 2. Anatomy\n```\n(snap === null || dismissed || remaining === 0) → null\nelse:\n<section class=\"panel\" aria-label=\"Finish setting up\"          ← inline pink-gradient override\n         style=\"padding:18px 22px;margin-bottom:18px;…pink gradient + 0.25 pink border\">\n  <div> (flex space-between, wrap)\n    <div>\n      <div> eyebrow \"Setup checklist\" (#d94e4e, .14em upper)\n      <h3> \"Finish setting up — {n} thing(s) left\" (Nunito 800 18px teal)\n    <button type=button aria-label=\"Hide checklist\" onClick=setDismissed> \"Hide\"\n  <div role=\"progressbar\" aria-valuenow={pct} …>                ← 4px track, pink gradient fill\n    <div style=\"width:{pct}%; transition:width 480ms\">\n  <ul style=\"grid auto-fit minmax(220px,1fr)\">                  ← item grid\n    <li> × 7 {\n      <span aria-hidden>  ← 20px circle: green #519843 \"✓\" if done, faint teal empty if not\n      <a href=\"/settings\"> item.label (muted+400 if done, fg+600 if not)\n    }\n```\n- **Slots/children:** none.\n- **No icon dependency** — the check is a literal `\"✓\"` glyph, not an `ICN`.\n- **Styling:** 100% inline; uses the shared `.panel` class as a shell then\n  overrides it inline (pink gradient + pink border). See css/setup-checklist.css\n  for the class-named equivalents.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| — | — | — | — | — |\n\nThe island takes **no props** — the route mounts it bare (`<SetupChecklist />`).\nAll display data comes from the `/profile` fetch; language from `langSignal`.\n\n## 4. States → cases\nAll data is client-fetched, so isolate models it via `_mocks` (the `GET /profile`\nresponse). Dismiss is internal `useState` — model via `_signals`. Language via\n`_signals`.\n\n| state | meaning | case |\n|---|---|---|\n| partial | profile loaded, several items incomplete → card shows with a partial bar | `cases/partial/partial.json` |\n| almost-done | only 1 item left → \"…1 thing left\" singular heading | `cases/almost-done/almost-done.json` |\n| all-done | every item complete → renders `null` (card gone) | `cases/all-done/all-done.json` |\n| loading | `snap === null` (fetch in flight) → renders `null` | `cases/loading/loading.json` |\n| dismissed | user clicked \"Hide\" → renders `null` (session-only) | `cases/dismissed/dismissed.json` |\n\n## 5. Events\n- `ev.expect(e => e.source === \"button[aria-label='Hide checklist']\" && e.type === \"click\")`\n  → sets `dismissed=true` → card unmounts (NOT persisted; returns next load).\n- `ev.expect(e => e.source === \"a[href='/settings']\" && e.type === \"click\")` →\n  nav `/settings` (each item row is a link to Settings).\n- No emitted custom events; no form submit; no fetch on click.\n\n## 6. Motion (extracted — setup-checklist.css)\n- **The only motion is the progress fill:** the `role=\"progressbar\"` inner div\n  has `transition: width 480ms` (linear; no named easing). When `pct` changes\n  (e.g. an item completes between renders), the bar slides to the new width over\n  480ms.\n- No `@keyframes`. No hover effects. The card is otherwise static.\n- **Jank finding:** none of note — a single `width` transition on a 4px bar is\n  cheap (it does trigger layout on the fill element, but the track is fixed-size\n  so no surrounding reflow).\n- **Reduced motion:** no component-local override; the **global tokens `@media\n  (prefers-reduced-motion: reduce)`** clamps the 480ms width transition to\n  `0.01ms`.\n\n## 7. Responsive (own @media)\n- **None of its own.** The item grid is intrinsically responsive via\n  `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))` — it reflows from\n  multi-column to single-column as the container narrows, with no media query.\n  The header row uses `flex-wrap: wrap` so \"Finish setting up…\" and \"Hide\" stack\n  on very narrow widths.\n\n## 8. A11y\n- `<section aria-label=\"Finish setting up\">` landmark; the bar is a proper\n  `role=\"progressbar\"` with `aria-valuemin/max/now` + an `aria-label`.\n- \"Hide\" is a real `<button>` with `aria-label=\"Hide checklist\"`.\n- Each item is a real `<a href>` (keyboard-operable). The check bubble is\n  `aria-hidden` (decorative); the label text conveys done/not-done only via color\n  + font-weight — **GAP:** completion isn't exposed to SR beyond the visible \"✓\"\n  (which is aria-hidden). Consider `aria-label` on the link including\n  \"(done)\"/\"(to do)\" or a visually-hidden status word.\n- Reduced motion handled globally (see Motion).\n\n## 9. Used on\n**`/dashboard` only.** Imported by `islands/DashboardPage.tsx`\n(`import SetupChecklist from \"./SetupChecklist.tsx\"`) and rendered once, between\nthe assistant banner and the KPIs. Not shared. This is the sole profile-collection\nsurface — there is no separate onboarding gate (see PROJECT FACT above).\n", "fixture": "{\n  \"category\": \"dashboard\",\n  \"folder\": \"setup-checklist\",\n  \"controls\": {}\n}\n", "cases": [{"name": "all-done.json", "content": "{\n  \"_name\": \"All done — every item complete → renders null (card disappears)\",\n  \"_signals\": { \"lang\": \"en\", \"dismissed\": false },\n  \"_mocks\": {\n    \"GET /profile\": {\n      \"user\": { \"id\": \"u1\", \"name\": \"Raphael Castro\", \"email\": \"raphael@rcincorporated.net\", \"language\": \"en\", \"createdAt\": 1700000000000, \"updatedAt\": 1700000000000 },\n      \"identity\": {\n        \"userId\": \"u1\",\n        \"businessName\": \"RC Incorporated\",\n        \"logoFileId\": \"file_logo_123\",\n        \"acceptedPaymentMethods\": { \"zelle\": { \"enabled\": true, \"mailTo\": \"raphael@rcincorporated.net\" } },\n        \"createdAt\": \"2026-05-01T00:00:00Z\",\n        \"updatedAt\": \"2026-05-01T00:00:00Z\"\n      },\n      \"address\": { \"userId\": \"u1\", \"street\": \"120 Maple St\", \"city\": \"Austin\", \"state\": \"TX\", \"postal\": \"78701\", \"country\": \"US\", \"createdAt\": \"2026-05-01T00:00:00Z\", \"updatedAt\": \"2026-05-01T00:00:00Z\" },\n      \"insurance\": { \"userId\": \"u1\", \"provider\": \"Hiscox\", \"policyNumber\": \"HX-99281\", \"createdAt\": \"2026-05-01T00:00:00Z\", \"updatedAt\": \"2026-05-01T00:00:00Z\" },\n      \"tax\": null,\n      \"contractDefaults\": null,\n      \"references\": [],\n      \"initials\": \"RC\"\n    }\n  }\n}\n"}, {"name": "almost-done.json", "content": "{\n  \"_name\": \"Almost done — only insurance left → singular \\\"1 thing left\\\" heading (6 of 7)\",\n  \"_signals\": { \"lang\": \"en\", \"dismissed\": false },\n  \"_mocks\": {\n    \"GET /profile\": {\n      \"user\": { \"id\": \"u1\", \"name\": \"Raphael Castro\", \"email\": \"raphael@rcincorporated.net\", \"language\": \"en\", \"createdAt\": 1700000000000, \"updatedAt\": 1700000000000 },\n      \"identity\": {\n        \"userId\": \"u1\",\n        \"businessName\": \"RC Incorporated\",\n        \"logoFileId\": \"file_logo_123\",\n        \"acceptedPaymentMethods\": { \"zelle\": { \"enabled\": true, \"mailTo\": \"raphael@rcincorporated.net\" } },\n        \"createdAt\": \"2026-05-01T00:00:00Z\",\n        \"updatedAt\": \"2026-05-01T00:00:00Z\"\n      },\n      \"address\": { \"userId\": \"u1\", \"street\": \"120 Maple St\", \"city\": \"Austin\", \"state\": \"TX\", \"postal\": \"78701\", \"country\": \"US\", \"createdAt\": \"2026-05-01T00:00:00Z\", \"updatedAt\": \"2026-05-01T00:00:00Z\" },\n      \"insurance\": null,\n      \"tax\": null,\n      \"contractDefaults\": null,\n      \"references\": [],\n      \"initials\": \"RC\"\n    }\n  }\n}\n"}, {"name": "dismissed.json", "content": "{\n  \"_name\": \"Dismissed — user clicked Hide; dismissed=true → renders null (session-only, not persisted)\",\n  \"_signals\": { \"lang\": \"en\", \"dismissed\": true },\n  \"_mocks\": {\n    \"GET /profile\": {\n      \"user\": { \"id\": \"u1\", \"name\": \"Raphael Castro\", \"email\": \"raphael@rcincorporated.net\", \"language\": \"en\", \"createdAt\": 1700000000000, \"updatedAt\": 1700000000000 },\n      \"identity\": { \"userId\": \"u1\", \"businessName\": \"RC Incorporated\", \"logoFileId\": null, \"acceptedPaymentMethods\": {}, \"createdAt\": \"2026-05-01T00:00:00Z\", \"updatedAt\": \"2026-05-01T00:00:00Z\" },\n      \"address\": null,\n      \"insurance\": null,\n      \"tax\": null,\n      \"contractDefaults\": null,\n      \"references\": [],\n      \"initials\": \"RC\"\n    }\n  }\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — /profile fetch in flight; snap===null → renders null\",\n  \"_signals\": { \"lang\": \"en\", \"dismissed\": false },\n  \"_mocks\": {\n    \"GET /profile\": { \"_delay\": \"pending\" }\n  }\n}\n"}, {"name": "partial.json", "content": "{\n  \"_name\": \"Partial — name+email+biz done, logo/address/payment/insurance left (4 of 7)\",\n  \"_signals\": { \"lang\": \"en\", \"dismissed\": false },\n  \"_mocks\": {\n    \"GET /profile\": {\n      \"user\": { \"id\": \"u1\", \"name\": \"Raphael Castro\", \"email\": \"raphael@rcincorporated.net\", \"language\": \"en\", \"createdAt\": 1700000000000, \"updatedAt\": 1700000000000 },\n      \"identity\": {\n        \"userId\": \"u1\",\n        \"businessName\": \"RC Incorporated\",\n        \"logoFileId\": null,\n        \"acceptedPaymentMethods\": {},\n        \"createdAt\": \"2026-05-01T00:00:00Z\",\n        \"updatedAt\": \"2026-05-01T00:00:00Z\"\n      },\n      \"address\": null,\n      \"insurance\": null,\n      \"tax\": null,\n      \"contractDefaults\": null,\n      \"references\": [],\n      \"initials\": \"RC\"\n    }\n  }\n}\n"}], "css": "/* ===========================================================================\n * SetupChecklist — NO entries in static/dashboard.css.\n *\n * The component is 100% INLINE-STYLED. The only shared class it uses is\n * `.panel` (from dash-sections / dashboard.css) as its outer shell, but it\n * then OVERRIDES the panel look with an inline pink-tinted gradient + border.\n * Everything below is reproduced from the inline `style=\"\"` attrs in\n * js/SetupChecklist.tsx so a rebuild can port it to a class if desired.\n * ======================================================================== */\n\n/* outer <section class=\"panel\"> inline overrides */\n.setup-checklist {\n  padding: 18px 22px;\n  margin-bottom: 18px;\n  background: linear-gradient(\n    135deg,\n    rgba(255, 107, 107, 0.08) 0%,\n    rgba(255, 107, 107, 0.02) 100%\n  );\n  border: 1px solid rgba(255, 107, 107, 0.25);\n}\n\n/* eyebrow */\n.setup-checklist__eyebrow {\n  font-size: 11px;\n  font-weight: 800;\n  letter-spacing: 0.14em;\n  text-transform: uppercase;\n  color: #d94e4e;\n}\n\n/* heading */\n.setup-checklist__heading {\n  margin: 4px 0 0;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 18px;\n  color: var(--brand-teal);\n}\n\n/* \"Hide\" dismiss button */\n.setup-checklist__hide {\n  appearance: none;\n  background: transparent;\n  border: 0;\n  font: inherit;\n  font-size: 12px;\n  color: var(--fg-muted);\n  cursor: pointer;\n  opacity: 0.7;\n}\n\n/* progress bar track (role=progressbar) */\n.setup-checklist__progress {\n  height: 4px;\n  border-radius: 999px;\n  background: rgba(255, 107, 107, 0.15);\n  overflow: hidden;\n  margin-top: 10px;\n}\n/* progress fill — width = pct%, eased */\n.setup-checklist__progress-fill {\n  height: 100%;\n  background: linear-gradient(90deg, #ff6b6b, #d94e4e);\n  transition: width 480ms; /* the only motion in this island */\n}\n\n/* item grid + rows */\n.setup-checklist__list {\n  margin: 12px 0 0;\n  padding: 0;\n  list-style: none;\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));\n  gap: 8px;\n}\n.setup-checklist__item {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 8px 12px;\n  border-radius: 10px;\n  border: 1px solid rgba(20, 72, 82, 0.10);\n  background: #fff;\n}\n/* check bubble — green (#519843) when done, faint teal when not */\n.setup-checklist__check {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 20px;\n  height: 20px;\n  border-radius: 50%;\n  color: #fff;\n  font-size: 12px;\n  font-weight: 800;\n  flex-shrink: 0;\n}\n\n/* Reduced motion: only the 480ms width transition exists; the global tokens\n * @media clamps it. No component-local override. */\n", "cssName": "setup-checklist.css", "source": "import { useEffect, useState } from \"preact/hooks\";\nimport { profileClient, type ProfileSnapshot } from \"../clients/profile.ts\";\nimport { langSignal, tFor } from \"../lib/i18n.ts\";\n\n/**\n * SetupChecklist — post-onboarding \"finish setting up\" card on the\n * dashboard. Lists the high-leverage items that onboarding doesn't\n * collect (logo, payment method, insurance, contract defaults) plus\n * any address piece the user skipped. Self-hides once every item is\n * checked.\n *\n * Pulled into its own island so the dashboard route stays SSR-clean —\n * fetching /profile here means the checklist appears only when we\n * actually need it without coupling the dashboard route to a\n * profile-shape read on every render.\n */\ninterface Item {\n  key: string;\n  label: string;\n  done: boolean;\n  href: string;\n}\n\nexport default function SetupChecklist() {\n  const [snap, setSnap] = useState<ProfileSnapshot | null>(null);\n  const [dismissed, setDismissed] = useState(false);\n\n  useEffect(() => {\n    let alive = true;\n    profileClient.get().then((p) => {\n      if (alive) setSnap(p);\n    }).catch(() => {});\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  if (!snap || dismissed) return null;\n\n  // Reactive app language (flips live on a Settings change); the profile fetch\n  // above is only for the checklist item-completeness data, not the language.\n  const lang = langSignal.value;\n  const id = snap.identity ?? null;\n  const acceptedAny = id?.acceptedPaymentMethods\n    ? Object.values(id.acceptedPaymentMethods).some(\n      (m) => m && (m as { enabled?: boolean }).enabled === true,\n    )\n    : false;\n\n  const items: Item[] = [\n    {\n      key: \"name\",\n      label: tFor(lang, \"setupChecklist.item.name\"),\n      done: !!snap.user.name?.trim(),\n      href: \"/settings\",\n    },\n    {\n      key: \"biz\",\n      label: tFor(lang, \"setupChecklist.item.businessName\"),\n      done: !!id?.businessName?.trim(),\n      href: \"/settings\",\n    },\n    {\n      key: \"email\",\n      label: tFor(lang, \"setupChecklist.item.email\"),\n      done: !!snap.user.email?.trim(),\n      href: \"/settings\",\n    },\n    {\n      key: \"logo\",\n      label: tFor(lang, \"setupChecklist.item.logo\"),\n      done: !!id?.logoFileId,\n      href: \"/settings\",\n    },\n    {\n      key: \"address\",\n      label: tFor(lang, \"setupChecklist.item.address\"),\n      done: !!(snap.address?.postal?.trim() || snap.address?.street?.trim()),\n      href: \"/settings\",\n    },\n    {\n      key: \"payment\",\n      label: tFor(lang, \"setupChecklist.item.payment\"),\n      done: acceptedAny,\n      href: \"/settings\",\n    },\n    {\n      key: \"insurance\",\n      label: tFor(lang, \"setupChecklist.item.insurance\"),\n      done: !!snap.insurance?.provider?.trim(),\n      href: \"/settings\",\n    },\n  ];\n  const remaining = items.filter((i) => !i.done);\n  if (remaining.length === 0) return null;\n\n  const pct = Math.round(\n    ((items.length - remaining.length) / items.length) * 100,\n  );\n\n  return (\n    <section\n      class=\"panel\"\n      aria-label={tFor(lang, \"setupChecklist.aria.section\")}\n      style=\"padding:18px 22px;margin-bottom:18px;background:linear-gradient(135deg,rgba(255,107,107,0.08) 0%,rgba(255,107,107,0.02) 100%);border:1px solid rgba(255,107,107,0.25)\"\n    >\n      <div style=\"display:flex;align-items:center;justify-content:space-between;gap:14px;flex-wrap:wrap\">\n        <div>\n          <div style=\"font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:#d94e4e\">\n            {tFor(lang, \"setupChecklist.eyebrow\")}\n          </div>\n          <h3 style=\"margin:4px 0 0;font-family:var(--font-heading);font-weight:800;font-size:18px;color:var(--brand-teal)\">\n            {tFor(\n              lang,\n              `setupChecklist.heading.${\n                remaining.length === 1 ? \"one\" : \"other\"\n              }`,\n              { n: remaining.length },\n            )}\n          </h3>\n        </div>\n        <button\n          type=\"button\"\n          aria-label={tFor(lang, \"setupChecklist.aria.hide\")}\n          onClick={() => setDismissed(true)}\n          style=\"appearance:none;background:transparent;border:0;font:inherit;font-size:12px;color:var(--fg-muted);cursor:pointer;opacity:0.7\"\n        >\n          {tFor(lang, \"setupChecklist.hide\")}\n        </button>\n      </div>\n      <div\n        role=\"progressbar\"\n        aria-label={tFor(lang, \"setupChecklist.aria.progress\")}\n        aria-valuemin={0}\n        aria-valuemax={100}\n        aria-valuenow={pct}\n        style=\"height:4px;border-radius:999px;background:rgba(255,107,107,0.15);overflow:hidden;margin-top:10px\"\n      >\n        <div\n          style={`height:100%;width:${pct}%;background:linear-gradient(90deg,#FF6B6B,#d94e4e);transition:width 480ms`}\n        />\n      </div>\n      <ul style=\"margin:12px 0 0;padding:0;list-style:none;display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:8px\">\n        {items.map((it) => (\n          <li\n            key={it.key}\n            style=\"display:flex;align-items:center;gap:8px;padding:8px 12px;border-radius:10px;border:1px solid rgba(20,72,82,0.10);background:#fff\"\n          >\n            <span\n              aria-hidden=\"true\"\n              style={`display:inline-flex;align-items:center;justify-content:center;width:20px;height:20px;border-radius:50%;background:${\n                it.done ? \"#519843\" : \"rgba(20,72,82,0.10)\"\n              };color:#fff;font-size:12px;font-weight:800;flex-shrink:0`}\n            >\n              {it.done ? \"✓\" : \"\"}\n            </span>\n            <a\n              href={it.href}\n              style={`flex:1;font-size:13px;color:${\n                it.done ? \"var(--fg-muted)\" : \"var(--fg)\"\n              };text-decoration:none;font-weight:${it.done ? 400 : 600}`}\n            >\n              {it.label}\n            </a>\n          </li>\n        ))}\n      </ul>\n    </section>\n  );\n}\n", "sourceName": "SetupChecklist.tsx", "checklist": "# Capture checklist — SetupChecklist\n\n**Theme:** light only.\n**Auth:** log in with dev master OTP `000000`.\n**Route:** `http://localhost:5280/dashboard`. The card renders between the\n`.assistant-cta` banner and the KPIs — only when the logged-in profile has at\nleast one incomplete item and the card hasn't been hidden this session.\n\n> PROJECT FACT: this is the entire profile-collection mechanism — there is NO\n> onboarding nav-gate/redirect. Capture it as an opt-in, dismissible card; do not\n> expect (or screenshot) any \"you must finish your profile\" wall.\n\n## Element(s) to crop\n- The `<section class=\"panel\" aria-label=\"Finish setting up\">` card: pink-tinted\n  gradient, eyebrow + heading, the 4px progress bar, and the auto-fit item grid\n  with green \"✓\" / empty check bubbles.\n\n## Viewports\n- The card has no media queries of its own; the item grid auto-fits\n  (`minmax(220px, 1fr)`). Shoot at **1280px** (multi-column item grid),\n  **720px** (fewer columns), and **390px** (single-column items; header\n  \"Finish setting up…\" + \"Hide\" wrap).\n\n## Transient / data states to drive\n1. **partial** — a profile with several items missing (e.g. no logo/address/\n   payment/insurance): the card shows with a partial bar and a mix of green-✓ and\n   empty bubbles. Heading reads \"Finish setting up — N things left\".\n2. **almost-done** — a profile with only one item left: heading reads\n   \"Finish setting up — 1 thing left\" (singular), bar near full.\n3. **all-done** — a fully-completed profile: the card is GONE (renders null).\n   Capture the dashboard WITHOUT the card to evidence the self-hide.\n4. **dismissed** — click \"Hide\"; the card unmounts. Then RELOAD and confirm it\n   COMES BACK (dismiss is session-only, not persisted) — capture both to document\n   the flagged non-persistence.\n5. **progress-bar tween** — if you complete an item in Settings and return so\n   `pct` changes between renders, film the 480ms `width` transition on the bar.\n   Re-shoot with `prefers-reduced-motion: reduce` (global clamp to 0.01ms — the\n   bar should snap).\n\n## Notes\n- To force the \"partial\" state on a seeded account, you may need a profile with\n  gaps; otherwise the card may already be hidden (all-done). Set up the account's\n  profile fields accordingly before capture.\n- NO fabricated screenshots — capture only the live `/dashboard` render.\n"}, {"name": "ticker", "component": "Ticker", "spec": "# Ticker\n\nAn **animated number** — eases a value from `0` to its target with\n`requestAnimationFrame` and renders the running figure as a bare text fragment.\nA direct port of the prototype's `useTicker` + `<Ticker/>`. It is the money/KPI\ncount-up, embedded inside other components — it has no box, no class, no chrome\nof its own.\n\n> **Naming collision (read this first).** This `islands/Ticker.tsx` is the rAF\n> number count-up. It is NOT the rotating headline marquee in the topbar — that\n> is `DashTopbar`'s `.topbar__ticker` (a `setInterval`-driven rotating line with\n> `tickerSlideIn`/`tickerPulse` keyframes, spec'd under\n> `shared-components/dash-topbar/`). The scope line \"rotating ticker line\" maps\n> to the topbar; the actual source file `Ticker.tsx` is this count-up. This spec\n> covers the real source file. Do not conflate the two `tickerPulse` /\n> `tickerSlideIn` keyframes (topbar) with this component — this component has NO\n> CSS keyframes at all.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (file lives in `islands/Ticker.tsx`). 30 lines.\n- **Interaction tier:** `island` — **client-only animation state**, no\n  interactivity, no server contact, no events.\n- **Client state owned (`useState`):** `v: number` — the current eased value,\n  starts at `0`, climbs to `value`.\n- **Lifecycle:** a single `useEffect([value, duration])`. On mount (and whenever\n  `value`/`duration` change) it captures `performance.now()`, then schedules a\n  `requestAnimationFrame` loop. Each frame: `t = min((now-start)/duration, 1)`,\n  eased by `1 - (1-t)^3` (ease-out-cubic), `setV(round(value*eased))`; loops\n  until `t >= 1`. Cleanup `cancelAnimationFrame(raf)`.\n- **Data source:** none — the target is the `value` prop, passed by the host\n  (`Outstanding`'s `owed`, the dead `Hero`'s `thisMonthBilled`). It does not read\n  `clients/*` or any cache.\n- **Honest-empty:** `value={0}` renders `\"0\"` immediately (the loop sets\n  `round(0*eased)=0` every frame). No special empty branch.\n- **Server mutations / `location.reload()`:** N/A — none.\n- **Liveness:** the **only timed animation on the dashboard body**. rAF, runs\n  once per `value` change. Not polling (it's a finite ease, not a repeating\n  interval).\n- **Reactivity:** if the host re-renders with a new `value` (e.g. live data\n  arrives after a refresh), the effect re-fires and the number **re-animates\n  from 0** to the new target — a full count-up on every value change, not a\n  delta-tween. FLAG: a data refresh that nudges `owed` by a dollar restarts the\n  whole 0→N spin (visually loud). Fix: tween from the previous `v`, not from 0.\n- **Data-shape hazards:** `value` must be a finite number; `NaN`/`Infinity` would\n  propagate through `round(value*eased)` and render `\"NaN\"`. Hosts pass already\n  cents→dollars-divided numbers (`pickKpis`), so this is not currently hit, but\n  there is no guard.\n\n## 2. Anatomy\n```\n<>{prefix}{v.toLocaleString(\"en-US\")}</>\n```\n- A **Preact fragment** with two text nodes: the optional `prefix` and the\n  comma-grouped current value. **No wrapper element, no class, no DOM box.**\n- All visual styling is inherited from the host text container:\n  - inside `Outstanding` → `.money__amt` (32px Nunito 800, `--brand-teal`); the\n    host renders `$<Ticker value={owed}/>` so the `$` is the host's, not the\n    ticker's `prefix`.\n  - inside the dead `Hero` → `.hero__title em` (pink, non-italic), as\n    `$<Ticker value={thisMonthBilled}/>`.\n- **No icon / asset / child dependencies.**\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `value` | number (target) | — (required) | number | no |\n| `prefix` | string | `\"\"` | text | no |\n| `duration` | number (ms) | `1400` | number | no |\n\n(Note: at the live call sites the `$` is supplied by the *host* markup, not via\n`prefix`. `prefix` is the generic API surface — exercise it in isolate.)\n\n## 4. States → cases\nAnimation is internal; isolate models it by setting `value`/`prefix`/`duration`\nas props and capturing the settled (end-of-ease) frame, plus a mid-flight frame.\n\n| state | meaning | case |\n|---|---|---|\n| money | typical Outstanding owed (settled at `6420` → \"6,420\") | `cases/money/money.json` |\n| zero | `value={0}` → renders \"0\" immediately | `cases/zero/zero.json` |\n| prefixed | `prefix=\"$\"` + large value (count-up filmstrip) | `cases/prefixed/prefixed.json` |\n\n(For a filmstrip of the in-flight ease, drive the screenshots, not a case — the\nintermediate `v` isn't a settable prop.)\n\n## 5. Events\n- **None.** No clicks, no inputs, no emitted custom events, no listeners. The\n  component is non-interactive output.\n- `ev.expect(...)` predicates: N/A — nothing for `capture(page)` to assert beyond\n  the rendered text settling at `value.toLocaleString()`.\n\n## 6. Motion (extracted)\n- **JS rAF count-up**, NOT CSS. 0 → `value` over `duration` (default **1400ms**),\n  eased `1 - (1-t)^3` (ease-out-cubic — fast start, gentle settle). One frame per\n  rAF tick (~60fps); `setV` per frame.\n- **No `@keyframes`, no CSS transition** of its own (see css/ticker.css — it is a\n  documentation-only stub).\n- **Jank finding:** `setV` on every animation frame triggers a Preact re-render\n  per frame for ~1.4s. For a single small text node this is cheap, but if many\n  Tickers mount at once (or the host subtree is heavy) the per-frame re-render\n  cost adds up. Fix: write the value into a ref + `textContent` (or a signal)\n  instead of `setState` to bypass the VDOM diff each frame.\n- **Reduced-motion: NOT honored — a11y gap.** The rAF loop runs regardless of\n  `prefers-reduced-motion: reduce`; there is no guard in the component and no CSS\n  to clamp (the global tokens `@media` only clamps CSS animation/transition\n  durations, which this has none of). A motion-sensitive user still sees the\n  number spin. **Fix:** read `matchMedia(\"(prefers-reduced-motion: reduce)\")` and\n  set `v = value` instantly (skip the loop) when reduced motion is requested.\n\n## 7. Responsive (own @media)\n- **None.** The component has no layout and no media queries — it inherits size\n  and wrapping from its host text container (`.money__amt` / `.hero__title em`).\n  Responsive behavior is the host's (Outstanding restacks at 640px via\n  dash-sections.css).\n\n## 8. A11y\n- Renders plain text — natively readable by screen readers.\n- **GAP — no `aria-live`** on the ticker (the host `.money__amt` also has none):\n  the number visibly changes for ~1.4s but the change isn't announced; an SR user\n  hears whatever is read at focus time, which may be a mid-count value.\n- **GAP — no reduced-motion guard** (see Motion): the count-up animation cannot\n  be disabled by the OS preference.\n- The `$` and `prefix` are decorative text, not labeled.\n\n## 9. Used on\n**`/dashboard`** only, embedded by `components/DashSections.tsx`:\n- `Outstanding` → `$<Ticker value={owed}/>` inside `.money__amt` (live).\n- `Hero` (DEAD export) → `$<Ticker value={thisMonthBilled}/>` inside\n  `.hero__title em` (no importer renders Hero).\nImported as `import Ticker from \"../islands/Ticker.tsx\"`. Not used elsewhere;\nthe topbar's rotating line is a separate `DashTopbar` mechanism (see the warning\nat the top).\n", "fixture": "{\n  \"category\": \"dashboard\",\n  \"folder\": \"ticker\",\n  \"controls\": {\n    \"value\": { \"type\": \"number\", \"value\": 6420 },\n    \"prefix\": { \"type\": \"text\", \"value\": \"\" },\n    \"duration\": { \"type\": \"number\", \"value\": 1400 }\n  }\n}\n", "cases": [{"name": "money.json", "content": "{\n  \"_name\": \"Money owed — settles at 6,420 (Outstanding .money__amt)\",\n  \"value\": 6420,\n  \"prefix\": \"\",\n  \"duration\": 1400\n}\n"}, {"name": "prefixed.json", "content": "{\n  \"_name\": \"Prefixed large value — \\\"$18,420\\\" count-up filmstrip\",\n  \"value\": 18420,\n  \"prefix\": \"$\",\n  \"duration\": 1400\n}\n"}, {"name": "zero.json", "content": "{\n  \"_name\": \"Zero — renders \\\"0\\\" immediately, no spin\",\n  \"value\": 0,\n  \"prefix\": \"\",\n  \"duration\": 1400\n}\n"}], "css": "/* ===========================================================================\n * Ticker — NO dedicated CSS.\n *\n * Ticker.tsx renders only a text fragment `<>{prefix}{v.toLocaleString()}</>`.\n * It has no wrapper element and no class of its own. All visual styling comes\n * from the host that embeds it:\n *   - inside Outstanding → `.money__amt` (32px Nunito 800, brand-teal) — see\n *     dash-sections/css/dash-sections.css\n *   - inside the dead Hero → `.hero__title em` (pink, non-italic)\n *\n * The animation is JS (requestAnimationFrame easing 0→value over `duration`ms,\n * default 1400), NOT CSS — see js/Ticker.tsx. There is no @keyframes here and\n * NO reduced-motion guard: the rAF count-up runs regardless of\n * prefers-reduced-motion (flagged as an a11y gap in ticker.md §6).\n * ======================================================================== */\n", "cssName": "ticker.css", "source": "/**\n * Animated number — eases from 0 → target with requestAnimationFrame.\n * Direct port of the prototype's `useTicker` + `<Ticker/>`.\n */\nimport { useEffect, useState } from \"preact/hooks\";\n\ninterface Props {\n  value: number;\n  prefix?: string;\n  duration?: number;\n}\n\nexport default function Ticker({ value, prefix = \"\", duration = 1400 }: Props) {\n  const [v, setV] = useState(0);\n\n  useEffect(() => {\n    const start = performance.now();\n    let raf = 0;\n    const tick = (now: number) => {\n      const t = Math.min((now - start) / duration, 1);\n      const eased = 1 - Math.pow(1 - t, 3);\n      setV(Math.round(value * eased));\n      if (t < 1) raf = requestAnimationFrame(tick);\n    };\n    raf = requestAnimationFrame(tick);\n    return () => cancelAnimationFrame(raf);\n  }, [value, duration]);\n\n  return <>{prefix}{v.toLocaleString(\"en-US\")}</>;\n}\n", "sourceName": "Ticker.tsx", "checklist": "# Capture checklist — Ticker\n\n**Theme:** light only.\n**Auth:** log in with dev master OTP `000000` (any seeded phone).\n**Route:** `http://localhost:5280/dashboard`. Ticker has no route of its own — it\nis a text node rendered inside the **Outstanding** money card\n(`.money__amt`). Capture it there.\n\n> Reminder: this is the rAF number count-up, NOT the topbar's rotating headline\n> marquee (`.topbar__ticker`, a `DashTopbar` mechanism). Do not shoot the topbar\n> line for this component.\n\n## Element(s) to crop\n- The `.money__amt` figure in the Outstanding card (`$` + the animated number).\n  Crop tight to the numerals so the count-up is the subject.\n\n## Viewports\n- The component has no media queries; it inherits from `.money__amt`. Shoot at\n  **1280px** (desktop) and **390px** (mobile, where Outstanding has restacked at\n  640px) to confirm it reflows with the host.\n\n## Transient states to drive (this is a MOTION component — film, don't just shot)\n1. **count-up (cold)** — load `/dashboard` with a cold cache so Outstanding\n   mounts fresh and the number eases **0 → owed over ~1400ms**\n   (ease-out-cubic). Film the full filmstrip: capture frames at ~0ms, ~200ms,\n   ~600ms, ~1000ms, ~1400ms to show the deceleration.\n2. **settled** — the final frame, e.g. `6,420` fully landed.\n3. **re-animate on data change** — if a refresh changes `owed`, the number spins\n   **from 0 again** (not a delta-tween). Capture this restart to document the\n   flagged behavior (loud re-spin on minor value changes).\n4. **zero** — an account with `owed=0`: renders \"0\" with no visible motion.\n5. **reduced-motion (NEGATIVE check)** — set\n   `prefers-reduced-motion: reduce` and reload. **EXPECT the count-up to STILL\n   RUN** (no guard) — capture this to evidence the a11y gap; do NOT assume it\n   stops.\n\n## Isolate filmstrip\n- In isolate, the `prefixed` case (`prefix:\"$\"`, `value:18420`) is the cleanest\n  count-up subject. The mid-flight value is not a settable prop, so film the\n  animation rather than expecting an intermediate case.\n\n## Notes\n- No CSS keyframes exist for this component — all motion is JS rAF. There is\n  nothing to capture in a CSS animation inspector; record the rendered text over\n  time instead.\n- NO fabricated screenshots — film the live `/dashboard` Outstanding card.\n"}, {"name": "welcome-back-toast", "component": "WelcomeBackToast", "spec": "# WelcomeBackToast\n\nA small \"Welcome back, {firstName}.\" pill that fades into the top-right of the\ndashboard for returning users who land from `/verify` with `?welcome=back` in the\nURL. It auto-dismisses after 3 seconds and strips the query param so a refresh\ndoesn't re-flash. For everyone else it renders nothing.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (file lives in `islands/WelcomeBackToast.tsx`). Mounted at\n  the dashboard route root (sibling of `.app`).\n- **Interaction tier:** `island` — client-only state, self-gating, one\n  fire-and-forget fetch for the name. Non-interactive output\n  (`pointer-events: none`).\n- **Self-gate (the headline behavior):** on mount it reads\n  `new URLSearchParams(location.search).get(\"welcome\")`; if it is not `\"back\"` it\n  returns early and never shows. So the pill only appears on the post-verify\n  redirect, not on normal dashboard loads.\n- **Client state owned (`useState`):**\n  - `show: boolean` — gates the pill; set `true` once the name resolves, set\n    `false` by a 3s timer.\n  - `name: string` — the user's first name (best-effort; defaults to a fallback).\n- **Data source:** `fetch(\"/api/me\", { credentials: \"include\" })` → `{ name? }`;\n  takes the first whitespace-delimited token as the first name. This is a **raw\n  `fetch`, not a `clients/*` helper** (a small inline call). On any non-ok\n  response or network error it silently bails (`catch {}`) — the pill simply\n  doesn't show rather than showing a broken state. (Note: the generic\n  fallback-name path is only reached at render via\n  `t(\"welcomeBackToast.nameFallback\")` → \"friend\"; a failed `/api/me` returns\n  before `setShow(true)`, so in practice a network failure suppresses the toast\n  entirely.)\n- **Query-param cleanup:** after a 50ms defer (so it doesn't race the param read),\n  it `history.replaceState`s the URL with `welcome` deleted — so a manual refresh\n  won't re-trigger the toast. In-place, no navigation.\n- **i18n (note — uses `lib/lang.ts`, not the JSON dict):** the message template\n  comes from `STRINGS[lang][\"welcome.back\"]` (`\"Welcome back, {firstName}.\"`),\n  with `{firstName}` replaced by the fetched name (or the `t(...)` fallback\n  \"friend\"). `lang` is `langSignal.value` at render (reactive). This is the\n  `lib/lang.ts` `STRINGS` map, distinct from the `lang/en.json` dict the other\n  components use.\n- **Server mutations / PRG / flash:** none — it's read-only feedback.\n- **`location.reload()`:** none. ✅ (It uses `replaceState`, not reload/assign.)\n- **Liveness:** request-response (one `/api/me` GET) + two timers (50ms param\n  cleanup, 3000ms auto-dismiss). No polling, no websocket. Cleanup clears the\n  dismiss timer and a `cancelled` flag on unmount.\n- **Data-shape hazards:** `me.name` is optional → `(me.name ?? \"\").trim().split(\n  /\\s+/)[0] ?? \"\"` yields `\"\"` for a nameless user, which falls through to the\n  \"friend\" fallback at render. `URLSearchParams`/`URL` are wrapped against SSR\n  (`typeof globalThis.window === \"undefined\"` guard up top). `replaceState` is\n  try/catch'd.\n\n## 2. Anatomy\n```\n(SSR / window undefined) → effect no-ops\n(welcome !== \"back\") → never shows\n(!show) → null\nelse:\n<div role=\"status\" aria-live=\"polite\"\n     style=\"position:fixed;top:18px;right:18px;z-index:9999;\n            inline-flex;gap:10px;padding:10px 16px 10px 14px;\n            background:#fff;border:1px solid rgba(0,0,0,.06);border-radius:999px;\n            box-shadow:0 6px 24px rgba(0,0,0,.10),0 1px 3px rgba(0,0,0,.04);\n            font-size:14px;font-weight:600;color:#144852;\n            animation:pm-toast-in 280ms cubic-bezier(.34,1.56,.64,1) both;\n            pointer-events:none\">\n  <span aria-hidden style=\"22px circle; background:#FF6B6B; color:#fff;\">👋</span>\n  <span>{message}</span>          ← \"Welcome back, {firstName}.\" (or \"…, friend.\")\n  <style> @keyframes pm-toast-in + reduced-motion guard\n```\n- **Slots/children:** none.\n- **No icon import** — the 👋 is an emoji in a pink circle. Fully inline-styled;\n  the only shared concept is the `--brand-pink`/`--brand-teal` hexes inlined as\n  `#FF6B6B`/`#144852`.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| — | — | — | — | — |\n\nThe island takes **no props** — mounted bare (`<WelcomeBackToast />`). The name\ncomes from `/api/me`, the gate from the URL, the language from `langSignal`.\n\n## 4. States → cases\nSelf-gating + fetched name; isolate models the URL param + `/api/me` via `_mocks`\nand the `show`/`name` via `_signals`. Language via `_signals`.\n\n| state | meaning | case |\n|---|---|---|\n| named | `?welcome=back` + `/api/me` → \"Welcome back, Raphael.\" | `cases/named/named.json` |\n| fallback-name | `?welcome=back` + nameless `/api/me` → \"Welcome back, friend.\" | `cases/fallback-name/fallback-name.json` |\n| no-param | no `?welcome=back` → renders nothing | `cases/no-param/no-param.json` |\n\n## 5. Events\n- **No user events** — `pointer-events: none`; the user cannot click it.\n- Internal: a `setTimeout(3000)` flips `show=false` (auto-dismiss); a\n  `setTimeout(50)` `history.replaceState`s the cleaned URL.\n- No emitted custom events; no nav; the only fetch is the one-shot `/api/me`.\n- `ev.expect(...)`: N/A for interaction — assert the rendered message text and\n  that the toast disappears after 3s.\n\n## 6. Motion (extracted — self-injected <style>, welcome-back-toast.css)\n- **Entrance only:** `@keyframes pm-toast-in` — `opacity 0→1` +\n  `translateY(-6px) scale(0.96) → translateY(0) scale(1)` over **280ms**\n  `cubic-bezier(.34,1.56,.64,1)` (`both` fill). A single gentle drop-in pop.\n- **Exit:** there is **no exit animation** — `show=false` unmounts the node\n  abruptly at t=3s (FLAG: the pill pops out with no fade; consider a reverse\n  `pm-toast-out` for symmetry).\n- No looping/idle motion; the 👋 does not animate here (unlike the coachmark's\n  waving 👋).\n- **Jank finding:** none — `position:fixed` + `transform`/`opacity` entrance is\n  compositor-friendly; the box is tiny.\n- **Reduced motion:** the island ships its OWN guard\n  (`@media (prefers-reduced-motion: reduce)` →\n  `[role=status][style*=\"pm-toast-in\"] { animation: none !important }`), in\n  ADDITION to the global tokens clamp. The pill then appears instantly with no\n  drop-in.\n\n## 7. Responsive (own @media)\n- **None.** It's a `position:fixed` pill pinned `top:18px right:18px` at all\n  widths; content sizes to its text. On a narrow viewport with a long localized\n  name it could approach the edge, but there is no media query and no max-width —\n  verify it doesn't overflow at 390px during capture.\n\n## 8. A11y\n- Correctly a **live region**: `role=\"status\"` + `aria-live=\"polite\"` — the\n  \"Welcome back, …\" message is announced to screen readers when it appears (the\n  right semantics for a transient, non-interactive toast).\n- `pointer-events: none` — not focusable, not clickable (intentional; it's\n  informational and auto-dismisses).\n- The 👋 avatar is `aria-hidden` (decorative).\n- Reduced motion handled (own guard + global).\n- **Minor:** auto-dismiss at 3s with no manual dismiss — fine for a polite status,\n  but a user who needs more time can't re-summon it (it's also param-stripped, so\n  refresh won't bring it back). Acceptable for a low-stakes greeting.\n\n## 9. Used on\n**`/dashboard` only.** Imported by `routes/dashboard/index.tsx`\n(`import WelcomeBackToast from \"../../islands/WelcomeBackToast.tsx\"`), rendered\nonce at the route root. Triggered exclusively by the `/verify` → `/dashboard?\nwelcome=back` returning-user redirect. Not shared.\n", "fixture": "{\n  \"category\": \"dashboard\",\n  \"folder\": \"welcome-back-toast\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"], \"signal\": true }\n  }\n}\n", "cases": [{"name": "fallback-name.json", "content": "{\n  \"_name\": \"Fallback name — ?welcome=back + nameless /api/me → \\\"Welcome back, friend.\\\"\",\n  \"_signals\": { \"lang\": \"en\", \"show\": true, \"name\": \"\" },\n  \"_mocks\": {\n    \"_url\": \"/dashboard?welcome=back\",\n    \"GET /api/me\": { \"name\": \"\" }\n  }\n}\n"}, {"name": "named.json", "content": "{\n  \"_name\": \"Named — ?welcome=back + /api/me resolves first name → \\\"Welcome back, Raphael.\\\"\",\n  \"_signals\": { \"lang\": \"en\", \"show\": true, \"name\": \"Raphael\" },\n  \"_mocks\": {\n    \"_url\": \"/dashboard?welcome=back\",\n    \"GET /api/me\": { \"name\": \"Raphael Castro\" }\n  }\n}\n"}, {"name": "no-param.json", "content": "{\n  \"_name\": \"No param — plain /dashboard load (no ?welcome=back) → renders nothing\",\n  \"_signals\": { \"lang\": \"en\", \"show\": false, \"name\": \"\" },\n  \"_mocks\": {\n    \"_url\": \"/dashboard\",\n    \"GET /api/me\": { \"name\": \"Raphael Castro\" }\n  }\n}\n"}], "css": "/* ===========================================================================\n * WelcomeBackToast — NO entries in static/dashboard.css.\n *\n * Fully INLINE-STYLED; ships its own <style> with the @keyframes below.\n * A position:fixed top-right pill (white, pill radius 999px, soft shadow,\n * teal #144852 text) with a pink 👋 avatar. pointer-events:none — purely\n * informational, auto-dismisses after 3s.\n * ======================================================================== */\n\n/* --- Self-injected keyframes (verbatim) --- */\n@keyframes pm-toast-in {\n  0%   { opacity: 0; transform: translateY(-6px) scale(0.96); }\n  100% { opacity: 1; transform: translateY(0)    scale(1); }\n}\n\n/* --- Reduced motion (verbatim — the island's own guard) --- */\n@media (prefers-reduced-motion: reduce) {\n  [role=status][style*=\"pm-toast-in\"] {\n    animation: none !important;\n  }\n}\n\n/* ---------------------------------------------------------------------------\n * Inline-style reference (documents the box for a rebuild):\n *   container : position:fixed; top:18px; right:18px; z-index:9999;\n *               padding:10px 16px 10px 14px; inline-flex; gap:10px;\n *               background:#fff; border:1px solid rgba(0,0,0,.06);\n *               border-radius:999px;\n *               box-shadow:0 6px 24px rgba(0,0,0,.10), 0 1px 3px rgba(0,0,0,.04);\n *               font-size:14px; font-weight:600; color:#144852;\n *               animation:pm-toast-in 280ms cubic-bezier(.34,1.56,.64,1) both;\n *               pointer-events:none\n *   avatar    : 22x22 circle; background:#FF6B6B; color:#fff; centered 👋\n * ------------------------------------------------------------------------- */\n", "cssName": "welcome-back-toast.css", "source": "import { useEffect, useState } from \"preact/hooks\";\nimport { langSignal, STRINGS } from \"../lib/lang.ts\";\nimport { t } from \"../lib/i18n.ts\";\n\nconst PINK = \"#FF6B6B\";\nconst TEAL = \"#144852\";\n\n/**\n * WelcomeBackToast — a 3-second pill that fades in on the dashboard\n * for returning users coming back from /verify with `?welcome=back`\n * in the URL. Drops the query param via history.replaceState so a\n * page refresh doesn't re-trigger.\n *\n * Self-gating: if `?welcome=back` isn't present, renders nothing.\n */\nexport default function WelcomeBackToast() {\n  const [show, setShow] = useState(false);\n  const [name, setName] = useState<string>(\"\");\n\n  useEffect(() => {\n    if (typeof globalThis.window === \"undefined\") return;\n    const params = new URLSearchParams(globalThis.location.search);\n    if (params.get(\"welcome\") !== \"back\") return;\n\n    let cancelled = false;\n    (async () => {\n      try {\n        const r = await fetch(\"/api/me\", { credentials: \"include\" });\n        if (!r.ok) return;\n        const me = await r.json() as { name?: string };\n        if (!cancelled) {\n          const first = (me.name ?? \"\").trim().split(/\\s+/)[0] ?? \"\";\n          setName(first);\n          setShow(true);\n        }\n      } catch { /* network blip — render the generic version */ }\n    })();\n\n    // Drop the query so a refresh doesn't re-flash. Defer past mount\n    // so the URL change doesn't race the params check above.\n    setTimeout(() => {\n      try {\n        const url = new URL(globalThis.location.href);\n        url.searchParams.delete(\"welcome\");\n        globalThis.history.replaceState(null, \"\", url.toString());\n      } catch { /* ignore */ }\n    }, 50);\n\n    const t = setTimeout(() => setShow(false), 3000);\n    return () => {\n      cancelled = true;\n      clearTimeout(t);\n    };\n  }, []);\n\n  if (!show) return null;\n  const lang = langSignal.value;\n  const tpl = STRINGS[lang][\"welcome.back\"];\n  const message = tpl.replace(\n    \"{firstName}\",\n    name || t(\"welcomeBackToast.nameFallback\"),\n  );\n\n  return (\n    <div\n      role=\"status\"\n      aria-live=\"polite\"\n      style={`position:fixed;top:18px;right:18px;z-index:9999;padding:10px 16px 10px 14px;display:inline-flex;align-items:center;gap:10px;background:#fff;border:1px solid rgba(0,0,0,0.06);border-radius:999px;box-shadow:0 6px 24px rgba(0,0,0,0.10),0 1px 3px rgba(0,0,0,0.04);font-family:inherit;font-size:14px;font-weight:600;color:${TEAL};animation:pm-toast-in 280ms cubic-bezier(0.34,1.56,0.64,1) both;pointer-events:none`}\n    >\n      <span\n        aria-hidden=\"true\"\n        style={`display:inline-flex;width:22px;height:22px;border-radius:50%;background:${PINK};color:#fff;align-items:center;justify-content:center;font-size:13px`}\n      >\n        👋\n      </span>\n      <span>{message}</span>\n      <style>\n        {`\n        @keyframes pm-toast-in {\n          0%   { opacity: 0; transform: translateY(-6px) scale(0.96); }\n          100% { opacity: 1; transform: translateY(0)    scale(1); }\n        }\n        @media (prefers-reduced-motion: reduce) {\n          [role=status][style*=\"pm-toast-in\"] { animation: none !important; }\n        }\n      `}\n      </style>\n    </div>\n  );\n}\n", "sourceName": "WelcomeBackToast.tsx", "checklist": "# Capture checklist — WelcomeBackToast\n\n**Theme:** light only.\n**Auth:** log in with dev master OTP `000000`.\n**Route:** `http://localhost:5280/dashboard?welcome=back` — the toast ONLY shows\nwhen `?welcome=back` is present (it normally arrives via the `/verify` →\n`/dashboard?welcome=back` returning-user redirect).\n\n## Driving the toast (it self-strips the param + auto-dismisses)\n- **To make it appear:** navigate to\n  `http://localhost:5280/dashboard?welcome=back`. The island fetches `/api/me`,\n  shows the pill, then after ~50ms `history.replaceState`s the URL to drop\n  `welcome` and after 3000ms removes the pill.\n- **Cache-bust the openurl** if testing on iOS Simulator (param is stripped, so a\n  repeat openurl to the same URL must actually re-navigate).\n- It will NOT re-show on refresh (param was stripped). Re-add `?welcome=back` to\n  the URL each time you capture.\n- **Timing:** capture within the 3s window. Drive it fast or temporarily widen\n  the dismiss timer if you need a longer hold for a clean shot.\n\n## Element(s) to crop\n- The top-right pill: white, pill radius (999px), soft shadow, teal (#144852)\n  text, a pink (#FF6B6B) 22px 👋 circle, message \"Welcome back, {firstName}.\".\n  Crop the top-right corner of the viewport including some margin so the\n  `top:18px right:18px` offset is visible.\n\n## Viewports\n- No own breakpoints (fixed top-right at all widths). Shoot:\n  - **1280px** (desktop).\n  - **390px** (mobile — VERIFY a long localized name doesn't overflow the right\n    edge; there is no max-width).\n\n## Transient states to drive\n1. **named** — a seeded user with a name: \"Welcome back, Raphael.\" (first token\n   only).\n2. **fallback-name** — a user with no name set: \"Welcome back, friend.\"\n   (`welcomeBackToast.nameFallback`).\n3. **entrance** — film the `pm-toast-in` drop-in: opacity 0→1 +\n   translateY(-6px) scale(.96) → settled, over 280ms bounce.\n4. **auto-dismiss** — confirm the pill DISAPPEARS at ~3s. NOTE: there is NO exit\n   animation — it pops out abruptly (capture the before/after to document the\n   missing fade-out).\n5. **no-param** — load plain `/dashboard` (no query): the pill never appears.\n   Capture a clean dashboard to evidence the gate.\n6. **param-stripped** — after the toast shows, confirm the address bar no longer\n   has `?welcome=back` (replaceState fired) and a refresh shows no toast.\n\n## Motion + reduced-motion\n- Film the 280ms entrance, then re-shoot with\n  **`prefers-reduced-motion: reduce`**: the island's own guard nulls\n  `pm-toast-in`, so the pill appears instantly with no drop-in. Capture the\n  static version.\n\n## Notes\n- `lang=es` (\"Bienvenido de nuevo, {firstName}.\") — capture both languages if\n  exercising i18n; the template comes from `lib/lang.ts` STRINGS, not the JSON\n  dict.\n- NO fabricated screenshots — drive the real toast via the `?welcome=back` URL.\n"}]}, {"name": "invoices", "pageMd": "# Page — /invoices\n\nReceivables view. Despite the URL, the page **reuses the Quotes code path**:\nsame `.qph/.qkpi/.qtrack/.qcard/.qcards/.qlay` classes (loaded from\n`quotes.css`), with the copy + stage moods swapped for invoice semantics. The\nwhole interactive surface is a single client island.\n\n## Purpose\nShow the contractor every receivable, bucketed by lifecycle stage, with a\nforecast headline (\"$X expected this week\"), a 4-cell KPI strip, and per-invoice\nflip cards that expose the mutation actions (send / nudge / confirm-received /\nfinish-draft / mute / discount / change-order). Plus a \"New invoice\" modal for\nstandalone (no-contract) invoices.\n\n## App-shell composition (order)\nRoute file: `routes/invoices/index.tsx` (copied to `js/index.tsx`). It is a\n`define.page` SSR route — NOT an island — that resolves `ctx.state.user`, builds\na localized greeting, and renders:\n\n```\n<Head> … </Head>\n<div class=\"app\">                         ← app-shell grid (dashboard.css)\n  <DashSidebar active=\"invoices\" />       ← SHARED island (shared-components/dash-sidebar)\n  <main class=\"main\">\n    <DashTopbar greetingDate greetingName/>← SHARED island (shared-components/dash-topbar)\n    <div class=\"content\">\n      <InvoicesPage />                    ← THE island (components/invoices-page)\n    </div>\n  </main>\n</div>\n```\n\nOrder: Sidebar, then main → Topbar → content → InvoicesPage.\n\n## `<Head>` — title + CSS\n- `<title>` = `tFor(lang, \"invoicesPage.docTitle\")`.\n- Stylesheets (keys): `css-dashboard` → `/dashboard.css`, `css-quotes` →\n  `/quotes.css`. **There is NO `/invoices.css`** and the page does **not** load\n  `/payments.css`. (Contrast: /payments loads all three + payments.css.)\n- Tokens + the `.app/.main/.content` shell come from dashboard.css; every\n  `.q*` class comes from quotes.css. See `../../design-tokens.md`.\n\n## SSR data\nThe route SSRs **only** `user.language` (→ doc title + greeting) and the\ngreeting date. **No invoice data is server-rendered.** All invoice/customer/\nforecast data is fetched client-side by the island on mount (see\n`components/invoices-page/invoices-page.md`). First paint = Skeletons.\n\n## Sections (top → bottom, inside `.content`)\n1. **InvoicesHero** (`.qph`) — forecast/outstanding headline + sub + at-risk\n   line + CTA row (New invoice button, Export-CSV link). 5 headline variants.\n   → `components/invoices-hero`.\n2. **NewInvoiceModal** — conditional overlay when the hero's New button is\n   clicked. → `components/new-invoice-modal`.\n3. **InvoicesKpis** (`.qkpi`) — Overdue / Out / Drafting / Paid-this-month.\n   → `components/invoices-kpis`.\n4. **`.qlay` → single column** of SIX collapsible **QuoteTrack** sections\n   (SHARED — `shared-components/quote-track`), each filled with a `.qcards` grid\n   of **InvoiceCard** flip cards (or an `EmptyTrack` hint):\n   - `01` Overdue · needs a poke (defaultOpen, sorted daysOverdue desc)\n   - `02` Awaiting confirmation (defaultOpen, sorted claimedAt desc) — the\n     claim→confirm bucket\n   - `03` Out for payment (defaultOpen, sorted daysIn desc)\n   - `04` Upcoming (defaultOpen=false, scheduled invoices, sorted scheduledFor)\n   - `05` Drafting (defaultOpen=false)\n   - `06` Paid this month (defaultOpen=false)\n   → cards in `components/invoice-card`.\n\n> Note: the island file's top comment lists 4 tracks; the live code renders 6\n> (it grew). The `.qlay` second (320px) track is empty on /invoices — no\n> `aside.qside` is rendered, so the rail column collapses.\n\n## Build order\n1. Confirm tokens (dashboard.css) + quotes.css `.q*` classes exist.\n2. Mount the SHARED app-shell (DashSidebar/DashTopbar) + Skeletons + QuoteTrack.\n3. Build `EnrichedInvoice` derivation + stage bucketing (island).\n4. InvoicesHero (5 variants) → InvoicesKpis → the 6 QuoteTracks.\n5. InvoiceCard flip card (front + back + adjust panel + all mutations).\n6. NewInvoiceModal.\n\n## Honest-empty\n- Truly-empty (0 invoices): hero shows the empty headline; KPIs all $0/0;\n  every track shows its `EmptyTrack` hint.\n- Each track independently renders `EmptyTrack` when its bucket is empty.\n\n## Data-shape hazards (flagged)\n- **Aging / urgency bucketing is recomputed client-side every render** over the\n  whole invoice list (`enrich()` → `daysOverdue`/`daysIn`/`stage`, then 6\n  `.filter().sort()` passes). On a real backend the per-invoice `urgency` and\n  the dashboard `agingBuckets` are date-derived scans over all open invoices —\n  see `../../data-model.md` §5.8. **[bucket on write / scheduled recompute].**\n- The `/api/invoices/forecast/this-week` call is fire-and-forget and silently\n  falls back to the legacy outstanding-total headline on 404.\n\n## Anti-patterns (see invoice-card.md / new-invoice-modal.md for fixes)\n- The page is a **whole-page island** that fetches on mount with frozen SSR\n  props (only `lang`). Every mutation in InvoiceCard + the modal ends in\n  `globalThis.location.reload()` — a full-page reload instead of a local\n  re-fetch/PRG. Flagged per-component.\n", "components": [{"name": "invoice-card", "component": "InvoiceCard", "spec": "# InvoiceCard\n\nThe per-invoice flip card (`.qcard`). Front = mood gradient + status + avatar +\namount + subline + CTA. Back = read + an \"Adjust this invoice\" panel (discount +\nchange-order) + an action row. This is where every invoice mutation lives.\n\n## 1. Classification & behavior\n- **Bucket:** local component of the InvoicesPage island\n  (`islands/InvoicesPage.tsx` lines 821–1409). Not its own island — it renders\n  inside the page island, so it shares that hydration boundary.\n- **Interaction tier:** **island-child, client-state + imperative mutations.**\n  No form/PRG; no Fresh Partial. Each action is a hand-rolled `fetch` then a\n  full-page reload.\n- **Client state owned:** `flipped`, `busy`, `adjustOpen`, `discountDollars`,\n  `coDesc`, `coDollars`, `coLink`, `adjErr`, `changeOrders` (lazy-loaded when\n  the adjust panel first opens), `copiedCoId`.\n- **Server actions + flash (all `fetch`, all end in `location.reload()` —\n  FLAG):**\n  | action | endpoint | when (stage) |\n  |---|---|---|\n  | confirm received | `POST /api/invoices/:id/confirm-payment` | claimed (CTA) |\n  | reject claim | `POST /api/invoices/:id/reject-claim` | claimed (back) |\n  | send text | `POST /api/invoices/:id/text` | overdue CTA / \"Text client\" |\n  | send now (both channels) | `POST …/email` + `POST …/text` (allSettled) | scheduled CTA |\n  | finish draft | `PUT /api/invoices/:id {status:\"sent\",issuedDate?}` then email+text | drafting CTA |\n  | toggle mute | `PUT /api/invoices/:id {remindersMuted:!x}` | overdue/out (back) |\n  | apply discount | `POST /api/invoices/:id/discount {discountCents}` | adjust panel |\n  | create change-order | `POST /api/invoices/:id/change-orders {description,deltaAmountCents}` | adjust panel |\n  | open invoice | `window.open(\"/i/:id\")` (new tab) | out/paid CTA + \"Open\" |\n  | copy CO link | `navigator.clipboard.writeText(origin+\"/co/:id\")` | per pending CO |\n  | load change-orders | `GET /api/invoices/:id/change-orders` | adjust panel open |\n  - **No flash/toast.** Success = the page reloads; failure on discount/CO sets\n    `adjErr` text; other failures are silent (the reload simply doesn't happen).\n- **Data source per region:**\n  - card content ← the `EnrichedInvoice` prop (derived in the island).\n  - `changeOrders` ← lazy `GET …/change-orders` on first adjust-open; `null`\n    until loaded; `[]`/list after. **honest-empty:** list block only renders\n    when `length>0`.\n- **Liveness:** none; full reload after each mutation.\n- **Data-shape hazards:**\n  - `stage` (the 6-way `overdue|out|claimed|scheduled|drafting|paid`) is the\n    island's derived bucket, NOT the stored `status` — see invoices-page.md §1.\n  - `invoiceRef` is cosmetic: `INV-` + first 6 chars of the id, uppercased.\n  - change-order `deltaAmountCents` rendered with `fmtMoneyExact` + a manual\n    `+`/`−` sign and `Math.abs`.\n- **Anti-patterns (FLAG):**\n  - **`globalThis.location.reload()` after every mutation.** Loses scroll, the\n    open track states (well, those persist via localStorage), the flip state,\n    and re-runs all three page fetches. **Fix:** a server-action form +\n    PRG, OR lift a `refetchInvoices()` callback from the island and re-pull\n    `/invoices` to patch just this card's bucket. The card is already a client\n    component, so the cleanest path is the lifted-callback refetch.\n  - `confirm-payment`/`reject-claim`/`discount`/`change-orders` hit\n    `/api/invoices/*` routes that are NOT in the documented client surface\n    (`../../../data-model.md` only lists `GET /invoices`, `POST /invoices`,\n    and the public claim) — these are front-end API routes layered on top.\n\n## 2. Anatomy\n```\n<article class=\"qcard [qcard--flipped]\" style=\"--mood-from/-to/-shadow/-status\">\n  <div class=\"qcard__mood\">\n    <div class=\"qcard__numeral\">{idx+1 padded}</div>\n    <div class=\"qcard__status\"><span dot/> {moodLabel}</div>\n  </div>\n  <div class=\"qcard__av\">{initials}</div>\n  <div class=\"qcard__body\">\n    <div class=\"qcard__client-name\">{client} · {invoiceRef}</div>\n    <h3 class=\"qcard__title\">{fmtMoney(amount)}</h3>     ← amount IS the title\n    <p class=\"qcard__story\">{subline}</p>\n  </div>\n  <div class=\"qcard__foot\">\n    <button class=\"qcard__cta\" data-cy=\"invoice-cta-{stage}\">{cta} →</button>\n    <div class=\"qcard__val-wrap\"> Due|Cleared + date </div>\n  </div>\n  <div class=\"qcard__back\" aria-hidden={!flipped}>\n    <div class=\"qcard__back-head\"> close-X · eyebrow · big amount · moodLabel </div>\n    <div class=\"qcard__back-body\">\n      <p class=\"qcard__read\"> stage-specific reading </p>\n      {stage!==\"paid\" && <button>Adjust this invoice ▾</button>\n        + inline-styled panel: discount input+Apply,\n          change-order desc+amount+Create-link, coLink, CO list w/ copy-link,\n          adjErr }\n    </div>\n    <div class=\"qcard__back-foot\">   ← up to 4 buttons\n      {cta} · Open · (claimed: Didn't-get-it) · (overdue/out: Mute toggle | else Text client)\n    </div>\n  </div>\n</article>\n```\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `inv` | `EnrichedInvoice` (required) | — | object | no |\n| `idx` | number (required) | — | number | no |\n| `now` | Date (required) | — | (date) | no |\n| `lang` | `Lang` (required) | — | select | no |\n\n`EnrichedInvoice` adds to `Invoice`: `client`, `initials`, `invoiceRef`,\n`daysOverdue`, `daysIn`, `stage`.\n\n## 4. States → cases (driven by `inv.stage` + flip + adjust)\n| state | meaning | case |\n|---|---|---|\n| overdue (front) | red mood, \"Send nudge\", daysOverdue subline | `cases/overdue/overdue.json` |\n| out (front) | teal mood, \"View invoice\" | `cases/out/out.json` |\n| claimed (front) | amber mood, \"Okay, I got it\", method/ref subline | `cases/claimed/claimed.json` |\n| scheduled (front) | purple mood, \"Send now\" | `cases/scheduled/scheduled.json` |\n| drafting (front) | brown mood, \"Finish + send\" | `cases/drafting/drafting.json` |\n| paid (front) | green mood, \"View receipt\", paid date | `cases/paid/paid.json` |\n| flipped | back face, adjust panel collapsed | `cases/flipped/flipped.json` |\n| adjust-open | back face, discount + CO panel + CO list | `cases/adjust-open/adjust-open.json` |\n| busy | mid-mutation, CTA shows \"…\", buttons disabled | `cases/busy/busy.json` |\n\n## 5. Events\n- `ev.expect(e => e.source===\"article.qcard\" && e.type===\"click\")` when not\n  flipped and target not in `.qcard__cta,.qcard__back` → `flipped=true`.\n- `…\"button.qcard__cta[data-cy=invoice-cta-{stage}]\"…click` → `ctaAction`\n  (dispatches by stage; see table §1).\n- `…\"button.qcard__back-close\"…click` → `flipped=false`.\n- `…button \"Adjust this invoice\"…click` → toggles `adjustOpen`; first open\n  triggers `loadChangeOrders()`.\n- Discount Apply / CO Create-link / per-CO Copy-link → their handlers.\n- back-foot Mute toggle / Text client / Didn't-get-it → their handlers.\n\n## 6. Motion (extracted)\n- **Flip:** `.qcard__back` `transform: translateY(8px) scale(.98)→\n  translateY(0) scale(1)` over `380ms var(--ease-bounce)` + `opacity 0→1` over\n  `240ms var(--ease-out)`. Pointer-events gated on `.qcard--flipped`.\n- **Hover lift:** `.qcard:hover` `translateY(-4px)` + bigger mood-tinted shadow,\n  `320ms` bounce.\n- **Status dot:** `q-pulse-dot` opacity 1→.4→1 over `2.4s` infinite.\n- **CTA arrow:** inline `transition:transform 240ms` (the `→` span);\n  `.qcard__cta:hover` widens `gap 6→9px`.\n- **Jank:** none notable — transforms/opacity only (GPU-friendly). The pulsing\n  dot is the only always-on animation. Reduced-motion via global clamp (no\n  component-local guard).\n\n## 7. Responsive\n- No own `@media`; inherits `.qcards` grid (`auto-fill minmax(320px,1fr)`,\n  collapses to 1 col `≤768px`). The adjust panel + back-foot are inline-styled\n  and flex/grid-wrap.\n\n## 8. A11y\n- The whole `<article>` is a click target (flip) but is not a button / not\n  focusable / no `aria-expanded` for the flip — keyboard users can't flip.\n  **Fix:** add a focusable flip affordance.\n- `.qcard__back aria-hidden={!flipped}` correctly hides the back from AT while\n  collapsed; close-X has `aria-label`.\n- The discount/CO inputs lack `<label for>` (visually-labeled divs only).\n\n## 9. Used on\n`/invoices` only (rendered by InvoicesPage in all 6 tracks). Reuses the Quotes\n`.qcard*` CSS verbatim — see `css/invoice-card.css`.\n", "fixture": "{\n  \"category\": \"invoices\",\n  \"folder\": \"invoice-card\",\n  \"controls\": {\n    \"inv\": { \"type\": \"json\", \"value\": {} },\n    \"idx\": { \"type\": \"number\", \"value\": 0 },\n    \"now\": { \"type\": \"text\", \"value\": \"2026-06-18T12:00:00.000Z\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "adjust-open.json", "content": "{\n  \"_name\": \"Adjust open — back face, discount + change-order panel expanded, CO list loaded\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"_signals\": { \"flipped\": true, \"adjustOpen\": true },\n  \"_mocks\": {\n    \"fetch:/api/invoices/inv3344bb/change-orders\": [\n      { \"id\": \"co-9001\", \"description\": \"Added gutter guards (both buildings)\", \"deltaAmountCents\": 48000, \"status\": \"pending\", \"createdAt\": \"2026-06-16T10:00:00.000Z\" },\n      { \"id\": \"co-9000\", \"description\": \"Removed pressure-wash line item\", \"deltaAmountCents\": -15000, \"status\": \"approved\", \"createdAt\": \"2026-06-14T10:00:00.000Z\" }\n    ]\n  },\n  \"inv\": {\n    \"id\": \"inv3344bb\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-309\",\n    \"customerId\": \"cust-gh\",\n    \"amount\": 580000,\n    \"issuedDate\": \"2026-06-15\",\n    \"dueDate\": \"2026-07-15\",\n    \"status\": \"sent\",\n    \"createdAt\": \"2026-06-15T09:00:00.000Z\",\n    \"updatedAt\": \"2026-06-15T09:00:00.000Z\",\n    \"client\": \"Greenleaf HOA\",\n    \"initials\": \"GH\",\n    \"invoiceRef\": \"INV-INV334\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 3,\n    \"stage\": \"out\"\n  }\n}\n"}, {"name": "busy.json", "content": "{\n  \"_name\": \"Busy — mid-mutation on a claimed card, CTA shows '…', all buttons disabled\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"_signals\": { \"busy\": true },\n  \"inv\": {\n    \"id\": \"inv1122aa\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-310\",\n    \"customerId\": \"cust-ml\",\n    \"amount\": 96000,\n    \"issuedDate\": \"2026-06-12\",\n    \"dueDate\": \"2026-06-26\",\n    \"status\": \"claimed\",\n    \"createdAt\": \"2026-06-12T09:00:00.000Z\",\n    \"updatedAt\": \"2026-06-17T15:30:00.000Z\",\n    \"paymentIntent\": {\n      \"method\": \"zelle\",\n      \"amount\": 96000,\n      \"reference\": \"ZL-88421\",\n      \"claimedAt\": \"2026-06-17T15:30:00.000Z\",\n      \"claimedBy\": \"Marcus Lin\"\n    },\n    \"client\": \"Marcus Lin\",\n    \"initials\": \"ML\",\n    \"invoiceRef\": \"INV-INV112\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 6,\n    \"stage\": \"claimed\"\n  }\n}\n"}, {"name": "claimed.json", "content": "{\n  \"_name\": \"Claimed (front) — amber mood, 'Okay, I got it', 'Customer paid via Zelle · ref ZL-88421'\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"inv\": {\n    \"id\": \"inv1122aa\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-310\",\n    \"customerId\": \"cust-ml\",\n    \"amount\": 96000,\n    \"issuedDate\": \"2026-06-12\",\n    \"dueDate\": \"2026-06-26\",\n    \"status\": \"claimed\",\n    \"createdAt\": \"2026-06-12T09:00:00.000Z\",\n    \"updatedAt\": \"2026-06-17T15:30:00.000Z\",\n    \"paymentIntent\": {\n      \"method\": \"zelle\",\n      \"amount\": 96000,\n      \"reference\": \"ZL-88421\",\n      \"claimedAt\": \"2026-06-17T15:30:00.000Z\",\n      \"claimedBy\": \"Marcus Lin\"\n    },\n    \"client\": \"Marcus Lin\",\n    \"initials\": \"ML\",\n    \"invoiceRef\": \"INV-INV112\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 6,\n    \"stage\": \"claimed\"\n  }\n}\n"}, {"name": "drafting.json", "content": "{\n  \"_name\": \"Drafting (front) — brown mood, 'Finish + send', 'Draft started Jun 18'\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"inv\": {\n    \"id\": \"inv7788dd\",\n    \"userId\": \"u1\",\n    \"customerId\": \"cust-cc\",\n    \"amount\": 142500,\n    \"dueDate\": \"2026-07-18\",\n    \"status\": \"draft\",\n    \"createdAt\": \"2026-06-18T10:00:00.000Z\",\n    \"updatedAt\": \"2026-06-18T10:00:00.000Z\",\n    \"client\": \"Cobblestone Cafe\",\n    \"initials\": \"CC\",\n    \"invoiceRef\": \"INV-INV778\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 0,\n    \"stage\": \"drafting\"\n  }\n}\n"}, {"name": "flipped.json", "content": "{\n  \"_name\": \"Flipped — back face on an overdue card, adjust panel collapsed\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"_signals\": { \"flipped\": true, \"adjustOpen\": false },\n  \"inv\": {\n    \"id\": \"inv0a1b2c\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-301\",\n    \"customerId\": \"cust-mg\",\n    \"amount\": 420000,\n    \"issuedDate\": \"2026-05-04\",\n    \"dueDate\": \"2026-05-18\",\n    \"status\": \"sent\",\n    \"remindersMuted\": false,\n    \"createdAt\": \"2026-05-04T09:00:00.000Z\",\n    \"updatedAt\": \"2026-05-04T09:00:00.000Z\",\n    \"client\": \"Maple Grove Apartments\",\n    \"initials\": \"MA\",\n    \"invoiceRef\": \"INV-INV0A1\",\n    \"daysOverdue\": 31,\n    \"daysIn\": 45,\n    \"stage\": \"overdue\"\n  }\n}\n"}, {"name": "out.json", "content": "{\n  \"_name\": \"Out for payment (front) — teal mood, 'View invoice', 'Out 3d · due Jul 15'\",\n  \"idx\": 1,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"inv\": {\n    \"id\": \"inv3344bb\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-309\",\n    \"customerId\": \"cust-gh\",\n    \"amount\": 580000,\n    \"issuedDate\": \"2026-06-15\",\n    \"dueDate\": \"2026-07-15\",\n    \"status\": \"sent\",\n    \"createdAt\": \"2026-06-15T09:00:00.000Z\",\n    \"updatedAt\": \"2026-06-15T09:00:00.000Z\",\n    \"client\": \"Greenleaf HOA\",\n    \"initials\": \"GH\",\n    \"invoiceRef\": \"INV-INV334\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 3,\n    \"stage\": \"out\"\n  }\n}\n"}, {"name": "overdue.json", "content": "{\n  \"_name\": \"Overdue (front) — red mood, 'Send nudge', '14d overdue · due May 18'\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"inv\": {\n    \"id\": \"inv0a1b2c\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-301\",\n    \"customerId\": \"cust-mg\",\n    \"amount\": 420000,\n    \"issuedDate\": \"2026-05-04\",\n    \"dueDate\": \"2026-05-18\",\n    \"status\": \"sent\",\n    \"installmentIndex\": 2,\n    \"installmentTotal\": 3,\n    \"createdAt\": \"2026-05-04T09:00:00.000Z\",\n    \"updatedAt\": \"2026-05-04T09:00:00.000Z\",\n    \"client\": \"Maple Grove Apartments\",\n    \"initials\": \"MA\",\n    \"invoiceRef\": \"INV-INV0A1\",\n    \"daysOverdue\": 31,\n    \"daysIn\": 45,\n    \"stage\": \"overdue\"\n  }\n}\n"}, {"name": "paid.json", "content": "{\n  \"_name\": \"Paid (front) — green mood, 'View receipt', 'Paid Jun 14', Cleared date in foot\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"inv\": {\n    \"id\": \"invbbccff\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-275\",\n    \"customerId\": \"cust-or\",\n    \"amount\": 190000,\n    \"issuedDate\": \"2026-06-01\",\n    \"dueDate\": \"2026-06-15\",\n    \"status\": \"paid\",\n    \"paidAt\": \"2026-06-14T13:00:00.000Z\",\n    \"createdAt\": \"2026-06-01T09:00:00.000Z\",\n    \"updatedAt\": \"2026-06-14T13:00:00.000Z\",\n    \"client\": \"Ortega Rentals\",\n    \"initials\": \"OR\",\n    \"invoiceRef\": \"INV-INVBBC\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 17,\n    \"stage\": \"paid\"\n  }\n}\n"}, {"name": "scheduled.json", "content": "{\n  \"_name\": \"Scheduled (front) — purple mood, 'Send now', 'Scheduled to send 2026-07-23'\",\n  \"idx\": 0,\n  \"now\": \"2026-06-18T12:00:00.000Z\",\n  \"lang\": \"en\",\n  \"inv\": {\n    \"id\": \"inv5566cc\",\n    \"userId\": \"u1\",\n    \"contractId\": \"ct-301\",\n    \"customerId\": \"cust-mg\",\n    \"amount\": 420000,\n    \"dueDate\": \"2026-07-30\",\n    \"status\": \"scheduled\",\n    \"scheduledFor\": \"2026-07-23\",\n    \"installmentIndex\": 3,\n    \"installmentTotal\": 3,\n    \"createdAt\": \"2026-05-04T09:00:00.000Z\",\n    \"updatedAt\": \"2026-05-04T09:00:00.000Z\",\n    \"client\": \"Maple Grove Apartments\",\n    \"initials\": \"MA\",\n    \"invoiceRef\": \"INV-INV556\",\n    \"daysOverdue\": 0,\n    \"daysIn\": 0,\n    \"stage\": \"scheduled\"\n  }\n}\n"}], "css": "/* ============================================================================\n   InvoiceCard — the `.qcard` flip card. Copied verbatim from static/quotes.css\n   (lines 199–659; the card shares the Quotes card styles). Tokens from\n   dashboard.css. Mood gradient is driven by 4 CSS custom props the island sets\n   inline per card: --mood-from / --mood-to / --mood-shadow / --mood-status.\n\n   The InvoiceCard's in-card \"Adjust this invoice\" panel (discount + change-\n   order) is INLINE-STYLED in the island (no classes) — see\n   ../js/InvoiceCard.tsx pointer / InvoicesPage.tsx lines 1209–1359.\n   ============================================================================ */\n\n.qcards {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));\n  gap: 16px;\n}\n\n.qcard {\n  position: relative;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  overflow: visible;\n  cursor: pointer;\n  box-shadow:\n    0 1px 0 rgba(26, 83, 92, 0.04),\n    0 8px 22px -14px rgba(26, 83, 92, 0.18);\n  transition:\n    transform 320ms var(--ease-bounce),\n    box-shadow 320ms var(--ease-out);\n  display: flex;\n  flex-direction: column;\n}\n.qcard:hover {\n  transform: translateY(-4px);\n  box-shadow:\n    0 2px 0 rgba(26, 83, 92, 0.05),\n    0 22px 42px -18px var(--mood-shadow, rgba(26, 83, 92, 0.28));\n}\n\n/* ---- Mood block (gradient header w/ giant numeral + status pill) ---- */\n.qcard__mood {\n  position: relative;\n  height: 138px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  border-radius: var(--radius-xl) var(--radius-xl) 0 0;\n  overflow: hidden;\n}\n.qcard__mood::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 18% 22%, rgba(255, 255, 255, 0.18) 0, transparent 35%),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n  pointer-events: none;\n}\n.qcard__numeral {\n  position: absolute;\n  bottom: 4px;\n  right: -2px;\n  font-family: var(--font-heading);\n  font-weight: 900;\n  font-size: 88px;\n  line-height: 1;\n  color: rgba(255, 255, 255, 0.16);\n  letter-spacing: -0.04em;\n  pointer-events: none;\n  user-select: none;\n}\n.qcard__status {\n  position: absolute;\n  top: 14px;\n  left: 14px;\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 5px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.92);\n  backdrop-filter: blur(4px);\n  color: var(--mood-status, var(--brand-teal));\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  z-index: 2;\n}\n.qcard__status-dot {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: currentColor;\n  animation: q-pulse-dot 2.4s infinite;\n}\n@keyframes q-pulse-dot {\n  0%, 100% { opacity: 1; }\n  50% { opacity: 0.4; }\n}\n/* .qcard__opens — used by PaymentCard for the method pill; defined here too\n   since it ships in the same quotes.css block. */\n.qcard__opens {\n  position: absolute;\n  top: 12px;\n  right: 12px;\n  display: inline-flex;\n  align-items: center;\n  gap: 5px;\n  padding: 6px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.18);\n  color: rgba(255, 255, 255, 0.95);\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.06em;\n  backdrop-filter: blur(3px);\n  z-index: 2;\n}\n\n/* ---- Avatar (overlaps mood + body) ---- */\n.qcard__av {\n  position: absolute;\n  left: 22px;\n  top: 92px;\n  width: 86px;\n  height: 86px;\n  border-radius: 22px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 28px;\n  letter-spacing: 0.02em;\n  z-index: 4;\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.28),\n    0 12px 24px -8px rgba(0, 0, 0, 0.32);\n}\n\n/* ---- Front body + foot ---- */\n.qcard__body { padding: 50px 22px 0; }\n.qcard__client-name {\n  font-family: var(--font-body);\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin: 0 0 4px;\n  letter-spacing: 0.02em;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qcard__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 19px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  line-height: 1.2;\n  margin: 0 0 14px;\n  overflow: hidden;\n  display: -webkit-box;\n  -webkit-line-clamp: 2;\n  -webkit-box-orient: vertical;\n}\n.qcard__story {\n  font-family: var(--font-body);\n  font-size: 14.5px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  text-wrap: pretty;\n  margin: 0;\n  display: -webkit-box;\n  -webkit-line-clamp: 4;\n  -webkit-box-orient: vertical;\n  overflow: hidden;\n}\n.qcard__foot {\n  margin-top: 18px;\n  padding: 14px 22px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  display: grid;\n  grid-template-columns: 1fr auto;\n  gap: 12px;\n  align-items: center;\n}\n.qcard__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 0;\n  border: none;\n  background: transparent;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--brand-pink);\n  cursor: pointer;\n  text-align: left;\n  line-height: 1.3;\n  transition: gap var(--dur-fast) var(--ease-out);\n}\n.qcard__cta:hover { gap: 9px; }\n.qcard__val-wrap { text-align: right; }\n.qcard__val-lbl {\n  font-family: var(--font-heading);\n  font-size: 9.5px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 1px;\n}\n.qcard__val-num {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  letter-spacing: -0.01em;\n  color: var(--brand-teal);\n}\n\n/* ---- Back face (flips in on .qcard--flipped) ---- */\n.qcard__back {\n  position: absolute;\n  inset: 0;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  z-index: 10;\n  display: flex;\n  flex-direction: column;\n  overflow: hidden;\n  transform: translateY(8px) scale(0.98);\n  opacity: 0;\n  pointer-events: none;\n  transition: transform 380ms var(--ease-bounce), opacity 240ms var(--ease-out);\n  box-shadow: 0 22px 50px -22px var(--mood-shadow, rgba(26, 83, 92, 0.4));\n}\n.qcard--flipped .qcard__back {\n  transform: translateY(0) scale(1);\n  opacity: 1;\n  pointer-events: auto;\n}\n.qcard__back-head {\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  color: #fff;\n  padding: 18px 22px 16px;\n  position: relative;\n  overflow: hidden;\n}\n.qcard__back-head::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 18% 22%, rgba(255, 255, 255, 0.18) 0, transparent 35%),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n  pointer-events: none;\n}\n.qcard__back-eyebrow {\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.1em;\n  text-transform: uppercase;\n  opacity: 0.78;\n  margin-bottom: 6px;\n}\n.qcard__back-big {\n  font-family: var(--font-heading);\n  font-weight: 900;\n  font-size: 36px;\n  line-height: 1;\n  letter-spacing: -0.02em;\n  margin: 0;\n}\n.qcard__back-big small {\n  font-size: 15px;\n  font-weight: 700;\n  opacity: 0.78;\n  margin-left: 4px;\n  letter-spacing: 0;\n}\n.qcard__back-close {\n  position: absolute;\n  top: 12px;\n  right: 12px;\n  width: 28px;\n  height: 28px;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.22);\n  color: #fff;\n  border: none;\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  z-index: 2;\n  transition: background var(--dur-fast) var(--ease-out);\n}\n.qcard__back-close:hover { background: rgba(255, 255, 255, 0.38); }\n.qcard__back-body {\n  padding: 18px 22px;\n  flex: 1 1 auto;\n  overflow-y: auto;\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n}\n.qcard__read {\n  font-family: var(--font-body);\n  font-size: 14px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  background: linear-gradient(\n    180deg,\n    rgba(255, 107, 107, 0.06) 0%,\n    rgba(255, 107, 107, 0.02) 100%\n  );\n  border-left: 3px solid var(--brand-pink);\n  border-radius: 0 10px 10px 0;\n  padding: 12px 14px;\n  text-wrap: pretty;\n  margin: 4px 0 0;\n}\n.qcard__read em { font-style: normal; color: var(--brand-pink); font-weight: 700; }\n.qcard__back-foot {\n  display: grid;\n  grid-template-columns: repeat(4, 1fr);\n  gap: 6px;\n  padding: 12px 14px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  background: rgba(26, 83, 92, 0.02);\n}\n.qcard__back-foot button {\n  background: #fff;\n  border: 1px solid rgba(26, 83, 92, 0.12);\n  border-radius: 10px;\n  padding: 9px 6px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 11px;\n  color: var(--brand-teal);\n  cursor: pointer;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 5px;\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    color var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n.qcard__back-foot button:hover {\n  border-color: var(--brand-pink);\n  color: var(--brand-pink);\n  transform: translateY(-1px);\n}\n\n@media (max-width: 768px) {\n  .qcards { grid-template-columns: 1fr; }\n}\n", "cssName": "invoice-card.css", "source": "/* SOURCE POINTER — the full verbatim source for InvoiceCard (and its\n * STAGE_MOOD / ChangeOrderRow / CO_CHIP_COLOR helpers) is too large to\n * usefully duplicate here. It lives, copied verbatim, in:\n *\n *   ../../invoices-page/js/InvoicesPage.tsx\n *     · STAGE_MOOD         lines 206–259\n *     · ChangeOrderRow     lines 807–813\n *     · CO_CHIP_COLOR      lines 815–819\n *     · function InvoiceCard(...)  lines 821–1409\n *     · methodLabel / fmtDate / initialsOf / enrich / EnrichedInvoice\n *       (used by the card) lines 63–204\n *\n * This component is a CLIENT-ONLY flip card with a full mutation surface\n * (confirm-received / reject-claim / send / finish-draft / mute / discount /\n * change-order). Every mutation calls a fetch then `globalThis.location\n * .reload()` — see invoice-card.md §1 for the anti-pattern flag + fix. Read\n * the canonical file above for the exact JSX/handlers; do not re-author from\n * memory. */\n// (Pointer only — no re-export. `InvoiceCard` is a non-exported local\n//  function inside InvoicesPage.tsx; the island's default export is the page.)\nexport {};\n", "sourceName": "InvoiceCard.tsx", "checklist": "# Capture checklist — InvoiceCard\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/invoices` — the `.qcard` flip cards live inside the\n  `.qcards` grid of each QuoteTrack. Different stages live in different tracks:\n  - Overdue → Track 01 \"Overdue · needs a poke\"\n  - Awaiting confirmation (claimed) → Track 02 (data-cy=awaiting-confirmation-track)\n  - Out → Track 03 \"Out for payment\"\n  - Scheduled → Track 04 \"Upcoming\" (data-cy=upcoming-track, defaultOpen=false)\n  - Drafting → Track 05\n  - Paid this month → Track 06 (defaultOpen=false)\n\n## Viewports\n- **1280px** — `.qcards` grid `auto-fill minmax(320px,1fr)` (multi-col).\n- **720px** — at ≤768 (own `@media` in invoice-card.css) `.qcards`→1col;\n  capture one full-width card.\n\n## Element(s) to crop\n- A single `<article class=\"qcard\">` FRONT face per stage (6 mood gradients):\n  - **overdue** pink (`#FFD9D9→#FF6B6B`), CTA \"Send nudge\"\n  - **out** teal (`#C8DDE0→#56969E`), CTA \"View invoice\"\n  - **claimed** amber (`#FFE7B5→#E5A331`), CTA \"Okay, I got it\", method subline\n  - **scheduled** purple (`#E4E0F7→#8B7DBF`), CTA \"Send now\"\n  - **drafting** brown (`#E1D7CD→#9C8074`), CTA \"Finish + send\"\n  - **paid** green (`#CFE5C8→#5FA34F`), CTA \"View receipt\"\n- The BACK face (flipped) — head gradient + close-X + big amount, the\n  stage-specific `.qcard__read`, and the 4-button `.qcard__back-foot`.\n- The BACK face with the **\"Adjust invoice\"** panel expanded (discount input +\n  Apply, change-order desc/amount + Create link, the loaded CO list with\n  per-pending Copy-link buttons, the approval link block).\n\n## Transient states to drive\n1. **front (×6 stages)** — captured above; the giant `.qcard__numeral` (idx+1\n   padded) sits bottom-right of the mood block, the avatar (`.qcard__av`)\n   overlaps mood+body.\n2. **flipped** — click the card body (not the CTA/back) → `.qcard--flipped`\n   slides the back in. `aria-hidden` flips to false.\n3. **adjust-open** — on a non-paid flipped card, click \"Adjust invoice ▾\" →\n   first open lazy-fetches `GET /api/invoices/:id/change-orders` and renders the\n   panel + CO list. Drive a pending CO to show the \"Copy link\" → \"Copied!\"\n   toast-less swap.\n4. **busy** — trigger a CTA (e.g. claimed \"Okay, I got it\") → CTA shows \"…\", all\n   back-foot buttons disabled until the `location.reload()` lands (FLAG: every\n   mutation full-reloads; film the moment before reload).\n5. **muted** — on overdue/out back-foot, toggle Mute → button reads \"Muted\".\n\n## Motion to film\n- **Flip:** `.qcard__back` `translateY(8px) scale(.98)→0,1` over `380ms\n  --ease-bounce` + `opacity 0→1` `240ms --ease-out`. Film one flip both ways.\n- **Hover lift:** `.qcard:hover` `translateY(-4px)` + mood-tinted shadow, `320ms`\n  bounce.\n- **Status dot:** `q-pulse-dot` opacity 1→.4→1, `2.4s` infinite (always-on).\n- **CTA arrow:** the `→` span `transition:transform 240ms`; `.qcard__cta:hover`\n  widens `gap 6→9px`; back-foot button hover `translateY(-1px)` + pink border.\n- Re-shoot a flip + the pulsing dot with `prefers-reduced-motion: reduce` (global\n  clamp — verify the flip is instant and the dot stops).\n\n## Notes\n- The `amount` IS the card title (`.qcard__title` = `fmtMoney(amount)`), not a\n  job name. The `.qcard__client-name` reads \"{client} · INV-XXXXXX\".\n- NO fabricated screenshots.\n"}, {"name": "invoices-hero", "component": "InvoicesHero", "spec": "# InvoicesHero\n\nThe editorial `.qph` page header for /invoices. Picks one of five headline\nvariants from the forecast + outstanding totals, shows a sub + optional at-risk\nline, and the New-invoice + Export-CSV CTA row.\n\n## 1. Classification & behavior\n- **Bucket:** local presentational component of InvoicesPage\n  (`islands/InvoicesPage.tsx` lines 566–724).\n- **Interaction tier:** **static / island-child.** No client state of its own.\n  One callback prop (`onNew`).\n- **Server actions:** none. The Export link is a plain `<a href=\n  /api/invoices/export.csv?year=YYYY>` (GET download, opens normally). The New\n  button calls `onNew()` (parent opens the modal).\n- **Data source:** all props (totals/counts/forecast/lang) computed by the\n  parent island. **Honest-empty:** the `trulyEmpty` + `fresh` branches render a\n  friendly headline when there are no invoices / nothing outstanding.\n- **Liveness:** none.\n- **Data-shape hazard:** the five-way branch order matters — truly-empty →\n  fresh(no-forecast) → forecast-this-week → forecast-next-week → legacy\n  outstanding. `haveForecast` requires ANY of the three forecast cents > 0.\n\n## 2. Anatomy\n```\n<header class=\"qph\"><div class=\"qph__copy\">\n  <div class=\"qph__eyebrow\"><span dot/> {eyebrow}</div>\n  <h1 class=\"qph__title\" data-cy=\"forecast-hero\"> {one of 5 variants, <em> on the money/key word} </h1>\n  <p class=\"qph__sub\"> {empty | forecast-breakdown(≤3) | \"N past due …\" | \"nothing past due\"} </p>\n  {atRisk>0 && <p class=\"qph__sub\" style=color:#a83b3b data-cy=\"forecast-at-risk\">⚠ …</p>}\n  <div class=\"qph__cta-row\">\n    <button class=\"qph__cta\" data-cy=\"invoice-new\"><I plus/> New invoice</button>\n    <a class=\"qph__cta qph__cta--ghost\" data-cy=\"invoice-export\" inline-styled-ghost>Export {year} CSV</a>\n  </div>\n</div></header>\n```\n- `.qph__copy` and `.qph__cta--ghost` are **UNSTYLED** classes (no CSS rule);\n  the ghost look is 100% inline style on the `<a>` (transparent bg + 1px current\n  -color border + margin-left:10px). See `css/invoices-hero.css`.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `outstandingTotal` | number (cents) | — | number | no |\n| `outstandingCount` | number | — | number | no |\n| `overdueCount` | number | — | number | no |\n| `totalInvoiceCount` | number | — | number | no |\n| `forecast` | `ForecastResult?` | undefined | object | no |\n| `lang` | `Lang` | — | select | no |\n| `onNew` | `() => void` | — | (callback→Events) | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| truly-empty | 0 invoices | `cases/empty/empty.json` |\n| fresh | invoices exist, nothing outstanding, no forecast | `cases/fresh/fresh.json` |\n| forecast-week | thisWeekCents>0 → \"$X expected this week\" + breakdown | `cases/forecast-week/forecast-week.json` |\n| forecast-next | nextWeekCents>0 → \"$X coming next week\" | `cases/forecast-next/forecast-next.json` |\n| outstanding | legacy \"$X on the way / across N invoices\" + at-risk | `cases/outstanding/outstanding.json` |\n\n## 5. Events\n- `ev.expect(e => e.source===\"button[data-cy=invoice-new]\" && e.type===\"click\")`\n  → `onNew()`.\n- Export is a navigation (no JS handler).\n\n## 6. Motion\n- `.qph__cta:hover` → `translateY(-1px)` over `--dur-fast var(--ease-out)`. No\n  other motion. Reduced-motion via global clamp.\n\n## 7. Responsive (from quotes.css)\n- `≤1200px`: `.qph` `flex-direction:column; align-items:flex-start`.\n\n## 8. A11y\n- The ⚠ at-risk line uses a literal \"⚠\" glyph (no `aria-label`); the danger\n  color `#a83b3b` is a hardcoded hex (matches the public-surface danger token).\n- CTA button + export link are real `<button>`/`<a>` — keyboard-OK.\n\n## 9. Used on\n`/invoices` only.\n", "fixture": "{\n  \"category\": \"invoices\",\n  \"folder\": \"invoices-hero\",\n  \"controls\": {\n    \"outstandingTotal\": { \"type\": \"number\", \"value\": 728500 },\n    \"outstandingCount\": { \"type\": \"number\", \"value\": 3 },\n    \"overdueCount\": { \"type\": \"number\", \"value\": 2 },\n    \"totalInvoiceCount\": { \"type\": \"number\", \"value\": 9 },\n    \"forecast\": { \"type\": \"json\", \"value\": null },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Truly-empty — 0 invoices ('No invoices yet — let's start the river.')\",\n  \"outstandingTotal\": 0,\n  \"outstandingCount\": 0,\n  \"overdueCount\": 0,\n  \"totalInvoiceCount\": 0,\n  \"forecast\": null,\n  \"lang\": \"en\"\n}\n"}, {"name": "forecast-next.json", "content": "{\n  \"_name\": \"Forecast next week — quiet this week, '$X coming next week.'\",\n  \"outstandingTotal\": 305000,\n  \"outstandingCount\": 2,\n  \"overdueCount\": 0,\n  \"totalInvoiceCount\": 7,\n  \"forecast\": {\n    \"thisWeekCents\": 0,\n    \"thisWeek\": [],\n    \"nextWeekCents\": 305000,\n    \"atRiskCents\": 0,\n    \"atRisk\": [],\n    \"asOf\": \"2026-06-18T12:00:00.000Z\"\n  },\n  \"lang\": \"en\"\n}\n"}, {"name": "forecast-week.json", "content": "{\n  \"_name\": \"Forecast this week — '$X expected this week' + per-day breakdown + at-risk line\",\n  \"outstandingTotal\": 728500,\n  \"outstandingCount\": 3,\n  \"overdueCount\": 1,\n  \"totalInvoiceCount\": 9,\n  \"forecast\": {\n    \"thisWeekCents\": 516000,\n    \"thisWeek\": [\n      { \"expectedLandDate\": \"2026-06-19\", \"amount\": 96000, \"label\": \"Marcus Lin\", \"invoiceId\": \"inv1122aa\", \"source\": \"claimed\" },\n      { \"expectedLandDate\": \"2026-06-20\", \"amount\": 240000, \"label\": \"Greenleaf HOA\", \"invoiceId\": \"inv3344bb\", \"source\": \"sent_due\" },\n      { \"expectedLandDate\": \"2026-06-22\", \"amount\": 180000, \"label\": \"Hilltop Diner\", \"invoiceId\": \"inv7d8e9f\", \"source\": \"sent_due\" }\n    ],\n    \"nextWeekCents\": 0,\n    \"atRiskCents\": 185000,\n    \"atRisk\": [\n      { \"expectedLandDate\": \"2026-06-03\", \"amount\": 185000, \"label\": \"Hilltop Diner\", \"invoiceId\": \"inv7d8e9f\", \"source\": \"sent_due\" }\n    ],\n    \"asOf\": \"2026-06-18T12:00:00.000Z\"\n  },\n  \"lang\": \"en\"\n}\n"}, {"name": "fresh.json", "content": "{\n  \"_name\": \"Fresh — invoices exist, nothing outstanding, no forecast ('All clear — nothing outstanding.')\",\n  \"outstandingTotal\": 0,\n  \"outstandingCount\": 0,\n  \"overdueCount\": 0,\n  \"totalInvoiceCount\": 6,\n  \"forecast\": null,\n  \"lang\": \"en\"\n}\n"}, {"name": "outstanding.json", "content": "{\n  \"_name\": \"Legacy outstanding — no forecast, '$7,285 on the way / across 3 invoices' + 'N past due'\",\n  \"outstandingTotal\": 728500,\n  \"outstandingCount\": 3,\n  \"overdueCount\": 2,\n  \"totalInvoiceCount\": 9,\n  \"forecast\": null,\n  \"lang\": \"en\"\n}\n"}], "css": "/* InvoicesHero uses the `.qph` hero block from static/quotes.css (verbatim in\n   ../../../css/invoices.css, lines for .qph / .qph__eyebrow / .qph__eyebrow-dot\n   / .qph__title / .qph__title em / .qph__sub / .qph__cta). Tokens from\n   dashboard.css.\n\n   UNSTYLED classes this component's markup adds (no rule anywhere):\n     .qph__copy        — wrapper div (renders as default block; fine)\n     .qph__cta--ghost  — the Export-CSV secondary CTA. It has NO ghost rule;\n                         the visible \"ghost\" look comes ENTIRELY from inline\n                         style on the <a>: background:transparent +\n                         border:1px solid currentColor + margin-left:10px.\n                         (.qph__cta still applies its pink bg via the base\n                         class UNLESS the inline background:transparent wins —\n                         which it does, since inline beats the class.) */\n", "cssName": "invoices-hero.css", "source": "/* Extracted verbatim from islands/InvoicesPage.tsx (the `InvoicesHero`\n * sub-component + its ForecastResult/ForecastEntry types). Editorial `.qph`\n * header — outstanding/forecast headline, sub, at-risk line, and the\n * New-invoice + Export-CSV CTA row. Renders one of five headline variants\n * (truly-empty · fresh · forecast-this-week · forecast-next-week · legacy\n * outstanding-total). */\nimport { I, ICN } from \"../../../../../shared-components/quote-track/js/dash-icons.tsx\";\nimport { fmtMoney } from \"../../../../invoices/components/invoices-page/js/format.ts\";\nimport { type Lang, tFor } from \"../../../../../../lib/i18n.ts\";\n\ninterface ForecastEntry {\n  expectedLandDate: string;\n  amount: number;\n  label: string;\n  invoiceId: string;\n  source: \"claimed\" | \"sent_due\" | \"scheduled\" | \"paid\";\n}\n\ninterface ForecastResult {\n  thisWeekCents: number;\n  thisWeek: ForecastEntry[];\n  nextWeekCents: number;\n  atRiskCents: number;\n  atRisk: ForecastEntry[];\n  asOf: string;\n}\n\n/** \"2026-05-19\" → \"Tue\" / \"May 19\" — short day-of-week label for the\n *  forecast hero breakdown. Falls back to MMM d when the date is more than a\n *  week out. */\nfunction shortDay(iso: string): string {\n  const d = new Date(`${iso}T12:00:00Z`);\n  if (!Number.isFinite(d.getTime())) return iso;\n  const diffDays = Math.round((d.getTime() - Date.now()) / (24 * 3600 * 1000));\n  if (diffDays >= 0 && diffDays < 7) {\n    return d.toLocaleDateString(\"en-US\", { weekday: \"short\", timeZone: \"UTC\" });\n  }\n  return d.toLocaleDateString(\"en-US\", {\n    month: \"short\",\n    day: \"numeric\",\n    timeZone: \"UTC\",\n  });\n}\n\nexport function InvoicesHero(\n  {\n    outstandingTotal,\n    outstandingCount,\n    overdueCount,\n    totalInvoiceCount,\n    forecast,\n    lang,\n    onNew,\n  }: {\n    outstandingTotal: number;\n    outstandingCount: number;\n    overdueCount: number;\n    totalInvoiceCount: number;\n    forecast?: ForecastResult;\n    lang: Lang;\n    onNew: () => void;\n  },\n) {\n  const trulyEmpty = totalInvoiceCount === 0;\n  const fresh = !trulyEmpty && outstandingCount === 0;\n  const haveForecast = !!forecast &&\n    (forecast.thisWeekCents > 0 || forecast.nextWeekCents > 0 ||\n      forecast.atRiskCents > 0);\n  return (\n    <header class=\"qph\">\n      <div class=\"qph__copy\">\n        <div class=\"qph__eyebrow\">\n          <span class=\"qph__eyebrow-dot\" /> {tFor(lang, \"invoicesPage.eyebrow\")}\n        </div>\n        <h1 class=\"qph__title\" data-cy=\"forecast-hero\">\n          {trulyEmpty\n            ? (\n              <>\n                {tFor(lang, \"invoicesPage.hero.emptyPre\")}{\" \"}\n                <em>{tFor(lang, \"invoicesPage.hero.emptyEm\")}</em>.\n              </>\n            )\n            : fresh && !haveForecast\n            ? (\n              <>\n                {tFor(lang, \"invoicesPage.hero.clearPre\")}{\" \"}\n                <em>{tFor(lang, \"invoicesPage.hero.clearEm\")}</em>.\n              </>\n            )\n            : haveForecast && forecast!.thisWeekCents > 0\n            ? (\n              <>\n                <em>{fmtMoney(forecast!.thisWeekCents)}</em>{\" \"}\n                {tFor(lang, \"invoicesPage.hero.expectedThisWeek\")}\n                <br />\n                {tFor(lang, \"invoicesPage.hero.across\")} {forecast!.thisWeek\n                  .length} {tFor(\n                    lang,\n                    forecast!.thisWeek.length === 1\n                      ? \"invoicesPage.unitPayment.one\"\n                      : \"invoicesPage.unitPayment.other\",\n                    { n: forecast!.thisWeek.length },\n                  )}.\n              </>\n            )\n            : haveForecast && forecast!.nextWeekCents > 0\n            ? (\n              <>\n                {tFor(lang, \"invoicesPage.hero.quietPre\")}{\" \"}\n                <em>{fmtMoney(forecast!.nextWeekCents)}</em>{\" \"}\n                {tFor(lang, \"invoicesPage.hero.comingNextWeek\")}\n              </>\n            )\n            : (\n              <>\n                <em>{fmtMoney(outstandingTotal)}</em>{\" \"}\n                {tFor(lang, \"invoicesPage.hero.onTheWay\")}\n                <br />\n                {tFor(lang, \"invoicesPage.hero.across\")} {outstandingCount}{\" \"}\n                {tFor(\n                  lang,\n                  outstandingCount === 1\n                    ? \"invoicesPage.unitInvoice.one\"\n                    : \"invoicesPage.unitInvoice.other\",\n                  { n: outstandingCount },\n                )}.\n              </>\n            )}\n        </h1>\n        <p class=\"qph__sub\">\n          {trulyEmpty\n            ? <>{tFor(lang, \"invoicesPage.sub.empty\")}</>\n            : haveForecast && forecast!.thisWeek.length > 0\n            ? (\n              <span data-cy=\"forecast-breakdown\">\n                {forecast!.thisWeek.slice(0, 3).map((e, i) => (\n                  <span key={e.invoiceId}>\n                    {i > 0 ? \" · \" : \"\"}\n                    {shortDay(e.expectedLandDate)}:{\" \"}\n                    <strong>{e.label} {fmtMoney(e.amount)}</strong>\n                  </span>\n                ))}\n              </span>\n            )\n            : overdueCount > 0\n            ? (\n              <>\n                <strong>{overdueCount}</strong> {tFor(\n                  lang,\n                  overdueCount === 1\n                    ? \"invoicesPage.sub.pastDueVerb.one\"\n                    : \"invoicesPage.sub.pastDueVerb.other\",\n                  { n: overdueCount },\n                )} {tFor(lang, \"invoicesPage.sub.pastDue\")}\n              </>\n            )\n            : <>{tFor(lang, \"invoicesPage.sub.nothingPastDue\")}</>}\n        </p>\n        {haveForecast && forecast!.atRiskCents > 0\n          ? (\n            <p\n              class=\"qph__sub\"\n              style=\"color:#a83b3b\"\n              data-cy=\"forecast-at-risk\"\n            >\n              ⚠ <strong>{fmtMoney(forecast!.atRiskCents)}</strong>{\" \"}\n              {tFor(lang, \"invoicesPage.atRisk.across\")} {forecast!.atRisk\n                .length} {tFor(lang, \"invoicesPage.atRisk.overdue\")} {tFor(\n                  lang,\n                  forecast!.atRisk.length === 1\n                    ? \"invoicesPage.unitInvoice.one\"\n                    : \"invoicesPage.unitInvoice.other\",\n                  { n: forecast!.atRisk.length },\n                )}.\n            </p>\n          )\n          : null}\n        <div class=\"qph__cta-row\">\n          <button\n            type=\"button\"\n            class=\"qph__cta\"\n            data-cy=\"invoice-new\"\n            onClick={onNew}\n            style=\"appearance:none;cursor:pointer;font:inherit\"\n          >\n            <I d={ICN.plus} size={14} sw={2.5} />{\" \"}\n            {tFor(lang, \"invoicesPage.newInvoice\")}\n          </button>\n          <a\n            class=\"qph__cta qph__cta--ghost\"\n            data-cy=\"invoice-export\"\n            href={`/api/invoices/export.csv?year=${new Date().getFullYear()}`}\n            style=\"margin-left:10px;background:transparent;border:1px solid currentColor\"\n          >\n            {tFor(lang, \"invoicesPage.exportCsv\", {\n              year: new Date().getFullYear(),\n            })}\n          </a>\n        </div>\n      </div>\n    </header>\n  );\n}\n", "sourceName": "InvoicesHero.tsx", "checklist": "# Capture checklist — InvoicesHero\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/invoices` — the hero is the `.qph` header at the top of\n  the page (the editorial block above the KPI strip).\n\n## Viewports (from quotes.css `@media`)\n- **1280px** — hero is a flex row (`align-items:flex-end; justify-content:\n  space-between`), CTA row to the right.\n- **1200px** boundary — at ≤1200 `.qph` becomes `flex-direction:column;\n  align-items:flex-start` (title/sub/CTAs stack). Shoot just above and just\n  below 1200 to capture the reflow.\n- **720px** — confirm the stacked layout + the `clamp(28px,4vw,44px)` title\n  scaling down.\n\n## Element(s) to crop\n- The `<header class=\"qph\">` block only (eyebrow dot → title → sub → optional\n  at-risk line → CTA row with New invoice + Export CSV).\n\n## Transient states to drive (the 5 headline variants — branch order matters)\n1. **truly-empty** — 0 invoices → \"No invoices yet — *let's start the river*.\"\n   + sub.empty. (Fresh account.)\n2. **fresh** — invoices exist, nothing outstanding, no forecast → \"All clear —\n   *nothing outstanding*.\" + \"Nothing past due…\".\n3. **forecast-week** — `/api/invoices/forecast/this-week` returns\n   `thisWeekCents>0` → \"*$X* expected this week / across N payments\" + the ≤3\n   per-day breakdown in the sub (data-cy=forecast-breakdown) + the ⚠ at-risk\n   line (data-cy=forecast-at-risk) when atRiskCents>0.\n4. **forecast-next** — `nextWeekCents>0`, `thisWeekCents=0` → \"Quiet week — *$X*\n   coming next week.\"\n5. **outstanding** (legacy) — forecast 404s/empty → \"*$X* on the way / across N\n   invoices\" + \"N is/are past due …\".\n\n> Driving forecast variants: the forecast is a fire-and-forget\n> `fetch(\"/api/invoices/forecast/this-week\")`. To force a variant, intercept that\n> endpoint (or use isolate cases) — older backends 404 it, which is the\n> outstanding fallback.\n\n## Motion to film\n- `.qph__cta:hover` → `translateY(-1px)` over `--dur-fast var(--ease-out)`. Film\n  the hover lift on the New-invoice pink CTA. The Export-CSV ghost `<a>` has the\n  same base `.qph__cta` transition. Re-shoot with reduced-motion (global clamp).\n- The ⚠ at-risk line is a literal glyph (no animation; check the `#a83b3b`\n  hardcoded danger color renders).\n\n## Notes\n- The `.qph__cta--ghost` look is 100% inline style (transparent bg + 1px\n  currentColor border + margin-left:10px), NOT a class rule — verify the Export\n  link reads as outlined, not solid pink. NO fabricated screenshots.\n"}, {"name": "invoices-kpis", "component": "InvoicesKpis", "spec": "# InvoicesKpis\n\nThe 4-cell `.qkpi` summary strip below the hero on /invoices: **Overdue** (pink\naccent when count > 0) · **Out for payment** · **Drafting** · **Paid this\nmonth**. Pure presentational — every value is a prop computed by the parent\nisland.\n\n## 1. Classification & behavior\n- **Bucket:** local presentational component of the InvoicesPage island\n  (`islands/InvoicesPage.tsx` lines 728–792). Not its own island — it renders\n  inside the page island's hydration boundary.\n- **Interaction tier:** **static / island-child.** No client state, no events,\n  no callbacks. It only reads props + `tFor(lang, …)`.\n- **Server actions + flash:** none. It is a read-only KPI display.\n- **Island client-state + refresh:** none of its own. When the page island\n  re-fetches (today via a child's `location.reload()`), the parent recomputes\n  the seven numbers and re-renders this strip with fresh props. **No\n  `location.reload()` lives here** — flag belongs to InvoiceCard /\n  NewInvoiceModal.\n- **Data source + honest-empty:** all data flows down as props — there is no\n  fetch here. The numbers are derived in the island from `GET /invoices`\n  (`dashboardClient.invoices`): `overdueCount`/`overdueTotal`,\n  `outCount`/`outTotal`, `draftingCount`, `paidCount`/`paidTotal` come from the\n  six `filter/sort` buckets over the enriched invoice list. **Honest-empty:**\n  with zero invoices every cell renders truthfully — `fmtMoney(0)` → `\"$0\"`,\n  counts → `0`, and the Drafting sub flips to `\"no drafts open\"`\n  (`invoicesPage.kpi.draftingSubEmpty`) instead of `\"finish + send\"`.\n- **Liveness / polling:** none. Frozen until the parent re-renders.\n- **Data-shape hazards (invoice aging/urgency buckets):**\n  - The **Overdue** total/count is the parent's **client-derived** aging\n    bucket (`enrich()` recomputes `daysOverdue` from `dueDate < today`), NOT the\n    DTO's own `urgency` field — see invoices-page.md §1 / data-model.md hazard\n    #8. So this KPI can disagree with a server-computed aging bucket.\n  - **Out** = the parent's `stage===\"out\"` bucket only (it excludes claimed +\n    scheduled). The KPI strip shows only 4 of the island's 6 tracks, so the\n    Out-for-payment total here is NOT \"everything not paid\" — claimed,\n    scheduled, and overdue dollars are counted separately / not at all in this\n    strip.\n  - Money props are **integer CENTS** (`overdueTotal`/`outTotal`/`paidTotal`),\n    formatted by `fmtMoney` (no-cents, rounded). `draftingCount` is a bare\n    integer rendered as the value itself (no money format) — the only cell whose\n    `.qkpi__val` is a count, not a dollar amount.\n\n## 2. Anatomy\n```\n<div class=\"qkpi\">\n  <div class=\"qkpi__cell [qkpi__cell--accent]\">        ← --accent only when overdueCount>0\n    <div class=\"qkpi__lbl\">{status.overdue → \"Overdue\"}</div>\n    <div class=\"qkpi__val\">{fmtMoney(overdueTotal)}</div>\n    <div class=\"qkpi__sub\">{overdueCount} {invoice|invoices}</div>\n  </div>\n  <div class=\"qkpi__cell\">                              ← Out for payment\n    <div class=\"qkpi__lbl\">{kpi.out → \"Out for payment\"}</div>\n    <div class=\"qkpi__val\">{fmtMoney(outTotal)}</div>\n    <div class=\"qkpi__sub\">{kpi.outSub → \"{n} on the way\"}</div>\n  </div>\n  <div class=\"qkpi__cell\">                              ← Drafting (count value, not $)\n    <div class=\"qkpi__lbl\">{kpi.drafting → \"Drafting\"}</div>\n    <div class=\"qkpi__val\">{draftingCount}</div>\n    <div class=\"qkpi__sub\">{draftingSub | draftingSubEmpty}</div>\n  </div>\n  <div class=\"qkpi__cell\">                              ← Paid this month\n    <div class=\"qkpi__lbl\">{kpi.paid → \"Paid this month\"}</div>\n    <div class=\"qkpi__val\">{fmtMoney(paidTotal)}</div>\n    <div class=\"qkpi__sub\">{kpi.paidSub → \"{n} cleared\"}</div>\n  </div>\n</div>\n```\n- No slots/children. No icon dependency. The accent class is the only\n  conditional. Cells 1/2/4 show money; cell 3 (Drafting) shows a raw count.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `overdueCount` | number | — | number | no |\n| `overdueTotal` | number (cents) | — | number | no |\n| `outCount` | number | — | number | no |\n| `outTotal` | number (cents) | — | number | no |\n| `draftingCount` | number | — | number | no |\n| `paidCount` | number | — | number | no |\n| `paidTotal` | number (cents) | — | number | no |\n| `lang` | `Lang` | — | select | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | all four cells with realistic numbers; Overdue accent ON | `cases/default/default.json` |\n| no-overdue | `overdueCount=0` → Overdue cell loses `--accent` (plain white) | `cases/no-overdue/no-overdue.json` |\n| empty | every count/total 0 → \"$0\", \"0 invoices\", \"no drafts open\" | `cases/empty/empty.json` |\n| es | Spanish labels/units | `cases/es/es.json` |\n\n## 5. Events\n- None. `capture(page)` finds no interactive elements in this component (no\n  buttons, links, or inputs). It is display-only.\n\n## 6. Motion\n- **None of its own** — no keyframes, no transitions on `.qkpi*`. The `--accent`\n  cell is a static pink gradient (no animation). Reduced-motion is moot here\n  (nothing animates); the global tokens clamp still applies app-wide.\n\n## 7. Responsive (own `@media`, from quotes.css)\n- Base: `.qkpi` is `grid-template-columns: repeat(4, 1fr)` (4 across).\n- `≤1200px`: `.qkpi` → `repeat(2, 1fr)` (2×2).\n- `≤768px`: `.qkpi` → `1fr` (single column stack).\n- Verify at **1280px**, **980px** (still 2-col since the cut is 1200), **720px**\n  (1-col) against quotes.css.\n\n## 8. A11y\n- Plain `<div>` grid of `<div>`s — no list/table semantics, no headings. Each\n  cell is label → value → sub with no programmatic association (a screen reader\n  reads three loose strings per cell). **Fix on rebuild:** use a `<dl>`\n  (`<dt>` label / `<dd>` value) per KPI, or `role=\"group\"` + `aria-label`.\n- The Overdue accent communicates urgency with **color only** (pink gradient) —\n  the count text carries the real signal, so it degrades acceptably, but add a\n  non-color cue if it becomes the sole indicator.\n\n## 9. Used on\n`/invoices` only (rendered once by InvoicesPage between the hero and the\ntracks). Reuses the Quotes `.qkpi*` CSS verbatim from `static/quotes.css` — see\n`css/invoices-kpis.css`. (The Quotes and Payments pages render their own `.qkpi`\nstrips with different copy; this one is the invoices variant.)\n", "fixture": "{\n  \"category\": \"invoices\",\n  \"folder\": \"invoices-kpis\",\n  \"controls\": {\n    \"overdueCount\": { \"type\": \"number\", \"value\": 2 },\n    \"overdueTotal\": { \"type\": \"number\", \"value\": 605000 },\n    \"outCount\": { \"type\": \"number\", \"value\": 1 },\n    \"outTotal\": { \"type\": \"number\", \"value\": 580000 },\n    \"draftingCount\": { \"type\": \"number\", \"value\": 2 },\n    \"paidCount\": { \"type\": \"number\", \"value\": 1 },\n    \"paidTotal\": { \"type\": \"number\", \"value\": 190000 },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "default.json", "content": "{\n  \"_name\": \"Default — all 4 cells, Overdue accent ON (count>0)\",\n  \"overdueCount\": 2,\n  \"overdueTotal\": 605000,\n  \"outCount\": 1,\n  \"outTotal\": 580000,\n  \"draftingCount\": 2,\n  \"paidCount\": 1,\n  \"paidTotal\": 190000,\n  \"lang\": \"en\"\n}\n"}, {"name": "empty.json", "content": "{\n  \"_name\": \"Empty — every count/total 0 → '$0', '0 invoices', Drafting sub 'no drafts open'\",\n  \"overdueCount\": 0,\n  \"overdueTotal\": 0,\n  \"outCount\": 0,\n  \"outTotal\": 0,\n  \"draftingCount\": 0,\n  \"paidCount\": 0,\n  \"paidTotal\": 0,\n  \"lang\": \"en\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — localized labels (Vencido / En cobro / Borradores / Pagado) + units\",\n  \"overdueCount\": 2,\n  \"overdueTotal\": 605000,\n  \"outCount\": 1,\n  \"outTotal\": 580000,\n  \"draftingCount\": 2,\n  \"paidCount\": 1,\n  \"paidTotal\": 190000,\n  \"lang\": \"es\"\n}\n"}, {"name": "no-overdue.json", "content": "{\n  \"_name\": \"No overdue — Overdue cell loses --accent (plain white), '$0 / 0 invoices'\",\n  \"overdueCount\": 0,\n  \"overdueTotal\": 0,\n  \"outCount\": 3,\n  \"outTotal\": 728500,\n  \"draftingCount\": 1,\n  \"paidCount\": 4,\n  \"paidTotal\": 612000,\n  \"lang\": \"en\"\n}\n"}], "css": "/* InvoicesKpis uses the `.qkpi` strip block from static/quotes.css (verbatim\n   in ../../../css/invoices.css): .qkpi / .qkpi__cell / .qkpi__cell--accent /\n   .qkpi__lbl / .qkpi__val / .qkpi__cell--accent .qkpi__val / .qkpi__sub.\n   The accent cell (pink gradient) is applied to the Overdue cell only when\n   overdueCount > 0. No component-local CSS. Tokens from dashboard.css. */\n", "cssName": "invoices-kpis.css", "source": "/* Extracted verbatim from islands/InvoicesPage.tsx (`InvoicesKpis`). The\n * 4-cell `.qkpi` strip: Overdue (accent when count>0) · Out for payment ·\n * Drafting · Paid this month. Pure presentational — all numbers are props. */\nimport { fmtMoney } from \"../../invoices-page/js/format.ts\";\nimport { type Lang, tFor } from \"../../../../../../lib/i18n.ts\";\n\nexport function InvoicesKpis(\n  {\n    overdueCount,\n    overdueTotal,\n    outCount,\n    outTotal,\n    draftingCount,\n    paidCount,\n    paidTotal,\n    lang,\n  }: {\n    overdueCount: number;\n    overdueTotal: number;\n    outCount: number;\n    outTotal: number;\n    draftingCount: number;\n    paidCount: number;\n    paidTotal: number;\n    lang: Lang;\n  },\n) {\n  return (\n    <div class=\"qkpi\">\n      <div class={`qkpi__cell${overdueCount > 0 ? \" qkpi__cell--accent\" : \"\"}`}>\n        <div class=\"qkpi__lbl\">{tFor(lang, \"status.overdue\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(overdueTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {overdueCount} {tFor(\n            lang,\n            overdueCount === 1\n              ? \"invoicesPage.unitInvoice.one\"\n              : \"invoicesPage.unitInvoice.other\",\n            { n: overdueCount },\n          )}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"invoicesPage.kpi.out\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(outTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"invoicesPage.kpi.outSub\", { n: outCount })}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"invoicesPage.kpi.drafting\")}</div>\n        <div class=\"qkpi__val\">{draftingCount}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(\n            lang,\n            draftingCount === 0\n              ? \"invoicesPage.kpi.draftingSubEmpty\"\n              : \"invoicesPage.kpi.draftingSub\",\n          )}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"invoicesPage.kpi.paid\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(paidTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"invoicesPage.kpi.paidSub\", { n: paidCount })}\n        </div>\n      </div>\n    </div>\n  );\n}\n", "sourceName": "InvoicesKpis.tsx", "checklist": "# Capture checklist — InvoicesKpis\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/invoices` — the `.qkpi` strip directly below the hero,\n  above the tracks.\n\n## Viewports (from quotes.css `@media`)\n- **1280px** — `repeat(4, 1fr)` (4 cells across).\n- **1200px** boundary — at ≤1200 → `repeat(2, 1fr)` (2×2). Shoot just above and\n  below 1200 to capture the reflow.\n- **720px** — at ≤768 → `1fr` (single-column stack of 4 cells).\n\n## Element(s) to crop\n- The full `<div class=\"qkpi\">` (all 4 cells: Overdue · Out for payment ·\n  Drafting · Paid this month).\n- A close crop of the **Overdue** cell in both states: with `--accent` (pink\n  gradient, count>0) and without (plain white card, count=0).\n\n## Transient states to drive\n1. **default** — Overdue count>0 → Overdue cell shows `.qkpi__cell--accent`\n   (pink `#fff1ed→#ffe4da` gradient, pink value text, pink-tinted border).\n2. **no-overdue** — Overdue count=0 → accent removed, cell is plain white like\n   the other three; value reads \"$0\".\n3. **empty** — all zeros: every value \"$0\" (Drafting \"0\"), Drafting sub flips to\n   \"no drafts open\"; Out sub \"0 on the way\"; Paid sub \"0 cleared\".\n4. **es** — Spanish labels + invoice/payment units.\n\n## Motion to film\n- **None.** This strip has no animation (the accent gradient is static). Nothing\n  to film; reduced-motion is moot. Confirm visually that no transition fires on\n  the cells.\n\n## Notes\n- The Drafting cell's `.qkpi__val` is a raw COUNT (e.g. \"2\"), not a dollar\n  amount — the only non-money value cell. Verify it doesn't render \"$2\".\n- NO fabricated screenshots.\n"}, {"name": "invoices-page", "component": "InvoicesPage", "spec": "# InvoicesPage\n\nThe /invoices island. Orchestrates the whole receivables surface: fetch →\nenrich → bucket → render hero/KPIs/6 tracks. 1632 lines; the large sub-pieces\n(InvoiceCard, NewInvoiceModal) have their own folders.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/InvoicesPage.tsx`).\n- **Interaction tier:** **whole-page island, client-only state.** Self-fetches\n  on mount; SSR seeds nothing (the `lang` prop is an ignored SSR seed — the\n  island reads `langSignal.value` instead so it re-renders live on a language\n  flip).\n- **Client state owned:**\n  - `s: State { loading, error, invoices, customers }` — the fetched data.\n  - `forecast?: ForecastResult` — separate fire-and-forget fetch.\n  - `newOpen: boolean` — NewInvoiceModal visibility.\n- **Server actions + flash:** none at the page level. All mutations live in\n  child InvoiceCard / NewInvoiceModal (each does `fetch` → `location.reload()`).\n- **Data source per region:**\n  - invoices ← `dashboardClient.invoices(undefined)` (`GET /invoices`), `.catch\n    → []`.\n  - customers ← `dashboardClient.customers()` (`GET /customers`), `.catch → []`.\n  - forecast ← `fetch(\"/api/invoices/forecast/this-week\")`, ok→json else\n    `undefined` (silent legacy fallback).\n  - All three fire in parallel `useEffect([])`; an `alive` flag guards unmount.\n- **Honest-empty:** `loading` → Skeletons (ShimmerStyle + PageHeaderSkeleton +\n  CardGridSkeleton rows=2 — all SHARED); `error` → `.qpage-error` div (an\n  UNSTYLED class — renders as default text, see §7); else the hero/KPIs render\n  with $0/0 and every track shows its `EmptyTrack` hint.\n- **Liveness:** request-response only; no polling/websocket. State refreshes\n  via child `location.reload()` (full page) — flagged below.\n- **Data-shape hazards:**\n  - **Invoice aging/urgency buckets are derived client-side** (`enrich()` +\n    six `filter/sort` passes) every render. The DTO's own `urgency` field\n    (`{label,tone,daysOverdue}`) is **ignored** — the island recomputes\n    `daysOverdue`/`daysIn`/`stage` from `dueDate`/`issuedDate`/`status`/`paidAt`\n    itself. So two notions of \"overdue\" can disagree. **[bucket on write].**\n  - `isDraft()` is deliberately broad (catches \"draft\"/\"drafting\"/\"drafted\",\n    no-status+no-issuedDate, pending+no-issuedDate) — backend `status` is an\n    open string.\n  - `customers` is `Array.isArray`-guarded in three places because the catch\n    returns `[]` but a malformed payload could be non-array.\n- **Anti-patterns:**\n  - Whole-page island with frozen SSR props (only `lang`); all data client-\n    fetched on mount. **Fix:** SSR the invoice list in the route loader and\n    hand it to the island as a hydration seed (eliminates the skeleton flash +\n    the empty-on-first-paint).\n  - Child mutations call **`location.reload()`** (FLAG) — see invoice-card.md /\n    new-invoice-modal.md. **Fix:** lift a refetch callback into the island and\n    re-pull `/invoices` instead of reloading the page.\n\n## 2. Anatomy\n```\n<> (fragment, inside .content)\n  <InvoicesHero …/>                         ← .qph\n  {newOpen && <NewInvoiceModal …/>}         ← fixed overlay\n  <InvoicesKpis …/>                         ← .qkpi\n  <div class=\"qlay\"><div>                    ← single column (2nd track empty)\n    <QuoteTrack 01 Overdue>      … InvoiceCard[] | EmptyTrack\n    <QuoteTrack 02 Awaiting>     … (data-cy=\"awaiting-confirmation-track\")\n    <QuoteTrack 03 Out>          …\n    <QuoteTrack 04 Upcoming>     … (data-cy=\"upcoming-track\", defaultOpen=false)\n    <QuoteTrack 05 Drafting>     … (defaultOpen=false)\n    <QuoteTrack 06 Paid month>   … (defaultOpen=false)\n  </div></div>\n</>\n```\n- Local helper components: `InvoicesHero`, `InvoicesKpis`, `EmptyTrack`,\n  `InvoiceCard`, `NewInvoiceModal`.\n- Helpers: `methodLabel`, `shortDay`, `monthLabel`, `fmtDate`, `initialsOf`,\n  `isDraft`, `enrich`, `STAGE_MOOD`.\n- Deps: QuoteTrack[shared], Skeletons[shared], `I`/`ICN`[dash-icons],\n  `fmtMoney`/`fmtMoneyExact`, `tFor`/`langSignal`, dashboardClient/clientsClient.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` | (ignored SSR seed) | — | reads `langSignal.value` instead |\n\n(No other props — it is the page island.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | Skeletons | `isolate/cases/loading/loading.json` |\n| error | `.qpage-error` text | `isolate/cases/error/error.json` |\n| empty | 0 invoices → empty hero + all tracks empty | `isolate/cases/empty/empty.json` |\n| populated | full pipeline across stages | `isolate/cases/populated/populated.json` |\n\n## 5. Events\n- `ev.expect(e => e.source===\"button.qph__cta[data-cy=invoice-new]\" &&\n  e.type===\"click\")` → sets `newOpen=true` (mounts NewInvoiceModal).\n- Track collapse events belong to QuoteTrack[shared].\n- Card flip / mutation events belong to InvoiceCard.\n\n## 6. Motion\n- None of its own. Skeleton shimmer = SHARED Skeletons; track collapse =\n  QuoteTrack; card flip = InvoiceCard. Reduced-motion via the global tokens\n  clamp.\n\n## 7. Responsive (own `@media`, from quotes.css)\n- `≤1200px`: `.qlay` → 1 col; `.qph` stacks; `.qkpi` → 2 cols.\n- `≤768px`: `.qcards` → 1 col; `.qkpi` → 1 col.\n\n## 8. A11y\n- `.qpage-error` has no `role=\"alert\"` and no styling (UNSTYLED class — no CSS\n  rule exists anywhere). **Fix:** add a styled error region + `role=\"alert\"`.\n- Loading state is silent (no `aria-busy`).\n\n## 9. Used on\n`/invoices` only (mounted by `routes/invoices/index.tsx`).\n", "fixture": "{\n  \"category\": \"invoices\",\n  \"folder\": \"invoices-page\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  },\n  \"components\": [\n    \"invoices-hero\",\n    \"invoices-kpis\",\n    \"invoice-card\",\n    \"new-invoice-modal\",\n    \"quote-track\"\n  ]\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty — 0 invoices → empty hero + $0 KPIs + every track EmptyTrack hint\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": false, \"error\": null },\n  \"_mocks\": {\n    \"dashboardClient.invoices\": [],\n    \"dashboardClient.customers\": [],\n    \"fetch:/api/invoices/forecast/this-week\": \"__reject:404__\"\n  },\n  \"_note\": \"totalInvoiceCount===0 → hero shows 'No invoices yet — let's start the river.' + sub.empty; KPIs all $0/0, Drafting sub 'no drafts open'; all 6 QuoteTracks render their EmptyTrack hint.\"\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — Promise.all rejected → .qpage-error (UNSTYLED, hazard)\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": false, \"error\": \"Network request failed\" },\n  \"_mocks\": {\n    \"dashboardClient.invoices\": \"__reject:Network request failed__\",\n    \"dashboardClient.customers\": \"__reject:Network request failed__\"\n  },\n  \"_note\": \"Each fetch is individually .catch-guarded to [] inside Promise.all, so a single rejection does NOT trip the error branch — the page renders empty instead. The error div only shows if the outer .then→.catch fires (e.g. an exception thrown in the mapper). To force the .qpage-error state in isolate, drive _signals.error directly. Renders: <div class=\\\"qpage-error\\\">Couldn't load invoices: Network request failed</div> — no styling, no role=alert.\"\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — skeletons while /invoices + /customers in flight\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": true },\n  \"_mocks\": {\n    \"dashboardClient.invoices\": \"__never_resolves__\",\n    \"dashboardClient.customers\": \"__never_resolves__\",\n    \"fetch:/api/invoices/forecast/this-week\": \"__never_resolves__\"\n  },\n  \"_note\": \"Renders <ShimmerStyle/> + <PageHeaderSkeleton/> + <CardGridSkeleton rows={2}/> (all SHARED). On-mount Promise.all is pending → loading=true.\"\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — full pipeline across overdue/claimed/out/scheduled/drafting/paid\",\n  \"lang\": \"en\",\n  \"_signals\": { \"loading\": false, \"error\": null },\n  \"_mocks\": {\n    \"dashboardClient.customers\": [\n      { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\", \"email\": \"janet@maplegrove.co\", \"phoneNumber\": \"+14155550124\" },\n      { \"id\": \"cust-hd\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\", \"email\": \"ray@hilltop.co\", \"phoneNumber\": \"+14155550150\" },\n      { \"id\": \"cust-ml\", \"userId\": \"u1\", \"name\": \"Marcus Lin\", \"email\": \"marcus.lin@gmail.com\", \"phoneNumber\": \"+14155550188\" },\n      { \"id\": \"cust-gh\", \"userId\": \"u1\", \"name\": \"Greenleaf HOA\", \"phoneNumber\": \"+14155550190\" },\n      { \"id\": \"cust-cc\", \"userId\": \"u1\", \"name\": \"Cobblestone Cafe\", \"email\": \"hi@cobblestone.cafe\" },\n      { \"id\": \"cust-or\", \"userId\": \"u1\", \"name\": \"Ortega Rentals\", \"phoneNumber\": \"+14155550133\" }\n    ],\n    \"dashboardClient.invoices\": [\n      { \"id\": \"inv0a1b2c\", \"userId\": \"u1\", \"contractId\": \"ct-301\", \"customerId\": \"cust-mg\", \"amount\": 420000, \"issuedDate\": \"2026-05-04\", \"dueDate\": \"2026-05-18\", \"status\": \"sent\", \"installmentIndex\": 2, \"installmentTotal\": 3, \"createdAt\": \"2026-05-04T09:00:00.000Z\", \"updatedAt\": \"2026-05-04T09:00:00.000Z\" },\n      { \"id\": \"inv7d8e9f\", \"userId\": \"u1\", \"contractId\": \"ct-288\", \"customerId\": \"cust-hd\", \"amount\": 185000, \"issuedDate\": \"2026-05-20\", \"dueDate\": \"2026-06-03\", \"status\": \"viewed\", \"createdAt\": \"2026-05-20T09:00:00.000Z\", \"updatedAt\": \"2026-06-01T11:00:00.000Z\" },\n      { \"id\": \"inv1122aa\", \"userId\": \"u1\", \"contractId\": \"ct-310\", \"customerId\": \"cust-ml\", \"amount\": 96000, \"issuedDate\": \"2026-06-12\", \"dueDate\": \"2026-06-26\", \"status\": \"claimed\", \"createdAt\": \"2026-06-12T09:00:00.000Z\", \"updatedAt\": \"2026-06-17T15:30:00.000Z\", \"paymentIntent\": { \"method\": \"zelle\", \"amount\": 96000, \"reference\": \"ZL-88421\", \"claimedAt\": \"2026-06-17T15:30:00.000Z\", \"claimedBy\": \"Marcus Lin\" } },\n      { \"id\": \"inv3344bb\", \"userId\": \"u1\", \"contractId\": \"ct-309\", \"customerId\": \"cust-gh\", \"amount\": 580000, \"issuedDate\": \"2026-06-15\", \"dueDate\": \"2026-07-15\", \"status\": \"sent\", \"createdAt\": \"2026-06-15T09:00:00.000Z\", \"updatedAt\": \"2026-06-15T09:00:00.000Z\" },\n      { \"id\": \"inv5566cc\", \"userId\": \"u1\", \"contractId\": \"ct-301\", \"customerId\": \"cust-mg\", \"amount\": 420000, \"dueDate\": \"2026-07-30\", \"status\": \"scheduled\", \"scheduledFor\": \"2026-07-23\", \"installmentIndex\": 3, \"installmentTotal\": 3, \"createdAt\": \"2026-05-04T09:00:00.000Z\", \"updatedAt\": \"2026-05-04T09:00:00.000Z\" },\n      { \"id\": \"inv7788dd\", \"userId\": \"u1\", \"customerId\": \"cust-cc\", \"amount\": 142500, \"dueDate\": \"2026-07-18\", \"status\": \"draft\", \"createdAt\": \"2026-06-18T10:00:00.000Z\", \"updatedAt\": \"2026-06-18T10:00:00.000Z\" },\n      { \"id\": \"inv99aaee\", \"userId\": \"u1\", \"amount\": 64000, \"dueDate\": \"2026-07-05\", \"status\": \"pending\", \"createdAt\": \"2026-06-17T08:00:00.000Z\", \"updatedAt\": \"2026-06-17T08:00:00.000Z\" },\n      { \"id\": \"invbbccff\", \"userId\": \"u1\", \"contractId\": \"ct-275\", \"customerId\": \"cust-or\", \"amount\": 190000, \"issuedDate\": \"2026-06-01\", \"dueDate\": \"2026-06-15\", \"status\": \"paid\", \"paidAt\": \"2026-06-14T13:00:00.000Z\", \"createdAt\": \"2026-06-01T09:00:00.000Z\", \"updatedAt\": \"2026-06-14T13:00:00.000Z\" },\n      { \"id\": \"invddee00\", \"userId\": \"u1\", \"contractId\": \"ct-260\", \"customerId\": \"cust-hd\", \"amount\": 38000, \"issuedDate\": \"2026-04-01\", \"dueDate\": \"2026-04-15\", \"status\": \"void\", \"createdAt\": \"2026-04-01T09:00:00.000Z\", \"updatedAt\": \"2026-04-20T09:00:00.000Z\" }\n    ],\n    \"fetch:/api/invoices/forecast/this-week\": \"__reject:404__\"\n  },\n  \"_note\": \"Statuses chosen to exercise every Invoice union value (scheduled|draft|pending|sent|viewed|claimed|paid|void) and the island's 6 derived buckets. With forecast 404'd the hero falls back to the legacy 'on the way' headline. inv0a1b2c (sent, due 2026-05-18 < today 2026-06-18) enriches to OVERDUE; inv7d8e9f (viewed, due 2026-06-03 < today) → OVERDUE; inv3344bb (sent, due in future) → OUT; inv1122aa → CLAIMED; inv5566cc → SCHEDULED; inv7788dd (draft) + inv99aaee (pending, no issuedDate) → DRAFTING; invbbccff → PAID-this-month; invddee00 (void, due 2026-04-15 < today, not paid/claimed/scheduled/draft) → daysOverdue>0 → lands in the OVERDUE bucket (no void handling — a real gap: void invoices are NOT filtered out and surface as overdue receivables).\"\n}\n"}], "css": "/* ============================================================================\n   InvoicesPage — the page-island's OWN layout chrome. The hero/KPI/card/track\n   visuals live in their sub-component CSS files (invoices-hero.css,\n   invoices-kpis.css, invoice-card.css) and in QuoteTrack[shared]; what belongs\n   to the page wrapper itself is the `.qlay` grid + the `.qpage-error` branch.\n\n   Ported verbatim from static/quotes.css (the invoices route loads\n   /dashboard.css + /quotes.css — there is NO invoices.css). Tokens come from\n   /dashboard.css.\n   ============================================================================ */\n\n/* The single-column layout the island wraps its 6 tracks in.\n   Source markup: <div class=\"qlay\"><div> …tracks… </div></div>.\n   NOTE the right rail (320px) is defined in the base rule but the invoices\n   island never renders a `.qside`, so on wide screens the second column is an\n   empty 320px gutter — a real layout quirk to flag (Quotes uses the rail; the\n   Invoices page copied the grid but dropped the rail). */\n.qlay {\n  display: grid;\n  grid-template-columns: 1fr 320px;\n  gap: 22px;\n  align-items: flex-start;\n}\n\n@media (max-width: 1200px) {\n  .qlay {\n    grid-template-columns: 1fr;\n  }\n  .qph {\n    flex-direction: column;\n    align-items: flex-start;\n  }\n  .qkpi {\n    grid-template-columns: repeat(2, 1fr);\n  }\n}\n\n@media (max-width: 768px) {\n  .qcards {\n    grid-template-columns: 1fr;\n  }\n  .qkpi {\n    grid-template-columns: 1fr;\n  }\n}\n\n/* ----------------------------------------------------------------------------\n   .qpage-error — the error branch ( {tFor(\"invoicesPage.loadError\")} {msg} ).\n   HAZARD: there is NO CSS rule for `.qpage-error` anywhere in quotes.css or\n   dashboard.css. The class is dead — the error renders as an unstyled default\n   <div> of body text, with no color/spacing/role=\"alert\". This is the same\n   gap flagged on quotes-page (.qpage-error) and clients-page (.cpage-error).\n\n   The block below is the REBUILD PROPOSAL (not present in source) so the error\n   state is styled + announced. Delete the \"proposal\" note when adopting.\n   ---------------------------------------------------------------------------- */\n.qpage-error {\n  /* PROPOSAL — source ships nothing; this makes the failure visible + a11y-ok */\n  margin: 22px 0;\n  padding: 14px 16px;\n  border-radius: var(--radius-lg);\n  background: var(--danger-bg, #fff1f1);\n  border: 1px solid rgba(224, 49, 49, 0.24);\n  color: var(--danger, #e03131);\n  font-family: var(--font-body);\n  font-size: 14px;\n  /* pair with role=\"alert\" in the markup on rebuild */\n}\n", "cssName": "invoices-page.css", "source": "/**\n * /invoices island — receivables view matching the canonical reference\n * (reference/extracted/Paperwork Monster Invoices.html). Despite the URL\n * saying \"Invoices\", the components share the Quotes code path with copy +\n * stage names swapped — that's why the classes are `.qph__*` / `.qkpi__*` /\n * `.qtrack__*` / `.qcard__*` (loaded from quotes.css).\n *\n * Structure:\n *   InvoicesHero  (.qph editorial header — outstanding total + sub)\n *   InvoicesKpis  (.qkpi 4-cell: Overdue / Out / Drafts / Paid this month)\n *   .qlay → 4 collapsible Track sections of InvoiceCard flip cards:\n *     01 Overdue · needs a poke      (sorted by daysOverdue desc, defaultOpen)\n *     02 Out for payment             (sorted by daysIn desc)\n *     03 Drafting                    (defaultOpen=false)\n *     04 Paid this month             (defaultOpen=false)\n */\nimport { useEffect, useMemo, useState } from \"preact/hooks\";\nimport {\n  type Customer,\n  dashboardClient,\n  type Invoice,\n} from \"../clients/dashboard.ts\";\nimport { clientsClient } from \"../clients/clients.ts\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport {\n  CardGridSkeleton,\n  PageHeaderSkeleton,\n  ShimmerStyle,\n} from \"../components/Skeletons.tsx\";\nimport { fmtMoney, fmtMoneyExact } from \"../lib/format.ts\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\nimport QuoteTrack from \"./QuoteTrack.tsx\";\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  invoices: Invoice[];\n  customers: Customer[];\n}\n\nconst INITIAL: State = {\n  loading: true,\n  error: null,\n  invoices: [],\n  customers: [],\n};\n\nconst SHORT_MONTH = [\n  \"Jan\",\n  \"Feb\",\n  \"Mar\",\n  \"Apr\",\n  \"May\",\n  \"Jun\",\n  \"Jul\",\n  \"Aug\",\n  \"Sep\",\n  \"Oct\",\n  \"Nov\",\n  \"Dec\",\n];\n\n/** Map an Invoice.paymentIntent.method (lowercase canonical) to the\n *  customer-facing word used in the \"customer paid X\" subline. Returns\n *  \"—\" for missing/unknown values rather than an empty string so the\n *  card layout doesn't collapse. */\nfunction methodLabel(m: string | undefined, lang: Lang): string {\n  switch (m) {\n    case \"check\":\n      return tFor(lang, \"invoicesPage.method.check\");\n    case \"venmo\":\n      return tFor(lang, \"invoicesPage.method.venmo\");\n    case \"zelle\":\n      return tFor(lang, \"invoicesPage.method.zelle\");\n    case \"cashapp\":\n      return tFor(lang, \"invoicesPage.method.cashApp\");\n    case \"paypal\":\n      return tFor(lang, \"invoicesPage.method.paypal\");\n    case \"cash\":\n      return tFor(lang, \"invoicesPage.method.cash\");\n    case \"ach\":\n      return tFor(lang, \"invoicesPage.method.ach\");\n    case \"card\":\n      return tFor(lang, \"invoicesPage.method.card\");\n    case \"other\":\n      return tFor(lang, \"invoicesPage.method.other\");\n    default:\n      return \"—\";\n  }\n}\n\n/** \"2026-05-19\" → \"Tue\" / \"May 19\" — short day-of-week label for the\n *  forecast hero breakdown. Falls back to MMM d when the date is more\n *  than a week out. */\nfunction shortDay(iso: string): string {\n  const d = new Date(`${iso}T12:00:00Z`);\n  if (!Number.isFinite(d.getTime())) return iso;\n  const diffDays = Math.round((d.getTime() - Date.now()) / (24 * 3600 * 1000));\n  if (diffDays >= 0 && diffDays < 7) {\n    return d.toLocaleDateString(\"en-US\", { weekday: \"short\", timeZone: \"UTC\" });\n  }\n  return d.toLocaleDateString(\"en-US\", {\n    month: \"short\",\n    day: \"numeric\",\n    timeZone: \"UTC\",\n  });\n}\n\nfunction monthLabel(idx: number, lang: Lang): string {\n  return tFor(lang, `invoicesPage.month.${SHORT_MONTH[idx].toLowerCase()}`);\n}\n\nfunction fmtDate(iso: string | undefined, now: Date, lang: Lang): string {\n  if (!iso) return \"—\";\n  const d = new Date(iso + (iso.length === 10 ? \"T00:00:00\" : \"\"));\n  if (Number.isNaN(d.getTime())) return iso;\n  const sameYear = d.getFullYear() === now.getFullYear();\n  return sameYear\n    ? `${monthLabel(d.getMonth(), lang)} ${d.getDate()}`\n    : `${monthLabel(d.getMonth(), lang)} ${d.getDate()}, ${d.getFullYear()}`;\n}\n\nfunction initialsOf(name: string): string {\n  const parts = name.trim().split(/\\s+/).filter(Boolean);\n  if (parts.length === 0) return \"—\";\n  if (parts.length === 1) return parts[0].slice(0, 2).toUpperCase();\n  return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();\n}\n\ninterface EnrichedInvoice extends Invoice {\n  client: string;\n  initials: string;\n  invoiceRef: string;\n  daysOverdue: number;\n  daysIn: number;\n  stage: \"scheduled\" | \"overdue\" | \"out\" | \"claimed\" | \"drafting\" | \"paid\";\n}\n\n/** Detect a draft invoice broadly. The backend's status field is an open\n *  string; \"draft\" is the canonical value, but we also catch \"drafting\" /\n *  \"drafted\" variants and any pending invoice that has no issuedDate set\n *  (i.e. created but not yet sent). */\nfunction isDraft(inv: Invoice): boolean {\n  const status = (inv.status ?? \"\").toLowerCase();\n  if (status === \"draft\" || status === \"drafting\" || status === \"drafted\") {\n    return true;\n  }\n  // No status string AND no issuedDate AND not paid → still being prepped.\n  if (!status && !inv.issuedDate && !inv.paidAt) return true;\n  // Pending status but never issued → also a draft.\n  if (status === \"pending\" && !inv.issuedDate) return true;\n  return false;\n}\n\nfunction enrich(\n  inv: Invoice,\n  customers: Map<string, string>,\n  now: Date,\n): EnrichedInvoice {\n  const client = (inv.customerId && customers.get(inv.customerId)) || \"—\";\n  const today = now.toISOString().slice(0, 10);\n  const due = inv.dueDate ?? \"\";\n  const issued = inv.issuedDate ?? inv.createdAt ?? \"\";\n  const rawStatus = (inv.status ?? \"\").toLowerCase();\n  const isPaid = rawStatus === \"paid\" || !!inv.paidAt;\n  const isClaimed = rawStatus === \"claimed\";\n  const isScheduled = rawStatus === \"scheduled\";\n  const draft = !isPaid && !isClaimed && !isScheduled && isDraft(inv);\n  const daysOverdue =\n    (!isPaid && !isClaimed && !isScheduled && !draft && due && due < today)\n      ? Math.floor(\n        (now.getTime() - new Date(due + \"T00:00:00\").getTime()) /\n          (24 * 3600 * 1000),\n      )\n      : 0;\n  const daysIn = issued\n    ? Math.max(\n      0,\n      Math.floor(\n        (now.getTime() - new Date(issued).getTime()) / (24 * 3600 * 1000),\n      ),\n    )\n    : 0;\n  const stage: EnrichedInvoice[\"stage\"] = isPaid\n    ? \"paid\"\n    : isClaimed\n    ? \"claimed\"\n    : isScheduled\n    ? \"scheduled\"\n    : draft\n    ? \"drafting\"\n    : daysOverdue > 0\n    ? \"overdue\"\n    : \"out\";\n  return {\n    ...inv,\n    client,\n    initials: initialsOf(client),\n    invoiceRef: `INV-${inv.id.slice(0, 6).toUpperCase()}`,\n    daysOverdue,\n    daysIn,\n    stage,\n  };\n}\n\nconst STAGE_MOOD: Record<\n  EnrichedInvoice[\"stage\"],\n  {\n    from: string;\n    to: string;\n    shadow: string;\n    statusFg: string;\n    /** i18n key resolved at render via `tFor(lang, labelKey)`. */\n    labelKey: string;\n  }\n> = {\n  overdue: {\n    from: \"#FFD9D9\",\n    to: \"#FF6B6B\",\n    shadow: \"rgba(255,107,107,0.30)\",\n    statusFg: \"#fff\",\n    labelKey: \"status.overdue\",\n  },\n  out: {\n    from: \"#C8DDE0\",\n    to: \"#56969E\",\n    shadow: \"rgba(86,150,158,0.28)\",\n    statusFg: \"#0F3036\",\n    labelKey: \"invoicesPage.stage.out\",\n  },\n  claimed: {\n    from: \"#FFE7B5\",\n    to: \"#E5A331\",\n    shadow: \"rgba(229,163,49,0.30)\",\n    statusFg: \"#5A3D08\",\n    labelKey: \"invoicesPage.stage.awaitingConfirmation\",\n  },\n  scheduled: {\n    from: \"#E4E0F7\",\n    to: \"#8B7DBF\",\n    shadow: \"rgba(139,125,191,0.28)\",\n    statusFg: \"#2C254A\",\n    labelKey: \"invoicesPage.stage.scheduled\",\n  },\n  drafting: {\n    from: \"#E1D7CD\",\n    to: \"#9C8074\",\n    shadow: \"rgba(156,128,116,0.32)\",\n    statusFg: \"#3F2D24\",\n    labelKey: \"status.draft\",\n  },\n  paid: {\n    from: \"#CFE5C8\",\n    to: \"#5FA34F\",\n    shadow: \"rgba(81,152,67,0.30)\",\n    statusFg: \"#1F3F18\",\n    labelKey: \"status.paid\",\n  },\n};\n\ninterface ForecastEntry {\n  expectedLandDate: string;\n  amount: number;\n  label: string;\n  invoiceId: string;\n  source: \"claimed\" | \"sent_due\" | \"scheduled\" | \"paid\";\n}\n\ninterface ForecastResult {\n  thisWeekCents: number;\n  thisWeek: ForecastEntry[];\n  nextWeekCents: number;\n  atRiskCents: number;\n  atRisk: ForecastEntry[];\n  asOf: string;\n}\n\nexport default function InvoicesPage(_props: { lang?: Lang }) {\n  // Self-source the reactive UI language. Reading langSignal.value during\n  // render makes this island re-render live when SettingsPage flips the\n  // language. The optional `lang` prop is an ignored SSR seed.\n  const lang = langSignal.value;\n  const [s, setS] = useState<State>(INITIAL);\n  const [forecast, setForecast] = useState<ForecastResult | undefined>(\n    undefined,\n  );\n  const [newOpen, setNewOpen] = useState(false);\n\n  useEffect(() => {\n    let alive = true;\n    Promise.all([\n      dashboardClient.invoices(undefined).catch(() => [] as Invoice[]),\n      dashboardClient.customers().catch(() => [] as Customer[]),\n    ]).then(([invoices, customers]) => {\n      if (!alive) return;\n      setS({ loading: false, error: null, invoices, customers });\n    }).catch((err: Error) => {\n      if (!alive) return;\n      setS({ ...INITIAL, loading: false, error: err.message });\n    });\n    // Forecast hero — fire-and-forget. If it 404s on older backends we\n    // silently fall back to the legacy \"outstanding total\" headline.\n    fetch(\"/api/invoices/forecast/this-week\", { credentials: \"include\" })\n      .then((r) => r.ok ? r.json() as Promise<ForecastResult> : undefined)\n      .then((f) => {\n        if (alive && f) setForecast(f);\n      })\n      .catch(() => {/* ignore */});\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  const customerNames = useMemo(\n    () =>\n      new Map(\n        (Array.isArray(s.customers) ? s.customers : []).map((\n          c,\n        ) => [c.id, c.name]),\n      ),\n    [s.customers],\n  );\n\n  if (s.loading) {\n    return (\n      <>\n        <ShimmerStyle />\n        <PageHeaderSkeleton />\n        <CardGridSkeleton rows={2} />\n      </>\n    );\n  }\n  if (s.error) {\n    return (\n      <div class=\"qpage-error\">\n        {tFor(lang, \"invoicesPage.loadError\")} {s.error}\n      </div>\n    );\n  }\n\n  const now = new Date();\n  const enriched = (Array.isArray(s.invoices) ? s.invoices : []).map((i) =>\n    enrich(i, customerNames, now)\n  );\n\n  const overdue = enriched.filter((i) => i.stage === \"overdue\").sort((a, b) =>\n    b.daysOverdue - a.daysOverdue\n  );\n  const out = enriched.filter((i) => i.stage === \"out\").sort((a, b) =>\n    b.daysIn - a.daysIn\n  );\n  const claimed = enriched.filter((i) => i.stage === \"claimed\").sort((a, b) =>\n    (b.paymentIntent?.claimedAt ?? \"\").localeCompare(\n      a.paymentIntent?.claimedAt ?? \"\",\n    )\n  );\n  const scheduled = enriched.filter((i) => i.stage === \"scheduled\").sort((\n    a,\n    b,\n  ) => (a.scheduledFor ?? \"\").localeCompare(b.scheduledFor ?? \"\"));\n  const drafting = enriched.filter((i) => i.stage === \"drafting\");\n  const monthCutoff = new Date(now.getFullYear(), now.getMonth(), 1);\n  const paidThisMonth = enriched.filter((i) =>\n    i.stage === \"paid\" && i.paidAt && new Date(i.paidAt) >= monthCutoff\n  );\n\n  const outstandingTotal = [...overdue, ...out].reduce(\n    (sum, i) => sum + (i.amount ?? 0),\n    0,\n  );\n  const overdueTotal = overdue.reduce((sum, i) => sum + (i.amount ?? 0), 0);\n  const outTotal = out.reduce((sum, i) => sum + (i.amount ?? 0), 0);\n  const paidThisMonthTotal = paidThisMonth.reduce(\n    (sum, i) => sum + (i.amount ?? 0),\n    0,\n  );\n\n  return (\n    <>\n      <InvoicesHero\n        outstandingTotal={outstandingTotal}\n        outstandingCount={overdue.length + out.length}\n        overdueCount={overdue.length}\n        totalInvoiceCount={enriched.length}\n        forecast={forecast}\n        lang={lang}\n        onNew={() => setNewOpen(true)}\n      />\n      {newOpen && (\n        <NewInvoiceModal\n          customers={Array.isArray(s.customers) ? s.customers : []}\n          lang={lang}\n          onClose={() => setNewOpen(false)}\n        />\n      )}\n      <InvoicesKpis\n        overdueCount={overdue.length}\n        overdueTotal={overdueTotal}\n        outCount={out.length}\n        outTotal={outTotal}\n        draftingCount={drafting.length}\n        paidCount={paidThisMonth.length}\n        paidTotal={paidThisMonthTotal}\n        lang={lang}\n      />\n\n      <div class=\"qlay\">\n        <div>\n          <QuoteTrack\n            num=\"01\"\n            title={tFor(lang, \"invoicesPage.track.overdueTitle\")}\n            count={overdue.length}\n            unit={tFor(lang, \"invoicesPage.unit.invoice\")}\n            defaultOpen\n            storageKey=\"invoices:track:01\"\n          >\n            {overdue.length === 0\n              ? <EmptyTrack hint={tFor(lang, \"invoicesPage.empty.overdue\")} />\n              : (\n                <div class=\"qcards\">\n                  {overdue.map((inv, i) => (\n                    <InvoiceCard\n                      key={inv.id}\n                      inv={inv}\n                      idx={i}\n                      now={now}\n                      lang={lang}\n                    />\n                  ))}\n                </div>\n              )}\n          </QuoteTrack>\n\n          <QuoteTrack\n            num=\"02\"\n            title={tFor(lang, \"invoicesPage.track.awaitingTitle\")}\n            count={claimed.length}\n            unit={tFor(lang, \"invoicesPage.unit.invoice\")}\n            defaultOpen\n            storageKey=\"invoices:track:awaiting\"\n          >\n            {claimed.length === 0\n              ? <EmptyTrack hint={tFor(lang, \"invoicesPage.empty.awaiting\")} />\n              : (\n                <div class=\"qcards\" data-cy=\"awaiting-confirmation-track\">\n                  {claimed.map((inv, i) => (\n                    <InvoiceCard\n                      key={inv.id}\n                      inv={inv}\n                      idx={i}\n                      now={now}\n                      lang={lang}\n                    />\n                  ))}\n                </div>\n              )}\n          </QuoteTrack>\n\n          <QuoteTrack\n            num=\"03\"\n            title={tFor(lang, \"invoicesPage.track.outTitle\")}\n            count={out.length}\n            unit={tFor(lang, \"invoicesPage.unit.invoice\")}\n            defaultOpen\n            storageKey=\"invoices:track:02\"\n          >\n            {out.length === 0\n              ? <EmptyTrack hint={tFor(lang, \"invoicesPage.empty.out\")} />\n              : (\n                <div class=\"qcards\">\n                  {out.map((inv, i) => (\n                    <InvoiceCard\n                      key={inv.id}\n                      inv={inv}\n                      idx={i}\n                      now={now}\n                      lang={lang}\n                    />\n                  ))}\n                </div>\n              )}\n          </QuoteTrack>\n\n          <QuoteTrack\n            num=\"04\"\n            title={tFor(lang, \"invoicesPage.track.upcomingTitle\")}\n            count={scheduled.length}\n            unit={tFor(lang, \"invoicesPage.unit.invoice\")}\n            defaultOpen={false}\n            storageKey=\"invoices:track:upcoming\"\n          >\n            {scheduled.length === 0\n              ? <EmptyTrack hint={tFor(lang, \"invoicesPage.empty.scheduled\")} />\n              : (\n                <div class=\"qcards\" data-cy=\"upcoming-track\">\n                  {scheduled.map((inv, i) => (\n                    <InvoiceCard\n                      key={inv.id}\n                      inv={inv}\n                      idx={i}\n                      now={now}\n                      lang={lang}\n                    />\n                  ))}\n                </div>\n              )}\n          </QuoteTrack>\n\n          <QuoteTrack\n            num=\"05\"\n            title={tFor(lang, \"invoicesPage.track.draftingTitle\")}\n            count={drafting.length}\n            unit={tFor(lang, \"invoicesPage.unit.invoice\")}\n            defaultOpen={false}\n            storageKey=\"invoices:track:03\"\n          >\n            {drafting.length === 0\n              ? <EmptyTrack hint={tFor(lang, \"invoicesPage.empty.drafting\")} />\n              : (\n                <div class=\"qcards\">\n                  {drafting.map((inv, i) => (\n                    <InvoiceCard\n                      key={inv.id}\n                      inv={inv}\n                      idx={i}\n                      now={now}\n                      lang={lang}\n                    />\n                  ))}\n                </div>\n              )}\n          </QuoteTrack>\n\n          <QuoteTrack\n            num=\"06\"\n            title={tFor(lang, \"invoicesPage.track.paidTitle\")}\n            count={paidThisMonth.length}\n            unit={tFor(lang, \"invoicesPage.unit.invoice\")}\n            defaultOpen={false}\n            storageKey=\"invoices:track:04\"\n          >\n            {paidThisMonth.length === 0\n              ? <EmptyTrack hint={tFor(lang, \"invoicesPage.empty.paid\")} />\n              : (\n                <div class=\"qcards\">\n                  {paidThisMonth.map((inv, i) => (\n                    <InvoiceCard\n                      key={inv.id}\n                      inv={inv}\n                      idx={i}\n                      now={now}\n                      lang={lang}\n                    />\n                  ))}\n                </div>\n              )}\n          </QuoteTrack>\n        </div>\n      </div>\n    </>\n  );\n}\n\n/* ---------------- Hero ---------------- */\n\nfunction InvoicesHero(\n  {\n    outstandingTotal,\n    outstandingCount,\n    overdueCount,\n    totalInvoiceCount,\n    forecast,\n    lang,\n    onNew,\n  }: {\n    outstandingTotal: number;\n    outstandingCount: number;\n    overdueCount: number;\n    totalInvoiceCount: number;\n    forecast?: ForecastResult;\n    lang: Lang;\n    onNew: () => void;\n  },\n) {\n  const trulyEmpty = totalInvoiceCount === 0;\n  const fresh = !trulyEmpty && outstandingCount === 0;\n  const haveForecast = !!forecast &&\n    (forecast.thisWeekCents > 0 || forecast.nextWeekCents > 0 ||\n      forecast.atRiskCents > 0);\n  return (\n    <header class=\"qph\">\n      <div class=\"qph__copy\">\n        <div class=\"qph__eyebrow\">\n          <span class=\"qph__eyebrow-dot\" /> {tFor(lang, \"invoicesPage.eyebrow\")}\n        </div>\n        <h1 class=\"qph__title\" data-cy=\"forecast-hero\">\n          {trulyEmpty\n            ? (\n              <>\n                {tFor(lang, \"invoicesPage.hero.emptyPre\")}{\" \"}\n                <em>{tFor(lang, \"invoicesPage.hero.emptyEm\")}</em>.\n              </>\n            )\n            : fresh && !haveForecast\n            ? (\n              <>\n                {tFor(lang, \"invoicesPage.hero.clearPre\")}{\" \"}\n                <em>{tFor(lang, \"invoicesPage.hero.clearEm\")}</em>.\n              </>\n            )\n            : haveForecast && forecast!.thisWeekCents > 0\n            ? (\n              <>\n                <em>{fmtMoney(forecast!.thisWeekCents)}</em>{\" \"}\n                {tFor(lang, \"invoicesPage.hero.expectedThisWeek\")}\n                <br />\n                {tFor(lang, \"invoicesPage.hero.across\")} {forecast!.thisWeek\n                  .length} {tFor(\n                    lang,\n                    forecast!.thisWeek.length === 1\n                      ? \"invoicesPage.unitPayment.one\"\n                      : \"invoicesPage.unitPayment.other\",\n                    { n: forecast!.thisWeek.length },\n                  )}.\n              </>\n            )\n            : haveForecast && forecast!.nextWeekCents > 0\n            ? (\n              <>\n                {tFor(lang, \"invoicesPage.hero.quietPre\")}{\" \"}\n                <em>{fmtMoney(forecast!.nextWeekCents)}</em>{\" \"}\n                {tFor(lang, \"invoicesPage.hero.comingNextWeek\")}\n              </>\n            )\n            : (\n              <>\n                <em>{fmtMoney(outstandingTotal)}</em>{\" \"}\n                {tFor(lang, \"invoicesPage.hero.onTheWay\")}\n                <br />\n                {tFor(lang, \"invoicesPage.hero.across\")} {outstandingCount}{\" \"}\n                {tFor(\n                  lang,\n                  outstandingCount === 1\n                    ? \"invoicesPage.unitInvoice.one\"\n                    : \"invoicesPage.unitInvoice.other\",\n                  { n: outstandingCount },\n                )}.\n              </>\n            )}\n        </h1>\n        <p class=\"qph__sub\">\n          {trulyEmpty\n            ? <>{tFor(lang, \"invoicesPage.sub.empty\")}</>\n            : haveForecast && forecast!.thisWeek.length > 0\n            ? (\n              <span data-cy=\"forecast-breakdown\">\n                {forecast!.thisWeek.slice(0, 3).map((e, i) => (\n                  <span key={e.invoiceId}>\n                    {i > 0 ? \" · \" : \"\"}\n                    {shortDay(e.expectedLandDate)}:{\" \"}\n                    <strong>{e.label} {fmtMoney(e.amount)}</strong>\n                  </span>\n                ))}\n              </span>\n            )\n            : overdueCount > 0\n            ? (\n              <>\n                <strong>{overdueCount}</strong> {tFor(\n                  lang,\n                  overdueCount === 1\n                    ? \"invoicesPage.sub.pastDueVerb.one\"\n                    : \"invoicesPage.sub.pastDueVerb.other\",\n                  { n: overdueCount },\n                )} {tFor(lang, \"invoicesPage.sub.pastDue\")}\n              </>\n            )\n            : <>{tFor(lang, \"invoicesPage.sub.nothingPastDue\")}</>}\n        </p>\n        {haveForecast && forecast!.atRiskCents > 0\n          ? (\n            <p\n              class=\"qph__sub\"\n              style=\"color:#a83b3b\"\n              data-cy=\"forecast-at-risk\"\n            >\n              ⚠ <strong>{fmtMoney(forecast!.atRiskCents)}</strong>{\" \"}\n              {tFor(lang, \"invoicesPage.atRisk.across\")} {forecast!.atRisk\n                .length} {tFor(lang, \"invoicesPage.atRisk.overdue\")} {tFor(\n                  lang,\n                  forecast!.atRisk.length === 1\n                    ? \"invoicesPage.unitInvoice.one\"\n                    : \"invoicesPage.unitInvoice.other\",\n                  { n: forecast!.atRisk.length },\n                )}.\n            </p>\n          )\n          : null}\n        <div class=\"qph__cta-row\">\n          <button\n            type=\"button\"\n            class=\"qph__cta\"\n            data-cy=\"invoice-new\"\n            onClick={onNew}\n            style=\"appearance:none;cursor:pointer;font:inherit\"\n          >\n            <I d={ICN.plus} size={14} sw={2.5} />{\" \"}\n            {tFor(lang, \"invoicesPage.newInvoice\")}\n          </button>\n          <a\n            class=\"qph__cta qph__cta--ghost\"\n            data-cy=\"invoice-export\"\n            href={`/api/invoices/export.csv?year=${new Date().getFullYear()}`}\n            style=\"margin-left:10px;background:transparent;border:1px solid currentColor\"\n          >\n            {tFor(lang, \"invoicesPage.exportCsv\", {\n              year: new Date().getFullYear(),\n            })}\n          </a>\n        </div>\n      </div>\n    </header>\n  );\n}\n\n/* ---------------- KPIs ---------------- */\n\nfunction InvoicesKpis(\n  {\n    overdueCount,\n    overdueTotal,\n    outCount,\n    outTotal,\n    draftingCount,\n    paidCount,\n    paidTotal,\n    lang,\n  }: {\n    overdueCount: number;\n    overdueTotal: number;\n    outCount: number;\n    outTotal: number;\n    draftingCount: number;\n    paidCount: number;\n    paidTotal: number;\n    lang: Lang;\n  },\n) {\n  return (\n    <div class=\"qkpi\">\n      <div class={`qkpi__cell${overdueCount > 0 ? \" qkpi__cell--accent\" : \"\"}`}>\n        <div class=\"qkpi__lbl\">{tFor(lang, \"status.overdue\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(overdueTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {overdueCount} {tFor(\n            lang,\n            overdueCount === 1\n              ? \"invoicesPage.unitInvoice.one\"\n              : \"invoicesPage.unitInvoice.other\",\n            { n: overdueCount },\n          )}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"invoicesPage.kpi.out\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(outTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"invoicesPage.kpi.outSub\", { n: outCount })}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"invoicesPage.kpi.drafting\")}</div>\n        <div class=\"qkpi__val\">{draftingCount}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(\n            lang,\n            draftingCount === 0\n              ? \"invoicesPage.kpi.draftingSubEmpty\"\n              : \"invoicesPage.kpi.draftingSub\",\n          )}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"invoicesPage.kpi.paid\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(paidTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"invoicesPage.kpi.paidSub\", { n: paidCount })}\n        </div>\n      </div>\n    </div>\n  );\n}\n\n/* ---------------- Empty track copy ---------------- */\n\nfunction EmptyTrack({ hint }: { hint: string }) {\n  return (\n    <div style=\"padding:14px 4px;color:var(--fg-muted, #6b7560);font-size:13.5px;line-height:1.5\">\n      {hint}\n    </div>\n  );\n}\n\n/* ---------------- Invoice card (flip) ---------------- */\n\n/** Change-order row as returned by GET /invoices/:id/change-orders. */\ninterface ChangeOrderRow {\n  id: string;\n  description: string;\n  deltaAmountCents: number;\n  status: \"pending\" | \"approved\" | \"declined\";\n  createdAt: string;\n}\n\nconst CO_CHIP_COLOR: Record<ChangeOrderRow[\"status\"], string> = {\n  pending: \"#b07d2a\",\n  approved: \"var(--brand-green)\",\n  declined: \"#a83b3b\",\n};\n\nfunction InvoiceCard(\n  { inv, idx, now, lang }: {\n    inv: EnrichedInvoice;\n    idx: number;\n    now: Date;\n    lang: Lang;\n  },\n) {\n  const [flipped, setFlipped] = useState(false);\n  const [busy, setBusy] = useState(false);\n  // Roadmap p.12: in-card discount + change-order controls.\n  const [adjustOpen, setAdjustOpen] = useState(false);\n  const [discountDollars, setDiscountDollars] = useState(\"\");\n  const [coDesc, setCoDesc] = useState(\"\");\n  const [coDollars, setCoDollars] = useState(\"\");\n  const [coLink, setCoLink] = useState<string | null>(null);\n  const [adjErr, setAdjErr] = useState<string | null>(null);\n  // Existing change orders for this invoice — loaded when the panel opens\n  // so pending approval links stay recoverable (audit: \"unrecoverable links\").\n  const [changeOrders, setChangeOrders] = useState<ChangeOrderRow[] | null>(\n    null,\n  );\n  const [copiedCoId, setCopiedCoId] = useState<string | null>(null);\n  const mood = STAGE_MOOD[inv.stage];\n  const moodLabel = tFor(lang, mood.labelKey);\n  const cta = inv.stage === \"claimed\"\n    ? tFor(lang, \"invoicesPage.cta.claimed\")\n    : inv.stage === \"overdue\"\n    ? tFor(lang, \"invoicesPage.cta.overdue\")\n    : inv.stage === \"scheduled\"\n    ? tFor(lang, \"invoicesPage.cta.scheduled\")\n    : inv.stage === \"out\"\n    ? tFor(lang, \"invoicesPage.cta.out\")\n    : inv.stage === \"drafting\"\n    ? tFor(lang, \"invoicesPage.cta.drafting\")\n    : tFor(lang, \"invoicesPage.cta.paid\");\n  const subline = inv.stage === \"claimed\"\n    ? `${\n      tFor(lang, \"invoicesPage.subline.claimed\", {\n        method: methodLabel(inv.paymentIntent?.method, lang),\n      })\n    }${\n      inv.paymentIntent?.reference\n        ? tFor(lang, \"invoicesPage.subline.ref\", {\n          ref: inv.paymentIntent.reference,\n        })\n        : \"\"\n    }`\n    : inv.stage === \"overdue\"\n    ? tFor(lang, \"invoicesPage.subline.overdue\", {\n      n: inv.daysOverdue,\n      date: fmtDate(inv.dueDate, now, lang),\n    })\n    : inv.stage === \"scheduled\"\n    ? tFor(lang, \"invoicesPage.subline.scheduled\", {\n      date: inv.scheduledFor ?? \"—\",\n    })\n    : inv.stage === \"out\"\n    ? tFor(lang, \"invoicesPage.subline.out\", {\n      n: inv.daysIn,\n      date: fmtDate(inv.dueDate, now, lang),\n    })\n    : inv.stage === \"drafting\"\n    ? tFor(lang, \"invoicesPage.subline.drafting\", {\n      date: fmtDate(inv.issuedDate ?? inv.createdAt, now, lang),\n    })\n    : tFor(lang, \"invoicesPage.subline.paid\", {\n      date: fmtDate(inv.paidAt, now, lang),\n    });\n\n  async function doConfirmReceived(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    setBusy(true);\n    try {\n      const r = await fetch(`/api/invoices/${inv.id}/confirm-payment`, {\n        method: \"POST\",\n        credentials: \"include\",\n      });\n      if (r.ok) globalThis.location.reload();\n    } finally {\n      setBusy(false);\n    }\n  }\n  // \"Didn't get it\" — reopen a claim the customer reported but you never\n  // actually received. Reverts the invoice to sent so they can re-pay.\n  async function doRejectClaim(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    setBusy(true);\n    try {\n      const r = await fetch(`/api/invoices/${inv.id}/reject-claim`, {\n        method: \"POST\",\n        credentials: \"include\",\n      });\n      if (r.ok) globalThis.location.reload();\n    } finally {\n      setBusy(false);\n    }\n  }\n  function doOpenInvoice(e: Event) {\n    e.stopPropagation();\n    // Public invoice page is the canonical detail surface today — opens\n    // in a new tab so the dashboard stays put.\n    globalThis.open(`/i/${inv.id}`, \"_blank\", \"noopener,noreferrer\");\n  }\n  async function doSendText(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    setBusy(true);\n    try {\n      const r = await fetch(`/api/invoices/${inv.id}/text`, {\n        method: \"POST\",\n        credentials: \"include\",\n      });\n      if (r.ok) globalThis.location.reload();\n    } finally {\n      setBusy(false);\n    }\n  }\n  async function doSendNow(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    setBusy(true);\n    try {\n      // Fire both channels — the backend coordinator handles \"no\n      // email/phone on file\" gracefully and the user gets a fresh state\n      // on reload either way.\n      await Promise.allSettled([\n        fetch(`/api/invoices/${inv.id}/email`, {\n          method: \"POST\",\n          credentials: \"include\",\n        }),\n        fetch(`/api/invoices/${inv.id}/text`, {\n          method: \"POST\",\n          credentials: \"include\",\n        }),\n      ]);\n      globalThis.location.reload();\n    } finally {\n      setBusy(false);\n    }\n  }\n  // \"Finish + send\" on a draft: a fully deterministic dashboard action — no\n  // assistant round-trip. Finalize the invoice (draft → sent, stamp issuedDate\n  // if missing), then dispatch to the customer over both channels. The backend\n  // send coordinators only stamp quotes as \"sent\", so we flip the invoice\n  // status ourselves up front; that also moves the card to \"Out for payment\"\n  // even when there's no email/phone on file to deliver to.\n  async function doFinishDraft(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    setBusy(true);\n    try {\n      await fetch(`/api/invoices/${inv.id}`, {\n        method: \"PUT\",\n        credentials: \"include\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({\n          status: \"sent\",\n          ...(inv.issuedDate\n            ? {}\n            : { issuedDate: new Date().toISOString().slice(0, 10) }),\n        }),\n      });\n      // Deliver over both channels — the backend handles \"no email/phone on\n      // file\" gracefully; the invoice still lands in \"Out for payment\".\n      await Promise.allSettled([\n        fetch(`/api/invoices/${inv.id}/email`, {\n          method: \"POST\",\n          credentials: \"include\",\n        }),\n        fetch(`/api/invoices/${inv.id}/text`, {\n          method: \"POST\",\n          credentials: \"include\",\n        }),\n      ]);\n      globalThis.location.reload();\n    } finally {\n      setBusy(false);\n    }\n  }\n  function ctaAction(e: Event) {\n    if (inv.stage === \"claimed\") return doConfirmReceived(e);\n    if (inv.stage === \"overdue\") return doSendText(e);\n    if (inv.stage === \"scheduled\") return doSendNow(e);\n    if (inv.stage === \"drafting\") return doFinishDraft(e);\n    return doOpenInvoice(e);\n  }\n  async function doToggleMute(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    setBusy(true);\n    try {\n      await fetch(`/api/invoices/${inv.id}`, {\n        method: \"PUT\",\n        credentials: \"include\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({ remindersMuted: !inv.remindersMuted }),\n      });\n      globalThis.location.reload();\n    } finally {\n      setBusy(false);\n    }\n  }\n  async function doApplyDiscount(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    const cents = Math.round(Number(discountDollars) * 100);\n    if (!cents || cents <= 0) {\n      setAdjErr(tFor(lang, \"invoicesPage.adjust.errDiscountAmount\"));\n      return;\n    }\n    setBusy(true);\n    setAdjErr(null);\n    try {\n      const r = await fetch(`/api/invoices/${inv.id}/discount`, {\n        method: \"POST\",\n        credentials: \"include\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({ discountCents: cents }),\n      });\n      if (r.ok) globalThis.location.reload();\n      else setAdjErr(tFor(lang, \"invoicesPage.adjust.errDiscountApply\"));\n    } finally {\n      setBusy(false);\n    }\n  }\n  async function loadChangeOrders() {\n    try {\n      const r = await fetch(`/api/invoices/${inv.id}/change-orders`, {\n        credentials: \"include\",\n      });\n      if (r.ok) setChangeOrders(await r.json() as ChangeOrderRow[]);\n    } catch { /* best-effort — the panel still works without the list */ }\n  }\n  useEffect(() => {\n    if (adjustOpen && changeOrders === null) loadChangeOrders();\n  }, [adjustOpen]);\n  async function doCopyCoLink(id: string) {\n    try {\n      await navigator.clipboard.writeText(\n        `${globalThis.location.origin}/co/${id}`,\n      );\n      setCopiedCoId(id);\n      setTimeout(() => setCopiedCoId((cur) => (cur === id ? null : cur)), 1500);\n    } catch { /* clipboard denied — nothing to surface */ }\n  }\n  async function doCreateChangeOrder(e: Event) {\n    e.stopPropagation();\n    if (busy) return;\n    const cents = Math.round(Number(coDollars) * 100);\n    if (!coDesc.trim() || !cents) {\n      setAdjErr(tFor(lang, \"invoicesPage.adjust.errCoFields\"));\n      return;\n    }\n    setBusy(true);\n    setAdjErr(null);\n    try {\n      const r = await fetch(`/api/invoices/${inv.id}/change-orders`, {\n        method: \"POST\",\n        credentials: \"include\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({\n          description: coDesc.trim(),\n          deltaAmountCents: cents,\n        }),\n      });\n      if (!r.ok) {\n        setAdjErr(tFor(lang, \"invoicesPage.adjust.errCoCreate\"));\n        return;\n      }\n      const co = await r.json() as { id: string };\n      setCoLink(`${globalThis.location.origin}/co/${co.id}`);\n      // Refresh the list so the new pending order shows up immediately.\n      loadChangeOrders();\n    } finally {\n      setBusy(false);\n    }\n  }\n  return (\n    <article\n      class={`qcard ${flipped ? \"qcard--flipped\" : \"\"}`}\n      onClick={(e) => {\n        if (flipped) return;\n        const t = e.target as HTMLElement;\n        if (t.closest(\".qcard__cta, .qcard__back\")) return;\n        setFlipped(true);\n      }}\n      style={`--mood-from:${mood.from};--mood-to:${mood.to};--mood-shadow:${mood.shadow};--mood-status:${mood.statusFg}`}\n    >\n      <div class=\"qcard__mood\">\n        <div class=\"qcard__numeral\">{String(idx + 1).padStart(2, \"0\")}</div>\n        <div class=\"qcard__status\">\n          <span class=\"qcard__status-dot\" /> {moodLabel}\n        </div>\n      </div>\n      <div class=\"qcard__av\">{inv.initials}</div>\n      <div class=\"qcard__body\">\n        <div class=\"qcard__client-name\">{inv.client} · {inv.invoiceRef}</div>\n        <h3 class=\"qcard__title\">{fmtMoney(inv.amount)}</h3>\n        <p class=\"qcard__story\">{subline}</p>\n      </div>\n      <div class=\"qcard__foot\">\n        <button\n          type=\"button\"\n          class=\"qcard__cta\"\n          data-cy={`invoice-cta-${inv.stage}`}\n          onClick={ctaAction}\n          disabled={busy}\n        >\n          {busy ? \"…\" : cta}{\" \"}\n          <span style=\"display:inline-block;transition:transform 240ms\">→</span>\n        </button>\n        <div class=\"qcard__val-wrap\">\n          <div class=\"qcard__val-lbl\">\n            {tFor(\n              lang,\n              inv.stage === \"paid\"\n                ? \"invoicesPage.valLbl.cleared\"\n                : \"invoicesPage.valLbl.due\",\n            )}\n          </div>\n          <div class=\"qcard__val-num\" style=\"font-size:13px\">\n            {inv.stage === \"paid\"\n              ? fmtDate(inv.paidAt, now, lang)\n              : fmtDate(inv.dueDate, now, lang)}\n          </div>\n        </div>\n      </div>\n\n      <div class=\"qcard__back\" aria-hidden={!flipped}>\n        <div class=\"qcard__back-head\">\n          <button\n            type=\"button\"\n            class=\"qcard__back-close\"\n            onClick={(e) => {\n              e.stopPropagation();\n              setFlipped(false);\n            }}\n            aria-label={tFor(lang, \"common.close\")}\n          >\n            <I d={ICN.x} size={14} sw={2.5} />\n          </button>\n          <div class=\"qcard__back-eyebrow\">\n            {tFor(lang, \"invoicesPage.back.eyebrow\")}\n          </div>\n          <p class=\"qcard__back-big\">\n            {fmtMoney(inv.amount)}\n            <small>· {moodLabel}</small>\n          </p>\n        </div>\n        <div class=\"qcard__back-body\">\n          <p class=\"qcard__read\">\n            {inv.stage === \"overdue\" && (\n              <>\n                {tFor(\n                  lang,\n                  inv.daysOverdue === 1\n                    ? \"invoicesPage.read.overdue.one\"\n                    : \"invoicesPage.read.overdue.other\",\n                  { n: inv.daysOverdue },\n                )}\n              </>\n            )}\n            {inv.stage === \"out\" && (\n              <>\n                {tFor(lang, \"invoicesPage.read.out\", {\n                  issued: fmtDate(inv.issuedDate ?? inv.createdAt, now, lang),\n                  due: fmtDate(inv.dueDate, now, lang),\n                })}\n              </>\n            )}\n            {inv.stage === \"drafting\" && (\n              <>\n                {tFor(lang, \"invoicesPage.read.drafting\", {\n                  date: fmtDate(inv.issuedDate ?? inv.createdAt, now, lang),\n                })}\n              </>\n            )}\n            {inv.stage === \"paid\" && (\n              <>\n                {tFor(lang, \"invoicesPage.read.paid\", {\n                  date: fmtDate(inv.paidAt, now, lang),\n                })}\n              </>\n            )}\n          </p>\n          {inv.stage !== \"paid\" && (\n            <div\n              style=\"margin-top:6px\"\n              onClick={(e) => e.stopPropagation()}\n            >\n              <button\n                type=\"button\"\n                onClick={() => setAdjustOpen((o) => !o)}\n                style=\"appearance:none;background:none;border:none;color:var(--brand-teal);font:inherit;font-size:12.5px;font-weight:700;cursor:pointer;padding:2px 0\"\n              >\n                {adjustOpen\n                  ? tFor(lang, \"invoicesPage.adjust.hide\")\n                  : `${tFor(lang, \"invoicesPage.adjust.open\")} ▾`}\n              </button>\n              {adjustOpen && (\n                <div style=\"margin-top:8px;display:flex;flex-direction:column;gap:12px;background:rgba(0,0,0,0.03);border-radius:10px;padding:10px 12px\">\n                  <div>\n                    <div style=\"font-size:11px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--fg-muted);margin-bottom:4px\">\n                      {tFor(lang, \"invoicesPage.adjust.discountLabel\")}\n                    </div>\n                    <div style=\"display:flex;gap:6px\">\n                      <input\n                        type=\"number\"\n                        min=\"0\"\n                        step=\"1\"\n                        placeholder={tFor(\n                          lang,\n                          \"invoicesPage.adjust.discountPlaceholder\",\n                        )}\n                        value={discountDollars}\n                        onInput={(e) =>\n                          setDiscountDollars(\n                            (e.target as HTMLInputElement).value,\n                          )}\n                        style=\"flex:1;min-width:0;padding:7px 9px;border:1px solid var(--border);border-radius:7px;font:inherit;font-size:13px\"\n                      />\n                      <button\n                        type=\"button\"\n                        onClick={doApplyDiscount}\n                        disabled={busy}\n                        style=\"padding:7px 12px;border:0;border-radius:7px;background:var(--brand-green);color:#fff;font:inherit;font-weight:700;cursor:pointer\"\n                      >\n                        {tFor(lang, \"invoicesPage.adjust.apply\")}\n                      </button>\n                    </div>\n                  </div>\n                  <div>\n                    <div style=\"font-size:11px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--fg-muted);margin-bottom:4px\">\n                      {tFor(lang, \"invoicesPage.adjust.coLabel\")}\n                    </div>\n                    <input\n                      type=\"text\"\n                      placeholder={tFor(\n                        lang,\n                        \"invoicesPage.adjust.coDescPlaceholder\",\n                      )}\n                      value={coDesc}\n                      onInput={(e) =>\n                        setCoDesc((e.target as HTMLInputElement).value)}\n                      style=\"width:100%;box-sizing:border-box;padding:7px 9px;border:1px solid var(--border);border-radius:7px;font:inherit;font-size:13px;margin-bottom:6px\"\n                    />\n                    <div style=\"display:flex;gap:6px\">\n                      <input\n                        type=\"number\"\n                        step=\"1\"\n                        placeholder={tFor(\n                          lang,\n                          \"invoicesPage.adjust.coAmountPlaceholder\",\n                        )}\n                        value={coDollars}\n                        onInput={(e) =>\n                          setCoDollars((e.target as HTMLInputElement).value)}\n                        style=\"flex:1;min-width:0;padding:7px 9px;border:1px solid var(--border);border-radius:7px;font:inherit;font-size:13px\"\n                      />\n                      <button\n                        type=\"button\"\n                        onClick={doCreateChangeOrder}\n                        disabled={busy}\n                        style=\"padding:7px 12px;border:0;border-radius:7px;background:var(--brand-teal);color:#fff;font:inherit;font-weight:700;cursor:pointer\"\n                      >\n                        {tFor(lang, \"invoicesPage.adjust.createLink\")}\n                      </button>\n                    </div>\n                  </div>\n                  {coLink && (\n                    <div style=\"font-size:12px;color:var(--fg)\">\n                      {tFor(lang, \"invoicesPage.adjust.approvalLink\")}{\" \"}\n                      <a\n                        href={coLink}\n                        target=\"_blank\"\n                        rel=\"noopener noreferrer\"\n                        style=\"color:var(--brand-teal);word-break:break-all\"\n                      >\n                        {coLink}\n                      </a>\n                      <div style=\"color:var(--fg-muted);margin-top:2px\">\n                        {tFor(lang, \"invoicesPage.adjust.approvalHelp\")}\n                      </div>\n                    </div>\n                  )}\n                  {changeOrders && changeOrders.length > 0 && (\n                    <div>\n                      <div style=\"font-size:11px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--fg-muted);margin-bottom:4px\">\n                        {tFor(lang, \"invoicesPage.adjust.coListLabel\")}\n                      </div>\n                      <div style=\"display:flex;flex-direction:column;gap:6px\">\n                        {changeOrders.map((co) => (\n                          <div\n                            key={co.id}\n                            style=\"display:flex;align-items:center;gap:8px;font-size:12.5px\"\n                          >\n                            <span style=\"flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap\">\n                              {co.description}\n                            </span>\n                            <span style=\"font-weight:700;white-space:nowrap\">\n                              {co.deltaAmountCents >= 0 ? \"+\" : \"−\"}\n                              {fmtMoneyExact(Math.abs(co.deltaAmountCents))}\n                            </span>\n                            <span\n                              style={`font-size:10.5px;font-weight:800;letter-spacing:.04em;text-transform:uppercase;padding:2px 7px;border-radius:999px;border:1px solid currentColor;color:${\n                                CO_CHIP_COLOR[co.status]\n                              }`}\n                            >\n                              {tFor(\n                                lang,\n                                `invoicesPage.adjust.coStatus.${co.status}`,\n                              )}\n                            </span>\n                            {co.status === \"pending\" && (\n                              <button\n                                type=\"button\"\n                                onClick={() => doCopyCoLink(co.id)}\n                                style=\"appearance:none;background:none;border:1px solid var(--border);border-radius:7px;padding:3px 8px;font:inherit;font-size:11.5px;font-weight:700;color:var(--brand-teal);cursor:pointer;white-space:nowrap\"\n                              >\n                                {copiedCoId === co.id\n                                  ? tFor(lang, \"invoicesPage.adjust.coCopied\")\n                                  : tFor(lang, \"invoicesPage.adjust.coCopyLink\")}\n                              </button>\n                            )}\n                          </div>\n                        ))}\n                      </div>\n                    </div>\n                  )}\n                  {adjErr && (\n                    <div style=\"color:#a83b3b;font-size:12px\">{adjErr}</div>\n                  )}\n                </div>\n              )}\n            </div>\n          )}\n        </div>\n        <div class=\"qcard__back-foot\">\n          <button\n            type=\"button\"\n            onClick={ctaAction}\n            disabled={busy}\n            data-cy={`invoice-back-cta-${inv.stage}`}\n          >\n            {busy ? \"…\" : cta.replace(/ →$/, \"\")}\n          </button>\n          <button type=\"button\" onClick={doOpenInvoice}>\n            {tFor(lang, \"invoicesPage.back.open\")}\n          </button>\n          {inv.stage === \"claimed\" && (\n            <button\n              type=\"button\"\n              onClick={doRejectClaim}\n              disabled={busy}\n              data-cy=\"invoice-reject-claim\"\n              title={tFor(lang, \"invoicesPage.back.rejectTitle\")}\n            >\n              {tFor(lang, \"invoicesPage.back.reject\")}\n            </button>\n          )}\n          {(inv.stage === \"overdue\" || inv.stage === \"out\")\n            ? (\n              <button\n                type=\"button\"\n                data-cy=\"invoice-mute-toggle\"\n                onClick={doToggleMute}\n                disabled={busy}\n                title={inv.remindersMuted\n                  ? tFor(lang, \"invoicesPage.back.muteTitleOff\")\n                  : tFor(lang, \"invoicesPage.back.muteTitleOn\")}\n              >\n                {inv.remindersMuted\n                  ? tFor(lang, \"invoicesPage.back.muted\")\n                  : tFor(lang, \"invoicesPage.back.mute\")}\n              </button>\n            )\n            : (\n              <button type=\"button\" onClick={doSendText} disabled={busy}>\n                {tFor(lang, \"invoicesPage.back.textClient\")}\n              </button>\n            )}\n        </div>\n      </div>\n    </article>\n  );\n}\n\n/* ---------------- New invoice modal ---------------- */\n\nconst NEW_SENTINEL = \"__new__\";\n\n/** Create a standalone invoice — no quote/contract behind it. Pick (or add)\n *  a client, set an amount + due date, and it lands as a draft in the\n *  Drafting track. Mirrors the add-client modal pattern on /clients. */\nfunction NewInvoiceModal(\n  { customers, lang, onClose }: {\n    customers: Customer[];\n    lang: Lang;\n    onClose: () => void;\n  },\n) {\n  const [clientSel, setClientSel] = useState(\"\");\n  const [newName, setNewName] = useState(\"\");\n  const [newPhone, setNewPhone] = useState(\"\");\n  const [newEmail, setNewEmail] = useState(\"\");\n  const [amount, setAmount] = useState(\"\");\n  const [dueDate, setDueDate] = useState(() => {\n    const d = new Date();\n    d.setDate(d.getDate() + 30);\n    return d.toISOString().slice(0, 10);\n  });\n  const [busy, setBusy] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const labelStyle =\n    \"display:flex;flex-direction:column;gap:5px;font-size:13px;font-weight:700;color:var(--fg-muted,#6b7560)\";\n  const inputStyle =\n    \"padding:11px 13px;border:1px solid var(--border,#d8dcd5);border-radius:10px;font:inherit;font-size:15px;font-weight:400;color:var(--fg)\";\n\n  async function submit(e: Event) {\n    e.preventDefault();\n    if (busy) return;\n    const cents = Math.round(Number(amount) * 100);\n    if (!cents || cents <= 0) {\n      setError(tFor(lang, \"invoicesPage.new.errAmount\"));\n      return;\n    }\n    if (!dueDate) {\n      setError(tFor(lang, \"invoicesPage.new.errDueDate\"));\n      return;\n    }\n    if (clientSel === NEW_SENTINEL && !newName.trim()) {\n      setError(tFor(lang, \"invoicesPage.new.errClientName\"));\n      return;\n    }\n    setBusy(true);\n    setError(null);\n    try {\n      let customerId: string | undefined;\n      if (clientSel === NEW_SENTINEL) {\n        const created = await clientsClient.create({\n          name: newName.trim(),\n          ...(newPhone.trim() ? { phoneNumber: newPhone.trim() } : {}),\n          ...(newEmail.trim() ? { email: newEmail.trim() } : {}),\n        });\n        customerId = created.id;\n      } else if (clientSel) {\n        customerId = clientSel;\n      }\n      await dashboardClient.createInvoice({\n        ...(customerId ? { customerId } : {}),\n        amount: cents,\n        dueDate,\n        issuedDate: new Date().toISOString().slice(0, 10),\n        status: \"draft\",\n      });\n      // Reload so the new draft enriches into the Drafting track — matches how\n      // the in-card actions refresh the page.\n      globalThis.location.reload();\n    } catch (err) {\n      setError(\n        err instanceof Error\n          ? err.message\n          : tFor(lang, \"invoicesPage.new.errCreate\"),\n      );\n      setBusy(false);\n    }\n  }\n\n  return (\n    <div\n      onClick={() => !busy && onClose()}\n      style=\"position:fixed;inset:0;background:rgba(20,40,45,0.45);display:flex;align-items:center;justify-content:center;z-index:1000;padding:20px\"\n    >\n      <form\n        data-cy=\"new-invoice-modal\"\n        onClick={(e) => e.stopPropagation()}\n        onSubmit={submit}\n        style=\"background:#fff;border-radius:16px;padding:24px 26px;max-width:460px;width:100%;box-shadow:0 24px 64px rgba(20,72,82,0.22);display:flex;flex-direction:column;gap:14px\"\n      >\n        <h2 style=\"margin:0;font-size:20px;font-weight:800;color:var(--fg,#144852)\">\n          {tFor(lang, \"invoicesPage.new.title\")}\n        </h2>\n        <p style=\"margin:0;font-size:13px;color:var(--fg-muted,#6b7560)\">\n          {tFor(lang, \"invoicesPage.new.intro\")}\n        </p>\n\n        <label style={labelStyle}>\n          {tFor(lang, \"invoicesPage.new.client\")}\n          <select\n            data-cy=\"new-invoice-client\"\n            value={clientSel}\n            disabled={busy}\n            onInput={(e) => setClientSel((e.target as HTMLSelectElement).value)}\n            style={inputStyle}\n          >\n            <option value=\"\">\n              {tFor(lang, \"invoicesPage.new.clientNone\")}\n            </option>\n            {customers.map((c) => (\n              <option key={c.id} value={c.id}>{c.name}</option>\n            ))}\n            <option value={NEW_SENTINEL}>\n              {tFor(lang, \"invoicesPage.new.newClient\")}\n            </option>\n          </select>\n        </label>\n\n        {clientSel === NEW_SENTINEL && (\n          <div style=\"display:flex;flex-direction:column;gap:12px;background:rgba(0,0,0,0.03);border-radius:10px;padding:12px 14px\">\n            <label style={labelStyle}>\n              {tFor(lang, \"settings.name\")}\n              <input\n                type=\"text\"\n                autoFocus\n                required\n                value={newName}\n                disabled={busy}\n                onInput={(e) =>\n                  setNewName((e.target as HTMLInputElement).value)}\n                style={inputStyle}\n              />\n            </label>\n            <label style={labelStyle}>\n              {tFor(lang, \"settings.phone\")}\n              <input\n                type=\"tel\"\n                value={newPhone}\n                disabled={busy}\n                onInput={(e) =>\n                  setNewPhone((e.target as HTMLInputElement).value)}\n                style={inputStyle}\n              />\n            </label>\n            <label style={labelStyle}>\n              {tFor(lang, \"settings.email\")}\n              <input\n                type=\"email\"\n                value={newEmail}\n                disabled={busy}\n                onInput={(e) =>\n                  setNewEmail((e.target as HTMLInputElement).value)}\n                style={inputStyle}\n              />\n            </label>\n          </div>\n        )}\n\n        <label style={labelStyle}>\n          {tFor(lang, \"invoicesPage.new.amount\")}\n          <input\n            type=\"number\"\n            min=\"0\"\n            step=\"0.01\"\n            inputMode=\"decimal\"\n            data-cy=\"new-invoice-amount\"\n            placeholder=\"0.00\"\n            value={amount}\n            disabled={busy}\n            onInput={(e) => setAmount((e.target as HTMLInputElement).value)}\n            style={inputStyle}\n          />\n        </label>\n\n        <label style={labelStyle}>\n          {tFor(lang, \"invoicesPage.new.dueDate\")}\n          <input\n            type=\"date\"\n            data-cy=\"new-invoice-due\"\n            value={dueDate}\n            disabled={busy}\n            onInput={(e) => setDueDate((e.target as HTMLInputElement).value)}\n            style={inputStyle}\n          />\n        </label>\n\n        {error && (\n          <p\n            role=\"alert\"\n            style=\"margin:0;color:#a83b3b;font-size:13.5px;font-weight:600\"\n          >\n            {error}\n          </p>\n        )}\n\n        <div style=\"display:flex;gap:10px;justify-content:flex-end;margin-top:6px\">\n          <button\n            type=\"button\"\n            disabled={busy}\n            onClick={onClose}\n            style=\"padding:10px 16px;border:0;background:transparent;color:var(--fg-muted,#6b7560);font:inherit;font-weight:700;cursor:pointer;border-radius:10px\"\n          >\n            {tFor(lang, \"common.cancel\")}\n          </button>\n          <button\n            type=\"submit\"\n            data-cy=\"new-invoice-submit\"\n            disabled={busy}\n            style=\"padding:10px 20px;border:0;border-radius:10px;background:var(--brand-green,#519843);color:#fff;font:inherit;font-weight:800;cursor:pointer\"\n          >\n            {busy\n              ? tFor(lang, \"invoicesPage.new.creating\")\n              : tFor(lang, \"invoicesPage.new.create\")}\n          </button>\n        </div>\n      </form>\n    </div>\n  );\n}\n", "sourceName": "InvoicesPage.tsx", "checklist": "# Capture checklist — InvoicesPage\n\n**Theme:** light only (single theme — no dark mode exists).\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/invoices` (the whole page island — sidebar + topbar +\n  hero + KPI strip + the 6 tracks all in one shot).\n\n## Viewports (from quotes.css `@media`)\n- **1280px** — desktop. NOTE the `.qlay` grid is `1fr 320px` but the invoices\n  island renders no right rail, so capture the empty 320px gutter on the right.\n- **980px** — still 2-col KPIs (the KPI cut is at ≤1200, not 980); confirm hero\n  is still inline at this width (hero stacks at ≤1200).\n- **1200px** boundary — at ≤1200 `.qlay`→1col, `.qph`→column-stacked, `.qkpi`→2×2.\n- **720px** — `.qcards`→1col, `.qkpi`→1col (the ≤768 block), sidebar→drawer\n  (DashSidebar shared behavior).\n\n## Element(s) to crop\n- Full page (hero → KPIs → tracks) at 1280 and 720.\n- The hero alone (its 5 headline variants) — see invoices-hero checklist.\n- The KPI strip alone (4 cells, Overdue accent) — see invoices-kpis checklist.\n- One expanded track with cards + one collapsed track (QuoteTrack[shared]).\n\n## Transient states to drive\n1. **loading** — hard-reload `/invoices`; capture the skeleton frame\n   (ShimmerStyle + PageHeaderSkeleton + CardGridSkeleton rows=2) before data\n   lands. May need network throttling to catch it.\n2. **populated** — the seeded/real pipeline: overdue, awaiting-confirmation,\n   out, upcoming, drafting, paid tracks each with cards.\n3. **empty** — a fresh account (0 invoices) → empty hero + all 6 EmptyTrack\n   hints + $0 KPIs.\n4. **new-invoice modal open** — click the hero **New invoice** CTA\n   (`button[data-cy=invoice-new]`); capture the overlay open over the page\n   (scrim + centered form). See new-invoice-modal checklist for its variants.\n5. **error** — force a `/invoices` failure (the .qpage-error UNSTYLED branch);\n   document that it renders as plain unstyled body text (hazard).\n\n## Motion to film\n- Page-level motion is all from children: skeleton shimmer (shared Skeletons),\n  track collapse (QuoteTrack: chevron 240ms bounce + body grid-rows 320ms), card\n  flip + hover-lift + pulsing status dot (InvoiceCard). Film a card flip and a\n  track collapse here in context. Re-shoot one with\n  `prefers-reduced-motion: reduce` (relies on the global tokens clamp — verify\n  the flip/collapse/shimmer all still).\n\n## Notes\n- NO fabricated screenshots — capture only against the live app at the widths\n  above. The right-rail empty gutter at ≥1201px is real; document it, don't crop\n  it out.\n"}, {"name": "new-invoice-modal", "component": "NewInvoiceModal", "spec": "# NewInvoiceModal\n\nThe create/schedule-an-invoice modal overlay opened from the hero's **New\ninvoice** CTA. Pick (or add) a client, set an amount + due date, and it creates\na standalone **draft** invoice — no quote/contract behind it. Mirrors the\nadd-client modal pattern on /clients. Fully **inline-styled** (no class hooks\nbeyond the form/layout).\n\n## 1. Classification & behavior\n- **Bucket:** local component of the InvoicesPage island\n  (`islands/InvoicesPage.tsx` `const NEW_SENTINEL` line 1413, `function\n  NewInvoiceModal()` lines 1418–1632). Not its own island — it mounts inside the\n  page island when `newOpen` is true.\n- **Interaction tier:** **client-only island overlay.** A modal mounted by the\n  parent (`{newOpen && <NewInvoiceModal …/>}`). No Fresh form/PRG, no Partial —\n  submit is a hand-rolled async handler.\n- **Client state owned:** `clientSel` (select value; `\"\"` = none,\n  `\"__new__\"` = add-new), `newName`, `newPhone`, `newEmail` (inline new-client\n  fields, shown only when `clientSel === \"__new__\"`), `amount` (string, dollars),\n  `dueDate` (string, seeded to **today + 30 days**), `busy`, `error`.\n- **Server action + flash (FLAG `location.reload()`):**\n  | step | call | endpoint |\n  |---|---|---|\n  | (optional) create client | `clientsClient.create({ name, phoneNumber?, email? })` | `POST /customers` |\n  | create invoice | `dashboardClient.createInvoice({ customerId?, amount(CENTS), dueDate, issuedDate, status:\"draft\" })` | `POST /invoices` |\n  | refresh | `globalThis.location.reload()` | — full page reload |\n  - On success it **does NOT call `onClose()` or re-fetch** — it\n    `globalThis.location.reload()`s so the new draft enriches into the Drafting\n    track. **No flash/toast** — the reload IS the feedback.\n  - On validation failure it sets `error` (rendered as a `role=\"alert\"`\n    paragraph) and returns without submitting; the modal stays open.\n  - On network/thrown failure it sets `error` to `err.message` (or\n    `invoicesPage.new.errCreate`) AND `setBusy(false)` so the user can retry.\n  - **Anti-pattern (FLAG):** the success path `location.reload()` discards all\n    client state (scroll, open tracks survive via localStorage, but the flip\n    states and any in-flight forecast re-fire) and re-runs the page's three\n    on-mount fetches. **Fix:** lift a `refetchInvoices()` callback from the\n    island (the same fix recommended for InvoiceCard) — on create success call\n    `onClose()` + refetch `/invoices`, never reload. The island already holds\n    `setS`; this is mechanical.\n- **Data source + honest-empty:**\n  - The client `<select>` is populated from the `customers` prop (passed down\n    from the island's `GET /customers`). **Honest-empty:** with zero customers\n    the dropdown still renders the two synthetic options (\"No client (add\n    later)\" + \"+ New client\"), so an invoice can always be created — picking\n    \"+ New client\" reveals the inline name/phone/email block.\n- **Liveness:** none — single create, then full reload.\n- **Data-shape hazards (invoice money/units):**\n  - `amount` is typed in **dollars** by the user (`type=\"number\" step=\"0.01\"`)\n    then converted to **integer CENTS** at submit (`Math.round(Number(amount) *\n    100)`) before `createInvoice` — matching the cents-everywhere convention.\n    Validation rejects `≤ 0` / non-numeric.\n  - The created invoice is forced `status:\"draft\"` with `issuedDate = today`, so\n    it lands in the island's **Drafting** bucket (`isDraft` true). It is NOT a\n    `scheduled` invoice despite the task's \"create/schedule\" framing — there is\n    no `scheduledFor` field set here; scheduled invoices come from\n    multi-installment contracts, not this modal.\n  - The new-client create fires **before** the invoice create; if the client\n    create succeeds but the invoice create throws, you get an orphan customer\n    (no rollback) — a real edge to flag.\n\n## 2. Anatomy\n```\n<div onClick=close-on-scrim style=\"fixed inset:0; bg:rgba(20,40,45,.45); flex center; z-index:1000; padding:20px\">\n  <form data-cy=\"new-invoice-modal\" onClick=stopPropagation onSubmit=submit\n        style=\"bg:#fff; radius:16px; pad:24/26; max-width:460px; shadow:0 24px 64px rgba(20,72,82,.22); flex-col gap:14\">\n    <h2>{new.title → \"New invoice\"}</h2>\n    <p>{new.intro}</p>\n\n    <label>{new.client}\n      <select data-cy=\"new-invoice-client\">\n        <option value=\"\">{new.clientNone → \"No client (add later)\"}</option>\n        {customers.map → <option value={c.id}>{c.name}</option>}\n        <option value=\"__new__\">{new.newClient → \"+ New client\"}</option>\n      </select>\n    </label>\n\n    {clientSel===\"__new__\" && <div sunken-panel rgba(0,0,0,.03)>\n       <label>{settings.name}  <input autoFocus required/></label>\n       <label>{settings.phone} <input type=tel/></label>\n       <label>{settings.email} <input type=email/></label>\n    </div>}\n\n    <label>{new.amount → \"Amount (USD)\"} <input type=number step=.01 data-cy=\"new-invoice-amount\" placeholder=\"0.00\"/></label>\n    <label>{new.dueDate}                 <input type=date data-cy=\"new-invoice-due\"/></label>\n\n    {error && <p role=\"alert\" style=\"color:#a83b3b\">{error}</p>}\n\n    <div actions flex end>\n      <button type=button (Cancel)>{common.cancel}</button>\n      <button type=submit data-cy=\"new-invoice-submit\" green>{busy ? new.creating : new.create}</button>\n    </div>\n  </form>\n</div>\n```\n- **100% inline-styled** — no `.qph__*`/`.qcard__*` classes; the only stable\n  hooks are the `data-cy` attributes. Shared field styles via two local style\n  strings: `labelStyle` (flex-col label, `--fg-muted` fallback `#6b7560`) and\n  `inputStyle` (1px `--border` fallback `#d8dcd5`, radius 10, 15px font).\n- Hardcoded hex fallbacks bypass tokens: scrim `rgba(20,40,45,.45)`, card shadow\n  `rgba(20,72,82,.22)`, error `#a83b3b`, green `--brand-green` fb `#519843`, fg\n  `--fg` fb `#144852`. See `css/new-invoice-modal.css`.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `customers` | `Customer[]` (required) | — | json | no |\n| `lang` | `Lang` (required) | — | select | no |\n| `onClose` | `() => void` (required) | — | (callback→Events) | no |\n\n(No `lang` reactivity here — it's a plain prop from the parent, which itself\nreads `langSignal`.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | open modal, existing-client select, empty amount, due=+30d | `cases/default/default.json` |\n| new-client | `clientSel=\"__new__\"` → inline name/phone/email block visible | `cases/new-client/new-client.json` |\n| error | validation/network failure → `role=\"alert\"` line shown | `cases/error/error.json` |\n| busy | mid-submit → all fields disabled, button reads \"Creating…\" | `cases/busy/busy.json` |\n| es | Spanish labels/copy | `cases/es/es.json` |\n\n## 5. Events\n- `capture(page)`: `e.source===\"form[data-cy=new-invoice-modal]\" &&\n  e.type===\"submit\"` → `submit` (validate → optional `POST /customers` →\n  `POST /invoices` → `location.reload()`).\n- `capture(page)`: `e.source===\"div\"(scrim) && e.type===\"click\"` → `onClose()`\n  **unless `busy`**.\n- `capture(page)`: `e.source===\"form\" && e.type===\"click\"` → `stopPropagation`\n  (clicks inside the card don't close it).\n- `capture(page)`: `e.source===\"button\"(Cancel) && e.type===\"click\"` →\n  `onClose()` (disabled while busy).\n- `capture(page)`: `select[data-cy=new-invoice-client] onInput` → sets\n  `clientSel`; value `\"__new__\"` reveals the new-client block.\n- Field `onInput` handlers update `newName`/`newPhone`/`newEmail`/`amount`/\n  `dueDate`.\n\n## 6. Motion\n- **None.** The modal appears/disappears by conditional mount — no\n  enter/exit transition in source; it pops in instantly. No keyframes on any\n  element. **Jank finding:** the abrupt pop-in (no fade/scale) is a polish gap,\n  not jank — there's nothing to drop frames. **Fix on rebuild:** add a\n  `--dur-fast` opacity/scale enter (and respect the global reduced-motion\n  clamp). Reduced-motion: nothing to clamp today.\n\n## 7. Responsive (own — inline, no `@media`)\n- No `@media` queries (it's inline-styled). Fluid by construction: scrim has\n  `padding:20px`; the card is `max-width:460px; width:100%`, so it shrinks to\n  fill small viewports with a 20px gutter. The `<select>`/`<input>`s are\n  full-width block. Verify at **1280px** and **375px** that the card never\n  overflows the gutter.\n\n## 8. A11y\n- **Gaps (flag for rebuild):**\n  - It is a plain `<div>`/`<form>`, **not** `role=\"dialog\"` + `aria-modal`; no\n    `aria-labelledby` pointing at the `<h2>`.\n  - **No focus trap** and **Escape does not close** — only scrim click / Cancel\n    do. (`autoFocus` lands on the new-client name input only when that block is\n    shown.)\n  - The `<label>`s wrap their controls (implicit association — OK), but the\n    field \"label\" text has no `for`/`id` pairing if split.\n  - Error uses `role=\"alert\"` — **good** (announced on appearance).\n  - Buttons are real `<button>`s; the green submit is the single solid primary,\n    Cancel is a ghost — correct action-row hierarchy.\n\n## 9. Used on\n`/invoices` only — mounted by InvoicesPage when the hero's **New invoice** CTA\nsets `newOpen=true`. Sole caller. Its create flow is the only place the\nfront-end calls `POST /invoices` / `dashboardClient.createInvoice`.\n", "fixture": "{\n  \"category\": \"invoices\",\n  \"folder\": \"new-invoice-modal\",\n  \"controls\": {\n    \"customers\": {\n      \"type\": \"json\",\n      \"value\": [\n        { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n        { \"id\": \"cust-hd\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\" },\n        { \"id\": \"cust-ml\", \"userId\": \"u1\", \"name\": \"Marcus Lin\" }\n      ]\n    },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "busy.json", "content": "{\n  \"_name\": \"Busy — mid-submit, all fields disabled, submit reads 'Creating…', scrim won't close\",\n  \"lang\": \"en\",\n  \"customers\": [\n    { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n    { \"id\": \"cust-hd\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\" }\n  ],\n  \"_signals\": {\n    \"clientSel\": \"cust-mg\",\n    \"amount\": \"1450.00\",\n    \"busy\": true\n  }\n}\n"}, {"name": "default.json", "content": "{\n  \"_name\": \"Default — open modal, client select populated, empty amount, due seeded +30d\",\n  \"lang\": \"en\",\n  \"customers\": [\n    { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n    { \"id\": \"cust-hd\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\" },\n    { \"id\": \"cust-ml\", \"userId\": \"u1\", \"name\": \"Marcus Lin\" }\n  ]\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — validation failed, role=alert line ('Enter an amount greater than zero.')\",\n  \"lang\": \"en\",\n  \"customers\": [\n    { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n    { \"id\": \"cust-hd\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\" }\n  ],\n  \"_signals\": {\n    \"amount\": \"0\",\n    \"error\": \"Enter an amount greater than zero.\"\n  },\n  \"_note\": \"Reachable two ways: client-side validation (amount<=0 / no dueDate / new-client missing name → invoicesPage.new.err* strings) or a thrown createInvoice (→ err.message or invoicesPage.new.errCreate). Either sets `error` and leaves the modal open; busy resets to false on the network path so the user can retry.\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — localized title/intro/labels/buttons\",\n  \"lang\": \"es\",\n  \"customers\": [\n    { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n    { \"id\": \"cust-ml\", \"userId\": \"u1\", \"name\": \"Marcus Lin\" }\n  ],\n  \"_signals\": { \"amount\": \"980.00\" }\n}\n"}, {"name": "new-client.json", "content": "{\n  \"_name\": \"New client — clientSel='__new__' reveals inline name/phone/email block (name autoFocus)\",\n  \"lang\": \"en\",\n  \"customers\": [\n    { \"id\": \"cust-mg\", \"userId\": \"u1\", \"name\": \"Maple Grove Apartments\" },\n    { \"id\": \"cust-hd\", \"userId\": \"u1\", \"name\": \"Hilltop Diner\" }\n  ],\n  \"_signals\": {\n    \"clientSel\": \"__new__\",\n    \"newName\": \"Rosa Delgado\",\n    \"newPhone\": \"(415) 555-0177\",\n    \"newEmail\": \"rosa.delgado@gmail.com\",\n    \"amount\": \"1450.00\"\n  }\n}\n"}], "css": "/* ============================================================================\n   NewInvoiceModal — EXTRACTED, not ported. The live component\n   (islands/InvoicesPage.tsx, NewInvoiceModal lines 1418–1632) carries NO\n   classes: every style is an inline `style=\"…\"` string. There is no rule for\n   this modal in static/quotes.css or static/dashboard.css. The rules below are\n   transcribed verbatim from those inline strings so the rebuild can lift them\n   into real classes (recommended) instead of re-inlining.\n\n   Hardcoded hex fallbacks in the source are preserved here next to their token\n   so the token mapping is obvious:\n     scrim  rgba(20,40,45,.45)   (no token; ~coffee/teal 45% — use --bg-overlay)\n     card shadow rgba(20,72,82,.22) (public-surface teal; map to --shadow-xl)\n     --fg-muted fb #6b7560 · --border fb #d8dcd5 · --fg fb #144852\n     --brand-green fb #519843 · error #a83b3b (public danger text)\n   ============================================================================ */\n\n/* Full-viewport scrim + centering. z-index 1000 sits above the app shell. */\n.new-invoice-scrim {\n  position: fixed;\n  inset: 0;\n  background: rgba(20, 40, 45, 0.45); /* → var(--bg-overlay) on rebuild */\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  z-index: 1000;\n  padding: 20px; /* keeps the card off the edges on small screens */\n}\n\n/* The modal card. max-width 460 + width 100% = fluid, no @media needed. */\n.new-invoice-card {\n  background: #fff;\n  border-radius: 16px;\n  padding: 24px 26px;\n  max-width: 460px;\n  width: 100%;\n  box-shadow: 0 24px 64px rgba(20, 72, 82, 0.22); /* → var(--shadow-xl) */\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n}\n\n.new-invoice-card h2 {\n  margin: 0;\n  font-size: 20px;\n  font-weight: 800;\n  color: var(--fg, #144852);\n}\n\n.new-invoice-card p.intro {\n  margin: 0;\n  font-size: 13px;\n  color: var(--fg-muted, #6b7560);\n}\n\n/* labelStyle — every <label> in the form */\n.new-invoice-label {\n  display: flex;\n  flex-direction: column;\n  gap: 5px;\n  font-size: 13px;\n  font-weight: 700;\n  color: var(--fg-muted, #6b7560);\n}\n\n/* inputStyle — shared by <select> and every <input> */\n.new-invoice-input {\n  padding: 11px 13px;\n  border: 1px solid var(--border, #d8dcd5);\n  border-radius: 10px;\n  font: inherit;\n  font-size: 15px;\n  font-weight: 400;\n  color: var(--fg);\n}\n\n/* The new-client sub-panel (revealed when \"+ New client\" is picked) */\n.new-invoice-newclient {\n  display: flex;\n  flex-direction: column;\n  gap: 12px;\n  background: rgba(0, 0, 0, 0.03);\n  border-radius: 10px;\n  padding: 12px 14px;\n}\n\n/* role=\"alert\" validation/network error line */\n.new-invoice-error {\n  margin: 0;\n  color: #a83b3b; /* public-surface danger text; → var(--danger) on rebuild */\n  font-size: 13.5px;\n  font-weight: 600;\n}\n\n/* Action row — right-aligned: ghost Cancel + solid green submit */\n.new-invoice-actions {\n  display: flex;\n  gap: 10px;\n  justify-content: flex-end;\n  margin-top: 6px;\n}\n.new-invoice-actions .cancel {\n  padding: 10px 16px;\n  border: 0;\n  background: transparent;\n  color: var(--fg-muted, #6b7560);\n  font: inherit;\n  font-weight: 700;\n  cursor: pointer;\n  border-radius: 10px;\n}\n.new-invoice-actions .submit {\n  padding: 10px 20px;\n  border: 0;\n  border-radius: 10px;\n  background: var(--brand-green, #519843);\n  color: #fff;\n  font: inherit;\n  font-weight: 800;\n  cursor: pointer;\n}\n.new-invoice-actions button:disabled {\n  /* source only sets disabled attr (no style); add a visible busy cue on rebuild */\n  cursor: default;\n  opacity: 0.7;\n}\n", "cssName": "new-invoice-modal.css", "source": "/* SOURCE POINTER — the full verbatim source for NewInvoiceModal lives in:\n *\n *   ../../invoices-page/js/InvoicesPage.tsx\n *     · const NEW_SENTINEL          line 1413\n *     · function NewInvoiceModal()  lines 1418–1632\n *\n * A fully inline-styled modal (no class hooks beyond the form layout). On\n * submit it optionally creates a Customer (clientsClient.create) then a draft\n * Invoice (dashboardClient.createInvoice) and `globalThis.location.reload()`s\n * to surface the new draft in the Drafting track — see new-invoice-modal.md\n * §1 for the PRG/island-refresh flag + fix. Read the canonical file above for\n * the exact JSX/validation; do not re-author from memory. */\nexport {};\n", "sourceName": "NewInvoiceModal.tsx", "checklist": "# Capture checklist — NewInvoiceModal\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/invoices` → click the hero **New invoice** CTA\n  (`button[data-cy=invoice-new]`) to open the overlay. The modal is\n  `[data-cy=new-invoice-modal]` (the `<form>`), centered on a fixed scrim.\n\n## Viewports (inline-styled — no `@media`)\n- **1280px** — modal card `max-width:460px` centered; scrim covers the whole\n  viewport with a 20px padding gutter.\n- **375px** (mobile) — card is `width:100%` minus the 20px scrim gutter; verify\n  it never overflows. (No breakpoint exists; it's fluid by construction.)\n\n## Element(s) to crop\n- The full overlay: scrim (`rgba(20,40,45,.45)`) + centered white card\n  (radius 16, shadow `0 24px 64px rgba(20,72,82,.22)`).\n- The card alone: h2 \"New invoice\" + intro + Client select + Amount + Due date +\n  the right-aligned Cancel (ghost) / Create invoice (solid green) action row.\n\n## Transient states to drive\n1. **default** — open over the page; Client select shows real customers +\n   \"No client (add later)\" + \"+ New client\"; Due date pre-filled to today+30d;\n   Amount empty (placeholder \"0.00\").\n2. **new-client** — pick \"+ New client\" → the sunken `rgba(0,0,0,.03)` panel\n   reveals Name (autoFocus) / Phone / Email inputs.\n3. **error** — submit with amount 0 (or empty due / new-client missing name) →\n   the `role=\"alert\"` line in `#a83b3b` (\"Enter an amount greater than zero.\").\n4. **busy** — submit a valid form → all fields disabled, submit button reads\n   \"Creating…\"; capture the frame before the success `location.reload()` (FLAG:\n   success full-reloads the page, no toast/close).\n5. **es** — set UI language to Spanish → localized title/intro/labels/buttons.\n\n## Motion to film\n- **None.** The modal pops in instantly on mount (no enter/exit transition in\n  source) and vanishes on close/reload. Nothing to film; note the abrupt pop-in\n  as a polish gap, not jank. Reduced-motion is moot.\n\n## Notes\n- Closing: click the scrim or Cancel (both blocked while `busy`). Escape does\n  NOT close (a11y gap to flag). The card click is `stopPropagation`'d so inner\n  clicks don't close it.\n- 100% inline-styled (only `data-cy` hooks) — see css/new-invoice-modal.css for\n  the extracted rule set. NO fabricated screenshots.\n"}]}, {"name": "landing", "pageMd": "# Page — landing (`/`)\n\nMarketing home. A long, fully STATIC server-rendered marketing page whose only\nhydrated component is the **PhoneChat** island (demo section). All other\ninteractivity — language toggle, hero word rotor, marquee, doc-tab cycling,\nanimated count-up, smooth-scroll, the contact/OTP form — is driven by a single\nvanilla-JS file loaded with a `<script defer>`, NOT by island hydration.\n\nSource: `routes/index.tsx` (1131 lines). Behavior: `static/landing-scripts.js`\n(697 lines, plain IIFE). Styling: `static/landing.css` (3124 lines). Copies are\nin `js/index.tsx`, `js/landing-scripts.js`, `css/landing.css`.\n\n## Classification\n- **Bucket:** `page-composition` — almost entirely `static` SSR markup with\n  `data-i18n` / `data-en` / `data-es` / `data-html` / `data-doc` / `data-lang`\n  hooks the vanilla JS reads. One mounted island (PhoneChat).\n- **Why vanilla JS, not an island:** the file's own header explains Fresh island\n  hydration was broken for \"post-effect\" islands (module-URL CORS), so the\n  behavior was ported to `static/landing-scripts.js` and loaded via\n  `<script src=\"/landing-scripts.js\" defer>`. Memory confirms this. The would-be\n  island `islands/LandingScripts.tsx` is therefore **dead** (see Dead code).\n\n## SSR handler / data fetched\n- `define.page(async function Landing(ctx))`. Calls `await loadUser(ctx.req)`;\n  **if a user is logged in → 302 to `/dashboard`** (landing is logged-out only).\n- No other server fetch. All copy is literal English in the JSX (the EN default),\n  swapped to ES client-side by `landing-scripts.js`'s inline `I18N` dict.\n- The route uses `tFor(\"en\", …)` only for the `<title>` + meta description.\n\n## `<Head>`\n- `<title>` = `tFor(\"en\",\"landing.head.title\")` = \"Paperwork Monster — You do\n  the work. We handle the paperwork.\"\n- `<meta name=\"description\">` = `landing.head.metaDescription`.\n- `<link rel=\"stylesheet\" href=\"/landing.css\">`.\n- `<script src=\"/landing-scripts.js\" defer>` — the behavior layer.\n\n## App-shell composition / islands mounted (in DOM order)\n1. **PhoneChat** island — the only hydrated component, in the `.demo` section\n   (`routes/index.tsx` line 751), props: `script=DEMO_SCRIPT_EN`,\n   `scriptEs=DEMO_SCRIPT_ES`, `quote=DEMO_QUOTE_EN`, `quoteEs=DEMO_QUOTE_ES`,\n   `messageCopy=\"Message\"`, `messageCopyEs=\"Mensaje\"`, `autoPlayOnView`.\n   See `components/phone-chat/`.\n- No DashSidebar/DashTopbar/MobileViewport here — this is the marketing surface,\n  not the app shell. No shared app components apply.\n\n## Section layout (top → bottom; all `static` unless noted)\n| # | Section | Key hooks / notes |\n|---|---|---|\n| 1 | `.nav-wrap > .container.nav` | `.brand` (img+span+em), `.lang-toggle` (two `data-lang` buttons, `role=tablist`), `.nav-links` anchors, `.btn-outline` → `/login`, `.btn-primary.cta-scroll` → `#contact` |\n| 2 | `.hero` | `.kicker` pill; `<h1>` with the **rotor** (`#rotor-track > .word[data-en][data-es]` × 4: quotes/contracts/invoices/paperwork); `.hero-ctas`; `.hero-trust` avatars; `.hero-visual > .hero-stage` (decorative `.hs-blob`, `.hs-badge`, `.hs-doc`, `.hs-phone` static mockup, `.spark` sparkles) |\n| 3 | `.marquee > #marquee-track` | one `<span data-en=\"…|…\" data-es=\"…|…\">`; JS builds two duplicated segments for the loop |\n| 4 | `.problem` | section-head + 3 `.problem-card` (01/02/03) |\n| 5 | `.docs` | `.doc-tabs` (3 `.doc-tab[data-doc]` quote/contract/invoice); `.doc-mockup` (`#doc-title/#doc-num/#doc-date/#doc-lines/#doc-totals`); `.doc-info` (`#doc-info-title/body/list`); `.doc-counter` with count-up `#doc-counter-num` |\n| 6 | `.features#features` | 4 `.feature` cards w/ icon variants **pink / green / teal / coffee** |\n| 7 | `.how#how-it-works` | 3 `.how-step` (num-circle 1/2/3) |\n| 8 | `.demo` | `.demo-info` (eyebrow + testimonial) **+ `<PhoneChat>` island** |\n| 9 | `.pricing#pricing` | 3 `.tier` (Starter $15 / **Pro $99 `.featured` + `.tier-badge`** / Crew $199), `.cta-scroll` CTAs |\n| 10 | `.contact#contact` | `.contact-card` = `.contact-info` (`.pm-steps` 1-2-3 + checks) **+ inline `<form id=\"contact-form\">`** with the SMS-preview phone `.cf-phone`, saved-phone chip `#cf-saved`, compose `#f-phone`, `.cf-cta` submit, `.cf-trust` |\n| 11 | `.footer` | brand + links + copy |\n\n## Events (from `static/landing-scripts.js` — the page's behavior layer)\n- **Language resolve + persist (on load):** active lang = `?lang=` (URL) >\n  `localStorage[\"pm:lang\"]` > `\"en\"`; mirrored back to `localStorage[\"pm:lang\"]`\n  AND a `pm_lang` cookie (so the SSR `/login` & `/verify` routes render in the\n  chosen language). `applyLang(lang)` swaps every `[data-i18n]` (`textContent`,\n  or `innerHTML` when `data-html=\"1\"`), the rotor words (`data-en|es`), the\n  marquee (`data-en|es` split on `|`, rebuilt as 2 segments), re-renders the doc\n  tab + (dead) chat, re-fits the rotor, toggles `.lang-toggle .on`.\n  - `ev.expect(e => e.source===\".lang-toggle button\" && e.type===\"click\")` →\n    `applyLang(btn.dataset.lang)` + `writeLangToUrl` (history.replaceState).\n- **Hero rotor:** `setInterval(2200ms)` cycles 4 words — current gets `.out`,\n  next gets `.in` (after a double rAF); `.out` removed after 600ms. `fitRotor()`\n  measures the widest word with a hidden probe span and sets `#rotor-track`\n  width (re-runs on `document.fonts.ready`, next rAF, +1s, and `resize` — to fix\n  Safari's fallback-font clip).\n- **Smooth scroll:** every `a[href^=\"#\"]` → `scrollIntoView({behavior:\"smooth\"})`;\n  scrolling to `#contact` focuses `#f-phone` after 600ms.\n- **Doc tabs:** click a `.doc-tab` → set `.on`, `renderDoc(dataset.doc)` rebuilds\n  `#doc-lines`/`#doc-totals`/`#doc-info-*` from the bilingual `DOC_CONTENT` dict.\n- **Count-up:** IntersectionObserver (threshold 0.3) on `#doc-counter-num` →\n  cubic-ease count 0→48,217 over 1800ms, locale-formatted (es-ES / en-US). Fires once.\n- **Contact form (`#contact-form`):**\n  - `#f-phone` input is live-masked via `formatPhone` (`(555) 123-4567`).\n  - **Saved-phone chip:** on load, if `localStorage[\"pm:last-phone\"]` exists,\n    un-hides `#cf-saved` showing the formatted number; clicking `#cf-saved-btn`\n    fills + `requestSubmit()`s the form; `#cf-saved-dismiss` removes the stored\n    number + re-hides the chip.\n  - **Submit:** `preventDefault` → `toE164` → guard `<10` digits (refocus) →\n    set CTA to \"Sending…\"/\"Enviando…\" → **POST `/api/auth/send-otp`**\n    `{phoneNumber, language}` → on ok, save `localStorage[\"pm:last-phone\"]` and\n    `location.href = \"/verify?phone=<e164>&lang=<curLang>\"`; on error, restore\n    CTA + write a red error into `#cf-meta`.\n  - `ev.expect(e => e.source===\"form#contact-form\" && e.type===\"submit\")`.\n- **`init`:** `applyLang(curLang); renderDoc(\"quote\"); renderChat()`.\n\n## Motion (extracted from landing.css; durations are real)\n- **Rotor word in/out:** `transform 520ms cubic-bezier(.34,1.4,.64,1)`, `opacity\n  380ms`, `filter blur 380ms` — `.in` rises from `translateY(60%)+blur(6px)`,\n  `.out` exits to `translateY(-60%)+blur(6px)`. The `.rotor` chip itself is a\n  pink `rotate(-1.5deg)` tab. **Jank:** JS sets `#rotor-track` width per active\n  word; mis-timed font load clips the word until `fitRotor` re-runs (handled).\n- **Marquee:** `@keyframes marquee` `translateX(0→-50%)` over **38s** linear\n  infinite (two duplicated segments make the loop seamless).\n- **Decorative hero floats:** `phoneFloat 6s`, `blobDrift`, `docFloat`,\n  `badgeFloat`, `twinkle` (sparkles) — ambient infinite loops on `.hs-*`/`.spark`.\n- **Count-up:** rAF cubic-ease over 1800ms (JS, not CSS).\n- **Contact phone:** `cfPulse` (live dot), `cfDot`, `cfSlideIn` keyframes;\n  `.cf-saved__btn` hover `translateY(-1px)`.\n- **Button spinner:** `spin 0.7s linear` (`.spinner`, loading states).\n- **Reduced motion:** landing.css has **3** `@media (prefers-reduced-motion:\n  reduce)` blocks — a global `*{animation/transition-duration:0.01ms!important}`\n  clamp, `html{scroll-behavior:auto}`, and the `.pm-steps` active-dot pulse off.\n  **The rebuild must preserve all three.**\n\n## Responsive (landing's own breakpoints — read css/landing.css)\n- `@media (max-width:980px)` — main layout collapse (hero/demo/grids stack).\n- `@media (max-width:560px)` — `.hero-grid` → single column AND **`.hero-visual`\n  is `display:none`** (the ~460px phone mockup caused horizontal scroll on\n  phones). The marketing container is `max-width:1200px`.\n- (No 720px breakpoint here — that's the product surface; landing uses 980/560.)\n\n## A11y\n- `.lang-toggle` is `role=\"tablist\"` with two buttons (no `aria-selected` /\n  `role=tab` on them — gap). `.pm-steps` has `aria-label`. Decorative SVGs are\n  unlabeled (acceptable). Smooth-scroll respects reduced motion. The hero visual\n  is `aria-hidden=\"true\"`. `data-html` injects trusted inline `<em>` only.\n\n## Build / composition order\n1. Tokens + `static/landing.css` (or the `@theme` rebuild) loaded first.\n2. SSR the static section tree (nav→hero→marquee→problem→docs→features→how→\n   demo→pricing→contact→footer) with all `data-*` hooks intact.\n3. Mount the **PhoneChat** island in `.demo` (props = the hardcoded DEMO_* arrays).\n4. Load `static/landing-scripts.js` `defer` — it wires language, rotor, marquee,\n   doc tabs, count-up, smooth-scroll, contact form, saved-phone chip on\n   DOMContentLoaded-equivalent (defer).\n5. (Rebuild decision) The i18n landing copy currently lives **twice**: literal EN\n   in the JSX + the full `I18N`/`DOC_CONTENT`/`CHAT_SCRIPT` dicts inside\n   `landing-scripts.js`. A clean rebuild should source both from `lang/*.json`.\n\n## Dead code (verified zero mounts — NO folders created for these)\n- `islands/LandingScripts.tsx` — the page loads the vanilla `landing-scripts.js`\n  instead; the island is never imported by `routes/index.tsx` (only a stale\n  comment mentions `<LandingScripts>`). DEAD.\n- `islands/HeroRotor.tsx` — the rotor is the SSR `#rotor-track` + the vanilla JS;\n  zero imports. DEAD.\n- `islands/DemoPhoneChat.tsx` — superseded by `PhoneChat` (mounted) + the demo's\n  reveal logic; zero imports. DEAD.\n- `islands/ContactForm.tsx` — the landing contact form is inline `<form\n  id=\"contact-form\">` markup driven by `landing-scripts.js`, not this island;\n  zero imports. DEAD.\n- **Dead WITHIN the live JS:** `landing-scripts.js`'s `renderChat()`,\n  `startReveal()`, `resetReveal()`, `quoteCardHTML()`, `CHAT_SCRIPT` target\n  `#chat-body`/`.chat-step`/`#chat-fill` — the route renders no such ids (the\n  PhoneChat island owns the demo with `class=\"chat-body\"`), so they early-return.\n  Legacy from when the demo was server-rendered static markup.\n\n## Anti-patterns flagged\n- **i18n copy duplicated** across JSX literals and the JS `I18N` dict (drift\n  risk; the app already has `lang/*.json`). Fix: render `data-i18n` text from\n  `tFor` SSR and drop the JS dict, or generate the JS dict from the JSON.\n- **Dead chat code in a live file** (`renderChat`/`startReveal`) — confusing\n  no-ops shipped to every visitor. Fix: delete now that PhoneChat owns the demo.\n- **Behavior layer is a `<script>` tag, not an island** — intentional workaround\n  for a (since-possibly-fixed) hydration bug. Worth re-evaluating: the rotor /\n  doc-tabs / count-up are classic island candidates. Not a correctness bug, but\n  it bypasses Fresh's hydration entirely. No `location.reload()` anti-pattern on\n  this page (the contact form does a forward nav to `/verify`, which is correct).\n", "components": [{"name": "phone-chat", "component": "PhoneChat", "spec": "# PhoneChat\n\nThe demo SMS-mockup phone that animates a contractor → assistant conversation,\nending in a quote card. Page-local to `/` (landing) — mounted in the `.demo`\nsection. Also drives a `/stories/demo-phone-chat` playground (story mode).\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PhoneChat.tsx`).\n- **Interaction tier:** `island` (client-only state + IntersectionObserver +\n  timer-driven reveal). No server calls, no mutations, no network of any kind.\n- **Client state owned (all `useState`):**\n  - `lang: \"en\"|\"es\"` — seeded from `langSignal.value` **only when** `scriptEs`\n    is supplied (`usesLangSignal`); otherwise from the `lang` prop. Subscribes\n    to `langSignal` so the landing language toggle re-renders + replays it.\n  - `shown: number` — how many script steps are rendered (slice end). Defaults\n    to `activeScript.length` (final state) so static review lands on the end.\n  - `hidden: Set<number>` — indices whose step is `display:none` (used to hide a\n    `typing` step once the real reply after it appears).\n  - refs: `cancelRef` (timer canceller), `chatBodyRef` (auto-scroll target),\n    `phoneRef` (IO target), `playedRef` (one-shot guard).\n- **Data source per region:** 100% from props. The conversation is the `script`\n  / `scriptEs` `Bubble[]` arrays; the quote card from `quote` / `quoteEs`\n  `QuoteCopy`; the input-bar placeholder from `messageCopy` / `messageCopyEs`.\n  The quote-card line **amounts** are NOT from props — `QuoteCard` pulls them\n  from i18n (`tFor(lang,\"phoneChat.quote.amount1..3/Total\")`) while the line\n  **labels** come from the `QuoteCopy` prop. Status-bar time, \"online\", PDF\n  badge, and brand name also come from `tFor(lang, …)`.\n- **Honest-empty:** N/A — content is always provided by the route's hardcoded\n  demo scripts; there is no fetch and no empty state in production. (`reset()`\n  produces an intentionally empty chat body for story mode only.)\n- **Liveness:** none — purely local timers. No request/response, no polling, no\n  websocket. The \"Online • SMS\" header is decorative.\n- **Reveal timing (mirrors legacy `LandingScripts.startReveal`):** each step's\n  delay accrues `+350ms` if it's a `typing` step, else `+200ms` for the first\n  step or `+700ms` for the rest; a `typing` step then adds a further `+1100ms`\n  linger before the next step. On reaching a non-typing step preceded by a\n  typing step, that prior typing index is added to `hidden`.\n- **Anti-pattern check:** NONE of the page-island/`location.reload()` family —\n  PhoneChat takes serializable props, owns only ephemeral animation state, never\n  reloads or refetches. Clean island. (The one smell is dual-source quote data:\n  labels from props, amounts from i18n — easy to desync; see hazards.)\n- **Cross-file note (DEAD on this page):** `static/landing-scripts.js` still\n  contains a `renderChat()/startReveal()/resetReveal()/chatIo()` port that\n  targets `#chat-body`, `.chat-step`, `#chat-fill`. The landing route renders\n  **no** `#chat-body` (PhoneChat uses `class=\"chat-body\"`, no id), so those JS\n  functions early-return — the island fully owns the demo. `chatIo()` does find\n  the island's `.phone` and calls `startReveal()`, which then no-ops on the\n  missing `#chat-body`. Treat that JS block as dead relative to this page.\n- **Data-shape hazards:**\n  - `usesLangSignal = !!scriptEs`. If `scriptEs` is omitted, the `langSignal`\n    subscription is skipped and the phone is frozen in `langProp`.\n  - `QuoteCard` amounts come from i18n, not the `QuoteCopy` prop — changing the\n    prop labels without updating the i18n amounts silently desyncs the card.\n  - `shown` defaults to `activeScript.length`; if a case wants the animation\n    visible it must drive `autoPlayOnView` or story controls, not just mount.\n\n## 2. Anatomy\n```\n<>\n  {controls && <div [inline-styled toolbar]>            ← story mode only\n     <button ▶ play> <button ⟲ reset> <button ⏭ end></div>}\n  <div class=\"phone-wrap\">\n    <div class=\"phone-bg\" />                             ← radial glow\n    <div class=\"phone\" ref=phoneRef>                     ← IO target (threshold .4)\n      <div class=\"phone-screen\">\n        <div class=\"phone-status\"><span>{time}</span><div class=\"icons\">×4</div></div>\n        <div class=\"chat-header\">\n          <div class=\"av-pm\"><img /logo-monster.png></div>\n          <div class=\"meta\"><strong>{brand}</strong><span>{online}</span></div>\n        </div>\n        <div class=\"chat-body\" ref=chatBodyRef>          ← auto-scroll to bottom\n          activeScript.slice(0,shown).map → <div class=\"chat-step {left|right} in\"\n              style=\"animation:bubble-in 360ms …; [display:none if hidden]\">\n            typing → <div class=\"typing\"><span>×3</div>\n            bubble → <div class=\"bubble {me|them}\" style={m.style}>{m.text}</div>\n            meta   → <div class=\"bubble-meta\">{m.text}</div>\n            quote  → <QuoteCard q=activeQuote lang>\n        </div>\n        <div class=\"chat-input\"><div class=\"field\">{inputCopy}</div>\n             <div class=\"send\"><svg send-arrow/></div></div>\n      </div>\n    </div>\n  </div>\n</>\n```\n- **QuoteCard sub-component:** `.quote-card > .qc-head(span + .pdf) + .row×3 +\n  .total`. Labels from `q`, amounts + PDF badge from `tFor(lang,…)`.\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `script` | `Bubble[]` | — (required) | (json) | no |\n| `scriptEs` | `Bubble[]` | `undefined` | (json) | enables langSignal sub |\n| `quote` | `QuoteCopy` | — (required) | (json) | no |\n| `quoteEs` | `QuoteCopy` | `undefined` | (json) | no |\n| `messageCopy` | string | `tFor(\"en\",\"phoneChat.input.message\")` | text | no |\n| `messageCopyEs` | string | `tFor(\"es\",\"phoneChat.input.message\")` | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | (used only when no scriptEs) |\n| `controls` | boolean | `false` | boolean | no |\n| `autoPlayOnView` | boolean | `false` | boolean | no |\n\n`Bubble = { side:\"left\"|\"right\"; kind:\"bubble\"|\"meta\"|\"quote\"|\"typing\";\ncls?:\"me\"|\"them\"; text?:string; style?:string }`.\n`QuoteCopy = { hd; l1; l2; l3; total }` (all string).\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| end-state | default mount: all steps shown, no controls (landing's static end frame before IO) | `cases/end-state/end-state.json` |\n| playing | mid-animation, a `typing` step visible (`_signals.shown` partway) | `cases/playing/playing.json` |\n| reset-empty | story `reset()` — empty chat body (`shown:0`) | `cases/reset-empty/reset-empty.json` |\n| es | Spanish script + quote active (`langSignal=\"es\"`) | `cases/es/es.json` |\n| story-controls | `controls=true` → Play/Reset/End toolbar above phone | `cases/story-controls/story-controls.json` |\n\nAnimation state (`shown`/`hidden`/`lang`) is internal `useState`; cases express\nit via `_signals` for the harness to seed, plus real prop arrays from the route.\n\n## 5. Events\n- `ev.expect(e => e.source===\"button\" && e.text.includes(\"play\") && e.type===\"click\")`\n  → `play()` (story toolbar). Resets to 0 then schedules the reveal timers.\n- `ev.expect(e => e.source===\"button\" && e.text.includes(\"reset\") && e.type===\"click\")`\n  → `reset()` → `shown=0`, `hidden={}`.\n- `ev.expect(e => e.source===\"button\" && e.text.includes(\"endState\") && e.type===\"click\")`\n  → `showAll()` → `shown=activeScript.length`.\n- External (received): `langSignal.subscribe` → `setLang(next)`; if\n  `autoPlayOnView && played`, replays in the new language one frame later.\n- External (received): `IntersectionObserver` on `.phone` (threshold 0.4) →\n  first intersection with `autoPlayOnView` calls `play()` once (`playedRef`).\n- No DOM click handlers on the phone itself; the send button + input are inert\n  decoration (no onClick).\n\n## 6. Motion (extracted)\n- **Bubble entrance — `bubble-in`** (`from{opacity:0;translateY(14px) scale(.96)}\n  to{opacity:1;translateY(0) scale(1)}`): applied INLINE per step as\n  `animation: bubble-in 360ms cubic-bezier(0.34,1.4,0.64,1) both`. Runs on mount\n  because each step is fresh-rendered with `.in` already set — so the `.chat-step`\n  CSS transition (320ms opacity / 360ms bounce transform) never fires for the\n  island. **Jank finding:** animating `transform`+`opacity` is compositor-friendly\n  (fine); but the auto-scroll `el.scrollTo({behavior:\"smooth\"})` on every\n  `shown`/`hidden` change can fight the entrance tween on slow devices. Fix:\n  gate the smooth-scroll to the last step or use `scrollTop = scrollHeight`\n  (instant) during rapid reveals.\n- **Typing dots — `typingBounce`** 1.2s ease-in-out infinite, three spans with\n  0/0.2/0.4s `animation-delay`: `30%{translateY(-4px);opacity:1}` else\n  `translateY(0);opacity:.5`.\n- **Reveal cadence:** JS timers (350ms typing / 200ms first / 700ms others /\n  +1100ms typing linger). Trigger: IO entry (landing) or Play button (story).\n- **Reduced motion:** landing.css global clamp (`*{animation/transition-duration:\n  0.01ms!important}`) flattens bubble-in + typingBounce. The JS reveal timers\n  still run (content appears, just without tweens).\n\n## 7. Responsive (this component's own widths)\n- The component has **no `@media` of its own** — the `.phone` is a fixed\n  `320px × 580px` mockup. The PAGE drops the whole hero/demo visual differently:\n  the landing `.demo-grid` (see landing.css `@media (max-width:980px)`) stacks\n  the copy above the phone on tablet/mobile. Capture at desktop + ≤980px to see\n  the stack, but the phone itself never reflows.\n\n## 8. A11y\n- `.phone-wrap` carries no role; on the landing route the parent demo visual is\n  decorative narrative. The status-bar dots, send button, and \"Online\" are not\n  labeled (acceptable — decorative). \n- **Gaps:** the auto-playing animation has no pause control on the landing\n  surface (only story mode gets buttons); the conversation text is real DOM so\n  SR users can read it, but there's no `aria-live` announcing each new bubble —\n  for a demo this is acceptable. The send `<svg>` has no title (decorative, ok).\n- Reduced motion respected via the global clamp (content still revealed).\n\n## 9. Used on\n- **Landing `/`** (`routes/index.tsx` line 751) — the `.demo` section, with\n  `script/scriptEs/quote/quoteEs`, `messageCopy=\"Message\"`, `autoPlayOnView`.\n- **`/stories/demo-phone-chat`** design playground — `controls` toolbar mode\n  (per the file's own docblock; the stories route is outside this spec's scope).\n- Page-local to landing (NOT a shared component). The dead islands\n  `DemoPhoneChat.tsx` + `HeroRotor.tsx` reference the same look but are unmounted.\n", "fixture": "{\n  \"category\": \"landing\",\n  \"folder\": \"phone-chat\",\n  \"controls\": {\n    \"script\": {\n      \"type\": \"json\",\n      \"value\": [\n        { \"side\": \"right\", \"kind\": \"bubble\", \"cls\": \"me\", \"text\": \"Kitchen remodel for the Hernández family. Cabinets, quartz counters, 3 days labor.\" },\n        { \"side\": \"right\", \"kind\": \"meta\", \"text\": \"9:38 AM\" },\n        { \"side\": \"left\", \"kind\": \"typing\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"Got it 👍 What zip code is the job in?\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"And rough square footage of countertop?\" },\n        { \"side\": \"right\", \"kind\": \"bubble\", \"cls\": \"me\", \"text\": \"78704. About 42 sq ft of counter.\" },\n        { \"side\": \"left\", \"kind\": \"typing\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"Perfect. Quote coming up — typical range for this is $10,800–$12,400.\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"Here's your quote, ready to send:\", \"style\": \"background:var(--mint-200)\" },\n        { \"side\": \"right\", \"kind\": \"quote\" },\n        { \"side\": \"right\", \"kind\": \"bubble\", \"cls\": \"me\", \"text\": \"Looks good. Send it to them.\" },\n        { \"side\": \"right\", \"kind\": \"meta\", \"text\": \"9:41 AM ✓ Sent to client\" }\n      ]\n    },\n    \"scriptEs\": {\n      \"type\": \"json\",\n      \"value\": [\n        { \"side\": \"right\", \"kind\": \"bubble\", \"cls\": \"me\", \"text\": \"Remodelación cocina para los Hernández. Gabinetes, cubierta de cuarzo, 3 días de mano de obra.\" },\n        { \"side\": \"right\", \"kind\": \"meta\", \"text\": \"9:38\" },\n        { \"side\": \"left\", \"kind\": \"typing\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"Listo 👍 ¿Cuál es el código postal del trabajo?\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"¿Y aproximadamente cuántos pies² de cubierta?\" },\n        { \"side\": \"right\", \"kind\": \"bubble\", \"cls\": \"me\", \"text\": \"78704. Como 42 pies² de cubierta.\" },\n        { \"side\": \"left\", \"kind\": \"typing\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"Perfecto. Va la cotización — rango típico $10.800–$12.400.\" },\n        { \"side\": \"left\", \"kind\": \"bubble\", \"cls\": \"them\", \"text\": \"Aquí está tu cotización, lista para enviar:\", \"style\": \"background:var(--mint-200)\" },\n        { \"side\": \"right\", \"kind\": \"quote\" },\n        { \"side\": \"right\", \"kind\": \"bubble\", \"cls\": \"me\", \"text\": \"Se ve bien. Mándasela.\" },\n        { \"side\": \"right\", \"kind\": \"meta\", \"text\": \"9:41 ✓ Enviado al cliente\" }\n      ]\n    },\n    \"quote\": {\n      \"type\": \"json\",\n      \"value\": { \"hd\": \"Quote · #PM-2641\", \"l1\": \"Cabinets & install\", \"l2\": \"Quartz countertops\", \"l3\": \"Demo & labor\", \"total\": \"Total\" }\n    },\n    \"quoteEs\": {\n      \"type\": \"json\",\n      \"value\": { \"hd\": \"Cotización · #PM-2641\", \"l1\": \"Gabinetes e instalación\", \"l2\": \"Cubiertas de cuarzo\", \"l3\": \"Demolición y mano de obra\", \"total\": \"Total\" }\n    },\n    \"messageCopy\": { \"type\": \"text\", \"value\": \"Message\" },\n    \"messageCopyEs\": { \"type\": \"text\", \"value\": \"Mensaje\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"], \"signal\": true },\n    \"controls\": { \"type\": \"boolean\", \"value\": false },\n    \"autoPlayOnView\": { \"type\": \"boolean\", \"value\": true }\n  }\n}\n", "cases": [{"name": "end-state.json", "content": "{\n  \"_name\": \"End state (all steps shown, landing default)\",\n  \"messageCopy\": \"Message\",\n  \"messageCopyEs\": \"Mensaje\",\n  \"controls\": false,\n  \"autoPlayOnView\": true,\n  \"_signals\": { \"lang\": \"en\", \"shown\": 12, \"hidden\": [2] }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — scriptEs + quoteEs active\",\n  \"messageCopy\": \"Message\",\n  \"messageCopyEs\": \"Mensaje\",\n  \"controls\": false,\n  \"autoPlayOnView\": true,\n  \"_signals\": { \"lang\": \"es\", \"shown\": 12, \"hidden\": [2] }\n}\n"}, {"name": "playing.json", "content": "{\n  \"_name\": \"Mid-play — typing indicator visible (step 3 of 12)\",\n  \"messageCopy\": \"Message\",\n  \"controls\": false,\n  \"autoPlayOnView\": true,\n  \"_signals\": { \"lang\": \"en\", \"shown\": 3, \"hidden\": [] }\n}\n"}, {"name": "reset-empty.json", "content": "{\n  \"_name\": \"Reset — empty chat body (story mode)\",\n  \"messageCopy\": \"Message\",\n  \"controls\": true,\n  \"autoPlayOnView\": false,\n  \"_signals\": { \"lang\": \"en\", \"shown\": 0, \"hidden\": [] }\n}\n"}, {"name": "story-controls.json", "content": "{\n  \"_name\": \"Story mode — Play/Reset/End toolbar\",\n  \"messageCopy\": \"Message\",\n  \"messageCopyEs\": \"Mensaje\",\n  \"controls\": true,\n  \"autoPlayOnView\": false,\n  \"_signals\": { \"lang\": \"en\", \"shown\": 12, \"hidden\": [2] }\n}\n"}], "css": "/* ============================================================\n   PhoneChat — demo SMS-mockup island.\n   Extracted from static/landing.css (the route's <Head> links\n   /landing.css). All rules below are the EXACT source rules for\n   the classes PhoneChat.tsx renders. Tokens: --mint-100/200,\n   --brand-green/teal/pink, --coffee-300, --border, --fg-subtle,\n   --fg-muted, --font-heading/body.\n\n   NOTE: the live landing.css ALSO contains a `phoneFloat` keyframe\n   + a `.hs-phone` mockup in the hero — that is a DIFFERENT, static\n   decorative phone (`.hs-*`), not this island. PhoneChat owns the\n   `.phone-wrap / .phone / .chat-*` set below only.\n   ============================================================ */\n\n.phone-wrap {\n  position: relative;\n  display: flex;\n  justify-content: center;\n}\n.phone-bg {\n  position: absolute;\n  inset: 20px -40px;\n  background: radial-gradient(\n    ellipse at center,\n    rgba(207, 229, 200, 0.6) 0%,\n    transparent 60%\n  );\n  pointer-events: none;\n  z-index: 0;\n}\n.phone {\n  position: relative;\n  z-index: 1;\n  width: 320px;\n  background: #1a1a1a;\n  border-radius: 44px;\n  padding: 12px;\n  box-shadow:\n    0 30px 60px rgba(100, 69, 54, 0.25),\n    0 0 0 1px rgba(0, 0, 0, 0.05);\n}\n.phone-screen {\n  background: var(--mint-100);\n  border-radius: 32px;\n  overflow: hidden;\n  height: 580px;\n  display: flex;\n  flex-direction: column;\n  position: relative; /* second rule at line 2081 merges this in */\n}\n.phone-status {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 14px 24px 8px;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  color: var(--brand-teal);\n}\n.phone-status .icons {\n  display: flex;\n  gap: 6px;\n}\n.phone-status .icons span {\n  display: inline-block;\n  width: 4px;\n  height: 12px;\n  background: var(--brand-teal);\n  border-radius: 1px;\n}\n.phone-status .icons span:nth-child(1) { height: 6px; }\n.phone-status .icons span:nth-child(2) { height: 8px; }\n.phone-status .icons span:nth-child(3) { height: 10px; }\n.phone-status .icons span:nth-child(4) { height: 12px; }\n\n.chat-header {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  padding: 12px 18px;\n  background: #fff;\n  border-bottom: 1px solid var(--border);\n}\n.chat-header .av-pm {\n  width: 36px;\n  height: 36px;\n  border-radius: 999px;\n  background: var(--brand-green);\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  overflow: hidden;\n}\n.chat-header .av-pm img { width: 28px; height: 28px; }\n.chat-header .meta { line-height: 1.2; }\n.chat-header .meta strong {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  color: var(--brand-teal);\n  display: block;\n}\n.chat-header .meta span {\n  font-size: 11px;\n  color: var(--brand-green);\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n}\n.chat-header .meta span::before {\n  content: \"\";\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: var(--brand-green);\n}\n\n.chat-body {\n  flex: 1;\n  padding: 16px;\n  overflow-y: auto;\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n  background: linear-gradient(180deg, var(--mint-100) 0%, #fff 100%);\n}\n/* Phones have no visible scrollbar; hide the track, keep scrolling. */\n.phone .chat-body { scrollbar-width: none; }\n.phone .chat-body::-webkit-scrollbar { width: 0; height: 0; display: none; }\n\n.bubble {\n  max-width: 78%;\n  padding: 10px 14px;\n  border-radius: 18px;\n  font-size: 14px;\n  line-height: 1.4;\n  font-family: var(--font-body);\n}\n.bubble.them {\n  background: #fff;\n  color: var(--brand-teal);\n  border-bottom-left-radius: 4px;\n  align-self: flex-start;\n  box-shadow: 0 2px 4px rgba(100, 69, 54, 0.06);\n}\n.bubble.me {\n  background: var(--brand-green);\n  color: #fff;\n  border-bottom-right-radius: 4px;\n  align-self: flex-end;\n}\n.bubble.me a { color: #fff; text-decoration: underline; }\n\n.chat-step {\n  opacity: 0;\n  transform: translateY(14px) scale(0.96);\n  transition:\n    opacity 320ms ease,\n    transform 360ms cubic-bezier(0.34, 1.4, 0.64, 1);\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.chat-step.in {\n  opacity: 1;\n  transform: translateY(0) scale(1);\n}\n.chat-step .bubble { align-self: flex-start; }\n.chat-step.right .bubble,\n.chat-step.right .quote-card,\n.chat-step.right .bubble-meta { align-self: flex-end; }\n.chat-step.right { align-items: flex-end; }\n.chat-step.left { align-items: flex-start; }\n\n/* The PhoneChat island fresh-mounts each chat-step with `.in` ALREADY set\n   AND an inline `animation: bubble-in 360ms cubic-bezier(0.34,1.4,0.64,1) both`\n   (see PhoneChat.tsx line ~276), so the CSS *transition* on `.chat-step`\n   never fires for the island; the keyframe below is what actually plays on\n   insertion. The transition path is the legacy DOM-mutation flow\n   (landing-scripts.js adds `.in` later) — kept for that dead path only. */\n@keyframes bubble-in {\n  from { opacity: 0; transform: translateY(14px) scale(0.96); }\n  to   { opacity: 1; transform: translateY(0) scale(1); }\n}\n\n.bubble-meta {\n  font-size: 10px;\n  color: var(--fg-subtle);\n  margin-top: 2px;\n  align-self: flex-end;\n  font-family: var(--font-heading);\n  font-weight: 700;\n}\n\n.typing {\n  display: inline-flex;\n  gap: 4px;\n  background: #fff;\n  padding: 12px 14px;\n  border-radius: 18px;\n  border-bottom-left-radius: 4px;\n  box-shadow: 0 2px 4px rgba(100, 69, 54, 0.06);\n  align-self: flex-start;\n}\n.typing span {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: var(--coffee-300);\n  animation: typingBounce 1.2s ease-in-out infinite;\n}\n.typing span:nth-child(2) { animation-delay: 0.2s; }\n.typing span:nth-child(3) { animation-delay: 0.4s; }\n@keyframes typingBounce {\n  0%, 60%, 100% { transform: translateY(0); opacity: 0.5; }\n  30%           { transform: translateY(-4px); opacity: 1; }\n}\n\n.quote-card {\n  background: #fff;\n  border-radius: 14px;\n  padding: 12px;\n  margin-top: 4px;\n  align-self: flex-end;\n  width: 80%;\n  border: 1px solid var(--border);\n  box-shadow: 0 4px 12px rgba(100, 69, 54, 0.1);\n}\n.quote-card .qc-head {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  color: var(--brand-teal);\n  font-size: 12px;\n  padding-bottom: 8px;\n  border-bottom: 1px solid var(--border);\n  margin-bottom: 8px;\n}\n.quote-card .qc-head .pdf {\n  font-size: 9px;\n  background: var(--brand-pink);\n  color: #fff;\n  padding: 2px 6px;\n  border-radius: 4px;\n}\n.quote-card .row {\n  display: flex;\n  justify-content: space-between;\n  font-size: 11px;\n  padding: 2px 0;\n  color: var(--fg-muted);\n}\n.quote-card .row strong {\n  color: var(--brand-teal);\n  font-family: var(--font-heading);\n  font-weight: 800;\n}\n.quote-card .total {\n  display: flex;\n  justify-content: space-between;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  color: var(--brand-pink);\n  font-size: 14px;\n  padding-top: 6px;\n  margin-top: 4px;\n  border-top: 1.5px solid var(--brand-teal);\n}\n\n.chat-input {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 10px 14px;\n  background: #fff;\n  border-top: 1px solid var(--border);\n}\n.chat-input .field {\n  flex: 1;\n  background: var(--mint-200);\n  border-radius: 999px;\n  padding: 8px 14px;\n  font-size: 13px;\n  color: var(--fg-subtle);\n}\n.chat-input .send {\n  width: 32px;\n  height: 32px;\n  border-radius: 999px;\n  background: var(--brand-green);\n  color: #fff;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n}\n\n/* The story-mode Play/Reset/End buttons (controls=true) are INLINE-styled in\n   PhoneChat.tsx — no class hooks — so there is no CSS for them here. */\n\n/* Reduced motion: landing.css has a global clamp at the top of the file:\n   @media (prefers-reduced-motion: reduce){ *,*::before,*::after{\n     animation-duration:0.01ms!important; transition-duration:0.01ms!important; } }\n   so bubble-in + typingBounce are both neutralized. PhoneChat keeps its JS\n   step timers regardless — only the visual tween is clamped. */\n", "cssName": "phone-chat.css", "source": "/**\n * Phone-mockup island that drives both the live landing's demo section\n * and the `/stories/demo-phone-chat` design playground.\n *\n *   - Story mode:    `controls` toggles the Play/Reset/End buttons.\n *   - Landing mode:  `autoPlayOnView` plays the conversation once the\n *                    phone scrolls into view (matches the legacy\n *                    LandingScripts behavior).\n *   - Lang toggle:   when both `script`/`scriptEs` (and `quote`/`quoteEs`)\n *                    are provided, the island subscribes to `langSignal`\n *                    and swaps copy + replays.\n *\n * Animation timing mirrors the original `LandingScripts.startReveal`\n * exactly — typing 350ms in + 1100ms linger, bubbles 200ms initial /\n * 700ms thereafter — so the visual cadence is unchanged from prod.\n */\n\nimport { useEffect, useRef, useState } from \"preact/hooks\";\nimport { langSignal } from \"../lib/lang.ts\";\nimport { tFor } from \"../lib/i18n.ts\";\n\nexport interface Bubble {\n  side: \"left\" | \"right\";\n  kind: \"bubble\" | \"meta\" | \"quote\" | \"typing\";\n  cls?: \"me\" | \"them\";\n  text?: string;\n  style?: string;\n}\n\nexport interface QuoteCopy {\n  hd: string;\n  l1: string;\n  l2: string;\n  l3: string;\n  total: string;\n}\n\ninterface Props {\n  script: Bubble[];\n  scriptEs?: Bubble[];\n  quote: QuoteCopy;\n  quoteEs?: QuoteCopy;\n  messageCopy?: string;\n  messageCopyEs?: string;\n  /** Default static lang when langSignal isn't being honored. */\n  lang?: \"en\" | \"es\";\n  /** When true, render Play/Reset/End buttons above the phone (story mode). */\n  controls?: boolean;\n  /** When true, start playback the first time the phone enters the\n   *  viewport. Otherwise the phone shows the end state by default. */\n  autoPlayOnView?: boolean;\n}\n\nfunction QuoteCard({ q, lang }: { q: QuoteCopy; lang: \"en\" | \"es\" }) {\n  return (\n    <div class=\"quote-card\">\n      <div class=\"qc-head\">\n        <span>{q.hd}</span>\n        <span class=\"pdf\">{tFor(lang, \"phoneChat.quote.pdfBadge\")}</span>\n      </div>\n      <div class=\"row\">\n        <span>{q.l1}</span>\n        <strong>{tFor(lang, \"phoneChat.quote.amount1\")}</strong>\n      </div>\n      <div class=\"row\">\n        <span>{q.l2}</span>\n        <strong>{tFor(lang, \"phoneChat.quote.amount2\")}</strong>\n      </div>\n      <div class=\"row\">\n        <span>{q.l3}</span>\n        <strong>{tFor(lang, \"phoneChat.quote.amount3\")}</strong>\n      </div>\n      <div class=\"total\">\n        <span>{q.total}</span>\n        <span>{tFor(lang, \"phoneChat.quote.amountTotal\")}</span>\n      </div>\n    </div>\n  );\n}\n\nexport default function PhoneChat(props: Props) {\n  const {\n    script,\n    scriptEs,\n    quote,\n    quoteEs,\n    messageCopy = tFor(\"en\", \"phoneChat.input.message\"),\n    messageCopyEs = tFor(\"es\", \"phoneChat.input.message\"),\n    lang: langProp = \"en\",\n    controls = false,\n    autoPlayOnView = false,\n  } = props;\n\n  // When both lang scripts are provided, treat langSignal as authoritative.\n  const usesLangSignal = !!scriptEs;\n  const [lang, setLang] = useState<\"en\" | \"es\">(\n    usesLangSignal ? (langSignal.value as \"en\" | \"es\") : langProp,\n  );\n\n  const activeScript = lang === \"es\" && scriptEs ? scriptEs : script;\n  const activeQuote = lang === \"es\" && quoteEs ? quoteEs : quote;\n  const activeInputCopy = lang === \"es\" ? messageCopyEs : messageCopy;\n\n  // `shown` = how many steps have the `.in` class. Default to all so static\n  // design review lands on the final state. autoPlayOnView replays once the\n  // phone scrolls into view.\n  const [shown, setShown] = useState(activeScript.length);\n  const [hidden, setHidden] = useState<Set<number>>(() => new Set());\n  const cancelRef = useRef<{ cancel?: () => void }>({});\n  const chatBodyRef = useRef<HTMLDivElement | null>(null);\n  const phoneRef = useRef<HTMLDivElement | null>(null);\n  const playedRef = useRef(false);\n\n  function cancel(): void {\n    cancelRef.current.cancel?.();\n    cancelRef.current.cancel = undefined;\n  }\n\n  function reset(): void {\n    cancel();\n    setShown(0);\n    setHidden(new Set());\n  }\n\n  function showAll(): void {\n    cancel();\n    setShown(activeScript.length);\n    setHidden(new Set());\n  }\n\n  function play(): void {\n    cancel();\n    setShown(0);\n    setHidden(new Set());\n\n    let cancelled = false;\n    const timers: number[] = [];\n    cancelRef.current.cancel = () => {\n      cancelled = true;\n      timers.forEach((t) => clearTimeout(t));\n    };\n\n    let delay = 0;\n    activeScript.forEach((step, i) => {\n      const isTyping = step.kind === \"typing\";\n      delay += isTyping ? 350 : (i === 0 ? 200 : 700);\n      timers.push(\n        globalThis.setTimeout(() => {\n          if (cancelled) return;\n          setShown(i + 1);\n          if (!isTyping && i > 0 && activeScript[i - 1].kind === \"typing\") {\n            setHidden((prev) => {\n              const next = new Set(prev);\n              next.add(i - 1);\n              return next;\n            });\n          }\n        }, delay) as unknown as number,\n      );\n      if (isTyping) delay += 1100;\n    });\n  }\n\n  // Subscribe to langSignal when bilingual; replay if autoPlay is on.\n  useEffect(() => {\n    if (!usesLangSignal) return;\n    const unsub = langSignal.subscribe((v) => {\n      const next = v as \"en\" | \"es\";\n      setLang(next);\n      if (autoPlayOnView && playedRef.current) {\n        // Restart in the new language.\n        playedRef.current = false; // allow re-trigger\n        // Defer the replay one frame so React state catches up first.\n        globalThis.setTimeout(() => {\n          play();\n          playedRef.current = true;\n        }, 0);\n      }\n    });\n    return unsub;\n  }, [usesLangSignal, autoPlayOnView]);\n\n  // Auto-play on viewport entry (landing mode).\n  useEffect(() => {\n    if (!autoPlayOnView) return;\n    const el = phoneRef.current;\n    if (!el) return;\n    const io = new IntersectionObserver((entries) => {\n      for (const e of entries) {\n        if (e.isIntersecting && !playedRef.current) {\n          playedRef.current = true;\n          play();\n          break;\n        }\n      }\n    }, { threshold: 0.4 });\n    io.observe(el);\n    return () => io.disconnect();\n  }, [autoPlayOnView]);\n\n  // Keep latest message visible.\n  useEffect(() => {\n    const el = chatBodyRef.current;\n    if (!el) return;\n    el.scrollTo({ top: el.scrollHeight, behavior: \"smooth\" });\n  }, [shown, hidden]);\n\n  // Stop pending timers on unmount.\n  useEffect(() => () => cancel(), []);\n\n  return (\n    <>\n      {controls && (\n        <div style=\"display: flex; gap: 8px; align-items: center; justify-content: center; font-family: var(--font-heading, system-ui); font-size: 13px; margin-bottom: 16px;\">\n          <button\n            type=\"button\"\n            onClick={play}\n            style=\"padding: 8px 16px; border-radius: 999px; border: none; background: var(--brand-pink, #FF6B6B); color: #fff; font-weight: 700; cursor: pointer;\"\n          >\n            ▶ {tFor(lang, \"phoneChat.controls.play\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={reset}\n            style=\"padding: 8px 16px; border-radius: 999px; border: 1px solid rgba(0,0,0,0.15); background: #fff; color: var(--brand-teal, #1A535C); font-weight: 600; cursor: pointer;\"\n          >\n            ⟲ {tFor(lang, \"phoneChat.controls.reset\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={showAll}\n            style=\"padding: 8px 16px; border-radius: 999px; border: 1px solid rgba(0,0,0,0.15); background: #fff; color: var(--brand-teal, #1A535C); font-weight: 600; cursor: pointer;\"\n          >\n            ⏭ {tFor(lang, \"phoneChat.controls.endState\")}\n          </button>\n        </div>\n      )}\n\n      <div class=\"phone-wrap\" style=\"position: relative;\">\n        <div class=\"phone-bg\"></div>\n        <div class=\"phone\" ref={phoneRef}>\n          <div class=\"phone-screen\">\n            <div class=\"phone-status\">\n              <span>{tFor(lang, \"phoneChat.statusBar.time\")}</span>\n              <div class=\"icons\">\n                <span></span>\n                <span></span>\n                <span></span>\n                <span></span>\n              </div>\n            </div>\n\n            <div class=\"chat-header\">\n              <div class=\"av-pm\">\n                <img src=\"/logo-monster.png\" alt=\"\" />\n              </div>\n              <div class=\"meta\">\n                <strong>{tFor(lang, \"brand.name\")}</strong>\n                <span>{tFor(lang, \"phoneChat.header.online\")}</span>\n              </div>\n            </div>\n\n            <div class=\"chat-body\" ref={chatBodyRef}>\n              {\n                /* Only render up to `shown`; the chat-body's height grows\n                  with each revealed step, keeping the latest flush with\n                  the input bar via the auto-scroll effect. */\n              }\n              {activeScript.slice(0, shown).map((m, i) => {\n                const sideClass = m.side === \"right\" ? \"right\" : \"left\";\n                const isHidden = hidden.has(i);\n                return (\n                  <div\n                    key={i}\n                    class={`chat-step ${sideClass} in`}\n                    style={`animation: bubble-in 360ms cubic-bezier(0.34, 1.4, 0.64, 1) both;${\n                      isHidden ? \" display: none;\" : \"\"\n                    }`}\n                  >\n                    {m.kind === \"typing\" && (\n                      <div class=\"typing\">\n                        <span></span>\n                        <span></span>\n                        <span></span>\n                      </div>\n                    )}\n                    {m.kind === \"bubble\" && (\n                      <div\n                        class={`bubble ${m.cls ?? \"\"}`.trim()}\n                        style={m.style}\n                      >\n                        {m.text}\n                      </div>\n                    )}\n                    {m.kind === \"meta\" && (\n                      <div class=\"bubble-meta\">{m.text}</div>\n                    )}\n                    {m.kind === \"quote\" && (\n                      <QuoteCard q={activeQuote} lang={lang} />\n                    )}\n                  </div>\n                );\n              })}\n            </div>\n\n            <div class=\"chat-input\">\n              <div class=\"field\">{activeInputCopy}</div>\n              <div class=\"send\">\n                <svg\n                  width=\"14\"\n                  height=\"14\"\n                  viewBox=\"0 0 24 24\"\n                  fill=\"none\"\n                  stroke=\"currentColor\"\n                  stroke-width=\"2.5\"\n                  stroke-linecap=\"round\"\n                  stroke-linejoin=\"round\"\n                >\n                  <line x1=\"22\" y1=\"2\" x2=\"11\" y2=\"13\" />\n                  <polygon points=\"22 2 15 22 11 13 2 9 22 2\" />\n                </svg>\n              </div>\n            </div>\n          </div>\n        </div>\n      </div>\n    </>\n  );\n}\n", "sourceName": "PhoneChat.tsx", "checklist": "# Capture checklist — PhoneChat\n\n**Theme:** light only.\n**Route/URL:** `http://localhost:5280/` — scroll to the `.demo` section (\"See it\nin action\"). Auth-free (landing) → **capture-ready** (no login needed).\nStory mode also at `/stories/demo-phone-chat` if that route is running.\n\n## Auth\nNone. Landing is public.\n\n## Viewports (this component never reflows internally; phone is fixed 320×580)\n- **Desktop ≥980px** — phone sits right of the demo copy (two-col `.demo-grid`).\n- **≤980px** — `.demo-grid` stacks (copy above phone); shoot to confirm the\n  stack (landing.css `@media (max-width:980px)`).\n- **≤560px** — page width check (hero visual drops, but the demo phone stays).\n\n## Element(s) to crop\n- The whole `.phone` mockup (tight crop incl. status bar + chat-input bar).\n- The `.quote-card` step alone (the payoff frame).\n- The `.typing` three-dot indicator (mid-conversation).\n\n## Transient states to drive\n1. **end-state** — let it finish; capture the final frame (quote + \"Sent to client\").\n2. **playing** — refresh and catch a `typing` step + a half-filled body\n   (re-trigger by scrolling the phone out of and back into view, or reload).\n3. **es** — flip the nav language toggle to \"Yo hablo Español\"; the phone\n   replays in Spanish (scriptEs/quoteEs). Capture the Spanish quote card.\n4. **story-controls** (story route only) — Play, Reset (empty), End-state frames.\n\n## Motion to film\n- `bubble-in` step entrance (360ms bounce) — film the reveal sequence end-to-end.\n- `typingBounce` dots (1.2s loop) — short loop.\n- Reduced motion: re-run with `prefers-reduced-motion: reduce`; bubbles should\n  appear instantly (global clamp) while the JS reveal cadence still steps.\n"}]}, {"name": "login", "pageMd": "# Page: `/login`\n\n**Route:** `routes/login.tsx` · **Source copied to:** `js/login.tsx` (route),\nplus island in `components/login-form/`.\n\n## Purpose\nDedicated phone-number entry screen for the passwordless **phone → OTP →\n`/verify`** auth flow (roadmap p.13). First step of sign-in/sign-up (same\nflow for both).\n\n## Classification & data flow\n- **Page tier:** mostly **static SSR** + one small island. `define.page` async\n  handler. No client data fetch on the page itself.\n- **Auth guard (server):** `loadUser(ctx.req)` → if already authenticated,\n  **302 → `/dashboard`** (server redirect, before render). This is the correct\n  PRG-style guard; keep it server-side.\n- **Language (server):** `langFromCookie(cookie)` ?? `pickLangFromAcceptLanguage(accept-language)`\n  — the saved cookie choice wins over the browser locale so the screen matches\n  the language picked on the landing page. All copy via `tFor(lang, …)`.\n- **The single island** is `<LoginForm>` (see `components/login-form/`), which\n  owns the phone input + \"send code\" POST and the client-side nav to\n  `/verify?phone=…`.\n\n## `<Head>`\n- `<title>{loginPage.title} · {brand.name}</title>`\n- `<link rel=\"stylesheet\" href=\"/verify.css\">` — **shared with `/verify`**\n  (same `.verify-shell` / `.verify-card` shell).\n\n## Layout / composition order\n```\n.verify-shell                         (centered full-viewport shell, verify.css)\n  .verify-card                        (white card, max-width, shadow)\n    a.brand  → href=\"/\"               (logo-monster.png 38×38 + \"Paperwork\" + <em>Monster</em> green)\n    h1   loginPage.heading            (32px)\n    p.muted  loginPage.subtitle       (--fg-muted, 16px)\n    <LoginForm />                     ← ISLAND (phone field + submit)\n```\nInline styles on the brand/h1/p; everything else from `verify.css`. The brand\nmark here is the **image** logo (`/logo-monster.png`), not the CSS `.brand__mark`\nglyph used in the app shell — note the two brand treatments.\n\n## Components on this page\n| Component | Where | Tier |\n|---|---|---|\n| `login-form` (`LoginForm`) | `pages/login/components/login-form/` | island — phone input, validation, POST send-otp, redirect to `/verify` |\n\nNo shared components (no app shell — this is a pre-auth screen).\n\n## Capture checklist (auth-free → capture-ready)\n- URL: `/login` (logged-out session; logged-in 302s away — clear `pm_session`\n  cookie first).\n- Viewports: 390 (mobile), 768, 1280. `verify.css` is a single-column centered\n  card at all widths — verify its own `@media` in `verify.css`.\n- States to shoot: empty, focused phone field, invalid number error, submitting\n  (drive via the island). Light theme only.\n\n## Build order\ndesign tokens → `verify.css` shell → `login-form` island → this page.\n", "components": [{"name": "login-form", "component": "LoginForm", "spec": "# LoginForm\n\nThe phone-entry form on `/login`. Collects a US phone number, POSTs an OTP\nrequest, then navigates to `/verify`. Reuses the exact OTP flow as the landing\ncontact form, but rendered as a clean island inside the `.verify-card`.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/LoginForm.tsx`).\n- **Interaction tier:** `island` (client-only state) that performs a **server\n  mutation via fetch + manual navigation** (not form+PRG; it's a JS submit\n  handler that calls the API client then sets `location.href`).\n- **Client state owned (`useState`):**\n  - `phone: string` — raw input value; displayed through `formatPhoneDisplay`\n    (`(555) 123-4567` mask) but stored raw.\n  - `submitting: boolean` — disables the button + swaps its label to `…`.\n  - `err: string | null` — inline error text (rendered as `<p role=\"alert\">`).\n  - reads `langSignal.value` (via `t()`) so labels localize live.\n- **Server mutation + feedback:**\n  - `onSubmit` → `e.preventDefault()` → normalize to E.164 (`toE164`) → guard\n    `< 10 digits` (sets `loginForm.phoneIncomplete`) → `landingClient.sendOtp(\n    { phoneNumber, language: lang })` → **POST `/api/auth/send-otp`**.\n  - **Success:** `globalThis.location.href = \"/verify?phone=<e164>&lang=<lang>\"`\n    — a full navigation to the verify page (acceptable: it's a step transition,\n    not a refresh-after-mutation-in-place). No flash/toast; the verify page is\n    the feedback.\n  - **Failure:** `err = \"Error: <status>\"` for an `ApiError`, else\n    `loginForm.sendFailed`. Button re-enables (`finally`).\n- **Liveness:** request-response only (one POST). No polling/ws.\n- **Anti-pattern check:** NOT the frozen-SSR-props + `location.reload()`\n  whole-page island anti-pattern. It owns only its own form state and navigates\n  forward on success. The only smell: `toE164` assumes a US `+1` when 10 digits\n  are entered, and the `< 10` guard uses `e164.replace(/\\D/g,\"\").length` which\n  for a leading `+1` counts 11 — so a 9-digit entry \"+1xxxxxxxxx\" can slip past;\n  minor. No reload to flag.\n- **Data-shape hazards:** the displayed value (`formatPhoneDisplay(phone)`) is\n  derived on every render from the raw `phone` state — typing non-digits is\n  silently stripped. `autoFocus` on the input (focus on mount).\n\n## 2. Anatomy (all elements INLINE-styled; no class hooks — see css/login-form.css)\n```\n<form onSubmit=onSubmit>                       ← flex column, gap 14, left-aligned\n  <label>\n    <span>{t(\"loginForm.phoneLabel\")}</span>   ← caption\n    <input type=tel inputMode=numeric autoComplete=tel autoFocus\n           value={formatPhoneDisplay(phone)} placeholder={t phonePlaceholder}\n           required />\n  </label>\n  {err && <p role=\"alert\">{err}</p>}           ← #a83b3b\n  <button type=submit disabled={submitting}>{submitting ? \"…\" : t submit}</button>\n  <p>{t(\"loginForm.helper\")}</p>               ← centered fine print\n</form>\n```\nRendered inside the route's `.verify-card` (login.tsx), under a `.brand` link,\nan `<h1>` (\"Welcome back\") and a `.muted` subtitle.\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| (none) | — | — | — | — |\n\nLoginForm takes **no props**. All copy comes from `t(…)` keyed off `langSignal`;\nbehavior is self-contained. (Cases below drive internal `useState` via `_signals`.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | empty input, autofocused, button enabled | `cases/default/default.json` |\n| filled | a formatted phone typed in | `cases/filled/filled.json` |\n| submitting | POST in flight: button disabled, label `…` | `cases/submitting/submitting.json` |\n| error | send failed / incomplete → red `<p role=alert>` | `cases/error/error.json` |\n| es | Spanish labels (`langSignal=\"es\"`) | `cases/es/es.json` |\n\n## 5. Events\n- `ev.expect(e => e.source===\"form\" && e.type===\"submit\")` → `onSubmit`:\n  validates, POSTs `/api/auth/send-otp`, navigates to `/verify?phone=…&lang=…`.\n- `ev.expect(e => e.source===\"input[type=tel]\" && e.type===\"input\")` →\n  `setPhone(value)` (re-masked on render).\n- `ev.expect(e => e.source===\"button[type=submit]\" && e.type===\"click\")` →\n  native form submit (same path as the submit event).\n- External (received): `langSignal` change → re-render with new `t()` strings.\n\n## 6. Motion (extracted)\n- **None of its own.** No CSS animation/transition on any LoginForm element\n  (everything is inline-styled with no `transition`). The submit label simply\n  swaps text to `…` while `submitting`. (The page chrome `.verify-card` has the\n  `--shadow-lg` static shadow; the `.btn` family transitions live elsewhere and\n  this button is NOT a `.btn` — it's a bespoke inline-styled `<button>`.)\n- **Reduced motion:** N/A.\n\n## 7. Responsive\n- No `@media` of its own. The input is `width:100%` inside the `.verify-card`\n  (max-width 440px), so it scales with the card. The verify-shell re-centers the\n  card above the iOS soft keyboard via `--vvh`/`--vvt` (MobileViewport island) —\n  see verify.css `.verify-shell`.\n\n## 8. A11y\n- Real `<form>` + `<label>`-wrapped `<input type=\"tel\" inputMode=\"numeric\"\n  autoComplete=\"tel\" required>` — native semantics, mobile numeric keypad,\n  browser tel autofill. `autoFocus` lands the caret on mount.\n- Error is announced via `<p role=\"alert\">`.\n- **Gaps:** the submit button has no busy `aria` beyond the disabled state +\n  `…` text (consider `aria-busy`); the input has no `aria-describedby` tying it\n  to the error or helper text.\n\n## 9. Used on\n- **`/login`** only (`routes/login.tsx` line 60). Page-local island.\n- NOT the same as the dead `islands/ContactForm.tsx` (the landing contact form\n  is inline markup in `routes/index.tsx` driven by `static/landing-scripts.js`).\n  Both hit the same `POST /api/auth/send-otp` endpoint, but LoginForm is the\n  only live OTP-request island.\n", "fixture": "{\n  \"category\": \"login\",\n  \"folder\": \"login-form\",\n  \"controls\": {\n    \"_lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"], \"signal\": true }\n  }\n}\n", "cases": [{"name": "default.json", "content": "{\n  \"_name\": \"Default — empty, autofocused\",\n  \"_signals\": { \"phone\": \"\", \"submitting\": false, \"err\": null, \"_langSignal\": \"en\" }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — incomplete number\",\n  \"_signals\": { \"phone\": \"512555\", \"submitting\": false, \"err\": \"Phone number is incomplete\", \"_langSignal\": \"en\" }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish labels (langSignal=es)\",\n  \"_signals\": { \"phone\": \"\", \"submitting\": false, \"err\": null, \"_langSignal\": \"es\" }\n}\n"}, {"name": "filled.json", "content": "{\n  \"_name\": \"Filled — formatted phone entered\",\n  \"_signals\": { \"phone\": \"5125550142\", \"submitting\": false, \"err\": null, \"_langSignal\": \"en\" }\n}\n"}, {"name": "submitting.json", "content": "{\n  \"_name\": \"Submitting — POST in flight, button shows …\",\n  \"_signals\": { \"phone\": \"5125550142\", \"submitting\": true, \"err\": null, \"_langSignal\": \"en\" }\n}\n"}], "css": "/* ============================================================\n   LoginForm — phone-entry island (/login).\n   THERE IS NO CLASS-BASED CSS FOR THIS COMPONENT. Every element in\n   LoginForm.tsx is styled with INLINE `style=\"…\"` strings (the\n   <form>, <label> caption, <input>, error <p>, submit <button>,\n   helper <p>). The route's /verify.css stylesheet only contributes\n   the surrounding chrome (`.verify-shell`, `.verify-card`, `.brand`,\n   `h1`, `.muted`) — see ../../../css/verify.css and login.md.\n\n   Below: the exact inline declarations LoginForm.tsx applies, reproduced\n   as comments so a rebuild can port them to classes or keep them inline.\n   The literal hex fallbacks in the source (e.g. #519843) are the same\n   values as the Sabor tokens — prefer the token.\n   ============================================================ */\n\n/* form (root) — LoginForm.tsx line 64\n   display:flex; flex-direction:column; gap:14px; text-align:left; */\n\n/* label caption <span> — line 67\n   display:block; font-size:13px; font-weight:700;\n   color: var(--fg-muted, #6b7560); margin-bottom:6px; */\n\n/* phone <input> — `inputStyle`, line 58\n   width:100%; box-sizing:border-box; padding:14px 16px;\n   border:1px solid var(--border, #d8dcd5); border-radius:12px;\n   font:inherit; font-size:17px; background:#fff; color:var(--fg); */\n\n/* error <p role=alert> — line 84\n   color:#a83b3b; font-size:14px; margin:0;\n   (#a83b3b is the public-surface danger red, NOT the app --danger #e03131) */\n\n/* submit <button> — line 92\n   appearance:none; border:0; border-radius:12px; padding:14px 18px;\n   background: var(--brand-green, #519843); color:#fff;\n   font:inherit; font-weight:800; font-size:16px; cursor:pointer;\n   (no :hover / :disabled style — disabled only changes the label to \"…\") */\n\n/* helper <p> — line 96\n   color:var(--fg-muted, #6b7560); font-size:13px; margin:2px 0 0;\n   text-align:center; */\n\n/* Motion: none of its own. Reduced motion: N/A (no CSS animation/transition). */\n", "cssName": "login-form.css", "source": "import { useState } from \"preact/hooks\";\nimport { langSignal } from \"../lib/lang.ts\";\nimport { t } from \"../lib/i18n.ts\";\nimport { landingClient } from \"../clients/landing.ts\";\nimport { ApiError } from \"../lib/api.ts\";\n\n/**\n * LoginForm — the clean, login-branded phone entry used by /login\n * (roadmap p.13). Reuses the exact same OTP flow as the landing contact\n * form: send a code, then hand off to /verify for the 6-digit step.\n */\nfunction formatPhoneDisplay(raw: string): string {\n  const digits = raw.replace(/\\D/g, \"\").slice(0, 10);\n  const a = digits.slice(0, 3), b = digits.slice(3, 6), c = digits.slice(6);\n  if (digits.length <= 3) return a;\n  if (digits.length <= 6) return `(${a}) ${b}`;\n  return `(${a}) ${b}-${c}`;\n}\n\nfunction toE164(raw: string): string {\n  const digits = raw.replace(/\\D/g, \"\");\n  if (raw.startsWith(\"+\")) return \"+\" + digits;\n  if (digits.length === 10) return \"+1\" + digits;\n  return \"+\" + digits;\n}\n\nexport default function LoginForm() {\n  const [phone, setPhone] = useState(\"\");\n  const [submitting, setSubmitting] = useState(false);\n  const [err, setErr] = useState<string | null>(null);\n\n  const lang = langSignal.value;\n\n  async function onSubmit(e: Event) {\n    e.preventDefault();\n    setErr(null);\n    const e164 = toE164(phone);\n    if (e164.replace(/\\D/g, \"\").length < 10) {\n      setErr(t(\"loginForm.phoneIncomplete\"));\n      return;\n    }\n    setSubmitting(true);\n    try {\n      await landingClient.sendOtp({ phoneNumber: e164, language: lang });\n      globalThis.location.href = `/verify?phone=${\n        encodeURIComponent(e164)\n      }&lang=${lang}`;\n    } catch (error) {\n      const msg = error instanceof ApiError\n        ? `${error.status}`\n        : t(\"loginForm.sendFailed\");\n      setErr(`Error: ${msg}`);\n    } finally {\n      setSubmitting(false);\n    }\n  }\n\n  const inputStyle =\n    \"width:100%;box-sizing:border-box;padding:14px 16px;border:1px solid var(--border,#d8dcd5);border-radius:12px;font:inherit;font-size:17px;background:#fff;color:var(--fg)\";\n\n  return (\n    <form\n      onSubmit={onSubmit}\n      style=\"display:flex;flex-direction:column;gap:14px;text-align:left\"\n    >\n      <label style=\"display:block\">\n        <span style=\"display:block;font-size:13px;font-weight:700;color:var(--fg-muted,#6b7560);margin-bottom:6px\">\n          {t(\"loginForm.phoneLabel\")}\n        </span>\n        <input\n          type=\"tel\"\n          inputMode=\"numeric\"\n          autoComplete=\"tel\"\n          autoFocus\n          value={formatPhoneDisplay(phone)}\n          onInput={(e) => setPhone((e.target as HTMLInputElement).value)}\n          placeholder={t(\"loginForm.phonePlaceholder\")}\n          required\n          style={inputStyle}\n        />\n      </label>\n      {err\n        ? (\n          <p style=\"color:#a83b3b;font-size:14px;margin:0\" role=\"alert\">\n            {err}\n          </p>\n        )\n        : null}\n      <button\n        type=\"submit\"\n        disabled={submitting}\n        style=\"appearance:none;border:0;border-radius:12px;padding:14px 18px;background:var(--brand-green,#519843);color:#fff;font:inherit;font-weight:800;font-size:16px;cursor:pointer\"\n      >\n        {submitting ? \"…\" : t(\"loginForm.submit\")}\n      </button>\n      <p style=\"color:var(--fg-muted,#6b7560);font-size:13px;margin:2px 0 0;text-align:center\">\n        {t(\"loginForm.helper\")}\n      </p>\n    </form>\n  );\n}\n", "sourceName": "LoginForm.tsx", "checklist": "# Capture checklist — LoginForm\n\n**Theme:** light only.\n**Route/URL:** `http://localhost:5280/login`. Auth-free → **capture-ready**\n(if a session cookie exists the route 302s to `/dashboard`; capture logged-out).\n\n## Auth\nNone to reach the page. To exercise the success path end-to-end use dev master\nOTP `000000` on the resulting `/verify` page.\n\n## Viewports (component has no own @media; it fills the .verify-card, max 440px)\n- **Desktop 1280px** — card centered.\n- **Mobile 390px** — card near full width; verify-shell re-centers above the\n  iOS soft keyboard (`--vvh`/`--vvt`). Capture with keyboard up if possible.\n\n## Element(s) to crop\n- The whole `.verify-card` (brand + \"Welcome back\" h1 + subtitle + form).\n- The form alone (label + input + submit + helper).\n\n## Transient states to drive\n1. **default** — empty, caret in the input (autofocus).\n2. **filled** — type `5125550142` → input shows `(512) 555-0142`.\n3. **submitting** — submit a valid number; the button shows `…` briefly before\n   navigation (throttle network or stub the API to hold this frame).\n4. **error** — submit an incomplete number (e.g. `512555`) → red\n   \"Phone number is incomplete\" `<p role=alert>`.\n5. **es** — set `localStorage[\"pm:lang\"]=\"es\"` (or arrive via the landing toggle\n   which sets the `pm_lang` cookie) → Spanish labels.\n\n## Motion to film\n- None — LoginForm has no CSS animation/transition. (Only the label text swaps\n  to `…`.) No reduced-motion variant needed.\n"}]}, {"name": "payments", "pageMd": "# Page — /payments\n\nEditorial \"money landed\" view of recorded payments. **MANUAL-ONLY**: payments\nare *recorded*, never processed in-app (no card/merchant flow). The page reuses\nthe Quotes card/track/layout classes and adds its own `.pph*` hero from\n`payments.css`. The whole interactive surface is one client island.\n\n## Purpose\nCelebrate received money. A big \"$X showed up this month\" hero with a fanned\nstack of recent-payment stubs, a 4-cell KPI strip, a \"Just landed\" track of\npayment flip cards + a compact tail-of-month list, an optional \"In transit\"\ntrack (ACH/check still inside their settlement window), and a right rail of\nanalytics widgets (cash-flow sparkline, top payors, method mix, a tip).\n\n## App-shell composition (order)\nRoute file: `routes/payments/index.tsx` (copied to `js/index.tsx`). A\n`define.page` SSR route (not an island), same shape as /invoices:\n\n```\n<Head> … </Head>\n<div class=\"app\">\n  <DashSidebar active=\"payments\" />        ← SHARED (shared-components/dash-sidebar)\n  <main class=\"main\">\n    <DashTopbar greetingDate greetingName/> ← SHARED (shared-components/dash-topbar)\n    <div class=\"content\">\n      <PaymentsPage />                      ← THE island (components/payments-page)\n    </div>\n  </main>\n</div>\n```\n\nOrder: Sidebar, then main → Topbar → content → PaymentsPage.\n\n## `<Head>` — title + CSS\n- `<title>` = `tFor(lang, \"paymentsRoute.docTitle\")`.\n- Stylesheets (keys, in order): `css-dashboard` → `/dashboard.css`,\n  `css-quotes` → `/quotes.css`, `css-payments` → `/payments.css`.\n- `/payments.css` supplies the `.pph*` editorial hero + `.pcard__amount`; all\n  other `.q*` classes come from quotes.css; tokens + shell from dashboard.css.\n  See `../../design-tokens.md`.\n\n## SSR data\nRoute SSRs only `user.language` (→ doc title + greeting) + greeting date. **No\npayment data is server-rendered.** The island fetches everything on mount\n(payments + invoices + customers). First paint = Skeletons.\n\n## Sections (top → bottom)\n1. **PaymentsHero** (`.pph`) — eyebrow (Payments · {month}), amount headline\n   (or a \"fresh\" empty variant), sub, two assistant-seeded CTAs, and the\n   `.pph__stack` of up to 3 rotated recent-payment stubs.\n   → `components/payments-hero`.\n2. **PaymentsKpis** (`.qkpi`) — Landed this month / In transit (accent) /\n   Needs attention / Avg days to pay. → `components/payments-kpis`.\n3. **`.qlay` → two columns**:\n   - **main column** — QuoteTrack sections (SHARED — `shared-components/\n     quote-track`):\n     - (conditional) \"Needs attention\" track — only if `attention.length>0`\n       (always 0 today; the schema carries no declined/returned status).\n     - \"Just landed\" track (defaultOpen) — a `.qcards` grid of **PaymentCard**\n       flip cards for ≤1-day-old payments, then a `.qdone` list of\n       **LandedRow** for older landed payments. Honest-empty hint when both 0.\n     - (conditional) \"In transit\" track — only if `transit.length>0`\n       (ACH <2d / check <5d still settling). defaultOpen=false.\n     - **Track numbering is dynamic**: \"01\"/\"02\"/\"03\" shift depending on\n       whether the attention track is present.\n   - **`aside.qside`** (sticky, 320px) — 4 widgets:\n     PSideFlow (sparkline) · PSideTopPayors · PSideMix · PSideTip.\n     → `components/payment-side-rail`.\n\n## Build order\n1. Tokens + quotes.css + payments.css.\n2. Shell (DashSidebar/DashTopbar) + Skeletons + QuoteTrack.\n3. `EnrichedPayment` derivation + `deriveStatus` (settlement windows).\n4. PaymentsHero (incl. fresh variant + stub stack) → PaymentsKpis.\n5. PaymentCard flip card; LandedRow; the conditional tracks.\n6. Side rail widgets.\n\n## Status taxonomy (derived, not stored)\nThe backend `Payment` model carries no status. The island derives:\n- **landed** — settled (instant methods, or aged past the settlement window).\n- **transit** — ACH/check still inside `SETTLE_DAYS` (ach 2, check 5).\n- **attention** — RESERVED for future declined/returned; always empty today,\n  so its track + KPI render but show 0, and the track hides itself.\n\n## Honest-empty\n- Fresh month (monthTotal/transitTotal/attentionCount all 0): hero shows a\n  \"fresh start\" variant; Landed track shows the `landedEmpty` hint; each side\n  widget shows its own empty copy (PSideFlow `flow.empty`, PSideTopPayors\n  `payors.empty`, PSideMix `mix.empty`).\n\n## Data-shape hazards (flagged)\n- **Payment-sum rollups over jobs are recomputed client-side every render**:\n  `monthTotal`, `transitTotal`, `avgDays`, AND all four side widgets\n  (PSideTopPayors tallies by client, PSideMix by method, PSideFlow buckets by\n  index). The dashboard equivalents (`payments.methodMixCents`, `topPayors`,\n  `revenue.sparkline12mo`) are whole-account Σ-over-all-payments rollups — see\n  `../../data-model.md` §5.1. **[denormalize / precompute; don't scan].**\n- **PSideFlow buckets amounts by array index, NOT by `receivedAt`** — the\n  \"12-week cash-flow shape\" is fabricated from order, not real dates (the\n  source comment admits it). The Feb/Mar/Apr axis labels are hardcoded.\n\n## Anti-patterns\n- Whole-page island, fetch-on-mount, frozen SSR props (only `lang`).\n- **Undefined CSS classes** in PSideTopPayors + LandedRow render unstyled —\n  see payment-side-rail.md / landed-row.md §6. (Real visible bug.)\n- PaymentCard's CTA + all three back-foot buttons are **no-ops**\n  (`e.stopPropagation()` only) — see payment-card.md.\n", "components": [{"name": "landed-row", "component": "LandedRow", "spec": "# LandedRow\n\nThe compact \"tail of month\" row (`.qdone__row`) for older-than-1-day landed\npayments — a won-style check badge, the client name, a `method · when ·\ninvoiceRef` sub-line, and the amount. Rendered in the `.qdone` two-up grid below\nthe recent PaymentCards inside the Just-landed track.\n\n## 1. Classification & behavior\n- **Bucket:** local presentational component of the PaymentsPage island\n  (`islands/PaymentsPage.tsx` lines 786–801; extracted to `js/LandedRow.tsx`).\n- **Interaction tier:** **static / island-child.** No client state, no fetch,\n  no events, no callbacks. (In the island source the row takes only `lang` + `p`\n  and calls the module-level `methodLabel`; the extracted folder version threads\n  `methodLabel` in as a prop to stay self-contained.)\n- **Server actions + flash:** none. The row is not clickable — no flip, no\n  navigation, no mutation (unlike the `.qcard` PaymentCard it sits beneath).\n- **Data source:** the single `p: EnrichedPayment` prop (parent-derived\n  `client`, `method`, `whenLabel`, `invoiceRef`, `amount`). **Honest-empty:** the\n  parent only renders rows when `olderLanded.length > 0`; the row itself has no\n  empty state.\n- **Liveness:** none.\n- **Data-shape hazards:**\n  - `invoiceRef` is cosmetic (`INV-` + first 6 chars of `invoiceId`).\n  - `whenLabel` is a relative humanization (`Today`/`Yesterday`/`Nd ago`) from\n    `daysAgo` — by the time a payment is a `LandedRow` it's always `>1d`, so the\n    label is always `\"Nd ago\"`.\n  - No status branch: a LandedRow is, by construction, always a settled\n    (`landed`) payment, so the badge is always the green `--won` variant.\n\n## 2. Anatomy\n```\n<div class=\"qdone__row\">\n  <div class=\"qdone__badge qdone__badge--won\"><I check size=13 sw=2.5/></div>\n  <div class=\"qdone__body\">                       ← UNSTYLED wrapper (see §6)\n    <div class=\"qdone__title\">{client}</div>\n    <div class=\"qdone__sub\">                       ← UNSTYLED (see §6)\n      {methodLabel(lang, method)} · {whenLabel} · {invoiceRef}\n    </div>\n  </div>\n  <div class=\"qdone__amt\">{fmtMoney(amount)}</div>\n</div>\n```\n- The `.qdone__row` grid template is `auto minmax(0,1fr) auto auto auto` — five\n  tracks — but this markup only fills three slots (badge, body, amt), so the two\n  trailing `auto` tracks collapse to 0. (The quotes/invoices won/lost rows that\n  defined this grid had more cells.)\n- Deps: `I`/`ICN.check`, `fmtMoney`, `methodLabel`/`tFor`.\n- **Icon dependency:** `I` + `ICN.check` (the won badge glyph).\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` (required) | — | select | no |\n| `p` | `EnrichedPayment` (required) | — | object | no |\n| `methodLabel` | `(lang, method) => string` | (island uses module fn) | (callback) | no |\n\n`p` fields read: `client`, `method`, `whenLabel`, `invoiceRef`, `amount`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | a settled older payment row (check method) | `cases/default/default.json` |\n| long-name | overflowing client name → ellipsis clip | `cases/long-name/long-name.json` |\n| es | Spanish method + when label | `cases/es/es.json` |\n\n## 5. Events\n- None. The row is non-interactive (no click handler, not a link/button).\n\n## 6. Motion (extracted, from quotes.css)\n- **Hover:** `.qdone__row:hover` → `border-color: var(--mint-300)` +\n  `transform: translateX(2px)`, over `var(--dur-fast) var(--ease-out)` (subtle\n  slide). Only motion on the row.\n- **Reduced motion:** no component-local guard — global tokens clamp; verify the\n  hover slide goes instant.\n- **CSS HAZARD (FLAG):** the markup uses `.qdone__body` (middle-cell wrapper) and\n  `.qdone__sub` (the `method·when·invoiceRef` line), but `static/quotes.css` only\n  defines `.qdone__title` / `.qdone__client` / `.qdone__when`. So **`.qdone__body`\n  and `.qdone__sub` are UNSTYLED** — the sub-line falls back to inherited body\n  text instead of the small muted `.qdone__client` treatment the design intends.\n  **Fix:** rename `.qdone__sub` → `.qdone__client` (or add the missing rules).\n\n## 7. Responsive (own `@media`, from quotes.css)\n- `≤1100px`: the parent `.qdone` grid → 1 column (rows go full-width).\n- `≤560px`: `.qdone__row` gap/padding tighten (`gap:8px; padding:12px`) and\n  `.qdone__when` is hidden — though this markup has no `.qdone__when` cell, so\n  that rule is a no-op here.\n\n## 8. A11y\n- The check badge is a decorative glyph with no label; the row's meaning is\n  carried by the visible text (client/method/when/amount) — acceptable, though\n  the badge could use an `aria-label=\"paid\"`.\n- The row is a `<div>` (not a list item) — wrapping the `.qdone` grid in a\n  `<ul>`/`<li>` would give AT list semantics. Currently plain divs.\n\n## 9. Used on\n`/payments` only (the `.qdone` tail list under the Just-landed track's recent\ncards). CSS = `css/landed-row.css` (the `.qdone*` block from `static/quotes.css`\n+ the flagged-undefined `.qdone__body`/`.qdone__sub`).\n", "fixture": "{\n  \"category\": \"payments\",\n  \"folder\": \"landed-row\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"p\": {\n      \"type\": \"object\",\n      \"value\": {\n        \"id\": \"pay_2bb1043f\",\n        \"userId\": \"usr_01\",\n        \"invoiceId\": \"inv_aa19ff2c8e30\",\n        \"amount\": 920000,\n        \"method\": \"check\",\n        \"receivedAt\": \"2026-06-10T09:30:00Z\",\n        \"createdAt\": \"2026-06-10T09:31:00Z\",\n        \"updatedAt\": \"2026-06-10T09:31:00Z\",\n        \"client\": \"Tom Becker\",\n        \"initials\": \"TB\",\n        \"invoiceRef\": \"INV-AA19FF\",\n        \"daysAgo\": 8,\n        \"whenLabel\": \"8d ago\",\n        \"note\": \"Check from Tom, deposited via mobile.\",\n        \"status\": \"landed\"\n      }\n    }\n  },\n  \"_note\": \"p is an EnrichedPayment (>1d landed → it's a LandedRow, not a card). Row reads client/method/whenLabel/invoiceRef/amount. amount 920000 → \\\"$9,200\\\". Badge is always --won (green). HAZARD: .qdone__body / .qdone__sub are undefined in CSS → the sub-line renders as plain inherited body text.\"\n}\n", "cases": [{"name": "default.json", "content": "{\n  \"_name\": \"Default — settled older check payment: won badge, \\\"Check · 8d ago · INV-AA19FF\\\", $9,200\",\n  \"lang\": \"en\",\n  \"p\": {\n    \"id\": \"pay_2bb1043f\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_aa19ff2c8e30\",\n    \"amount\": 920000,\n    \"method\": \"check\",\n    \"receivedAt\": \"2026-06-10T09:30:00Z\",\n    \"createdAt\": \"2026-06-10T09:31:00Z\",\n    \"updatedAt\": \"2026-06-10T09:31:00Z\",\n    \"client\": \"Tom Becker\",\n    \"initials\": \"TB\",\n    \"invoiceRef\": \"INV-AA19FF\",\n    \"daysAgo\": 8,\n    \"whenLabel\": \"8d ago\",\n    \"note\": \"Check from Tom, deposited via mobile.\",\n    \"status\": \"landed\"\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — method label + relative when label localize via es.json\",\n  \"lang\": \"es\",\n  \"p\": {\n    \"id\": \"pay_2bb1043f\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_aa19ff2c8e30\",\n    \"amount\": 920000,\n    \"method\": \"check\",\n    \"receivedAt\": \"2026-06-10T09:30:00Z\",\n    \"createdAt\": \"2026-06-10T09:31:00Z\",\n    \"updatedAt\": \"2026-06-10T09:31:00Z\",\n    \"client\": \"Tom Becker\",\n    \"initials\": \"TB\",\n    \"invoiceRef\": \"INV-AA19FF\",\n    \"daysAgo\": 8,\n    \"whenLabel\": \"hace 8d\",\n    \"note\": \"Cheque de Tom, depositado por móvil.\",\n    \"status\": \"landed\"\n  },\n  \"_note\": \"lang=es → methodLabel(es,'check') resolves the ES paymentMethod.check; whenLabel is pre-localized by the parent (paymentsPage.when.daysAgo es). invoiceRef + fmtMoney unchanged.\"\n}\n"}, {"name": "long-name.json", "content": "{\n  \"_name\": \"Long-name — overflowing client name clips with ellipsis (.qdone__title white-space:nowrap)\",\n  \"lang\": \"en\",\n  \"p\": {\n    \"id\": \"pay_5d77e9b2\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_77b0c1aa4410\",\n    \"amount\": 125000,\n    \"method\": \"ach\",\n    \"receivedAt\": \"2026-06-09T16:00:00Z\",\n    \"createdAt\": \"2026-06-09T16:05:00Z\",\n    \"updatedAt\": \"2026-06-09T16:05:00Z\",\n    \"client\": \"Greenfield Homeowners Association Board of Directors LLC\",\n    \"initials\": \"GH\",\n    \"invoiceRef\": \"INV-77B0C1\",\n    \"daysAgo\": 9,\n    \"whenLabel\": \"9d ago\",\n    \"note\": \"Greenfield's auto-pay cleared cleanly. Already in the account.\",\n    \"status\": \"landed\"\n  },\n  \"_note\": \".qdone__title is overflow:hidden + text-overflow:ellipsis + white-space:nowrap → the long name truncates inside the minmax(0,1fr) middle track. Verifies the truncation behavior. amount 125000 → \\\"$1,250\\\".\"\n}\n"}], "css": "/* ============================================================================\n   LandedRow — the `.qdone` grid + `.qdone__row` block. Copied verbatim from\n   static/quotes.css (lines 662–777). Tokens from dashboard.css.\n\n   HAZARD: the island markup uses `.qdone__body` and `.qdone__sub` (see\n   ../js/LandedRow.tsx), but quotes.css only defines `.qdone__title` /\n   `.qdone__client` / `.qdone__when`. So `.qdone__body` (the middle cell\n   wrapper) and `.qdone__sub` (the method·when·invoiceRef line) are UNSTYLED —\n   the sub-line falls back to inherited body text instead of the small muted\n   `.qdone__client` treatment. Flagged in landed-row.md §6.\n   ============================================================================ */\n\n.qdone {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: 10px;\n}\n@media (max-width: 1100px) {\n  .qdone { grid-template-columns: 1fr; }\n}\n.qdone__row {\n  display: grid;\n  grid-template-columns: auto minmax(0, 1fr) auto auto auto;\n  gap: 14px;\n  align-items: center;\n  padding: 14px 18px;\n  background: #fff;\n  border-radius: var(--radius-lg);\n  border: 1px solid rgba(26, 83, 92, 0.07);\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n@media (max-width: 560px) {\n  .qdone__row { gap: 8px; padding: 12px 12px; }\n  .qdone__when { display: none; }\n}\n.qdone__row:hover {\n  border-color: var(--mint-300);\n  transform: translateX(2px);\n}\n.qdone__badge {\n  width: 32px;\n  height: 32px;\n  border-radius: 10px;\n  display: grid;\n  place-items: center;\n  color: #fff;\n  flex-shrink: 0;\n}\n.qdone__badge--won { background: linear-gradient(135deg, #5fa34f, #3f7a33); }\n.qdone__badge--lost { background: linear-gradient(135deg, #b89d90, #785544); }\n.qdone__title {\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 13.5px;\n  color: var(--brand-teal);\n  line-height: 1.2;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qdone__client {\n  font-size: 11px;\n  color: var(--fg-muted);\n  margin-top: 2px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qdone__amt {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n}\n.qdone__when {\n  font-size: 11px;\n  color: var(--fg-muted);\n  font-family: var(--font-body);\n  text-align: right;\n}\n\n/* === UNSTYLED (markup uses these; quotes.css does NOT define them) ===\n   .qdone__body  — middle cell wrapper (renders as default block)\n   .qdone__sub   — method·when·invoiceRef line (renders as inherited body text;\n                   intended to look like .qdone__client). */\n", "cssName": "landed-row.css", "source": "/* Extracted verbatim from islands/PaymentsPage.tsx (`LandedRow`). Compact\n * \"tail of month\" row in the `.qdone` grid for older-than-1-day landed\n * payments — a won-style check badge, client name, method·when·invoiceRef\n * sub, and the amount. Pure presentational; no state, no actions. */\nimport { I, ICN } from \"../../payments-page/js/dash-icons.tsx\";\nimport { fmtMoney } from \"../../payments-page/js/format.ts\";\nimport { type Lang, tFor } from \"../../../../../../lib/i18n.ts\";\n\n// deno-lint-ignore no-explicit-any\nexport function LandedRow(\n  { lang, p, methodLabel }: {\n    lang: Lang;\n    p: any; // EnrichedPayment — see PaymentsPage.tsx line 148\n    methodLabel: (lang: Lang, m: string) => string;\n  },\n) {\n  return (\n    <div class=\"qdone__row\">\n      <div class=\"qdone__badge qdone__badge--won\">\n        <I d={ICN.check} size={13} sw={2.5} />\n      </div>\n      <div class=\"qdone__body\">\n        <div class=\"qdone__title\">{p.client}</div>\n        <div class=\"qdone__sub\">\n          {methodLabel(lang, p.method)} · {p.whenLabel} · {p.invoiceRef}\n        </div>\n      </div>\n      <div class=\"qdone__amt\">{fmtMoney(p.amount)}</div>\n    </div>\n  );\n}\n", "sourceName": "LandedRow.tsx", "checklist": "# Capture checklist — LandedRow\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/payments` — rows sit in the `.qdone` two-up grid BELOW\n  the recent PaymentCards, inside the **Just-landed** track. They only appear\n  when there are payments older than 1 day that landed this month\n  (`olderLanded.length > 0`). Child of the PaymentsPage island.\n\n## Viewports (own `@media`, from quotes.css)\n- **1280px** — `.qdone` is a 2-column grid (rows pair up).\n- **1100px** — `.qdone` → 1 column (rows go full width). Shoot at ~1099px.\n- **560px** — `.qdone__row` padding/gap tighten. Shoot at ~559px.\n\n## Element(s) to crop\n- A single `.qdone__row` (won badge + client + `method · when · invoiceRef`\n  sub + amount).\n- Crop the **sub-line** specifically to evidence it is **UNSTYLED**\n  (`.qdone__body`/`.qdone__sub` have no CSS rule → it renders as plain inherited\n  body text, not the small muted treatment).\n\n## Transient states to drive\n1. **default** — a seed with a check/ACH payment >1d old this month: standard\n   row.\n2. **long-name** — a customer with a very long name → confirm the `.qdone__title`\n   ellipsis truncation.\n3. **es** — Spanish UI language: method + relative when label localize.\n\n## Interactions\n- None — the row is not interactive. (Only the hover state below.)\n\n## Motion to film\n- **Hover** — hover the row: `border-color` → `--mint-300` + `translateX(2px)`\n  slide over `--dur-fast var(--ease-out)`. Film one hover.\n- Re-shoot the hover with `prefers-reduced-motion: reduce` (global clamp —\n  verify the slide goes instant).\n\n## Notes (bugs to evidence)\n- The `method·when·invoiceRef` sub-line is **unstyled** — this is the headline\n  finding for this row; the crop should show it reading as ordinary body text\n  rather than the intended small muted `.qdone__client` line.\n- The badge is always the green `--won` variant (a LandedRow is always a settled\n  payment) — there is no lost/danger variant in use here.\n"}, {"name": "payment-card", "component": "PaymentCard", "spec": "# PaymentCard\n\nThe per-payment flip card (`.qcard`) on /payments. Front = mood gradient +\nstatus + method pill + avatar + client·invoiceRef + the dominant amount + a human\nnote + a CTA + a method/ETA value cell. Back = the \"Payment trail\" + the note +\nthree action buttons. **All actions are no-ops** — this page is read-only.\n\n## 1. Classification & behavior\n- **Bucket:** local component of the PaymentsPage island\n  (`islands/PaymentsPage.tsx` lines 656–782; `STATUS_MOOD` map lines 658–685).\n  Not its own island — renders inside the page island, sharing its hydration\n  boundary.\n- **Interaction tier:** **island-child, client-state only (flip).** No\n  form/PRG, no Fresh Partial, no fetch.\n- **Client state owned:** `flipped: boolean` — the only state. (Contrast the\n  sibling InvoiceCard, which owns busy/adjust/discount/CO state because it\n  mutates; PaymentCard owns none of that.)\n- **Server actions + flash:** **NONE.** Every button is a no-op:\n  | button | location | handler |\n  |---|---|---|\n  | `.qcard__cta` (e.g. \"View receipt\") | front foot | `onClick={e => e.stopPropagation()}` only |\n  | Receipt / Match invoice / Text client | back foot | `onClick={e => e.stopPropagation()}` only |\n  | `.qcard__back-close` (X) | back head | `setFlipped(false)` (state only) |\n  - No `fetch`, no `location.reload()`, no flash/toast. The card cannot change\n    any data. **This is itself an anti-pattern: the buttons read as actionable\n    but do nothing** (payments-page.md §8 flags this).\n- **Data source:** the single `p: EnrichedPayment` prop (parent derives\n  `client`, `initials`, `invoiceRef`, `daysAgo`, `whenLabel`, `note`, `status`,\n  `etaDays` from the raw `Payment` + joined invoice/customer).\n- **Liveness:** none.\n- **Data-shape hazards:**\n  - **`status` is invented client-side** (`deriveStatus` + `SETTLE_DAYS`): the\n    `Payment` DTO has no status. ACH<2d / check<5d ⇒ `\"transit\"`; otherwise\n    `\"landed\"`. `\"attention\"` is never produced. So in practice only the\n    `landed` (green) and `transit` (teal) moods ever render; `attention` (pink)\n    is a dead branch.\n  - **`invoiceRef` is cosmetic:** `INV-` + first 6 chars of `invoiceId`,\n    uppercased — not a real document number.\n  - The CTA label/cta + back depend on `status` via the `STATUS_MOOD` map +\n    `paymentsPage.mood.{status}.{label|cta}` i18n keys.\n  - The value cell flips meaning by status: `transit` → \"Expected\" + `~Nd` ETA;\n    else → \"Method\" + the method label.\n\n## 2. Anatomy\n```\n<article class=\"qcard [qcard--flipped]\"\n         style=\"--mood-from/-to/-shadow/-status\"\n         onClick={flip-unless-flipped-and-not-on-.qcard__cta/.qcard__back}>\n  <div class=\"qcard__mood\">\n    <div class=\"qcard__numeral\">{idx+1 padded}</div>\n    <div class=\"qcard__status\"><span class=\"qcard__status-dot\"/> {moodLabel}</div>\n    <div class=\"qcard__opens\" style=\"text-transform:uppercase\">\n       <I METHOD_ICON[method]/> {methodLabel}</div>      ← method pill (reuses __opens)\n  </div>\n  <div class=\"qcard__av\">{initials}</div>\n  <div class=\"qcard__body\">\n    <div class=\"qcard__client-name\">{client} · {invoiceRef}</div>\n    <div class=\"pcard__amount\">{fmtMoney(amount)}</div>   ← dominant amount line\n    <p class=\"qcard__story\">{note}</p>\n  </div>\n  <div class=\"qcard__foot\">\n    <button class=\"qcard__cta\">{moodCta} <span →/></button>   ← NO-OP\n    <div class=\"qcard__val-wrap\">\n      <div class=\"qcard__val-lbl\">{transit?\"Expected\":\"Method\"}</div>\n      <div class=\"qcard__val-num\">{transit&&etaDays ? \"~Nd\" : methodLabel}</div>\n    </div>\n  </div>\n  <div class=\"qcard__back\" aria-hidden={!flipped}>\n    <div class=\"qcard__back-head\"><button .qcard__back-close X/>\n      <div class=\"qcard__back-eyebrow\">Payment trail</div>\n      <p class=\"qcard__back-big\">{fmtMoney(amount)} <small>· {methodLabel}</small></p></div>\n    <div class=\"qcard__back-body\"><p class=\"qcard__read\">{note}</p></div>\n    <div class=\"qcard__back-foot\">              ← three NO-OP buttons\n      <button>Receipt</button><button>Match invoice</button><button>Text client</button>\n    </div>\n  </div>\n</article>\n```\n- Deps: `STATUS_MOOD`, `METHOD_ICON`, `methodLabel` (from PaymentsPage.tsx),\n  `fmtMoney`, `tFor`, `I`/`ICN.x`.\n- `--mood-*` CSS vars are set inline per status (drive the mood gradient +\n  hover shadow tint + status text color).\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` (required) | — | select | no |\n| `p` | `EnrichedPayment` (required) | — | object | no |\n| `idx` | number (required) | — | number | no (drives the `.qcard__numeral`) |\n\n`EnrichedPayment` = `Payment` + `client`, `initials`, `invoiceRef`, `daysAgo`,\n`whenLabel`, `note`, `status` (`landed`|`transit`|`attention`), `etaDays?`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| landed (front) | green mood, \"View receipt\", method value cell | `cases/landed/landed.json` |\n| transit (front) | teal mood, \"View timeline\", \"Expected ~Nd\" cell | `cases/transit/transit.json` |\n| flipped | back face — trail + note + three no-op buttons | `cases/flipped/flipped.json` |\n| attention (front) | pink mood, \"Text client\" — DEAD branch, kept for fidelity | `cases/attention/attention.json` |\n| es | Spanish mood/cta/note/value | `cases/es/es.json` |\n\n## 5. Events\n- `ev.expect(e => e.source===\"article.qcard\" && e.type===\"click\")` when **not**\n  flipped and the target is **not** inside `.qcard__cta, .qcard__back` →\n  `flipped=true`.\n- `ev.expect(e => e.source===\"button.qcard__back-close\" && e.type===\"click\")` →\n  `flipped=false`.\n- All other buttons: `ev.expect(... type===\"click\")` → `stopPropagation()` only\n  (no state, no network). Confirm by predicate that nothing else fires.\n\n## 6. Motion (extracted, from quotes.css)\n- **Flip:** `.qcard__back` `transform: translateY(8px) scale(.98) → translateY(0)\n  scale(1)` over `380ms var(--ease-bounce)` + `opacity 0→1` over\n  `240ms var(--ease-out)`; `pointer-events` gated on `.qcard--flipped`.\n- **Hover lift:** `.qcard:hover` `translateY(-4px)` + a bigger mood-tinted\n  shadow (`var(--mood-shadow)`), `320ms` bounce.\n- **Status dot:** `q-pulse-dot` opacity `1→0.4→1` over `2.4s` infinite (the only\n  always-on animation).\n- **CTA arrow:** the `→` span has an inline `transition:transform 240ms`;\n  `.qcard__cta:hover` widens the gap.\n  - **Jank finding:** transforms/opacity only → GPU-friendly, smooth. The\n    pulsing dot is cheap. No layout-thrash.\n- **Reduced motion:** no component-local guard — relies on the global tokens\n  clamp. Verify the flip becomes instant and the dot stops pulsing.\n\n## 7. Responsive\n- No own `@media`. Inherits the `.qcards` grid\n  (`auto-fill minmax(320px,1fr)`, →1 col `≤768px`). The card's internal layout\n  is fixed (138px mood block, flex body/foot).\n\n## 8. A11y\n- The whole `<article>` is the flip click target but is **not a `<button>`, not\n  focusable, has no `aria-expanded`** — keyboard users cannot flip it.\n  **Fix:** add a focusable flip affordance + `aria-expanded`/`aria-controls`.\n- `.qcard__back aria-hidden={!flipped}` correctly hides the back face from AT\n  while collapsed; the close-X has `aria-label` (`common.close`).\n- The no-op CTAs/buttons announce as actionable buttons but do nothing —\n  remove them or wire them.\n\n## 9. Used on\n`/payments` only (rendered by PaymentsPage inside the Just-landed /\nIn-transit / Needs-attention tracks). Reuses the Quotes `.qcard*` CSS verbatim\nplus `.pcard__amount` from `static/payments.css` — see `css/payment-card.css`.\n", "fixture": "{\n  \"category\": \"payments\",\n  \"folder\": \"payment-card\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"idx\": { \"type\": \"number\", \"value\": 0 },\n    \"p\": {\n      \"type\": \"object\",\n      \"value\": {\n        \"id\": \"pay_8a1f20c4\",\n        \"userId\": \"usr_01\",\n        \"invoiceId\": \"inv_3c91ab77e201\",\n        \"amount\": 482000,\n        \"method\": \"zelle\",\n        \"receivedAt\": \"2026-06-18T14:20:00Z\",\n        \"createdAt\": \"2026-06-18T14:21:00Z\",\n        \"updatedAt\": \"2026-06-18T14:21:00Z\",\n        \"client\": \"Maria Delgado\",\n        \"initials\": \"MD\",\n        \"invoiceRef\": \"INV-3C91AB\",\n        \"daysAgo\": 0,\n        \"whenLabel\": \"Today\",\n        \"note\": \"Payment from Maria.\",\n        \"status\": \"landed\"\n      }\n    }\n  },\n  \"_signals\": { \"flipped\": false },\n  \"_note\": \"p is an EnrichedPayment = the stored Payment (id/userId/invoiceId/amount[CENTS]/method/receivedAt/createdAt/updatedAt) PLUS the parent-derived client/initials/invoiceRef/daysAgo/whenLabel/note/status/etaDays?. amount 482000 → \\\"$4,820\\\". status drives the mood gradient + CTA label + value cell; `flipped` is the only client state.\"\n}\n", "cases": [{"name": "attention.json", "content": "{\n  \"_name\": \"Attention (front) — pink mood, \\\"Attention\\\" status, \\\"Text client\\\" CTA — DEAD branch the page never produces, kept for visual fidelity\",\n  \"lang\": \"en\",\n  \"idx\": 0,\n  \"p\": {\n    \"id\": \"pay_zz000001\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_decl00aabbcc\",\n    \"amount\": 215000,\n    \"method\": \"check\",\n    \"receivedAt\": \"2026-06-16T10:00:00Z\",\n    \"createdAt\": \"2026-06-16T10:00:00Z\",\n    \"updatedAt\": \"2026-06-16T10:00:00Z\",\n    \"client\": \"Lopez Property Mgmt\",\n    \"initials\": \"LP\",\n    \"invoiceRef\": \"INV-DECL00\",\n    \"daysAgo\": 2,\n    \"whenLabel\": \"2d ago\",\n    \"note\": \"Payment from Lopez.\",\n    \"status\": \"attention\"\n  },\n  \"_signals\": { \"flipped\": false },\n  \"_note\": \"DOCUMENTATION-ONLY case — `deriveStatus` never returns \\\"attention\\\" (no source signal on Payment), so this mood is unreachable in the live app. Forced here to show STATUS_MOOD.attention (#FFD9D9→#FF6B6B, white status text), mood.attention.label=\\\"Attention\\\", cta=\\\"Text client\\\". Do NOT treat as a real-data screenshot.\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — mood label/cta, note, method label, value cell localize via es.json\",\n  \"lang\": \"es\",\n  \"idx\": 0,\n  \"p\": {\n    \"id\": \"pay_8a1f20c4\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_3c91ab77e201\",\n    \"amount\": 482000,\n    \"method\": \"zelle\",\n    \"receivedAt\": \"2026-06-18T14:20:00Z\",\n    \"createdAt\": \"2026-06-18T14:21:00Z\",\n    \"updatedAt\": \"2026-06-18T14:21:00Z\",\n    \"client\": \"Maria Delgado\",\n    \"initials\": \"MD\",\n    \"invoiceRef\": \"INV-3C91AB\",\n    \"daysAgo\": 0,\n    \"whenLabel\": \"Hoy\",\n    \"note\": \"Pago de Maria.\",\n    \"status\": \"landed\"\n  },\n  \"_signals\": { \"flipped\": false },\n  \"_note\": \"lang=es → mood.landed.label/cta, card.method/expected/eta, methodLabel, and the parent-supplied `note`/`whenLabel` (already localized by the parent via tFor(lang,…)) all read ES. fmtMoney stays en-US (\\\"$4,820\\\").\"\n}\n"}, {"name": "flipped.json", "content": "{\n  \"_name\": \"Flipped — back face: \\\"Payment trail\\\" + big amount + note + three NO-OP buttons (Receipt / Match invoice / Text client)\",\n  \"lang\": \"en\",\n  \"idx\": 1,\n  \"p\": {\n    \"id\": \"pay_2bb1043f\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_aa19ff2c8e30\",\n    \"amount\": 920000,\n    \"method\": \"check\",\n    \"receivedAt\": \"2026-06-10T09:30:00Z\",\n    \"createdAt\": \"2026-06-10T09:31:00Z\",\n    \"updatedAt\": \"2026-06-10T09:31:00Z\",\n    \"client\": \"Tom Becker\",\n    \"initials\": \"TB\",\n    \"invoiceRef\": \"INV-AA19FF\",\n    \"daysAgo\": 8,\n    \"whenLabel\": \"8d ago\",\n    \"note\": \"Check from Tom, deposited via mobile.\",\n    \"status\": \"landed\"\n  },\n  \"_signals\": { \"flipped\": true },\n  \"_note\": \"flipped=true → .qcard--flipped: back face slides in (translateY/scale + opacity). back-eyebrow \\\"Payment trail\\\", back-big \\\"$9,200 · Check\\\", read = the note, back-foot = Receipt/Match invoice/Text client (all stopPropagation no-ops). Close-X sets flipped=false.\"\n}\n"}, {"name": "landed.json", "content": "{\n  \"_name\": \"Landed (front) — green mood, \\\"Landed\\\" status, \\\"View receipt\\\" CTA, Method value cell\",\n  \"lang\": \"en\",\n  \"idx\": 0,\n  \"p\": {\n    \"id\": \"pay_8a1f20c4\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_3c91ab77e201\",\n    \"amount\": 482000,\n    \"method\": \"zelle\",\n    \"receivedAt\": \"2026-06-18T14:20:00Z\",\n    \"createdAt\": \"2026-06-18T14:21:00Z\",\n    \"updatedAt\": \"2026-06-18T14:21:00Z\",\n    \"client\": \"Maria Delgado\",\n    \"initials\": \"MD\",\n    \"invoiceRef\": \"INV-3C91AB\",\n    \"daysAgo\": 0,\n    \"whenLabel\": \"Today\",\n    \"note\": \"Payment from Maria.\",\n    \"status\": \"landed\"\n  },\n  \"_signals\": { \"flipped\": false },\n  \"_note\": \"STATUS_MOOD.landed gradient (#CFE5C8→#5FA34F). mood.landed.label=\\\"Landed\\\", cta=\\\"View receipt\\\". Value cell: lbl \\\"Method\\\" / num \\\"Zelle\\\". Numeral \\\"01\\\" (idx+1). pcard__amount \\\"$4,820\\\".\"\n}\n"}, {"name": "transit.json", "content": "{\n  \"_name\": \"Transit (front) — teal mood, \\\"In transit\\\" status, \\\"View timeline\\\" CTA, \\\"Expected ~2d\\\" value cell\",\n  \"lang\": \"en\",\n  \"idx\": 0,\n  \"p\": {\n    \"id\": \"pay_ac77001e\",\n    \"userId\": \"usr_01\",\n    \"invoiceId\": \"inv_77b0c1aa4410\",\n    \"amount\": 310000,\n    \"method\": \"ach\",\n    \"receivedAt\": \"2026-06-17T08:00:00Z\",\n    \"createdAt\": \"2026-06-17T08:00:00Z\",\n    \"updatedAt\": \"2026-06-17T08:00:00Z\",\n    \"client\": \"Greenfield HOA\",\n    \"initials\": \"GH\",\n    \"invoiceRef\": \"INV-77B0C1\",\n    \"daysAgo\": 1,\n    \"whenLabel\": \"Yesterday\",\n    \"note\": \"Greenfield's ACH transfer is in flight. Standard 2-day settlement.\",\n    \"status\": \"transit\",\n    \"etaDays\": 1\n  },\n  \"_signals\": { \"flipped\": false },\n  \"_note\": \"ACH 1d ago (<2d window) → status transit, etaDays 1. STATUS_MOOD.transit gradient (#C8DDE0→#56969E). mood.transit.label=\\\"In transit\\\", cta=\\\"View timeline\\\". Value cell flips to lbl \\\"Expected\\\" / num \\\"~1d\\\" (paymentsPage.card.eta). note uses transitAch. amount 310000 → \\\"$3,100\\\".\"\n}\n"}], "css": "/* PaymentCard reuses the full `.qcard` flip-card block (static/quotes.css) —\n   the verbatim extraction lives in\n   ../../../../invoices/components/invoice-card/css/invoice-card.css\n   (.qcard / .qcard__mood / __numeral / __status / __status-dot / __opens /\n   __av / __body / __client-name / __story / __foot / __cta / __val-* / __back*\n   + @keyframes q-pulse-dot). Tokens from dashboard.css.\n\n   ADDITIONS specific to /payments (from static/payments.css, also in\n   ../../payments-hero/css/payments-hero.css):\n     .pcard__amount — the dominant amount line under the client name on the\n                      card front (Nunito 800, 22px, tabular-nums, teal).\n   .qcard__opens is reused as the method pill in the mood block (top-right). */\n\n.pcard__amount {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 22px;\n  letter-spacing: -0.018em;\n  color: var(--brand-teal);\n  font-variant-numeric: tabular-nums;\n  margin: 4px 0 6px;\n}\n", "cssName": "payment-card.css", "source": "/* Extracted verbatim from islands/PaymentsPage.tsx (STATUS_MOOD + PaymentCard).\n * A `.qcard` flip card for a single recorded payment. Front shows method\n * glyph, client·invoiceRef, amount, a human note, a no-op CTA, and a\n * method/ETA value cell. Back shows the trail + three no-op action buttons\n * (Receipt / Match invoice / Text client). The only client state is `flipped`;\n * NONE of the buttons mutate — see payment-card.md §1. Depends on\n * METHOD_ICON / methodLabel from PaymentsPage.tsx (lines 65–144). */\nimport { useState } from \"preact/hooks\";\nimport { I, ICN } from \"../../payments-page/js/dash-icons.tsx\";\nimport { fmtMoney } from \"../../payments-page/js/format.ts\";\nimport { type Lang, tFor } from \"../../../../../../lib/i18n.ts\";\n// NOTE: EnrichedPayment, PaymentStatus, PaymentMethod, METHOD_ICON and\n// methodLabel are defined in ../../payments-page/js/PaymentsPage.tsx\n// (lines 65–260). Imported here as types/values in the real island; reproduced\n// inline below only as the structural reference for this folder.\n\ntype PaymentStatus = \"landed\" | \"transit\" | \"attention\";\n\nconst STATUS_MOOD: Record<\n  PaymentStatus,\n  { from: string; to: string; shadow: string; statusFg: string }\n> = {\n  landed: {\n    from: \"#CFE5C8\",\n    to: \"#5FA34F\",\n    shadow: \"rgba(81,152,67,0.28)\",\n    statusFg: \"#1F3F18\",\n  },\n  transit: {\n    from: \"#C8DDE0\",\n    to: \"#56969E\",\n    shadow: \"rgba(86,150,158,0.28)\",\n    statusFg: \"#0F3036\",\n  },\n  attention: {\n    from: \"#FFD9D9\",\n    to: \"#FF6B6B\",\n    shadow: \"rgba(255,107,107,0.30)\",\n    statusFg: \"#fff\",\n  },\n};\n\n// deno-lint-ignore no-explicit-any\nexport function PaymentCard(\n  { lang, p, idx, METHOD_ICON, methodLabel }: {\n    lang: Lang;\n    p: any; // EnrichedPayment — see PaymentsPage.tsx line 148\n    idx: number;\n    // deno-lint-ignore no-explicit-any\n    METHOD_ICON: Record<string, any>;\n    methodLabel: (lang: Lang, m: string) => string;\n  },\n) {\n  const [flipped, setFlipped] = useState(false);\n  const mood = STATUS_MOOD[p.status as PaymentStatus];\n  const moodLabel = tFor(lang, `paymentsPage.mood.${p.status}.label`);\n  const moodCta = tFor(lang, `paymentsPage.mood.${p.status}.cta`);\n  return (\n    <article\n      class={`qcard ${flipped ? \"qcard--flipped\" : \"\"}`}\n      onClick={(e) => {\n        if (flipped) return;\n        const t = e.target as HTMLElement;\n        if (t.closest(\".qcard__cta, .qcard__back\")) return;\n        setFlipped(true);\n      }}\n      style={`--mood-from:${mood.from};--mood-to:${mood.to};--mood-shadow:${mood.shadow};--mood-status:${mood.statusFg}`}\n    >\n      <div class=\"qcard__mood\">\n        <div class=\"qcard__numeral\">{String(idx + 1).padStart(2, \"0\")}</div>\n        <div class=\"qcard__status\">\n          <span class=\"qcard__status-dot\" /> {moodLabel}\n        </div>\n        <div class=\"qcard__opens\" style=\"text-transform:uppercase\">\n          <I d={METHOD_ICON[p.method]} size={12} sw={2} />{\" \"}\n          {methodLabel(lang, p.method)}\n        </div>\n      </div>\n      <div class=\"qcard__av\">{p.initials}</div>\n      <div class=\"qcard__body\">\n        <div class=\"qcard__client-name\">{p.client} · {p.invoiceRef}</div>\n        <div class=\"pcard__amount\">{fmtMoney(p.amount)}</div>\n        <p class=\"qcard__story\">{p.note}</p>\n      </div>\n      <div class=\"qcard__foot\">\n        <button\n          type=\"button\"\n          class=\"qcard__cta\"\n          onClick={(e) => e.stopPropagation()}\n        >\n          {moodCta}{\" \"}\n          <span style=\"display:inline-block;transition:transform 240ms\">→</span>\n        </button>\n        <div class=\"qcard__val-wrap\">\n          <div class=\"qcard__val-lbl\">\n            {p.status === \"transit\"\n              ? tFor(lang, \"paymentsPage.card.expected\")\n              : tFor(lang, \"paymentsPage.card.method\")}\n          </div>\n          <div class=\"qcard__val-num\" style=\"font-size:13px\">\n            {p.status === \"transit\" && p.etaDays\n              ? tFor(lang, \"paymentsPage.card.eta\", { n: p.etaDays })\n              : methodLabel(lang, p.method)}\n          </div>\n        </div>\n      </div>\n\n      <div class=\"qcard__back\" aria-hidden={!flipped}>\n        <div class=\"qcard__back-head\">\n          <button\n            type=\"button\"\n            class=\"qcard__back-close\"\n            onClick={(e) => {\n              e.stopPropagation();\n              setFlipped(false);\n            }}\n            aria-label={tFor(lang, \"common.close\")}\n          >\n            <I d={ICN.x} size={14} sw={2.5} />\n          </button>\n          <div class=\"qcard__back-eyebrow\">\n            {tFor(lang, \"paymentsPage.card.trail\")}\n          </div>\n          <p class=\"qcard__back-big\">\n            {fmtMoney(p.amount)}\n            <small>· {methodLabel(lang, p.method)}</small>\n          </p>\n        </div>\n        <div class=\"qcard__back-body\">\n          <p class=\"qcard__read\">{p.note}</p>\n        </div>\n        <div class=\"qcard__back-foot\">\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"paymentsPage.card.receipt\")}\n          </button>\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"paymentsPage.card.matchInvoice\")}\n          </button>\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"paymentsPage.card.textClient\")}\n          </button>\n        </div>\n      </div>\n    </article>\n  );\n}\n", "sourceName": "PaymentCard.tsx", "checklist": "# Capture checklist — PaymentCard\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/payments` — cards live inside the Just-landed (recent,\n  ≤1d) and In-transit tracks as `.qcards` → `.qcard`. Child of the PaymentsPage\n  island; you can't address one standalone.\n\n## Viewports\n- **1280px** — cards in a multi-column `.qcards` grid (`minmax(320px,1fr)`).\n- **768px** — `.qcards` →1 col (full-width cards). Shoot at ~767px to confirm a\n  single card spans the column.\n\n## Element(s) to crop\n- A **single `.qcard`** front face (mood block + numeral + status + method pill,\n  avatar, client·invoiceRef, the dominant `.pcard__amount`, note, CTA + value\n  cell).\n- The same card **flipped** (back face: trail + big amount + note + the three\n  action buttons).\n\n## Transient states to drive\n1. **landed** — a recent (≤1d) cash/zelle/card payment: green mood,\n   \"Landed\" status, \"View receipt\" CTA, \"Method\" value cell.\n2. **transit** — an ACH(<2d)/check(<5d) payment (in the In-transit track, expand\n   it first): teal mood, \"In transit\", \"View timeline\" CTA, \"Expected ~Nd\" cell.\n3. **flipped** — click the card body → the back face flips in. Click the back-X\n   to flip back.\n4. **attention** — **cannot be produced from real data** (the attention status\n   is never derived). Only drivable in isolate via the `attention` case; do not\n   stage a fake live screenshot — note it's a dead mood.\n5. **es** — Spanish UI language: mood label/CTA, note, method, value cell\n   localize.\n\n## Interactions to exercise\n- Click the card body (not on the CTA / not on the back) → flips to back.\n- Click the front `.qcard__cta`, and each back-foot button → confirm **nothing\n  happens** (no network, no reload) — they `stopPropagation` only. This is the\n  no-op bug to evidence.\n\n## Motion to film\n- **Flip** — `.qcard__back` slides + scales in (`380ms var(--ease-bounce)`) +\n  fades (`240ms ease-out`). Film one flip both directions.\n- **Hover lift** — `.qcard:hover` rises 4px with a mood-tinted shadow\n  (`320ms` bounce).\n- **Status dot pulse** — `q-pulse-dot` opacity 1→.4→1 over 2.4s (always on).\n- Re-shoot a flip with `prefers-reduced-motion: reduce` (global clamp — verify\n  flip is instant + dot stops).\n\n## Notes\n- The card is not keyboard-focusable (flip is mouse-only) — note this when\n  documenting; do not \"fix\" it for the screenshot.\n"}, {"name": "payment-side-rail", "component": "PaymentSideRail", "spec": "# PaymentSideRail\n\nThe sticky `aside.qside` rail on /payments — four stacked `.qside__card`\nwidgets: **PSideFlow** (12-week cash-flow sparkline) · **PSideTopPayors**\n(top-4 payors bars) · **PSideMix** (method-mix stacked bar + legend) · **PSideTip**\n(static teal \"Monster tip\" card).\n\n## 1. Classification & behavior\n- **Bucket:** four local presentational components of the PaymentsPage island\n  (`islands/PaymentsPage.tsx`: PSideFlow 805–881, PSideTopPayors 883–927,\n  PSideMix 929–1005, PSideTip 1007–1028). Rendered inside `aside.qside`.\n- **Interaction tier:** **static / island-child.** No client state, no fetch,\n  no events. All four are pure functions of props.\n- **Server actions + flash:** none. No buttons, no links, no mutations.\n- **Data source per widget (all derived in-component from props):**\n  - PSideFlow ← `landedAmounts: number[]` (the parent's `landed.map(p=>p.amount)`).\n  - PSideTopPayors / PSideMix ← `landed: EnrichedPayment[]`.\n  - PSideTip ← `lang` only (fully static copy).\n  - **Honest-empty:** each renders its own empty copy — PSideFlow `flow.empty`\n    (no weeks > 0), PSideTopPayors `payors.empty` (no entries), PSideMix\n    `mix.empty` (no segments). PSideTip always renders.\n- **Liveness:** none.\n- **Data-shape hazards (the flagged concern for this rail):**\n  - **All four recompute over the full `landed` array on EVERY render** — a\n    per-render rollup over payments. On a real backend these mirror\n    `DashboardStats.payments.{methodMixCents, topPayors}` +\n    `revenue.sparkline12mo` (whole-account Σ scans). **[denormalize / precompute;\n    don't scan on each render].**\n  - **PSideFlow buckets by ARRAY INDEX, not by `receivedAt`:**\n    `weeks[11 - (i % 12)] += a`. The \"12-week trend\" is **fabricated** — the\n    x-axis has nothing to do with time, and the labels (Feb/Mar/Apr) are\n    **hardcoded**, not derived from the data's dates. A real trend must bucket by\n    `receivedAt` week.\n  - **PSideTopPayors tallies by client display-name string** (`p.client`) — two\n    customers with the same name collide into one bar; an unlinked payment\n    (`client === \"—\"`) becomes a \"—\" payor.\n  - **PSideMix tallies by method** across the 9-method enum; segments with\n    `pct===0` are filtered out.\n- **Anti-patterns / CSS HAZARD (FLAG — real visible bug):**\n  - **PSideTopPayors markup references `.qside__rows / .qside__row /\n    .qside__rank / .qside__row-body / .qside__row-name / .qside__bar /\n    .qside__bar-fill / .qside__row-amt` — NONE of which are defined in any\n    `static/*.css`.** The widget renders as default block flow; only the inline\n    `width`/`background` on `.qside__bar-fill` lands, but with no track and no\n    explicit height it is effectively invisible. QuotesPage's equivalent rail\n    uses the **styled** `.qbig*`/`.qbar*` family. **Fix:** add the missing\n    `.qside__*` rules OR switch to `.qbig*`/`.qbar*` (reproduced in\n    `css/payment-side-rail.css`). PSideFlow / PSideMix / PSideTip are\n    inline-styled and render correctly.\n\n## 2. Anatomy\n```\n<aside class=\"qside\">                          ← sticky, top:16px (static ≤1200px)\n\n  <div class=\"qside__card\">                    ← PSideFlow\n    <div class=\"qside__title\">Cash-flow shape</div>\n    <div class=\"qside__sub\">Last 12 weeks · {amount} this week | nothing yet</div>\n    {hasData ? <svg> area+line+dots (inline, 220×60) </svg>\n             : flow.empty text}\n    <div inline-flex>Feb · Mar · Apr</div>      ← HARDCODED labels\n  </div>\n\n  <div class=\"qside__card\">                     ← PSideTopPayors (UNSTYLED inner)\n    <div class=\"qside__title\">Top payors this month</div>\n    <div class=\"qside__sub\">Who actually showed up with money</div>\n    {top.length ? <div class=\"qside__rows\">{ .qside__row × ≤4:\n        .qside__rank | .qside__row-body(.qside__row-name + .qside__bar>.qside__bar-fill) | .qside__row-amt }</div>\n                : payors.empty text}\n  </div>\n\n  <div class=\"qside__card\">                      ← PSideMix\n    <div class=\"qside__title\">How they paid</div>\n    <div class=\"qside__sub\">Method mix this month</div>\n    {segments.length ? <stacked bar (inline)> + <legend grid 1fr 1fr (inline)>\n                     : mix.empty text}\n  </div>\n\n  <div class=\"qside__card\" style=\"teal gradient, white text\">   ← PSideTip (static)\n    <div class=\"qside__title\">Monster tip</div>\n    <p>Zelle and Cash App … up to 5 days … deposits …</p>\n  </div>\n</aside>\n```\n- Deps: `fmtMoney`, `methodLabel`, `tFor`. PSideFlow builds an inline SVG with a\n  `linearGradient id=\"cfArea\"`. No shared icons.\n\n## 3. Props (per sub-component)\n| component | name | type | default | widget | signal? |\n|---|---|---|---|---|---|\n| PSideFlow | `lang` | `Lang` | — | select | no |\n| PSideFlow | `landedAmounts` | `number[]` (CENTS) | — | number[] | no |\n| PSideTopPayors | `lang` | `Lang` | — | select | no |\n| PSideTopPayors | `landed` | `EnrichedPayment[]` | — | object[] | no |\n| PSideMix | `lang` | `Lang` | — | select | no |\n| PSideMix | `landed` | `EnrichedPayment[]` | — | object[] | no |\n| PSideTip | `lang` | `Lang` | — | select | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| populated | all four widgets with data (sparkline, bars, mix, tip) | `cases/populated/populated.json` |\n| empty | fresh account — Flow/Payors/Mix show empty copy; Tip still renders | `cases/empty/empty.json` |\n| single-method | mix is one segment at 100% | `cases/single-method/single-method.json` |\n| es | Spanish titles/subs/empty/tip | `cases/es/es.json` |\n\n## 5. Events\n- None. The rail is non-interactive (no clicks, no nav).\n\n## 6. Motion (extracted)\n- **None component-local.** The cards are static. (No sparkline draw-in, no bar\n  grow — the `.qbar__fill width 1.2s` transition only exists on the *styled\n  alternative* classes, which the current markup doesn't use.)\n- **Sticky:** `.qside { position: sticky; top:16px }` — scroll-follow, not an\n  animation.\n- **Reduced motion:** no-op (nothing animates).\n\n## 7. Responsive (from quotes.css)\n- `≤1200px`: the rail goes `position:static` (drops below the main column,\n  stops sticking) — this is set on `.qside` in the page layout media query.\n- The `.qside__card` width follows the `.qlay` rail track (320px desktop → full\n  width once stacked).\n\n## 8. A11y\n- The inline SVG sparkline has no `role`/`aria-label`/`<title>` — it's an\n  unlabeled graphic. **Fix:** add `role=\"img\"` + an `aria-label` summarizing the\n  trend, or a visually-hidden text equivalent.\n- PSideMix conveys method shares with color swatches + text labels (not\n  color-only) — OK, but the swatch `<span>`s are decorative with no label.\n- PSideTopPayors, being unstyled, still exposes its text (rank/name/amount) to\n  AT in source order — readable but visually broken.\n\n## 9. Used on\n`/payments` only (the `aside.qside` rail in PaymentsPage). CSS =\n`css/payment-side-rail.css` (the `.qside` shell from `static/quotes.css` + the\nflagged-undefined `.qside__*` inner classes + the `.qbig*`/`.qbar*` styled\nalternative).\n", "fixture": "{\n  \"category\": \"payments\",\n  \"folder\": \"payment-side-rail\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"landedAmounts\": { \"type\": \"object\", \"value\": [482000, 125000, 920000, 64000] },\n    \"landed\": {\n      \"type\": \"object\",\n      \"value\": [\n        { \"id\": \"pay_8a1f20c4\", \"client\": \"Maria Delgado\", \"method\": \"zelle\", \"amount\": 482000 },\n        { \"id\": \"pay_5d77e9b2\", \"client\": \"Greenfield HOA\", \"method\": \"cash\", \"amount\": 125000 },\n        { \"id\": \"pay_2bb1043f\", \"client\": \"Tom Becker\", \"method\": \"check\", \"amount\": 920000 },\n        { \"id\": \"pay_9f3c7d10\", \"client\": \"Lopez Property Mgmt\", \"method\": \"card\", \"amount\": 64000 }\n      ]\n    }\n  },\n  \"_note\": \"Renders all four widgets. landedAmounts (CENTS) feeds PSideFlow (bucketed by index, NOT receivedAt). `landed` (EnrichedPayment subset: client/method/amount) feeds PSideTopPayors (tally by client) + PSideMix (tally by method). PSideTip reads lang only. Sum 1591000 → \\\"$15,910\\\". HAZARD: PSideTopPayors inner classes are undefined in CSS → renders unstyled.\"\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty — fresh account: Flow/Payors/Mix show empty copy; Tip still renders\",\n  \"lang\": \"en\",\n  \"landedAmounts\": [],\n  \"landed\": [],\n  \"_note\": \"PSideFlow: no weeks>0 → flow.subEmpty + flow.empty text (no SVG). PSideTopPayors: top.length 0 → payors.empty text. PSideMix: no segments → mix.empty text. PSideTip: always renders (static).\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — all four widget titles/subs/empty/tip localize via es.json\",\n  \"lang\": \"es\",\n  \"landedAmounts\": [482000, 125000, 920000, 64000],\n  \"landed\": [\n    { \"id\": \"pay_8a1f20c4\", \"client\": \"Maria Delgado\", \"method\": \"zelle\", \"amount\": 482000 },\n    { \"id\": \"pay_5d77e9b2\", \"client\": \"Greenfield HOA\", \"method\": \"cash\", \"amount\": 125000 },\n    { \"id\": \"pay_2bb1043f\", \"client\": \"Tom Becker\", \"method\": \"check\", \"amount\": 920000 },\n    { \"id\": \"pay_9f3c7d10\", \"client\": \"Lopez Property Mgmt\", \"method\": \"card\", \"amount\": 64000 }\n  ],\n  \"_note\": \"flow.title/subThisWeek, payors.title/sub, mix.title/sub, tip.* + methodLabel all resolve ES via es.json. The hardcoded Feb/Mar/Apr labels also pass through flow.feb/mar/apr keys (es may localize). fmtMoney stays en-US.\"\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — sparkline (this week $X), 4 payor bars (UNSTYLED), method-mix bar+legend, tip\",\n  \"lang\": \"en\",\n  \"landedAmounts\": [482000, 125000, 920000, 64000],\n  \"landed\": [\n    { \"id\": \"pay_8a1f20c4\", \"client\": \"Maria Delgado\", \"method\": \"zelle\", \"amount\": 482000 },\n    { \"id\": \"pay_5d77e9b2\", \"client\": \"Greenfield HOA\", \"method\": \"cash\", \"amount\": 125000 },\n    { \"id\": \"pay_2bb1043f\", \"client\": \"Tom Becker\", \"method\": \"check\", \"amount\": 920000 },\n    { \"id\": \"pay_9f3c7d10\", \"client\": \"Lopez Property Mgmt\", \"method\": \"card\", \"amount\": 64000 }\n  ],\n  \"_note\": \"PSideFlow: weeks bucketed by index → \\\"this week\\\" = weeks[11] (the i=0 amount, 482000). PSideTopPayors: Tom Becker top ($9,200) → bars scale to him; renders UNSTYLED (undefined .qside__* classes). PSideMix: 4 methods (zelle/cash/check/card) → stacked bar + 2-col legend. PSideTip: static teal card.\"\n}\n"}, {"name": "single-method.json", "content": "{\n  \"_name\": \"Single-method — PSideMix renders one segment at 100% (full-width bar, one legend row)\",\n  \"lang\": \"en\",\n  \"landedAmounts\": [482000, 310000],\n  \"landed\": [\n    { \"id\": \"pay_8a1f20c4\", \"client\": \"Maria Delgado\", \"method\": \"zelle\", \"amount\": 482000 },\n    { \"id\": \"pay_z2229911\", \"client\": \"Maria Delgado\", \"method\": \"zelle\", \"amount\": 310000 }\n  ],\n  \"_note\": \"Both payments are Zelle → PSideMix has a single segment at 100% (zelle color #8E5BD6), legend shows one \\\"Zelle 100%\\\" row. PSideTopPayors collides both into one \\\"Maria Delgado\\\" bar ($7,920) — documents the by-name tally hazard. PSideFlow shows a 2-point shape.\"\n}\n"}], "css": "/* ============================================================================\n   PaymentSideRail (PSideFlow / PSideTopPayors / PSideMix / PSideTip) — the\n   `.qside` widget shell. Copied from static/quotes.css (lines 780–809).\n   Tokens from dashboard.css.\n\n   IMPORTANT — much of this rail is UNSTYLED. The four widgets only have CSS\n   for the *card shell* (.qside__card / .qside__title / .qside__sub). Every\n   inner structure is either inline-styled in the island OR uses an UNDEFINED\n   class:\n\n   • PSideFlow   — inline-styled (SVG sparkline + flex labels). OK.\n   • PSideMix    — inline-styled (stacked bar + legend grid). OK.\n   • PSideTip    — inline-styled (teal gradient card). OK.\n   • PSideTopPayors — uses `.qside__rows .qside__row .qside__rank\n       .qside__row-body .qside__row-name .qside__bar .qside__bar-fill\n       .qside__row-amt`, NONE of which exist in any static/*.css. The list\n       renders as default block flow; only the inline `width`/`background` on\n       .qside__bar-fill produces a visible bar (with no track behind it and no\n       height — so it is effectively invisible). QuotesPage's equivalent uses\n       the STYLED .qbig*/.qbar* family instead.\n\n   REBUILD FIX: either (a) add the missing .qside__* rules, or (b) switch\n   PSideTopPayors to the existing .qbig*/.qbar* classes (defined in quotes.css\n   lines 810–865, reproduced at the bottom for convenience).\n   ============================================================================ */\n\n.qside {\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n  position: sticky;\n  top: 16px;\n}\n.qside__card {\n  background: #fff;\n  border-radius: var(--radius-xl);\n  padding: 18px;\n  border: 1px solid rgba(26, 83, 92, 0.07);\n}\n.qside__head {\n  display: flex;\n  align-items: baseline;\n  justify-content: space-between;\n  margin-bottom: 14px;\n}\n.qside__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n}\n.qside__sub {\n  font-size: 11px;\n  color: var(--fg-muted);\n}\n\n/* === UNDEFINED IN SOURCE — markup references these but no rule exists ===\n   .qside__rows  .qside__row  .qside__rank  .qside__row-body\n   .qside__row-name  .qside__bar  .qside__bar-fill  .qside__row-amt           */\n\n/* === The STYLED equivalents QuotesPage uses (rebuild target for the fix) === */\n.qbig { display: flex; flex-direction: column; gap: 10px; }\n.qbig__row {\n  display: grid;\n  grid-template-columns: auto 1fr auto;\n  gap: 10px;\n  align-items: center;\n}\n.qbig__rank {\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 800;\n  color: var(--brand-pink);\n  width: 18px;\n  letter-spacing: 0.05em;\n}\n.qbig__name {\n  font-family: var(--font-heading);\n  font-size: 13px;\n  font-weight: 700;\n  color: var(--brand-teal);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qbig__amt {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13.5px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n}\n.qbar {\n  height: 6px;\n  border-radius: 999px;\n  background: var(--mint-200);\n  overflow: hidden;\n  margin-top: 8px;\n}\n.qbar__fill {\n  height: 100%;\n  background: linear-gradient(90deg, var(--brand-green), var(--green-700));\n  border-radius: 999px;\n  transition: width 1.2s var(--ease-out);\n}\n", "cssName": "payment-side-rail.css", "source": "/* SOURCE POINTER — the four `aside.qside` widgets live verbatim in:\n *   ../../payments-page/js/PaymentsPage.tsx\n *     · PSideFlow       lines 805–881  (12-week cash-flow sparkline, SVG)\n *     · PSideTopPayors  lines 883–927  (top-4 payors bar list)\n *     · PSideMix        lines 929–1005 (method-mix stacked bar + legend)\n *     · PSideTip        lines 1007–1028(static \"Monster tip\" teal card)\n *\n * DATA-SHAPE HAZARD (flagged): all four are recomputed client-side over the\n * full `landed` array on every render — a per-render rollup over jobs/payments\n * (PSideTopPayors tallies by client, PSideMix by method, PSideFlow buckets\n * amounts into 12 fake weeks by index, NOT by receivedAt). See\n * payment-side-rail.md §1.\n *\n * CSS HAZARD (flagged): PSideTopPayors uses `.qside__rows/.qside__row/\n * .qside__rank/.qside__row-body/.qside__row-name/.qside__bar/.qside__bar-fill/\n * .qside__row-amt` — NONE of these are defined in any static/*.css. They\n * render UNSTYLED (the inline width/background on the bar-fill is the only\n * styling that lands). QuotesPage's equivalent rail uses the *styled*\n * `.qbig*`/`.qbar*` classes instead. See payment-side-rail.md §1/§6. */\nexport {};\n", "sourceName": "PaymentSideRail.tsx", "checklist": "# Capture checklist — PaymentSideRail\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/payments` — the `aside.qside` rail on the right of the\n  `.qlay` grid (desktop). Child of the PaymentsPage island; its four widgets\n  follow the seeded `landed` payments.\n\n## Viewports (page-layout `@media`)\n- **1280px** — rail is **sticky** (`top:16px`) beside the tracks. Scroll the\n  page to confirm it follows.\n- **1200px** — `.qside` goes `position:static` and the rail drops **below** the\n  main column (full width). Shoot at ~1199px.\n\n## Element(s) to crop\n- Each of the four `.qside__card`s individually:\n  1. **PSideFlow** — the inline SVG sparkline (area + line + dots) + the\n     hardcoded Feb/Mar/Apr axis labels.\n  2. **PSideTopPayors** — crop to **evidence the UNSTYLED bug**: undefined\n     `.qside__*` classes mean no bar track / no rank chip styling; compare to a\n     styled `.qbig*`/`.qbar*` rail elsewhere.\n  3. **PSideMix** — the stacked method-mix bar + the 2-column legend.\n  4. **PSideTip** — the teal-gradient \"Monster tip\" card (white text).\n\n## Transient states to drive\n1. **populated** — seed with several landed payments across methods: all four\n   widgets render with data.\n2. **empty** — wiped account: Flow/Payors/Mix each show their empty copy; Tip\n   still renders.\n3. **single-method** — a seed where every landed payment is one method: PSideMix\n   shows a single 100% segment + one legend row.\n4. **es** — Spanish UI language: all titles/subs/empty/tip localize.\n\n## Motion to film\n- **None** — the rail is static (no draw-in, no bar grow). The only \"motion\" is\n  the sticky scroll-follow at ≥1200px; capture that as a scroll, not an\n  animation. No reduced-motion concern.\n\n## Notes (bugs to evidence, do not fix for the shot)\n- **PSideTopPayors renders unstyled** — this is the headline finding for this\n  component; the crop should clearly show the broken bars.\n- **PSideFlow's trend is fabricated** — bucketed by array index, not by\n  `receivedAt`, and the Feb/Mar/Apr labels are hardcoded. The shape is decorative,\n  not a real time series.\n- The sparkline SVG is unlabeled (no `role=\"img\"`/`aria-label`).\n"}, {"name": "payments-hero", "component": "PaymentsHero", "spec": "# PaymentsHero\n\nThe `.pph` editorial page header for /payments — a \"money landed\" treatment.\nLeft = pink eyebrow + a big amount headline (`$X showed up this month.`) or a\n`fresh` empty variant + a sub line + two assistant-seeded CTAs. Right =\n`.pph__stack`, up to three rotated `.pph__stub` recent-payment cards\n(decorative, `aria-hidden`).\n\n## 1. Classification & behavior\n- **Bucket:** local presentational component of the PaymentsPage island\n  (`islands/PaymentsPage.tsx` lines 463–589). Not its own island — renders\n  inside the page island, sharing its hydration boundary.\n- **Interaction tier:** **static / island-child.** No client state, no fetch,\n  no callbacks. Pure props in, markup out.\n- **Server actions + flash:** none. Both CTAs are plain `<a href>` navigations\n  into the assistant — `recordCta` → `/assistant?seed=<recordSeed>`,\n  `exportCta` → `/assistant?seed=<exportSeed>`. They do NOT record/export here\n  (consistent with the page being read-only). No PRG, no toast.\n- **Data source:** all props, computed by the parent island from\n  `paymentsClient.list()` + the joined invoices/customers. **Honest-empty:** the\n  `fresh` branch (when `monthTotal===0 && transitTotal===0 && attentionCount===0`)\n  renders a friendly \"Nothing's landed yet — *let's change that*.\" headline + the\n  freshSub explainer instead of a `$0` number.\n- **Liveness:** none (request-response via parent).\n- **Data-shape hazards:**\n  - `monthTotal`/`transitTotal` are **per-render client-side Σ rollups** over the\n    landed/transit arrays in the parent (mirror of\n    `DashboardStats.payments.receivedYtdCents` / a settling subset). **[precompute\n    / denormalize].**\n  - `attentionCount` is **always 0** in practice — the `attention` status is\n    never produced (the `Payment` DTO carries no status signal), so the\n    `subUnstick`/`subAnd` clause never appears. Documented dead branch.\n  - The sub-line concatenation is order-dependent: `subPlus … subOnTheWay`\n    (transit) → `subAnd … subUnstick` (attention) → `subMonsters` tail; when\n    transit is 0 it collapses to a single `everyDollar` clause.\n- **Anti-patterns:** the stub stack is decorative (`aria-hidden=\"true\"`) — good.\n  The two CTAs reading as actions (\"Record a payment\", \"Export this month\") but\n  actually punting to the assistant is a mild expectation mismatch, inherited\n  from the page's read-only stance.\n\n## 2. Anatomy\n```\n<header class=\"pph\">\n  <div class=\"pph__main\">\n    <div class=\"pph__eyebrow\"><I check/> Payments · {Month}</div>\n    <h1 class=\"pph__title\" [style fresh→smaller clamp]>\n      {fresh ? \"Nothing's landed yet — <em>let's change that</em>.\"\n             : <><span class=\"pph__title-amount\"><sup>$</sup>{N}</span>\n                  <span class=\"pph__title-tail\">showed up this month.</span></>}\n    </h1>\n    <p class=\"pph__sub\">\n      {fresh ? freshSub\n             : (transit>0 ? \"Plus <strong>$X</strong> on the way\" : \"Every dollar logged.\")\n               + (attention>0 ? \" and <strong>N</strong> that need a quick text…\" : \"\")\n               + (transit>0 ? \". The monsters logged every dollar.\" : \"\")}\n    </p>\n    <div class=\"pph__cta-row\">\n      <a class=\"pph__cta\" href=\"/assistant?seed=…\"><I plus/> Record a payment</a>\n      <a class=\"pph__ghost\" href=\"/assistant?seed=…\"><I arrow/> Export this month</a>\n    </div>\n  </div>\n  <div class=\"pph__stack\" aria-hidden=\"true\">\n    {stubs.map((p,i) => <div class=\"pph__stub pph__stub--{i+1}\">\n       <div class=\"pph__stub-head\">\n         <div class=\"pph__stub-av\" style=\"background:{METHOD_AV_BG[method]}\">{initials}</div>\n         <div class=\"pph__stub-meta\"><div class=\"pph__stub-client\">{client}</div>\n           <div class=\"pph__stub-when\">{whenLabel} · {invoiceRef}</div></div>\n       </div>\n       <div class=\"pph__stub-amount\">{fmtMoney(amount)}</div>\n       <div class=\"pph__stub-foot\">\n         <span class=\"pph__stub-method\"><I METHOD_ICON[method]/> {methodLabel}</span>\n         <span class=\"pph__stub-tag\">Landed</span>\n       </div>\n    </div>)}\n  </div>\n</header>\n```\n- Local deps (live in PaymentsPage.tsx): `METHOD_AV_BG` (per-method avatar\n  gradient, lines 80–90), `METHOD_ICON`/`P2PIcon` (lines 92–144), `methodLabel`\n  (lines 77–78), `fmtMoney`, `tFor`, `I`/`ICN` (`check`/`plus`/`arrow`).\n- **Icon dependency:** `I` + `ICN.check|plus|arrow`; per-method `METHOD_ICON`.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` (required) | — | select | no (parent passes `langSignal.value`) |\n| `monthTotal` | number (CENTS, required) | — | number | no |\n| `transitTotal` | number (CENTS, required) | — | number | no |\n| `attentionCount` | number (required) | — | number | no |\n| `stubs` | `EnrichedPayment[]` (required, sliced to 3) | — | object[] | no |\n\n`stubs` items use: `id`, `method`, `initials`, `client`, `whenLabel`,\n`invoiceRef`, `amount` (the parent's `recentLanded.slice(0,3)`).\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| populated | `$X showed up this month.` + 3 stubs + \"Plus $Y on the way\" | `cases/populated/populated.json` |\n| fresh | all totals 0 → empty headline variant, no stubs | `cases/fresh/fresh.json` |\n| no-transit | landed only, `transitTotal=0` → sub = \"Every dollar logged.\" | `cases/no-transit/no-transit.json` |\n| es | Spanish UI language | `cases/es/es.json` |\n\n## 5. Events\n- No JS handlers. Both CTAs are `<a>` navigations (predicate-wise:\n  `ev.expect(e => e.source===\"a.pph__cta\" && e.type===\"click\")` = a navigation\n  to `/assistant?seed=…`, not a handler).\n\n## 6. Motion (extracted)\n- **CTA hover:** `.pph__cta` `transform: translateY(-1px)` over\n  `160ms var(--ease-bounce)`; `.pph__ghost:hover` background fade `160ms`.\n- **Stub stack hover:** `.pph__stack:hover .pph__stub--1/2/3` each translate up\n  ~1–3px (keeping their individual rotations) over `280ms var(--ease-bounce)`\n  (the `.pph__stub` `transition: transform 280ms var(--ease-bounce), box-shadow\n  240ms ease`). The three stubs are absolutely positioned and rotated\n  (-4.2°/2.6°/-1.3°) at top 0/70/140px.\n  - **Jank finding:** transforms-only, GPU-friendly — smooth. The stack has\n    `overflow:visible` so the rotated cards aren't clipped.\n- **Reduced motion:** no component-local guard — relies on the global tokens\n  reduced-motion clamp. Verify hover lift/CTA nudge go instant.\n\n## 7. Responsive (own `@media`, from static/payments.css)\n- `≤760px`: `.pph` → 1 column (stack drops below the copy), padding tightens;\n  `.pph__stack` grows to 320px tall and the stubs re-fan **vertically**\n  (left-aligned, rotations softened to -2.6°/1.6°/-0.8° at top 0/90/180px) so\n  each is clearly visible on a narrow screen.\n\n## 8. A11y\n- `.pph__stack` is `aria-hidden=\"true\"` — correct (purely decorative).\n- The eyebrow check glyph + CTA glyphs are inline SVGs with no labels, but the\n  adjacent text carries meaning — acceptable.\n- Headline uses a real `<h1>` (good page-heading semantics). The `<em>` in the\n  fresh variant is styled non-italic (`font-style:normal`) but still conveys\n  emphasis to AT.\n- CTAs are real `<a>` — keyboard-operable.\n\n## 9. Used on\n`/payments` only (rendered by PaymentsPage). CSS = `css/payments-hero.css`\n(extracted verbatim from `static/payments.css`).\n", "fixture": "{\n  \"category\": \"payments\",\n  \"folder\": \"payments-hero\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"monthTotal\": { \"type\": \"number\", \"value\": 1591000 },\n    \"transitTotal\": { \"type\": \"number\", \"value\": 525000 },\n    \"attentionCount\": { \"type\": \"number\", \"value\": 0 },\n    \"stubs\": {\n      \"type\": \"object\",\n      \"value\": [\n        { \"id\": \"pay_8a1f20c4\", \"method\": \"zelle\", \"initials\": \"MD\", \"client\": \"Maria Delgado\", \"whenLabel\": \"Today\", \"invoiceRef\": \"INV-3C91AB\", \"amount\": 482000 },\n        { \"id\": \"pay_5d77e9b2\", \"method\": \"cash\", \"initials\": \"GH\", \"client\": \"Greenfield HOA\", \"whenLabel\": \"Yesterday\", \"invoiceRef\": \"INV-77B0C1\", \"amount\": 125000 },\n        { \"id\": \"pay_2bb1043f\", \"method\": \"check\", \"initials\": \"TB\", \"client\": \"Tom Becker\", \"whenLabel\": \"8d ago\", \"invoiceRef\": \"INV-AA19FF\", \"amount\": 920000 }\n      ]\n    }\n  },\n  \"_note\": \"Money INTEGER CENTS. monthTotal 1591000 → \\\"$15,910\\\"; transitTotal 525000 → \\\"$5,250\\\". attentionCount is always 0 on the real page (no source signal). stubs = the parent's recentLanded.slice(0,3); each carries the EnrichedPayment subset the .pph__stub markup reads.\"\n}\n", "cases": [{"name": "es.json", "content": "{\n  \"_name\": \"Spanish — lang=es flips eyebrow/title-tail/sub/CTAs to ES strings\",\n  \"lang\": \"es\",\n  \"monthTotal\": 1591000,\n  \"transitTotal\": 525000,\n  \"attentionCount\": 0,\n  \"stubs\": [\n    { \"id\": \"pay_8a1f20c4\", \"method\": \"zelle\", \"initials\": \"MD\", \"client\": \"Maria Delgado\", \"whenLabel\": \"Hoy\", \"invoiceRef\": \"INV-3C91AB\", \"amount\": 482000 },\n    { \"id\": \"pay_2bb1043f\", \"method\": \"check\", \"initials\": \"TB\", \"client\": \"Tom Becker\", \"whenLabel\": \"hace 8d\", \"invoiceRef\": \"INV-AA19FF\", \"amount\": 920000 }\n  ],\n  \"_note\": \"All paymentsPage.* keys resolve via es.json (tFor(lang,…)). The amount number formatting (fmtMoney) stays en-US locale — only the surrounding copy localizes. whenLabel here is pre-localized by the parent (whenLabel() uses tFor(lang,…)).\"\n}\n"}, {"name": "fresh.json", "content": "{\n  \"_name\": \"Fresh — all totals 0 → \\\"Nothing's landed yet — let's change that.\\\" + freshSub; no stubs\",\n  \"lang\": \"en\",\n  \"monthTotal\": 0,\n  \"transitTotal\": 0,\n  \"attentionCount\": 0,\n  \"stubs\": [],\n  \"_note\": \"fresh branch: smaller clamped title (clamp(28px,3.4vw,44px)), pink non-italic <em> on \\\"let's change that\\\", freshSub explainer about clearing windows. Empty stubs → the .pph__stack renders no cards (still aria-hidden).\"\n}\n"}, {"name": "no-transit.json", "content": "{\n  \"_name\": \"No-transit — landed only, transitTotal=0 → sub reads \\\"Every dollar logged.\\\"\",\n  \"lang\": \"en\",\n  \"monthTotal\": 607000,\n  \"transitTotal\": 0,\n  \"attentionCount\": 0,\n  \"stubs\": [\n    { \"id\": \"pay_8a1f20c4\", \"method\": \"zelle\", \"initials\": \"MD\", \"client\": \"Maria Delgado\", \"whenLabel\": \"Today\", \"invoiceRef\": \"INV-3C91AB\", \"amount\": 482000 },\n    { \"id\": \"pay_5d77e9b2\", \"method\": \"cash\", \"initials\": \"GH\", \"client\": \"Greenfield HOA\", \"whenLabel\": \"Yesterday\", \"invoiceRef\": \"INV-77B0C1\", \"amount\": 125000 }\n  ],\n  \"_note\": \"transit=0 → the sub collapses to the single `everyDollar` clause (no \\\"Plus $X on the way\\\", no trailing \\\"The monsters logged every dollar.\\\"). monthTotal 607000 → \\\"$6,070\\\". Two stubs.\"\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — \\\"$15,910 showed up this month.\\\" + 3 stubs + \\\"Plus $5,250 on the way\\\"\",\n  \"lang\": \"en\",\n  \"monthTotal\": 1591000,\n  \"transitTotal\": 525000,\n  \"attentionCount\": 0,\n  \"stubs\": [\n    { \"id\": \"pay_8a1f20c4\", \"method\": \"zelle\", \"initials\": \"MD\", \"client\": \"Maria Delgado\", \"whenLabel\": \"Today\", \"invoiceRef\": \"INV-3C91AB\", \"amount\": 482000 },\n    { \"id\": \"pay_5d77e9b2\", \"method\": \"cash\", \"initials\": \"GH\", \"client\": \"Greenfield HOA\", \"whenLabel\": \"Yesterday\", \"invoiceRef\": \"INV-77B0C1\", \"amount\": 125000 },\n    { \"id\": \"pay_2bb1043f\", \"method\": \"check\", \"initials\": \"TB\", \"client\": \"Tom Becker\", \"whenLabel\": \"8d ago\", \"invoiceRef\": \"INV-AA19FF\", \"amount\": 920000 }\n  ]\n}\n"}], "css": "/* ============== PAYMENTS HERO — \"money landed\" treatment ==============\n   Extracted verbatim from reference/extracted/Paperwork Monster Payments.html.\n   Loaded by routes/payments/index.tsx alongside dashboard.css + quotes.css. */\n\n.pph {\n  position: relative;\n  display: grid;\n  grid-template-columns: minmax(0, 1.15fr) minmax(220px, 0.95fr);\n  gap: 24px;\n  align-items: center;\n  padding: 32px 32px 32px 24px;\n  margin: 4px 0 26px;\n  overflow: visible;\n}\n.pph__main {\n  position: relative;\n  z-index: 2;\n}\n.pph__eyebrow {\n  display: inline-flex;\n  align-items: center;\n  gap: 9px;\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 800;\n  letter-spacing: 0.14em;\n  text-transform: uppercase;\n  color: var(--brand-pink);\n  margin-bottom: 12px;\n  padding: 5px 11px 5px 9px;\n  background: rgba(255, 107, 107, 0.10);\n  border: 1px solid rgba(255, 107, 107, 0.22);\n  border-radius: var(--radius-pill);\n}\n.pph__eyebrow svg {\n  color: var(--brand-pink);\n}\n.pph__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: clamp(40px, 5.4vw, 72px);\n  line-height: 0.92;\n  letter-spacing: -0.025em;\n  color: var(--brand-teal);\n  margin: 0 0 12px;\n  font-variant-numeric: tabular-nums;\n  text-wrap: balance;\n}\n.pph__title-amount {\n  color: var(--brand-pink);\n  font-style: normal;\n  display: inline-block;\n}\n.pph__title-amount sup {\n  font-size: 0.46em;\n  vertical-align: 0.6em;\n  margin-right: 0.06em;\n  font-weight: 800;\n  letter-spacing: 0;\n}\n.pph__title-tail {\n  display: block;\n  font-size: clamp(22px, 2.8vw, 36px);\n  color: var(--brand-teal);\n  line-height: 1.05;\n  margin-top: 4px;\n  letter-spacing: -0.018em;\n}\n.pph__sub {\n  font-family: var(--font-body);\n  font-size: 15px;\n  line-height: 1.55;\n  color: var(--fg-muted);\n  margin: 14px 0 22px;\n  max-width: 46ch;\n  text-wrap: pretty;\n}\n.pph__sub strong {\n  color: var(--brand-teal);\n  font-weight: 700;\n}\n.pph__cta-row {\n  display: flex;\n  align-items: center;\n  gap: 14px;\n  flex-wrap: wrap;\n}\n.pph__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  padding: 13px 20px;\n  background: var(--brand-pink);\n  color: #fff;\n  border: none;\n  border-radius: var(--radius-pill);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 14px;\n  text-decoration: none;\n  cursor: pointer;\n  box-shadow: 0 6px 18px -6px rgba(255, 107, 107, 0.55);\n  transition: transform 160ms var(--ease-bounce);\n}\n.pph__cta:hover {\n  transform: translateY(-1px);\n}\n.pph__ghost {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  padding: 12px 16px;\n  background: transparent;\n  color: var(--brand-teal);\n  border: 1px solid rgba(26, 83, 92, 0.18);\n  border-radius: var(--radius-pill);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 13px;\n  text-decoration: none;\n  cursor: pointer;\n  transition: background 160ms;\n}\n.pph__ghost:hover {\n  background: rgba(26, 83, 92, 0.04);\n}\n\n/* Stack of recent-payment \"stubs\" on the right */\n.pph__stack {\n  position: relative;\n  height: 260px;\n  display: block;\n  min-width: 0;\n}\n.pph__stub {\n  position: absolute;\n  right: 0;\n  width: clamp(220px, 100%, 280px);\n  background: #fff;\n  border-radius: 18px;\n  border: 1px solid rgba(26, 83, 92, 0.10);\n  box-shadow:\n    0 18px 40px -18px rgba(26, 53, 48, 0.28),\n    0 2px 4px rgba(26, 53, 48, 0.05);\n  padding: 14px 16px;\n  transition: transform 280ms var(--ease-bounce), box-shadow 240ms ease;\n}\n.pph__stub--3 {\n  top: 0;\n  transform: rotate(-4.2deg) translateX(-22px);\n  z-index: 1;\n}\n.pph__stub--2 {\n  top: 70px;\n  transform: rotate(2.6deg) translateX(8px);\n  z-index: 2;\n}\n.pph__stub--1 {\n  top: 140px;\n  transform: rotate(-1.3deg) translateX(-6px);\n  z-index: 3;\n}\n.pph__stack:hover .pph__stub--1 {\n  transform: rotate(-1.3deg) translate(-6px, -3px);\n}\n.pph__stack:hover .pph__stub--2 {\n  transform: rotate(2.6deg) translate(8px, -2px);\n}\n.pph__stack:hover .pph__stub--3 {\n  transform: rotate(-4.2deg) translate(-22px, -1px);\n}\n\n.pph__stub-head {\n  display: flex;\n  align-items: center;\n  gap: 9px;\n  margin-bottom: 12px;\n}\n.pph__stub-av {\n  width: 30px;\n  height: 30px;\n  border-radius: 9px;\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 11px;\n  letter-spacing: 0.04em;\n  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.22);\n  flex-shrink: 0;\n}\n.pph__stub-meta {\n  flex: 1;\n  min-width: 0;\n  line-height: 1.15;\n}\n.pph__stub-client {\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12.5px;\n  color: var(--brand-teal);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.pph__stub-when {\n  font-family: var(--font-body);\n  font-size: 10.5px;\n  color: var(--fg-subtle);\n  text-transform: uppercase;\n  letter-spacing: 0.08em;\n  margin-top: 1px;\n}\n.pph__stub-amount {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: -0.02em;\n  color: var(--brand-teal);\n  font-variant-numeric: tabular-nums;\n  margin-bottom: 4px;\n}\n.pph__stub-foot {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  font-family: var(--font-body);\n  font-size: 11px;\n  color: var(--fg-muted);\n  border-top: 1px dashed rgba(26, 83, 92, 0.18);\n  padding-top: 8px;\n  margin-top: 10px;\n}\n.pph__stub-method {\n  display: inline-flex;\n  align-items: center;\n  gap: 5px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 10px;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--brand-teal);\n}\n.pph__stub-tag {\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 10px;\n  letter-spacing: 0.06em;\n  text-transform: uppercase;\n  padding: 3px 8px;\n  border-radius: var(--radius-pill);\n  background: var(--green-50);\n  color: var(--green-600);\n}\n.pph__stub-tag::before {\n  content: \"\";\n  width: 5px;\n  height: 5px;\n  border-radius: 999px;\n  background: var(--green-600);\n}\n\n/* Payment-card amount line — pcard__amount lives inside .qcard structure\n   on /payments. The amount is the dominant info on the card, so it gets\n   its own line treatment under the client name. */\n.pcard__amount {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 22px;\n  letter-spacing: -0.018em;\n  color: var(--brand-teal);\n  font-variant-numeric: tabular-nums;\n  margin: 4px 0 6px;\n}\n\n@media (max-width: 760px) {\n  .pph {\n    grid-template-columns: 1fr;\n    padding: 22px 18px 8px;\n    gap: 8px;\n  }\n  .pph__stack {\n    height: 320px;\n    margin-top: 4px;\n  }\n  .pph__stub {\n    right: auto;\n    left: 0;\n    width: min(94%, 320px);\n  }\n  /* Vertical fan — each stub clearly visible */\n  .pph__stub--3 {\n    top: 0;\n    transform: rotate(-2.6deg) translateX(6px);\n  }\n  .pph__stub--2 {\n    top: 90px;\n    transform: rotate(1.6deg) translateX(0);\n  }\n  .pph__stub--1 {\n    top: 180px;\n    transform: rotate(-0.8deg) translateX(8px);\n  }\n  .pph__stack:hover .pph__stub--1 {\n    transform: rotate(-0.8deg) translate(8px, -3px);\n  }\n  .pph__stack:hover .pph__stub--2 {\n    transform: rotate(1.6deg) translate(0, -2px);\n  }\n  .pph__stack:hover .pph__stub--3 {\n    transform: rotate(-2.6deg) translate(6px, -1px);\n  }\n}\n", "cssName": "payments-hero.css", "source": "/* SOURCE POINTER — full verbatim source lives in:\n *   ../../payments-page/js/PaymentsPage.tsx\n *     · function PaymentsHero()  lines 463–589\n *     · METHOD_AV_BG             lines 80–90\n *     · METHOD_ICON / P2PIcon    lines 92–144\n *     · methodLabel              lines 77–78\n *\n * The `.pph` editorial header (own stylesheet /payments.css). Left = eyebrow\n * + amount headline (\"$X showed up this month\" or a \"fresh\" empty variant) +\n * sub + two assistant-seeded CTAs. Right = `.pph__stack` — up to three\n * rotated `.pph__stub` recent-payment cards (aria-hidden decoration). Pure\n * presentational; no client state, no fetch. See payments-hero.md. */\nexport {};\n", "sourceName": "PaymentsHero.tsx", "checklist": "# Capture checklist — PaymentsHero\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/payments` — the `.pph` band is the first element on the\n  page (above the KPI strip). It's a child of the PaymentsPage island, so its\n  state follows the seeded payment data; you can't address it standalone.\n\n## Viewports (own `@media`)\n- **1280px** — two-column hero: copy left, rotated stub stack right.\n- **760px** — the `@media (max-width:760px)` cutover: hero → 1 col, the stub\n  stack re-fans **vertically** below the copy. Shoot at ~759px to confirm.\n\n## Element(s) to crop\n- The whole `<header class=\"pph\">` (copy + stub stack).\n- A single `.pph__stub` close-up (avatar gradient + amount + method pill +\n  \"Landed\" tag).\n\n## Transient states to drive\n1. **populated** — a seed with landed payments this month: the\n   `$X showed up this month.` headline + up to three rotated stubs + the\n   \"Plus $Y on the way\" sub (needs at least one in-transit payment).\n2. **fresh** — wiped/empty account (0 payments): the smaller\n   \"Nothing's landed yet — *let's change that*.\" variant, freshSub copy, empty\n   stack.\n3. **no-transit** — landed-only seed (no ACH/check inside its window): sub reads\n   just \"Every dollar logged.\" (no \"Plus … on the way\" clause).\n4. **es** — switch UI language to Spanish (Settings) → eyebrow / title-tail /\n   sub / CTAs localize; the `$` number stays en-US formatted.\n\n## Motion to film\n- **Stub stack hover** — hover `.pph__stack`; the three `.pph__stub--1/2/3`\n  lift together (~1–3px each, keeping their rotations) over\n  `280ms var(--ease-bounce)`.\n- **CTA hover** — `.pph__cta` nudges up 1px (`160ms` bounce); `.pph__ghost`\n  background fades.\n- Re-shoot the stub-hover with `prefers-reduced-motion: reduce` (relies on the\n  global clamp — verify it stills).\n\n## Notes\n- `.pph__stack` is decorative (`aria-hidden`) — capture is for visual fidelity\n  only.\n- Both CTAs navigate to `/assistant?seed=…` (they do NOT record/export here) —\n  if filming a click, expect a route change, not an in-place action.\n"}, {"name": "payments-kpis", "component": "PaymentsKpis", "spec": "# PaymentsKpis\n\nThe 4-cell `.qkpi` KPI strip on /payments: **Landed this month** · **In\ntransit** (pink-accent cell) · **Needs attention** · **Avg days to pay**.\n\n## 1. Classification & behavior\n- **Bucket:** local presentational component of the PaymentsPage island\n  (`islands/PaymentsPage.tsx` lines 593–654). Not its own island.\n- **Interaction tier:** **static / island-child.** No client state, no fetch,\n  no events, no callbacks. Every number is a prop.\n- **Server actions + flash:** none. Non-interactive — the cells are not links\n  or buttons (unlike some KPI strips elsewhere).\n- **Data source:** all props, computed by the parent island. **Honest-empty:**\n  the Avg-days cell shows `—` + `noHistory` copy when `avgDays===0`; the other\n  cells degrade to `$0` / `0` naturally.\n- **Liveness:** none.\n- **Data-shape hazards (rollups — the flagged concern for this tile):**\n  - **`monthTotal` / `transitTotal` / `attentionTotal` are per-render Σ scans**\n    in the parent over the landed/transit/attention arrays. On a real backend\n    these mirror `DashboardStats.payments.receivedYtdCents` and a settling\n    subset — whole-account aggregates. **[precompute counters; don't scan on\n    each load].**\n  - **`landedCount` / `transitCount` / `attentionCount`** are array lengths of\n    the same client-side buckets (count-per-status). **[status-bucketed\n    counters].**\n  - **`avgDays`** = mean of `(receivedAt − invoice.issuedDate)` over landed\n    payments that have an `issuedDate` — a join+average recomputed every render.\n    **[materialize a payment-cycle metric].**\n  - **`attentionCount`/`attentionTotal` are structurally always 0** — the\n    `attention` status is never produced (no source signal on `Payment`), so the\n    \"Needs attention\" cell perpetually reads `0` / \"$0 held up\". Dead metric.\n\n## 2. Anatomy\n```\n<div class=\"qkpi\">\n  <div class=\"qkpi__cell\">                          ← Landed this month\n    <div class=\"qkpi__lbl\">Landed this month</div>\n    <div class=\"qkpi__val\">{fmtMoney(monthTotal)}</div>\n    <div class=\"qkpi__sub\">{n} payment(s)</div>      ← tnFor plural\n  </div>\n  <div class=\"qkpi__cell qkpi__cell--accent\">        ← In transit (PINK accent)\n    <div class=\"qkpi__lbl\">In transit</div>\n    <div class=\"qkpi__val\">{fmtMoney(transitTotal)}</div>   ← pink val\n    <div class=\"qkpi__sub\">{n} on the way</div>\n  </div>\n  <div class=\"qkpi__cell\">                            ← Needs attention\n    <div class=\"qkpi__lbl\">Needs attention</div>\n    <div class=\"qkpi__val\">{attentionCount}</div>     ← a COUNT, not money\n    <div class=\"qkpi__sub\">{fmtMoney(attentionTotal)} held up</div>\n  </div>\n  <div class=\"qkpi__cell\">                            ← Avg days to pay\n    <div class=\"qkpi__lbl\">Avg days to pay</div>\n    <div class=\"qkpi__val\">{avgDays>0 ? \"Nd\" : \"—\"}</div>\n    <div class=\"qkpi__sub\">{avgDays>0 ? \"across landed payments\" : \"no paid history yet\"}</div>\n  </div>\n</div>\n```\n- Note the **In-transit** cell is **always** the accent cell here (vs Invoices'\n  KPIs where the accent is conditional on overdue count).\n- Deps: `fmtMoney`, `tFor`, and the `tnFor` plural helper (PaymentsPage.tsx\n  lines 43–44). No icons.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` (required) | — | select | no |\n| `landedCount` | number (required) | — | number | no |\n| `monthTotal` | number (CENTS, required) | — | number | no |\n| `transitCount` | number (required) | — | number | no |\n| `transitTotal` | number (CENTS, required) | — | number | no |\n| `attentionCount` | number (required) | — | number | no |\n| `attentionTotal` | number (CENTS, required) | — | number | no |\n| `avgDays` | number (required) | — | number | no |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| populated | all four cells with real numbers | `cases/populated/populated.json` |\n| empty | fresh account, all 0 → \"$0\", avg \"—\" | `cases/empty/empty.json` |\n| with-transit | non-zero In-transit accent cell | `cases/with-transit/with-transit.json` |\n| es | Spanish labels + plurals | `cases/es/es.json` |\n\n## 5. Events\n- None. The cells are static `<div>`s (no click/nav).\n\n## 6. Motion\n- None of its own. (The `.qkpi__cell--accent` gradient is static.) Reduced-motion\n  is a no-op here.\n\n## 7. Responsive (from quotes.css)\n- `≤1200px`: `.qkpi` → `repeat(2, 1fr)` (2-up).\n- `≤768px`: `.qkpi` → `1fr` (stacked single column).\n\n## 8. A11y\n- Labels are uppercase `.qkpi__lbl` text tied visually (not programmatically) to\n  the value. No `<dl>`/`<dt>`/`<dd>` semantics — purely visual grouping.\n  **Fix:** wrap each cell as a `<dl>` (or add `aria-label` summarizing\n  label+value) so AT reads \"Landed this month: $15,910\".\n- The pink accent value relies on color alone to distinguish In-transit — the\n  text label \"In transit\" carries the meaning, so it's not color-only.\n\n## 9. Used on\n`/payments` only (rendered by PaymentsPage). CSS = `css/payments-kpis.css`\n(documents the `.qkpi*` reuse from `static/quotes.css`; no component-local CSS).\n", "fixture": "{\n  \"category\": \"payments\",\n  \"folder\": \"payments-kpis\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"landedCount\": { \"type\": \"number\", \"value\": 4 },\n    \"monthTotal\": { \"type\": \"number\", \"value\": 1591000 },\n    \"transitCount\": { \"type\": \"number\", \"value\": 2 },\n    \"transitTotal\": { \"type\": \"number\", \"value\": 525000 },\n    \"attentionCount\": { \"type\": \"number\", \"value\": 0 },\n    \"attentionTotal\": { \"type\": \"number\", \"value\": 0 },\n    \"avgDays\": { \"type\": \"number\", \"value\": 6 }\n  },\n  \"_note\": \"Money INTEGER CENTS (monthTotal 1591000 → \\\"$15,910\\\", transitTotal 525000 → \\\"$5,250\\\"). attentionCount/attentionTotal default 0 because the page never produces the attention status. avgDays is days (not cents).\"\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty — fresh account, all 0 → \\\"$0\\\" cells, Avg shows \\\"—\\\" + \\\"no paid history yet\\\"\",\n  \"lang\": \"en\",\n  \"landedCount\": 0,\n  \"monthTotal\": 0,\n  \"transitCount\": 0,\n  \"transitTotal\": 0,\n  \"attentionCount\": 0,\n  \"attentionTotal\": 0,\n  \"avgDays\": 0,\n  \"_note\": \"avgDays=0 → Avg cell renders \\\"—\\\" and the noHistory sub. Landed sub uses the plural \\\"0 payments\\\". In-transit accent cell still renders (always accent) with \\\"$0\\\" / \\\"0 on the way\\\".\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — labels + plurals localize via es.json\",\n  \"lang\": \"es\",\n  \"landedCount\": 4,\n  \"monthTotal\": 1591000,\n  \"transitCount\": 2,\n  \"transitTotal\": 525000,\n  \"attentionCount\": 0,\n  \"attentionTotal\": 0,\n  \"avgDays\": 6,\n  \"_note\": \"All qkpi labels/subs resolve through es.json (landedLabel, transitLabel, attentionLabel, avgLabel, payments.one/other plural, onTheWay, heldUp, acrossLanded). Money formatting stays en-US.\"\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — $15,910 landed (4) · $5,250 in transit (2) · 0 attention · 6d avg\",\n  \"lang\": \"en\",\n  \"landedCount\": 4,\n  \"monthTotal\": 1591000,\n  \"transitCount\": 2,\n  \"transitTotal\": 525000,\n  \"attentionCount\": 0,\n  \"attentionTotal\": 0,\n  \"avgDays\": 6\n}\n"}, {"name": "with-transit.json", "content": "{\n  \"_name\": \"With-transit — pink-accent In-transit cell carries a non-zero $5,250 (2 on the way)\",\n  \"lang\": \"en\",\n  \"landedCount\": 1,\n  \"monthTotal\": 482000,\n  \"transitCount\": 2,\n  \"transitTotal\": 525000,\n  \"attentionCount\": 0,\n  \"attentionTotal\": 0,\n  \"avgDays\": 3,\n  \"_note\": \"Focus the In-transit accent cell: gradient bg (#fff1ed→#ffe4da), pink value \\\"$5,250\\\", sub \\\"2 on the way\\\". monthTotal 482000 → \\\"$4,820\\\" (just the single landed Zelle).\"\n}\n"}], "css": "/* PaymentsKpis reuses the `.qkpi` strip block (static/quotes.css) — verbatim\n   in ../../../../invoices/css/invoices.css / quotes.css: .qkpi / .qkpi__cell /\n   .qkpi__cell--accent / .qkpi__lbl / .qkpi__val / .qkpi__sub. Here the accent\n   (pink-gradient) cell is the *In transit* cell (always accent, unlike\n   InvoicesKpis where accent is conditional on overdueCount). No component-local\n   CSS. Tokens from dashboard.css. */\n", "cssName": "payments-kpis.css", "source": "/* SOURCE POINTER — full verbatim source lives in:\n *   ../../payments-page/js/PaymentsPage.tsx\n *     · function PaymentsKpis()  lines 593–654\n *     · tnFor plural helper       lines 43–44\n *\n * 4-cell `.qkpi` strip (styles from quotes.css): Landed this month · In\n * transit (accent cell) · Needs attention · Avg days to pay (shows \"—\" when\n * no history). Pure presentational — all numbers are props. */\nexport {};\n", "sourceName": "PaymentsKpis.tsx", "checklist": "# Capture checklist — PaymentsKpis\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/payments` — the `.qkpi` 4-cell strip sits directly\n  under the `.pph` hero. Child of the PaymentsPage island; its numbers follow\n  the seeded payment data.\n\n## Viewports (own `@media`, from quotes.css)\n- **1280px** — 4 cells in one row.\n- **1200px** — `repeat(2, 1fr)` (2×2 grid). Shoot at ~1199px.\n- **768px** — single column (4 stacked). Shoot at ~767px.\n\n## Element(s) to crop\n- The full `<div class=\"qkpi\">` strip (all four cells).\n- The **In-transit** accent cell alone (pink gradient + pink value) to show it\n  differs from the three plain white cells.\n\n## Transient states to drive\n1. **populated** — seed with landed + in-transit payments: all four cells carry\n   real numbers; Avg shows \"Nd\".\n2. **empty** — wiped account: every cell \"$0\"/\"0\", Avg shows \"—\" +\n   \"no paid history yet\".\n3. **with-transit** — at least one ACH(<2d)/check(<5d): the accent cell value is\n   non-zero.\n4. **es** — Spanish UI language: labels + the payment plural localize.\n\n## Motion to film\n- None — the strip is static (the accent gradient doesn't animate). No\n  reduced-motion concern.\n\n## Notes\n- The \"Needs attention\" cell will read **0 / \"$0 held up\"** on every real seed\n  (the attention status is never produced) — capture it as-is and note the dead\n  metric; do not fabricate a non-zero attention value for a \"real\" screenshot.\n"}, {"name": "payments-page", "component": "PaymentsPage", "spec": "# PaymentsPage\n\nThe /payments island. Fetch → enrich → derive status → bucket → render\nhero/KPIs/tracks/side-rail. 1028 lines; PaymentCard, LandedRow, the four side\nwidgets, hero, KPIs are local sub-components (own folders).\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PaymentsPage.tsx`).\n- **Interaction tier:** **whole-page island, client-only state.** Self-fetches\n  on mount; `lang` prop is an ignored SSR seed (reads `langSignal.value`).\n- **Client state owned:** `s: State { loading, error, payments, invoices,\n  customers }`. That's it — there are **no mutations on this page**, so no busy\n  flags, no modals. (PaymentCard owns only a local `flipped`.)\n- **Server actions + flash:** **none.** This is a read-only page. PaymentCard's\n  CTA + back-foot buttons are no-ops (`stopPropagation` only); the hero CTAs are\n  links into `/assistant?seed=…`. No `location.reload()` anywhere.\n- **Data source per region:**\n  - payments ← `paymentsClient.list()` (`GET /payments`), `.catch → []`.\n  - invoices ← `dashboardClient.invoices(undefined)`, `.catch → []` (joined to\n    payments to resolve customer + issuedDate for avg-days).\n  - customers ← `dashboardClient.customers()`, `.catch → []`.\n  - parallel `useEffect([])`, `alive` guard.\n- **Honest-empty:** `loading` → Skeletons (SHARED); `error` → `.qpage-error`\n  (UNSTYLED, see §8); else \"fresh\" hero variant + Landed-track hint + per-widget\n  empty copy.\n- **Liveness:** request-response only; no polling. (No refresh path at all — the\n  page never mutates.)\n- **Data-shape hazards:**\n  - **Payment-sum rollups over jobs/payments are recomputed client-side every\n    render**: `monthTotal`/`transitTotal`/`attentionTotal`, `avgDays`\n    (receivedAt − issuedDate over landed), and all four side widgets. On a real\n    backend these mirror `DashboardStats.payments.{methodMixCents,topPayors}` +\n    `revenue.sparkline12mo` — whole-account Σ scans. **[denormalize/precompute].**\n  - **Status is invented client-side** (`deriveStatus` + `SETTLE_DAYS`) — the\n    `Payment` DTO has no status. ACH<2d / check<5d ⇒ \"transit\"; everything else\n    \"landed\". `attention` is never produced (no source signal) so its track +\n    KPI always read 0.\n  - **PSideFlow buckets by array index** (`weeks[11-(i%12)] += a`), NOT by\n    `receivedAt` — fabricated trend; hardcoded Feb/Mar/Apr labels.\n- **Anti-patterns:**\n  - Whole-page island, frozen SSR props (only `lang`), fetch-on-mount. **Fix:**\n    SSR the payment list as a hydration seed.\n  - **Undefined CSS classes** in the Top-payors widget + LandedRow render\n    unstyled (see payment-side-rail.md / landed-row.md). Real visible bug.\n  - PaymentCard actions are dead (no-ops). The \"Record a payment\" / \"Export\n    this month\" hero CTAs route through the assistant rather than acting here.\n\n## 2. Anatomy\n```\n<>\n  <PaymentsHero …/>                          ← .pph + .pph__stack stubs\n  <PaymentsKpis …/>                          ← .qkpi (In-transit cell accent)\n  <div class=\"qlay\">\n    <div>                                     ← main column\n      {attention>0 && <QuoteTrack 01 Needs attention> PaymentCard[] }\n      <QuoteTrack 0n Just landed>\n        recentLanded → .qcards PaymentCard[]\n        olderLanded  → .qdone   LandedRow[]\n        (else landedEmpty hint)\n      {transit>0 && <QuoteTrack 0m In transit> PaymentCard[] }\n    </div>\n    <aside class=\"qside\">                      ← sticky rail\n      <PSideFlow/> <PSideTopPayors/> <PSideMix/> <PSideTip/>\n    </aside>\n  </div>\n</>\n```\n- Local helpers: `methodLabel`, `tnFor`, `P2PIcon`, `METHOD_ICON`,\n  `METHOD_AV_BG`, `SETTLE_DAYS`, `deriveStatus`, `initialsOf`, `whenLabel`,\n  `noteFor`, `enrich`, `STATUS_MOOD`.\n- Deps: QuoteTrack[shared], Skeletons[shared], `I`/`ICN`, `fmtMoney`, `tFor`/\n  `langSignal`, paymentsClient/dashboardClient.\n\n## 3. Props\n| name | type | default | widget | signal? |\n|---|---|---|---|---|\n| `lang` | `Lang` | (ignored SSR seed) | — | reads `langSignal.value` |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | Skeletons | `isolate/cases/loading/loading.json` |\n| error | `.qpage-error` text | `isolate/cases/error/error.json` |\n| fresh | 0 payments → fresh hero + empty widgets | `isolate/cases/fresh/fresh.json` |\n| landed-only | landed payments, no transit/attention | `isolate/cases/landed/landed.json` |\n| with-transit | an ACH/check still settling → transit track shows | `isolate/cases/with-transit/with-transit.json` |\n\n## 5. Events\n- No page-level interactive events. (Track collapse → QuoteTrack; card flip →\n  PaymentCard; hero CTAs are plain `<a>` navigations.)\n\n## 6. Motion\n- None of its own. Hero stub-stack hover/rotate = payments-hero. Card flip =\n  PaymentCard. Reduced-motion via global clamp.\n\n## 7. Responsive (own `@media`)\n- quotes.css: `≤1200px` `.qlay`→1 col + `.qside` static; `≤768px` `.qcards`/\n  `.qkpi`→1 col; `≤1100px` `.qdone`→1 col; `≤560px` `.qdone__row` tighter +\n  `.qdone__when` hidden.\n- payments.css: `≤760px` `.pph`→1 col + stub stack re-fans vertically.\n\n## 8. A11y\n- `.qpage-error` unstyled, no `role=\"alert\"`.\n- `.pph__stack` is `aria-hidden=\"true\"` (decorative) — good.\n- PaymentCard buttons that do nothing should be removed or wired (they read as\n  actionable but aren't).\n\n## 9. Used on\n`/payments` only (mounted by `routes/payments/index.tsx`).\n", "fixture": "{\n  \"category\": \"payments\",\n  \"folder\": \"payments-page\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  },\n  \"_note\": \"Whole-page island (islands/PaymentsPage.tsx). The only real prop is `lang` (an ignored SSR seed — the island reads langSignal.value). Every visible state is driven by what the three clients return, so cases stub them via `_mocks`: paymentsClient.list() → Payment[], dashboardClient.invoices() → Invoice[], dashboardClient.customers() → Customer[]. Money is INTEGER CENTS. There are NO mutations on this page; the only client state is `s:{loading,error,payments,invoices,customers}` plus each PaymentCard's local `flipped`.\"\n}\n", "cases": [{"name": "error.json", "content": "{\n  \"_name\": \"Error — .qpage-error text (UNSTYLED, no role=alert)\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"langSignal\": \"en\"\n  },\n  \"_mocks\": {\n    \"paymentsClient.list\": { \"_throw\": \"Network request failed (500)\" },\n    \"dashboardClient.invoices\": { \"value\": [] },\n    \"dashboardClient.customers\": { \"value\": [] }\n  },\n  \"_note\": \"Each client has its own .catch→[] so a single rejection does NOT trip the error branch — only a throw INSIDE the .then (or the outer Promise.all rejecting before the per-client catch) sets s.error. The renderable error state shows `paymentsPage.loadError` = \\\"Couldn't load payments: {error}\\\". Isolate must force the .then/Promise.all to reject; the per-call _throw documents intent.\"\n}\n"}, {"name": "fresh.json", "content": "{\n  \"_name\": \"Fresh — 0 payments → fresh hero (\\\"Nothing's landed yet — let's change that.\\\") + empty side widgets + Landed-track hint\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"langSignal\": \"en\"\n  },\n  \"_mocks\": {\n    \"paymentsClient.list\": { \"value\": [] },\n    \"dashboardClient.invoices\": { \"value\": [] },\n    \"dashboardClient.customers\": { \"value\": [] }\n  },\n  \"_note\": \"monthTotal=transitTotal=attentionCount=0 ⇒ PaymentsHero `fresh` branch (smaller clamped title, pink <em>, freshSub copy, both CTAs still link to /assistant?seed=…). KPIs all read $0 / — (avgDays=0 → \\\"—\\\" + noHistory). Only the Just-landed track renders (attention/transit hidden), showing `paymentsPage.track.landedEmpty`. Side rail: PSideFlow.subEmpty + flow.empty, PSideTopPayors.empty, PSideMix.empty, PSideTip (static).\"\n}\n"}, {"name": "landed.json", "content": "{\n  \"_name\": \"Landed-only — settled payments this month, no transit/attention; recent (≤1d) → flip cards, older → .qdone rows\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"langSignal\": \"en\"\n  },\n  \"_mocks\": {\n    \"paymentsClient.list\": {\n      \"value\": [\n        { \"id\": \"pay_8a1f20c4\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_3c91ab77e201\", \"amount\": 482000, \"method\": \"zelle\", \"receivedAt\": \"2026-06-18T14:20:00Z\", \"createdAt\": \"2026-06-18T14:21:00Z\", \"updatedAt\": \"2026-06-18T14:21:00Z\" },\n        { \"id\": \"pay_5d77e9b2\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_77b0c1aa4410\", \"amount\": 125000, \"method\": \"cash\", \"receivedAt\": \"2026-06-17T16:00:00Z\", \"createdAt\": \"2026-06-17T16:05:00Z\", \"updatedAt\": \"2026-06-17T16:05:00Z\" },\n        { \"id\": \"pay_2bb1043f\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_aa19ff2c8e30\", \"amount\": 920000, \"method\": \"check\", \"receivedAt\": \"2026-06-10T09:30:00Z\", \"createdAt\": \"2026-06-10T09:31:00Z\", \"updatedAt\": \"2026-06-10T09:31:00Z\" },\n        { \"id\": \"pay_9f3c7d10\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_4410ce92ab51\", \"amount\": 64000, \"method\": \"card\", \"receivedAt\": \"2026-06-05T11:00:00Z\", \"createdAt\": \"2026-06-05T11:00:00Z\", \"updatedAt\": \"2026-06-05T11:00:00Z\" }\n      ]\n    },\n    \"dashboardClient.invoices\": {\n      \"value\": [\n        { \"id\": \"inv_3c91ab77e201\", \"userId\": \"usr_01\", \"customerId\": \"cus_77a1\", \"amount\": 482000, \"issuedDate\": \"2026-06-11\", \"dueDate\": \"2026-06-25\", \"status\": \"paid\", \"createdAt\": \"2026-06-11T00:00:00Z\", \"updatedAt\": \"2026-06-18T14:21:00Z\" },\n        { \"id\": \"inv_77b0c1aa4410\", \"userId\": \"usr_01\", \"customerId\": \"cus_22b9\", \"amount\": 125000, \"issuedDate\": \"2026-06-12\", \"dueDate\": \"2026-06-26\", \"status\": \"paid\", \"createdAt\": \"2026-06-12T00:00:00Z\", \"updatedAt\": \"2026-06-17T16:05:00Z\" },\n        { \"id\": \"inv_aa19ff2c8e30\", \"userId\": \"usr_01\", \"customerId\": \"cus_5c3d\", \"amount\": 920000, \"issuedDate\": \"2026-06-02\", \"dueDate\": \"2026-06-16\", \"status\": \"paid\", \"createdAt\": \"2026-06-02T00:00:00Z\", \"updatedAt\": \"2026-06-10T09:31:00Z\" },\n        { \"id\": \"inv_4410ce92ab51\", \"userId\": \"usr_01\", \"customerId\": \"cus_8e10\", \"amount\": 64000, \"issuedDate\": \"2026-05-30\", \"dueDate\": \"2026-06-13\", \"status\": \"paid\", \"createdAt\": \"2026-05-30T00:00:00Z\", \"updatedAt\": \"2026-06-05T11:00:00Z\" }\n      ]\n    },\n    \"dashboardClient.customers\": {\n      \"value\": [\n        { \"id\": \"cus_77a1\", \"userId\": \"usr_01\", \"name\": \"Maria Delgado\", \"phoneNumber\": \"+15125550199\" },\n        { \"id\": \"cus_22b9\", \"userId\": \"usr_01\", \"name\": \"Greenfield HOA\", \"email\": \"board@greenfieldhoa.org\" },\n        { \"id\": \"cus_5c3d\", \"userId\": \"usr_01\", \"name\": \"Tom Becker\", \"phoneNumber\": \"+15125550174\" },\n        { \"id\": \"cus_8e10\", \"userId\": \"usr_01\", \"name\": \"Lopez Property Mgmt\", \"email\": \"ar@lopezpm.com\" }\n      ]\n    }\n  },\n  \"_note\": \"All four are landed (zelle/cash/card settle instantly; check from 2026-06-10 is 8d ago > its 5d window → landed). monthTotal = 482000+125000+920000+64000 = 1591000 cents → \\\"$15,910\\\". recentLanded (daysAgo≤1): the 2026-06-18 Zelle (today) + 2026-06-17 cash (yesterday) render as .qcards PaymentCard flip cards + populate the hero stub stack. olderLanded (>1d): the check + card render as .qdone LandedRow rows. transit/attention tracks hidden (Just-landed becomes Track 01). avgDays = mean(receivedAt − issuedDate) across the four. PSideTopPayors/PSideMix populate from these (HAZARD: recomputed client-side every render).\"\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — Skeletons (ShimmerStyle + PageHeaderSkeleton + CardGridSkeleton rows=2)\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"langSignal\": \"en\"\n  },\n  \"_mocks\": {\n    \"paymentsClient.list\": { \"_delay\": 100000, \"value\": [] },\n    \"dashboardClient.invoices\": { \"_delay\": 100000, \"value\": [] },\n    \"dashboardClient.customers\": { \"_delay\": 100000, \"value\": [] }\n  },\n  \"_note\": \"useEffect Promise.all never resolves within the frame → s.loading stays true → the shared Skeletons branch renders (no hero/KPIs/tracks). Hold the never-resolving mocks to film the shimmer.\"\n}\n"}, {"name": "with-transit.json", "content": "{\n  \"_name\": \"With-transit — an ACH (<2d) + a check (<5d) still settling → In-transit track shows (collapsed) + transit total in hero/KPI accent cell\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"langSignal\": \"en\"\n  },\n  \"_mocks\": {\n    \"paymentsClient.list\": {\n      \"value\": [\n        { \"id\": \"pay_8a1f20c4\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_3c91ab77e201\", \"amount\": 482000, \"method\": \"zelle\", \"receivedAt\": \"2026-06-18T14:20:00Z\", \"createdAt\": \"2026-06-18T14:21:00Z\", \"updatedAt\": \"2026-06-18T14:21:00Z\" },\n        { \"id\": \"pay_ac77001e\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_77b0c1aa4410\", \"amount\": 310000, \"method\": \"ach\", \"receivedAt\": \"2026-06-17T08:00:00Z\", \"createdAt\": \"2026-06-17T08:00:00Z\", \"updatedAt\": \"2026-06-17T08:00:00Z\" },\n        { \"id\": \"pay_ck902bd5\", \"userId\": \"usr_01\", \"invoiceId\": \"inv_aa19ff2c8e30\", \"amount\": 215000, \"method\": \"check\", \"receivedAt\": \"2026-06-16T10:00:00Z\", \"createdAt\": \"2026-06-16T10:00:00Z\", \"updatedAt\": \"2026-06-16T10:00:00Z\" }\n      ]\n    },\n    \"dashboardClient.invoices\": {\n      \"value\": [\n        { \"id\": \"inv_3c91ab77e201\", \"userId\": \"usr_01\", \"customerId\": \"cus_77a1\", \"amount\": 482000, \"issuedDate\": \"2026-06-11\", \"dueDate\": \"2026-06-25\", \"status\": \"paid\", \"createdAt\": \"2026-06-11T00:00:00Z\", \"updatedAt\": \"2026-06-18T14:21:00Z\" },\n        { \"id\": \"inv_77b0c1aa4410\", \"userId\": \"usr_01\", \"customerId\": \"cus_22b9\", \"amount\": 310000, \"issuedDate\": \"2026-06-13\", \"dueDate\": \"2026-06-27\", \"status\": \"claimed\", \"createdAt\": \"2026-06-13T00:00:00Z\", \"updatedAt\": \"2026-06-17T08:00:00Z\" },\n        { \"id\": \"inv_aa19ff2c8e30\", \"userId\": \"usr_01\", \"customerId\": \"cus_5c3d\", \"amount\": 215000, \"issuedDate\": \"2026-06-09\", \"dueDate\": \"2026-06-23\", \"status\": \"claimed\", \"createdAt\": \"2026-06-09T00:00:00Z\", \"updatedAt\": \"2026-06-16T10:00:00Z\" }\n      ]\n    },\n    \"dashboardClient.customers\": {\n      \"value\": [\n        { \"id\": \"cus_77a1\", \"userId\": \"usr_01\", \"name\": \"Maria Delgado\", \"phoneNumber\": \"+15125550199\" },\n        { \"id\": \"cus_22b9\", \"userId\": \"usr_01\", \"name\": \"Greenfield HOA\", \"email\": \"board@greenfieldhoa.org\" },\n        { \"id\": \"cus_5c3d\", \"userId\": \"usr_01\", \"name\": \"Tom Becker\", \"phoneNumber\": \"+15125550174\" }\n      ]\n    }\n  },\n  \"_note\": \"Zelle (today) → landed. ACH 2026-06-17 = 1d ago < 2d window → transit (etaDays 1). Check 2026-06-16 = 2d ago < 5d window → transit (etaDays 3). transit total = 310000+215000 = 525000 → \\\"$5,250\\\" shown in the hero subPlus line + the pink-accent In-transit KPI cell. The In-transit QuoteTrack renders defaultOpen=false (collapsed → must click to expand). attention still 0 (no source signal) so that track + KPI read 0. Track numbering: Just-landed=01, In-transit=02.\"\n}\n"}], "css": "/* ============================================================================\n   PaymentsPage — the page-shell CSS the whole-page island owns directly.\n   The island composes sub-components that bring their own extracted CSS:\n     · PaymentsHero    → ../../payments-hero/css/payments-hero.css   (.pph*, from static/payments.css)\n     · PaymentsKpis    → ../../payments-kpis/css/payments-kpis.css   (.qkpi*, from static/quotes.css)\n     · PaymentCard     → ../../payment-card/css/payment-card.css     (.qcard* + .pcard__amount)\n     · LandedRow       → ../../landed-row/css/landed-row.css         (.qdone*)\n     · PaymentSideRail → ../../payment-side-rail/css/payment-side-rail.css (.qside*)\n     · QuoteTrack      → shared-components/quote-track/css/quote-track.css   (.qtrack*)\n     · Skeletons       → shared (loading branch)\n\n   What's LEFT for the page itself: the two-column layout grid `.qlay`\n   (main column + sticky `.qside` rail) — copied verbatim from\n   static/quotes.css (lines 898–931) — plus the .qcards card grid that wraps\n   the recent PaymentCards, and the UNSTYLED `.qpage-error` hook.\n   Tokens from dashboard.css / quotes.css. Single light theme.\n   ============================================================================ */\n\n/* Two-column page layout: main + sticky side rail. */\n.qlay {\n  display: grid;\n  grid-template-columns: 1fr 320px;\n  gap: 22px;\n  align-items: flex-start;\n}\n\n/* Card grid that holds the recent-landed PaymentCard flip cards. */\n.qcards {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));\n  gap: 16px;\n}\n\n@media (max-width: 1200px) {\n  .qlay {\n    grid-template-columns: 1fr;\n  }\n  /* rail drops below the main column and stops sticking */\n  .qside {\n    position: static;\n  }\n  .qkpi {\n    grid-template-columns: repeat(2, 1fr);\n  }\n}\n\n@media (max-width: 768px) {\n  .qcards {\n    grid-template-columns: 1fr;\n  }\n  .qkpi {\n    grid-template-columns: 1fr;\n  }\n}\n\n/* === UNSTYLED IN SOURCE — the error branch renders this with no rule ===\n   `.qpage-error` has NO definition in any static/*.css and no role=\"alert\";\n   it falls back to default block body text. Flagged in payments-page.md §8.\n   REBUILD FIX: give it a styled error-card (danger-bg + danger fg + padding +\n   radius) and role=\"alert\"/aria-live=\"assertive\" so AT announces a load\n   failure. */\n", "cssName": "payments-page.css", "source": "/**\n * /payments island — editorial \"money landed\" treatment matching the\n * canonical reference (reference/extracted/Paperwork Monster Payments.html).\n *\n * Structure:\n *   PaymentsHero  (.pph + stub stack of recent landed payments)\n *   PaymentsKpis  (.qkpis 4-cell)\n *   .qlay grid:\n *     main → Track sections (.qtrack) of PaymentCard (.qcard) flip cards\n *            and a tail-of-month LandedRow list (.qdone)\n *     aside.qside → PSideFlow, PSideTopPayors, PSideMix, PSideTip\n *\n * Status taxonomy:\n *   - landed:    payment has been recorded (current data model only carries this)\n *   - transit:   reserved for future when a \"pending settlement\" status exists\n *   - attention: reserved for future declined/returned tracking\n *\n * Tracks for transit/attention render conditionally — when there's no data\n * for them the page reads cleanly without empty sections.\n */\nimport { useEffect, useMemo, useState } from \"preact/hooks\";\nimport {\n  type Payment,\n  type PaymentMethod,\n  paymentsClient,\n} from \"../clients/payments.ts\";\nimport {\n  type Customer,\n  dashboardClient,\n  type Invoice,\n} from \"../clients/dashboard.ts\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport {\n  CardGridSkeleton,\n  PageHeaderSkeleton,\n  ShimmerStyle,\n} from \"../components/Skeletons.tsx\";\nimport { fmtMoney } from \"../lib/format.ts\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\n/** Plural helper bound to an explicit language (the reactive `tn` reads the\n *  global lang signal; the logged-in app threads `lang` explicitly). */\nconst tnFor = (lang: Lang, key: string, n: number, vars?: Record<string, string | number>): string =>\n  tFor(lang, `${key}.${n === 1 ? \"one\" : \"other\"}`, { n, ...vars });\nimport QuoteTrack from \"./QuoteTrack.tsx\";\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  payments: Payment[];\n  invoices: Invoice[];\n  customers: Customer[];\n}\n\nconst INITIAL: State = {\n  loading: true,\n  error: null,\n  payments: [],\n  invoices: [],\n  customers: [],\n};\n\n/** i18n key per method. Reuses the seeded shared paymentMethod.* keys where\n *  the wording matches exactly; ACH/Card carry this page's own short labels. */\nconst METHOD_KEY: Record<PaymentMethod, string> = {\n  cash: \"paymentMethod.cash\",\n  check: \"paymentMethod.check\",\n  ach: \"paymentsPage.method.ach\",\n  card: \"paymentsPage.method.card\",\n  venmo: \"paymentMethod.venmo\",\n  zelle: \"paymentMethod.zelle\",\n  cashapp: \"paymentMethod.cashApp\",\n  paypal: \"paymentMethod.paypal\",\n  other: \"paymentMethod.other\",\n};\n\nconst methodLabel = (lang: Lang, m: PaymentMethod): string =>\n  tFor(lang, METHOD_KEY[m]);\n\nconst METHOD_AV_BG: Record<PaymentMethod, string> = {\n  ach: \"linear-gradient(135deg,#4F8C6B,#2F6448)\",\n  card: \"linear-gradient(135deg,#2A6F77,#0F3A40)\",\n  check: \"linear-gradient(135deg,#9C8074,#5C4034)\",\n  cash: \"linear-gradient(135deg,#E07A8C,#C04060)\",\n  venmo: \"linear-gradient(135deg,#3D95CE,#1F6FA8)\",\n  zelle: \"linear-gradient(135deg,#8E5BD6,#6A2CB8)\",\n  cashapp: \"linear-gradient(135deg,#4FB35F,#2E8B40)\",\n  paypal: \"linear-gradient(135deg,#2A6F9E,#143A6B)\",\n  other: \"linear-gradient(135deg,#9C8074,#5C4034)\",\n};\n\n/** Shared glyph for the peer-to-peer wallets (Venmo/Zelle/Cash App/PayPal):\n *  a phone with a $ — returned as bare SVG children to drop into <I d=…>. */\nfunction P2PIcon() {\n  return (\n    <>\n      <rect x=\"6\" y=\"3\" width=\"12\" height=\"18\" rx=\"2.5\" />\n      <path d=\"M12 6v1.5M12 16.5V18M10 9.5h3a1.5 1.5 0 0 1 0 3h-2a1.5 1.5 0 0 0 0 3h3\" />\n    </>\n  );\n}\n\n/** SVG paths for the inline payment-method icon. Self-contained so the\n *  pph__stub-method line doesn't need a sprite sheet. */\nconst METHOD_ICON: Record<PaymentMethod, preact.JSX.Element> = {\n  ach: (\n    <>\n      <rect x=\"3\" y=\"6\" width=\"18\" height=\"13\" rx=\"2\" />\n      <path d=\"M3 10h18\" />\n      <path d=\"M7 15h4\" />\n    </>\n  ),\n  card: (\n    <>\n      <rect x=\"3\" y=\"5\" width=\"18\" height=\"14\" rx=\"2\" />\n      <path d=\"M3 10h18\" />\n    </>\n  ),\n  check: (\n    <>\n      <rect x=\"2\" y=\"6\" width=\"20\" height=\"12\" rx=\"1.5\" />\n      <path d=\"M2 10h20\" />\n      <path d=\"M14 14h4\" />\n    </>\n  ),\n  cash: (\n    <>\n      <rect x=\"2\" y=\"6\" width=\"20\" height=\"12\" rx=\"2\" />\n      <circle cx=\"12\" cy=\"12\" r=\"3\" />\n      <path d=\"M5 9.5h.01M19 14.5h.01\" />\n    </>\n  ),\n  // Peer-to-peer wallets share a phone-with-$ glyph; the label disambiguates.\n  venmo: <P2PIcon />,\n  zelle: <P2PIcon />,\n  cashapp: <P2PIcon />,\n  paypal: <P2PIcon />,\n  other: (\n    <>\n      <circle cx=\"12\" cy=\"12\" r=\"9\" />\n      <path d=\"M9 12h6M12 9v6\" />\n    </>\n  ),\n};\n\ntype PaymentStatus = \"landed\" | \"transit\" | \"attention\";\n\ninterface EnrichedPayment extends Payment {\n  client: string;\n  initials: string;\n  invoiceRef: string;\n  daysAgo: number;\n  whenLabel: string;\n  note: string;\n  status: PaymentStatus;\n  /** ACH and checks have settling windows. Card/cash are instant. */\n  etaDays?: number;\n}\n\n/** Derive status from method + age. The current backend Payment model\n *  doesn't carry an explicit status; for ACH and checks we apply the\n *  industry-standard settlement windows. Once a payment ages past its\n *  window it counts as \"landed\". */\nconst SETTLE_DAYS: Record<PaymentMethod, number> = {\n  ach: 2, // standard ACH settlement\n  check: 5, // mailed check + deposit clearing\n  card: 0, // captured instantly\n  cash: 0, // instant\n  venmo: 0, // peer-to-peer — funds land instantly\n  zelle: 0, // instant\n  cashapp: 0, // instant\n  paypal: 0, // instant\n  other: 0, // unknown — treat as instant\n};\n\nfunction deriveStatus(\n  method: PaymentMethod,\n  daysAgo: number,\n): { status: PaymentStatus; etaDays?: number } {\n  const settleDays = SETTLE_DAYS[method];\n  if (settleDays > 0 && daysAgo < settleDays) {\n    return { status: \"transit\", etaDays: settleDays - daysAgo };\n  }\n  return { status: \"landed\" };\n}\n\nfunction initialsOf(name: string): string {\n  const parts = name.trim().split(/\\s+/).filter(Boolean);\n  if (parts.length === 0) return \"—\";\n  if (parts.length === 1) return parts[0].slice(0, 2).toUpperCase();\n  return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();\n}\n\nfunction whenLabel(lang: Lang, daysAgo: number): string {\n  if (daysAgo <= 0) return tFor(lang, \"paymentsPage.when.today\");\n  if (daysAgo === 1) return tFor(lang, \"paymentsPage.when.yesterday\");\n  return tFor(lang, \"paymentsPage.when.daysAgo\", { n: daysAgo });\n}\n\nfunction noteFor(\n  lang: Lang,\n  method: PaymentMethod,\n  daysAgo: number,\n  client: string,\n  status: PaymentStatus = \"landed\",\n): string {\n  const name = client.split(/\\s+/)[0];\n  if (status === \"transit\") {\n    if (method === \"ach\") {\n      return tFor(lang, \"paymentsPage.note.transitAch\", { name });\n    }\n    if (method === \"check\") {\n      return tFor(lang, \"paymentsPage.note.transitCheck\", { name });\n    }\n  }\n  switch (method) {\n    case \"ach\":\n      return tFor(lang, \"paymentsPage.note.ach\", { name });\n    case \"card\":\n      return tFor(lang, \"paymentsPage.note.card\", { name });\n    case \"check\":\n      return tFor(lang, \"paymentsPage.note.check\", { name });\n    case \"cash\":\n      return daysAgo === 0\n        ? tFor(lang, \"paymentsPage.note.cashToday\", { name })\n        : tFor(lang, \"paymentsPage.note.cash\", { name });\n    default:\n      return tFor(lang, \"paymentsPage.note.default\", { name });\n  }\n}\n\nfunction enrich(\n  lang: Lang,\n  p: Payment,\n  invoices: Map<string, Invoice>,\n  customers: Map<string, string>,\n  now: Date,\n): EnrichedPayment {\n  const inv = invoices.get(p.invoiceId);\n  const customerId = inv?.customerId;\n  const client = (customerId && customers.get(customerId)) || \"—\";\n  const daysAgo = Math.max(\n    0,\n    Math.floor(\n      (now.getTime() - new Date(p.receivedAt).getTime()) / (24 * 3600 * 1000),\n    ),\n  );\n  const { status, etaDays } = deriveStatus(p.method, daysAgo);\n  return {\n    ...p,\n    client,\n    initials: initialsOf(client),\n    invoiceRef: `INV-${p.invoiceId.slice(0, 6).toUpperCase()}`,\n    daysAgo,\n    whenLabel: whenLabel(lang, daysAgo),\n    note: noteFor(lang, p.method, daysAgo, client, status),\n    status,\n    etaDays,\n  };\n}\n\nexport default function PaymentsPage({ lang: _lang }: { lang?: Lang } = {}) {\n  // Self-source the reactive UI language; reading langSignal.value here makes\n  // this island re-render live when the language flips (Settings change). The\n  // optional `lang` prop is retained only as an ignored SSR seed.\n  const lang = langSignal.value;\n  const [s, setS] = useState<State>(INITIAL);\n\n  useEffect(() => {\n    let alive = true;\n    Promise.all([\n      paymentsClient.list().catch(() => [] as Payment[]),\n      dashboardClient.invoices(undefined).catch(() => [] as Invoice[]),\n      dashboardClient.customers().catch(() => [] as Customer[]),\n    ]).then(([payments, invoices, customers]) => {\n      if (!alive) return;\n      setS({ loading: false, error: null, payments, invoices, customers });\n    }).catch((err: Error) => {\n      if (!alive) return;\n      setS({ ...INITIAL, loading: false, error: err.message });\n    });\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  const customerNames = useMemo(\n    () =>\n      new Map(\n        (Array.isArray(s.customers) ? s.customers : []).map((\n          c,\n        ) => [c.id, c.name]),\n      ),\n    [s.customers],\n  );\n  const invoiceById = useMemo(\n    () =>\n      new Map(\n        (Array.isArray(s.invoices) ? s.invoices : []).map((i) => [i.id, i]),\n      ),\n    [s.invoices],\n  );\n\n  if (s.loading) {\n    return (\n      <>\n        <ShimmerStyle />\n        <PageHeaderSkeleton />\n        <CardGridSkeleton rows={2} />\n      </>\n    );\n  }\n  if (s.error) {\n    return (\n      <div class=\"qpage-error\">\n        {tFor(lang, \"paymentsPage.loadError\", { error: s.error })}\n      </div>\n    );\n  }\n\n  const now = new Date();\n  const enriched = s.payments\n    .map((p) => enrich(lang, p, invoiceById, customerNames, now))\n    .sort((a, b) => a.daysAgo - b.daysAgo);\n\n  const monthCutoff = new Date(now.getFullYear(), now.getMonth(), 1);\n  // Partition by derived status. Landed = settled this month. Transit =\n  // still inside the method's settlement window (ACH < 2d, check < 5d).\n  // Attention is reserved for declined/returned signals once the schema\n  // carries them; right now the section conditionally hides itself.\n  const landed = enriched.filter((p) =>\n    p.status === \"landed\" && new Date(p.receivedAt) >= monthCutoff\n  );\n  const transit = enriched.filter((p) => p.status === \"transit\");\n  const attention = enriched.filter((p) => p.status === \"attention\");\n  const recentLanded = landed.filter((p) => p.daysAgo <= 1);\n  const olderLanded = landed.filter((p) => p.daysAgo > 1);\n\n  const monthTotal = landed.reduce((sum, p) => sum + p.amount, 0);\n  const transitTotal = transit.reduce((sum, p) => sum + p.amount, 0);\n  const attentionTotal = attention.reduce((sum, p) => sum + p.amount, 0);\n\n  // Avg days to pay — over landed payments, average of receivedAt minus invoice issuedDate.\n  const daysToPay = landed.flatMap((p) => {\n    const inv = invoiceById.get(p.invoiceId);\n    if (!inv?.issuedDate) return [];\n    const issued = new Date(inv.issuedDate).getTime();\n    const received = new Date(p.receivedAt).getTime();\n    if (!Number.isFinite(issued) || !Number.isFinite(received)) return [];\n    return [Math.max(0, Math.floor((received - issued) / (24 * 3600 * 1000)))];\n  });\n  const avgDays = daysToPay.length > 0\n    ? Math.round(daysToPay.reduce((s, d) => s + d, 0) / daysToPay.length)\n    : 0;\n\n  // Top three landed for the hero stub stack (rotated cards)\n  const stubs = recentLanded.slice(0, 3);\n\n  return (\n    <>\n      <PaymentsHero\n        lang={lang}\n        monthTotal={monthTotal}\n        transitTotal={transitTotal}\n        attentionCount={attention.length}\n        stubs={stubs}\n      />\n      <PaymentsKpis\n        lang={lang}\n        landedCount={landed.length}\n        monthTotal={monthTotal}\n        transitCount={transit.length}\n        transitTotal={transitTotal}\n        attentionCount={attention.length}\n        attentionTotal={attentionTotal}\n        avgDays={avgDays}\n      />\n\n      <div class=\"qlay\">\n        <div>\n          {attention.length > 0 && (\n            <QuoteTrack\n              num=\"01\"\n              title={tFor(lang, \"paymentsPage.track.attention\")}\n              count={attention.length}\n              unit={tFor(lang, \"paymentsPage.unit.payment\")}\n              defaultOpen\n              storageKey=\"payments:track:attention\"\n            >\n              <div class=\"qcards\">\n                {attention.map((p, i) => (\n                  <PaymentCard key={p.id} lang={lang} p={p} idx={i} />\n                ))}\n              </div>\n            </QuoteTrack>\n          )}\n\n          <QuoteTrack\n            num={attention.length > 0 ? \"02\" : \"01\"}\n            title={tFor(lang, \"paymentsPage.track.landed\")}\n            count={landed.length}\n            unit={tFor(lang, \"paymentsPage.unit.payment\")}\n            defaultOpen\n            storageKey=\"payments:track:landed\"\n          >\n            {recentLanded.length === 0 && olderLanded.length === 0\n              ? (\n                <div style=\"padding:14px 4px;color:var(--fg-muted, #6b7560);font-size:13.5px;line-height:1.5\">\n                  {tFor(lang, \"paymentsPage.track.landedEmpty\")}\n                </div>\n              )\n              : (\n                <>\n                  {recentLanded.length > 0 && (\n                    <div class=\"qcards\">\n                      {recentLanded.map((p, i) => (\n                        <PaymentCard key={p.id} lang={lang} p={p} idx={i} />\n                      ))}\n                    </div>\n                  )}\n                  {olderLanded.length > 0 && (\n                    <div class=\"qdone\" style=\"margin-top:14px\">\n                      {olderLanded.map((p) => (\n                        <LandedRow key={p.id} lang={lang} p={p} />\n                      ))}\n                    </div>\n                  )}\n                </>\n              )}\n          </QuoteTrack>\n\n          {transit.length > 0 && (\n            <QuoteTrack\n              num={attention.length > 0 ? \"03\" : \"02\"}\n              unit={tFor(lang, \"paymentsPage.unit.payment\")}\n              title={tFor(lang, \"paymentsPage.track.transit\")}\n              count={transit.length}\n              defaultOpen={false}\n              storageKey=\"payments:track:transit\"\n            >\n              <div class=\"qcards\">\n                {transit.map((p, i) => (\n                  <PaymentCard key={p.id} lang={lang} p={p} idx={i} />\n                ))}\n              </div>\n            </QuoteTrack>\n          )}\n        </div>\n\n        <aside class=\"qside\">\n          <PSideFlow lang={lang} landedAmounts={landed.map((p) => p.amount)} />\n          <PSideTopPayors lang={lang} landed={landed} />\n          <PSideMix lang={lang} landed={landed} />\n          <PSideTip lang={lang} />\n        </aside>\n      </div>\n    </>\n  );\n}\n\n/* ---------------- Hero ---------------- */\n\nfunction PaymentsHero(\n  { lang, monthTotal, transitTotal, attentionCount, stubs }: {\n    lang: Lang;\n    monthTotal: number;\n    transitTotal: number;\n    attentionCount: number;\n    stubs: EnrichedPayment[];\n  },\n) {\n  const monthName = new Date().toLocaleString(\"en-US\", { month: \"long\" });\n  const fresh = monthTotal === 0 && transitTotal === 0 && attentionCount === 0;\n  return (\n    <header class=\"pph\">\n      <div class=\"pph__main\">\n        <div class=\"pph__eyebrow\">\n          <I d={ICN.check} size={11} sw={3} />{\" \"}\n          {tFor(lang, \"paymentsPage.hero.eyebrow\", { month: monthName })}\n        </div>\n        <h1\n          class=\"pph__title\"\n          style={fresh\n            ? \"font-size:clamp(28px,3.4vw,44px);line-height:1.15\"\n            : \"\"}\n        >\n          {fresh\n            ? (\n              <>\n                {tFor(lang, \"paymentsPage.hero.freshTitlePre\")}{\" \"}\n                <em style=\"color:var(--brand-pink);font-style:normal\">\n                  {tFor(lang, \"paymentsPage.hero.freshTitleEm\")}\n                </em>.\n              </>\n            )\n            : (\n              <>\n                <span class=\"pph__title-amount\">\n                  <sup>$</sup>\n                  {fmtMoney(monthTotal).replace(/^\\$/, \"\")}\n                </span>\n                <span class=\"pph__title-tail\">\n                  {tFor(lang, \"paymentsPage.hero.titleTail\")}\n                </span>\n              </>\n            )}\n        </h1>\n        <p class=\"pph__sub\">\n          {fresh\n            ? <>{tFor(lang, \"paymentsPage.hero.freshSub\")}</>\n            : (\n              <>\n                {transitTotal > 0\n                  ? (\n                    <>\n                      {tFor(lang, \"paymentsPage.hero.subPlus\")}{\" \"}\n                      <strong>{fmtMoney(transitTotal)}</strong>{\" \"}\n                      {tFor(lang, \"paymentsPage.hero.subOnTheWay\")}\n                    </>\n                  )\n                  : <>{tFor(lang, \"paymentsPage.hero.everyDollar\")}</>}\n                {attentionCount > 0\n                  ? (\n                    <>\n                      {\" \"}\n                      {tFor(lang, \"paymentsPage.hero.subAnd\")}{\" \"}\n                      <strong>{attentionCount}</strong>{\" \"}\n                      {tFor(lang, \"paymentsPage.hero.subUnstick\")}\n                    </>\n                  )\n                  : null}\n                {transitTotal > 0\n                  ? <>{tFor(lang, \"paymentsPage.hero.subMonsters\")}</>\n                  : null}\n              </>\n            )}\n        </p>\n        <div class=\"pph__cta-row\">\n          <a\n            class=\"pph__cta\"\n            href={`/assistant?seed=${\n              encodeURIComponent(tFor(lang, \"paymentsPage.hero.recordSeed\"))\n            }`}\n          >\n            <I d={ICN.plus} size={14} sw={2.5} />{\" \"}\n            {tFor(lang, \"paymentsPage.hero.recordCta\")}\n          </a>\n          <a\n            class=\"pph__ghost\"\n            href={`/assistant?seed=${\n              encodeURIComponent(tFor(lang, \"paymentsPage.hero.exportSeed\"))\n            }`}\n          >\n            <I d={ICN.arrow} size={13} sw={2.5} />{\" \"}\n            {tFor(lang, \"paymentsPage.hero.exportCta\")}\n          </a>\n        </div>\n      </div>\n      <div class=\"pph__stack\" aria-hidden=\"true\">\n        {stubs.map((p, i) => (\n          <div key={p.id} class={`pph__stub pph__stub--${i + 1}`}>\n            <div class=\"pph__stub-head\">\n              <div\n                class=\"pph__stub-av\"\n                style={`background:${METHOD_AV_BG[p.method]}`}\n              >\n                {p.initials}\n              </div>\n              <div class=\"pph__stub-meta\">\n                <div class=\"pph__stub-client\">{p.client}</div>\n                <div class=\"pph__stub-when\">{p.whenLabel} · {p.invoiceRef}</div>\n              </div>\n            </div>\n            <div class=\"pph__stub-amount\">{fmtMoney(p.amount)}</div>\n            <div class=\"pph__stub-foot\">\n              <span class=\"pph__stub-method\">\n                <I d={METHOD_ICON[p.method]} size={11} sw={2} />{\" \"}\n                {methodLabel(lang, p.method)}\n              </span>\n              <span class=\"pph__stub-tag\">\n                {tFor(lang, \"paymentsPage.stub.landed\")}\n              </span>\n            </div>\n          </div>\n        ))}\n      </div>\n    </header>\n  );\n}\n\n/* ---------------- KPIs ---------------- */\n\nfunction PaymentsKpis(\n  {\n    lang,\n    landedCount,\n    monthTotal,\n    transitCount,\n    transitTotal,\n    attentionCount,\n    attentionTotal,\n    avgDays,\n  }: {\n    lang: Lang;\n    landedCount: number;\n    monthTotal: number;\n    transitCount: number;\n    transitTotal: number;\n    attentionCount: number;\n    attentionTotal: number;\n    avgDays: number;\n  },\n) {\n  return (\n    <div class=\"qkpi\">\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"paymentsPage.kpi.landedLabel\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(monthTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {tnFor(lang, \"paymentsPage.kpi.payments\", landedCount)}\n        </div>\n      </div>\n      <div class=\"qkpi__cell qkpi__cell--accent\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"paymentsPage.kpi.transitLabel\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(transitTotal)}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"paymentsPage.kpi.onTheWay\", { n: transitCount })}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">\n          {tFor(lang, \"paymentsPage.kpi.attentionLabel\")}\n        </div>\n        <div class=\"qkpi__val\">{attentionCount}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"paymentsPage.kpi.heldUp\", {\n            amount: fmtMoney(attentionTotal),\n          })}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"paymentsPage.kpi.avgLabel\")}</div>\n        <div class=\"qkpi__val\">\n          {avgDays > 0 ? tFor(lang, \"paymentsPage.value.days\", { n: avgDays }) : \"—\"}\n        </div>\n        <div class=\"qkpi__sub\">\n          {avgDays > 0\n            ? tFor(lang, \"paymentsPage.kpi.acrossLanded\")\n            : tFor(lang, \"paymentsPage.kpi.noHistory\")}\n        </div>\n      </div>\n    </div>\n  );\n}\n\n/* ---------------- Payment card (flip) ---------------- */\n\nconst STATUS_MOOD: Record<\n  PaymentStatus,\n  {\n    from: string;\n    to: string;\n    shadow: string;\n    statusFg: string;\n  }\n> = {\n  landed: {\n    from: \"#CFE5C8\",\n    to: \"#5FA34F\",\n    shadow: \"rgba(81,152,67,0.28)\",\n    statusFg: \"#1F3F18\",\n  },\n  transit: {\n    from: \"#C8DDE0\",\n    to: \"#56969E\",\n    shadow: \"rgba(86,150,158,0.28)\",\n    statusFg: \"#0F3036\",\n  },\n  attention: {\n    from: \"#FFD9D9\",\n    to: \"#FF6B6B\",\n    shadow: \"rgba(255,107,107,0.30)\",\n    statusFg: \"#fff\",\n  },\n};\n\nfunction PaymentCard(\n  { lang, p, idx }: { lang: Lang; p: EnrichedPayment; idx: number },\n) {\n  const [flipped, setFlipped] = useState(false);\n  const mood = STATUS_MOOD[p.status];\n  const moodLabel = tFor(lang, `paymentsPage.mood.${p.status}.label`);\n  const moodCta = tFor(lang, `paymentsPage.mood.${p.status}.cta`);\n  return (\n    <article\n      class={`qcard ${flipped ? \"qcard--flipped\" : \"\"}`}\n      onClick={(e) => {\n        if (flipped) return;\n        const t = e.target as HTMLElement;\n        if (t.closest(\".qcard__cta, .qcard__back\")) return;\n        setFlipped(true);\n      }}\n      style={`--mood-from:${mood.from};--mood-to:${mood.to};--mood-shadow:${mood.shadow};--mood-status:${mood.statusFg}`}\n    >\n      <div class=\"qcard__mood\">\n        <div class=\"qcard__numeral\">{String(idx + 1).padStart(2, \"0\")}</div>\n        <div class=\"qcard__status\">\n          <span class=\"qcard__status-dot\" /> {moodLabel}\n        </div>\n        <div class=\"qcard__opens\" style=\"text-transform:uppercase\">\n          <I d={METHOD_ICON[p.method]} size={12} sw={2} />{\" \"}\n          {methodLabel(lang, p.method)}\n        </div>\n      </div>\n      <div class=\"qcard__av\">{p.initials}</div>\n      <div class=\"qcard__body\">\n        <div class=\"qcard__client-name\">{p.client} · {p.invoiceRef}</div>\n        <div class=\"pcard__amount\">{fmtMoney(p.amount)}</div>\n        <p class=\"qcard__story\">{p.note}</p>\n      </div>\n      <div class=\"qcard__foot\">\n        <button\n          type=\"button\"\n          class=\"qcard__cta\"\n          onClick={(e) => e.stopPropagation()}\n        >\n          {moodCta}{\" \"}\n          <span style=\"display:inline-block;transition:transform 240ms\">→</span>\n        </button>\n        <div class=\"qcard__val-wrap\">\n          <div class=\"qcard__val-lbl\">\n            {p.status === \"transit\"\n              ? tFor(lang, \"paymentsPage.card.expected\")\n              : tFor(lang, \"paymentsPage.card.method\")}\n          </div>\n          <div class=\"qcard__val-num\" style=\"font-size:13px\">\n            {p.status === \"transit\" && p.etaDays\n              ? tFor(lang, \"paymentsPage.card.eta\", { n: p.etaDays })\n              : methodLabel(lang, p.method)}\n          </div>\n        </div>\n      </div>\n\n      <div class=\"qcard__back\" aria-hidden={!flipped}>\n        <div class=\"qcard__back-head\">\n          <button\n            type=\"button\"\n            class=\"qcard__back-close\"\n            onClick={(e) => {\n              e.stopPropagation();\n              setFlipped(false);\n            }}\n            aria-label={tFor(lang, \"common.close\")}\n          >\n            <I d={ICN.x} size={14} sw={2.5} />\n          </button>\n          <div class=\"qcard__back-eyebrow\">\n            {tFor(lang, \"paymentsPage.card.trail\")}\n          </div>\n          <p class=\"qcard__back-big\">\n            {fmtMoney(p.amount)}\n            <small>· {methodLabel(lang, p.method)}</small>\n          </p>\n        </div>\n        <div class=\"qcard__back-body\">\n          <p class=\"qcard__read\">{p.note}</p>\n        </div>\n        <div class=\"qcard__back-foot\">\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"paymentsPage.card.receipt\")}\n          </button>\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"paymentsPage.card.matchInvoice\")}\n          </button>\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"paymentsPage.card.textClient\")}\n          </button>\n        </div>\n      </div>\n    </article>\n  );\n}\n\n/* ---------------- Landed row (compact tail of month) ---------------- */\n\nfunction LandedRow({ lang, p }: { lang: Lang; p: EnrichedPayment }) {\n  return (\n    <div class=\"qdone__row\">\n      <div class=\"qdone__badge qdone__badge--won\">\n        <I d={ICN.check} size={13} sw={2.5} />\n      </div>\n      <div class=\"qdone__body\">\n        <div class=\"qdone__title\">{p.client}</div>\n        <div class=\"qdone__sub\">\n          {methodLabel(lang, p.method)} · {p.whenLabel} · {p.invoiceRef}\n        </div>\n      </div>\n      <div class=\"qdone__amt\">{fmtMoney(p.amount)}</div>\n    </div>\n  );\n}\n\n/* ---------------- Side rail components ---------------- */\n\nfunction PSideFlow(\n  { lang, landedAmounts }: { lang: Lang; landedAmounts: number[] },\n) {\n  // Split landed into 12 weekly buckets. Real history would key off\n  // receivedAt; for now we just slot in the rolling totals.\n  const weeks = new Array(12).fill(0);\n  landedAmounts.forEach((a, i) => {\n    weeks[11 - (i % 12)] += a;\n  });\n  const hasData = weeks.some((v) => v > 0);\n  if (!hasData) {\n    return (\n      <div class=\"qside__card\">\n        <div class=\"qside__title\">{tFor(lang, \"paymentsPage.flow.title\")}</div>\n        <div class=\"qside__sub\" style=\"margin:2px 0 12px\">\n          {tFor(lang, \"paymentsPage.flow.subEmpty\")}\n        </div>\n        <div style=\"font-size:13px;color:var(--fg-muted);line-height:1.45\">\n          {tFor(lang, \"paymentsPage.flow.empty\")}\n        </div>\n      </div>\n    );\n  }\n  const max = Math.max(1, ...weeks);\n  const w = 220, h = 60, pad = 4;\n  const pts = weeks.map((v, i) => {\n    const x = pad + (i * (w - pad * 2) / Math.max(1, weeks.length - 1));\n    const y = h - pad - ((v / max) * (h - pad * 2));\n    return [x, y] as const;\n  });\n  const path = pts.map((\n    pt,\n    i,\n  ) => (i === 0 ? `M${pt[0]},${pt[1]}` : `L${pt[0]},${pt[1]}`)).join(\" \");\n  const area = `${path} L${pts[pts.length - 1][0]},${h} L${pts[0][0]},${h} Z`;\n  return (\n    <div class=\"qside__card\">\n      <div class=\"qside__title\">{tFor(lang, \"paymentsPage.flow.title\")}</div>\n      <div class=\"qside__sub\" style=\"margin:2px 0 12px\">\n        {tFor(lang, \"paymentsPage.flow.subThisWeek\", {\n          amount: fmtMoney(weeks[weeks.length - 1]),\n        })}\n      </div>\n      <svg width={w} height={h} style=\"display:block;width:100%;height:auto\">\n        <defs>\n          <linearGradient id=\"cfArea\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\n            <stop offset=\"0%\" stopColor=\"#FF6B6B\" stopOpacity=\"0.32\" />\n            <stop offset=\"100%\" stopColor=\"#FF6B6B\" stopOpacity=\"0\" />\n          </linearGradient>\n        </defs>\n        <path d={area} fill=\"url(#cfArea)\" />\n        <path\n          d={path}\n          fill=\"none\"\n          stroke=\"#FF6B6B\"\n          stroke-width=\"2\"\n          stroke-linecap=\"round\"\n          stroke-linejoin=\"round\"\n        />\n        {pts.map((pt, i) => (\n          <circle\n            key={i}\n            cx={pt[0]}\n            cy={pt[1]}\n            r={i === pts.length - 1 ? 3 : 1.5}\n            fill=\"#FF6B6B\"\n          />\n        ))}\n      </svg>\n      <div style=\"display:flex;justify-content:space-between;font-size:11px;color:var(--fg-muted);margin-top:8px;font-family:var(--font-body)\">\n        <span>{tFor(lang, \"paymentsPage.flow.feb\")}</span>\n        <span>{tFor(lang, \"paymentsPage.flow.mar\")}</span>\n        <span>{tFor(lang, \"paymentsPage.flow.apr\")}</span>\n      </div>\n    </div>\n  );\n}\n\nfunction PSideTopPayors(\n  { lang, landed }: { lang: Lang; landed: EnrichedPayment[] },\n) {\n  const tally = new Map<string, number>();\n  for (const p of landed) {\n    tally.set(p.client, (tally.get(p.client) ?? 0) + p.amount);\n  }\n  const top = [...tally.entries()].sort((a, b) => b[1] - a[1]).slice(0, 4);\n  const max = top[0]?.[1] ?? 1;\n  return (\n    <div class=\"qside__card\">\n      <div class=\"qside__title\">{tFor(lang, \"paymentsPage.payors.title\")}</div>\n      <div class=\"qside__sub\" style=\"margin:2px 0 14px\">\n        {tFor(lang, \"paymentsPage.payors.sub\")}\n      </div>\n      {top.length === 0\n        ? (\n          <div style=\"font-size:13px;color:var(--fg-muted)\">\n            {tFor(lang, \"paymentsPage.payors.empty\")}\n          </div>\n        )\n        : (\n          <div class=\"qside__rows\">\n            {top.map(([client, amt], i) => (\n              <div key={client} class=\"qside__row\">\n                <div class=\"qside__rank\">{String(i + 1).padStart(2, \"0\")}</div>\n                <div class=\"qside__row-body\">\n                  <div class=\"qside__row-name\">{client}</div>\n                  <div class=\"qside__bar\">\n                    <div\n                      class=\"qside__bar-fill\"\n                      style={`width:${\n                        (amt / max) * 100\n                      }%;background:linear-gradient(90deg,#4F8C6B,#2F6448)`}\n                    />\n                  </div>\n                </div>\n                <div class=\"qside__row-amt\">{fmtMoney(amt)}</div>\n              </div>\n            ))}\n          </div>\n        )}\n    </div>\n  );\n}\n\nfunction PSideMix({ lang, landed }: { lang: Lang; landed: EnrichedPayment[] }) {\n  const tally: Record<PaymentMethod, number> = {\n    ach: 0,\n    card: 0,\n    check: 0,\n    cash: 0,\n    venmo: 0,\n    zelle: 0,\n    cashapp: 0,\n    paypal: 0,\n    other: 0,\n  };\n  for (const p of landed) tally[p.method] += p.amount;\n  const total = Object.values(tally).reduce((s, v) => s + v, 0);\n  const colors: Record<PaymentMethod, string> = {\n    ach: \"#4F8C6B\",\n    card: \"#2A6F77\",\n    check: \"#9C8074\",\n    cash: \"#E07A8C\",\n    venmo: \"#3D95CE\",\n    zelle: \"#8E5BD6\",\n    cashapp: \"#4FB35F\",\n    paypal: \"#2A6F9E\",\n    other: \"#6b7560\",\n  };\n  const segments = (Object.keys(tally) as PaymentMethod[])\n    .map((m) => ({ method: m, pct: total > 0 ? (tally[m] / total) * 100 : 0 }))\n    .filter((s) => s.pct > 0);\n  return (\n    <div class=\"qside__card\">\n      <div class=\"qside__title\">{tFor(lang, \"paymentsPage.mix.title\")}</div>\n      <div class=\"qside__sub\" style=\"margin:2px 0 14px\">\n        {tFor(lang, \"paymentsPage.mix.sub\")}\n      </div>\n      {segments.length === 0\n        ? (\n          <div style=\"font-size:13px;color:var(--fg-muted)\">\n            {tFor(lang, \"paymentsPage.mix.empty\")}\n          </div>\n        )\n        : (\n          <>\n            <div style=\"display:flex;height:14px;border-radius:999px;overflow:hidden;box-shadow:inset 0 1px 2px rgba(0,0,0,0.06);margin-bottom:14px\">\n              {segments.map((s) => (\n                <div\n                  key={s.method}\n                  style={`width:${s.pct}%;background:${colors[s.method]}`}\n                />\n              ))}\n            </div>\n            <div style=\"display:grid;grid-template-columns:1fr 1fr;gap:8px 14px\">\n              {segments.map((s) => (\n                <div\n                  key={s.method}\n                  style=\"display:flex;align-items:center;gap:7px;font-family:var(--font-body);font-size:12px\"\n                >\n                  <span\n                    style={`width:10px;height:10px;border-radius:3px;background:${\n                      colors[s.method]\n                    };flex-shrink:0`}\n                  />\n                  <span style=\"color:var(--brand-teal);font-weight:700\">\n                    {methodLabel(lang, s.method)}\n                  </span>\n                  <span style=\"color:var(--fg-muted);margin-left:auto\">\n                    {tFor(lang, \"paymentsPage.mix.pct\", {\n                      n: Math.round(s.pct),\n                    })}\n                  </span>\n                </div>\n              ))}\n            </div>\n          </>\n        )}\n    </div>\n  );\n}\n\nfunction PSideTip({ lang }: { lang: Lang }) {\n  return (\n    <div\n      class=\"qside__card\"\n      style=\"background:linear-gradient(135deg,#1A535C,#0F3A40);color:#fff;border:none\"\n    >\n      <div class=\"qside__title\" style=\"color:#fff;margin-bottom:8px\">\n        {tFor(lang, \"paymentsPage.tip.title\")}\n      </div>\n      <p style=\"margin:0;font-family:var(--font-body);font-size:13.5px;line-height:1.55;color:rgba(255,255,255,0.88)\">\n        <strong style=\"color:#fff\">\n          {tFor(lang, \"paymentsPage.tip.zelleCashApp\")}\n        </strong>{\" \"}\n        {tFor(lang, \"paymentsPage.tip.mid\")}{\" \"}\n        <strong style=\"color:#fff\">\n          {tFor(lang, \"paymentsPage.tip.upTo5\")}\n        </strong>{\" \"}\n        {tFor(lang, \"paymentsPage.tip.end\")}\n      </p>\n    </div>\n  );\n}\n", "sourceName": "PaymentsPage.tsx", "checklist": "# Capture checklist — PaymentsPage\n\n**Theme:** light only (no dark mode exists).\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/payments` — the whole-page island\n  (`routes/payments/index.tsx` → `islands/PaymentsPage.tsx`). It self-fetches on\n  mount (`/payments`, `/invoices`, `/customers` via the dashboard client), so\n  whatever state the seeded backend returns is what you'll see.\n\n## Viewports (own + composed CSS breakpoints)\nDrive **all** of these — the page composes quotes.css + payments.css media\nqueries, which fire at different widths:\n- **1280px** — full two-column `.qlay` (main + sticky `.qside` rail), KPI 4-up.\n- **1200px** — `.qlay`→1 col, rail goes static (drops below), `.qkpi`→2-up.\n- **1100px** — `.qdone` tail grid →1 col (landed-row.css).\n- **768px** — `.qcards` + `.qkpi` →1 col.\n- **760px** — `.pph` hero →1 col; the stub stack re-fans vertically\n  (payments.css `@media (max-width:760px)`).\n- **560px** — `.qdone__row` tighter, `.qdone__when` hidden.\n\n## Element(s) to crop\n- **Full page** (hero → KPIs → tracks → rail) at 1280px and 760px.\n- The **hero band** (`.pph`) alone — both the populated `$X showed up` variant\n  and the `fresh` empty variant.\n- The **`.qlay`** two-column grid (to show the sticky rail beside the tracks).\n\n## Transient states to drive (one screenshot each)\n1. **loading** — throttle the network (or use a seed account whose fetches are\n   slow); the Skeletons branch (ShimmerStyle + PageHeaderSkeleton +\n   CardGridSkeleton rows=2) shows before the Promise.all resolves.\n2. **error** — force a fetch to throw inside the `.then` (devtools request\n   block on `/payments`, then reload). Confirm `.qpage-error` renders the\n   `paymentsPage.loadError` copy — note it is **UNSTYLED** (plain body text, no\n   alert role) — this is a real visible bug to document, not fabricate.\n3. **fresh** — a brand-new / wiped account (`GET /me/wipe`) with **0 payments**:\n   the hero shows the smaller \"Nothing's landed yet — *let's change that*.\"\n   variant, KPIs read `$0` / `—`, only the Just-landed track renders with its\n   `landedEmpty` hint, and every side widget shows its own empty copy.\n4. **landed-only** — a seed with several settled payments this month: recent\n   (≤1d) ones become `.qcard` flip cards + populate the hero stub stack; older\n   ones become `.qdone` LandedRow rows. transit/attention tracks absent.\n5. **with-transit** — include an ACH received <2d ago and/or a check received\n   <5d ago: the **In transit** track appears (collapsed by default — click to\n   expand), the pink-accent In-transit KPI cell shows a non-zero total, and the\n   hero sub gains the \"Plus $X on the way\" line.\n\n## Interactions to exercise (for the composed children — not page-level)\n- Click a `.qtrack__head` to collapse/expand a track (QuoteTrack — persists via\n  localStorage `payments:track:*`).\n- Click a `.qcard` body to flip it (PaymentCard back face). Click the back-X to\n  flip back. Note the front/back action buttons are **no-ops**\n  (`stopPropagation` only).\n- Hover `.pph__stack` to see the three stub cards lift in unison.\n\n## Motion to film\n- **Hero stub stack hover** — the three `.pph__stub--1/2/3` translate up\n  together over `280ms var(--ease-bounce)` (payments-hero).\n- **Card flip** — `.qcard__back` slides+scales in over `380ms var(--ease-bounce)`\n  (payment-card).\n- **Track collapse/expand** — chevron rotate + body grid-rows (QuoteTrack).\n- Re-shoot one flip/collapse with `prefers-reduced-motion: reduce` to confirm\n  the global tokens clamp stills it (no component-local guard).\n\n## Notes / anti-patterns to capture as evidence\n- The page is a **whole-page island with frozen SSR props** (only `lang`) that\n  **fetches on mount** — there is a visible empty-then-pop on first paint.\n  (Documented fix in payments-page.md §1: SSR a hydration seed.)\n- **No `location.reload()`** anywhere on this page (read-only) — confirm by\n  watching the network tab while clicking around: no full navigations.\n- The **Top-payors** rail widget renders **unstyled** (undefined `.qside__*`\n  classes) — crop it to evidence the bug (see payment-side-rail.md).\n"}]}, {"name": "public-change-order", "pageMd": "# Page: `/co/:id` — Public change order\n\n**Route:** `routes/co/[id].tsx` · **Source copied to:** `js/[id].tsx`. Island in\n`components/public-change-order-actions/`.\n\n## Purpose\nCustomer-facing document for a contract **change order** (scope/price change),\nopened from an SMS/email link. Customer reviews the delta and approves/declines.\n\n## Classification & data flow\n- **Page tier:** **SSR document** (`define.page` async) + one action island.\n  No auth — a token-shaped `:id` link.\n- **Data (server):** `ssrBackendGet<ChangeOrderPublic>('/change-orders/:id/public')`.\n  Shape: `{ id, description, deltaAmountCents, status: pending|approved|declined,\n  currentAmount?, newAmount?, businessName?, commsLanguage?, decidedAt? }`.\n  On `!ok` → renders an **error card** (link expired / not available).\n- **Language:** `commsLanguage === \"es\" ? es : en` (contractor's outgoing-comms\n  language). All copy via `tFor(lang, \"changeOrderPublic.*\")`.\n- **Money:** local `money(cents)` helper (`$x,xxx.xx`); `deltaAmountCents` is\n  **cents**; `currentAmount`/`newAmount` passed to `money()` as cents too.\n- **Surface:** the **public inline-styled palette** (NOT Sabor tokens) — see\n  `design-tokens.md` § Public surface. Local consts: `TEAL #144852`,\n  `GREEN #519843`, `INK #1c2c30`, `MUTED #6b7a7e`, `LINE #e3e8e6`,\n  `CREAM #fffdf7`, `BG #f7f6f1`, `PINK #FF6B6B`, `PINK_DARK #d94e4e`. System\n  font stack. Loads `/landing.css` only for resets.\n\n## `<Head>`\n`<title>{changeOrderPublic.docTitle}</title>` · `<link href=\"/landing.css\">`.\n\n## Layout / composition order\n```\ndiv (BG #f7f6f1, min-h 100dvh, kb-inset aware padding)\n  div max-width:560px\n    !co → error card (white, heading + message)\n    co  → article (CREAM, radius 24, pink-bordered, shadow)\n            div  8px top bar  → linear-gradient(PINK → PINK_DARK)\n            padding 32/36\n              eyebrow  businessName (PINK_DARK, uppercase, tracked)\n              h1   changeOrderPublic.heading  (TEAL, 28px, 900)\n              p    changeOrderPublic.intro     (MUTED)\n              section (\"What's changing\", white card):\n                label  whatsChanging\n                p      co.description\n                breakdown (top-bordered):\n                  currentTotal / previousTotal (when status===\"approved\")  → money(currentAmount)\n                  added / credit  (sign by deltaAmountCents ≥ 0)           → ±money(|delta|)  (credit in GREEN)\n                  newTotal / proposedTotal (when status===\"declined\")      → money(newAmount) (declined in MUTED, else GREEN)\n              <PublicChangeOrderActions changeOrderId initialStatus lang />  ← ISLAND\n```\nNote the status-aware wording: approved shows \"previous total\", declined frames\nthe new total as a \"proposed total\" that never took effect.\n\n## Components\n| Component | Folder | Tier |\n|---|---|---|\n| `PublicChangeOrderActions` | `components/public-change-order-actions/` | island — approve/decline buttons → POST; confirms in place; `initialStatus` seeds the rendered state. |\n\n## Data-shape hazard\nThe `/public` read is a composite join (change order → contract → invoice\nsnapshot) fanned out per customer link-load — flag for caching/point-read.\n\n## Capture checklist (auth-free, but needs a valid token id)\n- URL: `/co/<changeOrderId>` (real id; otherwise error card renders — itself a\n  capturable state).\n- Viewports: 390 + 640 (single-column, max-width 560). Light theme.\n- States: pending (action buttons), approved, declined, expired/error, `es`.\n\n## Build order\npublic palette primitives → `public-change-order-actions` island → this page.\n", "components": [{"name": "public-change-order-actions", "component": "PublicChangeOrderActions", "spec": "# PublicChangeOrderActions\n\nThe customer-facing **Approve / Decline** control on the public change-order page\n(`/co/:id`). The SSR route renders the document (eyebrow, heading, \"what's\nchanging\" card, the money breakdown band); this island is mounted at the foot of\nthat card and lets the homeowner decide. Approving applies the delta to the\nlinked invoice **server-side** (the backend refuses to mark the order approved if\nthat invoice write fails).\n\nSource: `islands/PublicChangeOrderActions.tsx` (copied to\n`js/PublicChangeOrderActions.tsx`, 117 lines). Mounted by `routes/co/[id].tsx`.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PublicChangeOrderActions.tsx`).\n- **Interaction tier:** `island` — client state + a `fetch` POST mutation. NOT a\n  Fresh Partial, NOT an SSR form/PRG. The two buttons are `<button type=\"button\">`\n  wired to an `onClick` that calls `fetch` directly.\n- **Surface:** **public palette** (inline hex literals — `#519843` green,\n  `#1c2c30` ink, `#6b7a7e` muted, `#e3e8e6` hairline, `#a83b3b` declined-text),\n  NOT Sabor tokens. The host page loads `/landing.css` only for resets.\n- **Client state owned:**\n  - `status: \"pending\" | \"approved\" | \"declined\"` — seeded from the\n    `initialStatus` prop (the SSR'd `ChangeOrderPublic.status`). This is the\n    terminal latch: once not `\"pending\"`, the action buttons are gone and a\n    result panel renders instead.\n  - `busy: \"approve\" | \"decline\" | null` — which action is in flight (default\n    `null`). Disables BOTH buttons while non-null.\n  - `error: string | undefined` — inline error text under the buttons.\n- **Server mutation (action endpoint) + feedback:**\n  - **Approve:** `POST /api/change-orders/:id/approve` (same-origin Fresh `/api`\n    proxy → backend `POST /change-orders/:id/approve`), `credentials:\"include\"`,\n    no body. On `res.ok` → `setStatus(\"approved\")` → swaps to the green approved\n    panel.\n  - **Decline:** `POST /api/change-orders/:id/decline` (no body). On ok →\n    `setStatus(\"declined\")` → declined panel.\n  - **Flash:** inline only. While in flight the clicked button's label swaps to\n    its busy copy (Approve → `publicChangeOrderActions.approving` \"Approving…\";\n    Decline → literal `\"…\"`) and both buttons are `disabled`. No toast, no\n    redirect, no flash cookie.\n- **Logical-failure mapping (leak prevention):** on a non-`ok` response the\n  island parses `body.reason`. If `reason === \"invoice_update_failed\"` it shows\n  the localized `publicChangeOrderActions.applyError` (\"We couldn't update the\n  invoice — nothing was changed…\") rather than echoing the raw reason; any other\n  `reason` is shown as-is, else falls back to\n  `publicChangeOrderActions.submitError` (\"Couldn't submit ({status})\"). On a\n  thrown/network error, the caught `err.message` is shown.\n- **`location.reload()` FLAG:** **None — and that is correct.** State transitions\n  are pure in-memory `useState` (the `status` latch), and the result panels are\n  rendered client-side from that state. This is the GOOD pattern (contrast\n  `PublicSignContract`, which DOES `location.reload()` after sign). Do NOT\n  introduce a reload here on rebuild; the approved/declined panels already convey\n  the outcome without re-hitting the server.\n- **Liveness:** request-response only. No polling, no websocket, no SSE.\n- **Data source / honest-empty:** the island owns no fetch on mount — all display\n  data (the money band) is SSR'd by the route; this island only receives\n  `changeOrderId`, `initialStatus`, `lang` as props. If the route can't resolve\n  the change order it renders its own error card and never mounts this island. If\n  `initialStatus` is already `approved`/`declined` (link re-opened after a\n  decision), the island mounts straight into the terminal panel — no buttons.\n- **Reactivity / i18n:** `lang` is a **prop** (`\"en\"|\"es\"`, default `\"en\"`),\n  resolved by the route from `co.commsLanguage` (the contractor's outgoing-comms\n  language). All strings via `tFor(lang, key, vars)`. There is **no on-page\n  language toggle**, so this island is NOT reactive to `langSignal` — it renders\n  in the document's fixed language.\n- **Data-shape hazards:** none of its own (it consumes scalars). The page it\n  lives on inherits data-model.md hazard #7 — `GET /change-orders/:id/public`\n  is a customer-facing join hit from SMS/email links; the approve path writes the\n  delta onto the bound invoice and only then flips status\n  (\"invoice_update_failed\" guards that ordering). The id passed here\n  (`co.id`) is the only FK the island sees; `userId`/`contractId`/`invoiceId`\n  are server-side and never reach the client.\n\n## 2. Anatomy (100% inline styles — no CSS classes)\n```\nstatus !== \"pending\"  →  result panel (terminal)\n<div style=\"margin-top:24px;\n            background: approved ? rgba(81,152,67,0.08) : rgba(168,59,59,0.06);\n            border:1px solid #e3e8e6; border-radius:14px; padding:20px 24px\">\n  <div eyebrow  style=\"font:11px/800; letter-spacing:.14em; uppercase;\n                       color: approved ? #519843 : #a83b3b\">\n     { approved ? publicChangeOrderActions.approvedLabel \"Approved\"\n                : status.declined \"Declined\" }\n  </div>\n  <p style=\"margin:8px 0 0; color:#1c2c30; font:15px/1.55\">\n     { approved ? publicChangeOrderActions.approvedMessage\n                : publicChangeOrderActions.declinedMessage }\n  </p>\n</div>\n\nstatus === \"pending\"  →  action stack\n<div style=\"margin-top:24px; display:flex; flex-direction:column; gap:12px\">\n  <button approve  type=button  disabled={busy!==null}\n     style=\"border:0; border-radius:12px; padding:15px 18px; background:#519843;\n            color:#fff; font:16px/800; cursor:pointer\">\n     { busy===\"approve\" ? \"Approving…\" : \"Approve this change\" }\n  </button>\n  <button decline  type=button  disabled={busy!==null}\n     style=\"border:1px solid #e3e8e6; border-radius:12px; padding:13px 18px;\n            background:#fff; color:#6b7a7e; font:15px/700; cursor:pointer\">\n     { busy===\"decline\" ? \"…\" : \"Decline\" }\n  </button>\n  {error && <p role=\"alert\" style=\"color:#a83b3b; font:13px; margin:2px 0 0\">{error}</p>}\n</div>\n```\n- **Button hierarchy (matches global rule):** one solid green primary (Approve),\n  one outlined-on-white secondary (Decline). Good — no two competing gradient\n  CTAs.\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `changeOrderId` | `string` (required) | — | text | no |\n| `initialStatus` | `\"pending\"\\|\"approved\"\\|\"declined\"` (required) | — | select | no (seeds `status` state) |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n`busy` and `error` are internal `useState`, not props — drive via Events / case\n`_signals`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| pending | idle — Approve + Decline buttons | `cases/pending/pending.json` |\n| approving | Approve clicked, POST in flight (both disabled, \"Approving…\") | `cases/approving/approving.json` |\n| approved | terminal — green panel \"Approved\" + approvedMessage | `cases/approved/approved.json` |\n| declined | terminal — pink-tinted panel \"Declined\" + declinedMessage | `cases/declined/declined.json` |\n| error | inline red `role=\"alert\"` (e.g. invoice_update_failed → applyError) | `cases/error/error.json` |\n| es | Spanish strings throughout | `cases/es/es.json` |\n\n`status` is seeded by `initialStatus`; isolate drives the terminal panels by\nsetting `initialStatus` to `\"approved\"`/`\"declined\"`, and the transient\nbusy/error via `_signals`.\n\n## 5. Events (`capture(page)` predicates)\n- `ev.expect(e => e.source === \"button\" /* Approve */ && e.type === \"click\")`\n  → `decide(\"approve\")` → `setBusy(\"approve\")` → `POST /api/change-orders/:id/approve`\n  → on ok `setStatus(\"approved\")`; on fail `setError(...)`, `setBusy(null)`.\n- `ev.expect(e => e.source === \"button\" /* Decline */ && e.type === \"click\")`\n  → `decide(\"decline\")` → `POST /api/change-orders/:id/decline` → `setStatus(\"declined\")`.\n- Re-entrancy guard: `decide()` early-returns while `busy` is non-null, so a\n  second click is ignored — preserve this (the only double-submit guard).\n- (Neither button carries a stable class/id — capture by tag + label text /\n  position. Rebuild SHOULD add a stable selector, e.g. `data-cy`, for testability;\n  the sibling `PublicInvoiceClaim` already uses `data-cy` hooks.)\n\n## 6. Motion (real CSS only)\n- **None declared.** No `@keyframes`, no `transition`. The pending→terminal swap\n  and the disabled state flip are instant (the label text just changes, the\n  buttons gain `disabled`). Nothing to film; no reduced-motion concern of its own.\n- The decorative pink top-bar / shadows belong to the SSR route document, not\n  this island.\n\n## 7. Responsive\n- No own `@media`. The action stack is `display:flex; flex-direction:column`\n  (full-width buttons), inside the route's `max-width:560px` document column with\n  `padding:32px 16px`. Buttons are full-width at every breakpoint, so it reflows\n  cleanly on phones with no query needed.\n\n## 8. A11y\n- The inline error is `role=\"alert\"` — good (announced on failure).\n- Both controls are real `<button type=\"button\">` with `disabled` while busy.\n- **Gaps:**\n  - The pending→terminal transition moves no focus and the result panel has no\n    `role=\"status\"`/`aria-live`, so a screen-reader user who triggered Approve\n    isn't told the outcome. Rebuild: announce the result panel (and/or move focus\n    to it).\n  - The busy state communicates only via the label text swap + `disabled`; no\n    `aria-busy` on the button. Add `aria-busy={busy!==null}`.\n  - Buttons have no `aria-label`/stable hook; the \"…\" decline-busy label is\n    meaningless to SR users (give Decline a busy label string like Approve's).\n\n## 9. Used on\n- `/co/:id` only — mounted by `routes/co/[id].tsx` at the bottom of the\n  change-order document `<article>`, passed `changeOrderId={co.id}`,\n  `initialStatus={co.status}`, `lang` (from `co.commsLanguage`). Sole importer.\n", "fixture": "{\n  \"category\": \"public-change-order\",\n  \"folder\": \"public-change-order-actions\",\n  \"controls\": {\n    \"changeOrderId\": { \"type\": \"text\", \"value\": \"co_7d2f1a9b3c4e5f60\" },\n    \"initialStatus\": { \"type\": \"select\", \"value\": \"pending\", \"options\": [\"pending\", \"approved\", \"declined\"] },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "approved.json", "content": "{\n  \"_name\": \"Approved — terminal green panel (rgba(81,152,67,0.08) bg): eyebrow 'Approved' (#519843) + 'Thanks! Your updated invoice total reflects this change.' No buttons.\",\n  \"changeOrderId\": \"co_7d2f1a9b3c4e5f60\",\n  \"initialStatus\": \"approved\",\n  \"lang\": \"en\",\n  \"_signals\": { \"status\": \"approved\", \"busy\": null },\n  \"_note\": \"Reached either by clicking Approve (status latches in-memory, NO reload) or by re-opening the link after a prior approval (initialStatus seeds status='approved' on mount).\"\n}\n"}, {"name": "approving.json", "content": "{\n  \"_name\": \"Approving — POST /approve in flight; green button reads 'Approving…', both buttons disabled.\",\n  \"changeOrderId\": \"co_7d2f1a9b3c4e5f60\",\n  \"initialStatus\": \"pending\",\n  \"lang\": \"en\",\n  \"_signals\": { \"status\": \"pending\", \"busy\": \"approve\" },\n  \"_note\": \"busy='approve' disables BOTH buttons (disabled={busy!==null}). The Decline button stays its idle 'Decline' label while Approve shows 'Approving…'. Drive by stalling the /api/change-orders/:id/approve mock.\"\n}\n"}, {"name": "declined.json", "content": "{\n  \"_name\": \"Declined — terminal pink-tinted panel (rgba(168,59,59,0.06) bg): eyebrow 'Declined' (#a83b3b) + 'No problem — we've let your contractor know.' No buttons.\",\n  \"changeOrderId\": \"co_7d2f1a9b3c4e5f60\",\n  \"initialStatus\": \"declined\",\n  \"lang\": \"en\",\n  \"_signals\": { \"status\": \"declined\", \"busy\": null }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — approve POST returned non-ok with reason='invoice_update_failed'; inline red role='alert' shows the localized applyError ('We couldn't update the invoice — nothing was changed…'), buttons re-enabled for retry.\",\n  \"changeOrderId\": \"co_7d2f1a9b3c4e5f60\",\n  \"initialStatus\": \"pending\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"status\": \"pending\",\n    \"busy\": null,\n    \"error\": \"We couldn't update the invoice — nothing was changed. Please try again or contact your contractor.\"\n  },\n  \"_note\": \"The raw backend reason 'invoice_update_failed' is mapped to the friendly applyError string — the raw token is never shown to the homeowner. Other reasons surface as-is; missing reason falls back to submitError 'Couldn't submit ({status})'. After error, busy resets to null so both buttons are clickable again.\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — lang='es' (resolved from co.commsLanguage='es'); buttons 'Aprobar este cambio' / 'Rechazar'. Approving label 'Aprobando…'.\",\n  \"changeOrderId\": \"co_7d2f1a9b3c4e5f60\",\n  \"initialStatus\": \"pending\",\n  \"lang\": \"es\",\n  \"_signals\": { \"status\": \"pending\", \"busy\": null },\n  \"_note\": \"Terminal panels in es: approvedLabel 'Aprobado' + '¡Gracias! El total actualizado de tu factura refleja este cambio.'; declined reuses status.declined 'Rechazado' + 'No hay problema. Ya le avisamos a tu contratista.'\"\n}\n"}, {"name": "pending.json", "content": "{\n  \"_name\": \"Pending — idle action stack: solid green 'Approve this change' over outlined white 'Decline'. No error.\",\n  \"changeOrderId\": \"co_7d2f1a9b3c4e5f60\",\n  \"initialStatus\": \"pending\",\n  \"lang\": \"en\",\n  \"_signals\": { \"status\": \"pending\", \"busy\": null }\n}\n"}], "css": "/* public-change-order-actions.css — EXTRACTED real CSS.\n *\n * The change-order actions island is 100% inline-styled and emits NO <style>\n * and uses NO CSS classes — only inline `style=` strings. There is genuinely no\n * extractable rule. Recorded for completeness + the inline structure key.\n * Surface: PUBLIC palette (inline hex, NOT Sabor tokens). The host route\n * /co/:id loads /landing.css only for resets; this island links nothing.\n *\n * Public palette literals used (from js/PublicChangeOrderActions.tsx):\n *   GREEN     #519843   approve button bg / approved eyebrow\n *   INK       #1c2c30   result-panel body text\n *   MUTED     #6b7a7e   decline button text\n *   LINE      #e3e8e6   decline border + result-panel border\n *   declined  #a83b3b   declined eyebrow + error text\n *\n * Action stack (status === \"pending\"):\n *   wrapper : margin-top:24px; display:flex; flex-direction:column; gap:12px\n *   approve : border:0; border-radius:12px; padding:15px 18px; background:#519843;\n *             color:#fff; font:inherit 16px/800; cursor:pointer\n *   decline : border:1px solid #e3e8e6; border-radius:12px; padding:13px 18px;\n *             background:#fff; color:#6b7a7e; font:inherit 15px/700; cursor:pointer\n *   both    : disabled while busy !== null (disabled attr only — no style change\n *             beyond the UA default; label text swaps instead)\n *   error   : <p role=\"alert\"> color:#a83b3b; font-size:13px; margin:2px 0 0\n *\n * Result panel (status !== \"pending\", terminal):\n *   wrapper : margin-top:24px; border:1px solid #e3e8e6; border-radius:14px;\n *             padding:20px 24px;\n *             bg = approved ? rgba(81,152,67,0.08) : rgba(168,59,59,0.06)\n *   eyebrow : font-size:11px; font-weight:800; letter-spacing:.14em; uppercase;\n *             color = approved ? #519843 : #a83b3b\n *   body    : margin:8px 0 0; color:#1c2c30; font-size:15px; line-height:1.55\n *\n * No motion: NO @keyframes, NO transition declared. State swaps are instant.\n * No @media: full-width column buttons inside the route's 560px doc column —\n *   reflows without a query. No reduced-motion concern.\n *\n * NOTE: unlike the sibling PublicInvoiceClaim, this island has NO data-cy/test\n * hooks and no stable class/id on the buttons — capture them by tag + label\n * text. Rebuild should add stable selectors for testability.\n */\n", "cssName": "public-change-order-actions.css", "source": "import { useState } from \"preact/hooks\";\nimport { tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  changeOrderId: string;\n  initialStatus: \"pending\" | \"approved\" | \"declined\";\n  lang?: \"en\" | \"es\";\n}\n\nconst GREEN = \"#519843\";\nconst INK = \"#1c2c30\";\nconst MUTED = \"#6b7a7e\";\nconst LINE = \"#e3e8e6\";\n\n/**\n * PublicChangeOrderActions — customer-side Approve / Decline buttons on the\n * public change-order page (/co/:id). Approving applies the delta to the\n * linked invoice server-side (roadmap p.12).\n */\nexport default function PublicChangeOrderActions(\n  { changeOrderId, initialStatus, lang = \"en\" }: Props,\n) {\n  const [status, setStatus] = useState(initialStatus);\n  const [busy, setBusy] = useState<\"approve\" | \"decline\" | null>(null);\n  const [error, setError] = useState<string | undefined>(undefined);\n\n  async function decide(action: \"approve\" | \"decline\") {\n    if (busy) return;\n    setBusy(action);\n    setError(undefined);\n    try {\n      const res = await fetch(`/api/change-orders/${changeOrderId}/${action}`, {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        credentials: \"include\",\n      });\n      if (!res.ok) {\n        const body = await res.json().catch(() => ({}));\n        // The backend refuses to mark the order approved if the invoice\n        // write fails — translate that reason instead of echoing it raw.\n        throw new Error(\n          body.reason === \"invoice_update_failed\"\n            ? tFor(lang, \"publicChangeOrderActions.applyError\")\n            : body.reason ??\n              tFor(lang, \"publicChangeOrderActions.submitError\", {\n                status: res.status,\n              }),\n        );\n      }\n      setStatus(action === \"approve\" ? \"approved\" : \"declined\");\n    } catch (err) {\n      setError((err as Error).message);\n    } finally {\n      setBusy(null);\n    }\n  }\n\n  if (status !== \"pending\") {\n    return (\n      <div\n        style={`margin-top:24px;background:${\n          status === \"approved\"\n            ? \"rgba(81,152,67,0.08)\"\n            : \"rgba(168,59,59,0.06)\"\n        };border:1px solid ${LINE};border-radius:14px;padding:20px 24px`}\n      >\n        <div\n          style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${\n            status === \"approved\" ? GREEN : \"#a83b3b\"\n          }`}\n        >\n          {status === \"approved\"\n            ? tFor(lang, \"publicChangeOrderActions.approvedLabel\")\n            : tFor(lang, \"status.declined\")}\n        </div>\n        <p\n          style={`margin:8px 0 0;color:${INK};font-size:15px;line-height:1.55`}\n        >\n          {status === \"approved\"\n            ? tFor(lang, \"publicChangeOrderActions.approvedMessage\")\n            : tFor(lang, \"publicChangeOrderActions.declinedMessage\")}\n        </p>\n      </div>\n    );\n  }\n\n  return (\n    <div style=\"margin-top:24px;display:flex;flex-direction:column;gap:12px\">\n      <button\n        type=\"button\"\n        disabled={busy !== null}\n        onClick={() => decide(\"approve\")}\n        style={`appearance:none;border:0;border-radius:12px;padding:15px 18px;background:${GREEN};color:#fff;font:inherit;font-weight:800;font-size:16px;cursor:pointer`}\n      >\n        {busy === \"approve\"\n          ? tFor(lang, \"publicChangeOrderActions.approving\")\n          : tFor(lang, \"publicChangeOrderActions.approve\")}\n      </button>\n      <button\n        type=\"button\"\n        disabled={busy !== null}\n        onClick={() => decide(\"decline\")}\n        style={`appearance:none;border:1px solid ${LINE};border-radius:12px;padding:13px 18px;background:#fff;color:${MUTED};font:inherit;font-weight:700;font-size:15px;cursor:pointer`}\n      >\n        {busy === \"decline\" ? \"…\" : tFor(lang, \"publicChangeOrderActions.decline\")}\n      </button>\n      {error\n        ? (\n          <p style=\"color:#a83b3b;font-size:13px;margin:2px 0 0\" role=\"alert\">\n            {error}\n          </p>\n        )\n        : null}\n    </div>\n  );\n}\n", "sourceName": "PublicChangeOrderActions.tsx", "checklist": "# Capture checklist — PublicChangeOrderActions\n\n**Theme:** light only. **Palette:** public surface (inline hex, NOT Sabor).\n**Auth:** NONE — `/co/:id` is a public, auth-free customer link. It does need a\n**valid change-order token id** to resolve (otherwise the route renders its own\n\"can't open this\" error card and the island isn't mounted). No fabricated\nscreenshots — drive on a real `/co/:id` page.\n\n## Route / URL\n- `http://localhost:5280/co/<changeOrderId>` for an order whose backend `status`\n  is `pending` (to capture the action buttons). Crop the area BELOW the money\n  breakdown band (the \"Previous/Current total · Added/Credit · New total\" rows),\n  where this island mounts.\n- For the terminal panels, open a link whose order is already `approved` /\n  `declined` (the island seeds `status` from `initialStatus` on mount), OR drive\n  the transition live by clicking the button against the real backend.\n- For Spanish, use an order whose `commsLanguage === \"es\"` (the route resolves\n  `lang` from `co.commsLanguage`).\n\n## Viewports (no own @media)\n- **~390px** (primary — phone; buttons are full-width).\n- **560px** (the route's document column cap).\n\n## Element(s) to crop\n- The full-width action stack (green Approve over outlined Decline) for pending;\n  the single tinted result panel for approved/declined. Include a sliver of the\n  money band above for context.\n\n## States to drive\n1. **pending** — idle: solid green \"Approve this change\" + outlined white\n   \"Decline\".\n2. **approving** — click Approve; capture the disabled \"Approving…\" label (both\n   buttons disabled). Drive by stalling the `/api/change-orders/:id/approve`\n   response, or screenshot quickly.\n3. **approved** — after a successful approve (or on an already-approved link):\n   green-tinted `rgba(81,152,67,0.08)` panel, eyebrow \"Approved\" (#519843), body\n   \"Thanks! Your updated invoice total reflects this change.\" Confirm NO buttons\n   remain and NO page reload happened (in-memory state swap).\n4. **declined** — after Decline (or an already-declined link): pink-tinted\n   `rgba(168,59,59,0.06)` panel, eyebrow \"Declined\" (#a83b3b), body \"No\n   problem — we've let your contractor know.\"\n5. **error** — force a non-ok approve with `reason:\"invoice_update_failed\"` (mock\n   the proxy) → inline red `role=\"alert\"` reading the friendly applyError\n   (\"We couldn't update the invoice — nothing was changed…\"). Confirm the raw\n   reason token never appears, and both buttons are clickable again for retry.\n6. **es** — Spanish: \"Aprobar este cambio\" / \"Rechazar\", busy \"Aprobando…\",\n   approved \"Aprobado\" + \"¡Gracias! El total actualizado…\".\n\n## Motion\n- None to film — no CSS transitions/keyframes; the pending→terminal swap and the\n  disabled flip are instant.\n"}]}, {"name": "public-contract", "pageMd": "# Page — public-contract (`/c/:id`)\n\nThe public, auth-FREE service-agreement page a customer opens from an SMS/email\nlink. It shows the full contract document and (when unsigned) an inline\nsignature pad to e-sign. Route file: `routes/c/[id].tsx` (46 lines).\n\n## Purpose\nLet a customer review and electronically sign their contract without logging\nin. The page is intentionally document-like (printable) and renders in the\ncontractor's outgoing-comms language.\n\n## Composition & data flow\n- **NOT SSR-fetched.** Unlike the sibling invoice page, the route does **not**\n  call `ssrBackendGet`. The route is a synchronous `define.page` that paints a\n  shell + mounts the **`PublicContractView` island**, which fetches the\n  contract **client-side** in a `useEffect`:\n  `fetch(\"/api/contracts/:id/public\")` (same-origin `/api` proxy) and paints a\n  skeleton first. This was a deliberate fix (problems.md #25): the old SSR\n  `await` blocked the first byte → blank-white screen on slow networks.\n- **Data source:** `GET /contracts/:id/public` → `ContractPublic`\n  (data-model § 1.5). Auth-FREE.\n- **Component tree:**\n  - `routes/c/[id].tsx` (page-composition) — `<Head>` (title, `/landing.css`,\n    inline `<style>` mobile+print block), page wrapper div, max-width:760px\n    column.\n    - `PublicContractView` (island) — loading skeleton / `ErrorCard` / `ContractDoc`.\n      - `ContractDoc` (shared-components/contract-doc) — the rendered document.\n        - `PublicSignContract` (island) — mounted by `ContractDoc` only when unsigned.\n- **Error / expired state:** the island catches a non-ok fetch (404/expired/\n  revoked link) and renders the exported **`ErrorCard`** (\"This link is no\n  longer available\" / \"We can't open this agreement\"). There is no SSR error\n  branch on this route — the error is purely client-side.\n\n## Sections\n1. Brand strip (logo/business name/address) — `.ctr__no-print`.\n2. Pink-ribbon document card (`.ctr`): doc-tag + status pill, hero title,\n   \"between … and …\", To/From party cards.\n3. Job details (bullets + line-item table + green total card).\n4. Payment schedule milestones.\n5. Terms (wizard grid + 14 numbered legal clauses).\n6. Signature block (contractor card + customer card; unsigned → sign pad;\n   signed → \"Signed and binding\" band).\n7. Contact footer + \"powered by\" strip.\n\n(All section internals are owned by `contract-doc`; see that spec.)\n\n## Page palette / shell (inline)\n- Wrapper: `min-height:100dvh; background:#f7f6f1; color:#1c2c30;\n  font-family:-apple-system,…; padding:32px 16px calc(64px + var(--kb-inset,0px));\n  scroll-padding-bottom:var(--kb-inset,0px)`. The `--kb-inset` var is the mobile\n  soft-keyboard inset (set by `islands/MobileViewport.tsx`) so the sign-pad +\n  name input aren't hidden behind the keyboard.\n- Column: `max-width:760px; margin:0 auto`.\n- CSS: `/landing.css` (resets + the `.spinner` class the sign island reuses) +\n  the route's inline `<style>` (mobile 720px overrides + print). See\n  `css/public-contract.css`. **`/contracts.css` is NOT loaded here** — that\n  stylesheet belongs to the authed `routes/contracts/index.tsx` list page.\n\n## Build order\n1. Build the page shell (Head, wrapper, 760px column, inline mobile+print CSS).\n2. Build `contract-doc` (shared) against its isolate cases — the bulk of the UI.\n3. Build the `public-sign-contract` island (canvas sign pad) it mounts.\n4. Build the `public-contract-view` island (skeleton → fetch → ContractDoc /\n   ErrorCard) and wire it into the route.\n\n## Local components\n- `components/public-contract-view/` ← `islands/PublicContractView.tsx` (island).\n- `components/public-sign-contract/` ← `islands/PublicSignContract.tsx` (island,\n  signature capture).\n\n## Anti-patterns (page level)\n- **`location.reload()`** in `PublicSignContract` after a successful sign\n  (900ms timeout) — flagged in that component's spec. A SPA-friendly rebuild\n  would swap to the signed view in place (like the invoice claim does with\n  local `done` state).\n- **Public composite-read fan-out** on `GET /contracts/:id/public` — data-model\n  hazard #7. Serve from a denormalized projection.\n", "components": [{"name": "public-contract-view", "component": "PublicContractView", "spec": "# public-contract-view (PublicContractView)\n\nClient-side loader for the public agreement page. Renders a skeleton\nimmediately, fetches the contract over the `/api` proxy, then swaps in\n`ContractDoc` (or `ErrorCard`).\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PublicContractView.tsx`, 90 lines).\n- **Interaction tier:** `island` — client state + one fetch effect.\n- **Client state owned:** a single `useState<State>` where\n  `phase: \"loading\" | \"error\" | \"ok\"`, plus `contract?` and `message?`.\n- **Server-mutation action endpoint:** none (read-only loader). No flash.\n- **Data source:** `fetch(\"/api/contracts/:id/public\")` in a `useEffect`\n  keyed on `id`, with an `alive` guard to drop late responses on unmount.\n  Backend: `GET /contracts/:id/public` → `ContractPublic` (data-model § 1.5).\n  Auth-FREE (`accept: application/json`, no session).\n  - **Honest-empty / expired:** `r.ok===false` (or a thrown fetch) →\n    `phase:\"error\"`, message = `tFor(lang,\"publicContract.linkGone\")`. Renders\n    `ErrorCard`. No fabricated content — a missing/expired link shows the card,\n    not an empty doc.\n- **Liveness:** request-response only. One fetch on mount; no polling/socket.\n  After signing, `PublicSignContract` reloads the whole page (so this island\n  re-mounts and re-fetches the now-signed projection).\n- **Anti-patterns:** none in this island itself (clean fetch + guard). It does\n  inherit the page's `location.reload()` flow from the sign island.\n- **Data-shape hazards:** the fetched `ContractPublic` is the composite-read\n  join (hazard #7) — but the cost is on the backend; the island just consumes\n  it.\n\n## 2. Anatomy\n- **OK:** returns `<ContractDoc contract={s.contract} />` (no wrapper of its own).\n- **Error:** returns `<ErrorCard message=… />` (exported from contract-doc).\n- **Loading:** returns a local `LoadingSkeleton` — an inline `<style>` with the\n  `ctr-skel` opacity-pulse keyframe + a mimic of the doc card: brand-strip bar,\n  pink/teal ribbon (`#144852→#0e333b`), and 5 staggered placeholder bars\n  (widths 220/320/140/280/200, `animation-delay i*0.08s`) + a 120px block.\n  Fill colours `#e3e8e6`/`#e9edeb`/`#eef2f0`; card `#fffdf7` radius 24.\n- All inline-styled; no CSS classes.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `id` | string (required) | — | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no (used only for the error copy) |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | skeleton shimmer, fetch in flight | `cases/loading/loading.json` |\n| error | non-ok fetch → ErrorCard | `cases/error/error.json` |\n| ok | fetch resolves → ContractDoc | `cases/ok/ok.json` |\n\nCases use `_mocks` to stub the `/api/contracts/:id/public` response (delay for\nloading, 404 for error, full `ContractPublic` body for ok).\n\n## 5. Events\n- No user events. The only \"event\" is the mount effect firing the fetch:\n  `ev.expect(net => net.url.endsWith(\"/api/contracts/\"+id+\"/public\") && net.method===\"GET\")`.\n\n## 6. Motion (real CSS)\n- `@keyframes ctr-skel { 0%,100%{opacity:.55} 50%{opacity:.85} }` — a slow\n  opacity pulse on the placeholder bars, 1.2s ease-in-out infinite, staggered.\n- **Reduced-motion gap (flag):** no component-local `prefers-reduced-motion`\n  guard, and the public surface does NOT load the Sabor tokens' global clamp,\n  so the pulse keeps running under reduced-motion. Rebuild should add a guard.\n- The skeleton→doc swap is an instant replace (no cross-fade).\n\n## 7. Responsive\n- None of its own — it fills the route's `max-width:760px` column. Skeleton bar\n  widths use `max-width:100%` so they don't overflow narrow phones.\n\n## 8. A11y\n- Skeleton bars are decorative (no text, no `aria`). The error card is plain\n  text. The OK path delegates to `ContractDoc`. No live-region announcement of\n  the loading→ready transition (minor).\n\n## 9. Used on\n- **public-contract route** (`routes/c/[id].tsx`) — the only mount point. It is\n  the route's sole child; it renders `ContractDoc`/`ErrorCard`.\n", "fixture": "{\n  \"category\": \"public-contract\",\n  \"folder\": \"public-contract-view\",\n  \"controls\": {\n    \"id\": { \"type\": \"text\", \"value\": \"ctr_9f3a2b71c0d4e5f6\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "error.json", "content": "{\n  \"_name\": \"Error / link gone — fetch returns non-ok → ErrorCard ('This link is no longer available')\",\n  \"id\": \"ctr_dead0000\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"GET /api/contracts/ctr_dead0000/public\": { \"status\": 404, \"body\": { \"reason\": \"not_found\" } }\n  }\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — skeleton shimmer (fetch in flight)\",\n  \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"GET /api/contracts/ctr_9f3a2b71c0d4e5f6/public\": { \"_delayMs\": 100000, \"status\": 200, \"body\": {} }\n  }\n}\n"}, {"name": "ok.json", "content": "{\n  \"_name\": \"OK — fetch resolves → swaps in <ContractDoc> (unsigned)\",\n  \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"GET /api/contracts/ctr_9f3a2b71c0d4e5f6/public\": {\n      \"status\": 200,\n      \"body\": {\n        \"id\": \"ctr_9f3a2b71c0d4e5f6\",\n        \"quoteId\": \"qt_5521aa\",\n        \"customerId\": \"cus_77a1\",\n        \"status\": \"draft\",\n        \"totalAmount\": 1450000,\n        \"effectiveDate\": \"2026-06-10\",\n        \"startDate\": \"2026-06-24\",\n        \"estimatedCompletionDate\": \"2026-07-18\",\n        \"contractor\": {\n          \"name\": \"Hans Hansen\",\n          \"businessName\": \"Hansen Decks & Patios\",\n          \"phoneNumber\": \"+15125550148\",\n          \"email\": \"hans@hansendecks.com\",\n          \"addressLine\": \"812 Cedar Ln, Austin, TX 78704\",\n          \"state\": \"TX\",\n          \"commsLanguage\": \"en\"\n        },\n        \"customer\": { \"name\": \"Maria Delgado\", \"email\": \"maria.delgado@example.com\", \"phoneNumber\": \"+15125550199\" },\n        \"jobDetails\": {\n          \"jobName\": \"Backyard Deck Build\",\n          \"summary\": \"build a 16x20 cedar deck\",\n          \"description\": \"Demo and haul the old deck.\\nFrame and build a 16x20 cedar deck.\\nInstall stairs and railing.\\nSeal and stain.\",\n          \"lineItems\": [\n            { \"description\": \"Demo & haul existing deck\", \"price\": 180000, \"quantity\": 1 },\n            { \"description\": \"Framing & decking — cedar 16x20\", \"price\": 920000, \"quantity\": 1 },\n            { \"description\": \"Stairs & railing\", \"price\": 250000, \"quantity\": 1 },\n            { \"description\": \"Seal & stain\", \"price\": 100000, \"quantity\": 1 }\n          ]\n        },\n        \"terms\": [\n          { \"stepId\": \"customer\", \"label\": \"Customer\", \"value\": \"Maria Delgado\" },\n          { \"stepId\": \"payment_terms\", \"label\": \"Payment terms\", \"value\": \"50% deposit, 50% on completion\" },\n          { \"stepId\": \"warranty\", \"label\": \"Warranty\", \"value\": \"12 months\" }\n        ],\n        \"createdAt\": \"2026-06-10T15:04:00Z\"\n      }\n    }\n  }\n}\n"}], "css": "/* public-contract-view.css — EXTRACTED real CSS.\n *\n * The island is entirely inline-styled. The ONLY CSS rule it emits is the\n * skeleton shimmer keyframe, declared inline via a <style> tag inside the\n * LoadingSkeleton render (so it ships with the island even before the doc\n * loads). Reproduced verbatim. Surface: PUBLIC palette.\n */\n\n/* Pulsing opacity on the skeleton placeholder bars (loading phase only). */\n@keyframes ctr-skel {\n  0%, 100% { opacity: .55; }\n  50%      { opacity: .85; }\n}\n\n/* Each placeholder bar applies inline:\n *   animation: ctr-skel 1.2s ease-in-out infinite;\n *   animation-delay: <i*0.08>s   (staggered across the 5 title/line bars)\n * Bar fills: #e3e8e6 (eyebrow), #e9edeb (text bars), #eef2f0 (block).\n * Card: background #fffdf7; border-radius 24px; ribbon #144852→#0e333b. */\n\n/* NOTE: no component-local prefers-reduced-motion guard — relies on the global\n * tokens clamp (NOT loaded on the public surface). Flag: on /landing.css the\n * pulse keeps running under reduced-motion. */\n", "cssName": "public-contract-view.css", "source": "/**\n * PublicContractView — client-side loader for the public agreement page.\n *\n * The route used to `await` the backend in SSR, so the browser saw a blank\n * white screen until the first byte (problems.md #25). This island renders a\n * skeleton immediately, then fetches `/contracts/:id/public` through the\n * same-origin /api proxy and swaps in the real document (or an error card).\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport {\n  ContractDoc,\n  type ContractPublic,\n  ErrorCard,\n} from \"../components/contract-doc.tsx\";\nimport { tFor } from \"../lib/i18n.ts\";\n\ninterface State {\n  phase: \"loading\" | \"error\" | \"ok\";\n  contract?: ContractPublic;\n  message?: string;\n}\n\nexport default function PublicContractView(\n  { id, lang = \"en\" }: { id: string; lang?: \"en\" | \"es\" },\n) {\n  const LINK_GONE = tFor(lang, \"publicContract.linkGone\");\n  const [s, setS] = useState<State>({ phase: \"loading\" });\n\n  useEffect(() => {\n    let alive = true;\n    fetch(`/api/contracts/${id}/public`, {\n      headers: { accept: \"application/json\" },\n    })\n      .then(async (r) => {\n        if (!alive) return;\n        if (r.ok) {\n          setS({ phase: \"ok\", contract: (await r.json()) as ContractPublic });\n        } else {\n          setS({ phase: \"error\", message: LINK_GONE });\n        }\n      })\n      .catch(() => {\n        if (alive) setS({ phase: \"error\", message: LINK_GONE });\n      });\n    return () => {\n      alive = false;\n    };\n  }, [id]);\n\n  if (s.phase === \"ok\" && s.contract) {\n    return <ContractDoc contract={s.contract} />;\n  }\n  if (s.phase === \"error\") {\n    return (\n      <ErrorCard\n        message={s.message ?? tFor(lang, \"publicContract.notAvailable\")}\n      />\n    );\n  }\n  return <LoadingSkeleton />;\n}\n\n/** Lightweight placeholder shown while the agreement loads — mirrors the\n *  document card's shape so the swap-in isn't jarring. */\nfunction LoadingSkeleton() {\n  return (\n    <>\n      <style>\n        {`@keyframes ctr-skel { 0%,100% { opacity:.55 } 50% { opacity:.85 } }`}\n      </style>\n      <div style=\"text-align:center;margin-bottom:18px\">\n        <div style=\"display:inline-block;height:11px;width:160px;border-radius:6px;background:#e3e8e6;animation:ctr-skel 1.2s ease-in-out infinite\" />\n      </div>\n      <div style=\"background:#fffdf7;border-radius:24px;box-shadow:0 14px 50px rgba(20,72,82,0.10);overflow:hidden;border:1px solid rgba(20,72,82,0.10)\">\n        <div style=\"height:8px;background:linear-gradient(90deg,#144852 0%,#0e333b 100%)\" />\n        <div style=\"padding:36px 44px 40px;display:flex;flex-direction:column;gap:18px\">\n          {[220, 320, 140, 280, 200].map((w, i) => (\n            <div\n              key={i}\n              style={`height:18px;width:${w}px;max-width:100%;border-radius:8px;background:#e9edeb;animation:ctr-skel 1.2s ease-in-out infinite;animation-delay:${\n                i * 0.08\n              }s`}\n            />\n          ))}\n          <div style=\"margin-top:8px;height:120px;border-radius:14px;background:#eef2f0;animation:ctr-skel 1.2s ease-in-out infinite\" />\n        </div>\n      </div>\n    </>\n  );\n}\n", "sourceName": "PublicContractView.tsx", "checklist": "# Capture checklist — PublicContractView\n\n- **Route:** `/c/<contractId>` (PUBLIC, auth-free, needs a real contract id).\n- **Surface:** inline-styled **public palette**; embeds the shared `contract-doc`.\n- **Viewports:** 390 (mobile-first), 640. Single column.\n- **Crop targets:** the rendered contract document (`contract-doc`); the sign\n  call-to-action that launches `PublicSignContract`.\n- **States to drive:**\n  - `unsigned` — contract shown with a \"sign\" action.\n  - `signed` — post-signature state (signature block filled, action gone).\n  - `expired/error` — invalid/expired link.\n  - `es` — Spanish (terms stored in English, localized at render).\n- **Theme:** light only. **No fabricated screenshots** — needs a live backend + valid token.\n"}, {"name": "public-sign-contract", "component": "PublicSignContract", "spec": "# PublicSignContract\n\nThe customer-facing **signature pad** on the public contract page (`/c/:id`).\nTwo-input e-sign flow: **(1) draw** your signature on a canvas (pointer / touch /\npen), **(2) type** your full legal name underneath. Both are POSTed to\n`/api/contracts/:id/sign` as `{ name, signature /* PNG data URL */ }`. The typed\nname is the legal-record fallback; the canvas captures the visual mark.\n\nSource: `islands/PublicSignContract.tsx` (copied to `js/PublicSignContract.tsx`,\n476 lines). Mounted by `routes/c/[id].tsx` under the contract document.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PublicSignContract.tsx`).\n- **Interaction tier:** `island` — canvas drawing + `fetch` POST mutation. NOT a\n  Fresh Partial, NOT an SSR form/PRG.\n- **Surface:** **public palette** (inline hex — `#FF6B6B`/`#d94e4e` pink pad\n  frame, `#144852` teal ink stroke, `#519843` green submit, `#1c2c30` ink,\n  `#6b7a7e` muted, `#e3e8e6` hairline), NOT Sabor. Its sole external CSS dep is\n  `.spinner` (+ `spin` keyframe) from `/landing.css` (the host route loads it;\n  see `css/public-sign-contract.css`, which reproduces it).\n- **Client state owned:**\n  - `hasInk: boolean` (signal/`useState`) — whether any committed stroke exists;\n    gates the placeholder, undo/clear enable, and submit enable.\n  - `name: string` — the typed legal name input.\n  - `submitting: boolean` — POST in flight.\n  - `status: \"idle\" | \"ok\" | \"error\"` — terminal state (`ok` swaps to the success\n    panel).\n  - `err: string | undefined` — inline error text.\n  - **Imperative refs (NOT signals):** `canvasRef` (the `<canvas>`),\n    `strokesRef: Stroke[]` (committed strokes), `drawingRef: Stroke | null` (the\n    in-progress stroke). A `Stroke` is `{ points: {x,y,t}[] }` — `t` is\n    `performance.now()` per sample, used for speed→width.\n- **Drawing interaction (the core mechanic):**\n  - **Pointer Events** unify mouse/touch/pen. `onPointerDown` →\n    `canvas.setPointerCapture(e.pointerId)`, starts a new `drawingRef` stroke with\n    the first sample, and sets `hasInk=true` immediately (so the placeholder\n    clears under the very first stroke, not on pointer-up). `onPointerMove` pushes\n    a `{x,y,t}` sample and calls `redraw()`. `onPointerUp` /\n    `onPointerCancel` / `onPointerLeave` all route to `onPointerUp`, which\n    releases capture, commits the stroke to `strokesRef` **only if it has >1\n    point** (a bare tap adds nothing), nulls `drawingRef`, and re-syncs `hasInk`\n    to `strokesRef.length > 0` (so a tap that drew nothing restores the\n    placeholder).\n  - **Variable stroke width** = ink-pen feel: per segment, `speed = dist/dt` and\n    `width = clamp(0.9, 3.2, 3.4 - speed*1.6)` — faster = thinner.\n  - **Hi-DPI:** `setupCanvas()` sizes the backing buffer to\n    `rect.width/height * devicePixelRatio` and `ctx.scale(dpr,dpr)`.\n  - **Undo** pops the last stroke + redraws; **Clear** empties `strokesRef`. Both\n    `disabled` when `!hasInk`.\n  - **Resize-aware:** a `resize` listener re-runs `setupCanvas()` (which re-sizes\n    the buffer and `redraw()`s) so the signature survives a viewport change /\n    rotation.\n  - **PNG export (`exportSignaturePng`)** crops to the strokes' bounding box\n    (+10px pad), caps width at 480px, redraws onto an offscreen canvas with a\n    white fill, and returns a `toDataURL(\"image/png\")` — keeping the payload\n    ~3–10KB to stay under the backend's 64KB row limit (a full hi-DPI canvas\n    would be ~150KB+).\n- **Server mutation + feedback:**\n  - `onSign` (form submit) early-returns unless `name.trim()` AND `hasInk`. Then\n    `POST /api/contracts/:id/sign` JSON `{ name: trimmed, signature: dataUrl }`.\n    On non-ok it throws `text.slice(0,200)` (truncated body) or the status code.\n  - **Flash:** the submit button swaps to `<span class=\"spinner\"/> + \"Signing…\"`\n    and `disabled`; on success `status=\"ok\"` shows the green check panel.\n- **`location.reload()` FLAG — PRESENT.** On success the island sets\n  `status=\"ok\"`, then `setTimeout(() => globalThis.location.reload(), 900)` so the\n  **SSR-rendered \"signed\" contract** takes over (the right-hand customer-signature\n  card fills in with the typed name + date, matching the contractor card). This is\n  a **flagged anti-pattern**: a full-document reload to \"refresh\" after a\n  mutation. The 900ms green-check is purely cosmetic before the hard reload.\n  - **Fix:** prefer a server PRG (`POST /sign` responds `303 → /c/:id`, browser\n    re-fetches the signed doc via the server), or have the island patch the\n    customer-signature card in place from the success response (the data it needs —\n    typed name + date — is already in hand) instead of reloading. Contrast the\n    sibling `PublicInvoiceClaim`, which does the correct in-memory swap with NO\n    reload.\n- **Liveness:** request-response only. No polling/websocket.\n- **Data source / honest-empty:** owns no fetch on mount — receives\n  `contractId` + `lang` as props; the contract document itself is SSR'd by the\n  route. If the contract is already signed/declined the route doesn't mount this\n  island (renders the signed doc instead).\n- **Reactivity / i18n:** `lang` is a **prop** (`\"en\"|\"es\"`, default `\"en\"`),\n  resolved by the route; all strings via `tFor(lang,key,vars)`. No on-page toggle\n  → NOT reactive to `langSignal`.\n\n### JS jank lint — forced layout (synchronous reflow) in pointer handlers\n- **FINDING (jank):** `onPointerMove` runs on every pointer sample and calls\n  `pointerXY(e)` → `canvas.getBoundingClientRect()`, then `redraw()` →\n  `canvas.getBoundingClientRect()` **again**. `getBoundingClientRect()` forces a\n  **synchronous layout flush**; calling it (twice) at pointer-sample frequency\n  (often 60–240Hz on a pen/trackpad) is classic **layout thrash** in the hot\n  drawing path. It coexists with a 2D-canvas paint, so the cost is a forced\n  reflow + a full canvas repaint per move.\n- **FINDING (algorithmic):** `redraw()` re-clears and **replays ALL committed\n  strokes plus the in-progress stroke from scratch on every pointermove** — O(total\n  points drawn so far) per sample. As the signature grows, each new sample gets\n  more expensive (quadratic-ish over a stroke), so a long signature visibly\n  degrades. There is no `requestAnimationFrame` batching — `redraw()` runs\n  synchronously inside the event handler.\n- **FIX:**\n  1. **Cache the rect** — read `getBoundingClientRect()` once in `setupCanvas()`\n     (and on the existing `resize` listener) into a ref; reuse it in `pointerXY`\n     and `redraw` instead of re-measuring per move. (Also account for scroll if\n     the page can scroll during signing.)\n  2. **Incremental draw** — in `onPointerMove`, draw only the newest segment\n     (`lineTo` from the previous point) onto the live context instead of full\n     `redraw()`; reserve the full replay for undo/clear/resize.\n  3. **rAF-coalesce** pointermove → at most one draw per frame\n     (`requestAnimationFrame`), dropping intermediate samples' redundant repaints.\n- **Reduced-motion:** N/A to the drawing itself (user-driven), but see Motion §6\n  for the `.spinner` and the submit `transition`.\n\n### A11y — keyboard / typed-name fallback\n- **There IS a typed-name fallback** — the `<input type=\"text\" autoComplete=\"name\"\n  required>` is a fully keyboard-accessible legal-name capture, and the disclaimer\n  frames the typed name as the binding record.\n- **BUT signing still HARD-REQUIRES a drawn mark:** `onSign` early-returns and the\n  submit stays `disabled` unless **both** `name.trim()` AND `hasInk`. The canvas\n  is **pointer-only** — there is **no keyboard path to produce ink** (no\n  type-to-sign, no \"use my typed name as signature\" toggle). So a\n  keyboard-only / no-pointer / screen-reader user **cannot complete signing at\n  all**. This is the headline a11y gap (see §8).\n\n## 2. Anatomy (inline styles; see js + css for literals)\n```\nstatus === \"ok\"  →  success panel (transient, 900ms before reload)\n<div style=\"margin-top:24px;\n            background:linear-gradient(135deg,rgba(81,152,67,0.10),rgba(81,152,67,0.04));\n            border:1px solid rgba(72,158,95,0.35); border-radius:18px;\n            padding:24px; text-align:center\">\n  <div circle #519843 + ✓ svg/>\n  <div weight:800 #519843 18px>  publicSign.success.title  (\"Contract signed!\")\n  <div #6b7a7e 13px>             publicSign.success.body\n</div>\n\nstatus !== \"ok\"  →  the form\n<form onSubmit=onSign style=\"margin-top:24px;text-align:left\">\n  ── header strip ──\n  <div eyebrow #d94e4e>  publicSign.eyebrow\n  <div actions>\n     <button Undo  type=button disabled={!hasInk}  aria-label  (↺ svg) />\n     <button Clear type=button disabled={!hasInk}  aria-label  (🗑 svg) />\n  ── pad ──\n  <div pad-wrapper  border:2px dashed #FF6B6B; radius:18px; box-shadow:…>\n     <div faint-grid aria-hidden/>        20px×20px rgba(255,107,107,0.05)\n     <div baseline   aria-hidden/>        border-bottom @ 36px from bottom\n     <div \"✕\" guide  aria-hidden/>        Helvetica Neue 900, bottom-left\n     <canvas ref onPointerDown/Move/Up/Cancel/Leave\n             style=\"width:100%;height:200px;touch-action:none;cursor:crosshair\"/>\n     {!hasInk && <div placeholder aria-hidden>publicSign.pad.heading + .sub</div>}\n  ── typed legal name ──\n  <label>  publicSign.nameLabel\n  <input type=text required autoComplete=name value=name onInput placeholder=publicSign.namePlaceholder/>\n  <div disclaimer #6b7a7e 12px>  publicSign.disclaimer\n  {err && <div #b3261e 13px>publicSign.error{err}</div>}\n  ── submit ──\n  <button type=submit disabled={submitting || !name.trim() || !hasInk}\n     style=\"width:100%;\n            bg = enabled ? linear-gradient(135deg,#519843,#71a85f) : #a8c8a0;\n            box-shadow = enabled ? 0 10px 22px -6px rgba(81,152,67,0.55) : none;\n            transition: transform 160ms\">\n     { submitting ? <span class=spinner/> + publicSign.submitting\n       : (hasInk && name.trim()) ? ✒ svg + publicSign.submitEnabled\n                                 : ✒ svg + publicSign.submitDisabled }\n  </button>\n</form>\n```\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `contractId` | `string` (required) | — | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n`hasInk`/`name`/`submitting`/`status`/`err` are internal `useState`; the\nstrokes/drawing/canvas are imperative refs — drive via Events / case `_signals`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| idle | empty pad + placeholder; name empty; undo/clear + submit disabled | `cases/idle/idle.json` |\n| inked | `hasInk=true` + name filled → submit enabled (green gradient) | `cases/inked/inked.json` |\n| submitting | POST in flight → spinner + \"Signing…\", inputs locked | `cases/submitting/submitting.json` |\n| success | `status=\"ok\"` green check panel (transient; reload follows) | `cases/success/success.json` |\n| error | POST failed → red error line; form stays interactive for retry | `cases/error/error.json` |\n| es | Spanish strings throughout | `cases/es/es.json` |\n\nThe canvas strokes are imperative refs, not signals — isolate can only fake the\npost-stroke UI via the `hasInk` signal; **real ink must be produced by the Events\npointer sequence** (a fixture cannot inject `strokesRef`).\n\n## 5. Events (`capture(page)` predicates)\n- `ev.expect(e => e.source === \"canvas\" && e.type === \"pointerdown\")` →\n  `setPointerCapture` + start stroke + `setHasInk(true)`.\n- `ev.expect(e => e.source === \"canvas\" && e.type === \"pointermove\")` → push\n  sample + `redraw()` (the jank hot path — §1).\n- `ev.expect(e => e.source === \"canvas\" && e.type === \"pointerup\")` → commit\n  stroke (if >1 pt) + re-sync `hasInk`.\n- `ev.expect(e => e.source === \"button\" /* Undo */ && e.type === \"click\")` →\n  `undoStroke()`.\n- `ev.expect(e => e.source === \"button\" /* Clear */ && e.type === \"click\")` →\n  `clearPad()`.\n- `ev.expect(e => e.source === \"input\" /* name */ && e.type === \"input\")` →\n  `setName(value)`.\n- `ev.expect(e => e.source === \"form\" && e.type === \"submit\")` → `onSign` →\n  `POST /api/contracts/:id/sign` → on ok `status=\"ok\"` → **900ms →\n  location.reload() (FLAGGED)**.\n\n## 6. Motion (real CSS only)\n- **Submit button `transition: transform 160ms`** — the ONLY declared transition\n  on the island (no transform is actually applied in source beyond hover/active\n  the UA may add; it's a latent affordance).\n- **`.spinner`** (from `/landing.css`, reproduced in `css/`): 14px ring,\n  `border-right-color:transparent`, `animation: spin 0.7s linear infinite`\n  (`@keyframes spin { to { transform: rotate(360deg) } }`). Shown only while\n  `submitting`.\n- The canvas redraw is JS-driven (no CSS animation).\n- **Reduced-motion:** the island declares no reduced-motion block; the global\n  token clamp (`prefers-reduced-motion: reduce` → durations 0.01ms) applies to\n  the `.spinner`/transition once landing.css/tokens are present. Rebuild should\n  keep the spinner honoring reduced-motion.\n\n## 7. Responsive\n- No own `@media`. The pad is `width:100%; height:200px` (fixed height) inside the\n  route's document column; the button row + inputs are full-width. The `resize`\n  listener re-runs `setupCanvas()` so the backing buffer + drawn strokes survive\n  width changes / rotation. `touch-action:none` on the canvas is load-bearing —\n  it lets pointer drawing work on touch without the browser hijacking the gesture\n  to scroll.\n\n## 8. A11y\n- Undo/Clear carry `aria-label` (`publicSign.undoAria`/`clearAria`); the pad's\n  decorative grid/baseline/✕/placeholder are all `aria-hidden`. Typed name input\n  is `required` + `autoComplete=\"name\"`. Submit label changes meaningfully\n  (`submitDisabled`→`submitEnabled`).\n- **GAPS (headline):**\n  - **No keyboard path to sign.** Producing ink is pointer-only; signing requires\n    `hasInk`, so a keyboard-only / no-pointer / SR user is **fully blocked**. The\n    typed name alone cannot satisfy submit. Rebuild: offer a \"use my typed name as\n    my signature\" option (render the typed name into the canvas) or accept a typed\n    signature as the legal mark — i.e. make the drawn mark optional when a name is\n    present.\n  - The `<canvas>` has no `role`/`aria-label`/instructions for SR users — it's an\n    opaque interactive region.\n  - Error is a plain `<div>` (no `role=\"alert\"`/`aria-live`) → a send failure\n    isn't announced.\n  - On success no focus is moved to the result panel, and the 900ms reload yanks\n    context out from under assistive tech.\n\n## 9. Used on\n- `/c/:id` only — mounted by `routes/c/[id].tsx` under the contract document, only\n  when the contract is not already signed/declined. Sole importer; passed\n  `contractId` + `lang` (from the contractor's comms language).\n", "fixture": "{\n  \"category\": \"public-contract\",\n  \"folder\": \"public-sign-contract\",\n  \"controls\": {\n    \"contractId\": { \"type\": \"text\", \"value\": \"ctr_9f3a2b71c0d4e5f6\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "error.json", "content": "{\n  \"_name\": \"Error — POST /sign failed; red error line under the disclaimer, form still interactive for retry\",\n  \"contractId\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\",\n  \"_signals\": { \"hasInk\": true, \"name\": \"Maria Delgado\", \"status\": \"error\", \"err\": \"413 Payload Too Large\", \"submitting\": false }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — lang='es'; eyebrow 'Tu firma', pad 'Dibuja tu firma aquí', submit 'Todo bien — firma el contrato →'\",\n  \"contractId\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"es\",\n  \"_signals\": { \"hasInk\": true, \"name\": \"Maria Delgado\", \"status\": \"idle\" }\n}\n"}, {"name": "idle.json", "content": "{\n  \"_name\": \"Idle — empty pad with 'Draw your signature here' placeholder, name empty, undo/clear disabled, submit disabled+green-muted\",\n  \"contractId\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\"\n}\n"}, {"name": "inked.json", "content": "{\n  \"_name\": \"Inked + named — hasInk=true (placeholder gone, strokes drawn), name filled, undo/clear enabled, submit enabled (green gradient, 'Looks good — sign the contract →')\",\n  \"contractId\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\",\n  \"_signals\": { \"hasInk\": true, \"name\": \"Maria Delgado\", \"status\": \"idle\" },\n  \"_note\": \"hasInk + name drive the submit's enabled styling. The canvas strokes (strokesRef) are imperative refs, not signals — isolate can only fake the post-stroke UI via hasInk; real ink must be drawn by the Events pointer sequence.\"\n}\n"}, {"name": "submitting.json", "content": "{\n  \"_name\": \"Submitting — POST /sign in flight; button shows <span class='spinner'> + 'Signing…', inputs locked\",\n  \"contractId\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\",\n  \"_signals\": { \"hasInk\": true, \"name\": \"Maria Delgado\", \"submitting\": true, \"status\": \"idle\" }\n}\n"}, {"name": "success.json", "content": "{\n  \"_name\": \"Success — status='ok' green check panel ('Contract signed!'). NOTE: in the real app this is transient — a 900ms setTimeout then location.reload() takes over with the SSR signed doc. FLAGGED anti-pattern.\",\n  \"contractId\": \"ctr_9f3a2b71c0d4e5f6\",\n  \"lang\": \"en\",\n  \"_signals\": { \"status\": \"ok\" }\n}\n"}], "css": "/* public-sign-contract.css — EXTRACTED real CSS.\n *\n * The signature-pad island is 100% inline-styled. It emits NO <style> of its\n * own. Its single CSS dependency is the `.spinner` class (+ `spin` keyframe),\n * which lives in /landing.css — loaded by the host route /c/:id, NOT by this\n * island. Reproduced here so the island's submitting state renders without\n * landing.css. Surface: PUBLIC palette.\n *\n * Canvas/pad inline structure (see js/PublicSignContract.tsx):\n *   pad wrapper: background #fff; border:2px dashed #FF6B6B (PINK);\n *                border-radius:18px; overflow:hidden;\n *                box-shadow:0 8px 24px -10px rgba(255,107,107,0.30)\n *   faint grid : linear-gradient 20px×20px @ rgba(255,107,107,0.05)\n *   baseline   : border-bottom 1.5px solid rgba(255,107,107,0.35), 36px from bottom\n *   \"✕\" guide  : Helvetica Neue 900, rgba(255,107,107,0.45), 18px, bottom-left\n *   <canvas>   : display:block; width:100%; height:200px;\n *                touch-action:none; cursor:crosshair   <- touch-action:none is\n *                load-bearing: lets pointer drawing work on touch w/o scroll.\n *   submit btn : enabled  → linear-gradient(135deg,#519843,#71a85f)\n *                disabled → flat #a8c8a0\n *                transition: transform 160ms   (the only declared transition)\n */\n\n/* Inline button spinner — borrowed from /landing.css. */\n.spinner {\n  display: inline-block;\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 50%;\n  vertical-align: -2px;\n  animation: spin 0.7s linear infinite;\n}\n@keyframes spin { to { transform: rotate(360deg); } }\n", "cssName": "public-sign-contract.css", "source": "import { useEffect, useRef, useState } from \"preact/hooks\";\nimport { tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  contractId: string;\n  /** Customer-facing copy language (defaults to en; non-breaking). */\n  lang?: \"en\" | \"es\";\n}\n\ninterface Stroke {\n  points: { x: number; y: number; t: number }[];\n}\n\nconst PINK = \"#FF6B6B\";\nconst PINK_DARK = \"#d94e4e\";\nconst TEAL = \"#144852\";\nconst GREEN = \"#519843\";\nconst INK = \"#1c2c30\";\nconst MUTED = \"#6b7a7e\";\nconst LINE = \"#e3e8e6\";\n\n/**\n * Customer-facing signature pad. Two-input flow:\n *   1. **Draw** your signature on a canvas (pointer / touch / pen).\n *   2. **Type** your full legal name underneath.\n * Both are submitted to /api/contracts/:id/sign as `signature` (PNG data\n * URL) + `name`. The typed name is the legal-record fallback; the canvas\n * captures the visual mark.\n *\n * The pad supports:\n *   - Hi-DPI strokes (we scale the canvas backing buffer to devicePixelRatio).\n *   - Variable stroke width (faster movement = thinner line) for an\n *     ink-pen feel rather than a flat Sharpie.\n *   - Undo last stroke (popping the strokes array and replaying).\n *   - Clear pad (reset to blank).\n *   - Resize-aware redraw (so the signature doesn't disappear if the\n *     viewport changes mid-flow).\n */\nexport default function PublicSignContract({ contractId, lang = \"en\" }: Props) {\n  const canvasRef = useRef<HTMLCanvasElement | null>(null);\n  const strokesRef = useRef<Stroke[]>([]);\n  const drawingRef = useRef<Stroke | null>(null);\n  const [hasInk, setHasInk] = useState(false);\n  const [name, setName] = useState(\"\");\n  const [submitting, setSubmitting] = useState(false);\n  const [status, setStatus] = useState<\"idle\" | \"ok\" | \"error\">(\"idle\");\n  const [err, setErr] = useState<string | undefined>();\n\n  // ---- canvas plumbing ------------------------------------------------------\n\n  function getCanvas() {\n    return canvasRef.current!;\n  }\n\n  function setupCanvas() {\n    const canvas = getCanvas();\n    if (!canvas) return;\n    const dpr = globalThis.devicePixelRatio || 1;\n    const rect = canvas.getBoundingClientRect();\n    canvas.width = Math.round(rect.width * dpr);\n    canvas.height = Math.round(rect.height * dpr);\n    const ctx = canvas.getContext(\"2d\");\n    if (!ctx) return;\n    ctx.scale(dpr, dpr);\n    redraw();\n  }\n\n  function redraw() {\n    const canvas = getCanvas();\n    if (!canvas) return;\n    const ctx = canvas.getContext(\"2d\");\n    if (!ctx) return;\n    const rect = canvas.getBoundingClientRect();\n    ctx.clearRect(0, 0, rect.width, rect.height);\n    ctx.lineCap = \"round\";\n    ctx.lineJoin = \"round\";\n    ctx.strokeStyle = TEAL;\n    for (const stroke of strokesRef.current) {\n      drawStroke(ctx, stroke);\n    }\n    if (drawingRef.current) drawStroke(ctx, drawingRef.current);\n  }\n\n  function drawStroke(ctx: CanvasRenderingContext2D, stroke: Stroke) {\n    const pts = stroke.points;\n    if (pts.length < 2) return;\n    for (let i = 1; i < pts.length; i++) {\n      const a = pts[i - 1];\n      const b = pts[i];\n      const dt = Math.max(1, b.t - a.t);\n      const dist = Math.hypot(b.x - a.x, b.y - a.y);\n      const speed = dist / dt; // px / ms\n      // Faster = thinner. 0.05px/ms → ~3.2px; 1.5px/ms → ~1.0px.\n      const width = Math.max(0.9, Math.min(3.2, 3.4 - speed * 1.6));\n      ctx.lineWidth = width;\n      ctx.beginPath();\n      ctx.moveTo(a.x, a.y);\n      ctx.lineTo(b.x, b.y);\n      ctx.stroke();\n    }\n  }\n\n  function pointerXY(\n    e: PointerEvent | MouseEvent | TouchEvent,\n  ): { x: number; y: number } {\n    const canvas = getCanvas();\n    const rect = canvas.getBoundingClientRect();\n    const native = \"touches\" in e\n      ? (e.touches[0] ?? (e as TouchEvent).changedTouches[0])\n      : (e as PointerEvent | MouseEvent);\n    const x = (native as { clientX: number }).clientX - rect.left;\n    const y = (native as { clientY: number }).clientY - rect.top;\n    return { x, y };\n  }\n\n  // ---- event handlers -------------------------------------------------------\n\n  function onPointerDown(e: PointerEvent) {\n    e.preventDefault();\n    const canvas = getCanvas();\n    canvas.setPointerCapture(e.pointerId);\n    const { x, y } = pointerXY(e);\n    drawingRef.current = { points: [{ x, y, t: performance.now() }] };\n    // Clear the helper placeholder as soon as the first stroke begins, not\n    // on pointer-up — otherwise it overlaps the ink through the first stroke.\n    setHasInk(true);\n  }\n\n  function onPointerMove(e: PointerEvent) {\n    if (!drawingRef.current) return;\n    e.preventDefault();\n    const { x, y } = pointerXY(e);\n    drawingRef.current.points.push({ x, y, t: performance.now() });\n    redraw();\n  }\n\n  function onPointerUp(e: PointerEvent) {\n    if (!drawingRef.current) return;\n    e.preventDefault();\n    const canvas = getCanvas();\n    try {\n      canvas.releasePointerCapture(e.pointerId);\n    } catch { /* noop */ }\n    if (drawingRef.current.points.length > 1) {\n      strokesRef.current.push(drawingRef.current);\n    }\n    drawingRef.current = null;\n    // Re-sync to real ink: a bare tap (no movement) added no stroke, so the\n    // placeholder should come back rather than stay hidden over a blank pad.\n    setHasInk(strokesRef.current.length > 0);\n    redraw();\n  }\n\n  function clearPad() {\n    strokesRef.current = [];\n    drawingRef.current = null;\n    setHasInk(false);\n    redraw();\n  }\n\n  function undoStroke() {\n    if (strokesRef.current.length === 0) return;\n    strokesRef.current.pop();\n    setHasInk(strokesRef.current.length > 0);\n    redraw();\n  }\n\n  // ---- lifecycle ------------------------------------------------------------\n\n  useEffect(() => {\n    setupCanvas();\n    const onResize = () => setupCanvas();\n    globalThis.addEventListener(\"resize\", onResize);\n    return () => globalThis.removeEventListener(\"resize\", onResize);\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, []);\n\n  // ---- submit ---------------------------------------------------------------\n\n  /** Render a compact PNG of just the inked region. The full canvas at\n   *  hi-DPI is ~150KB+ which trips the backend's 64KB row-size limit;\n   *  this shrinks to ~3-10KB by (a) cropping to the strokes' bounding box\n   *  and (b) capping output width at 480px. */\n  function exportSignaturePng(): string {\n    const all = strokesRef.current;\n    if (all.length === 0) return \"\";\n    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;\n    for (const s of all) {\n      for (const p of s.points) {\n        if (p.x < minX) minX = p.x;\n        if (p.y < minY) minY = p.y;\n        if (p.x > maxX) maxX = p.x;\n        if (p.y > maxY) maxY = p.y;\n      }\n    }\n    const pad = 10;\n    minX = Math.max(0, minX - pad);\n    minY = Math.max(0, minY - pad);\n    maxX = maxX + pad;\n    maxY = maxY + pad;\n    const srcW = Math.max(1, maxX - minX);\n    const srcH = Math.max(1, maxY - minY);\n    const maxW = 480;\n    const scale = srcW > maxW ? maxW / srcW : 1;\n    const outW = Math.round(srcW * scale);\n    const outH = Math.round(srcH * scale);\n    const off = document.createElement(\"canvas\");\n    off.width = outW;\n    off.height = outH;\n    const ctx = off.getContext(\"2d\");\n    if (!ctx) return \"\";\n    ctx.fillStyle = \"#ffffff\";\n    ctx.fillRect(0, 0, outW, outH);\n    ctx.translate(-minX * scale, -minY * scale);\n    ctx.scale(scale, scale);\n    ctx.lineCap = \"round\";\n    ctx.lineJoin = \"round\";\n    ctx.strokeStyle = TEAL;\n    for (const stroke of all) drawStroke(ctx, stroke);\n    return off.toDataURL(\"image/png\");\n  }\n\n  async function onSign(e: Event) {\n    e.preventDefault();\n    const trimmed = name.trim();\n    if (!trimmed || !hasInk) return;\n    setSubmitting(true);\n    setErr(undefined);\n    try {\n      const dataUrl = exportSignaturePng();\n      const r = await fetch(`/api/contracts/${contractId}/sign`, {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({ name: trimmed, signature: dataUrl }),\n      });\n      if (!r.ok) {\n        const text = await r.text().catch(() => \"\");\n        throw new Error(text.slice(0, 200) || `${r.status}`);\n      }\n      setStatus(\"ok\");\n      // Reload after a brief moment so the SSR-rendered \"signed\" state\n      // takes over: the customer-signature card on the right fills in\n      // with the typed name + date, matching the contractor card on the\n      // left.\n      setTimeout(() => {\n        try {\n          globalThis.location.reload();\n        } catch { /* SSR-safe */ }\n      }, 900);\n    } catch (e) {\n      setStatus(\"error\");\n      setErr((e as Error).message);\n    } finally {\n      setSubmitting(false);\n    }\n  }\n\n  // ---- render ---------------------------------------------------------------\n\n  if (status === \"ok\") {\n    return (\n      <div\n        style={`margin-top:24px;background:linear-gradient(135deg,rgba(81,152,67,0.10) 0%,rgba(81,152,67,0.04) 100%);border:1px solid rgba(72,158,95,0.35);border-radius:18px;padding:24px;text-align:center`}\n      >\n        <div\n          style={`width:48px;height:48px;border-radius:50%;background:${GREEN};color:#fff;display:inline-flex;align-items:center;justify-content:center;margin-bottom:10px`}\n        >\n          <svg\n            width=\"22\"\n            height=\"22\"\n            viewBox=\"0 0 24 24\"\n            fill=\"none\"\n            stroke=\"currentColor\"\n            stroke-width=\"3\"\n            stroke-linecap=\"round\"\n            stroke-linejoin=\"round\"\n          >\n            <path d=\"M20 6 9 17l-5-5\" />\n          </svg>\n        </div>\n        <div style={`font-weight:800;color:${GREEN};font-size:18px`}>\n          {tFor(lang, \"publicSign.success.title\")}\n        </div>\n        <div\n          style={`margin-top:6px;color:${MUTED};font-size:13px;max-width:320px;margin-left:auto;margin-right:auto`}\n        >\n          {tFor(lang, \"publicSign.success.body\")}\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <form onSubmit={onSign} style=\"margin-top:24px;text-align:left\">\n      {/* Sign header strip */}\n      <div\n        style={`display:flex;justify-content:space-between;align-items:center;margin-bottom:8px`}\n      >\n        <div\n          style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${PINK_DARK}`}\n        >\n          {tFor(lang, \"publicSign.eyebrow\")}\n        </div>\n        <div style={`display:flex;gap:8px`}>\n          <button\n            type=\"button\"\n            onClick={undoStroke}\n            disabled={!hasInk}\n            aria-label={tFor(lang, \"publicSign.undoAria\")}\n            style={`background:transparent;border:1px solid ${LINE};border-radius:8px;padding:6px 10px;font-size:11px;font-weight:700;color:${\n              hasInk ? INK : MUTED\n            };cursor:${\n              hasInk ? \"pointer\" : \"not-allowed\"\n            };display:inline-flex;align-items:center;gap:6px`}\n          >\n            <svg\n              width=\"12\"\n              height=\"12\"\n              viewBox=\"0 0 24 24\"\n              fill=\"none\"\n              stroke=\"currentColor\"\n              stroke-width=\"2.5\"\n              stroke-linecap=\"round\"\n              stroke-linejoin=\"round\"\n            >\n              <path d=\"M3 7v6h6\" />\n              <path d=\"M21 17a9 9 0 0 0-15-6.7L3 13\" />\n            </svg>\n            {tFor(lang, \"publicSign.undo\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={clearPad}\n            disabled={!hasInk}\n            aria-label={tFor(lang, \"publicSign.clearAria\")}\n            style={`background:transparent;border:1px solid ${LINE};border-radius:8px;padding:6px 10px;font-size:11px;font-weight:700;color:${\n              hasInk ? INK : MUTED\n            };cursor:${\n              hasInk ? \"pointer\" : \"not-allowed\"\n            };display:inline-flex;align-items:center;gap:6px`}\n          >\n            <svg\n              width=\"12\"\n              height=\"12\"\n              viewBox=\"0 0 24 24\"\n              fill=\"none\"\n              stroke=\"currentColor\"\n              stroke-width=\"2.5\"\n              stroke-linecap=\"round\"\n              stroke-linejoin=\"round\"\n            >\n              <path d=\"M3 6h18\" />\n              <path d=\"M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2\" />\n              <path d=\"M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6\" />\n            </svg>\n            {tFor(lang, \"publicSign.clear\")}\n          </button>\n        </div>\n      </div>\n\n      {/* Pad */}\n      <div\n        style={`position:relative;background:#fff;border:2px dashed ${PINK};border-radius:18px;overflow:hidden;box-shadow:0 8px 24px -10px rgba(255,107,107,0.30)`}\n      >\n        {/* faint grid + baseline so it feels like a signing card */}\n        <div\n          aria-hidden=\"true\"\n          style={`position:absolute;inset:0;background-image:linear-gradient(rgba(255,107,107,0.05) 1px,transparent 1px),linear-gradient(90deg,rgba(255,107,107,0.05) 1px,transparent 1px);background-size:20px 20px;pointer-events:none`}\n        />\n        <div\n          aria-hidden=\"true\"\n          style={`position:absolute;left:32px;right:32px;bottom:36px;border-bottom:1.5px solid rgba(255,107,107,0.35);pointer-events:none`}\n        />\n        <div\n          aria-hidden=\"true\"\n          style={`position:absolute;left:24px;bottom:30px;font-family:'Helvetica Neue',Helvetica,Arial,sans-serif;font-weight:900;color:rgba(255,107,107,0.45);font-size:18px;pointer-events:none`}\n        >\n          ✕\n        </div>\n        <canvas\n          ref={canvasRef}\n          onPointerDown={onPointerDown}\n          onPointerMove={onPointerMove}\n          onPointerUp={onPointerUp}\n          onPointerCancel={onPointerUp}\n          onPointerLeave={onPointerUp}\n          style={`display:block;width:100%;height:200px;touch-action:none;cursor:crosshair`}\n        />\n        {!hasInk && (\n          <div\n            style={`position:absolute;inset:0;display:flex;align-items:center;justify-content:center;flex-direction:column;color:${MUTED};font-size:13px;pointer-events:none;text-align:center;padding:0 16px`}\n            aria-hidden=\"true\"\n          >\n            <div style={`font-size:14px;font-weight:700;color:${TEAL}`}>\n              {tFor(lang, \"publicSign.pad.heading\")}\n            </div>\n            <div style={`margin-top:4px;font-size:12px;color:${MUTED}`}>\n              {tFor(lang, \"publicSign.pad.sub\")}\n            </div>\n          </div>\n        )}\n      </div>\n\n      {/* Typed legal name */}\n      <label\n        style={`display:block;margin-top:18px;font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED};margin-bottom:6px`}\n      >\n        {tFor(lang, \"publicSign.nameLabel\")}\n      </label>\n      <input\n        type=\"text\"\n        value={name}\n        onInput={(e) => setName((e.target as HTMLInputElement).value)}\n        placeholder={tFor(lang, \"publicSign.namePlaceholder\")}\n        autoComplete=\"name\"\n        style={`width:100%;padding:14px 16px;border:1px solid ${LINE};border-radius:12px;font-size:16px;color:${INK};font-family:inherit;background:#fff;box-sizing:border-box`}\n        required\n      />\n      <div style={`margin-top:8px;font-size:12px;color:${MUTED}`}>\n        {tFor(lang, \"publicSign.disclaimer\")}\n      </div>\n\n      {err && (\n        <div style={`margin-top:12px;color:#b3261e;font-size:13px`}>\n          {tFor(lang, \"publicSign.error\", { err })}\n        </div>\n      )}\n\n      <button\n        type=\"submit\"\n        disabled={submitting || !name.trim() || !hasInk}\n        style={`margin-top:18px;width:100%;background:${\n          submitting || !name.trim() || !hasInk\n            ? \"#a8c8a0\"\n            : `linear-gradient(135deg,${GREEN} 0%,#71a85f 100%)`\n        };color:#fff;border:0;font-weight:800;font-size:16px;padding:18px 28px;border-radius:14px;box-shadow:${\n          submitting || !name.trim() || !hasInk\n            ? \"none\"\n            : \"0 10px 22px -6px rgba(81,152,67,0.55)\"\n        };cursor:${\n          submitting || !name.trim() || !hasInk ? \"default\" : \"pointer\"\n        };transition:transform 160ms;display:flex;align-items:center;justify-content:center;gap:10px`}\n      >\n        {submitting\n          ? (\n            <>\n              <span class=\"spinner\" aria-hidden=\"true\" />{\" \"}\n              {tFor(lang, \"publicSign.submitting\")}\n            </>\n          )\n          : (\n            <>\n              <svg\n                width=\"18\"\n                height=\"18\"\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                stroke-width=\"2.5\"\n                stroke-linecap=\"round\"\n                stroke-linejoin=\"round\"\n              >\n                <path d=\"M12 19l9 2-2-9-9-9-7 7z\" />\n                <path d=\"M14 5l5 5\" />\n              </svg>\n              <span>\n                {hasInk && name.trim()\n                  ? tFor(lang, \"publicSign.submitEnabled\")\n                  : tFor(lang, \"publicSign.submitDisabled\")}\n              </span>\n            </>\n          )}\n      </button>\n    </form>\n  );\n}\n", "sourceName": "PublicSignContract.tsx", "checklist": "# Capture checklist — PublicSignContract\n\n- **Route:** `/c/<contractId>` (PUBLIC, auth-free, needs a real unsigned contract).\n- **Surface:** inline-styled **public palette**.\n- **Viewports:** 390 (mobile-first — primary touch target), 640.\n- **Crop targets:** the signature canvas, the typed-name field, the submit button,\n  the post-sign success panel.\n- **States to drive:**\n  - `empty` — blank canvas, submit disabled (`hasInk` false).\n  - `ink` — pointer-drawn signature present.\n  - `typed-name` — name typed.\n  - `submitting` — spinner.\n  - `signed` — 900 ms success panel before the hard reload (flagged anti-pattern).\n- **Motion/jank:** capture is secondary to the documented jank lint —\n  `onPointerMove` calls `getBoundingClientRect()` twice/sample + full stroke\n  redraw per sample (no rAF). **A11y:** ink is pointer-only; keyboard/SR users\n  are blocked from signing — note in any review still.\n- **Theme:** light only. **No fabricated screenshots** — needs a live backend + valid token.\n"}]}, {"name": "public-invoice", "pageMd": "# Page: `/i/:id` — Public invoice\n\n**Route:** `routes/i/[id].tsx` · **Source copied to:** `js/[id].tsx`. Island in\n`components/public-invoice-claim/`.\n\n## Purpose\nCustomer-facing invoice document opened from an SMS/email link. Shows amount\ndue, milestone context, prior payments, and (when unpaid) lets the customer\n**claim a manual payment** (MANUAL-ONLY: Zelle / CashApp / check / cash / etc.\n— no card processing). Claim → contractor confirms → paid.\n\n## Classification & data flow\n- **Page tier:** **SSR document** (`define.page` async) + one claim island. No\n  auth — token-shaped `:id`.\n- **Data (server):** `ssrBackendGet<InvoicePublic>('/invoices/:id/public')`. Rich\n  composite (see `data-model.md` §1.7): `{ id, contractId?, customerId?, status?,\n  amount?, dueDate?, issuedDate?, paidAt?, installmentIndex?, installmentTotal?,\n  paymentIntent?{method,amount,reference?,claimedAt,claimedBy?}, contractor?{…,\n  acceptedPaymentMethods?}, customer?{…}, jobDetails?{…ByLang}, siblings?[…],\n  acceptedMethods?[{method,handle?}] }`. On `!ok` → `ErrorCard` (link expired).\n- **Derived state (in `InvoiceDoc`):** `paid` (status===\"paid\" || paidAt),\n  `claimed` (status===\"claimed\" && paymentIntent), `pastDue` (isPastDue(dueDate)),\n  `es` (contractor.commsLanguage), `jobName` (…ByLang fallback chain),\n  `milestoneLabel` (installmentIndex/Total → deposit/progress/final),\n  `paidSoFar` (siblings that are paid).\n- **Language:** `commsLanguage === \"es\"` → `es`, else `en`; `tFor(lang, …)`.\n- **Money:** `fmtMoneyExact(amount)` from `lib/format.ts` (amount in **cents**).\n- **Surface:** **public inline-styled palette** (NOT Sabor). Consts:\n  `PINK #FF6B6B`, `PINK_DARK #d94e4e`, `TEAL #144852`, `GREEN #519843`,\n  `INK #1c2c30`, `MUTED #6b7a7e`, `LINE #e3e8e6`, `BG #f7f6f1`. System font;\n  `'Helvetica Neue'` for the hero + amount numerals. Loads `/landing.css` for resets.\n\n## `<Head>`\n`<title>{publicInvoice.docTitle}</title>` · `<link href=\"/landing.css\">`.\n\n## Layout / composition order\n```\ndiv (BG, min-h 100dvh, kb-inset padding)\n  div max-width:680px\n    err|!invoice → <ErrorCard message />  (brand eyebrow + white card)\n    else → <InvoiceDoc invoice />:\n      article (white, radius 24, pink-bordered, shadow)\n        8px top bar  → linear-gradient(PINK → PINK_DARK)\n        padding 32/36:\n          header row (space-between, wrap):\n            left: optional logo (/api/public-logo/invoice/:id), businessLabel\n                  eyebrow (PINK_DARK), contractor.addressLine (MUTED)\n            right: <StatusPill paid|claimed|pastDue|due es />  (Pill: paid=GREEN,\n                   claimed=amber \"awaiting confirmation\", pastDue=red, due=pink)\n          h1   jobName  (TEAL, 32px, 900, Helvetica Neue)\n          milestoneLabel (when installments)  (MUTED)\n          amount card (green gradient #e8f3e2→#dceadb):\n            label  status.paid | publicInvoice.amountDue ;  dueOn/paidOn date\n            value  fmtMoneyExact(amount)  (TEAL, 38px, tabular-nums)\n          paidSoFar strip (when prior installments paid): green pills #idx · $amt\n          ── one of:\n             paid    → <ReceivedNote paidAt contractorFirst es />     (green)\n             claimed → <ClaimedNote intent contractorFirst es />      (amber; method+reference)\n             else    → <PublicInvoiceClaim invoiceId acceptedMethods customerName lang /> ← ISLAND\n          contact footer (when phone/email): tel:/mailto: links (TEAL)\n        powered-by footer: /logo.png + publicInvoice.poweredBy {id: first-8 upper}\n```\n\n## Components\n| Component | Folder | Tier |\n|---|---|---|\n| `PublicInvoiceClaim` | `components/public-invoice-claim/` | island — customer picks a method + reference and claims payment → POST claim; transitions the doc to the \"claimed/awaiting confirmation\" state. MANUAL-ONLY. |\n\nRoute-level inline render functions documented as page-composition: `InvoiceDoc`,\n`ErrorCard`, `StatusPill`, `Pill`, `ClaimedNote`, `ReceivedNote` (+ helpers\n`milestoneTitle`, `methodFriendly`, `isPastDue`).\n\n## Data-shape hazard\n`/invoices/:id/public` fans out a deep join: invoice → contract → quote →\ncustomer → contractor profile → accepted methods → **sibling installments**.\nEvery customer link-load triggers it — flag for denormalization/caching.\n\n## Capture checklist (auth-free, needs valid token id)\n- URL: `/i/<invoiceId>`. Viewports: 390 + 640 (single column, max-width 680).\n- States: due, past-due, claimed (awaiting confirmation), paid (ReceivedNote),\n  with-installments (paidSoFar strip + milestone label), expired/error, `es`.\n- Light theme only.\n\n## Build order\npublic palette primitives → `public-invoice-claim` island → this page.\n", "components": [{"name": "public-invoice-claim", "component": "PublicInvoiceClaim", "spec": "# PublicInvoiceClaim\n\nThe customer-facing **\"I'm paying you by X\"** control on the public invoice page\n(`/i/:id`). The contractor's accepted payment methods render as chip buttons;\ntapping a chip reveals that method's handle/address and a reference + name input;\n\"I sent it\" POSTs a **payment claim**. This is the customer half of the\n**MANUAL-ONLY** payment flow — the customer *claims* a payment, the contractor\nlater *confirms* it. **There is no card processing / merchant rail anywhere.**\n\nSource: `islands/PublicInvoiceClaim.tsx` (copied to `js/PublicInvoiceClaim.tsx`,\n313 lines). Mounted by `routes/i/[id].tsx` under the invoice document.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PublicInvoiceClaim.tsx`).\n- **Interaction tier:** `island` — client state + a `fetch` POST mutation. NOT a\n  Fresh Partial, NOT an SSR form/PRG.\n- **Surface:** **public palette** (inline hex — `#FF6B6B`/`#d94e4e` pink active\n  chip, `#519843` green submit, `#144852` teal, `#1c2c30` ink, `#6b7a7e` muted,\n  `#e3e8e6` hairline), NOT Sabor. Emits NO `<style>` and uses NO CSS classes —\n  100% inline (see `css/public-invoice-claim.css`, which records the literals +\n  the `data-cy`/`data-kbd-group` hooks).\n- **Manual-only payment model (data-model alignment):**\n  - The flow ties to **`Invoice.paymentIntent`** — the customer claim is what\n    *creates* it. `POST /api/invoices/:id/claim-payment { method, reference?,\n    claimedBy? }` flips the invoice to `status:\"claimed\"` and stamps\n    `paymentIntent` (method/amount/reference/claimedAt/claimedBy). The contractor\n    later confirms → creates a `Payment` row → invoice flips to `paid`. **This\n    island never sees money move** — it records intent, nothing more.\n  - **No card/merchant processing.** The chips are pay-OUTSIDE-the-app rails\n    (Zelle/CashApp/check/cash/Venmo/PayPal/ACH/other). Even when `card`/`ach`\n    appear in the method enum they are *log-how-you-paid* labels, not in-app\n    charges — consistent with the project's manual-only rule.\n- **Client state owned:**\n  - `selected: string | undefined` — the chosen method key (default `undefined`;\n    no detail panel until one is picked).\n  - `reference: string` — optional confirmation #/memo (placeholder varies by\n    method).\n  - `claimedBy: string` — pre-filled from the `customerName` prop, editable.\n  - `submitting: boolean` — POST in flight.\n  - `done: boolean` — terminal latch → swaps to the green \"thanks\" panel.\n  - `error: string | undefined` — inline error in the detail panel.\n- **Server mutation + feedback:**\n  - `submit()` early-returns unless `selected` AND not already `submitting`. POSTs\n    only the present keys (`reference`/`claimedBy` omitted when blank),\n    `credentials:\"include\"`. On non-ok: parses `body.reason` and shows it, else\n    `publicInvoiceClaim.submitError` (\"Couldn't record that (HTTP {status})\"). On\n    ok → `setDone(true)`.\n  - **Flash:** inline only. The \"I sent it\" button swaps to\n    `publicInvoiceClaim.sending` and `cursor:wait`, `disabled` while submitting;\n    error renders inline in the detail panel; success swaps the whole section to\n    the thanks panel.\n- **`location.reload()` FLAG — NONE, and that is the GOOD pattern.** Success is a\n  pure in-memory state swap (`done=true` → thanks panel); the page does NOT hard\n  refresh. This is the correct contrast to the sibling `PublicSignContract`, which\n  DOES `location.reload()` after sign. Do NOT introduce a reload here on rebuild.\n- **Liveness:** request-response only. No polling/websocket.\n- **Data source / honest-empty:**\n  - Owns no fetch on mount — receives `invoiceId`, `acceptedMethods`,\n    `customerName`, `lang` as props (the SSR route joins\n    `InvoicePublic.acceptedMethods` from the contractor's identity).\n  - **Honest-empty:** if `acceptedMethods.length === 0`, the island renders the\n    pink fallback panel `publicInvoiceClaim.noMethods` (\"Reach out to your\n    contractor to coordinate payment…\") instead of an empty chip row — no\n    selectable UI. This is the brand-new / unconfigured-payments case.\n- **Reactivity / i18n:** `lang` is a **prop** (`\"en\"|\"es\"`, default `\"en\"`),\n  resolved by the route from the contractor's comms language; all strings via\n  `tFor(lang,key,vars)` plus three module helpers — `methodLabel`,\n  `methodInstructions` (per-method send copy, handle-aware), `referencePlaceholder`\n  (per-method input hint). No on-page toggle → NOT reactive to `langSignal`.\n- **Data-shape hazards:** none of its own (it consumes a small `Method[]`). The\n  page inherits data-model.md hazard #7 — `GET /invoices/:id/public` fans out a\n  join (invoice → contract → quote → customer → contractor profile → accepted\n  methods → sibling installments) on every uncached, SMS/email-driven load.\n  `acceptedMethods` here is the joined `acceptedMethods` slice\n  (`{ method, handle? }[]`); `card`/`ach` may carry a raw `handle` shown verbatim.\n\n## 2. Anatomy (inline styles; see css for literals + data-cy hooks)\n```\nacceptedMethods.length === 0  →  <section data-cy=\"claim-no-methods\">  (pink fallback)\n   bg rgba(255,107,107,0.04); border rgba(255,107,107,0.20); radius 14px\n   → publicInvoiceClaim.noMethods\n\ndone === true  →  <section data-cy=\"claim-thanks\">  (green terminal)\n   bg rgba(81,152,67,0.08); border rgba(81,152,67,0.30); radius 14px\n   <div eyebrow #519843>  publicInvoiceClaim.thanksHeading\n   <p #1c2c30 15px>       publicInvoiceClaim.thanksBody\n\nelse  →  <section data-cy=\"claim-form\" margin-top:28px>\n  <div eyebrow #6b7a7e>  publicInvoiceClaim.howToPay\n  <div chip-row flex-wrap gap:8px>\n     acceptedMethods.map(m →\n       <button data-cy=claim-method-{m.method}  onClick=setSelected(m.method)\n          style=\"radius:999px; padding:9px 16px; 13.5px/700;\n                 active ? bg #FF6B6B/color #fff/border #d94e4e\n                        : bg #fff /color #1c2c30/border #e3e8e6\">\n          {methodLabel(m.method, lang)} />)\n  {selectedMethod &&\n   <div data-cy=\"claim-detail\" data-kbd-group\n        style=\"bg #fff; border 1px #e3e8e6; radius:14px; padding:18px 20px\">\n     <div eyebrow #144852>  {methodLabel(selectedMethod.method, lang)}\n     <div #1c2c30 14.5px>   {methodInstructions(selectedMethod, lang)}   ← \"Send to <handle> on Zelle\"\n     <label>  publicInvoiceClaim.referenceLabel\n       <input data-cy=claim-reference value=reference placeholder=referencePlaceholder(...)/>\n     <label>  publicInvoiceClaim.nameLabel\n       <input data-cy=claim-name value=claimedBy placeholder=namePlaceholder/>\n     {error && <div #a83b3b 13px>{error}</div>}\n     <button data-cy=claim-submit type=button onClick=submit disabled={submitting}\n        style=\"width:100%; bg #519843; color #fff; radius:12px; padding:14px 18px;\n               15px/800; box-shadow:0 8px 18px -6px rgba(81,152,67,0.45);\n               cursor: submitting ? wait : pointer\">\n        { submitting ? publicInvoiceClaim.sending : publicInvoiceClaim.iSentIt }\n     </button>\n   </div>}\n</section>\n```\n- **`methodInstructions` (handle-aware):** Venmo/Zelle/CashApp/PayPal →\n  \"Send to {handle} on {svc}\" (or \"Send via {svc}\" if no handle); check →\n  mail-to address or generic; cash → coordinate copy; ach/card/other → raw\n  `handle` if present else generic.\n- **`data-kbd-group`** on the detail panel = a mobile soft-keyboard scroll hook\n  (keeps the focused input in view above the keyboard inset).\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `invoiceId` | `string` (required) | — | text | no |\n| `acceptedMethods` | `{ method: string; handle?: string }[]` (required) | — | json | no |\n| `customerName` | `string?` | `undefined` (pre-fills `claimedBy`) | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n`selected`/`reference`/`claimedBy`/`submitting`/`done`/`error` are internal\n`useState` — drive via Events / case `_signals`.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| methods | chip row, none selected (no detail panel) | `cases/methods/methods.json` |\n| zelle-selected | a chip active (pink) + detail panel (\"Send to … on Zelle\") + inputs | `cases/zelle-selected/zelle-selected.json` |\n| submitting | claim POST in flight → \"Sending…\", cursor:wait, disabled | `cases/submitting/submitting.json` |\n| thanks | `done=true` green panel \"payment marked as sent\" (NO reload) | `cases/thanks/thanks.json` |\n| no-methods | `acceptedMethods=[]` → pink fallback \"coordinate payment\" | `cases/no-methods/no-methods.json` |\n| error | claim POST failed → red error line in detail panel; form stays open | `cases/error/error.json` |\n| es | Spanish strings + method labels (Cheque/Efectivo) + \"Ya lo envié\" | `cases/es/es.json` |\n\n## 5. Events (`capture(page)` predicates)\n- `ev.expect(e => e.source === \"button.claim-method-zelle\" && e.type === \"click\")`\n  → `setSelected(\"zelle\")` → reveals the detail panel (one per method key:\n  `claim-method-cashapp`, `-check`, `-cash`, …).\n- `ev.expect(e => e.source === \"input.claim-reference\" && e.type === \"input\")` →\n  `setReference(value)`.\n- `ev.expect(e => e.source === \"input.claim-name\" && e.type === \"input\")` →\n  `setClaimedBy(value)`.\n- `ev.expect(e => e.source === \"button.claim-submit\" && e.type === \"click\")` →\n  `submit()` → `POST /api/invoices/:id/claim-payment` → on ok `setDone(true)`\n  (thanks panel, NO reload); on fail `setError(...)`.\n- Re-entrancy guard: `submit()` early-returns while `submitting` (the only\n  double-submit guard) — preserve it.\n- (Selectors use the source's `data-cy` hooks — stable; reuse them on rebuild.)\n\n## 6. Motion (real CSS only)\n- **None declared.** No `@keyframes`, no `transition`. Chip active/inactive,\n  detail-panel reveal, \"Sending…\" label swap, and the thanks/no-methods panels are\n  all instant. Nothing to film; no reduced-motion concern.\n\n## 7. Responsive\n- No own `@media`. The chip row is `display:flex; flex-wrap:wrap; gap:8px` so chips\n  wrap on narrow screens; the detail panel + inputs are full-width\n  (`box-sizing:border-box`). Lives inside the route's document column. The\n  `data-kbd-group` hook handles soft-keyboard avoidance on mobile.\n\n## 8. A11y\n- The detail-panel labels are real `<label>`/`<span>` pairs wrapping their inputs.\n- **Gaps:**\n  - Chips are `<button type=\"button\">` (focusable — good) but expose no\n    `aria-pressed` for the active/selected state — a SR user can't tell which\n    method is chosen. Add `aria-pressed={active}`.\n  - The inline error is a plain `<div>` (no `role=\"alert\"`/`aria-live`) → a claim\n    failure isn't announced. Add `role=\"alert\"`.\n  - On success no focus is moved to the thanks panel and it has no `role=\"status\"`.\n  - The detail panel appears on chip select with no focus management (focus stays\n    on the chip); consider moving focus to the reference input.\n\n## 9. Used on\n- `/i/:id` only — mounted by `routes/i/[id].tsx` under the invoice document,\n  passed `invoiceId`, `acceptedMethods` (joined from `InvoicePublic`),\n  `customerName`, `lang`. Sole importer.\n", "fixture": "{\n  \"category\": \"public-invoice\",\n  \"folder\": \"public-invoice-claim\",\n  \"controls\": {\n    \"invoiceId\": { \"type\": \"text\", \"value\": \"inv_3b8c1d2e\" },\n    \"acceptedMethods\": {\n      \"type\": \"json\",\n      \"value\": [\n        { \"method\": \"zelle\", \"handle\": \"hans@hansendecks.com\" },\n        { \"method\": \"cashapp\", \"handle\": \"$hansendecks\" },\n        { \"method\": \"check\", \"handle\": \"812 Cedar Ln, Austin, TX 78704\" },\n        { \"method\": \"cash\" }\n      ]\n    },\n    \"customerName\": { \"type\": \"text\", \"value\": \"Maria Delgado\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "error.json", "content": "{\n  \"_name\": \"Error — claim POST failed; red error line inside the detail panel (body.reason or 'Couldn't record that (HTTP 500)'), form stays open\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [\n    { \"method\": \"zelle\", \"handle\": \"hans@hansendecks.com\" },\n    { \"method\": \"cash\" }\n  ],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"en\",\n  \"_signals\": { \"selected\": \"zelle\", \"error\": \"already_claimed\", \"submitting\": false, \"claimedBy\": \"Maria Delgado\" }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — lang='es'; '¿Cómo quieres pagar?', method labels (Cheque/Efectivo), 'Ya lo envié' submit\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [\n    { \"method\": \"zelle\", \"handle\": \"hans@hansendecks.com\" },\n    { \"method\": \"check\", \"handle\": \"812 Cedar Ln, Austin, TX 78704\" },\n    { \"method\": \"cash\" }\n  ],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"es\",\n  \"_signals\": { \"selected\": \"check\", \"claimedBy\": \"Maria Delgado\" }\n}\n"}, {"name": "methods.json", "content": "{\n  \"_name\": \"Methods — chip row, none selected yet (no detail panel)\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [\n    { \"method\": \"zelle\", \"handle\": \"hans@hansendecks.com\" },\n    { \"method\": \"cashapp\", \"handle\": \"$hansendecks\" },\n    { \"method\": \"check\", \"handle\": \"812 Cedar Ln, Austin, TX 78704\" },\n    { \"method\": \"cash\" }\n  ],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"en\"\n}\n"}, {"name": "no-methods.json", "content": "{\n  \"_name\": \"No methods — contractor configured none; pink fallback panel ('Reach out to your contractor to coordinate payment…')\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"en\"\n}\n"}, {"name": "submitting.json", "content": "{\n  \"_name\": \"Submitting — POST /claim-payment in flight; button reads 'Sending…', cursor:wait, disabled\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [\n    { \"method\": \"cashapp\", \"handle\": \"$hansendecks\" },\n    { \"method\": \"cash\" }\n  ],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"en\",\n  \"_signals\": { \"selected\": \"cashapp\", \"submitting\": true, \"claimedBy\": \"Maria Delgado\", \"reference\": \"Apr rent\" }\n}\n"}, {"name": "thanks.json", "content": "{\n  \"_name\": \"Thanks — done=true after a successful claim; green panel ('Thanks — payment marked as sent'). NO reload (local state swap, the GOOD pattern vs PublicSignContract).\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [\n    { \"method\": \"zelle\", \"handle\": \"hans@hansendecks.com\" },\n    { \"method\": \"cash\" }\n  ],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"en\",\n  \"_signals\": { \"done\": true }\n}\n"}, {"name": "zelle-selected.json", "content": "{\n  \"_name\": \"Zelle selected — pink active chip + detail panel ('Send to hans@hansendecks.com on Zelle'), reference + name inputs, 'I sent it' button\",\n  \"invoiceId\": \"inv_3b8c1d2e\",\n  \"acceptedMethods\": [\n    { \"method\": \"zelle\", \"handle\": \"hans@hansendecks.com\" },\n    { \"method\": \"cashapp\", \"handle\": \"$hansendecks\" },\n    { \"method\": \"check\", \"handle\": \"812 Cedar Ln, Austin, TX 78704\" },\n    { \"method\": \"cash\" }\n  ],\n  \"customerName\": \"Maria Delgado\",\n  \"lang\": \"en\",\n  \"_signals\": { \"selected\": \"zelle\", \"claimedBy\": \"Maria Delgado\" }\n}\n"}], "css": "/* public-invoice-claim.css — EXTRACTED real CSS.\n *\n * The claim island is 100% inline-styled and emits NO <style> and uses NO CSS\n * classes — it relies only on inline `style=` strings. There is genuinely no\n * extractable rule. Recorded for completeness + the inline structure key.\n * Surface: PUBLIC palette.\n *\n * Method chip (button) inline styles (see js/PublicInvoiceClaim.tsx):\n *   inactive: background #fff; color #1c2c30 (INK); border 1px #e3e8e6 (LINE)\n *   active  : background #FF6B6B (PINK); color #fff; border 1px #d94e4e (PINK_DARK)\n *   shared  : border-radius:999px; padding:9px 16px; font 13.5px/700; cursor:pointer\n *\n * Detail panel (revealed on chip select):\n *   background #fff; border 1px #e3e8e6; border-radius:14px; padding:18px 20px\n *   inputs: padding:10px 12px; border 1px #e3e8e6; border-radius:10px; 14px\n *   \"I sent it\" button: background #519843 (GREEN); color #fff; border-radius:12px;\n *     padding:14px 18px; 15px/800; box-shadow:0 8px 18px -6px rgba(81,152,67,0.45);\n *     cursor: pointer | wait (while submitting). NO transition declared.\n *\n * Thanks panel: rgba(81,152,67,0.08) bg, rgba(81,152,67,0.30) border, radius 14px.\n * No-methods panel: rgba(255,107,107,0.04) bg, rgba(255,107,107,0.20) border.\n *\n * No motion, no @media, no reduced-motion concern.\n *\n * data-cy / data-kbd-group hooks present for test + mobile-keyboard scroll:\n *   claim-form, claim-no-methods, claim-thanks, claim-detail (also\n *   data-kbd-group), claim-method-<m>, claim-reference, claim-name,\n *   claim-submit.\n */\n", "cssName": "public-invoice-claim.css", "source": "import { useState } from \"preact/hooks\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\n\ninterface Method {\n  method: string;\n  handle?: string;\n}\n\ninterface Props {\n  invoiceId: string;\n  acceptedMethods: Method[];\n  customerName?: string;\n  /** Outgoing-comms language (roadmap p.13). Customer-facing. */\n  lang?: \"en\" | \"es\";\n}\n\nconst TEAL = \"#144852\";\nconst GREEN = \"#519843\";\nconst INK = \"#1c2c30\";\nconst MUTED = \"#6b7a7e\";\nconst LINE = \"#e3e8e6\";\nconst PINK = \"#FF6B6B\";\nconst PINK_DARK = \"#d94e4e\";\n\n/**\n * PublicInvoiceClaim — customer-side \"I'm paying you by X\" UI on the\n * public invoice page (/i/:id).\n *\n * Flow:\n *   1. Customer sees the contractor's accepted methods as chip buttons.\n *      Tapping a chip reveals that method's handle/address inline\n *      (\"Send to @hans-hansen\") and a small reference + name input.\n *   2. Customer hits \"I sent it\" → POSTs to /api/invoices/:id/claim-payment.\n *      Invoice flips to `claimed` server-side.\n *   3. Local state updates to a \"thank you\" view so the page doesn't need\n *      a hard refresh.\n *\n * If the contractor hasn't configured any methods, we render a fallback\n * \"reach out to coordinate payment\" message and a tel/mailto pair.\n */\nexport default function PublicInvoiceClaim(\n  { invoiceId, acceptedMethods, customerName, lang = \"en\" }: Props,\n) {\n  const [selected, setSelected] = useState<string | undefined>(undefined);\n  const [reference, setReference] = useState(\"\");\n  const [claimedBy, setClaimedBy] = useState(customerName ?? \"\");\n  const [submitting, setSubmitting] = useState(false);\n  const [done, setDone] = useState(false);\n  const [error, setError] = useState<string | undefined>(undefined);\n\n  if (acceptedMethods.length === 0) {\n    return (\n      <section\n        data-cy=\"claim-no-methods\"\n        style={`margin-top:24px;background:rgba(255,107,107,0.04);border:1px solid rgba(255,107,107,0.20);border-radius:14px;padding:18px 22px;color:${INK};font-size:14px;line-height:1.55`}\n      >\n        {tFor(lang, \"publicInvoiceClaim.noMethods\")}\n      </section>\n    );\n  }\n\n  if (done) {\n    return (\n      <section\n        data-cy=\"claim-thanks\"\n        style={`margin-top:24px;background:rgba(81,152,67,0.08);border:1px solid rgba(81,152,67,0.30);border-radius:14px;padding:20px 24px`}\n      >\n        <div\n          style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${GREEN}`}\n        >\n          {tFor(lang, \"publicInvoiceClaim.thanksHeading\")}\n        </div>\n        <p\n          style={`margin:8px 0 0;color:${INK};font-size:15px;line-height:1.55`}\n        >\n          {tFor(lang, \"publicInvoiceClaim.thanksBody\")}\n        </p>\n      </section>\n    );\n  }\n\n  const selectedMethod = selected\n    ? acceptedMethods.find((m) => m.method === selected)\n    : undefined;\n\n  async function submit() {\n    if (!selected || submitting) return;\n    setSubmitting(true);\n    setError(undefined);\n    try {\n      const res = await fetch(`/api/invoices/${invoiceId}/claim-payment`, {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        credentials: \"include\",\n        body: JSON.stringify({\n          method: selected,\n          ...(reference.trim() ? { reference: reference.trim() } : {}),\n          ...(claimedBy.trim() ? { claimedBy: claimedBy.trim() } : {}),\n        }),\n      });\n      if (!res.ok) {\n        const body = await res.json().catch(() => ({}));\n        throw new Error(\n          body.reason ??\n            tFor(lang, \"publicInvoiceClaim.submitError\", {\n              status: res.status,\n            }),\n        );\n      }\n      setDone(true);\n    } catch (err) {\n      setError((err as Error).message);\n    } finally {\n      setSubmitting(false);\n    }\n  }\n\n  return (\n    <section style=\"margin-top:28px\" data-cy=\"claim-form\">\n      <div\n        style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${MUTED}`}\n      >\n        {tFor(lang, \"publicInvoiceClaim.howToPay\")}\n      </div>\n      <div style=\"margin-top:12px;display:flex;flex-wrap:wrap;gap:8px\">\n        {acceptedMethods.map((m) => {\n          const active = m.method === selected;\n          return (\n            <button\n              type=\"button\"\n              key={m.method}\n              data-cy={`claim-method-${m.method}`}\n              onClick={() => setSelected(m.method)}\n              style={`appearance:none;cursor:pointer;background:${\n                active ? PINK : \"#fff\"\n              };color:${active ? \"#fff\" : INK};border:1px solid ${\n                active ? PINK_DARK : LINE\n              };border-radius:999px;padding:9px 16px;font-size:13.5px;font-weight:700;letter-spacing:.01em`}\n            >\n              {methodLabel(m.method, lang)}\n            </button>\n          );\n        })}\n      </div>\n\n      {selectedMethod\n        ? (\n          <div\n            data-cy=\"claim-detail\"\n            data-kbd-group\n            style={`margin-top:18px;background:#fff;border:1px solid ${LINE};border-radius:14px;padding:18px 20px`}\n          >\n            <div\n              style={`font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:${TEAL}`}\n            >\n              {methodLabel(selectedMethod.method, lang)}\n            </div>\n            <div\n              style={`margin-top:8px;color:${INK};font-size:14.5px;line-height:1.55`}\n            >\n              {methodInstructions(selectedMethod, lang)}\n            </div>\n            <label style=\"display:block;margin-top:14px\">\n              <span\n                style={`display:block;font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;color:${MUTED};margin-bottom:4px`}\n              >\n                {tFor(lang, \"publicInvoiceClaim.referenceLabel\")}\n              </span>\n              <input\n                type=\"text\"\n                value={reference}\n                data-cy=\"claim-reference\"\n                placeholder={referencePlaceholder(selectedMethod.method, lang)}\n                onInput={(e) =>\n                  setReference((e.currentTarget as HTMLInputElement).value)}\n                style={`width:100%;box-sizing:border-box;padding:10px 12px;border:1px solid ${LINE};border-radius:10px;font-size:14px;color:${INK}`}\n              />\n            </label>\n            <label style=\"display:block;margin-top:10px\">\n              <span\n                style={`display:block;font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;color:${MUTED};margin-bottom:4px`}\n              >\n                {tFor(lang, \"publicInvoiceClaim.nameLabel\")}\n              </span>\n              <input\n                type=\"text\"\n                value={claimedBy}\n                data-cy=\"claim-name\"\n                placeholder={tFor(lang, \"publicInvoiceClaim.namePlaceholder\")}\n                onInput={(e) =>\n                  setClaimedBy((e.currentTarget as HTMLInputElement).value)}\n                style={`width:100%;box-sizing:border-box;padding:10px 12px;border:1px solid ${LINE};border-radius:10px;font-size:14px;color:${INK}`}\n              />\n            </label>\n            {error\n              ? (\n                <div style={`margin-top:10px;color:#a83b3b;font-size:13px`}>\n                  {error}\n                </div>\n              )\n              : null}\n            <button\n              type=\"button\"\n              data-cy=\"claim-submit\"\n              onClick={submit}\n              disabled={submitting}\n              style={`margin-top:14px;width:100%;appearance:none;cursor:${\n                submitting ? \"wait\" : \"pointer\"\n              };background:${GREEN};color:#fff;border:0;border-radius:12px;padding:14px 18px;font-size:15px;font-weight:800;letter-spacing:.01em;box-shadow:0 8px 18px -6px rgba(81,152,67,0.45)`}\n            >\n              {submitting\n                ? tFor(lang, \"publicInvoiceClaim.sending\")\n                : tFor(lang, \"publicInvoiceClaim.iSentIt\")}\n            </button>\n          </div>\n        )\n        : null}\n    </section>\n  );\n}\n\nfunction methodLabel(method: string, lang: Lang = \"en\"): string {\n  switch (method) {\n    case \"check\":\n      return tFor(lang, \"paymentMethod.check\");\n    case \"venmo\":\n      return tFor(lang, \"paymentMethod.venmo\");\n    case \"zelle\":\n      return tFor(lang, \"paymentMethod.zelle\");\n    case \"cashapp\":\n      return tFor(lang, \"paymentMethod.cashApp\");\n    case \"paypal\":\n      return tFor(lang, \"paymentMethod.paypal\");\n    case \"cash\":\n      return tFor(lang, \"paymentMethod.cash\");\n    case \"ach\":\n      return tFor(lang, \"paymentMethod.ach\");\n    case \"card\":\n      return tFor(lang, \"paymentMethod.card\");\n    case \"other\":\n      return tFor(lang, \"paymentMethod.other\");\n    default:\n      return method;\n  }\n}\n\nfunction methodInstructions(m: Method, lang: Lang = \"en\"): string {\n  const sendTo = (svc: string) =>\n    m.handle\n      ? tFor(lang, \"publicInvoiceClaim.instructions.sendToHandle\", {\n        handle: m.handle,\n        svc,\n      })\n      : tFor(lang, \"publicInvoiceClaim.instructions.sendVia\", { svc });\n  switch (m.method) {\n    case \"check\":\n      return m.handle\n        ? tFor(lang, \"publicInvoiceClaim.instructions.checkHandle\", {\n          handle: m.handle,\n        })\n        : tFor(lang, \"publicInvoiceClaim.instructions.check\");\n    case \"venmo\":\n      return sendTo(\"Venmo\");\n    case \"zelle\":\n      return sendTo(\"Zelle\");\n    case \"cashapp\":\n      return sendTo(\"Cash App\");\n    case \"paypal\":\n      return sendTo(\"PayPal\");\n    case \"cash\":\n      return tFor(lang, \"publicInvoiceClaim.instructions.cash\");\n    case \"ach\":\n      return m.handle\n        ? m.handle\n        : tFor(lang, \"publicInvoiceClaim.instructions.ach\");\n    case \"card\":\n      return m.handle\n        ? m.handle\n        : tFor(lang, \"publicInvoiceClaim.instructions.card\");\n    case \"other\":\n      return m.handle\n        ? m.handle\n        : tFor(lang, \"publicInvoiceClaim.instructions.other\");\n    default:\n      return tFor(lang, \"publicInvoiceClaim.instructions.default\");\n  }\n}\n\nfunction referencePlaceholder(method: string, lang: Lang = \"en\"): string {\n  switch (method) {\n    case \"check\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.check\");\n    case \"venmo\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.venmo\");\n    case \"zelle\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.zelle\");\n    case \"cashapp\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.cashapp\");\n    case \"paypal\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.paypal\");\n    case \"cash\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.cash\");\n    case \"ach\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.ach\");\n    case \"card\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.card\");\n    case \"other\":\n      return tFor(lang, \"publicInvoiceClaim.refPlaceholder.other\");\n    default:\n      return \"\";\n  }\n}\n", "sourceName": "PublicInvoiceClaim.tsx", "checklist": "# Capture checklist — PublicInvoiceClaim\n\n- **Route:** `/i/<invoiceId>` (PUBLIC, auth-free, needs a real **unpaid,\n  unclaimed** invoice — paid/claimed invoices render ReceivedNote/ClaimedNote\n  instead and the island doesn't mount).\n- **Surface:** inline-styled **public palette**.\n- **Viewports:** 390 (mobile-first — primary), 640. Single column.\n- **Crop targets:** the payment-method chip row (`acceptedMethods`), the\n  reference/handle field, the claim button, the post-claim \"thanks\" swap.\n- **States to drive:**\n  - `methods` — chips for the contractor's enabled manual methods (Zelle /\n    CashApp / check / cash / venmo / paypal / ach / other).\n  - `selected` — a method chosen + optional reference entered.\n  - `claiming` — submitting (`POST /api/invoices/:id/claim-payment`).\n  - `claimed` — in-memory thanks state (no reload — good pattern).\n  - `es` — Spanish.\n- **MANUAL-ONLY** — no card processing; this records a claim against\n  `Invoice.paymentIntent` for the contractor to confirm.\n- **Theme:** light only. **No fabricated screenshots** — needs a live backend + valid token.\n"}]}, {"name": "public-quote", "pageMd": "# Page — Public Quote (`/q/:id`)\n\nCustomer-facing, **auth-free** SSR quote document. A homeowner opens a link from\nan SMS/email (`https://…/q/<quoteId>`), reads the estimate, and either **accepts**\n(type-name e-sign), **declines** (with reason), or **asks a question** — all\nwithout logging in. This is the *public surface*: a printed-document look with a\n**separate inline-styled palette** (warm paper `#f7f6f1`, headings `#144852`,\nbody `#1c2c30`, system font stack, 18px card radius). It is **not** Sabor-tokened\n— see `design-tokens.md` § Public surface palette.\n\nRoute source: `routes/q/[id].tsx` (copied to `js/[id].tsx`, 345 lines).\n\n## 1. Purpose & data flow\n- **SSR fetch:** `ssrBackendGet<QuotePublic>(`/quotes/${id}/public`)` inside the\n  `define.page` async handler. `ssrBackendGet` returns\n  `{ ok, status, data?, errorText? }` (`lib/backend-fetch.ts`). On `r.ok` →\n  `quote = r.data`; otherwise `quote` stays `undefined`.\n- **No auth.** Ownership is implicit in the route + opaque id (the link itself is\n  the bearer token). There is no session/cookie gate, no `ssrBackendGetAuthed`.\n- **Language resolution (multi-lang):** `lang: Lang = quote?.contractor?.commsLanguage === \"es\" ? \"es\" : \"en\"` (roadmap p.13 — customer-facing copy follows the\n  contractor's *outgoing-comms* language, default `en`). Resolved ONCE at SSR and\n  threaded as a prop to `PublicShell`, `QuoteCard`, and the islands. The page uses\n  `tFor(lang, key, vars)` (explicit-lang lookup) — never the reactive `t()`,\n  because there is no on-page language toggle.\n- **Per-language content fields:** `QuotePublic` carries `jobNameByLang`,\n  `summaryByLang`, `descriptionByLang` (keyed by lang code). The page reads\n  `…ByLang?.[lang] ?? <flat fallback>` so the document renders in the picked\n  language when those maps are populated (see `project_job_details_multilang`\n  memory). Title precedence:\n  `jobNameByLang[lang] ?? jobName ?? summaryByLang[lang] ?? summary ?? tFor(lang,\"publicQuote.quote\")`.\n- **Money is CENTS.** `estimatedTotal` and each `lineItems[].price` are integer\n  cents; `fmtMoneyExact` divides by 100 (`1099000` → `\"$10,990.00\"`). Total =\n  `estimatedTotal ?? Σ price*qty`.\n\n### Data-shape hazards\n- **Public composite-read join fan-out (data-model #7):** `/quotes/:id/public`\n  joins the quote → **contractor profile** (`name/businessName/phone/email/\n  addressLine/commsLanguage/hasLogo`) + **customer** (`name`) into one\n  `QuotePublic`. These links are customer-facing, uncached, and hit from\n  SMS/email (bursty). Rebuild target: serve a **denormalized public projection /\n  point-read**, not a live multi-table join.\n- **Honest-empty / expired-link:** when `!r.ok` the page builds\n  `err = tFor(lang,\"publicQuote.linkExpired\")` and renders `ErrorCard`. Also\n  guards `err || !quote`. Note the lang on an error is derived from a\n  `quote?.contractor` that is `undefined` → always falls back to `en` on a failed\n  fetch (a *Spanish* expired link will show in English — minor).\n- `quote.id.slice(0,8)` is rendered as the human reference (`#a1b2c3d4`).\n\n## 2. Page composition (inline render functions — DOCUMENT THESE)\nThe route defines three module-local render functions (NOT islands, NOT shared\ncomponents). They are the page's `page-composition` tier and must be rebuilt as\npart of this page:\n\n| fn | role | tier |\n|---|---|---|\n| `PublicShell({children,brand,address,logoUrl,lang})` | warm-paper outer frame: `min-height:100dvh;background:#f7f6f1`, centered `max-width:640px` column, optional logo `<img>` (≤48px tall, from `/api/public-logo/quote/:id` when `contractor.hasLogo`), green eyebrow headline (businessName→name→`publicQuote.contractorFallback`), optional address line, the `{children}` slot, and a `powered by Paperwork Monster` footer. | page-composition |\n| `QuoteCard({quote})` | the white `border-radius:18px` document card: header (eyebrow \"Quote\" + hero `<h1>` + optional sub-summary + `#id8` + accepted/declined status pill), optional greeting, job-details paragraph/bullets, **line-item table (only when `lineItems.length > 1`)**, estimated-total band, the mounted `PublicQuoteActions` island (only when not accepted/declined), and a contractor contact footer (tel/mailto). | page-composition |\n| `ErrorCard({message,lang})` | white card, `publicQuote.errorHeading` + message. Shown on expired/unavailable. | page-composition |\n| `jobDetailsBlurb(summary)` | helper: lower-cases + strips a leading `\"Quote: \"` so the derived job-details sentence reads naturally. | (pure fn) |\n\n`<Head>` sets a per-lang `<title>` and loads `/landing.css` (resets only).\n\n## 3. Sections (top → bottom, inside PublicShell)\n1. **Brand header** — optional logo, green uppercase eyebrow (business/contractor\n   name), optional address, 18px gap.\n2. **QuoteCard header** — \"QUOTE\" eyebrow, hero title (`Helvetica Neue` 24/900),\n   optional secondary summary line (only when a distinct `jobName` ≠ `summary`),\n   `#<id8>`, and a status pill: **Accepted** (green, `status===\"accepted\"`) /\n   **Declined** (`#a83b3b`, `status===\"lost\"`).\n3. **Greeting** — `publicQuote.greeting {name}` (customer first name) when a\n   linked customer name exists.\n4. **Job details** — three branches off `detailLines(qDesc)`:\n   - `>1` lines → green-bulleted `<ul>`;\n   - `1` line → `<p white-space:pre-wrap>`;\n   - `0` lines → derived sentence `publicQuote.jobDetails` interpolating\n     `jobDetailsBlurb(summary)` + `publicQuote.linesOfWork.{one|other}` (only when\n     a `summary` exists; else nothing).\n5. **Line items table** — **rendered only when `lineItems.length > 1`** (a single\n   line just repeats the total; the bullets above already describe scope). The\n   `Qty` column is shown only when some line has `quantity > 1` (`showQty`); each\n   row total = `price*qty` via `fmtMoneyExact`.\n6. **Estimated-total band** — green-tinted gradient pill (`border-radius:14px`),\n   \"Estimated total\" eyebrow + big `Helvetica Neue` 28/900 amount.\n7. **Actions island** — `<PublicQuoteActions>` mounts here **unless** the quote is\n   already `accepted`/`lost` (settled quotes hide all action UI).\n8. **Contact footer** — dashed top border; tel + mailto links when the contractor\n   has a phone/email (`fmtPhone`, `telHref`).\n9. **Powered-by footer** — subtle `#b9c1bf` `powered by {brand}`.\n\n## 4. States\n| state | trigger | rendering |\n|---|---|---|\n| valid-pending | `r.ok`, `status` ∉ {accepted,lost} | full card + actions island |\n| accepted | `status===\"accepted\"` | green \"Accepted\" pill; **no** actions island |\n| declined | `status===\"lost\"` | `#a83b3b` \"Declined\" pill; **no** actions island |\n| expired-link | `!r.ok` | `ErrorCard` with `publicQuote.linkExpired` |\n| unavailable | `r.ok` but `!quote` (defensive) | `ErrorCard` with `publicQuote.unavailable` |\n| es | `contractor.commsLanguage===\"es\"` | every string via `tFor(\"es\",…)` |\n\n## 5. Build order\n1. `PublicShell` frame (paper bg, 640px column, logo/eyebrow/address/footer).\n2. `ErrorCard` (cheap; unblocks the failure path).\n3. `QuoteCard` header + status pill.\n4. Job-details branch logic (`detailLines` + derived sentence).\n5. Line-item table (gated `>1`, `showQty` column).\n6. Estimated-total band.\n7. Mount `PublicQuoteActions` island (see its spec) — gated on not-settled.\n8. Contact footer + powered-by.\n9. Wire `<Head>` title precedence + `/landing.css`.\n\n## 6. Local components\n- `components/public-quote-actions/` ← `islands/PublicQuoteActions.tsx` (island;\n  decline/ask + delegates accept to PublicAcceptQuote).\n- `components/public-accept-quote/` ← `islands/PublicAcceptQuote.tsx` (island;\n  type-name e-sign accept flow).\n\n## 7. Responsive & a11y\n- **Single column**, `max-width:640px` container, `padding:32px 16px`. No media\n  queries of its own — the 640px cap *is* the responsive story; verify at ~390px\n  (mobile) and 640px. Uses `100dvh` + `--kb-inset` padding so the soft keyboard\n  doesn't cover the inputs.\n- A11y gaps: the status pill is a decorative `<span>` (no role); the document has\n  one `<h1>` (good). Inputs/buttons live in the islands (see their specs).\n\n## 8. CSS\nAlmost entirely inline styles (captured in `js/[id].tsx` and the component specs).\n`css/public-quote.css` documents only the externally-loaded `/landing.css` reset\nexpectations + the `--kb-inset` soft-keyboard variable contract.\n", "components": [{"name": "public-accept-quote", "component": "PublicAcceptQuote", "spec": "# PublicAcceptQuote\n\nThe \"Accept this quote\" island — a type-your-name e-sign that POSTs an acceptance\nfor the customer-facing quote at `/q/:id`. Renders below the SSR'd quote summary.\nOn success it swaps to a green confirmation card.\n\nSource: `islands/PublicAcceptQuote.tsx` (copied to `js/PublicAcceptQuote.tsx`,\n152 lines). Mounted by `PublicQuoteActions`, NOT directly by the page.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PublicAcceptQuote.tsx`).\n- **Interaction tier:** `island` — local state + a single mutating `fetch`.\n- **Client state owned:**\n  - `name: string` — typed full name (used as both `name` and `signature`).\n  - `submitting: boolean`.\n  - `status: \"idle\" | \"ok\" | \"error\"` (seeded to `\"ok\"` when `initialAccepted`).\n  - `err?: string` (friendly, localized).\n- **Server mutation (action endpoint):** `POST /api/quotes/:id/accept` JSON\n  `{ name, signature }` when a name was typed, else `{}`. Same-origin via Fresh's\n  `/api` proxy — the source comment notes this avoids the CORS an absolute\n  `localhost:3000` URL would trip in the browser. Body is JSON because the\n  backend `@Body()` parser needs JSON (a plain form POST parses to an empty\n  object).\n- **Flash / feedback:** inline. Submit label → `publicAcceptQuote.submitting`\n  (\"Accepting…\"); the button is disabled until a name is typed. On success →\n  green \"✓ Quote accepted\" card with a contractor-named follow-up sentence. No\n  toast, no redirect.\n- **Logical-failure handling:** like the actions island — a 200 with\n  `{ok:false, reason:\"already_accepted\"|\"already_declined\"}` is re-thrown and\n  mapped by `friendlyError()` to a localized sentence; raw payload never shown.\n- **`initialAccepted` prop:** when the parent (`PublicQuoteActions`) remounts this\n  island for an already-accepted quote, it passes `initialAccepted` so `status`\n  starts `\"ok\"` and it renders the success card immediately.\n- **Liveness:** request-response only. No polling/socket.\n- **Data source:** none — `quoteId`, `contractorFirstName`, `lang` are props.\n\n### Anti-patterns\n- **No `location.reload()`** — good; resolution is in-state via `status`/\n  `onAccepted`. Flag any rebuild that tries to reload to \"show the accepted state.\"\n\n### Reactivity / i18n\n- `lang` prop (`\"en\"|\"es\"`, default `\"en\"`), all strings via `tFor(lang,…)`. Not\n  reactive to `langSignal` (no on-page toggle).\n\n## 2. Anatomy (inline styles captured)\n### Idle / form\n```\n<form onSubmit=onAccept style=\"margin-top:24px;text-align:left\">\n  <label style=\"font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin-bottom:6px\">\n    \"Type your full name to sign\"\n  <input type=text required autoComplete=name aria-describedby=\"accept-hint\"\n    placeholder=\"Jane Doe\"\n    style=\"width:100%;padding:12px 14px;border:1px solid #e3e8e6;border-radius:10px;font-size:15px;color:#1c2c30;background:#fff\">\n  {!name && !err && <div id=\"accept-hint\" style=\"margin-top:8px;color:#6b7a7e;font-size:12px\">\n    \"Type your name above to enable the Accept button.\"}\n  {err && <div style=\"margin-top:10px;color:#b3261e;font-size:13px\"> \"Couldn't accept — \" + msg}\n  <button type=submit disabled aria-disabled\n    style=\"margin-top:16px;width:100%;background:{bg};color:#fff;border:0;font-weight:800;font-size:15px;padding:14px 28px;border-radius:12px;box-shadow:0 6px 14px rgba(81,152,67,0.35);cursor:{ptr}\">\n    label = submitting ? \"Accepting…\" : \"Accept this quote →\"\n</form>\n```\n- **Button bg:** enabled `#519843`; **disabled `#7a9a73`** (darker muted teal —\n  source comment cites audit P5.4 for AA contrast on white text).\n\n### Success (status===\"ok\")\n```\n<div style=\"margin-top:24px;background:rgba(81,152,67,0.10);border:1px solid rgba(72,158,95,0.30);border-radius:14px;padding:18px 20px;text-align:center\">\n  <div style=\"font-weight:800;color:#519843;font-size:16px\"> \"✓ Quote accepted\"\n  <div style=\"margin-top:6px;color:#6b7a7e;font-size:13px\">\n    who ? \"{Marcus} will be in touch to schedule.\" : \"Your contractor will be in touch to schedule.\"\n</div>\n```\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `quoteId` | string (required) | — | text | no |\n| `contractorFirstName` | string? | `undefined` (→ generic success sub) | text | no |\n| `initialAccepted` | boolean | `false` (→ seeds `status=\"ok\"`) | boolean | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n(`onAccepted` is internal wiring to the parent island — drive via Events, not a prop control.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| idle | empty name → button disabled (`#7a9a73`) + hint shown | `cases/idle/idle.json` |\n| filled | name typed → button enabled (`#519843`), hint gone | `cases/filled/filled.json` |\n| submitting | `submitting=true` → \"Accepting…\", disabled | `cases/submitting/submitting.json` |\n| success | `status=\"ok\"` (or `initialAccepted`) → green ✓ card | `cases/success/success.json` |\n| error | `status=\"error\"` → inline \"Couldn't accept — …\" | `cases/error/error.json` |\n| es | Spanish strings | `cases/es/es.json` |\n\n## 5. Events\n- `e.source===\"input.name\" && e.type===\"input\"` → `setName(value)` (enables button when non-empty).\n- `e.source===\"form.accept\" && e.type===\"submit\"` → `POST /api/quotes/:id/accept`\n  → on ok `status=\"ok\"` + `onAccepted()`.\n\n## 6. Motion (real CSS only)\n- **None.** No keyframes/transitions in source. The `box-shadow:0 6px 14px\n  rgba(81,152,67,0.35)` on the submit button is static (no hover transition\n  declared). State swaps are instant. No reduced-motion concern.\n\n## 7. Responsive\n- Full-width within the page's 640px cap; `width:100%` input + button stack. No\n  own `@media`.\n\n## 8. A11y\n- `<input required aria-describedby=\"accept-hint\">` with a hint div carrying that\n  id — good wiring. Button has `aria-disabled` mirroring `disabled`.\n- **Gaps:** the inline error div has no `role=\"alert\"`/`aria-live`, so a screen\n  reader won't announce an accept failure; success card isn't focused on resolve.\n  Rebuild fix: add `role=\"alert\"` to the error and move focus to the success card.\n\n## 9. Used on\n`/q/:id`, mounted by `PublicQuoteActions` (rendered while `resolved!==\"declined\"`,\nand again with `initialAccepted` once `resolved===\"accepted\"`).\n", "fixture": "{\n  \"category\": \"public-quote\",\n  \"folder\": \"public-accept-quote\",\n  \"controls\": {\n    \"quoteId\": { \"type\": \"text\", \"value\": \"a1b2c3d4-1111-2222-3333-444455556666\" },\n    \"contractorFirstName\": { \"type\": \"text\", \"value\": \"Marcus\" },\n    \"initialAccepted\": { \"type\": \"boolean\", \"value\": false },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "error.json", "content": "{\n  \"_name\": \"Error — already-accepted (200 {ok:false}) surfaced friendly\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcus\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"name\": \"Jane Doe\",\n    \"submitting\": false,\n    \"status\": \"error\",\n    \"err\": \"This quote has already been accepted.\"\n  },\n  \"_mocks\": {\n    \"POST /api/quotes/:id/accept\": {\n      \"status\": 200,\n      \"body\": { \"ok\": false, \"reason\": \"already_accepted\" }\n    }\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — filled (Aceptar esta cotización)\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcos\",\n  \"lang\": \"es\",\n  \"_signals\": { \"name\": \"Juan Pérez\", \"submitting\": false, \"status\": \"idle\" }\n}\n"}, {"name": "filled.json", "content": "{\n  \"_name\": \"Filled — name typed, button enabled (#519843), hint gone\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcus\",\n  \"lang\": \"en\",\n  \"_signals\": { \"name\": \"Jane Doe\", \"submitting\": false, \"status\": \"idle\" }\n}\n"}, {"name": "idle.json", "content": "{\n  \"_name\": \"Idle — empty name, button disabled (#7a9a73) + hint\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcus\",\n  \"lang\": \"en\",\n  \"_signals\": { \"name\": \"\", \"submitting\": false, \"status\": \"idle\" }\n}\n"}, {"name": "submitting.json", "content": "{\n  \"_name\": \"Submitting — 'Accepting…', disabled mid-POST\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcus\",\n  \"lang\": \"en\",\n  \"_signals\": { \"name\": \"Jane Doe\", \"submitting\": true, \"status\": \"idle\" }\n}\n"}, {"name": "success.json", "content": "{\n  \"_name\": \"Success — ✓ Quote accepted, named follow-up\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcus\",\n  \"initialAccepted\": true,\n  \"lang\": \"en\",\n  \"_signals\": { \"name\": \"Jane Doe\", \"submitting\": false, \"status\": \"ok\" }\n}\n"}], "css": null, "cssName": null, "source": "import { useState } from \"preact/hooks\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  quoteId: string;\n  contractorFirstName?: string;\n  /** Notify parent so it can hide its sibling Decline/Ask buttons. */\n  onAccepted?: () => void;\n  /** Render directly in the success state (used when remounted by parent). */\n  initialAccepted?: boolean;\n  /** Outgoing-comms language (roadmap p.13). Customer-facing. */\n  lang?: \"en\" | \"es\";\n}\n\n/**\n * Customer-facing \"Accept this quote\" island. Renders below the SSR'd\n * quote summary. Submits a fetch with JSON (the backend's @Body() parser\n * needs JSON — a plain form POST gets parsed into an empty object).\n *\n * Name is optional because the backend's AcceptQuoteDto allows it; a\n * type-your-name field gives us a record of who clicked accept without\n * forcing the customer to draw a signature on a phone.\n */\nfunction friendlyError(raw: string, lang: Lang = \"en\"): string {\n  const acc = tFor(lang, \"publicAcceptQuote.error.alreadyAccepted\");\n  const dec = tFor(lang, \"publicAcceptQuote.error.alreadyDeclined\");\n  try {\n    const parsed = JSON.parse(raw) as { reason?: string; message?: string };\n    const reason = parsed?.reason;\n    if (reason === \"already_accepted\") return acc;\n    if (reason === \"already_declined\") return dec;\n    if (parsed?.message && typeof parsed.message === \"string\") {\n      return parsed.message;\n    }\n  } catch { /* not JSON, fall through */ }\n  if (/already_accepted/.test(raw)) return acc;\n  if (/already_declined/.test(raw)) return dec;\n  return tFor(lang, \"publicAcceptQuote.error.generic\");\n}\n\nexport default function PublicAcceptQuote(\n  { quoteId, contractorFirstName, onAccepted, initialAccepted, lang = \"en\" }:\n    Props,\n) {\n  const [name, setName] = useState(\"\");\n  const [submitting, setSubmitting] = useState(false);\n  const [status, setStatus] = useState<\"idle\" | \"ok\" | \"error\">(\n    initialAccepted ? \"ok\" : \"idle\",\n  );\n  const [err, setErr] = useState<string | undefined>();\n\n  async function onAccept(e: Event) {\n    e.preventDefault();\n    setSubmitting(true);\n    setErr(undefined);\n    try {\n      // Same-origin via Fresh's /api proxy — avoids CORS that an absolute\n      // localhost:3000 URL would trip in the browser.\n      const r = await fetch(`/api/quotes/${quoteId}/accept`, {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify(\n          name.trim() ? { name: name.trim(), signature: name.trim() } : {},\n        ),\n      });\n      const text = await r.text().catch(() => \"\");\n      if (!r.ok) throw new Error(text.slice(0, 200) || `${r.status}`);\n      try {\n        const parsed = JSON.parse(text || \"{}\") as { ok?: boolean };\n        if (parsed && parsed.ok === false) throw new Error(text);\n      } catch (parseErr) {\n        if (parseErr instanceof Error && parseErr.message === text) {\n          throw parseErr;\n        }\n      }\n      setStatus(\"ok\");\n      onAccepted?.();\n    } catch (e) {\n      setStatus(\"error\");\n      setErr(friendlyError((e as Error).message, lang));\n    } finally {\n      setSubmitting(false);\n    }\n  }\n\n  if (status === \"ok\") {\n    const who = contractorFirstName?.trim();\n    return (\n      <div style=\"margin-top:24px;background:rgba(81,152,67,0.10);border:1px solid rgba(72,158,95,0.30);border-radius:14px;padding:18px 20px;text-align:center\">\n        <div style=\"font-weight:800;color:#519843;font-size:16px\">\n          {\"✓ \"}\n          {tFor(lang, \"publicAcceptQuote.success.title\")}\n        </div>\n        <div style=\"margin-top:6px;color:#6b7a7e;font-size:13px\">\n          {who\n            ? tFor(lang, \"publicAcceptQuote.success.subNamed\", { name: who })\n            : tFor(lang, \"publicAcceptQuote.success.sub\")}\n        </div>\n      </div>\n    );\n  }\n\n  const disabled = submitting || !name.trim();\n  // Disabled state uses a darker muted teal (per audit P5.4) for AA contrast on white text.\n  const bg = disabled ? \"#7a9a73\" : \"#519843\";\n\n  return (\n    <form onSubmit={onAccept} style=\"margin-top:24px;text-align:left;\">\n      <label style=\"display:block;font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin-bottom:6px;\">\n        {tFor(lang, \"publicAcceptQuote.nameLabel\")}\n      </label>\n      <input\n        type=\"text\"\n        value={name}\n        onInput={(e) => setName((e.target as HTMLInputElement).value)}\n        placeholder={tFor(lang, \"publicAcceptQuote.namePlaceholder\")}\n        autoComplete=\"name\"\n        style=\"width:100%;padding:12px 14px;border:1px solid #e3e8e6;border-radius:10px;font-size:15px;color:#1c2c30;font-family:inherit;background:#fff;\"\n        required\n        aria-describedby=\"accept-hint\"\n      />\n      {!name.trim() && !err && (\n        <div\n          id=\"accept-hint\"\n          style=\"margin-top:8px;color:#6b7a7e;font-size:12px\"\n        >\n          {tFor(lang, \"publicAcceptQuote.hint\")}\n        </div>\n      )}\n      {err\n        ? (\n          <div style=\"margin-top:10px;color:#b3261e;font-size:13px\">\n            {tFor(lang, \"publicAcceptQuote.errorPrefix\")}\n            {err}\n          </div>\n        )\n        : null}\n      <button\n        type=\"submit\"\n        disabled={disabled}\n        aria-disabled={disabled}\n        style={`margin-top:16px;width:100%;background:${bg};color:#fff;border:0;font-weight:800;font-size:15px;padding:14px 28px;border-radius:12px;box-shadow:0 6px 14px rgba(81,152,67,0.35);cursor:${\n          disabled ? \"not-allowed\" : \"pointer\"\n        };`}\n      >\n        {submitting\n          ? tFor(lang, \"publicAcceptQuote.submitting\")\n          : `${tFor(lang, \"publicAcceptQuote.submit\")} →`}\n      </button>\n    </form>\n  );\n}\n", "sourceName": "PublicAcceptQuote.tsx", "checklist": "# Capture checklist — PublicAcceptQuote\n\n- **Route:** `/q/<quoteId>` (PUBLIC, auth-free, but needs a real quote id whose\n  status is not yet accepted/lost). Rendered by `PublicQuoteActions`.\n- **Surface:** the inline-styled **public palette** (design-tokens.md § Public\n  surface), NOT Sabor.\n- **Viewports:** 390 (mobile-first, primary), 640 (max card width). Single column.\n- **Crop targets:** the accept/decline action area below the quote card; the\n  accept confirmation step.\n- **States to drive:**\n  - `prompt` — accept + decline buttons visible.\n  - `confirming` — accept confirmation flow open (name/typed-consent).\n  - `accepted` — success state (the page re-renders the accepted badge).\n  - `es` — Spanish (contractor `commsLanguage === \"es\"`).\n- **Theme:** light only. **No fabricated screenshots** — needs a live backend + valid token.\n"}, {"name": "public-quote-actions", "component": "PublicQuoteActions", "spec": "# PublicQuoteActions\n\nThe customer-facing actions panel under the SSR'd quote summary on `/q/:id`.\nThree modes — **Accept** (delegated to the `PublicAcceptQuote` island), **Decline**\n(reason chips + note + name), and **Ask a question** (question + contact + name).\nOnce the quote is resolved (accepted OR declined) both secondary buttons vanish so\nthe customer cannot fire a second mutation against a settled quote.\n\nSource: `islands/PublicQuoteActions.tsx` (copied to `js/PublicQuoteActions.tsx`,\n409 lines). Renders three module-local sub-components: `DeclinedCard`,\n`DeclineForm`, `AskForm`.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/PublicQuoteActions.tsx`).\n- **Interaction tier:** `island` — client state + form-POST mutations via\n  `fetch`. NOT a Fresh Partial; NOT a server-rendered form.\n- **Client state owned:**\n  - `mode: \"actions\" | \"decline\" | \"ask\"` — which panel is showing (default `\"actions\"`).\n  - `resolved: \"accepted\" | \"declined\" | null` — terminal latch (default `null`).\n  - sub-form local state (`DeclineForm`/`AskForm`): `reason?`, `note`, `name`,\n    `question`, `contactBack`, `status: \"idle\"|\"submitting\"|\"ok\"|\"error\"`, `err?`.\n- **Server mutations (action endpoints):**\n  - **Decline:** `POST /api/quotes/:id/decline` JSON\n    `{ reason?, note?, name? }` (only present keys). Same-origin Fresh `/api`\n    proxy → backend (avoids CORS). On success → `onDeclined()` latches\n    `resolved=\"declined\"`.\n  - **Ask:** `POST /api/quotes/:id/inquiry` JSON `{ question, contactBack?, name? }`.\n    On success the `AskForm` shows its own \"Question sent\" card (no latch — asking\n    doesn't settle the quote).\n  - **Accept:** delegated — `PublicAcceptQuote` POSTs `/api/quotes/:id/accept`\n    (see that component). `onAccepted()` here latches `resolved=\"accepted\"`.\n- **Flash / feedback:** inline only. Submit buttons swap label →\n  `publicQuoteActions.sending` (\"Sending…\") and go `disabled`+`opacity:.7`+\n  `cursor:not-allowed`. Errors render inline (`publicQuoteActions.sendError`\n  prefix + friendly message). No toast, no redirect.\n- **Logical-failure handling (subtle):** the decline/accept endpoints return\n  **HTTP 200** with `{ok:false, reason:\"already_accepted\"|\"already_declined\"}` on\n  a logical failure. The island parses the body and re-throws so those are treated\n  as errors, then `friendlyError()` maps the reason to a localized sentence —\n  **never echoing the raw JSON to the homeowner** (deliberate leak prevention).\n- **Liveness:** request-response only. No polling, no websocket, no SSE.\n- **Data source:** none of its own — all inputs are **props** from the SSR\n  `QuoteCard` (`quoteId`, `contractorFirstName`, `customerName`, `lang`).\n- **Honest-empty / settled:** when the parent passes a quote that's already\n  accepted/lost it simply does NOT mount this island (gated in `QuoteCard`). The\n  island's own terminal states are the success/declined cards.\n\n### Anti-patterns\n- **No `location.reload()`** anywhere — good. State transitions are in-memory via\n  `useState` + the `resolved` latch. (Contrast: flag if a rebuild reaches for a\n  reload to \"refresh\" after accept/decline — it must not.)\n- **Double-submit guard is the `resolved` latch + per-form `submitting` disable**,\n  NOT a re-fetch. Preserve it.\n\n### Reactivity / i18n\n- `lang` is a **prop** (`\"en\"|\"es\"`, default `\"en\"`), threaded from the SSR\n  page's `commsLanguage` resolution. All strings via `tFor(lang,key,vars)`. There\n  is no on-page language toggle, so this is NOT reactive to `langSignal`.\n- `es` is a local boolean (`lang === \"es\"`) passed into the sub-forms.\n\n## 2. Anatomy (inline styles captured)\n```\n<div>                                              ← root (PublicQuoteActions)\n  {resolved!==\"declined\" && <PublicAcceptQuote …/>} ← accept island (always, until declined)\n  {resolved===\"declined\" && <DeclinedCard/>}\n\n  {resolved===null && mode===\"actions\" &&           ← secondary button row\n    <div style=\"margin-top:14px;display:flex;gap:10px;justify-content:center;flex-wrap:wrap\">\n      <button \"Ask a question\"  style=\"bg:#fff;border:1px solid #d8e0db;color:#144852;font-weight:700;font-size:13px;padding:10px 18px;border-radius:10px\"/>\n      <button \"Decline\"         style=\"bg:#fff;border:1px solid #d8e0db;color:#6b7a7e;…\"/>\n    </div>}\n\n  {mode===\"decline\" && <DeclineForm/>}\n  {mode===\"ask\"     && <AskForm/>}\n</div>\n```\nWhen `resolved===\"accepted\"` the whole component short-circuits to a single\n`<PublicAcceptQuote … initialAccepted/>` (renders its own success card).\n\n### DeclineForm (inline)\n```\n<form style=\"margin-top:18px;background:#fff;border:1px solid #e3e8e6;border-radius:14px;padding:18px 20px;text-align:left\">\n  <div header row>  \"Decline this quote\"  + <button × close (aria-label)>\n  <div \"Quick reason (optional):\">\n  <div chips row flex-wrap gap:8px>\n    reasonChips: Price · Timing · Going elsewhere · Other\n      active chip: border #519843; bg rgba(81,152,67,.10); color #144852; weight 800\n      idle chip:   border #d8e0db; bg #fff; color #6b7a7e; weight 600\n      (radius:999px pill; toggling re-clicks clears it)\n  <label \"Anything to share? (optional)\">  <textarea rows=3>\n  <label \"Your name (optional)\">           <input autoComplete=name>\n  {err && <div color:#b3261e>}             ← \"Couldn't send — \" + friendly msg\n  <button submit  style=\"bg:#a83b3b;color:#fff;weight:800;padding:12px 20px;radius:12px;width:100%\"\n    label = submitting ? \"Sending…\" : \"Send decline\">\n</form>\n```\n### AskForm (inline) — same card chrome; submit button is teal `#144852`\n```\n<form …same card…>\n  header \"Ask a question\" + close ×\n  <label \"Your question\"> <textarea rows=3 required>\n  <label \"How can they reach you? (optional)\"> <input \"Phone or email\">\n  <label \"Your name (optional)\"> <input autoComplete=name>\n  {err && inline}\n  <button submit  bg:#144852  disabled until question non-empty\n    label = submitting ? \"Sending…\" : \"Send question\">\n</form>\n```\nOn success → replaces form with teal-tinted card: ✓ \"Question sent\" +\n\"Your contractor will follow up directly.\"\n\n### DeclinedCard (inline) — shown after a successful decline\n```\n<div style=\"margin-top:18px;background:#fdf2f2;border:1px solid #f3d4d4;border-radius:14px;padding:18px 20px;text-align:center\">\n  <div weight:800 color:#a83b3b font:16px>  \"Got it — thanks for letting them know\"\n  <div color:#6b7a7e font:13px>             \"Your contractor has been notified.\"\n</div>\n```\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `quoteId` | string (required) | — | text | no |\n| `contractorFirstName` | string? | `undefined` | text | no |\n| `customerName` | string? | `undefined` (pre-fills decline/ask name) | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no |\n\n(All callback-style behaviors — `onAccepted`/`onDeclined`/`onCancel` — are\n**internal** wiring between this island and its children, not public props. Drive\nthem via Events.)\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| actions | default — accept form + Ask/Decline button row | `cases/actions/actions.json` |\n| decline-form | `mode=\"decline\"` — reason chips + note + name | `cases/decline-form/decline-form.json` |\n| declined-card | `resolved=\"declined\"` — success card, no buttons | `cases/declined-card/declined-card.json` |\n| ask-form | `mode=\"ask\"` — question + contact + name | `cases/ask-form/ask-form.json` |\n| ask-sent | `AskForm status=\"ok\"` — ✓ Question sent card | `cases/ask-sent/ask-sent.json` |\n| error | inline send error (e.g. already_accepted) | `cases/error/error.json` |\n| es | Spanish strings throughout | `cases/es/es.json` |\n\n## 5. Events (`capture(page)` predicates)\n- `e.source===\"button.askButton\" && e.type===\"click\"` → `setMode(\"ask\")`.\n- `e.source===\"button.declineButton\" && e.type===\"click\"` → `setMode(\"decline\")`.\n- `e.source===\"button.chip\" && e.type===\"click\"` → toggle `reason` (re-click clears).\n- `e.source===\"form.decline\" && e.type===\"submit\"` → `POST /api/quotes/:id/decline`\n  → on ok `onDeclined()` → `resolved=\"declined\"`.\n- `e.source===\"form.ask\" && e.type===\"submit\"` → `POST /api/quotes/:id/inquiry`\n  → on ok AskForm shows sent card.\n- `e.source===\"button.close\" && e.type===\"click\"` → `onCancel()` → `setMode(\"actions\")`.\n- (accept submit lives in PublicAcceptQuote → `onAccepted()` → `resolved=\"accepted\"`.)\n\n## 6. Motion (real CSS only)\n- **None declared.** No `@keyframes`, no `transition` properties in source. State\n  changes (mode swap, disabled, error appear) are instant. The only visual deltas\n  are the inline `opacity` (1→.7) and `cursor` on disabled submit buttons, applied\n  synchronously with no transition. Nothing to film; no reduced-motion concern.\n\n## 7. Responsive\n- Single column inside the page's 640px cap. The button row is\n  `display:flex;flex-wrap:wrap;justify-content:center` so the two secondary\n  buttons wrap on narrow screens. Forms are full-width (`width:100%` inputs).\n  No own `@media`.\n\n## 8. A11y\n- Close buttons carry `aria-label` (`publicQuoteActions.{decline,ask}Form.closeAria`). Good.\n- Ask `<textarea>` is `required`; submit stays `disabled` until non-empty.\n- **Gaps:** reason chips are `<button type=button>` (focusable — good) but expose\n  no `aria-pressed` for their toggled state; the inline error is a plain `<div>`\n  with no `role=\"alert\"` / `aria-live` (a screen reader won't announce a send\n  failure). Rebuild fix: add `aria-pressed={active}` to chips and\n  `role=\"alert\"` to the error div. No focus is moved to the success card on\n  resolve (consider focusing it).\n\n## 9. Used on\n`/q/:id` only — mounted by the inline `QuoteCard` render function in\n`routes/q/[id].tsx`, and only when the quote is not already accepted/lost.\n", "fixture": "{\n  \"category\": \"public-quote\",\n  \"folder\": \"public-quote-actions\",\n  \"controls\": {\n    \"quoteId\": { \"type\": \"text\", \"value\": \"a1b2c3d4-1111-2222-3333-444455556666\" },\n    \"contractorFirstName\": { \"type\": \"text\", \"value\": \"Marcus\" },\n    \"customerName\": { \"type\": \"text\", \"value\": \"Jane Doe\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "actions.json", "content": "{\n  \"_name\": \"Actions — accept form + Ask/Decline row\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"contractorFirstName\": \"Marcus\",\n  \"customerName\": \"Jane Doe\",\n  \"lang\": \"en\",\n  \"_signals\": { \"mode\": \"actions\", \"resolved\": null }\n}\n"}, {"name": "ask-form.json", "content": "{\n  \"_name\": \"Ask form — question + contact + name\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"customerName\": \"Jane Doe\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"mode\": \"ask\",\n    \"resolved\": null,\n    \"question\": \"What's the timeline if I sign by Friday?\",\n    \"contactBack\": \"512-555-0000\",\n    \"name\": \"Jane Doe\",\n    \"status\": \"idle\"\n  }\n}\n"}, {"name": "ask-sent.json", "content": "{\n  \"_name\": \"Ask sent — ✓ Question sent confirmation card\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"customerName\": \"Jane Doe\",\n  \"lang\": \"en\",\n  \"_signals\": { \"mode\": \"ask\", \"resolved\": null, \"status\": \"ok\" }\n}\n"}, {"name": "decline-form.json", "content": "{\n  \"_name\": \"Decline form — reason chips + note + name (price selected)\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"customerName\": \"Jane Doe\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"mode\": \"decline\",\n    \"resolved\": null,\n    \"reason\": \"price\",\n    \"note\": \"Looking at a few options, the budget came in lower than expected.\",\n    \"name\": \"Jane Doe\",\n    \"status\": \"idle\"\n  }\n}\n"}, {"name": "declined-card.json", "content": "{\n  \"_name\": \"Declined card — terminal, secondary buttons hidden\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"customerName\": \"Jane Doe\",\n  \"lang\": \"en\",\n  \"_signals\": { \"mode\": \"decline\", \"resolved\": \"declined\" }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — already-accepted (200 {ok:false}) surfaced friendly\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"customerName\": \"Jane Doe\",\n  \"lang\": \"en\",\n  \"_signals\": {\n    \"mode\": \"decline\",\n    \"resolved\": null,\n    \"reason\": \"timing\",\n    \"status\": \"error\",\n    \"err\": \"This quote has already been accepted.\"\n  },\n  \"_mocks\": {\n    \"POST /api/quotes/:id/decline\": {\n      \"status\": 200,\n      \"body\": { \"ok\": false, \"reason\": \"already_accepted\" }\n    }\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — decline form (Rechazar esta cotización)\",\n  \"quoteId\": \"a1b2c3d4-1111-2222-3333-444455556666\",\n  \"customerName\": \"Juan Pérez\",\n  \"lang\": \"es\",\n  \"_signals\": {\n    \"mode\": \"decline\",\n    \"resolved\": null,\n    \"reason\": \"price\",\n    \"name\": \"Juan Pérez\",\n    \"status\": \"idle\"\n  }\n}\n"}], "css": "/* PublicQuoteActions — FULLY INLINE-STYLED. No class-based CSS exists in source.\n * Every visual is an inline style string on the element (captured in the .md\n * anatomy and in js/PublicQuoteActions.tsx). This file is a placeholder that\n * records the inline palette/shape constants so a class-based rebuild is\n * mechanical. There are NO @keyframes / transitions to preserve. */\n\n/* Inline-style constants observed (for reference only — source applies them inline):\n *   --pq-paper:        #fff      card surface\n *   --pq-line:         #e3e8e6   hairline / input border\n *   --pq-line-soft:    #d8e0db   secondary button + idle chip border\n *   --pq-ink:          #1c2c30   input text\n *   --pq-teal:         #144852   Ask submit / Ask button label / active chip text\n *   --pq-muted:        #6b7a7e   labels, Decline button text, hints\n *   --pq-green:        #519843   active chip border\n *   --pq-green-tint:   rgba(81,152,67,0.10)   active chip bg\n *   --pq-danger:       #a83b3b   Decline submit bg + DeclinedCard heading\n *   --pq-danger-bg:    #fdf2f2   DeclinedCard bg\n *   --pq-danger-line:  #f3d4d4   DeclinedCard border\n *   --pq-err:          #b3261e   inline send-error text\n *   --pq-radius-card:  14px      form/card radius\n *   --pq-radius-input: 10px      input/secondary-button radius\n *   --pq-radius-btn:   12px      primary submit radius\n *   --pq-radius-pill:  999px     reason chips\n *\n * Light theme only. Do not add a dark variant. */\n", "cssName": "public-quote-actions.css", "source": "import { useState } from \"preact/hooks\";\nimport PublicAcceptQuote from \"./PublicAcceptQuote.tsx\";\nimport { tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  quoteId: string;\n  contractorFirstName?: string;\n  /** Linked customer's full name, when the quote has one. Pre-fills the\n   *  decline / ask forms — the contractor often knows who they sent it to,\n   *  no reason to make the homeowner re-type it. */\n  customerName?: string;\n  /** Outgoing-comms language (roadmap p.13). Customer-facing. */\n  lang?: \"en\" | \"es\";\n}\n\ntype Mode = \"actions\" | \"decline\" | \"ask\";\ntype Status = \"idle\" | \"submitting\" | \"ok\" | \"error\";\ntype Resolved = \"accepted\" | \"declined\" | null;\n\nfunction reasonChips(es: boolean): { id: string; label: string }[] {\n  const lang = es ? \"es\" : \"en\";\n  return [\n    { id: \"price\", label: tFor(lang, \"publicQuoteActions.reason.price\") },\n    { id: \"timing\", label: tFor(lang, \"publicQuoteActions.reason.timing\") },\n    {\n      id: \"going_elsewhere\",\n      label: tFor(lang, \"publicQuoteActions.reason.goingElsewhere\"),\n    },\n    { id: \"other\", label: tFor(lang, \"publicQuoteActions.reason.other\") },\n  ];\n}\n\nfunction friendlyError(raw: string, es = false): string {\n  // Backend returns JSON like {\"ok\":false,\"reason\":\"already_accepted\"}\n  // when the customer attempts a second mutation on a settled quote.\n  // Surfacing the raw payload to a homeowner is the worst kind of leak.\n  const lang = es ? \"es\" : \"en\";\n  const acc = tFor(lang, \"publicQuoteActions.error.alreadyAccepted\");\n  const dec = tFor(lang, \"publicQuoteActions.error.alreadyDeclined\");\n  try {\n    const parsed = JSON.parse(raw) as { reason?: string; message?: string };\n    const reason = parsed?.reason;\n    if (reason === \"already_accepted\") return acc;\n    if (reason === \"already_declined\") return dec;\n    if (parsed?.message && typeof parsed.message === \"string\") {\n      return parsed.message;\n    }\n  } catch { /* not JSON, fall through */ }\n  if (/already_accepted/.test(raw)) return acc;\n  if (/already_declined/.test(raw)) return dec;\n  return tFor(lang, \"publicQuoteActions.error.generic\");\n}\n\n/**\n * Public-quote actions panel — Accept (delegated to PublicAcceptQuote),\n * Decline, and Ask-a-question. Once the customer resolves the quote\n * (accept or decline) we hide *both* secondary buttons immediately so\n * the panel can't fire a second mutation against a settled quote.\n */\nexport default function PublicQuoteActions(\n  { quoteId, contractorFirstName, customerName, lang = \"en\" }: Props,\n) {\n  const [mode, setMode] = useState<Mode>(\"actions\");\n  const [resolved, setResolved] = useState<Resolved>(null);\n  const es = lang === \"es\";\n\n  // Once accepted, the Accept island renders its own success card; we\n  // only need to hide our secondary buttons. Once declined, we render\n  // the decline success card from this component instead.\n  if (resolved === \"accepted\") {\n    return (\n      <PublicAcceptQuote\n        quoteId={quoteId}\n        contractorFirstName={contractorFirstName}\n        lang={lang}\n        initialAccepted\n      />\n    );\n  }\n\n  return (\n    <div>\n      {resolved !== \"declined\" && (\n        <PublicAcceptQuote\n          quoteId={quoteId}\n          contractorFirstName={contractorFirstName}\n          lang={lang}\n          onAccepted={() => setResolved(\"accepted\")}\n        />\n      )}\n\n      {resolved === \"declined\" && <DeclinedCard es={es} />}\n\n      {resolved === null && mode === \"actions\" && (\n        <div style=\"margin-top:14px;display:flex;gap:10px;justify-content:center;flex-wrap:wrap\">\n          <button\n            type=\"button\"\n            onClick={() => setMode(\"ask\")}\n            style=\"background:#fff;border:1px solid #d8e0db;color:#144852;font-weight:700;font-size:13px;padding:10px 18px;border-radius:10px;cursor:pointer\"\n          >\n            {tFor(lang, \"publicQuoteActions.askButton\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={() => setMode(\"decline\")}\n            style=\"background:#fff;border:1px solid #d8e0db;color:#6b7a7e;font-weight:700;font-size:13px;padding:10px 18px;border-radius:10px;cursor:pointer\"\n          >\n            {tFor(lang, \"publicQuoteActions.declineButton\")}\n          </button>\n        </div>\n      )}\n\n      {resolved === null && mode === \"decline\" && (\n        <DeclineForm\n          quoteId={quoteId}\n          customerName={customerName}\n          es={es}\n          onCancel={() => setMode(\"actions\")}\n          onDeclined={() => setResolved(\"declined\")}\n        />\n      )}\n      {resolved === null && mode === \"ask\" && (\n        <AskForm\n          quoteId={quoteId}\n          customerName={customerName}\n          es={es}\n          onCancel={() => setMode(\"actions\")}\n        />\n      )}\n    </div>\n  );\n}\n\nfunction DeclinedCard({ es }: { es: boolean }) {\n  const lang = es ? \"es\" : \"en\";\n  return (\n    <div style=\"margin-top:18px;background:#fdf2f2;border:1px solid #f3d4d4;border-radius:14px;padding:18px 20px;text-align:center\">\n      <div style=\"font-weight:800;color:#a83b3b;font-size:16px\">\n        {tFor(lang, \"publicQuoteActions.declinedCard.title\")}\n      </div>\n      <div style=\"margin-top:6px;color:#6b7a7e;font-size:13px\">\n        {tFor(lang, \"publicQuoteActions.declinedCard.body\")}\n      </div>\n    </div>\n  );\n}\n\nfunction DeclineForm(\n  { quoteId, customerName, es, onCancel, onDeclined }: {\n    quoteId: string;\n    customerName?: string;\n    es: boolean;\n    onCancel: () => void;\n    onDeclined: () => void;\n  },\n) {\n  const lang = es ? \"es\" : \"en\";\n  const [reason, setReason] = useState<string | undefined>(undefined);\n  const [note, setNote] = useState(\"\");\n  const [name, setName] = useState(customerName?.trim() ?? \"\");\n  const [status, setStatus] = useState<Status>(\"idle\");\n  const [err, setErr] = useState<string | undefined>();\n\n  async function submit(e: Event) {\n    e.preventDefault();\n    setStatus(\"submitting\");\n    setErr(undefined);\n    try {\n      const r = await fetch(`/api/quotes/${quoteId}/decline`, {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({\n          ...(reason ? { reason } : {}),\n          ...(note.trim() ? { note: note.trim() } : {}),\n          ...(name.trim() ? { name: name.trim() } : {}),\n        }),\n      });\n      const text = await r.text().catch(() => \"\");\n      if (!r.ok) {\n        throw new Error(text.slice(0, 200) || `${r.status}`);\n      }\n      // The endpoint returns 200 with {ok:false,reason:...} on logical\n      // failures (already_accepted) — treat those as errors too.\n      try {\n        const parsed = JSON.parse(text || \"{}\") as { ok?: boolean };\n        if (parsed && parsed.ok === false) throw new Error(text);\n      } catch (parseErr) {\n        if (parseErr instanceof Error && parseErr.message === text) {\n          throw parseErr;\n        }\n      }\n      setStatus(\"ok\");\n      onDeclined();\n    } catch (e) {\n      setStatus(\"error\");\n      setErr(friendlyError((e as Error).message, es));\n    }\n  }\n\n  return (\n    <form\n      onSubmit={submit}\n      style=\"margin-top:18px;background:#fff;border:1px solid #e3e8e6;border-radius:14px;padding:18px 20px;text-align:left\"\n    >\n      <div style=\"display:flex;justify-content:space-between;align-items:center;margin-bottom:10px\">\n        <div style=\"font-weight:800;color:#144852;font-size:15px\">\n          {tFor(lang, \"publicQuoteActions.declineForm.title\")}\n        </div>\n        <button\n          type=\"button\"\n          onClick={onCancel}\n          aria-label={tFor(lang, \"publicQuoteActions.declineForm.closeAria\")}\n          style=\"background:transparent;border:0;color:#6b7a7e;font-size:18px;cursor:pointer;padding:0 4px\"\n        >\n          ×\n        </button>\n      </div>\n      <div style=\"font-size:12px;color:#6b7a7e;margin-bottom:8px\">\n        {tFor(lang, \"publicQuoteActions.declineForm.reasonPrompt\")}\n      </div>\n      <div style=\"display:flex;flex-wrap:wrap;gap:8px;margin-bottom:12px\">\n        {reasonChips(es).map((r) => {\n          const active = reason === r.id;\n          return (\n            <button\n              key={r.id}\n              type=\"button\"\n              onClick={() => setReason(active ? undefined : r.id)}\n              style={`border:1px solid ${\n                active ? \"#519843\" : \"#d8e0db\"\n              };background:${active ? \"rgba(81,152,67,0.10)\" : \"#fff\"};color:${\n                active ? \"#144852\" : \"#6b7a7e\"\n              };font-weight:${\n                active ? 800 : 600\n              };font-size:12px;padding:6px 12px;border-radius:999px;cursor:pointer`}\n            >\n              {r.label}\n            </button>\n          );\n        })}\n      </div>\n      <label style=\"display:block;font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin-bottom:6px\">\n        {tFor(lang, \"publicQuoteActions.declineForm.noteLabel\")}\n      </label>\n      <textarea\n        value={note}\n        onInput={(e) => setNote((e.target as HTMLTextAreaElement).value)}\n        rows={3}\n        placeholder={tFor(lang, \"publicQuoteActions.declineForm.notePlaceholder\")}\n        style=\"width:100%;padding:10px 12px;border:1px solid #e3e8e6;border-radius:10px;font-size:14px;color:#1c2c30;font-family:inherit;background:#fff;resize:vertical\"\n      />\n      <label style=\"display:block;font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin:12px 0 6px\">\n        {tFor(lang, \"publicQuoteActions.nameLabel\")}\n      </label>\n      <input\n        type=\"text\"\n        value={name}\n        onInput={(e) => setName((e.target as HTMLInputElement).value)}\n        placeholder={tFor(lang, \"publicQuoteActions.namePlaceholder\")}\n        autoComplete=\"name\"\n        style=\"width:100%;padding:10px 12px;border:1px solid #e3e8e6;border-radius:10px;font-size:14px;color:#1c2c30;font-family:inherit;background:#fff\"\n      />\n      {err && (\n        <div style=\"margin-top:10px;color:#b3261e;font-size:13px\">\n          {tFor(lang, \"publicQuoteActions.sendError\")}\n          {err}\n        </div>\n      )}\n      <button\n        type=\"submit\"\n        disabled={status === \"submitting\"}\n        style={`margin-top:14px;width:100%;background:#a83b3b;color:#fff;border:0;font-weight:800;font-size:14px;padding:12px 20px;border-radius:12px;cursor:${\n          status === \"submitting\" ? \"not-allowed\" : \"pointer\"\n        };opacity:${status === \"submitting\" ? 0.7 : 1}`}\n      >\n        {status === \"submitting\"\n          ? tFor(lang, \"publicQuoteActions.sending\")\n          : tFor(lang, \"publicQuoteActions.declineForm.submit\")}\n      </button>\n    </form>\n  );\n}\n\nfunction AskForm(\n  { quoteId, customerName, es, onCancel }: {\n    quoteId: string;\n    customerName?: string;\n    es: boolean;\n    onCancel: () => void;\n  },\n) {\n  const lang = es ? \"es\" : \"en\";\n  const [question, setQuestion] = useState(\"\");\n  const [contactBack, setContactBack] = useState(\"\");\n  const [name, setName] = useState(customerName?.trim() ?? \"\");\n  const [status, setStatus] = useState<Status>(\"idle\");\n  const [err, setErr] = useState<string | undefined>();\n\n  async function submit(e: Event) {\n    e.preventDefault();\n    if (!question.trim()) return;\n    setStatus(\"submitting\");\n    setErr(undefined);\n    try {\n      const r = await fetch(`/api/quotes/${quoteId}/inquiry`, {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify({\n          question: question.trim(),\n          ...(contactBack.trim() ? { contactBack: contactBack.trim() } : {}),\n          ...(name.trim() ? { name: name.trim() } : {}),\n        }),\n      });\n      if (!r.ok) {\n        const text = await r.text().catch(() => \"\");\n        throw new Error(text.slice(0, 200) || `${r.status}`);\n      }\n      setStatus(\"ok\");\n    } catch (e) {\n      setStatus(\"error\");\n      setErr(friendlyError((e as Error).message, es));\n    }\n  }\n\n  if (status === \"ok\") {\n    return (\n      <div style=\"margin-top:18px;background:rgba(20,72,82,0.06);border:1px solid rgba(20,72,82,0.18);border-radius:14px;padding:18px 20px;text-align:center\">\n        <div style=\"font-weight:800;color:#144852;font-size:16px\">\n          {tFor(lang, \"publicQuoteActions.askForm.sentTitle\")}\n        </div>\n        <div style=\"margin-top:6px;color:#6b7a7e;font-size:13px\">\n          {tFor(lang, \"publicQuoteActions.askForm.sentBody\")}\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <form\n      onSubmit={submit}\n      style=\"margin-top:18px;background:#fff;border:1px solid #e3e8e6;border-radius:14px;padding:18px 20px;text-align:left\"\n    >\n      <div style=\"display:flex;justify-content:space-between;align-items:center;margin-bottom:10px\">\n        <div style=\"font-weight:800;color:#144852;font-size:15px\">\n          {tFor(lang, \"publicQuoteActions.askButton\")}\n        </div>\n        <button\n          type=\"button\"\n          onClick={onCancel}\n          aria-label={tFor(lang, \"publicQuoteActions.askForm.closeAria\")}\n          style=\"background:transparent;border:0;color:#6b7a7e;font-size:18px;cursor:pointer;padding:0 4px\"\n        >\n          ×\n        </button>\n      </div>\n      <label style=\"display:block;font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin-bottom:6px\">\n        {tFor(lang, \"publicQuoteActions.askForm.questionLabel\")}\n      </label>\n      <textarea\n        value={question}\n        onInput={(e) => setQuestion((e.target as HTMLTextAreaElement).value)}\n        rows={3}\n        placeholder={tFor(lang, \"publicQuoteActions.askForm.questionPlaceholder\")}\n        required\n        style=\"width:100%;padding:10px 12px;border:1px solid #e3e8e6;border-radius:10px;font-size:14px;color:#1c2c30;font-family:inherit;background:#fff;resize:vertical\"\n      />\n      <label style=\"display:block;font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin:12px 0 6px\">\n        {tFor(lang, \"publicQuoteActions.askForm.contactLabel\")}\n      </label>\n      <input\n        type=\"text\"\n        value={contactBack}\n        onInput={(e) => setContactBack((e.target as HTMLInputElement).value)}\n        placeholder={tFor(lang, \"publicQuoteActions.askForm.contactPlaceholder\")}\n        style=\"width:100%;padding:10px 12px;border:1px solid #e3e8e6;border-radius:10px;font-size:14px;color:#1c2c30;font-family:inherit;background:#fff\"\n      />\n      <label style=\"display:block;font-size:11px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:#6b7a7e;margin:12px 0 6px\">\n        {tFor(lang, \"publicQuoteActions.nameLabel\")}\n      </label>\n      <input\n        type=\"text\"\n        value={name}\n        onInput={(e) => setName((e.target as HTMLInputElement).value)}\n        placeholder={tFor(lang, \"publicQuoteActions.namePlaceholder\")}\n        autoComplete=\"name\"\n        style=\"width:100%;padding:10px 12px;border:1px solid #e3e8e6;border-radius:10px;font-size:14px;color:#1c2c30;font-family:inherit;background:#fff\"\n      />\n      {err && (\n        <div style=\"margin-top:10px;color:#b3261e;font-size:13px\">\n          {tFor(lang, \"publicQuoteActions.sendError\")}\n          {err}\n        </div>\n      )}\n      <button\n        type=\"submit\"\n        disabled={status === \"submitting\" || !question.trim()}\n        style={`margin-top:14px;width:100%;background:#144852;color:#fff;border:0;font-weight:800;font-size:14px;padding:12px 20px;border-radius:12px;cursor:${\n          status === \"submitting\" || !question.trim()\n            ? \"not-allowed\"\n            : \"pointer\"\n        };opacity:${status === \"submitting\" || !question.trim() ? 0.7 : 1}`}\n      >\n        {status === \"submitting\"\n          ? tFor(lang, \"publicQuoteActions.sending\")\n          : tFor(lang, \"publicQuoteActions.askForm.submit\")}\n      </button>\n    </form>\n  );\n}\n", "sourceName": "PublicQuoteActions.tsx", "checklist": "# Capture checklist — PublicQuoteActions\n\n**Theme:** light only. **Palette:** public surface (inline hex, NOT Sabor).\n**Auth:** NONE — driven on a real `/q/:id` page; no fabricated screenshots.\n\n## Route / URL\n- `http://localhost:5280/q/<quoteId>` for a quote whose `status` is NOT\n  accepted/lost (otherwise the island isn't mounted). Crop the panel below the\n  estimated-total band.\n- For Spanish, use a quote whose `contractor.commsLanguage===\"es\"`.\n\n## Viewports\n- **~390px** (primary — secondary buttons must wrap correctly).\n- **640px** (column cap).\n\n## States to drive\n1. **actions** — accept name field + the centered \"Ask a question\" / \"Decline\"\n   button row.\n2. **decline-form** — click Decline → reason chips (Price/Timing/Going\n   elsewhere/Other), pick one (active chip = green border + tint + weight 800),\n   note textarea, name input, red `#a83b3b` \"Send decline\" button.\n3. **declined-card** — submit decline (real backend) → pink `#fdf2f2` card\n   \"Got it — thanks for letting them know\"; confirm BOTH secondary buttons are\n   gone and the accept form is replaced.\n4. **ask-form** — click Ask a question → question textarea (required), contact\n   input, name input, teal `#144852` \"Send question\" button (disabled until the\n   question is non-empty — capture the disabled state).\n5. **ask-sent** — submit a question → teal-tinted ✓ \"Question sent\" card.\n6. **error** — drive an already-settled quote (or mock a 200 `{ok:false,\n   reason:\"already_accepted\"}`) → inline red \"Couldn't send — This quote has\n   already been accepted.\" Confirm NO raw JSON leaks.\n7. **es** — Spanish: \"Rechazar esta cotización\", chips \"Precio/Tiempos/Elegí otra\n   opción/Otro\", \"Enviar rechazo\".\n\n## Transient states\n- Submit \"Sending…\" (`status===\"submitting\"`) — button label swap, opacity .7,\n  cursor not-allowed.\n\n## Motion\nNone. No CSS transitions/keyframes — state swaps are instant. Nothing to film.\n"}]}, {"name": "quotes", "pageMd": "# Page — /quotes\n\nThe contractor's quote pipeline: a \"where does every open quote stand right\nnow\" board. Hero headline (Σ open value + stale count), 4 KPI cells, three\ncollapsible pipeline **tracks** (Out for response / Drafting / Decided this\nmonth) of flippable quote cards, and a sticky right sidebar (top quotes by\nvalue, win-rate gauge, AI tip).\n\nSource route: `routes/quotes/index.tsx` → `js/index.tsx`.\n\n## App-shell composition (island order)\n\nThe route is an `define.page` SSR shell. It renders the standard authed\napp-shell and mounts exactly one data island:\n\n```\n<div class=\"app\">\n  <DashSidebar active=\"quotes\" />          ← shared island\n  <main class=\"main\">\n    <DashTopbar greetingName={…} />        ← shared island\n    <div class=\"content\">\n      <QuotesPage />                        ← page island (the whole page body)\n    </div>\n  </main>\n</div>\n```\n\n- **DashSidebar / DashTopbar** are shared (see `shared-components/`). The route\n  passes `active=\"quotes\"` and a first-name `greetingName` (localized \"there\"\n  fallback). `MobileViewport` / `ImpersonationBanner` are part of the global\n  layout (`routes/_app` / `_layout`), not mounted here.\n- `QuotesPage` is the only page-local island; everything below the topbar is\n  its subtree.\n\n## `<Head>`\n\n- **title:** `Quotes · Paperwork Monster` (hard-coded literal in the route —\n  NOT localized via `tFor`, unlike /contracts which uses\n  `contractsPage.docTitle`).\n- **CSS:** two `<link>` tags, in order:\n  1. `/dashboard.css` (key `css-dashboard`) — tokens + app-shell + skeleton +\n     `.app/.main/.content` chrome.\n  2. `/quotes.css` (key `css-quotes`) — all quotes-page classes (copied to\n     `css/quotes.css`).\n\n## SSR data\n\n**None for the body.** The route computes only `greetingName` from\n`ctx.state.user` (name + language). All quote/analytics data is fetched\n**client-side** by the `QuotesPage` island on mount — the server ships an empty\nshell + skeleton. (`user.language` decides the SSR `lang` seed but QuotesPage\nignores the prop and reads `langSignal.value` live.)\n\n## Sections (top → bottom, all inside QuotesPage)\n\n1. **QuotesHero** (`.qph`) — eyebrow, big headline, sub, pink \"+ New quote\" CTA.\n   3 copy variants: empty / all-warm / has-stale.\n2. **QuotesKpis** (`.qkpi`) — 4 cells: Out for response (accent), Drafting,\n   Decided, Win rate (gated <5 decided → \"—\").\n3. **`.qlay`** two-column grid (`1fr 320px`, collapses at 1200px):\n   - **left:** three `QuoteTrack` [shared] groups:\n     - 01 Out for response → `.qcards` of `QuoteCard` (sorted opened>sent>cooling>stale)\n     - 02 Drafting → `.qcards` of `QuoteCard`\n     - 03 Decided this month → `.qdone` of `DecidedRow` (with inline DeleteQuoteButton icon)\n   - **right:** `<aside class=\"qside\">` sticky → QSideBig, QSideRate, QSideTip.\n\n## Build order\n\n1. Tokens + `/dashboard.css` shell (`.app/.main/.content`), shared Skeletons.\n2. `quote-track/` [shared — reference only] — the collapse mechanic.\n3. `quotes-sections/` (page-composition; SSR markup for hero/kpi/decided/sidebar).\n4. `delete-quote-button/` (island; used by DecidedRow + QuoteCard).\n5. `quote-card/` (island; flip card).\n6. `quotes-page/` (island; fetch + group + compose all of the above).\n7. Route shell `index.tsx` (Head + app-shell + `<QuotesPage/>`).\n\n## Local components (this folder)\n\n| folder | source | bucket |\n|---|---|---|\n| `quotes-page/` | `islands/QuotesPage.tsx` | island (data/orchestrator) |\n| `quotes-sections/` | `components/QuotesSections.tsx` | page-composition (SSR) |\n| `quote-card/` | `islands/QuoteCard.tsx` | island (client-only flip) |\n| `delete-quote-button/` | `islands/DeleteQuoteButton.tsx` | island (mutation) |\n\nShared (reference only, do NOT re-spec): `DashSidebar`, `DashTopbar`,\n`QuoteTrack`, `Skeletons`, `dash-icons` (`I`/`ICN`).\n\n## Page-level anti-pattern (see quotes-page/ for detail)\n\n`QuotesPage` is a **whole-page island** that fetches on mount and freezes the\nresult in `useState`. `DeleteQuoteButton` mutates then calls\n`globalThis.location.reload()` — a full-document reload to reflect a delete,\nwhich re-runs SSR + re-fetches everything. Flagged; PRG/Partial fix proposed in\n`quotes-page/quotes-page.md` §1 and `delete-quote-button/`.\n", "components": [{"name": "delete-quote-button", "component": "DeleteQuoteButton", "spec": "# DeleteQuoteButton\n\nA small **confirm → delete → reload** action button. Two visual variants: a\n`btn` (the unclassed \"Delete\" button in QuoteCard's back foot) and an `icon` (the\ncompact `×` in a DecidedRow). On click it fires a native `confirm()`, then\n`DELETE /quotes/:id`, then **reloads the whole document**.\n\n## 1. Classification & behavior\n\n- **Bucket:** `island` (`islands/DeleteQuoteButton.tsx`).\n- **Interaction tier:** `island` — a **client-driven server mutation**. This is\n  the one mutating control in the quotes feature.\n- **Server action + flash:** there is **no `<form>` / PRG / Fresh route handler**.\n  The mutation is a client `fetch` via `quotesClient.delete(id)` →\n  `DELETE /quotes/:id` → `{ ok }`. There is **no flash message**; success\n  feedback is the page reload, failure feedback is a native `alert()`.\n- **Island client-state + refresh:**\n  - `busy: boolean` (`useState`) — set true between confirm and the\n    delete-resolve; disables the button (`disabled={busy}`, cursor `wait`). On\n    error it's reset to false (so the user can retry); on success the page\n    reloads before it matters.\n  - `lang = langSignal.value` — read **during render**, so the label\n    (\"Delete\"/\"Deleting…\"/aria-label) re-localizes live when SettingsPage flips\n    language. The `lang` **prop** is an ignored SSR seed (the interface keeps it\n    but the function body reads the signal).\n- **`location.reload()` FLAG — YES, this is the reload.**\n  `await quotesClient.delete(id); globalThis.location.reload();`. This is the\n  canonical \"whole-page island + reload-after-mutation\" anti-pattern: the quote\n  list lives in `QuotesPage`'s frozen `useState`, so a delete can't surgically\n  splice the removed row — the app nukes the entire document, re-running every\n  fetch (`/quotes`, win-rate, insight) and re-rendering the shell\n  (`DashSidebar`/`DashTopbar` + their `dash-cache` reads). Deleting one decided\n  row triggers a full SPA reload.\n- **Data source:** none of its own; it only **writes** (`DELETE /quotes/:id`).\n  Takes the target `id` as a prop.\n- **Honest-empty:** N/A — it's a single action button; it always renders given an\n  `id`. (After it deletes the last quote, the reloaded page shows the\n  honest-empty QuotesHero.)\n- **Liveness:** none. One-shot mutation, no polling/websocket.\n- **Confirm gate:** `globalThis.confirm(resolvedConfirm)` — a **native browser\n  confirm dialog** (\"Delete this quote? This cannot be undone.\"), not an in-app\n  modal. Cancel → no-op (returns before `setBusy`).\n- **Error path:** `quotesClient.delete` reject → `setBusy(false)` +\n  `globalThis.alert(tFor(lang,\"deleteQuoteButton.error\",{message}))` (native\n  alert, not a toast).\n- **Data-shape hazards:**\n  - **Reload after mutate** (above) — the headline rebuild target.\n  - **Native `confirm`/`alert`** block the thread and are unstyled OS dialogs —\n    no design-system styling, no i18n beyond the message string passed in, and\n    `confirm` is sometimes suppressed in embedded/automation contexts (drive the\n    confirm explicitly when capturing).\n  - **Frozen `id` prop** — fine here (the parent re-renders with new ids on\n    reload), but the lack of an `onDeleted(id)` callback is exactly why the\n    parent must reload instead of splicing.\n\n### Anti-pattern + rebuild fix\n\nThis is the mutation half of the QuotesPage page-island anti-pattern. Two viable\nrebuilds (mirrors the quotes-page spec):\n1. **`<form method=\"POST\">` + PRG / Fresh Partial:** post the delete to a route\n   handler that deletes then `303`-redirects back to `/quotes`, rendered through\n   an `f-partial` so only the list fragment swaps — no JS reload, success/error\n   as a **flash** on the redirect, confirm as a styled in-app dialog\n   (`<dialog>` / island modal) instead of native `confirm`.\n2. **Lift + optimistic island state:** hold the `quotes` array in a signal owned\n   by QuotesPage; pass `onDeleted(id)` down; this button calls it to splice the\n   row (optimistic) and reconciles on the `{ ok }` response. No document reload.\n   (Callbacks aren't serializable → in isolate they surface as **Events**, not\n   props.)\n\n## 2. Anatomy\n\n```\nvariant=\"icon\":\n  <button type=button class=\"qdone__del\" onClick disabled={busy}\n          aria-label={ariaLabel} title={ariaLabel}>×</button>\n\nvariant=\"btn\" (default):\n  <button type=button onClick disabled={busy}>\n    {busy ? \"Deleting…\" : resolvedLabel}     ← UNCLASSED; inherits .qcard__back-foot button\n  </button>\n```\n- **Slots/children:** none.\n- **No dedicated CSS.** The icon variant uses `.qdone__del` (decided-row scoped,\n  in `static/quotes.css` / mirrored in quotes-sections css). The btn variant is\n  an **unclassed `<button>`** that inherits `.qcard__back-foot button` rules when\n  rendered inside QuoteCard's back face. Both are reproduced in this folder's\n  `css/delete-quote-button.css` so the button can be isolated without its host\n  page CSS.\n- **No icon dependency** — the `×` is a literal U+00D7 glyph, not an `<I>`.\n\n## 3. Props\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `id` | string (quote id) | — (required) | text | no |\n| `variant` | `\"btn\"\\|\"icon\"` | `\"btn\"` | select | no |\n| `label` | string (overrides \"Delete\") | `tFor(lang,\"deleteQuoteButton.label\")` | text | no |\n| `confirmText` | string (overrides confirm copy) | `tFor(lang,\"deleteQuoteButton.confirm\")` | text | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | **ignored SSR seed** — live lang from `langSignal.value` |\n\n## 4. States → cases\n\n| state | meaning | case |\n|---|---|---|\n| btn | default variant, idle (\"Delete\") | `cases/btn/btn.json` |\n| btn-busy | mid-delete (\"Deleting…\", disabled, cursor wait) | `cases/btn-busy/btn-busy.json` |\n| icon | compact `×` (decided-row), idle | `cases/icon/icon.json` |\n| icon-busy | `×` disabled (opacity .5, cursor wait) | `cases/icon-busy/icon-busy.json` |\n| es | Spanish label/confirm | `cases/es/es.json` |\n\n> Isolate note: `busy` is internal `useState`. The `*-busy` cases force it via\n> `\"_signals\": { \"busy\": true }`. To exercise the real flow, mock\n> `quotesClient.delete` (`_mocks`) — a never-resolving promise keeps it busy; a\n> reject surfaces the native `alert`. The native `confirm` must be auto-accepted\n> in the harness (it blocks otherwise).\n\n## 5. Events\n\n- `ev.expect(e => e.source === \"button.qdone__del\" && e.type === \"click\")`\n  (icon) / `e.source === \"button\" && e.type === \"click\"` (btn) → `stopPropagation`\n  (so it doesn't flip the parent card), then `confirm()`; on accept →\n  `setBusy(true)` → `quotesClient.delete(id)` → `location.reload()`; on reject of\n  the fetch → `setBusy(false)` + `alert(error)`.\n- `confirm` cancel → no-op (early return, no busy, no fetch).\n\n## 6. Motion (extracted)\n\n- **No keyframes.** Hover transitions only:\n  - icon (`.qdone__del`): `border-color`/`color`/`background` over `--dur-fast`\n    `--ease-out` → pink-tinted on hover.\n  - btn (`.qcard__back-foot button`): `border-color`/`color` + `translateY(-1px)`\n    over `--dur-fast` on hover → pink.\n- **Disabled:** `opacity:.5; cursor:wait` (no transition on the disabled flip —\n  it just snaps).\n- **Jank:** none — trivial color/transform transitions.\n- **Reduced motion:** no local guard; relies on the global tokens clamp (the\n  hover transitions go instant). The native `confirm`/`alert` dialogs are\n  OS-rendered and unaffected.\n\n## 7. Responsive\n\n- **No own `@media`.** Geometry is fixed (icon 24×24px; btn padding 9px×6px). The\n  icon variant sits in the DecidedRow grid, which hides the `.qdone__when` column\n  at **560px** but keeps the `×` (quotes-sections css). Verify within those host\n  widths.\n\n## 8. A11y\n\n- icon variant: has `aria-label` + `title` (`deleteQuoteButton.ariaLabel` =\n  \"Delete quote\") — good, since `×` alone isn't a label.\n- btn variant: text label (\"Delete\") is its accessible name — fine.\n- `disabled={busy}` correctly removes it from tab order mid-delete.\n- **Gap:** destructive action gated only by a **native `confirm`** — no\n  `aria`-described danger affordance, no focus management, no styled\n  confirmation. Rebuild: a styled confirm dialog (`<dialog>`/island modal) with\n  focus trap + `aria-describedby`, and announce success/failure via an\n  `aria-live` flash rather than a native `alert`.\n\n## 9. Used on\n\n`/quotes` only, via two hosts:\n- **QuoteCard back foot** — `<DeleteQuoteButton id={q.id}>` (btn variant)\n  [`islands/QuoteCard.tsx:198`].\n- **DecidedRow** — `<DeleteQuoteButton id={q.id} variant=\"icon\">`\n  [`components/QuotesSections.tsx:186`].\n\nBoth hosts render only on `/quotes`. No other consumer (grep of\n`DeleteQuoteButton` import/JSX across `routes/ islands/ components/`).\n", "fixture": "{\n  \"category\": \"quotes\",\n  \"folder\": \"delete-quote-button\",\n  \"controls\": {\n    \"id\": { \"type\": \"text\", \"value\": \"Q-1099\" },\n    \"variant\": { \"type\": \"select\", \"value\": \"btn\", \"options\": [\"btn\", \"icon\"] },\n    \"label\": { \"type\": \"text\", \"value\": \"\" },\n    \"confirmText\": { \"type\": \"text\", \"value\": \"\" },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  }\n}\n", "cases": [{"name": "btn-busy.json", "content": "{\n  \"_name\": \"Btn busy — mid-delete (\\\"Deleting…\\\", disabled, cursor wait)\",\n  \"id\": \"Q-1099\",\n  \"variant\": \"btn\",\n  \"lang\": \"en\",\n  \"_signals\": { \"busy\": true },\n  \"_mocks\": { \"quotesClient.delete\": \"__never_resolves__\" }\n}\n"}, {"name": "btn.json", "content": "{\n  \"_name\": \"Btn — default variant, idle (\\\"Delete\\\")\",\n  \"id\": \"Q-1099\",\n  \"variant\": \"btn\",\n  \"lang\": \"en\"\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — localized label + confirm copy (btn)\",\n  \"id\": \"Q-1099\",\n  \"variant\": \"btn\",\n  \"lang\": \"es\"\n}\n"}, {"name": "icon-busy.json", "content": "{\n  \"_name\": \"Icon busy — × disabled (opacity .5, cursor wait)\",\n  \"id\": \"Q-1094\",\n  \"variant\": \"icon\",\n  \"lang\": \"en\",\n  \"_signals\": { \"busy\": true },\n  \"_mocks\": { \"quotesClient.delete\": \"__never_resolves__\" }\n}\n"}, {"name": "icon.json", "content": "{\n  \"_name\": \"Icon — compact × (decided-row), idle\",\n  \"id\": \"Q-1094\",\n  \"variant\": \"icon\",\n  \"lang\": \"en\"\n}\n"}], "css": "/* ============================================================\n   DeleteQuoteButton — has NO dedicated CSS of its own.\n   It renders into styling owned by its host:\n   - variant=\"icon\"  → class \"qdone__del\" (decided-row × button,\n                        styled in static/quotes.css; mirrored in\n                        ../../quotes-sections/css/quotes-sections.css)\n   - variant=\"btn\"   → an UNCLASSED <button> that inherits the\n                        .qcard__back-foot button rules when rendered inside\n                        QuoteCard's back face (static/quotes.css; mirrored in\n                        ../../quote-card/css/quote-card.css)\n   The icon-variant rules are reproduced here so the button can be isolated\n   without its host page CSS loaded.\n   Tokens: --brand-pink, --fg-muted, --dur-fast, --ease-out.\n   ============================================================ */\n\n/* icon variant (qdone__del) */\n.qdone__del {\n  width: 24px;\n  height: 24px;\n  border-radius: 999px;\n  border: 1px solid rgba(26, 83, 92, 0.12);\n  background: #fff;\n  color: var(--fg-muted);\n  font-size: 14px;\n  line-height: 1;\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    color var(--dur-fast) var(--ease-out),\n    background var(--dur-fast) var(--ease-out);\n}\n.qdone__del:hover {\n  border-color: var(--brand-pink);\n  color: var(--brand-pink);\n  background: rgba(255, 107, 107, 0.06);\n}\n.qdone__del:disabled { opacity: 0.5; cursor: wait; }\n\n/* btn variant — inherits from host (.qcard__back-foot button). Reproduced\n   here for isolation so the \"Delete\"/\"Deleting…\" button renders as designed. */\n.qcard__back-foot button {\n  background: #fff;\n  border: 1px solid rgba(26, 83, 92, 0.12);\n  border-radius: 10px;\n  padding: 9px 6px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 11px;\n  color: var(--brand-teal);\n  cursor: pointer;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 5px;\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    color var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n.qcard__back-foot button:hover {\n  border-color: var(--brand-pink);\n  color: var(--brand-pink);\n  transform: translateY(-1px);\n}\n.qcard__back-foot button:disabled { opacity: 0.5; cursor: wait; }\n", "cssName": "delete-quote-button.css", "source": "import { useState } from \"preact/hooks\";\nimport { quotesClient } from \"../clients/quotes.ts\";\nimport { type Lang, langSignal, tFor } from \"../lib/i18n.ts\";\n\ninterface Props {\n  id: string;\n  /** Visual variant: \"btn\" matches qcard__back-foot buttons; \"icon\" is a compact × for table rows. */\n  variant?: \"btn\" | \"icon\";\n  label?: string;\n  confirmText?: string;\n  /** Optional SSR seed only; the live language is read from langSignal. */\n  lang?: Lang;\n}\n\nexport default function DeleteQuoteButton({\n  id,\n  variant = \"btn\",\n  label,\n  confirmText,\n}: Props) {\n  const lang = langSignal.value;\n  const [busy, setBusy] = useState(false);\n  const resolvedLabel = label ?? tFor(lang, \"deleteQuoteButton.label\");\n  const resolvedConfirm = confirmText ?? tFor(lang, \"deleteQuoteButton.confirm\");\n\n  async function onClick(e: MouseEvent) {\n    e.stopPropagation();\n    if (busy) return;\n    if (!globalThis.confirm(resolvedConfirm)) return;\n    setBusy(true);\n    try {\n      await quotesClient.delete(id);\n      globalThis.location.reload();\n    } catch (err) {\n      setBusy(false);\n      globalThis.alert(tFor(lang, \"deleteQuoteButton.error\", { message: (err as Error).message }));\n    }\n  }\n\n  if (variant === \"icon\") {\n    return (\n      <button\n        type=\"button\"\n        class=\"qdone__del\"\n        onClick={onClick}\n        disabled={busy}\n        aria-label={tFor(lang, \"deleteQuoteButton.ariaLabel\")}\n        title={tFor(lang, \"deleteQuoteButton.ariaLabel\")}\n      >\n        ×\n      </button>\n    );\n  }\n\n  return (\n    <button type=\"button\" onClick={onClick} disabled={busy}>\n      {busy ? tFor(lang, \"deleteQuoteButton.deleting\") : resolvedLabel}\n    </button>\n  );\n}\n", "sourceName": "DeleteQuoteButton.tsx", "checklist": "# Capture checklist — DeleteQuoteButton\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/quotes`. The button appears in two places:\n  - **btn variant** — inside a QuoteCard back foot: click a card to **flip** it,\n    the \"Delete\" button is the 4th button in `.qcard__back-foot`.\n  - **icon variant** — the `×` at the right of each **DecidedRow** in Track 03\n    \"Decided this month\" (expand the collapsed track to see them).\n\n## Viewports\n- **1280px** and **560px**. At 560px the DecidedRow hides `.qdone__when` but\n  keeps the `×` — confirm the icon button survives the reflow. The btn variant\n  lives in the fixed-geometry card foot (no reflow).\n\n## Element(s) to crop\n- The btn variant: crop the full `.qcard__back-foot` (4 buttons) so \"Delete\"\n  reads in context next to Resend / Copy link / View as client.\n- The icon variant: crop a single `.qdone__row` showing the `×` at the row end.\n\n## Transient states to drive\n1. **btn idle / icon idle** — default \"Delete\" / `×`.\n2. **hover** — both go pink-tinted (border+color; btn also lifts `-1px`).\n3. **delete-confirm** — click either → native\n   `confirm(\"Delete this quote? This cannot be undone.\")`. **Shoot the confirm\n   dialog.** This is the key transient. Then **cancel** (accepting fires\n   `DELETE /quotes/:id` then `location.reload()` — the page nukes itself).\n4. **busy** — to capture \"Deleting…\" / disabled `×` without a backend, mock\n   `quotesClient.delete` as never-resolving (isolate `btn-busy`/`icon-busy`\n   cases) and accept the confirm; the label flips to \"Deleting…\" and the button\n   disables (`opacity:.5; cursor:wait`).\n5. **error alert** — mock `quotesClient.delete` to reject → native\n   `alert(\"Couldn't delete quote: …\")`. Shoot the alert; `busy` resets to false\n   after.\n\n## Motion to film\n- Only hover transitions (`--dur-fast` color/transform). No keyframes. Film a\n  hover in/out for each variant. Re-shoot with `prefers-reduced-motion: reduce`\n  (global tokens clamp — hover goes instant). Note: the destructive flow's real\n  \"motion\" is the **full page reload** on success — flag it, don't pixel-diff it.\n"}, {"name": "quote-card", "component": "QuoteCard", "spec": "# QuoteCard\n\nThe pipeline quote card with a **flip-to-back engagement timeline**. A gradient\n\"mood\" header + avatar + client/title/story + CTA/value foot on the front; click\nflips to a back face showing the open-events timeline and a \"reading\" of the\nprospect's behavior. Rendered one-per-quote inside the \"Out for response\" and\n\"Drafting\" tracks on `/quotes`.\n\n## 1. Classification & behavior\n\n- **Bucket:** `island` (`islands/QuoteCard.tsx`).\n- **Interaction tier:** `island` — **client-only state**, no data fetch of its\n  own. It is a presentational card that receives a fully-built `Quote` from its\n  parent island (`QuotesPage` via `mapCard`).\n- **Server action + flash:** none directly. Its `qcard__back-foot` does host a\n  child mutation — `<DeleteQuoteButton id={q.id}>` (variant `\"btn\"`) — whose\n  delete path is the page-level reload anti-pattern (see delete-quote-button\n  spec). The other three foot buttons are **client-only**: Resend is a dead stub\n  (`onClick=stopPropagation` only), Copy-link writes `navigator.clipboard`, View\n  -as-client does `globalThis.open('/q/:id','_blank','noopener')` (a real new-tab\n  navigation, justified — not a reload).\n- **Island client-state + refresh:**\n  - `flipped: boolean` (`useState`) — front/back face. Set true on a card click\n    (unless the click originated inside `.qcard__cta`, `.qcard__flip-hint`, or\n    `.qcard__back`); reset false by the back-face close button.\n  - `copied: boolean` (`useState`) — momentary \"Copied!\" label after a successful\n    clipboard write; auto-reverts after `1500ms` via `setTimeout`.\n  - `lang` is a **prop** (default `\"en\"`), NOT read from `langSignal` here — the\n    parent (`QuotesPage`) reads `langSignal.value` and passes it down, so the\n    card re-renders with the page.\n- **`location.reload()` FLAG:** not in this file — but its child\n  **`DeleteQuoteButton` calls `globalThis.location.reload()`** after a successful\n  delete. The card holds no list state, so it cannot remove itself; the reload\n  lives one level down. Flagged here because the Delete button only ever appears\n  inside this card's back foot (btn variant) and the decided-row (icon variant).\n- **Data source:** none of its own — all display fields are pre-derived props on\n  the `Quote` object. The story (`QSTORIES[q.id]`), mood palette\n  (`moodForQuote(q)`), open-events (`buildOpens(q)`), and behavioral \"reading\"\n  (`readingFor(q, opens)`) are all computed **synchronously from the seed\n  helpers in `lib/quotes-seed.ts`** at render — there is no network read. On the\n  live path the engagement data arrives baked into the `QuoteCard` DTO from\n  `GET /quotes` and is mapped into the presentational `Quote` by the parent.\n- **Honest-empty:** if `q.opens === 0`, `buildOpens` returns `[]` → the back face\n  renders the `quoteCard.back.noOpens` line instead of a timeline, and the front\n  hides the `.qcard__opens` badge (`showOpens = stage !== \"draft\" && opens > 0`).\n- **Liveness:** none. Frozen props; no polling/websocket. A customer opening the\n  quote after mount won't bump the opens count without a parent re-fetch (which\n  only happens on a language flip or full reload).\n- **Reactivity / i18n:** `lang` prop drives every label via `tFor(lang, …)`\n  (CTA, value label, back eyebrow/opens/buttons). The `cta` text is a 7-way\n  branch on `q.stage` (+ an `opens >= 3` sub-branch for `opened`).\n- **Data-shape hazards:**\n  - **`value` UNIT MISMATCH (seed dollars vs live cents).** `q.value` is passed\n    straight to `fmtMoney`, which is **cents** by contract and intentionally NOT\n    unit-tolerant. On the **live** path `mapCard` copies `c.estimatedTotal`\n    (CENTS) → renders correctly. But the **static seed** `QPIPELINE` literals are\n    in **DOLLARS** (`value: 3850` for Q-1107), predating the audit#3 cents\n    unification — feeding the raw seed value to `fmtMoney` would render \"$39\"\n    instead of \"$3,850\". **Cases here use CENTS** (`385000`) to match the live\n    `mapCard` contract that actually feeds this island; do not copy the seed\n    literals verbatim into a fixture or the money reads wrong.\n  - **`band`/`shadow` are dead passengers.** The `Quote` carries `band`/`shadow`,\n    but the card recomputes the real per-stage palette via `moodForQuote(q)` and\n    writes `--mood-from/-to/-shadow/-status` inline. `mapCard` hard-codes\n    band/shadow to the pink ramp for every quote — never read here.\n  - **`opened` has two moods.** `moodForQuote` returns `opened_hot` (orange ramp)\n    when `stage===\"opened\" && opens >= 3`, else the green `opened` ramp — so two\n    \"opened\" cards can look different. The CTA mirrors this (`sendOffer` vs\n    `friendlyNudge`).\n  - **`stage`, not `status`.** This card is keyed entirely off the derived\n    **`stage`** (draft/sent/opened/cooling/stale/won/lost), never the stored\n    `status`.\n  - The `.qcard__flip-hint` pill has CSS but is **not rendered** by the TSX\n    (`onCardClick` even guards against clicks on it) — dead selector.\n\n## 2. Anatomy\n\n```\n<article class=\"qcard [qcard--flipped]\" style=\"--mood-from/-to/-shadow/-status\" onClick=onCardClick>\n  ── FRONT ──\n  <div class=\"qcard__mood\">                               ← gradient header (mood ramp)\n    <div class=\"qcard__numeral\">{idx+1 padStart 2 '0'}</div>  ← giant faint \"03\"\n    <div class=\"qcard__status\"><span dot/> {stageLabel[stage]}</div>\n    {showOpens && <div class=\"qcard__opens\"><OpenDots count=min(opens,5)/> {opens}×</div>}\n  </div>\n  <div class=\"qcard__av\">{q.initials}</div>               ← avatar, overlaps mood+body\n  <div class=\"qcard__body\">\n    <div class=\"qcard__client-name\">{q.client}</div>\n    <h3 class=\"qcard__title\">{q.title}</h3>               ← 2-line clamp\n    <p class=\"qcard__story\">{QSTORIES[id] ?? fallback}</p> ← 4-line clamp\n  </div>\n  <div class=\"qcard__foot\">\n    <button class=\"qcard__cta\" type=button onClick=stopProp>{cta} <span>→</span></button>\n    <div class=\"qcard__val-wrap\">\n      <div class=\"qcard__val-lbl\">{valueLabel}</div>\n      <div class=\"qcard__val-num\">{fmtMoney(value)}</div>\n    </div>\n  </div>\n  ── BACK (absolute, covers front; opacity/transform toggled by --flipped) ──\n  <div class=\"qcard__back\" aria-hidden={!flipped}>\n    <div class=\"qcard__back-head\">                        ← gradient (mood ramp)\n      <button class=\"qcard__back-close\" onClick=close><I x/></button>\n      <div class=\"qcard__back-eyebrow\">{back.eyebrow}</div>\n      <p class=\"qcard__back-big\">{opens}<small>{opens label} · {firstName}</small></p>\n    </div>\n    <div class=\"qcard__back-body\">\n      {opens.length\n        ? <div class=\"qcard__timeline\">{opens.map → .qcard__topen (dot + when/time + device)}</div>\n        : <div class=\"qcard__topen-meta\">{back.noOpens}</div>}\n      <p class=\"qcard__read\">{reading.text}{reading.em && <em/>}{reading.tail}</p>\n    </div>\n    <div class=\"qcard__back-foot\">                         ← 4 equal buttons\n      <button>{back.resend}</button>                       ← dead stub\n      <button onClick=copyPublicLink>{copied?linkCopied:copyLink}</button>\n      <button onClick=open('/q/:id')>{back.viewAsClient}</button>\n      <DeleteQuoteButton id={q.id}/>                        ← [island] btn variant\n    </div>\n  </div>\n</article>\n```\n- **Slots/children:** none — fully driven by the `q` prop.\n- **Icon dependency:** `I` + `ICN.x` from `lib/dash-icons.tsx` (copied to\n  `js/dash-icons.tsx`).\n- **Embeds:** `DeleteQuoteButton` [island, see its spec].\n\n## 3. Props\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `q` | `Quote` (presentational seed shape; see below) | — (required) | object | no |\n| `idx` | number (0-based; renders as `01`,`02`…) | — (required) | number | no |\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | no (prop, not langSignal) |\n\n`Quote` (from `lib/quotes-seed.ts` — the **presentational** shape, NOT the\n`QuoteCard` DTO): `{ id, title, client, initials, customerId?, stage:\ndraft|sent|opened|cooling|stale|won|lost, value:number /*CENTS on live path*/,\ndaysIn, opens, sentDays:number|null, decidedDays?, band:[string,string],\nshadow:string }`.\n\n## 4. States → cases\n\n| state | meaning | case |\n|---|---|---|\n| opened-hot | `stage:\"opened\"`, `opens:3` → orange `opened_hot` mood, opens badge, \"Send the offer\", \"shopping\" reading | `cases/opened-hot/opened-hot.json` |\n| sent | `stage:\"sent\"`, `opens:0` → pink mood, NO opens badge, \"Set a reminder\" | `cases/sent/sent.json` |\n| draft | `stage:\"draft\"`, `opens:0` → coffee mood, \"Finish + send\", draft reading | `cases/draft/draft.json` |\n| stale | `stage:\"stale\"`, `opens:1` → red mood, \"Win it back\", stale reading | `cases/stale/stale.json` |\n| won | `stage:\"won\"` → green mood, \"Open quote\" (decided; card-as-rendered) | `cases/won/won.json` |\n| flipped | back face shown (timeline + reading + foot) | `cases/flipped/flipped.json` |\n| es | Spanish CTA/labels/story | `cases/es/es.json` |\n\n> Isolate note: `flipped` uses `\"_signals\": { \"flipped\": true }` to force the\n> back face. The opens timeline, story, mood, and reading are all derived from\n> the `q` prop by the seed helpers at render — no `_mocks` needed.\n\n## 5. Events\n\n- `ev.expect(e => e.source === \"article.qcard\" && e.type === \"click\")` → sets\n  `flipped=true` (unless the click target is inside `.qcard__cta`,\n  `.qcard__flip-hint`, or `.qcard__back`).\n- `ev.expect(e => e.source === \"button.qcard__back-close\" && e.type === \"click\")`\n  → `flipped=false` (+ `stopPropagation`).\n- `ev.expect(e => e.source === \"button.qcard__cta\" && e.type === \"click\")` →\n  `stopPropagation` only (no nav — keeps the card from flipping; CTA is inert).\n- Copy-link button → `navigator.clipboard.writeText('{origin}/q/{id}')`, sets\n  `copied=true` for 1500ms. View-as-client → `globalThis.open('/q/:id','_blank')`.\n- Delete button click → bubbles into `DeleteQuoteButton` (confirm → delete →\n  reload). Owned by that island's spec.\n\n## 6. Motion (extracted)\n\n- **Card hover:** `transform: translateY(-4px)` + deepened mood shadow over\n  `320ms` (`--ease-bounce` on transform, `--ease-out` on shadow).\n- **Flip:** `.qcard__back` animates `transform: translateY(8px) scale(.98) →\n  translateY(0) scale(1)` over `380ms var(--ease-bounce)` + `opacity 0→1` over\n  `240ms var(--ease-out)`; `pointer-events` flips none→auto. Not a 3D Y-rotation\n  — it's a scale/fade reveal of an absolutely-positioned back face over the\n  front (front stays put underneath).\n- **Status dot:** `q-pulse-dot` keyframe — `opacity 1→.4→1` over `2.4s infinite`\n  (the **only** keyframe in `static/quotes.css`).\n- **CTA arrow / gap:** `.qcard__cta` `gap 6px→9px` on hover (`--dur-fast`); the\n  inline `→ span` has `transition: transform 240ms` (no hover rule assigns a\n  transform — effectively idle).\n- **Jank finding:** the flip animates `transform`+`opacity` (compositor-friendly,\n  smooth). The bounce easing (`cubic-bezier(.34,1.56,.64,1)`) overshoots scale\n  past 1 then settles — intended. `.qcard__back-body` is `overflow-y:auto`, so a\n  long timeline scrolls inside the flipped card rather than overflowing.\n- **Reduced motion:** **no component-local guard** — relies on the global tokens\n  reduced-motion clamp (`animation/transition-duration → 0.01ms`). Verify the\n  flip becomes instant and the pulse dot stills.\n\n## 7. Responsive\n\n- **No own `@media`.** The card has fixed internal geometry (138px mood header,\n  86px avatar). It reflows only via its parent grid: `.qcards` is\n  `repeat(auto-fill, minmax(320px, 1fr))`, so cards drop to a single column once\n  the track column is < ~320px wide. The page's `.qlay` 1fr+320px → 1fr collapse\n  happens at **1200px** (quotes-page spec). Verify within those host widths; the\n  card itself does not re-query.\n\n## 8. A11y\n\n- **Whole-card click via `<article onClick>`** — the front face is not a button\n  and is not keyboard-operable; flipping requires a mouse/touch tap. Rebuild fix:\n  make the card (or a clear affordance) a real `<button>`/`role`+`tabindex` with\n  `aria-expanded` for the flip, or expose a focusable flip control.\n- **Back face uses `aria-hidden={!flipped}`** — good (hides the off-screen face\n  from AT), but because the front isn't `aria-hidden` when flipped, both faces\n  can be in the a11y tree at once while the front sits behind. Confirm/clean up.\n- The back-close button has an `aria-label` (`common.close`) — good. The CTA and\n  foot buttons are real `<button type=button>` and focusable.\n- Numeral/mood are decorative (`pointer-events:none`, `user-select:none`).\n\n## 9. Used on\n\n**`/quotes` only** (the flip `islands/QuoteCard.tsx`), rendered by `QuotesPage`\ninside the \"Out for response\" (`outSorted`) and \"Drafting\" (`drafts`) tracks.\nVerified by grep of `<QuoteCard` JSX usage across `routes/ islands/ components/`.\n\n> **Correction to brief:** the flip island is **not** rendered on the dashboard\n> or inside the assistant. `islands/DashboardPage.tsx` imports only the\n> `QuoteCard` *DTO type* from `clients/quotes.ts` and renders its own\n> `QuoteRow` (`quoteToRow`); `islands/AsstChat.tsx`'s `lastQuoteCard` (line\n> ~1051) is a local action-card *message* variable, not this island. The\n> `<QuoteCard …>` at `routes/q/[id].tsx:137` and `islands/PhoneChat.tsx:54` are\n> **separate local components** with different prop shapes (`QuotePublic` /\n> `QuoteCopy`), not this island. (PhoneChat's is the landing-demo card.) So the\n> only live consumer of THIS island is `QuotesPage`.\n", "fixture": "{\n  \"category\": \"quotes\",\n  \"folder\": \"quote-card\",\n  \"controls\": {\n    \"idx\": { \"type\": \"number\", \"value\": 0 },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] },\n    \"q.title\": { \"type\": \"text\", \"value\": \"Garage epoxy floor — 2-car\" },\n    \"q.client\": { \"type\": \"text\", \"value\": \"Tom & Linda Kowalski\" },\n    \"q.initials\": { \"type\": \"text\", \"value\": \"TK\" },\n    \"q.stage\": {\n      \"type\": \"select\",\n      \"value\": \"opened\",\n      \"options\": [\"draft\", \"sent\", \"opened\", \"cooling\", \"stale\", \"won\", \"lost\"]\n    },\n    \"q.value\": { \"type\": \"number\", \"value\": 385000 },\n    \"q.opens\": { \"type\": \"number\", \"value\": 3 },\n    \"q.sentDays\": { \"type\": \"number\", \"value\": 1 }\n  }\n}\n", "cases": [{"name": "draft.json", "content": "{\n  \"_name\": \"Draft — Q-1109, never sent (coffee mood, no opens badge, \\\"Finish + send\\\")\",\n  \"idx\": 2,\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1109\",\n    \"title\": \"Storefront awning replacement\",\n    \"client\": \"Cobblestone Cafe\",\n    \"initials\": \"CC\",\n    \"customerId\": \"cust-cc\",\n    \"stage\": \"draft\",\n    \"value\": 425000,\n    \"daysIn\": 1,\n    \"opens\": 0,\n    \"sentDays\": null,\n    \"band\": [\"#9C8074\", \"#5C4034\"],\n    \"shadow\": \"rgba(92,64,52,0.32)\"\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — localized CTA / value label / story (Q-1107 opened-hot)\",\n  \"idx\": 0,\n  \"lang\": \"es\",\n  \"q\": {\n    \"id\": \"Q-1107\",\n    \"title\": \"Garage epoxy floor — 2-car\",\n    \"client\": \"Tom & Linda Kowalski\",\n    \"initials\": \"TK\",\n    \"customerId\": \"cust-tk\",\n    \"stage\": \"opened\",\n    \"value\": 385000,\n    \"daysIn\": 1,\n    \"opens\": 3,\n    \"sentDays\": 1,\n    \"band\": [\"#F7A893\", \"#E8704F\"],\n    \"shadow\": \"rgba(232,112,79,0.4)\"\n  }\n}\n"}, {"name": "flipped.json", "content": "{\n  \"_name\": \"Flipped — back face: opens timeline + reading + foot (Q-1107)\",\n  \"idx\": 0,\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1107\",\n    \"title\": \"Garage epoxy floor — 2-car\",\n    \"client\": \"Tom & Linda Kowalski\",\n    \"initials\": \"TK\",\n    \"customerId\": \"cust-tk\",\n    \"stage\": \"opened\",\n    \"value\": 385000,\n    \"daysIn\": 1,\n    \"opens\": 3,\n    \"sentDays\": 1,\n    \"band\": [\"#F7A893\", \"#E8704F\"],\n    \"shadow\": \"rgba(232,112,79,0.4)\"\n  },\n  \"_signals\": { \"flipped\": true }\n}\n"}, {"name": "opened-hot.json", "content": "{\n  \"_name\": \"Opened-hot — Q-1107, opens≥3 (orange mood, opens badge, \\\"Send the offer\\\")\",\n  \"idx\": 0,\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1107\",\n    \"title\": \"Garage epoxy floor — 2-car\",\n    \"client\": \"Tom & Linda Kowalski\",\n    \"initials\": \"TK\",\n    \"customerId\": \"cust-tk\",\n    \"stage\": \"opened\",\n    \"value\": 385000,\n    \"daysIn\": 1,\n    \"opens\": 3,\n    \"sentDays\": 1,\n    \"band\": [\"#F7A893\", \"#E8704F\"],\n    \"shadow\": \"rgba(232,112,79,0.4)\"\n  }\n}\n"}, {"name": "sent.json", "content": "{\n  \"_name\": \"Sent — Q-1108, no opens (pink mood, NO opens badge, \\\"Set a reminder\\\")\",\n  \"idx\": 1,\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1108\",\n    \"title\": \"Fence stain — back run\",\n    \"client\": \"Marcus Lin\",\n    \"initials\": \"ML\",\n    \"customerId\": \"cust-ml\",\n    \"stage\": \"sent\",\n    \"value\": 240000,\n    \"daysIn\": 0,\n    \"opens\": 0,\n    \"sentDays\": 0,\n    \"band\": [\"#FFB3B3\", \"#FF6B6B\"],\n    \"shadow\": \"rgba(255,107,107,0.35)\"\n  }\n}\n"}, {"name": "stale.json", "content": "{\n  \"_name\": \"Stale — Q-1101, 12 days out, 1 open (red mood, \\\"Win it back\\\")\",\n  \"idx\": 3,\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1101\",\n    \"title\": \"Deck stain & seal\",\n    \"client\": \"Hilltop Diner\",\n    \"initials\": \"HD\",\n    \"customerId\": \"cust-hd\",\n    \"stage\": \"stale\",\n    \"value\": 185000,\n    \"daysIn\": 12,\n    \"opens\": 1,\n    \"sentDays\": 12,\n    \"band\": [\"#FF6B6B\", \"#D63F3F\"],\n    \"shadow\": \"rgba(255,107,107,0.4)\"\n  }\n}\n"}, {"name": "won.json", "content": "{\n  \"_name\": \"Won — Q-1099, decided (green mood, \\\"Open quote\\\"; card-as-rendered, not the DecidedRow)\",\n  \"idx\": 0,\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1099\",\n    \"title\": \"Building C re-roof\",\n    \"client\": \"Maple Grove Apartments\",\n    \"initials\": \"MG\",\n    \"customerId\": \"cust-mg\",\n    \"stage\": \"won\",\n    \"value\": 1420000,\n    \"daysIn\": 0,\n    \"opens\": 4,\n    \"sentDays\": 6,\n    \"decidedDays\": 1,\n    \"band\": [\"#5FA34F\", \"#3F7A33\"],\n    \"shadow\": \"rgba(81,152,67,0.35)\"\n  }\n}\n"}], "css": "/* ============================================================\n   QuoteCard — extracted from static/quotes.css (.qcard* rules).\n   Tokens: --brand-teal, --brand-pink, --fg-muted, --mint-200,\n   --radius-xl, --radius-pill, --font-heading, --font-body,\n   --dur-fast, --ease-out, --ease-bounce.\n   Mood colors arrive as inline custom props on the .qcard element:\n   --mood-from / --mood-to / --mood-shadow / --mood-status\n   (set by moodForQuote() in lib/quotes-seed.ts).\n   ============================================================ */\n\n/* --- grid the page slots cards into (defined here for isolate context) --- */\n.qcards {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));\n  gap: 16px;\n}\n\n/* --- card shell --- */\n.qcard {\n  position: relative;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  overflow: visible;\n  cursor: pointer;\n  box-shadow:\n    0 1px 0 rgba(26, 83, 92, 0.04),\n    0 8px 22px -14px rgba(26, 83, 92, 0.18);\n  transition:\n    transform 320ms var(--ease-bounce),\n    box-shadow 320ms var(--ease-out);\n  display: flex;\n  flex-direction: column;\n}\n.qcard:hover {\n  transform: translateY(-4px);\n  box-shadow:\n    0 2px 0 rgba(26, 83, 92, 0.05),\n    0 22px 42px -18px var(--mood-shadow, rgba(26, 83, 92, 0.28));\n}\n\n/* --- mood block (gradient header) --- */\n.qcard__mood {\n  position: relative;\n  height: 138px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  border-radius: var(--radius-xl) var(--radius-xl) 0 0;\n  overflow: hidden;\n}\n.qcard__mood::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 18% 22%, rgba(255, 255, 255, 0.18) 0, transparent 35%),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n  pointer-events: none;\n}\n.qcard__numeral {\n  position: absolute;\n  /* Anchored bottom:4px (NOT -18px) so the descender isn't clipped by\n     .qcard__mood overflow:hidden — see #30. ContractCard's .kcard__numeral\n     still uses bottom:-18px (clipping bug not yet ported back). */\n  bottom: 4px;\n  right: -2px;\n  font-family: var(--font-heading);\n  font-weight: 900;\n  font-size: 88px;\n  line-height: 1;\n  color: rgba(255, 255, 255, 0.16);\n  letter-spacing: -0.04em;\n  pointer-events: none;\n  user-select: none;\n}\n.qcard__status {\n  position: absolute;\n  top: 14px;\n  left: 14px;\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 5px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.92);\n  backdrop-filter: blur(4px);\n  color: var(--mood-status, var(--brand-teal));\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  z-index: 2;\n}\n.qcard__status-dot {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: currentColor;\n  animation: q-pulse-dot 2.4s infinite;\n}\n@keyframes q-pulse-dot {\n  0%, 100% { opacity: 1; }\n  50% { opacity: 0.4; }\n}\n.qcard__opens {\n  position: absolute;\n  top: 12px;\n  right: 12px;\n  display: inline-flex;\n  align-items: center;\n  gap: 5px;\n  padding: 6px 10px;\n  border-radius: var(--radius-pill);\n  background: rgba(255, 255, 255, 0.18);\n  color: rgba(255, 255, 255, 0.95);\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.06em;\n  backdrop-filter: blur(3px);\n  z-index: 2;\n}\n.qcard__opens-dots {\n  display: inline-flex;\n  gap: 3px;\n  align-items: center;\n}\n.qcard__opens-dot {\n  width: 5px;\n  height: 5px;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.30);\n}\n.qcard__opens-dot--on {\n  background: rgba(255, 255, 255, 0.85);\n}\n\n/* --- avatar (overlaps mood + body) --- */\n.qcard__av {\n  position: absolute;\n  left: 22px;\n  top: 92px;\n  width: 86px;\n  height: 86px;\n  border-radius: 22px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  display: grid;\n  place-items: center;\n  color: #fff;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 28px;\n  letter-spacing: 0.02em;\n  z-index: 4;\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.28),\n    0 12px 24px -8px rgba(0, 0, 0, 0.32);\n}\n\n/* --- body --- */\n.qcard__body { padding: 50px 22px 0; }\n.qcard__client-name {\n  font-family: var(--font-body);\n  font-size: 12px;\n  color: var(--fg-muted);\n  margin: 0 0 4px;\n  letter-spacing: 0.02em;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qcard__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 19px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n  line-height: 1.2;\n  margin: 0 0 14px;\n  overflow: hidden;\n  display: -webkit-box;\n  -webkit-line-clamp: 2;\n  -webkit-box-orient: vertical;\n}\n.qcard__story {\n  font-family: var(--font-body);\n  font-size: 14.5px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  text-wrap: pretty;\n  margin: 0;\n  display: -webkit-box;\n  -webkit-line-clamp: 4;\n  -webkit-box-orient: vertical;\n  overflow: hidden;\n}\n\n/* --- foot (CTA + value) --- */\n.qcard__foot {\n  margin-top: 18px;\n  padding: 14px 22px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  display: grid;\n  grid-template-columns: 1fr auto;\n  gap: 12px;\n  align-items: center;\n}\n.qcard__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding: 0;\n  border: none;\n  background: transparent;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 12px;\n  color: var(--brand-pink);\n  cursor: pointer;\n  text-align: left;\n  line-height: 1.3;\n  transition: gap var(--dur-fast) var(--ease-out);\n}\n.qcard__cta:hover { gap: 9px; }\n.qcard__val-wrap { text-align: right; }\n.qcard__val-lbl {\n  font-family: var(--font-heading);\n  font-size: 9.5px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 1px;\n}\n.qcard__val-num {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 15px;\n  letter-spacing: -0.01em;\n  color: var(--brand-teal);\n}\n\n/* --- back face (flip target, absolutely covers the front) --- */\n.qcard__back {\n  position: absolute;\n  inset: 0;\n  background: #fff;\n  border-radius: var(--radius-xl);\n  z-index: 10;\n  display: flex;\n  flex-direction: column;\n  overflow: hidden;\n  transform: translateY(8px) scale(0.98);\n  opacity: 0;\n  pointer-events: none;\n  transition: transform 380ms var(--ease-bounce), opacity 240ms var(--ease-out);\n  box-shadow: 0 22px 50px -22px var(--mood-shadow, rgba(26, 83, 92, 0.4));\n}\n.qcard--flipped .qcard__back {\n  transform: translateY(0) scale(1);\n  opacity: 1;\n  pointer-events: auto;\n}\n.qcard__back-head {\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  color: #fff;\n  padding: 18px 22px 16px;\n  position: relative;\n  overflow: hidden;\n}\n.qcard__back-head::before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background-image:\n    radial-gradient(circle at 18% 22%, rgba(255, 255, 255, 0.18) 0, transparent 35%),\n    radial-gradient(circle at 82% 78%, rgba(0, 0, 0, 0.10) 0, transparent 40%);\n  pointer-events: none;\n}\n.qcard__back-eyebrow {\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 800;\n  letter-spacing: 0.1em;\n  text-transform: uppercase;\n  opacity: 0.78;\n  margin-bottom: 6px;\n}\n.qcard__back-big {\n  font-family: var(--font-heading);\n  font-weight: 900;\n  font-size: 36px;\n  line-height: 1;\n  letter-spacing: -0.02em;\n  margin: 0;\n}\n.qcard__back-big small {\n  font-size: 15px;\n  font-weight: 700;\n  opacity: 0.78;\n  margin-left: 4px;\n  letter-spacing: 0;\n}\n.qcard__back-close {\n  position: absolute;\n  top: 12px;\n  right: 12px;\n  width: 28px;\n  height: 28px;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.22);\n  color: #fff;\n  border: none;\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  z-index: 2;\n  transition: background var(--dur-fast) var(--ease-out);\n}\n.qcard__back-close:hover { background: rgba(255, 255, 255, 0.38); }\n.qcard__back-body {\n  padding: 18px 22px;\n  flex: 1 1 auto;\n  overflow-y: auto;\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n}\n\n/* --- opens timeline (back) --- */\n.qcard__timeline {\n  display: flex;\n  flex-direction: column;\n  position: relative;\n}\n.qcard__timeline::before {\n  content: \"\";\n  position: absolute;\n  left: 6px;\n  top: 6px;\n  bottom: 6px;\n  width: 1px;\n  background: linear-gradient(to bottom, var(--mood-from), var(--mood-to));\n  opacity: 0.3;\n}\n.qcard__topen {\n  display: grid;\n  grid-template-columns: 14px 1fr auto;\n  gap: 10px;\n  align-items: center;\n  padding: 7px 0;\n  position: relative;\n}\n.qcard__topen-dot {\n  width: 13px;\n  height: 13px;\n  border-radius: 999px;\n  background: linear-gradient(135deg, var(--mood-from) 0%, var(--mood-to) 100%);\n  box-shadow: 0 0 0 3px #fff, 0 0 0 4px rgba(0, 0, 0, 0.06);\n  z-index: 1;\n}\n.qcard__topen-when {\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 13px;\n  color: var(--brand-teal);\n  letter-spacing: -0.005em;\n}\n.qcard__topen-when strong { color: var(--brand-teal); }\n.qcard__topen-meta {\n  font-family: var(--font-body);\n  font-size: 11px;\n  color: var(--fg-muted);\n  margin-top: 1px;\n}\n.qcard__topen-dev {\n  font-size: 11px;\n  color: var(--fg-muted);\n  text-align: right;\n}\n.qcard__read {\n  font-family: var(--font-body);\n  font-size: 14px;\n  line-height: 1.5;\n  color: var(--brand-teal);\n  background: linear-gradient(180deg, rgba(255, 107, 107, 0.06) 0%, rgba(255, 107, 107, 0.02) 100%);\n  border-left: 3px solid var(--brand-pink);\n  border-radius: 0 10px 10px 0;\n  padding: 12px 14px;\n  text-wrap: pretty;\n  margin: 4px 0 0;\n}\n.qcard__read em {\n  font-style: normal;\n  color: var(--brand-pink);\n  font-weight: 700;\n}\n.qcard__back-foot {\n  display: grid;\n  grid-template-columns: repeat(4, 1fr);\n  gap: 6px;\n  padding: 12px 14px;\n  border-top: 1px solid rgba(26, 83, 92, 0.08);\n  background: rgba(26, 83, 92, 0.02);\n}\n.qcard__back-foot button {\n  background: #fff;\n  border: 1px solid rgba(26, 83, 92, 0.12);\n  border-radius: 10px;\n  padding: 9px 6px;\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 11px;\n  color: var(--brand-teal);\n  cursor: pointer;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 5px;\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    color var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n.qcard__back-foot button:hover {\n  border-color: var(--brand-pink);\n  color: var(--brand-pink);\n  transform: translateY(-1px);\n}\n\n/* --- flip-hint pill (CSS exists; NOT rendered by QuoteCard.tsx — dead) --- */\n.qcard__flip-hint {\n  position: absolute;\n  bottom: 10px;\n  right: 14px;\n  z-index: 2;\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n  padding: 4px 9px;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.16);\n  color: rgba(255, 255, 255, 0.85);\n  font-family: var(--font-heading);\n  font-size: 9px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  backdrop-filter: blur(3px);\n  cursor: pointer;\n  transition: background var(--dur-fast) var(--ease-out);\n  pointer-events: auto;\n}\n.qcard__flip-hint:hover { background: rgba(255, 255, 255, 0.3); }\n\n/* No component-local reduced-motion block — relies on the global tokens\n   reduced-motion clamp (every transition/animation duration → 0.01ms). */\n", "cssName": "quote-card.css", "source": "/**\n * Pipeline quote card with flip-to-back engagement timeline.\n * Mirrors the prototype's QuoteCard component verbatim.\n */\nimport { useState } from \"preact/hooks\";\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { fmtMoney } from \"../lib/format.ts\";\nimport { tFor } from \"../lib/i18n.ts\";\nimport {\n  buildOpens,\n  moodForQuote,\n  QSTORIES,\n  type Quote,\n  readingFor,\n  stageLabel,\n} from \"../lib/quotes-seed.ts\";\nimport DeleteQuoteButton from \"./DeleteQuoteButton.tsx\";\n\ninterface Props {\n  q: Quote;\n  idx: number;\n  lang?: \"en\" | \"es\";\n}\n\nfunction OpenDots({ count, max = 5 }: { count: number; max?: number }) {\n  return (\n    <span class=\"qcard__opens-dots\">\n      {Array.from({ length: max }).map((_, i) => (\n        <span\n          key={i}\n          class={`qcard__opens-dot ${i < count ? \"qcard__opens-dot--on\" : \"\"}`}\n        />\n      ))}\n    </span>\n  );\n}\n\nexport default function QuoteCard({ q, idx, lang = \"en\" }: Props) {\n  const [flipped, setFlipped] = useState(false);\n  const [copied, setCopied] = useState(false);\n\n  async function copyPublicLink(e: MouseEvent) {\n    e.stopPropagation();\n    try {\n      await navigator.clipboard.writeText(\n        `${globalThis.location.origin}/q/${q.id}`,\n      );\n      setCopied(true);\n      setTimeout(() => setCopied(false), 1500);\n    } catch { /* clipboard unavailable — leave the label as-is */ }\n  }\n  const mood = moodForQuote(q);\n  const story = QSTORIES[q.id] ?? tFor(lang, \"quoteCard.storyFallback\");\n  const cta = q.stage === \"draft\"\n    ? tFor(lang, \"quoteCard.cta.finishSend\")\n    : q.stage === \"opened\" && q.opens >= 3\n    ? tFor(lang, \"quoteCard.cta.sendOffer\")\n    : q.stage === \"opened\"\n    ? tFor(lang, \"quoteCard.cta.friendlyNudge\")\n    : q.stage === \"cooling\"\n    ? tFor(lang, \"quoteCard.cta.trimResend\")\n    : q.stage === \"stale\"\n    ? tFor(lang, \"quoteCard.cta.winItBack\")\n    : q.stage === \"sent\"\n    ? tFor(lang, \"quoteCard.cta.setReminder\")\n    : tFor(lang, \"quoteCard.cta.openQuote\");\n  const showOpens = q.stage !== \"draft\" && q.opens > 0;\n  const opens = buildOpens(q);\n  const reading = readingFor(q, opens);\n\n  function onCardClick(e: MouseEvent) {\n    if (flipped) return;\n    const target = e.target as HTMLElement;\n    if (\n      target.closest(\".qcard__cta\") || target.closest(\".qcard__flip-hint\") ||\n      target.closest(\".qcard__back\")\n    ) return;\n    setFlipped(true);\n  }\n\n  const styleStr = `--mood-from:${mood.from};` +\n    `--mood-to:${mood.to};` +\n    `--mood-shadow:${mood.shadow};` +\n    `--mood-status:${mood.statusFg};`;\n\n  return (\n    <article\n      class={`qcard ${flipped ? \"qcard--flipped\" : \"\"}`}\n      style={styleStr}\n      onClick={onCardClick}\n    >\n      <div class=\"qcard__mood\">\n        <div class=\"qcard__numeral\">{String(idx + 1).padStart(2, \"0\")}</div>\n        <div class=\"qcard__status\">\n          <span class=\"qcard__status-dot\" />\n          {stageLabel[q.stage]}\n        </div>\n        {showOpens && (\n          <div class=\"qcard__opens\">\n            <OpenDots count={Math.min(q.opens, 5)} /> {q.opens}×\n          </div>\n        )}\n      </div>\n      <div class=\"qcard__av\">{q.initials}</div>\n      <div class=\"qcard__body\">\n        <div class=\"qcard__client-name\">{q.client}</div>\n        <h3 class=\"qcard__title\">{q.title}</h3>\n        <p class=\"qcard__story\">{story}</p>\n      </div>\n      <div class=\"qcard__foot\">\n        <button\n          class=\"qcard__cta\"\n          type=\"button\"\n          onClick={(e) => e.stopPropagation()}\n        >\n          {cta}{\" \"}\n          <span style=\"display:inline-block;transition:transform 240ms\">→</span>\n        </button>\n        <div class=\"qcard__val-wrap\">\n          <div class=\"qcard__val-lbl\">{tFor(lang, \"quoteCard.valueLabel\")}</div>\n          <div class=\"qcard__val-num\">{fmtMoney(q.value)}</div>\n        </div>\n      </div>\n\n      <div class=\"qcard__back\" aria-hidden={!flipped}>\n        <div class=\"qcard__back-head\">\n          <button\n            class=\"qcard__back-close\"\n            type=\"button\"\n            onClick={(e) => {\n              e.stopPropagation();\n              setFlipped(false);\n            }}\n            aria-label={tFor(lang, \"common.close\")}\n          >\n            <I d={ICN.x} size={14} sw={2.5} />\n          </button>\n          <div class=\"qcard__back-eyebrow\">\n            {tFor(lang, \"quoteCard.back.eyebrow\")}\n          </div>\n          <p class=\"qcard__back-big\">\n            {q.opens}\n            <small>\n              {tFor(\n                lang,\n                `quoteCard.back.opens.${q.opens === 1 ? \"one\" : \"other\"}`,\n                { n: q.opens },\n              )} · {q.client.split(/\\s+/)[0]}\n            </small>\n          </p>\n        </div>\n        <div class=\"qcard__back-body\">\n          {opens.length > 0\n            ? (\n              <div class=\"qcard__timeline\">\n                {opens.map((o, i) => (\n                  <div class=\"qcard__topen\" key={i}>\n                    <span class=\"qcard__topen-dot\" />\n                    <div>\n                      <div class=\"qcard__topen-when\">\n                        <strong>{o.when}</strong> · {o.time}\n                      </div>\n                    </div>\n                    <div class=\"qcard__topen-dev\">{o.device}</div>\n                  </div>\n                ))}\n              </div>\n            )\n            : (\n              <div class=\"qcard__topen-meta\" style=\"padding:8px 0\">\n                {tFor(lang, \"quoteCard.back.noOpens\")}\n              </div>\n            )}\n          <p class=\"qcard__read\">\n            {reading.text}\n            {reading.em ? <em>{reading.em}</em> : null}\n            {reading.tail ?? null}\n          </p>\n        </div>\n        <div class=\"qcard__back-foot\">\n          <button type=\"button\" onClick={(e) => e.stopPropagation()}>\n            {tFor(lang, \"quoteCard.back.resend\")}\n          </button>\n          <button type=\"button\" onClick={copyPublicLink}>\n            {copied\n              ? tFor(lang, \"quoteCard.back.linkCopied\")\n              : tFor(lang, \"quoteCard.back.copyLink\")}\n          </button>\n          <button\n            type=\"button\"\n            onClick={(e) => {\n              e.stopPropagation();\n              globalThis.open(`/q/${q.id}`, \"_blank\", \"noopener\");\n            }}\n          >\n            {tFor(lang, \"quoteCard.back.viewAsClient\")}\n          </button>\n          <DeleteQuoteButton id={q.id} />\n        </div>\n      </div>\n    </article>\n  );\n}\n", "sourceName": "QuoteCard.tsx", "checklist": "# Capture checklist — QuoteCard\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/quotes` — cards live inside Track 01 \"Out for response\"\n  (defaults open) and Track 02 \"Drafting\" (collapsed → expand it to shoot the\n  draft card). Decided quotes render as DecidedRows, NOT QuoteCards, so the\n  `won` card-as-rendered state is isolate-only (drive via the isolate `won`\n  case, not the live page).\n\n## Viewports\n- **1280px** (full track grid, ~3 cards/row) and **640px** (single column —\n  `.qcards` minmax(320px) drops to 1 col; the page shell also cuts at 640/641px).\n- The card has **no own `@media`**; reflow is driven by `.qcards` and the page\n  `.qlay` 1200px collapse. Shoot at 1280 + 640 to capture both grid widths.\n\n## Element(s) to crop\n- A single `<article class=\"qcard\">` — capture each mood ramp:\n  - **opened-hot** (orange, opens badge \"3×\") — Q-1107.\n  - **sent** (pink, no opens badge) — Q-1108.\n  - **draft** (coffee, no badge) — Q-1109 (expand Track 02).\n  - **stale** (red) — Q-1101.\n- Crop tight to the article (it has `overflow:visible` + a drop shadow — include\n  ~24px bleed for the shadow).\n\n## Transient states to drive\n1. **front** — default; note the `q-pulse-dot` status dot pulsing.\n2. **hover** — card lifts `translateY(-4px)` with a deeper mood-tinted shadow.\n3. **flipped** — click the card body (not the CTA) → back face scales/fades in\n   (380ms bounce). Shoot: the opens **timeline** (Q-1107, 3 opens across\n   iPhone/Mac) AND the empty `quoteCard.back.noOpens` line (drive a 0-opens card,\n   e.g. a fresh `sent`).\n4. **copied** — click \"Copy link\" in the back foot → label flips to \"Copied!\"\n   for 1500ms (clipboard write). Capture mid-window.\n5. **delete-confirm** — click \"Delete\" in the back foot → native\n   `globalThis.confirm(\"Delete this quote? This cannot be undone.\")` dialog.\n   Shoot the confirm dialog (then cancel — confirming reloads the page).\n6. **back-close** — back-face × button (top-right) returns to front.\n\n## Motion to film\n- **Flip:** `.qcard__back` `translateY(8px) scale(.98) → 0/1` over 380ms\n  `--ease-bounce` + opacity 240ms `--ease-out`. Film one flip + one close.\n- **Hover lift:** 320ms transform (bounce) + shadow (ease-out).\n- **Pulse dot:** `q-pulse-dot` 2.4s loop (the only quotes.css keyframe).\n- Re-shoot the flip with `prefers-reduced-motion: reduce` — there is NO\n  component-local guard, so verify the global tokens clamp makes it instant.\n"}, {"name": "quotes-page", "component": "QuotesPage", "spec": "# QuotesPage\n\nThe whole `/quotes` page body: a client-side data island that fetches the\npipeline + win-rate + insight on mount, groups quotes into the three tracks,\nand composes the SSR sections + flip cards + sidebar.\n\n## 1. Classification & behavior\n\n- **Bucket:** `island` (`islands/QuotesPage.tsx`).\n- **Interaction tier:** `island` — **whole-page client-only island.** The SSR\n  route renders an empty shell; this island owns the entire visible body and\n  every fetch.\n- **Server action + flash:** none. There is no form here, no Fresh route handler\n  for the body, no PRG. All reads are client `fetch` via `quotesClient`.\n- **Island client-state + refresh:**\n  - `s: State = { loading, error, quotes, winRate, insight }` in `useState`,\n    seeded `{ loading:true … }`.\n  - `lang = langSignal.value` read **during render** → the island re-renders\n    when SettingsPage flips the UI language; the `lang` prop is an ignored SSR\n    seed (commented as such).\n  - `useEffect([lang])`: `Promise.all([list, winRate(90), insight])` (each\n    `.catch` → fallback empty/null), then `setS({loading:false, quotes: cards.map(mapCard), …})`.\n    Re-fetches whenever `lang` changes. Uses an `alive` guard to drop stale\n    resolutions.\n- **`location.reload()` FLAG:** not in this file — but its child\n  **`DeleteQuoteButton` calls `globalThis.location.reload()`** after a successful\n  delete. Because the data lives only in this island's frozen `useState`, a\n  delete can't surgically update the list, so the app nukes the whole document.\n  See §1 anti-pattern.\n- **Data source per region:**\n  - quotes → `GET /quotes` (`QuoteCard[]`, engagement-derived).\n  - win-rate → `GET /analytics/quotes/win-rate?days=90` (`WinRate`).\n  - insight → `GET /analytics/quotes/insight` (`Insight`, feeds QSideTip).\n- **Honest-empty:** if `/quotes` returns `[]` (or throws → caught → `[]`), all\n  groups are empty; QuotesHero shows its **empty** variant, KPIs read 0 /\n  win-rate \"—\", tracks render with 0 cards (`.qcards` empty), sidebar widgets\n  degrade (QSideBig empty list, QSideRate \"—\", QSideTip default text).\n- **Liveness:** none. One fetch on mount (+ refetch on language change). No\n  polling, no websocket. A quote opened by a customer won't update without a\n  reload.\n- **Data-shape hazards:**\n  - **Money unit:** `mapCard` copies `c.estimatedTotal` (data-model: **CENTS**)\n    straight into `q.value`, and `fmtMoney` expects **cents** — consistent here.\n    Contrast ContractsPage, which must `* 100` because its card strings are\n    dollars. Don't \"fix\" one to match the other.\n  - **stage vs status:** the island groups by **`stage`** (derived engagement:\n    draft/sent/opened/cooling/stale/won/lost), NOT stored `status`. `open` =\n    draft+sent+opened+cooling+stale; `out` = those minus draft; `decided` =\n    won+lost.\n  - **clientCount** counts only quotes with a truthy `customerId` (a `Set`),\n    deliberately so two unlinked \"—\" quotes don't collapse into one phantom\n    client (#31).\n  - **win-rate fallback math** is defensive and slightly convoluted: if the\n    `/win-rate` endpoint is null it recomputes won/lost/decided from the decided\n    group; the `lost` fallback expression double-evaluates the won count.\n  - `mapCard` hard-codes `band`/`shadow` to the pink ramp for EVERY quote — the\n    real per-stage mood color is recomputed inside QuoteCard via\n    `moodForQuote(q)`, so these two fields are effectively dead passengers.\n\n### Anti-pattern (page-island + reload)\n\nPer the breakdown brief: QuotesPage is exactly the \"whole-page island with\nfrozen SSR/fetch props + `location.reload()` after mutation\" anti-pattern.\n\n- **Frozen data:** fetched once into `useState`; the only refresh paths are a\n  language flip or a full reload.\n- **Reload after mutate:** `DeleteQuoteButton` → `quotesClient.delete(id)` →\n  `location.reload()`. A delete of one decided row reloads the entire SPA,\n  re-running every fetch and re-rendering the shell.\n- **Fix (rebuild):** Two viable directions —\n  1. **Fresh Partial / form+PRG:** make delete a `<form method=\"POST\">` posting\n     to a route handler that deletes then redirects back to `/quotes`\n     (`303`), rendered through an `f-partial` so only the list swaps — no JS\n     reload, flash via the redirect.\n  2. **Lift + optimistic island state:** hold `quotes` in a signal owned by\n     QuotesPage; DeleteQuoteButton calls a passed-in `onDeleted(id)` that splices\n     the array. No document reload. (Callbacks aren't serializable, so in\n     isolate they surface as Events, not props.)\n- This island also re-fetches all three endpoints on **every** language toggle;\n  cheaper would be to re-map the existing `quotes` array client-side.\n\n## 2. Anatomy\n\n```\nQuotesPage (Fragment)\n├─ loading → <ShimmerStyle/> + <PageHeaderSkeleton/> + <CardGridSkeleton rows=3/>   [shared]\n├─ error   → <div class=\"qpage-error\">{loadError}</div>                              [UNSTYLED — hazard]\n└─ ready\n   ├─ <QuotesHero .../>                       (QuotesSections)\n   ├─ <QuotesKpis .../>                        (QuotesSections)\n   └─ <div class=\"qlay\">\n        ├─ <div> (tracks column)\n        │   ├─ QuoteTrack 01 \"Out for response\"  → .qcards [QuoteCard ×N, sorted]\n        │   ├─ QuoteTrack 02 \"Drafting\"          → .qcards [QuoteCard ×N]\n        │   └─ QuoteTrack 03 \"Decided this month\"→ .qdone  [DecidedRow ×N]\n        └─ <aside class=\"qside\">                 (QSideBig, QSideRate, QSideTip)\n```\n\n## 3. Props\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `lang` | `\"en\"\\|\"es\"` | `\"en\"` | select | **ignored SSR seed** — live lang from `langSignal.value` |\n\nNo other props. Everything else is fetched.\n\n## 4. States → cases\n\n| state | meaning | case |\n|---|---|---|\n| loading | initial mount, fetch in flight → skeletons | `cases/loading/loading.json` |\n| populated | seed pipeline grouped into 3 tracks + sidebar | `cases/populated/populated.json` |\n| empty | `/quotes` → `[]` → empty hero, 0 KPIs, \"—\" rate | `cases/empty/empty.json` |\n| error | a fetch rejected past its `.catch` → `.qpage-error` | `cases/error/error.json` |\n\n> Isolate note: this island self-fetches. The cases drive the **rendered**\n> states by mocking `quotesClient.list/winRate/insight` (`_mocks`) rather than\n> passing props. `loading` mocks a never-resolving promise; `empty` mocks `[]`;\n> `error` mocks a reject on all three.\n\n## 5. Events\n\n- `ev.expect(e => e.source === \"header.qtrack__head\" && e.type === \"click\")` →\n  bubbles to QuoteTrack [shared] (collapse). Not owned here.\n- `ev.expect(e => e.source === \"article.qcard\" && e.type === \"click\")` → bubbles\n  to QuoteCard flip. Not owned here.\n- The \"+ New quote\" CTA (QuotesHero) is a `type=\"button\"` with **no onClick** —\n  a dead stub (see quotes-sections).\n\n## 6. Motion\n\nNo motion of its own. Composed motion: QuoteTrack collapse (240ms chevron +\n320ms grid-rows), QuoteCard flip (380ms bounce), `.qbar__fill` width (1.2s),\n`.qrate` static SVG. Skeleton shimmer is the only thing animating in `loading`.\nReduced-motion handled by the global tokens clamp.\n\n## 7. Responsive\n\nOwn breakpoint: `.qlay` collapses `1fr 320px` → `1fr` at **max-width 1200px**\n(sidebar drops below the tracks; `.qside` loses `position:sticky`). Child reflow\n(`.qph` stack, `.qkpi` 4→2→1, `.qcards` →1col, `.qdone` →1col) handled by\nquotes-sections / quote-card / quote-track CSS at 1200/1100/768/560px.\n\n## 8. A11y\n\n- **Error region** `.qpage-error` is a bare div — no `role=\"alert\"`/`aria-live`,\n  and (worse) **unstyled** because no CSS rule targets it. Rebuild: real error\n  card + `role=\"alert\"`.\n- Loading skeletons should expose `aria-busy`/`aria-hidden` (owned by shared\n  Skeletons).\n- Inherits the QuoteTrack header a11y gap (clickable `<header>`, not a button) —\n  flagged in the shared QuoteTrack spec.\n\n## 9. Used on\n\n`/quotes` only. It is the page body for `routes/quotes/index.tsx`. Not reused\nelsewhere (the dashboard builds its own `QuoteRow` list from `QuoteCard[]` data,\nand does NOT mount this island or the flip QuoteCard).\n", "fixture": "{\n  \"category\": \"quotes\",\n  \"folder\": \"quotes-page\",\n  \"controls\": {\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  },\n  \"components\": [\n    \"quotes-sections\",\n    \"quote-card\",\n    \"delete-quote-button\",\n    \"quote-track\"\n  ]\n}\n", "cases": [{"name": "empty.json", "content": "{\n  \"_name\": \"Empty — no quotes (honest-empty hero + 0 KPIs + \\\"—\\\" rate)\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"quotesClient.list\": [],\n    \"quotesClient.winRate\": { \"windowDays\": 90, \"decided\": 0, \"won\": 0, \"lost\": 0, \"winRate\": null },\n    \"quotesClient.insight\": null\n  }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — fetch rejected → .qpage-error (UNSTYLED, hazard)\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"quotesClient.list\": \"__reject:Network request failed__\",\n    \"quotesClient.winRate\": \"__reject:Network request failed__\",\n    \"quotesClient.insight\": \"__reject:Network request failed__\"\n  },\n  \"_note\": \"Each fetch in QuotesPage has its own .catch() → fallback, so a single rejection does NOT trip the error branch. The error div only renders if Promise.all's .catch fires (e.g. an exception thrown in the .then mapper). To force it in isolate, make .list resolve to a non-array that breaks mapCard, or mock the whole Promise.all to reject.\"\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — skeletons while fetch in flight\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"quotesClient.list\": \"__never_resolves__\",\n    \"quotesClient.winRate\": \"__never_resolves__\",\n    \"quotesClient.insight\": \"__never_resolves__\"\n  }\n}\n"}, {"name": "populated.json", "content": "{\n  \"_name\": \"Populated — full seed pipeline (mirrors lib/quotes-seed.ts)\",\n  \"lang\": \"en\",\n  \"_mocks\": {\n    \"quotesClient.list\": [\n      { \"id\": \"Q-1107\", \"customerId\": \"cust-tk\", \"customerName\": \"Tom & Linda Kowalski\", \"jobName\": \"Garage epoxy floor — 2-car\", \"estimatedTotal\": 385000, \"stage\": \"opened\", \"daysIn\": 1, \"opens\": 3, \"sentDays\": 1, \"lastOpenAt\": \"2026-06-18T14:18:00Z\", \"decidedDays\": null, \"createdAt\": \"2026-06-17T09:00:00Z\", \"updatedAt\": \"2026-06-18T14:18:00Z\" },\n      { \"id\": \"Q-1106\", \"customerId\": \"cust-gh\", \"customerName\": \"Greenleaf HOA\", \"jobName\": \"Common-area paint — buildings A & B\", \"estimatedTotal\": 580000, \"stage\": \"opened\", \"daysIn\": 3, \"opens\": 2, \"sentDays\": 3, \"lastOpenAt\": \"2026-06-17T11:00:00Z\", \"decidedDays\": null, \"createdAt\": \"2026-06-15T09:00:00Z\", \"updatedAt\": \"2026-06-17T11:00:00Z\" },\n      { \"id\": \"Q-1108\", \"customerId\": \"cust-ml\", \"customerName\": \"Marcus Lin\", \"jobName\": \"Fence stain — back run\", \"estimatedTotal\": 240000, \"stage\": \"sent\", \"daysIn\": 0, \"opens\": 0, \"sentDays\": 0, \"lastOpenAt\": null, \"decidedDays\": null, \"createdAt\": \"2026-06-18T08:00:00Z\", \"updatedAt\": \"2026-06-18T08:00:00Z\" },\n      { \"id\": \"Q-1101\", \"customerId\": \"cust-hd\", \"customerName\": \"Hilltop Diner\", \"jobName\": \"Deck stain & seal\", \"estimatedTotal\": 185000, \"stage\": \"stale\", \"daysIn\": 12, \"opens\": 1, \"sentDays\": 12, \"lastOpenAt\": \"2026-06-08T16:00:00Z\", \"decidedDays\": null, \"createdAt\": \"2026-06-06T09:00:00Z\", \"updatedAt\": \"2026-06-08T16:00:00Z\" },\n      { \"id\": \"Q-1109\", \"customerId\": \"cust-cc\", \"customerName\": \"Cobblestone Cafe\", \"jobName\": \"Storefront awning replacement\", \"estimatedTotal\": 425000, \"stage\": \"draft\", \"daysIn\": 1, \"opens\": 0, \"sentDays\": null, \"lastOpenAt\": null, \"decidedDays\": null, \"createdAt\": \"2026-06-17T09:00:00Z\", \"updatedAt\": \"2026-06-17T09:00:00Z\" },\n      { \"id\": \"Q-1099\", \"customerId\": \"cust-mg\", \"customerName\": \"Maple Grove Apartments\", \"jobName\": \"Building C re-roof\", \"estimatedTotal\": 1420000, \"stage\": \"won\", \"daysIn\": 0, \"opens\": 4, \"sentDays\": 6, \"lastOpenAt\": \"2026-06-16T10:00:00Z\", \"decidedDays\": 1, \"createdAt\": \"2026-06-10T09:00:00Z\", \"updatedAt\": \"2026-06-17T09:00:00Z\" },\n      { \"id\": \"Q-1094\", \"customerId\": \"cust-or\", \"customerName\": \"Ortega Rentals\", \"jobName\": \"Window trim repaint\", \"estimatedTotal\": 190000, \"stage\": \"lost\", \"daysIn\": 0, \"opens\": 2, \"sentDays\": 8, \"lastOpenAt\": \"2026-06-12T10:00:00Z\", \"decidedDays\": 4, \"createdAt\": \"2026-06-06T09:00:00Z\", \"updatedAt\": \"2026-06-14T09:00:00Z\" }\n    ],\n    \"quotesClient.winRate\": { \"windowDays\": 90, \"decided\": 8, \"won\": 6, \"lost\": 2, \"winRate\": 75 },\n    \"quotesClient.insight\": { \"text\": \"Quotes you send before noon get opened 2× faster. Try sending Marcus Lin's today.\", \"kind\": \"best_day_of_week\" }\n  }\n}\n"}], "css": "/* ============================================================\n   QuotesPage — the island owns only LAYOUT + LOADING/ERROR shells.\n   The visible chrome (hero/kpi/cards/sidebar) is styled by its children\n   (QuotesSections, QuoteCard, QuoteTrack[shared]). Extracted from\n   static/quotes.css (.qlay) + Skeletons[shared].\n   Tokens: --font-body, --ease-out.\n   ============================================================ */\n\n/* Two-column body: pipeline tracks (1fr) + sticky sidebar (320px). */\n.qlay {\n  display: grid;\n  grid-template-columns: 1fr 320px;\n  gap: 22px;\n  align-items: flex-start;\n}\n\n@media (max-width: 1200px) {\n  .qlay { grid-template-columns: 1fr; }\n  /* .qside drops its sticky positioning at this breakpoint (see\n     quotes-sections.css). */\n}\n\n/* ERROR STATE — the island renders <div class=\"qpage-error\">…</div>.\n   HAZARD: this class has NO rule in static/quotes.css OR static/dashboard.css,\n   so the error message renders as unstyled body text (default font/size/color).\n   Rebuild should add a real error-card style; reproduced minimal here so the\n   isolate error case is at least legible. */\n.qpage-error {\n  font-family: var(--font-body);\n  font-size: 14px;\n  color: var(--fg, #1a535c);\n  padding: 24px 0;\n}\n\n/* LOADING STATE — rendered via the shared Skeletons:\n   <ShimmerStyle/> + <PageHeaderSkeleton/> + <CardGridSkeleton rows={3}/>.\n   Those styles are owned by shared-components/skeletons — see there. */\n", "cssName": "quotes-page.css", "source": "/**\n * Top-level data island for /quotes. The SSR route renders only the page\n * shell; this island fetches the pipeline + win-rate + insight from the\n * backend on mount.\n */\nimport { useEffect, useState } from \"preact/hooks\";\nimport {\n  type Insight,\n  type QuoteCard as BackendQuoteCard,\n  quotesClient,\n  type WinRate,\n} from \"../clients/quotes.ts\";\nimport {\n  DecidedRow,\n  QSideBig,\n  QSideRate,\n  QSideTip,\n  QuotesHero,\n  QuotesKpis,\n} from \"../components/QuotesSections.tsx\";\nimport QuoteTrack from \"./QuoteTrack.tsx\";\nimport QuoteCard from \"./QuoteCard.tsx\";\nimport type { Quote } from \"../lib/quotes-seed.ts\";\nimport {\n  CardGridSkeleton,\n  PageHeaderSkeleton,\n  ShimmerStyle,\n} from \"../components/Skeletons.tsx\";\nimport { langSignal, tFor } from \"../lib/i18n.ts\";\n\n// Inner sort order for the \"Out for response\" track — most engaged\n// (opened) at top, then sent, cooling, stale. Mirrors the reference's\n// pipeline layout.\nconst STAGE_ORDER = { opened: 0, sent: 1, cooling: 2, stale: 3 } as const;\n\nfunction initialsFromName(name: string | null | undefined): string {\n  if (!name) return \"—\";\n  const parts = name.trim().split(/\\s+/).filter(Boolean);\n  if (parts.length === 0) return \"—\";\n  if (parts.length === 1) return parts[0].slice(0, 2).toUpperCase();\n  return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();\n}\n\nfunction clientFromSummary(summary: string | null | undefined): string {\n  if (!summary) return \"—\";\n  const m = summary.match(/—\\s*(.+)$/);\n  return m ? m[1].trim() : \"—\";\n}\n\nfunction mapCard(c: BackendQuoteCard, lang: \"en\" | \"es\"): Quote {\n  const fallbackClient = clientFromSummary(c.summary);\n  const client = c.customerName ?? fallbackClient;\n  return {\n    id: c.id,\n    // Prefer the ≤3-word Job Name (roadmap p.10) so cards read the same as\n    // the quote, agreement, and invoice headings; summary is the fallback.\n    title: c.jobName?.trim() || c.summary ||\n      tFor(lang, \"quotesPage.untitledQuote\"),\n    client,\n    customerId: c.customerId,\n    initials: initialsFromName(c.customerName ?? fallbackClient),\n    stage: c.stage,\n    value: c.estimatedTotal ?? 0,\n    daysIn: c.daysIn,\n    opens: c.opens,\n    sentDays: c.sentDays,\n    decidedDays: c.decidedDays ?? undefined,\n    band: [\"#FFB3B3\", \"#FF6B6B\"],\n    shadow: \"rgba(255,107,107,0.35)\",\n  };\n}\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  quotes: Quote[];\n  winRate: WinRate | null;\n  insight: Insight | null;\n}\n\nconst INITIAL: State = {\n  loading: true,\n  error: null,\n  quotes: [],\n  winRate: null,\n  insight: null,\n};\n\nexport default function QuotesPage(_props: { lang?: \"en\" | \"es\" }) {\n  // Self-source the live UI language: reading langSignal.value during render\n  // makes this island re-render when SettingsPage flips the language. The\n  // optional `lang` prop is an ignored SSR seed.\n  const lang = langSignal.value;\n  const [s, setS] = useState<State>(INITIAL);\n\n  useEffect(() => {\n    let alive = true;\n    Promise.all([\n      quotesClient.list().catch(() => [] as BackendQuoteCard[]),\n      quotesClient.winRate(90).catch(() => null as WinRate | null),\n      quotesClient.insight().catch(() => null as Insight | null),\n    ]).then(([cards, winRate, insight]) => {\n      if (!alive) return;\n      const safeCards = Array.isArray(cards) ? cards : [];\n      setS({\n        loading: false,\n        error: null,\n        quotes: safeCards.map((c) => mapCard(c, lang)),\n        winRate,\n        insight,\n      });\n    }).catch((err: Error) => {\n      if (!alive) return;\n      setS({ ...INITIAL, loading: false, error: err.message });\n    });\n    return () => {\n      alive = false;\n    };\n  }, [lang]);\n\n  if (s.loading) {\n    return (\n      <>\n        <ShimmerStyle />\n        <PageHeaderSkeleton />\n        <CardGridSkeleton rows={3} />\n      </>\n    );\n  }\n  if (s.error) {\n    return (\n      <div class=\"qpage-error\">\n        {tFor(lang, \"quotesPage.loadError\", { error: s.error })}\n      </div>\n    );\n  }\n\n  const { quotes, winRate, insight } = s;\n\n  const open = quotes.filter((q) =>\n    [\"draft\", \"sent\", \"opened\", \"cooling\", \"stale\"].includes(q.stage)\n  );\n  const openTotal = open.reduce((acc, q) => acc + q.value, 0);\n  const stale = quotes.filter((q) => q.stage === \"stale\");\n\n  const out = quotes.filter((q) =>\n    [\"sent\", \"opened\", \"cooling\", \"stale\"].includes(q.stage)\n  );\n  const outVal = out.reduce((acc, q) => acc + q.value, 0);\n  const drafts = quotes.filter((q) => q.stage === \"draft\");\n  const decided = quotes.filter((q) => [\"won\", \"lost\"].includes(q.stage));\n\n  // Inner sort: opened > sent > cooling > stale within \"Out for response\".\n  const outSorted = [...out].sort((a, b) => {\n    const av = STAGE_ORDER[a.stage as keyof typeof STAGE_ORDER] ?? 9;\n    const bv = STAGE_ORDER[b.stage as keyof typeof STAGE_ORDER] ?? 9;\n    return av - bv;\n  });\n\n  const won = winRate?.won ?? decided.filter((q) => q.stage === \"won\").length;\n  const lost = winRate?.lost ??\n    (decided.length -\n      (winRate?.won ?? decided.filter((q) => q.stage === \"won\").length));\n  const decidedCount = winRate?.decided ?? decided.length;\n  const winRatePct = winRate?.winRate ??\n    (decidedCount ? Math.round((won / decidedCount) * 100) : 0);\n\n  return (\n    <>\n      <QuotesHero\n        lang={lang}\n        openCount={open.length}\n        openTotal={openTotal}\n        staleCount={stale.length}\n        // Only count quotes with a real customer link. Two unlinked quotes\n        // shouldn't collapse into a single phantom \"—\" client (#31).\n        clientCount={new Set(\n          open.map((q) => q.customerId).filter((id): id is string =>\n            Boolean(id)\n          ),\n        ).size}\n      />\n      <QuotesKpis\n        lang={lang}\n        outValue={outVal}\n        outCount={out.length}\n        draftCount={drafts.length}\n        decidedCount={decidedCount}\n        wonCount={won}\n        lostCount={lost}\n        winRate={winRatePct}\n      />\n      <div class=\"qlay\">\n        <div>\n          <QuoteTrack\n            lang={lang}\n            num=\"01\"\n            title={tFor(lang, \"quotesPage.track.outForResponse\")}\n            count={out.length}\n            defaultOpen\n            storageKey=\"quotes:track:01\"\n          >\n            <div class=\"qcards\">\n              {outSorted.map((q, i) => (\n                <QuoteCard key={q.id} q={q} idx={i} lang={lang} />\n              ))}\n            </div>\n          </QuoteTrack>\n\n          <QuoteTrack\n            lang={lang}\n            num=\"02\"\n            title={tFor(lang, \"quotesPage.track.drafting\")}\n            count={drafts.length}\n            defaultOpen={false}\n            storageKey=\"quotes:track:02\"\n          >\n            <div class=\"qcards\">\n              {drafts.map((q, i) => (\n                <QuoteCard key={q.id} q={q} idx={i} lang={lang} />\n              ))}\n            </div>\n          </QuoteTrack>\n\n          <QuoteTrack\n            lang={lang}\n            num=\"03\"\n            title={tFor(lang, \"quotesPage.track.decidedThisMonth\")}\n            count={decided.length}\n            defaultOpen={false}\n            storageKey=\"quotes:track:03\"\n          >\n            <div class=\"qdone\">\n              {decided.map((q) => <DecidedRow key={q.id} q={q} lang={lang} />)}\n            </div>\n          </QuoteTrack>\n        </div>\n\n        <aside class=\"qside\">\n          <QSideBig open={out} lang={lang} />\n          <QSideRate won={won} lost={lost} lang={lang} />\n          <QSideTip text={insight?.text} lang={lang} />\n        </aside>\n      </div>\n    </>\n  );\n}\n", "sourceName": "QuotesPage.tsx", "checklist": "# Capture checklist — QuotesPage\n\n- **Route:** `/quotes` (auth-gated — dev master OTP `000000`).\n- **Viewports** (quotes.css real `@media`: 1200 / 1100 / 768 / 560px): 390, 768,\n  1100, 1280.\n- **Crop targets:** the `.content` island; the QuotesSections blocks (hero, KPIs,\n  side rate gauge); a `QuoteCard` (front + flipped); the QuoteTrack tracks.\n- **States to drive:**\n  - `loading` — Skeletons on mount.\n  - `ready` — populated from `lib/quotes-seed.ts` (`QPIPELINE`, 15 rows).\n  - `empty` — no quotes (honest-empty).\n  - `error` — fetch failure.\n  - card moods: opened-hot, sent, draft, cooling/stale, won, lost.\n- **Theme:** light only. **No fabricated screenshots** — capture against a seeded dev backend.\n"}, {"name": "quotes-sections", "component": "QuotesSections", "spec": "# QuotesSections\n\nThe six **server-rendered (static)** sections of `/quotes`, exported as separate\nfunctions from one module: `QuotesHero`, `QuotesKpis`, `DecidedRow`, `QSideBig`,\n`QSideRate`, `QSideTip`. The interactive bits (track collapse, card flip, delete)\nlive in islands; these are pure presentational components the page island\ncomposes.\n\n## 1. Classification & behavior\n\n- **Bucket:** `static` / **page-composition** (`components/QuotesSections.tsx`).\n  These are plain function components (no `useState`/effects/signals) — they\n  render from props and are composed by `islands/QuotesPage.tsx`.\n- **Interaction tier:** **static** — no client state of their own. Every value is\n  a prop passed down from the page island (which owns the fetch). The one\n  interactive element they *embed* is `DeleteQuoteButton` (icon variant) inside\n  `DecidedRow` — an island.\n- **Server action + flash:** none. No forms, no PRG here. (The embedded\n  `DeleteQuoteButton` does the client-fetch + reload — flagged in its own spec;\n  `DecidedRow` just places it.) The `QuotesHero` `.qph__cta` \"New quote\" button\n  is a `type=\"button\"` with **no `onClick`** — a **dead stub** (no handler\n  anywhere; rebuild should wire it to the new-quote flow or remove it).\n- **Island client-state + refresh:** N/A (static). They re-render only when the\n  parent island re-renders (language flip / reload).\n- **`location.reload()` FLAG:** not here — but `DecidedRow` embeds\n  `<DeleteQuoteButton variant=\"icon\">`, which reloads on delete. Flagged\n  transitively (see delete-quote-button spec).\n- **Data source:** none — all props. The parent (`QuotesPage`) derives them from\n  `GET /quotes` (the `QuoteCard[]` → presentational `Quote[]`) +\n  `/analytics/quotes/win-rate` + `/insight`.\n- **Honest-empty:** **QuotesHero owns the empty state** — `empty = openCount===0`\n  renders the \"Nothing in the pipeline yet.\" headline + empty sub, hiding the\n  money/stale copy. `QuotesKpis` shows `0`/`—` cells. `QSideBig` renders an empty\n  `.qbig` list if `open=[]`. `QSideRate` shows `—` + \"needs more\" copy below the\n  confidence threshold. `QSideTip` falls back to `quotesTip.default` when no\n  insight text. So the section set degrades gracefully to a coherent empty page.\n- **Liveness:** none.\n- **Reactivity / i18n:** every label via `tFor(lang, …)`; a local `tnFor(lang,\n  key, n)` helper does explicit-language plural picking (mirrors the reactive\n  `tn()` but honors the resolved `lang` prop instead of `langSignal`, because\n  these are SSR components).\n- **Data-shape hazards:**\n  - **Money is CENTS.** `outValue`/`openTotal`/`q.value` all flow into `fmtMoney`\n    (cents). The parent's `mapCard` puts `estimatedTotal` (cents) into\n    `q.value` — consistent. (The static seed literals are dollars; the live\n    `mapCard` path is cents — see quote-card spec's unit hazard. Cases here use\n    CENTS.)\n  - **Win-rate confidence threshold.** `WIN_RATE_MIN_N = 5`: below 5 decided\n    quotes the percentage is suppressed to `—` in BOTH `QuotesKpis` (gates the\n    `qkpi__val`) and `QSideRate` (gates the gauge arc + number), showing a\n    breakdown/\"need N more\" instead. A real one-accept \"100%\" is deliberately\n    hidden until N≥5. Two components encode the same threshold — keep them in\n    sync on rebuild.\n  - **`QSideRate` recomputes its own pct** from `won/lost` props\n    (`Math.round(won/decided*100)`), independent of the `winRate` value\n    `QuotesKpis` receives — two slightly different rate computations on the same\n    page (the KPI trusts the endpoint's `winRate`; the gauge recomputes). Verify\n    they agree.\n  - **`QSideBig` sorts by `value` desc and slices top 4**; `max = top4[0].value`\n    drives the bar widths (`value/max*100%`). Empty `open` → `max=1` guard avoids\n    div-by-zero.\n  - **`DecidedRow` `when`** uses `decidedDays ?? 0`; `1 → \"yesterday\"`, else\n    `\"{n}d ago\"`. A `decidedDays:0` would read \"0d ago\" (no \"today\" string).\n  - **`QSideTip` is fully inline-styled** (teal gradient, white text, no class) —\n    the only section without a CSS class hook.\n\n## 2. Anatomy\n\n```\nQuotesHero ({openCount, openTotal, staleCount, clientCount, lang})\n  <div class=\"qph\">\n    <div>\n      <div class=\"qph__eyebrow\"><span dot/> {eyebrow}</div>\n      {empty   → <h1 class=\"qph__title\"><em>{emptyLine1}</em><br/>{emptyLine2}</h1> + <p qph__sub>{emptySub}</p>}\n      {allWarm → <h1><em>{fmtMoney(openTotal)}</em> {warmTitle}</h1> + warmSub}\n      {else    → <h1><em>{fmtMoney(openTotal)}</em> {staleTitle(quotes,verb)}</h1> + staleSub(<strong>{staleCount}</strong>)}\n    </div>\n    <button class=\"qph__cta\" type=button>{<I plus/>} {cta}</button>   ← DEAD STUB (no onClick)\n  </div>\n\nQuotesKpis ({outValue,outCount,draftCount,decidedCount,wonCount,lostCount,winRate,lang})\n  <div class=\"qkpi\"> 4× .qkpi__cell (first --accent):\n    [Out for response → fmtMoney(outValue) / \"{outCount} waiting\"]\n    [Drafting        → {draftCount} / sub]\n    [Decided         → {decidedCount} / \"{won} won · {lost} lost\"]\n    [Win rate        → {decided>=5 ? \"{winRate}%\" : \"—\"} / contextual sub]\n\nDecidedRow ({q, lang})\n  <div class=\"qdone__row\">\n    <div class=\"qdone__badge qdone__badge--{won|lost}\"><I {check|x}/></div>\n    <div><div qdone__title>{q.title}</div><div qdone__client>{q.client}</div></div>\n    <div class=\"qdone__amt [qdone__amt--lost]\">{fmtMoney(q.value)}</div>\n    <div class=\"qdone__when\">{when}</div>\n    <DeleteQuoteButton id={q.id} variant=\"icon\"/>                    ← [island]\n  </div>\n\nQSideBig ({open, lang})  → .qside__card > .qbig (top-4 by value, each row + .qbar fill)\nQSideRate ({won, lost, lang}) → .qside__card > .qrate (SVG arc gauge + number/labels; gated by N≥5)\nQSideTip ({text?, lang}) → inline-styled teal card (insight text ?? default)\n```\n- **Slots/children:** none — props only.\n- **Icon dependency:** `I` + `ICN.plus`/`ICN.check`/`ICN.x` from\n  `lib/dash-icons.tsx` (copied to `js/dash-icons.tsx`).\n- **Embeds:** `DeleteQuoteButton` (icon variant) in `DecidedRow` [island].\n\n## 3. Props\n\n`QuotesHero`: `openCount:number`, `openTotal:number /*CENTS*/`,\n`staleCount:number`, `clientCount:number`, `lang?:\"en\"|\"es\"`.\n`QuotesKpis`: `outValue:number /*CENTS*/`, `outCount`, `draftCount`,\n`decidedCount`, `wonCount`, `lostCount`, `winRate:number`, `lang?`.\n`DecidedRow`: `q:Quote`, `lang?`.\n`QSideBig`: `open:Quote[]`, `lang?`.\n`QSideRate`: `won:number`, `lost:number`, `lang?`.\n`QSideTip`: `text?:string`, `lang?`.\n\n(`Quote` = the presentational seed shape from `lib/quotes-seed.ts` — see\nquote-card spec §3.)\n\n## 4. States → cases\n\n| state | meaning | case |\n|---|---|---|\n| hero-stale | QuotesHero with stale quotes (money + \"needs a nudge\" copy) | `cases/hero-stale/hero-stale.json` |\n| hero-warm | QuotesHero, all warm (no stale) | `cases/hero-warm/hero-warm.json` |\n| hero-empty | QuotesHero honest-empty (\"Nothing in the pipeline yet.\") | `cases/hero-empty/hero-empty.json` |\n| kpis-confident | QuotesKpis, decided≥5 → real win-rate % | `cases/kpis-confident/kpis-confident.json` |\n| kpis-not-enough | QuotesKpis, decided<5 → \"—\" + \"need more\" | `cases/kpis-not-enough/kpis-not-enough.json` |\n| decided-won | DecidedRow won (green badge, amount) | `cases/decided-won/decided-won.json` |\n| decided-lost | DecidedRow lost (coffee badge, struck-through amount) | `cases/decided-lost/decided-lost.json` |\n| side-rate-confident | QSideRate gauge filled (N≥5) | `cases/side-rate-confident/side-rate-confident.json` |\n| side-rate-low | QSideRate \"—\" (N<5) | `cases/side-rate-low/side-rate-low.json` |\n| es | Spanish labels across hero+kpis | `cases/es/es.json` |\n\n> Isolate note: this is page-composition — `fixture.json` lists the embedded\n> `components` and each case names which section + the props that section\n> renders. Because the exports are separate functions, a case targets one\n> section at a time (its `_name` says which); the props are bare. `DecidedRow`\n> cases include the embedded `DeleteQuoteButton` (icon).\n\n## 5. Events\n\n- No own events (static). Transitively:\n  - `ev.expect(e => e.source === \"button.qph__cta\" && e.type === \"click\")` → **no\n    handler** (dead stub) — assert it does nothing.\n  - `ev.expect(e => e.source === \"button.qdone__del\" && e.type === \"click\")` →\n    bubbles into `DeleteQuoteButton` (icon). Owned by that island.\n\n## 6. Motion (extracted)\n\n- **`.qbar__fill` width:** `transition: width 1.2s var(--ease-out)` — the top-quote\n  bars grow from 0 to their `value/max` width on (re)render. The slowest motion\n  on the page.\n- **`.qph__cta` hover:** `translateY(-1px)` over `--dur-fast`.\n- **`.qdone__row` hover:** `translateX(2px)` + `border-color → --mint-300` over\n  `--dur-fast`.\n- **`.qdone__del` hover:** color/border/bg → pink (owned by DeleteQuoteButton).\n- **`QSideRate` SVG** is **static** (no stroke-dashoffset animation) — the arc is\n  rendered at its final `stroke-dasharray` directly.\n- **No keyframes** (the only quotes.css keyframe, `q-pulse-dot`, belongs to\n  QuoteCard).\n- **Jank finding:** the 1.2s bar transition triggers on every parent re-render\n  (e.g. language flip re-fetches → bars re-animate from 0). Mildly distracting on\n  a no-op refresh; consider animating only on first mount.\n- **Reduced motion:** no component-local guard; relies on the global tokens clamp\n  (bar fill + hover transitions go instant).\n\n## 7. Responsive\n\nOwn `@media` (in `static/quotes.css`, mirrored in this folder's css):\n- **1200px:** `.qph` → column (CTA drops below the title); `.qkpi` 4 → 2 cols;\n  `.qside` loses `position:sticky` (becomes `static`).\n- **1100px:** `.qdone` 2 → 1 col (decided rows stack full-width).\n- **768px:** `.qkpi` 2 → 1 col.\n- **560px:** `.qdone__row` tightens padding/gap and **hides `.qdone__when`**.\nShoot the sections at 1280 / 1100 / 768 / 560.\n\n## 8. A11y\n\n- **QuotesHero CTA** is a real `<button type=button>` but **does nothing** — a\n  focusable control with no action confuses keyboard/AT users. Rebuild: wire it\n  or remove it.\n- KPI / decided / sidebar values are plain text — fine, but the win-rate `—`\n  fallback conveys \"not enough data\" only visually; consider an `aria-label`\n  spelling it out.\n- `QSideRate` SVG gauge has **no `role=\"img\"`/`aria-label`** — the percentage is\n  in adjacent text, so it's not invisible, but the arc itself is undescribed.\n- DecidedRow badge (`✓`/`✗`) is an icon-only status — the won/lost meaning is\n  carried by the badge color + struck amount; add a text/`aria` label of the\n  outcome.\n- Inherits the embedded `DeleteQuoteButton` a11y notes (native confirm).\n\n## 9. Used on\n\n`/quotes` only — all six exports are imported and composed by\n`islands/QuotesPage.tsx`. No other consumer (grep of `QuotesSections` import).\n`DecidedRow` is the only section that embeds an island\n(`DeleteQuoteButton` icon variant, `QuotesSections.tsx:186`).\n", "fixture": "{\n  \"category\": \"quotes\",\n  \"folder\": \"quotes-sections\",\n  \"controls\": {\n    \"section\": {\n      \"type\": \"select\",\n      \"value\": \"QuotesHero\",\n      \"options\": [\"QuotesHero\", \"QuotesKpis\", \"DecidedRow\", \"QSideBig\", \"QSideRate\", \"QSideTip\"]\n    },\n    \"lang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"] }\n  },\n  \"components\": {\n    \"delete-quote-button\": \"embedded by DecidedRow (variant=\\\"icon\\\")\"\n  }\n}\n", "cases": [{"name": "decided-lost.json", "content": "{\n  \"_name\": \"DecidedRow — lost (coffee badge, struck-through amount)\",\n  \"section\": \"DecidedRow\",\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1094\",\n    \"title\": \"Window trim repaint\",\n    \"client\": \"Ortega Rentals\",\n    \"initials\": \"OR\",\n    \"stage\": \"lost\",\n    \"value\": 190000,\n    \"daysIn\": 0,\n    \"opens\": 2,\n    \"sentDays\": 8,\n    \"decidedDays\": 4,\n    \"band\": [\"#B89D90\", \"#785544\"],\n    \"shadow\": \"rgba(120,85,68,0.35)\"\n  }\n}\n"}, {"name": "decided-won.json", "content": "{\n  \"_name\": \"DecidedRow — won (green badge, amount, embeds DeleteQuoteButton icon)\",\n  \"section\": \"DecidedRow\",\n  \"lang\": \"en\",\n  \"q\": {\n    \"id\": \"Q-1099\",\n    \"title\": \"Building C re-roof\",\n    \"client\": \"Maple Grove Apartments\",\n    \"initials\": \"MG\",\n    \"stage\": \"won\",\n    \"value\": 1420000,\n    \"daysIn\": 0,\n    \"opens\": 4,\n    \"sentDays\": 6,\n    \"decidedDays\": 1,\n    \"band\": [\"#5FA34F\", \"#3F7A33\"],\n    \"shadow\": \"rgba(81,152,67,0.35)\"\n  }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — QuotesHero stale (localized headline + sub)\",\n  \"section\": \"QuotesHero\",\n  \"lang\": \"es\",\n  \"openCount\": 9,\n  \"openTotal\": 3168000,\n  \"staleCount\": 2,\n  \"clientCount\": 8\n}\n"}, {"name": "hero-empty.json", "content": "{\n  \"_name\": \"QuotesHero — honest-empty (\\\"Nothing in the pipeline yet.\\\")\",\n  \"section\": \"QuotesHero\",\n  \"lang\": \"en\",\n  \"openCount\": 0,\n  \"openTotal\": 0,\n  \"staleCount\": 0,\n  \"clientCount\": 0\n}\n"}, {"name": "hero-stale.json", "content": "{\n  \"_name\": \"QuotesHero — stale (money headline + \\\"needs a nudge\\\")\",\n  \"section\": \"QuotesHero\",\n  \"lang\": \"en\",\n  \"openCount\": 9,\n  \"openTotal\": 3168000,\n  \"staleCount\": 2,\n  \"clientCount\": 8\n}\n"}, {"name": "hero-warm.json", "content": "{\n  \"_name\": \"QuotesHero — all warm (no stale)\",\n  \"section\": \"QuotesHero\",\n  \"lang\": \"en\",\n  \"openCount\": 5,\n  \"openTotal\": 1899000,\n  \"staleCount\": 0,\n  \"clientCount\": 5\n}\n"}, {"name": "kpis-confident.json", "content": "{\n  \"_name\": \"QuotesKpis — decided≥5 → real win-rate %\",\n  \"section\": \"QuotesKpis\",\n  \"lang\": \"en\",\n  \"outValue\": 2983000,\n  \"outCount\": 7,\n  \"draftCount\": 2,\n  \"decidedCount\": 6,\n  \"wonCount\": 4,\n  \"lostCount\": 2,\n  \"winRate\": 67\n}\n"}, {"name": "kpis-not-enough.json", "content": "{\n  \"_name\": \"QuotesKpis — decided<5 → \\\"—\\\" + \\\"need more\\\"\",\n  \"section\": \"QuotesKpis\",\n  \"lang\": \"en\",\n  \"outValue\": 1240000,\n  \"outCount\": 4,\n  \"draftCount\": 2,\n  \"decidedCount\": 2,\n  \"wonCount\": 2,\n  \"lostCount\": 0,\n  \"winRate\": 100\n}\n"}, {"name": "side-rate-confident.json", "content": "{\n  \"_name\": \"QSideRate — gauge filled (decided≥5)\",\n  \"section\": \"QSideRate\",\n  \"lang\": \"en\",\n  \"won\": 6,\n  \"lost\": 2\n}\n"}, {"name": "side-rate-low.json", "content": "{\n  \"_name\": \"QSideRate — \\\"—\\\" (decided<5, \\\"need N more\\\")\",\n  \"section\": \"QSideRate\",\n  \"lang\": \"en\",\n  \"won\": 2,\n  \"lost\": 1\n}\n"}], "css": "/* ============================================================\n   QuotesSections — extracted from static/quotes.css.\n   Covers the 6 SSR sections: QuotesHero (.qph), QuotesKpis (.qkpi),\n   DecidedRow (.qdone), QSideBig (.qside/.qbig/.qbar), QSideRate (.qrate),\n   QSideTip (inline-styled, no class). The .qdone__del button styling is\n   owned by DeleteQuoteButton (variant=\"icon\") but lives here since the\n   selector is decided-row scoped.\n   Tokens: --brand-teal, --brand-pink, --brand-green, --green-700,\n   --fg-muted, --mint-200, --mint-300, --radius-lg, --radius-xl,\n   --radius-pill, --font-heading, --font-body, --dur-fast, --ease-out.\n   ============================================================ */\n\n/* --- Hero (.qph) --- */\n.qph {\n  display: flex;\n  align-items: flex-end;\n  justify-content: space-between;\n  gap: 24px;\n  padding: 18px 0 22px;\n  border-bottom: 1px solid rgba(26, 83, 92, 0.08);\n  margin-bottom: 22px;\n}\n.qph__eyebrow {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 700;\n  letter-spacing: 0.1em;\n  text-transform: uppercase;\n  color: var(--brand-pink);\n  margin-bottom: 6px;\n}\n.qph__eyebrow-dot {\n  width: 6px;\n  height: 6px;\n  border-radius: 999px;\n  background: var(--brand-pink);\n}\n.qph__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: clamp(28px, 4vw, 44px);\n  color: var(--brand-teal);\n  line-height: 1.05;\n  letter-spacing: -0.02em;\n  margin: 0 0 12px;\n  text-wrap: balance;\n}\n.qph__title em {\n  color: var(--brand-pink);\n  font-style: normal;\n}\n.qph__sub {\n  font-family: var(--font-body);\n  font-size: 14.5px;\n  color: var(--fg-muted);\n  line-height: 1.5;\n  max-width: 520px;\n  text-wrap: pretty;\n}\n.qph__sub strong {\n  color: var(--brand-teal);\n  font-weight: 700;\n}\n.qph__cta {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  padding: 13px 20px;\n  background: var(--brand-pink);\n  color: #fff;\n  border: none;\n  border-radius: var(--radius-pill);\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13px;\n  letter-spacing: 0.01em;\n  text-decoration: none;\n  cursor: pointer;\n  box-shadow: 0 8px 18px -8px rgba(255, 107, 107, 0.55);\n  transition: transform var(--dur-fast) var(--ease-out);\n  flex-shrink: 0;\n}\n.qph__cta:hover { transform: translateY(-1px); }\n\n/* --- KPI strip (.qkpi) --- */\n.qkpi {\n  display: grid;\n  grid-template-columns: repeat(4, 1fr);\n  gap: 10px;\n  margin-bottom: 22px;\n}\n.qkpi__cell {\n  background: #fff;\n  border-radius: var(--radius-lg);\n  padding: 14px 16px;\n  border: 1px solid rgba(26, 83, 92, 0.07);\n  position: relative;\n  overflow: hidden;\n}\n.qkpi__cell--accent {\n  background: linear-gradient(135deg, #fff1ed 0%, #ffe4da 100%);\n  border-color: rgba(255, 107, 107, 0.2);\n}\n.qkpi__lbl {\n  font-family: var(--font-heading);\n  font-size: 10px;\n  font-weight: 700;\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--fg-muted);\n  margin-bottom: 4px;\n}\n.qkpi__val {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 22px;\n  color: var(--brand-teal);\n  letter-spacing: -0.02em;\n  line-height: 1;\n}\n.qkpi__cell--accent .qkpi__val { color: var(--brand-pink); }\n.qkpi__sub {\n  font-size: 11.5px;\n  color: var(--fg-muted);\n  margin-top: 4px;\n}\n\n/* --- Decided rows (.qdone) --- */\n.qdone {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: 10px;\n}\n@media (max-width: 1100px) {\n  .qdone { grid-template-columns: 1fr; }\n}\n.qdone__row {\n  /* minmax(0,1fr) lets the title cell shrink/ellipsize on phone widths. */\n  display: grid;\n  grid-template-columns: auto minmax(0, 1fr) auto auto auto;\n  gap: 14px;\n  align-items: center;\n  padding: 14px 18px;\n  background: #fff;\n  border-radius: var(--radius-lg);\n  border: 1px solid rgba(26, 83, 92, 0.07);\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    transform var(--dur-fast) var(--ease-out);\n}\n.qdone__row:hover {\n  border-color: var(--mint-300);\n  transform: translateX(2px);\n}\n@media (max-width: 560px) {\n  .qdone__row { gap: 8px; padding: 12px 12px; }\n  .qdone__when { display: none; }\n}\n.qdone__badge {\n  width: 32px;\n  height: 32px;\n  border-radius: 10px;\n  display: grid;\n  place-items: center;\n  color: #fff;\n  flex-shrink: 0;\n}\n.qdone__badge--won { background: linear-gradient(135deg, #5fa34f, #3f7a33); }\n.qdone__badge--lost { background: linear-gradient(135deg, #b89d90, #785544); }\n.qdone__title {\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 13.5px;\n  color: var(--brand-teal);\n  line-height: 1.2;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qdone__client {\n  font-size: 11px;\n  color: var(--fg-muted);\n  margin-top: 2px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qdone__amt {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n}\n.qdone__amt--lost {\n  color: var(--fg-muted);\n  text-decoration: line-through;\n  text-decoration-thickness: 1px;\n  opacity: 0.7;\n}\n.qdone__when {\n  font-size: 11px;\n  color: var(--fg-muted);\n  font-family: var(--font-body);\n  text-align: right;\n}\n/* DeleteQuoteButton variant=\"icon\" target (rendered inside DecidedRow) */\n.qdone__del {\n  width: 24px;\n  height: 24px;\n  border-radius: 999px;\n  border: 1px solid rgba(26, 83, 92, 0.12);\n  background: #fff;\n  color: var(--fg-muted);\n  font-size: 14px;\n  line-height: 1;\n  cursor: pointer;\n  display: grid;\n  place-items: center;\n  transition:\n    border-color var(--dur-fast) var(--ease-out),\n    color var(--dur-fast) var(--ease-out),\n    background var(--dur-fast) var(--ease-out);\n}\n.qdone__del:hover {\n  border-color: var(--brand-pink);\n  color: var(--brand-pink);\n  background: rgba(255, 107, 107, 0.06);\n}\n.qdone__del:disabled { opacity: 0.5; cursor: wait; }\n\n/* --- Sidebar widgets (.qside) --- */\n.qside {\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n  position: sticky;\n  top: 16px;\n}\n.qside__card {\n  background: #fff;\n  border-radius: var(--radius-xl);\n  padding: 18px;\n  border: 1px solid rgba(26, 83, 92, 0.07);\n}\n.qside__head {\n  display: flex;\n  align-items: baseline;\n  justify-content: space-between;\n  margin-bottom: 14px;\n}\n.qside__title {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 14px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n}\n.qside__sub { font-size: 11px; color: var(--fg-muted); }\n\n/* QSideBig top-quotes list */\n.qbig { display: flex; flex-direction: column; gap: 10px; }\n.qbig__row {\n  display: grid;\n  grid-template-columns: auto 1fr auto;\n  gap: 10px;\n  align-items: center;\n}\n.qbig__rank {\n  font-family: var(--font-heading);\n  font-size: 11px;\n  font-weight: 800;\n  color: var(--brand-pink);\n  width: 18px;\n  letter-spacing: 0.05em;\n}\n.qbig__name {\n  font-family: var(--font-heading);\n  font-size: 13px;\n  font-weight: 700;\n  color: var(--brand-teal);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qbig__sub {\n  font-size: 11px;\n  color: var(--fg-muted);\n  margin-top: 1px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.qbig__amt {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 13.5px;\n  color: var(--brand-teal);\n  letter-spacing: -0.01em;\n}\n.qbar {\n  height: 6px;\n  border-radius: 999px;\n  background: var(--mint-200);\n  overflow: hidden;\n  margin-top: 8px;\n}\n.qbar__fill {\n  height: 100%;\n  background: linear-gradient(90deg, var(--brand-green), var(--green-700));\n  border-radius: 999px;\n  transition: width 1.2s var(--ease-out);\n}\n\n/* QSideRate gauge */\n.qrate {\n  display: grid;\n  grid-template-columns: 110px 1fr;\n  gap: 16px;\n  align-items: center;\n}\n.qrate__svg { width: 110px; height: 70px; }\n.qrate__num {\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 24px;\n  color: var(--brand-teal);\n  letter-spacing: -0.02em;\n  line-height: 1;\n}\n.qrate__num-pct {\n  font-size: 14px;\n  color: var(--fg-muted);\n  margin-left: 2px;\n}\n.qrate__lbl {\n  font-size: 11.5px;\n  color: var(--fg-muted);\n  margin-top: 4px;\n  line-height: 1.4;\n}\n\n/* QSideTip has no class — fully inline-styled in QuotesSections.tsx:\n   background:linear-gradient(135deg,#1A535C,#0F3A40); color:#fff; border:none. */\n\n/* Responsive — these live in static/quotes.css and affect the sections. */\n@media (max-width: 1200px) {\n  .qph { flex-direction: column; align-items: flex-start; }\n  .qkpi { grid-template-columns: repeat(2, 1fr); }\n  .qside { position: static; }\n}\n@media (max-width: 768px) {\n  .qkpi { grid-template-columns: 1fr; }\n}\n", "cssName": "quotes-sections.css", "source": "/**\n * Server-rendered sections for /quotes. Ported from the prototype's\n * QuotesHero, QuotesKpis, DecidedRow, QSideBig, QSideRate, QSideTip.\n * The interactive Track collapse + QuoteCard flip live in islands/.\n */\nimport { I, ICN } from \"../lib/dash-icons.tsx\";\nimport { fmtMoney } from \"../lib/format.ts\";\nimport { type Lang, tFor } from \"../lib/i18n.ts\";\nimport { type Quote } from \"../lib/quotes-seed.ts\";\nimport DeleteQuoteButton from \"../islands/DeleteQuoteButton.tsx\";\n\n/** Explicit-language plural picker for SSR components (mirrors tn() but\n *  honors the resolved `lang` prop instead of the reactive langSignal). */\nfunction tnFor(lang: Lang, key: string, n: number, vars?: Record<string, string | number>): string {\n  return tFor(lang, `${key}.${n === 1 ? \"one\" : \"other\"}`, { n, ...vars });\n}\n\ninterface HeroProps {\n  openCount: number;\n  openTotal: number;\n  staleCount: number;\n  clientCount: number;\n  lang?: Lang;\n}\n\nexport function QuotesHero(\n  { openCount, openTotal, staleCount, clientCount, lang = \"en\" }: HeroProps,\n) {\n  const empty = openCount === 0;\n  const allWarm = !empty && staleCount === 0;\n  const openQuotes = tnFor(lang, \"quotesHero.openQuotes\", openCount);\n  const clients = tnFor(lang, \"quotesHero.clients\", clientCount);\n  return (\n    <div class=\"qph\">\n      <div>\n        <div class=\"qph__eyebrow\">\n          <span class=\"qph__eyebrow-dot\" />\n          {tFor(lang, \"quotesHero.eyebrow\")}\n        </div>\n        {empty\n          ? (\n            <>\n              <h1 class=\"qph__title\">\n                <em>{tFor(lang, \"quotesHero.emptyTitleLine1\")}</em>\n                <br />\n                {tFor(lang, \"quotesHero.emptyTitleLine2\")}\n              </h1>\n              <p class=\"qph__sub\">\n                {tFor(lang, \"quotesHero.emptySub\")}\n              </p>\n            </>\n          )\n          : allWarm\n          ? (\n            <>\n              <h1 class=\"qph__title\">\n                <em>{fmtMoney(openTotal)}</em>{\" \"}\n                {tFor(lang, \"quotesHero.warmTitle\")}\n              </h1>\n              <p class=\"qph__sub\">\n                {tFor(lang, \"quotesHero.warmSub\", { openQuotes, clients })}\n              </p>\n            </>\n          )\n          : (\n            <>\n              <h1 class=\"qph__title\">\n                <em>{fmtMoney(openTotal)}</em>{\" \"}\n                {tFor(lang, \"quotesHero.staleTitle\", {\n                  quotes: tnFor(lang, \"quotesHero.staleQuotes\", staleCount),\n                  verb: tFor(\n                    lang,\n                    staleCount === 1\n                      ? \"quotesHero.needsSingular\"\n                      : \"quotesHero.needsPlural\",\n                  ),\n                })}\n              </h1>\n              <p class=\"qph__sub\">\n                {tFor(lang, \"quotesHero.staleSubPre\", { openQuotes, clients })}\n                {\" \"}\n                <strong>{staleCount}</strong>{\" \"}\n                {tFor(lang, \"quotesHero.staleSubPost\")}\n              </p>\n            </>\n          )}\n      </div>\n      <button class=\"qph__cta\" type=\"button\">\n        <I d={ICN.plus} size={14} sw={2.5} /> {tFor(lang, \"quotesHero.cta\")}\n      </button>\n    </div>\n  );\n}\n\ninterface KpisProps {\n  outValue: number;\n  outCount: number;\n  draftCount: number;\n  decidedCount: number;\n  wonCount: number;\n  lostCount: number;\n  winRate: number;\n  lang?: Lang;\n}\n\n/** Below this threshold the win-rate percentage is mathematically true but\n *  operationally meaningless (one accept becomes \"100%\"). Show only the\n *  breakdown until enough quotes have decided. */\nconst WIN_RATE_MIN_N = 5;\n\nexport function QuotesKpis(\n  {\n    outValue,\n    outCount,\n    draftCount,\n    decidedCount,\n    wonCount,\n    lostCount,\n    winRate,\n    lang = \"en\",\n  }: KpisProps,\n) {\n  const winRateConfident = decidedCount >= WIN_RATE_MIN_N;\n  return (\n    <div class=\"qkpi\">\n      <div class=\"qkpi__cell qkpi__cell--accent\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"quotesKpi.outLbl\")}</div>\n        <div class=\"qkpi__val\">{fmtMoney(outValue)}</div>\n        <div class=\"qkpi__sub\">\n          {tnFor(lang, \"quotesKpi.outWaiting\", outCount)}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"quotesKpi.draftingLbl\")}</div>\n        <div class=\"qkpi__val\">{draftCount}</div>\n        <div class=\"qkpi__sub\">{tFor(lang, \"quotesKpi.draftingSub\")}</div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"quotesKpi.decidedLbl\")}</div>\n        <div class=\"qkpi__val\">{decidedCount}</div>\n        <div class=\"qkpi__sub\">\n          {tFor(lang, \"quotesKpi.wonLost\", { won: wonCount, lost: lostCount })}\n        </div>\n      </div>\n      <div class=\"qkpi__cell\">\n        <div class=\"qkpi__lbl\">{tFor(lang, \"quotesKpi.winRateLbl\")}</div>\n        <div class=\"qkpi__val\">\n          {winRateConfident\n            ? tFor(lang, \"quotesKpi.winRateValue\", { pct: winRate })\n            : \"—\"}\n        </div>\n        <div class=\"qkpi__sub\">\n          {decidedCount === 0\n            ? tFor(lang, \"quotesKpi.notEnough\")\n            : winRateConfident\n            ? tFor(lang, \"quotesKpi.decidedN\", { n: decidedCount })\n            : tFor(lang, \"quotesKpi.needMore\", {\n              won: wonCount,\n              lost: lostCount,\n              n: WIN_RATE_MIN_N - decidedCount,\n            })}\n        </div>\n      </div>\n    </div>\n  );\n}\n\nexport function DecidedRow({ q, lang = \"en\" }: { q: Quote; lang?: Lang }) {\n  const decidedDays = q.decidedDays ?? 0;\n  const when = decidedDays === 1\n    ? tFor(lang, \"quotesDecided.yesterday\")\n    : tFor(lang, \"quotesDecided.daysAgo\", { n: decidedDays });\n  return (\n    <div class=\"qdone__row\">\n      <div class={`qdone__badge qdone__badge--${q.stage}`}>\n        <I d={q.stage === \"won\" ? ICN.check : ICN.x} size={16} sw={2.5} />\n      </div>\n      <div>\n        <div class=\"qdone__title\">{q.title}</div>\n        <div class=\"qdone__client\">{q.client}</div>\n      </div>\n      <div class={`qdone__amt ${q.stage === \"lost\" ? \"qdone__amt--lost\" : \"\"}`}>\n        {fmtMoney(q.value)}\n      </div>\n      <div class=\"qdone__when\">{when}</div>\n      <DeleteQuoteButton id={q.id} variant=\"icon\" />\n    </div>\n  );\n}\n\ninterface QSideBigProps {\n  open: Quote[];\n  lang?: Lang;\n}\n\nexport function QSideBig({ open, lang = \"en\" }: QSideBigProps) {\n  const top4 = [...open].sort((a, b) => b.value - a.value).slice(0, 4);\n  const max = top4[0]?.value ?? 1;\n  return (\n    <div class=\"qside__card\">\n      <div class=\"qside__head\">\n        <div>\n          <div class=\"qside__title\">{tFor(lang, \"quotesSide.topTitle\")}</div>\n          <div class=\"qside__sub\">{tFor(lang, \"quotesSide.topSub\")}</div>\n        </div>\n      </div>\n      <div class=\"qbig\">\n        {top4.map((q, i) => (\n          <div key={q.id}>\n            <div class=\"qbig__row\">\n              <span class=\"qbig__rank\">{String(i + 1).padStart(2, \"0\")}</span>\n              <div style=\"min-width:0\">\n                <div class=\"qbig__name\">{q.client}</div>\n                <div class=\"qbig__sub\">{q.title}</div>\n              </div>\n              <span class=\"qbig__amt\">{fmtMoney(q.value)}</span>\n            </div>\n            <div class=\"qbar\">\n              <div\n                class=\"qbar__fill\"\n                style={`width: ${(q.value / max) * 100}%`}\n              />\n            </div>\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n}\n\ninterface QSideRateProps {\n  won: number;\n  lost: number;\n  lang?: Lang;\n}\n\nexport function QSideRate({ won, lost, lang = \"en\" }: QSideRateProps) {\n  const decided = won + lost;\n  const confident = decided >= WIN_RATE_MIN_N;\n  const pct = decided > 0 ? Math.round((won / decided) * 100) : 0;\n  const C = Math.PI * 42;\n  const dash = confident ? (pct / 100) * C : 0;\n  return (\n    <div class=\"qside__card\">\n      <div class=\"qside__head\">\n        <div>\n          <div class=\"qside__title\">{tFor(lang, \"quotesSide.rateTitle\")}</div>\n          <div class=\"qside__sub\">{tFor(lang, \"quotesSide.rateSub\")}</div>\n        </div>\n      </div>\n      <div class=\"qrate\">\n        <svg class=\"qrate__svg\" viewBox=\"0 0 110 70\">\n          <path\n            d=\"M 13 60 A 42 42 0 0 1 97 60\"\n            fill=\"none\"\n            stroke=\"var(--mint-200)\"\n            stroke-width=\"10\"\n            stroke-linecap=\"round\"\n          />\n          {confident && (\n            <path\n              d=\"M 13 60 A 42 42 0 0 1 97 60\"\n              fill=\"none\"\n              stroke=\"url(#qg)\"\n              stroke-width=\"10\"\n              stroke-linecap=\"round\"\n              stroke-dasharray={`${dash} ${C}`}\n            />\n          )}\n          <defs>\n            <linearGradient id=\"qg\" x1=\"0\" x2=\"1\">\n              <stop offset=\"0%\" stop-color=\"#5FA34F\" />\n              <stop offset=\"100%\" stop-color=\"#3F7A33\" />\n            </linearGradient>\n          </defs>\n        </svg>\n        <div>\n          {confident\n            ? (\n              <>\n                <div class=\"qrate__num\">\n                  {pct}\n                  <span class=\"qrate__num-pct\">%</span>\n                </div>\n                <div class=\"qrate__lbl\">\n                  {tFor(lang, \"quotesRate.wonLost\", { won, lost })}\n                  <br />\n                  {tFor(lang, \"quotesRate.ofDecided\", { decided })}\n                </div>\n              </>\n            )\n            : (\n              <>\n                <div\n                  class=\"qrate__num\"\n                  style=\"font-size:32px;color:var(--fg-muted)\"\n                >\n                  —\n                </div>\n                <div class=\"qrate__lbl\">\n                  {decided === 0\n                    ? <>{tFor(lang, \"quotesRate.noneDecided\")}</>\n                    : (\n                      <>\n                        {tFor(lang, \"quotesRate.wonLost\", { won, lost })}\n                        <br />\n                        {tFor(lang, \"quotesRate.needMore\", {\n                          n: WIN_RATE_MIN_N - decided,\n                        })}\n                      </>\n                    )}\n                </div>\n              </>\n            )}\n        </div>\n      </div>\n    </div>\n  );\n}\n\nexport function QSideTip(\n  { text, lang = \"en\" }: { text?: string; lang?: Lang } = {},\n) {\n  return (\n    <div\n      class=\"qside__card\"\n      style=\"background:linear-gradient(135deg,#1A535C,#0F3A40);color:#fff;border:none\"\n    >\n      <div class=\"qside__title\" style=\"color:#fff;margin-bottom:8px\">\n        {tFor(lang, \"quotesTip.title\")}\n      </div>\n      <p style=\"font:400 13.5px/1.5 var(--font-body);color:rgba(255,255,255,0.85);margin:0;text-wrap:pretty\">\n        {text ?? tFor(lang, \"quotesTip.default\")}\n      </p>\n    </div>\n  );\n}\n", "sourceName": "QuotesSections.tsx", "checklist": "# Capture checklist — QuotesSections\n\n**Theme:** light only.\n**Auth:** dev master OTP `000000`.\n\n## Route / URL\n- `http://localhost:5280/quotes` — all six sections render here:\n  - **QuotesHero** (`.qph`) — top of page.\n  - **QuotesKpis** (`.qkpi`) — 4-cell strip under the hero.\n  - **DecidedRow** (`.qdone__row`) — inside Track 03 \"Decided this month\"\n    (expand the collapsed track).\n  - **QSideBig / QSideRate / QSideTip** (`.qside`) — the right sidebar (drops\n    below the tracks under 1200px).\n\n## Viewports\n- **1280px** (full layout: sticky sidebar beside the tracks, 4-col KPIs).\n- **1100px** (DecidedRow grid 2 → 1 col).\n- **768px** (KPIs 2 → 1 col).\n- **560px** (DecidedRow tightens + hides `.qdone__when`).\n- Note the 1200px breakpoint: `.qph` stacks, `.qkpi` 4 → 2, `.qside` un-stickies.\n\n## Element(s) to crop\n- **Hero** — crop `.qph`; capture all three variants by seeding the pipeline:\n  - **stale** (money headline + bold stale count) — the seeded default.\n  - **warm** (all-warm copy) — needs a seed with 0 stale quotes.\n  - **empty** (\"Nothing in the pipeline yet.\") — seed an empty account (or the\n    isolate `hero-empty` case).\n- **KPIs** — crop `.qkpi`; capture **confident** (decided≥5 → \"67%\") and\n  **not-enough** (decided<5 → \"—\" win-rate cell). Threshold N=5.\n- **Decided rows** — crop a won row (green ✓ badge) and a lost row (coffee ✗\n  badge, struck-through amount). Include the `×` delete icon at the row end.\n- **Sidebar** — crop each `.qside__card`: QSideBig (top-4 bars), QSideRate\n  (gauge filled vs \"—\"), QSideTip (teal insight card).\n\n## Transient states to drive\n1. **qbar fill** — on (re)load the QSideBig bars animate width 0 → value/max over\n   1.2s. Film once; capture the settled state.\n2. **hero CTA** — \"New quote\" button: confirm it is a **dead stub** (click does\n   nothing — no flip, no nav). Note in the capture.\n3. **decided-row hover** — row slides `+2px` and border tints mint.\n4. **delete-confirm** — the DecidedRow `×` → native confirm dialog (see\n   delete-quote-button checklist); cancel to avoid the reload.\n5. **win-rate threshold** — drive both sides of N=5 to capture the `—` vs `%`\n   states in BOTH QuotesKpis and QSideRate.\n\n## Motion to film\n- **`.qbar__fill`** width 1.2s ease-out (slowest motion on the page) — re-triggers\n  on every parent re-render (e.g. language flip); flag the re-animate.\n- Hover micro-motions: `.qph__cta` (-1px), `.qdone__row` (+2px).\n- QSideRate SVG is **static** (no arc animation) — no filming needed.\n- Re-shoot with `prefers-reduced-motion: reduce` — no component-local guard;\n  verify the global tokens clamp stills the bar fill + hovers.\n"}]}, {"name": "settings", "pageMd": "# Page — `/settings`\n\nSource route: `routes/settings/index.tsx` → `js/index.tsx`.\n\n## Purpose\nThe contractor's account & business profile editor. Loads the composite\n`ProfileSnapshot` (user + the five business sub-entities) and lets the\ncontractor edit everything in place: account fields, business identity + logo,\nmailing address, insurance (+ cert upload), tax (TIN + W-9 upload), contract\ndefaults (read-only here), accepted payment methods (with a retype-to-confirm\nguard), the contractor's own app language (en/es), the set of outbound comms\nlanguages, and a **Danger Zone** that irreversibly wipes the account and 100%\nof its data via `GET /me/wipe` (PROJECT FACT — manual, type-\"DELETE\"-to-arm).\n\n## App-shell composition (order)\nThe route is a server page (`define.page`) that assembles the standard authed\napp-shell, then mounts the page island:\n\n```\n<Head> …title + /dashboard.css… </Head>\n<div class=\"app\">                         ← shell (SHARED)\n  <DashSidebar active=\"settings\" />       ← SHARED island\n  <main class=\"main\">\n    <DashTopbar greetingDate greetingName />  ← SHARED island\n    <div class=\"content\">\n      <SettingsPage />                    ← PAGE ISLAND (components/settings-page)\n    </div>\n  </main>\n</div>\n```\n- `DashSidebar`, `DashTopbar` → **shared-components/** (reference only).\n- `ImpersonationBanner` is mounted globally in `routes/_app.tsx` (also shared) —\n  it pins above this page during a super-admin impersonation session.\n\n## `<Head>`\n- **Title:** `tFor(lang, \"settingsRoute.docTitle\")` (i18n; en/es per\n  `user.language`).\n- **CSS:** `<link rel=\"stylesheet\" href=\"/dashboard.css\" />` — the ONLY\n  stylesheet. There is no `settings.css` in the live app; SettingsPage reuses\n  dashboard's `.hero`/`.grid`/`.panel` and inline-styles the rest. (Extracted\n  to `css/settings.css` + the component's `css/settings-page.css` here.)\n\n## SSR data\n- The route reads `ctx.state.user` (resolved by the global auth middleware via\n  `GET /me`) ONLY to compute the topbar greeting (`greetingName`,\n  `greetingDate`) and pick `lang`. **No profile data is fetched server-side** —\n  `SettingsPage` fetches `GET /profile` (ProfileSnapshot) client-side on mount.\n  So first paint is the skeleton, not server-rendered content.\n\n## Sections (top → bottom, as SettingsPage renders)\n1. **Hero** — `.hero` (1-col): business/display/user name as title + tagline.\n2. **Read-only summary grid** (`.grid`): **Account** card + **Business\n   identity** card (honest-empty → \"Nothing set yet\" when blank).\n3. **Edit details** card (`EditCard`): name, email, business name (save on\n   blur), logo upload, **app language** select (flips whole app instantly via\n   `langSignal` + persists), **comms languages** checkboxes.\n4. **Address + Insurance** grid: `AddressEditCard` (save-on-blur) +\n   `InsuranceEditCard` (fields save-on-blur + cert file upload).\n5. **Tax + Contract-defaults** grid: `TaxEditCard` (TIN save-on-blur + W-9\n   upload; raw TIN never returns, only masked) + read-only **Contract\n   defaults** card.\n6. **Payment methods** card (`PaymentsEditCard`): per-method enable toggle +\n   handle entry with retype-to-confirm; one explicit Save button.\n7. **Danger Zone** card (`DangerZoneCard`): type-\"DELETE\" to arm → wipe.\n\n## Build order\n1. Tokens + `/dashboard.css` shell (shared) in place; app-shell mounting.\n2. `css/settings.css` (hero/grid/panel) loaded via the route's single\n   dashboard.css link.\n3. Build the `settings-page` island (see its `.md`): fetch → skeleton →\n   loaded; then each sub-card (read-only Card, EditPanel shell, then the five\n   editors), then PaymentsEditCard, then DangerZoneCard last.\n4. Wire the language signal + per-card `onSaved` lift.\n\n## Local components\n- `components/settings-page/` ← `islands/SettingsPage.tsx` (1314 lines; the\n  whole page is one island; sub-cards are in-file function components, not\n  separate files — documented as one component with macro structure).\n", "components": [{"name": "settings-page", "component": "SettingsPage", "spec": "# SettingsPage\n\nThe whole `/settings` content area as a single island. Fetches the composite\n`ProfileSnapshot` on mount and edits it in place across seven cards. Source:\n`islands/SettingsPage.tsx` (1314 lines). **Pragmatic spec:** macro structure +\nall sections + states, not every line.\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/SettingsPage.tsx`).\n- **Interaction tier:** **whole-page island, client-only.** No form POST/PRG, no\n  Fresh Partial. All persistence is client `fetch` → JSON API → optimistic-ish\n  local state update.\n- **Server actions + feedback** (each card owns its own busy/error/saved):\n  - Account: `profileClient.updateUser(patch)` → `PUT /me`.\n  - Identity (business name, logo, comms languages):\n    `profileClient.updateIdentity(patch)` → `PUT /profile/identity`.\n  - Address: `updateAddress` → `PUT /profile/address`.\n  - Insurance: `updateInsurance` → `PUT /profile/insurance`.\n  - Tax: `updateTax` → `PUT /profile/tax`.\n  - Payments: `updateIdentity({ acceptedPaymentMethods })` (whole object,\n    shallow-merged so untouched methods survive).\n  - Logo / cert / W-9 uploads: `filesClient.uploadBlob(file)` → `POST /files`\n    → returns `FileRecord`, then the file id is saved via the relevant\n    `update*`. A 413 with `body.code === \"file_too_large\"` surfaces the\n    \"too large\" copy; anything else → generic per-card message (raw error\n    `console.error`'d, never shown).\n  - **Danger Zone:** `profileClient.wipeAccount()` → **`GET /me/wipe`**\n    (irreversible; nukes user + 100% of data — PROJECT FACT), then\n    `globalThis.location.href = \"/login\"`.\n  - **Flash/feedback pattern:** `EditPanel` shows \"Saving…\" while busy, then\n    \"Saved\" (green) on success; errors render a red line under the card. Most\n    text fields **save on blur** when the value changed; Payments + Danger Zone\n    use explicit buttons.\n- **Client state owned:**\n  - Top island: `s = { loading, error, profile: ProfileSnapshot|null }`.\n  - Per-card local `useState` for each field + `busy` (string-tagged in\n    EditCard/Insurance/Tax: `\"user\"|\"identity\"|\"logo\"|\"fields\"|\"file\"|\"save\"`)\n    + `err` + `saved`. Payments owns a `Record<key,{enabled,value,confirm}>`.\n    Danger Zone owns `confirm` (armed when `=== \"DELETE\"`).\n  - `onSaved(partial)` lifts each saved sub-entity back into `s.profile` so\n    neighbouring cards stay in sync **without a refetch** (no\n    `location.reload()` for saves — good).\n- **`location.href` flags (NOT reload, but full navigation):**\n  - Danger Zone → `/login` (justified: all sessions are dropped server-side).\n  - There is **no other reload**; saves mutate local state only.\n- **Language signal:** the app-language `<select>` calls `setLang(v)`\n  (`lib/i18n.ts` `langSignal`) which flips the **entire app instantly** (signal)\n  AND persists via `saveUser({language})`. `tr()` re-reads `langSignal.value` so\n  the whole screen re-renders in the new language with no reload.\n- **Data source per region + honest-empty:** single `GET /profile` →\n  `ProfileSnapshot`. Read-only `Card`s filter out empty fields and show\n  `settings.nothingSet` (\"Nothing set yet\") when all are blank. The masked TIN\n  (`tax.tinMasked`) is the only thing shown for tax; the raw TIN is never\n  returned.\n- **Liveness:** request-response only. One fetch on mount; no polling/socket.\n- **Data-shape hazards:**\n  - `coverageCents` is CENTS — the UI divides by 100 to show dollars and\n    multiplies back on save (`Math.round(Number*100)`). A non-numeric entry\n    guards with `!Number.isNaN`.\n  - `commsLanguages?: string[]` falls back to legacy single `commsLanguage`, and\n    the toggle **refuses to let the set hit zero** (always ≥1 enabled).\n  - `acceptedPaymentMethods` is `Partial<Record<PaymentMethodKey, …>>`; the card\n    spreads the existing object so methods it doesn't render (`other`) survive.\n  - Money-routing handles (Venmo/Zelle/CashApp/PayPal) are **required + retype-\n    confirmed** when enabled; check `mailTo` / ACH+card `instructions` are\n    optional and skip the confirm field.\n\n## 2. Anatomy\n```\n<>                                          ← island root (loading→skeleton; error→.dashpage-error)\n  <section class=\"hero\" style=\"grid-template-columns:1fr\">\n    <div class=\"hero__copy\">\n      <h1 class=\"hero__title\"> businessName ?? displayName ?? user.name ?? \"Your business\" </h1>\n      <p class=\"hero__sub\"> settings.heroSub </p>\n  <div class=\"grid\">                        ← read-only summary\n    <Card \"Account\">  name · phone(fmtPhone) · email · language </Card>\n    <Card \"Business identity\">  businessName · legalName · license </Card>\n  <EditCard>                                ← .panel (EditPanel shell)\n    name | email | businessName  (save-on-blur)\n    [Upload/Replace logo]  + logo hint\n    app-language <select>  (setLang + persist)\n    comms-languages [en][es] toggle chips (≥1)\n  <div class=\"grid\">\n    <AddressEditCard>  street | city | state(2) | zip | country  (save-on-blur)\n    <InsuranceEditCard>  provider | policy | coverage($) | expires(date) + [Upload cert]\n  <div class=\"grid\">\n    <TaxEditCard>  TIN(masked-on-file) save-on-blur + [Upload W-9]\n    <Card \"Contract defaults\">  net terms · deposit% · warranty days\n  <PaymentsEditCard>                         ← per-method toggle rows + retype-confirm + [Save]\n  <DangerZoneCard>                           ← .panel red; type \"DELETE\" → [Wipe]\n</>\n```\n- **Shared building blocks:** `Skeletons` (`ShimmerStyle`, `PageHeaderSkeleton`,\n  `CardGridSkeleton rows={2}`) for the loading state — see shared-components.\n- **Class hooks present but UNSTYLED (FLAG):** `.settings-edit__input` on the\n  name/email/business inputs has no CSS rule anywhere; the `inputStyle` inline\n  string does all the styling. `.dashpage-error` (error branch) is likewise\n  undefined in any stylesheet.\n\n## 3. Props\nNone. The island fetches its own data.\n\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| — | — | — | — | — |\n\nSub-card props are internal: each editor takes `{ snapshot: ProfileSnapshot,\nonSaved: (partial) => void }`; `DangerZoneCard` takes `{ snapshot }` (unused).\nThese are not isolate controls (the island has no props) — for isolation, drive\nvia `_signals` (the `s` state) — see isolate cases.\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| loading | mount, `GET /profile` in flight → Shimmer + PageHeader + CardGrid skeletons | `cases/loading/loading.json` |\n| error | fetch failed / no profile → `.dashpage-error` message | `cases/error/error.json` |\n| loaded | full profile rendered, all cards idle | `cases/loaded/loaded.json` |\n| danger-armed | \"DELETE\" typed → wipe button armed (red, enabled) | `cases/danger-armed/danger-armed.json` |\n| payments-mismatch | enabled handle ≠ confirm → red border + \"doesn't match\" | `cases/payments-mismatch/payments-mismatch.json` |\n\n(Other transient sub-states — per-card \"Saving…\"/\"Saved\", upload \"Uploading…\",\nfile-too-large error — are documented in Events/§5 and captured via the\ncapture-checklist, not separate isolate cases, since they're momentary.)\n\n## 5. Events\n- **Save-on-blur (text fields):** `ev.expect(e => e.type === \"blur\" &&\n  e.source.startsWith(\"input\") && /* value !== was */)` → the card's\n  `save*(patch)` → busy → \"Saved\"/error. Fires only when the trimmed value\n  changed.\n- **App-language select:** `ev.expect(e => e.type === \"change\" &&\n  e.source === \"select.app-language\")` → `setLang(v)` (instant whole-app\n  re-render) + `saveUser({language})`.\n- **Comms-language toggle:** `change` on a checkbox → recompute set (never\n  empty) → `saveIdentity({commsLanguages, commsLanguage})`.\n- **Logo / cert / W-9 buttons:** `click` → hidden `<input type=file>.click()`;\n  the input's `change` → `uploadBlob` → save file id.\n- **Payments Save:** `click` on the primary button → validate every enabled\n  required handle has a matching confirm → `updateIdentity`.\n- **Danger Zone:** `input` on the confirm field arms when `=== \"DELETE\"`;\n  `click` on the (armed) wipe button → `wipeAccount()` → redirect `/login`.\n\n## 6. Motion\n- The island itself has **no keyframes**. Motion comes entirely from the\n  **Skeletons** shimmer during load (see shared-components/skeletons — that\n  `@keyframes shimmer` is the only animation here) and the panel/hero static\n  shadows.\n- **Jank watch:** none specific to this island; the hero's layered radial\n  gradients are static.\n- **Reduced-motion:** the global token CSS clamps animation/transition durations\n  to `0.01ms !important`; the shimmer respects it via the shared Skeletons. No\n  local reduced-motion block needed.\n\n## 7. Responsive\n- No `@media` of its own. The borrowed `.hero`/`.grid` collapse to one column at\n  **`max-width: 640px`** (dashboard.css), so the read-only pairs and\n  Address/Insurance/Tax pairs stack on phones.\n- The editable field grids are intrinsically responsive:\n  `grid-template-columns: repeat(auto-fit, minmax(180px, 1fr))` — they reflow\n  without a media query.\n- The shell adds keyboard-clearing bottom padding (`--kb-inset`) on phones so the\n  Danger Zone field scrolls above the soft keyboard (shell-owned).\n\n## 8. A11y\n- Every input is wrapped in a `<label>` with an uppercase caption `<span>`; the\n  three free-text identity inputs also carry explicit `aria-label`\n  (`settings.aria.editName/editEmail/editBusinessName`). Hidden file inputs have\n  `aria-label`s (`aria.logoFile/insuranceCertFile/w9File`).\n- The Danger Zone confirm input has `aria-label` (`aria.wipeConfirm`); the wipe\n  button is `disabled` until armed.\n- **Gaps:** the per-card \"Saving…/Saved\" status is a plain `<span>` (no\n  `aria-live`), so SR users aren't announced save completion; the payment\n  mismatch error likewise isn't a live region.\n\n## 9. Used on\n`/settings` only — `routes/settings/index.tsx` mounts `<SettingsPage />` inside\n`<div class=\"content\">`. Single instance. (The shared `SetupChecklist` on the\ndashboard pulls from the same `/profile` endpoint but is a separate component.)\n", "fixture": "{\n  \"category\": \"settings\",\n  \"folder\": \"settings-page\",\n  \"controls\": {}\n}\n", "cases": [{"name": "danger-armed.json", "content": "{\n  \"_name\": \"Danger Zone — armed (\\\"DELETE\\\" typed, wipe button enabled & red)\",\n  \"_note\": \"Drives DangerZoneCard's local useState `confirm` to the armed value. The page must be in the loaded state for the card to render; `s` carries a minimal valid ProfileSnapshot. `confirm` lives in the sub-card, not the island root — in isolate, prime the input value to \\\"DELETE\\\" so `armed` (confirm.trim().toUpperCase() === \\\"DELETE\\\") flips true.\",\n  \"_signals\": {\n    \"s\": {\n      \"loading\": false,\n      \"error\": null,\n      \"profile\": {\n        \"user\": { \"id\": \"usr_8f2a\", \"phoneNumber\": \"+15125550143\", \"name\": \"Maria Lopez\", \"email\": \"maria@lopezlandscaping.com\", \"language\": \"en\", \"createdAt\": 1718000000000, \"updatedAt\": 1718500000000 },\n        \"identity\": { \"userId\": \"usr_8f2a\", \"businessName\": \"Lopez Landscaping\", \"createdAt\": \"2024-06-10T00:00:00.000Z\", \"updatedAt\": \"2025-01-12T00:00:00.000Z\" },\n        \"address\": null,\n        \"insurance\": null,\n        \"tax\": null,\n        \"contractDefaults\": null,\n        \"references\": [],\n        \"initials\": \"ML\"\n      }\n    },\n    \"dangerZone\": { \"confirm\": \"DELETE\", \"busy\": false, \"err\": null }\n  }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — profile fetch failed\",\n  \"_signals\": {\n    \"s\": { \"loading\": false, \"error\": \"Network request failed\", \"profile\": null }\n  }\n}\n"}, {"name": "loaded.json", "content": "{\n  \"_name\": \"Loaded — full ProfileSnapshot, all cards idle\",\n  \"_signals\": {\n    \"s\": {\n      \"loading\": false,\n      \"error\": null,\n      \"profile\": {\n        \"user\": {\n          \"id\": \"usr_8f2a\",\n          \"phoneNumber\": \"+15125550143\",\n          \"name\": \"Maria Lopez\",\n          \"email\": \"maria@lopezlandscaping.com\",\n          \"language\": \"en\",\n          \"createdAt\": 1718000000000,\n          \"updatedAt\": 1718500000000\n        },\n        \"identity\": {\n          \"userId\": \"usr_8f2a\",\n          \"businessName\": \"Lopez Landscaping\",\n          \"displayName\": \"Lopez Landscaping\",\n          \"legalName\": \"Lopez Landscaping LLC\",\n          \"businessLicense\": \"TX-LSC-44821\",\n          \"logoFileId\": \"file_3c9d12ab\",\n          \"commsLanguage\": \"en\",\n          \"commsLanguages\": [\"en\", \"es\"],\n          \"acceptedPaymentMethods\": {\n            \"venmo\": { \"enabled\": true, \"handle\": \"@lopez-landscaping\" },\n            \"zelle\": { \"enabled\": true, \"handle\": \"maria@lopezlandscaping.com\" },\n            \"check\": { \"enabled\": true, \"mailTo\": \"PO Box 144, Austin TX 78701\" },\n            \"cash\": { \"enabled\": true }\n          },\n          \"createdAt\": \"2024-06-10T00:00:00.000Z\",\n          \"updatedAt\": \"2025-01-12T00:00:00.000Z\"\n        },\n        \"address\": {\n          \"userId\": \"usr_8f2a\",\n          \"street\": \"1408 Maple Ave\",\n          \"city\": \"Austin\",\n          \"state\": \"TX\",\n          \"postal\": \"78704\",\n          \"country\": \"US\",\n          \"createdAt\": \"2024-06-10T00:00:00.000Z\",\n          \"updatedAt\": \"2025-01-12T00:00:00.000Z\"\n        },\n        \"insurance\": {\n          \"userId\": \"usr_8f2a\",\n          \"provider\": \"Hartford\",\n          \"policyNumber\": \"HF-9920043\",\n          \"coverageCents\": 100000000,\n          \"expiresAt\": \"2026-03-31\",\n          \"insuranceFileId\": \"file_ins_77a1\",\n          \"insuranceUploadedAt\": \"2025-01-04T00:00:00.000Z\",\n          \"createdAt\": \"2024-06-10T00:00:00.000Z\",\n          \"updatedAt\": \"2025-01-12T00:00:00.000Z\"\n        },\n        \"tax\": {\n          \"tinMasked\": \"**-***4821\",\n          \"w9FileId\": \"file_w9_22c0\",\n          \"w9UploadedAt\": \"2025-01-04T00:00:00.000Z\"\n        },\n        \"contractDefaults\": {\n          \"userId\": \"usr_8f2a\",\n          \"paymentTermsDays\": 15,\n          \"depositPct\": 30,\n          \"warrantyDays\": 365,\n          \"createdAt\": \"2024-06-10T00:00:00.000Z\",\n          \"updatedAt\": \"2025-01-12T00:00:00.000Z\"\n        },\n        \"references\": [],\n        \"initials\": \"ML\"\n      }\n    }\n  }\n}\n"}, {"name": "loading.json", "content": "{\n  \"_name\": \"Loading — GET /profile in flight\",\n  \"_signals\": {\n    \"s\": { \"loading\": true, \"error\": null, \"profile\": null }\n  }\n}\n"}, {"name": "payments-mismatch.json", "content": "{\n  \"_name\": \"Payments — enabled Venmo handle ≠ confirm (red border + \\\"doesn't match\\\")\",\n  \"_note\": \"Drives PaymentsEditCard's local `rows` map: Venmo enabled with value !== confirm so `mismatch(r)` is true (required field, no `optional`). The page must be loaded. The mismatch is purely local card state, not the island root — prime the venmo row's value/confirm to differ.\",\n  \"_signals\": {\n    \"s\": {\n      \"loading\": false,\n      \"error\": null,\n      \"profile\": {\n        \"user\": { \"id\": \"usr_8f2a\", \"phoneNumber\": \"+15125550143\", \"name\": \"Maria Lopez\", \"email\": \"maria@lopezlandscaping.com\", \"language\": \"en\", \"createdAt\": 1718000000000, \"updatedAt\": 1718500000000 },\n        \"identity\": { \"userId\": \"usr_8f2a\", \"businessName\": \"Lopez Landscaping\", \"acceptedPaymentMethods\": { \"venmo\": { \"enabled\": true, \"handle\": \"@lopez-landscaping\" } }, \"createdAt\": \"2024-06-10T00:00:00.000Z\", \"updatedAt\": \"2025-01-12T00:00:00.000Z\" },\n        \"address\": null, \"insurance\": null, \"tax\": null, \"contractDefaults\": null,\n        \"references\": [], \"initials\": \"ML\"\n      }\n    },\n    \"payments\": {\n      \"rows\": {\n        \"venmo\": { \"enabled\": true, \"value\": \"@lopez-landscaping\", \"confirm\": \"@lopez-landscapng\" },\n        \"cashapp\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" },\n        \"zelle\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" },\n        \"paypal\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" },\n        \"check\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" },\n        \"ach\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" },\n        \"card\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" },\n        \"cash\": { \"enabled\": false, \"value\": \"\", \"confirm\": \"\" }\n      },\n      \"busy\": false,\n      \"err\": null,\n      \"saved\": false\n    }\n  }\n}\n"}], "css": "/* ============================================================\n   SettingsPage — extracted INLINE-style literals.\n   The island uses ZERO dedicated stylesheet classes; every field, button,\n   grid and card override is an inline `style=\"…\"` string (a handful are\n   hoisted to module consts: inputStyle, labelStyle, btnSecondary, btnPrimary).\n   The only stylesheet classes it borrows are dashboard's .panel / .hero /\n   .grid (see the page-level css/settings.css). Reproduced here as ready-to-use\n   rules so the screen can be rebuilt without re-deriving the literals.\n\n   The two class hooks present in the markup — `.settings-edit__input` on the\n   name/email/business inputs — have NO CSS rule in any static/*.css (FLAG:\n   dead/placeholder class; inputStyle inline string does all the work).\n   ============================================================ */\n\n/* ---- inputStyle const (every text/email/number/date/select field) ---- */\n.settings-input {\n  width: 100%;\n  box-sizing: border-box;\n  padding: 8px 10px;\n  border: 1px solid var(--border, #d8dcd5);\n  border-radius: 8px;\n  font: inherit;\n  font-size: 13.5px;\n  background: #fff;\n  color: var(--fg);\n}\n/* mismatch state on the payments retype-confirm input (bad === true) */\n.settings-input--error { border-color: #a83b3b; }\n\n/* ---- labelStyle const (uppercase field caption) ---- */\n.settings-label {\n  display: block;\n  font-size: 11.5px;\n  font-weight: 700;\n  letter-spacing: 0.06em;\n  text-transform: uppercase;\n  color: var(--fg-muted, #6b7560);\n  margin-bottom: 4px;\n}\n\n/* ---- btnSecondary const (Upload logo / Upload cert / Upload W-9) ---- */\n.settings-btn-secondary {\n  padding: 8px 14px;\n  border-radius: 8px;\n  border: 1px solid var(--border, #d8dcd5);\n  background: #fff;\n  color: var(--fg);\n  cursor: pointer;\n  font: inherit;\n  font-weight: 600;\n}\n\n/* ---- btnPrimary const (Save payment methods) ---- */\n.settings-btn-primary {\n  padding: 9px 16px;\n  border-radius: 8px;\n  border: 0;\n  background: var(--brand-green, #519843);\n  color: #fff;\n  cursor: pointer;\n  font: inherit;\n  font-weight: 700;\n}\n\n/* ---- Panel padding override (inline on every Card/EditPanel) ---- */\n.settings-panel { /* applied via .panel + inline style=\"padding:18px 20px\" */\n  padding: 18px 20px;\n}\n.settings-panel__head { /* inline margin-bottom:12px, no bottom border */\n  margin-bottom: 12px;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n}\n\n/* ---- EditPanel status line ---- */\n.settings-status--saving { font-size: 11.5px; color: var(--fg-muted); }\n.settings-status--saved  { font-size: 11.5px; color: var(--brand-green, #519843); }\n.settings-error          { margin-top: 10px; color: #a83b3b; font-size: 12.5px; }\n\n/* ---- Read-only Card key/value grid ---- */\n.settings-kv {\n  display: grid;\n  grid-template-columns: max-content 1fr;\n  gap: 8px 18px;\n  font-size: 13.5px;\n}\n.settings-kv__k { color: var(--fg-muted, #6b7560); }\n.settings-kv__v { color: var(--fg); }\n.settings-kv__empty { color: var(--fg-muted, #6b7560); font-size: 13px; }\n\n/* ---- Editable field grid (auto-fit columns) ---- */\n.settings-field-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));\n  gap: 14px; /* address/insurance use 12px; payments inner uses 10px */\n}\n\n/* ---- Comms-language chips & payment-method toggle cards ---- */\n.settings-toggle {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  cursor: pointer;\n  border: 1px solid var(--border, #d8dcd5);\n  border-radius: 8px;\n  padding: 8px 12px;\n  background: #fff;\n}\n.settings-toggle--on { border-color: var(--brand-green, #519843); }\n\n.settings-pay-row {\n  border: 1px solid var(--border, #d8dcd5);\n  border-radius: 10px;\n  padding: 12px 14px;\n  background: #fff;\n}\n.settings-pay-row--on { border-color: var(--brand-green, #519843); }\n\n/* ---- Danger Zone (off-token destructive red, mirrors AdminPage danger red) ---- */\n.settings-danger {\n  /* applied via .panel + inline border/background */\n  padding: 18px 20px;\n  border: 1px solid #e3b4b4;\n  background: #fdf6f6;\n}\n.settings-danger__title { color: #a83b3b; }\n.settings-danger__btn {            /* disarmed */\n  padding: 9px 16px;\n  border-radius: 8px;\n  border: 0;\n  background: #d8b4b4;             /* armed → #a83b3b */\n  color: #fff;\n  cursor: not-allowed;            /* armed && !busy → pointer */\n  font: inherit;\n  font-weight: 700;\n}\n.settings-danger__btn--armed { background: #a83b3b; cursor: pointer; }\n", "cssName": "settings-page.css", "source": "/**\n * Top-level data island for /settings. Fetches the composite profile from\n * the backend (user + business identity + address + insurance + tax +\n * contract defaults) and lets the contractor edit it in place.\n *\n * Roadmap p.2/p.3: the mailing address, insurance (incl. file upload),\n * W-9 upload, and accepted payment methods are all editable here. Each\n * card owns its own save state and bubbles the saved value up via onSaved\n * so neighbouring cards stay in sync without a full refetch.\n */\nimport { useEffect, useRef, useState } from \"preact/hooks\";\nimport type { ComponentChildren } from \"preact\";\nimport { profileClient, type ProfileSnapshot } from \"../clients/profile.ts\";\nimport { filesClient } from \"../clients/files.ts\";\nimport { ApiError } from \"../lib/api.ts\";\nimport { fmtPhone } from \"../lib/format.ts\";\nimport { type Lang, langSignal, setLang, tFor } from \"../lib/i18n.ts\";\nimport {\n  CardGridSkeleton,\n  PageHeaderSkeleton,\n  ShimmerStyle,\n} from \"../components/Skeletons.tsx\";\n\ninterface State {\n  loading: boolean;\n  error: string | null;\n  profile: ProfileSnapshot | null;\n}\n\nconst INITIAL: State = { loading: true, error: null, profile: null };\n\nconst inputStyle =\n  \"width:100%;box-sizing:border-box;padding:8px 10px;border:1px solid var(--border, #d8dcd5);border-radius:8px;font:inherit;font-size:13.5px;background:#fff;color:var(--fg)\";\nconst labelStyle =\n  \"display:block;font-size:11.5px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--fg-muted, #6b7560);margin-bottom:4px\";\nconst btnSecondary =\n  \"padding:8px 14px;border-radius:8px;border:1px solid var(--border, #d8dcd5);background:#fff;color:var(--fg);cursor:pointer;font:inherit;font-weight:600\";\nconst btnPrimary =\n  \"padding:9px 16px;border-radius:8px;border:0;background:var(--brand-green,#519843);color:#fff;cursor:pointer;font:inherit;font-weight:700\";\n\n/**\n * Map a file-upload failure to a friendly, localized message. The raw error\n * is always logged for debugging, so users never see \"POST /files failed:\n * 500\". A structured \"file_too_large\" signal from the backend (413) surfaces\n * the specific too-large copy; everything else falls back to the generic\n * per-card message.\n */\nfunction uploadErrorMessage(ex: unknown, tooLarge: string, fallback: string): string {\n  console.error(\"file upload failed\", ex);\n  if (ex instanceof ApiError) {\n    const body = ex.body as { code?: unknown } | null;\n    if (body && typeof body === \"object\" && body.code === \"file_too_large\") {\n      return tooLarge;\n    }\n  }\n  return fallback;\n}\n\n/** Roadmap p.13: neutral Latin-American Spanish for the Settings UI, keyed\n *  off the contractor's `user.language`. One dictionary so the whole screen\n *  flips together (no mixed-language state). */\nfunction tr(es: boolean) {\n  const lang: Lang = es ? \"es\" : \"en\";\n  return {\n    heroSub: tFor(lang, \"settings.heroSub\"),\n    saving: tFor(lang, \"settings.saving\"),\n    saved: tFor(lang, \"settings.saved\"),\n    saveFailed: tFor(lang, \"settings.saveFailed\"),\n    logoUploadFailed: tFor(lang, \"settings.logoUploadFailed\"),\n    uploadFailed: tFor(lang, \"settings.uploadFailed\"),\n    uploadTooLarge: tFor(lang, \"settings.uploadTooLarge\"),\n    nothingSet: tFor(lang, \"settings.nothingSet\"),\n    // Card titles\n    account: tFor(lang, \"common.account\"),\n    businessIdentity: tFor(lang, \"settings.businessIdentity\"),\n    mailingAddress: tFor(lang, \"settings.mailingAddress\"),\n    insurance: tFor(lang, \"settings.insurance\"),\n    tax: tFor(lang, \"settings.tax\"),\n    contractDefaults: tFor(lang, \"settings.contractDefaults\"),\n    payments: tFor(lang, \"settings.payments\"),\n    editDetails: tFor(lang, \"settings.editDetails\"),\n    // Account / identity rows + fields\n    name: tFor(lang, \"settings.name\"),\n    phone: tFor(lang, \"settings.phone\"),\n    email: tFor(lang, \"settings.email\"),\n    language: tFor(lang, \"settings.language\"),\n    spanish: tFor(lang, \"settings.spanish\"),\n    english: tFor(lang, \"settings.english\"),\n    legalName: tFor(lang, \"settings.legalName\"),\n    license: tFor(lang, \"settings.license\"),\n    businessName: tFor(lang, \"settings.businessName\"),\n    // Address\n    street: tFor(lang, \"settings.street\"),\n    city: tFor(lang, \"settings.city\"),\n    state: tFor(lang, \"settings.state\"),\n    zip: tFor(lang, \"settings.zip\"),\n    country: tFor(lang, \"settings.country\"),\n    // Logo\n    uploadLogo: tFor(lang, \"settings.uploadLogo\"),\n    replaceLogo: tFor(lang, \"settings.replaceLogo\"),\n    uploading: tFor(lang, \"settings.uploading\"),\n    logoSet: tFor(lang, \"settings.logoSet\"),\n    logoHint: tFor(lang, \"settings.logoHint\"),\n    // Contractor's own UI language (the app + assistant they see).\n    appLang: tFor(lang, \"settings.appLang\"),\n    appLangHint: tFor(lang, \"settings.appLangHint\"),\n    // Outgoing-comms languages (what the contractor can SEND quotes in).\n    commsLang: tFor(lang, \"settings.commsLang\"),\n    commsLangHint: tFor(lang, \"settings.commsLangHint\"),\n    // Insurance\n    provider: tFor(lang, \"settings.provider\"),\n    policyNumber: tFor(lang, \"settings.policyNumber\"),\n    coverage: tFor(lang, \"settings.coverage\"),\n    expires: tFor(lang, \"settings.expires\"),\n    uploadCert: tFor(lang, \"settings.uploadCert\"),\n    replaceCert: tFor(lang, \"settings.replaceCert\"),\n    certOnFile: tFor(lang, \"settings.certOnFile\"),\n    certHint: tFor(lang, \"settings.certHint\"),\n    // Tax\n    tinLabel: tFor(lang, \"settings.tinLabel\"),\n    onFile: tFor(lang, \"settings.onFile\"),\n    enterToReplace: tFor(lang, \"settings.enterToReplace\"),\n    uploadW9: tFor(lang, \"settings.uploadW9\"),\n    replaceW9: tFor(lang, \"settings.replaceW9\"),\n    uploaded: tFor(lang, \"settings.uploaded\"),\n    w9Hint: tFor(lang, \"settings.w9Hint\"),\n    // Contract defaults\n    paymentTerms: tFor(lang, \"settings.paymentTerms\"),\n    deposit: tFor(lang, \"settings.deposit\"),\n    warranty: tFor(lang, \"settings.warranty\"),\n    days: tFor(lang, \"settings.days\"),\n    // Payments\n    paymentsIntro: tFor(lang, \"settings.paymentsIntro\"),\n    yourHandle: tFor(lang, \"settings.yourHandle\"),\n    mailingOptional: tFor(lang, \"settings.mailingOptional\"),\n    instructionsOptional: tFor(lang, \"settings.instructionsOptional\"),\n    retypeConfirm: tFor(lang, \"settings.retypeConfirm\"),\n    noMatch: tFor(lang, \"settings.noMatch\"),\n    savePayments: tFor(lang, \"settings.savePayments\"),\n    enterHandle: (m: string) =>\n      tFor(lang, \"settings.payments.enterHandle\", { m }),\n    handleMismatch: (m: string) =>\n      tFor(lang, \"settings.payments.handleMismatch\", { m }),\n    // Danger zone\n    dangerZone: tFor(lang, \"settings.dangerZone\"),\n    wipeIntro: tFor(lang, \"settings.wipeIntro\"),\n    wipeConfirmLabel: tFor(lang, \"settings.wipeConfirmLabel\"),\n    wipeButton: tFor(lang, \"settings.wipeButton\"),\n    wiping: tFor(lang, \"settings.wiping\"),\n    wipeFailed: tFor(lang, \"settings.wipeFailed\"),\n    // Aria labels\n    ariaEditName: tFor(lang, \"settings.aria.editName\"),\n    ariaEditEmail: tFor(lang, \"settings.aria.editEmail\"),\n    ariaEditBusinessName: tFor(lang, \"settings.aria.editBusinessName\"),\n    ariaUploadLogo: tFor(lang, \"settings.aria.uploadLogo\"),\n    ariaLogoFile: tFor(lang, \"settings.aria.logoFile\"),\n    ariaInsuranceCertFile: tFor(lang, \"settings.aria.insuranceCertFile\"),\n    ariaW9File: tFor(lang, \"settings.aria.w9File\"),\n    ariaWipeConfirm: tFor(lang, \"settings.aria.wipeConfirm\"),\n    // Placeholders\n    phStreet: tFor(lang, \"settings.ph.street\"),\n    phState: tFor(lang, \"settings.ph.state\"),\n    phCoverage: tFor(lang, \"settings.ph.coverage\"),\n    phTin: tFor(lang, \"settings.ph.tin\"),\n  };\n}\n\nfunction Card(\n  { title, rows, empty = tFor(\"en\", \"settings.nothingSet\") }: {\n    title: string;\n    rows: Array<[string, string | undefined | null]>;\n    empty?: string;\n  },\n) {\n  const filled = rows.filter(([, v]) =>\n    v !== undefined && v !== null && v !== \"\"\n  );\n  return (\n    <div class=\"panel\" style=\"padding:18px 20px\">\n      <div class=\"panel__head\" style=\"margin-bottom:12px\">\n        <h3 class=\"panel__title\">{title}</h3>\n      </div>\n      {filled.length === 0\n        ? (\n          <div style=\"color:var(--fg-muted, #6b7560);font-size:13px\">\n            {empty}\n          </div>\n        )\n        : (\n          <div style=\"display:grid;grid-template-columns:max-content 1fr;gap:8px 18px;font-size:13.5px\">\n            {filled.map(([k, v]) => (\n              <>\n                <div key={`k-${k}`} style=\"color:var(--fg-muted, #6b7560)\">\n                  {k}\n                </div>\n                <div key={`v-${k}`} style=\"color:var(--fg)\">{v}</div>\n              </>\n            ))}\n          </div>\n        )}\n    </div>\n  );\n}\n\n/** Small shell for an editable card: title, optional status line, error. */\nfunction EditPanel(\n  {\n    title,\n    children,\n    busy,\n    err,\n    saved,\n    saving = tFor(\"en\", \"settings.saving\"),\n    savedLabel = tFor(\"en\", \"settings.saved\"),\n  }: {\n    title: string;\n    children: ComponentChildren;\n    busy?: boolean;\n    err?: string | null;\n    saved?: boolean;\n    saving?: string;\n    savedLabel?: string;\n  },\n) {\n  return (\n    <div class=\"panel\" style=\"padding:18px 20px\">\n      <div\n        class=\"panel__head\"\n        style=\"margin-bottom:12px;display:flex;align-items:center;justify-content:space-between;gap:10px\"\n      >\n        <h3 class=\"panel__title\">{title}</h3>\n        {busy\n          ? <span style=\"font-size:11.5px;color:var(--fg-muted)\">{saving}</span>\n          : saved\n          ? (\n            <span style=\"font-size:11.5px;color:var(--brand-green,#519843)\">\n              {savedLabel}\n            </span>\n          )\n          : null}\n      </div>\n      {children}\n      {err\n        ? (\n          <div style=\"margin-top:10px;color:#a83b3b;font-size:12.5px\">\n            {err}\n          </div>\n        )\n        : null}\n    </div>\n  );\n}\n\n/** EditCard — name, email, business name, and logo. Saves on blur. */\nfunction EditCard(\n  { snapshot, onSaved }: {\n    snapshot: ProfileSnapshot;\n    onSaved: (partial: Partial<ProfileSnapshot>) => void;\n  },\n) {\n  const [name, setName] = useState(snapshot.user.name ?? \"\");\n  const [email, setEmail] = useState(snapshot.user.email ?? \"\");\n  const [biz, setBiz] = useState(snapshot.identity?.businessName ?? \"\");\n  const [busy, setBusy] = useState<string | null>(null);\n  const [err, setErr] = useState<string | null>(null);\n  const logoInputRef = useRef<HTMLInputElement | null>(null);\n  const t = tr(langSignal.value === \"es\");\n\n  async function saveUser(patch: Record<string, unknown>) {\n    setBusy(\"user\");\n    setErr(null);\n    try {\n      const u = await profileClient.updateUser(patch);\n      onSaved({ user: { ...snapshot.user, ...u } as typeof snapshot.user });\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.saveFailed);\n    } finally {\n      setBusy(null);\n    }\n  }\n  async function saveIdentity(patch: Record<string, unknown>) {\n    setBusy(\"identity\");\n    setErr(null);\n    try {\n      const id = await profileClient.updateIdentity(patch);\n      onSaved({\n        identity: {\n          ...(snapshot.identity ?? {} as never),\n          ...id,\n        } as typeof snapshot.identity,\n      });\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.saveFailed);\n    } finally {\n      setBusy(null);\n    }\n  }\n  async function onLogoPick(e: Event) {\n    const file = (e.target as HTMLInputElement).files?.[0];\n    if (!file) return;\n    setBusy(\"logo\");\n    setErr(null);\n    try {\n      const rec = await filesClient.uploadBlob(file, file.name);\n      await saveIdentity({ logoFileId: rec.id });\n    } catch (ex) {\n      setErr(uploadErrorMessage(ex, t.uploadTooLarge, t.logoUploadFailed));\n    } finally {\n      setBusy(null);\n    }\n  }\n\n  return (\n    <EditPanel\n      title={t.editDetails}\n      busy={!!busy}\n      err={err}\n      saving={t.saving}\n      savedLabel={t.saved}\n    >\n      <div style=\"display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:14px\">\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.name}</span>\n          <input\n            type=\"text\"\n            class=\"settings-edit__input\"\n            style={inputStyle}\n            value={name}\n            disabled={busy === \"user\"}\n            onInput={(e) => setName((e.target as HTMLInputElement).value)}\n            onBlur={() =>\n              name.trim() !== (snapshot.user.name ?? \"\") &&\n              saveUser({ name: name.trim() })}\n            aria-label={t.ariaEditName}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.email}</span>\n          <input\n            type=\"email\"\n            class=\"settings-edit__input\"\n            style={inputStyle}\n            value={email}\n            disabled={busy === \"user\"}\n            onInput={(e) => setEmail((e.target as HTMLInputElement).value)}\n            onBlur={() =>\n              email.trim() !== (snapshot.user.email ?? \"\") &&\n              saveUser({ email: email.trim() })}\n            aria-label={t.ariaEditEmail}\n          />\n        </label>\n        <label style=\"display:block;grid-column:1 / -1\">\n          <span style={labelStyle}>{t.businessName}</span>\n          <input\n            type=\"text\"\n            class=\"settings-edit__input\"\n            style={inputStyle}\n            value={biz}\n            disabled={busy === \"identity\"}\n            onInput={(e) => setBiz((e.target as HTMLInputElement).value)}\n            onBlur={() =>\n              biz.trim() !== (snapshot.identity?.businessName ?? \"\") &&\n              saveIdentity({ businessName: biz.trim() })}\n            aria-label={t.ariaEditBusinessName}\n          />\n        </label>\n        <div style=\"grid-column:1 / -1;display:flex;align-items:center;gap:12px\">\n          <button\n            type=\"button\"\n            class=\"btn btn-secondary\"\n            aria-label={t.ariaUploadLogo}\n            disabled={busy === \"logo\"}\n            onClick={() => logoInputRef.current?.click()}\n            style={btnSecondary}\n          >\n            {busy === \"logo\"\n              ? t.uploading\n              : (snapshot.identity?.logoFileId ? t.replaceLogo : t.uploadLogo)}\n          </button>\n          <input\n            ref={logoInputRef}\n            type=\"file\"\n            accept=\"image/*\"\n            aria-label={t.ariaLogoFile}\n            style=\"display:none\"\n            onChange={onLogoPick}\n          />\n          {snapshot.identity?.logoFileId\n            ? (\n              <span style=\"font-size:12px;color:var(--fg-muted)\">\n                {t.logoSet} · {snapshot.identity.logoFileId.slice(0, 8)}…\n              </span>\n            )\n            : (\n              <span style=\"font-size:12px;color:var(--fg-muted)\">\n                {t.logoHint}\n              </span>\n            )}\n        </div>\n        <label style=\"display:block;grid-column:1 / -1\">\n          <span style={labelStyle}>{t.appLang}</span>\n          <select\n            style={inputStyle}\n            disabled={busy === \"user\"}\n            value={langSignal.value === \"es\" ? \"es\" : \"en\"}\n            onChange={(e) => {\n              const v = (e.target as HTMLSelectElement).value as Lang;\n              setLang(v); // flip the whole app instantly\n              saveUser({ language: v }); // …and persist\n            }}\n          >\n            <option value=\"en\">{t.english}</option>\n            <option value=\"es\">{t.spanish}</option>\n          </select>\n          <span style=\"display:block;margin-top:4px;font-size:12px;color:var(--fg-muted)\">\n            {t.appLangHint}\n          </span>\n        </label>\n        <div style=\"display:block;grid-column:1 / -1\">\n          <span style={labelStyle}>{t.commsLang}</span>\n          <div style=\"display:flex;flex-wrap:wrap;gap:10px;margin-top:2px\">\n            {[{ code: \"en\", label: t.english }, {\n              code: \"es\",\n              label: t.spanish,\n            }]\n              .map((lng) => {\n                // Selected set: the explicit list, else fall back to the legacy\n                // single commsLanguage (so existing accounts show their language\n                // pre-checked), else English.\n                const selected = snapshot.identity?.commsLanguages?.length\n                  ? snapshot.identity.commsLanguages\n                  : [snapshot.identity?.commsLanguage ?? \"en\"];\n                const on = selected.includes(lng.code);\n                return (\n                  <label\n                    key={lng.code}\n                    style={`display:flex;align-items:center;gap:8px;cursor:pointer;border:1px solid ${\n                      on\n                        ? \"var(--brand-green,#519843)\"\n                        : \"var(--border,#d8dcd5)\"\n                    };border-radius:8px;padding:8px 12px;background:#fff`}\n                  >\n                    <input\n                      type=\"checkbox\"\n                      checked={on}\n                      disabled={busy === \"identity\"}\n                      onChange={(e) => {\n                        const want = (e.target as HTMLInputElement).checked;\n                        const set = new Set(selected);\n                        if (want) set.add(lng.code);\n                        else set.delete(lng.code);\n                        // Always keep at least one language enabled.\n                        if (set.size === 0) return;\n                        const list = [\"en\", \"es\"].filter((c) => set.has(c));\n                        saveIdentity({\n                          commsLanguages: list,\n                          commsLanguage: list[0],\n                        });\n                      }}\n                    />\n                    <span style=\"font-weight:700;font-size:13.5px;color:var(--fg)\">\n                      {lng.label}\n                    </span>\n                  </label>\n                );\n              })}\n          </div>\n          <span style=\"display:block;margin-top:4px;font-size:12px;color:var(--fg-muted)\">\n            {t.commsLangHint}\n          </span>\n        </div>\n      </div>\n    </EditPanel>\n  );\n}\n\n/** AddressEditCard — editable mailing address (roadmap p.2). Saves on blur. */\nfunction AddressEditCard(\n  { snapshot, onSaved }: {\n    snapshot: ProfileSnapshot;\n    onSaved: (partial: Partial<ProfileSnapshot>) => void;\n  },\n) {\n  const a = snapshot.address;\n  const [street, setStreet] = useState(a?.street ?? \"\");\n  const [city, setCity] = useState(a?.city ?? \"\");\n  const [stateV, setStateV] = useState(a?.state ?? \"\");\n  const [postal, setPostal] = useState(a?.postal ?? \"\");\n  const [country, setCountry] = useState(a?.country ?? \"US\");\n  const [busy, setBusy] = useState(false);\n  const [err, setErr] = useState<string | null>(null);\n  const [saved, setSaved] = useState(false);\n  const t = tr(langSignal.value === \"es\");\n\n  async function save(patch: Record<string, string>) {\n    setBusy(true);\n    setErr(null);\n    setSaved(false);\n    try {\n      const next = await profileClient.updateAddress(patch);\n      onSaved({ address: next });\n      setSaved(true);\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.saveFailed);\n    } finally {\n      setBusy(false);\n    }\n  }\n  const blur = (field: string, val: string, was: string) => {\n    if (val.trim() !== (was ?? \"\")) save({ [field]: val.trim() });\n  };\n\n  return (\n    <EditPanel\n      title={t.mailingAddress}\n      busy={busy}\n      err={err}\n      saved={saved}\n      saving={t.saving}\n      savedLabel={t.saved}\n    >\n      <div style=\"display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px\">\n        <label style=\"display:block;grid-column:1 / -1\">\n          <span style={labelStyle}>{t.street}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={street}\n            disabled={busy}\n            placeholder={t.phStreet}\n            onInput={(e) => setStreet((e.target as HTMLInputElement).value)}\n            onBlur={() => blur(\"street\", street, a?.street ?? \"\")}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.city}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={city}\n            disabled={busy}\n            onInput={(e) => setCity((e.target as HTMLInputElement).value)}\n            onBlur={() => blur(\"city\", city, a?.city ?? \"\")}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.state}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={stateV}\n            disabled={busy}\n            maxLength={2}\n            placeholder={t.phState}\n            onInput={(e) =>\n              setStateV((e.target as HTMLInputElement).value.toUpperCase())}\n            onBlur={() => blur(\"state\", stateV, a?.state ?? \"\")}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.zip}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={postal}\n            disabled={busy}\n            onInput={(e) => setPostal((e.target as HTMLInputElement).value)}\n            onBlur={() => blur(\"postal\", postal, a?.postal ?? \"\")}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.country}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={country}\n            disabled={busy}\n            onInput={(e) => setCountry((e.target as HTMLInputElement).value)}\n            onBlur={() => blur(\"country\", country, a?.country ?? \"\")}\n          />\n        </label>\n      </div>\n    </EditPanel>\n  );\n}\n\n/** InsuranceEditCard — provider/policy/coverage/expiry + file upload (roadmap p.2). */\nfunction InsuranceEditCard(\n  { snapshot, onSaved }: {\n    snapshot: ProfileSnapshot;\n    onSaved: (partial: Partial<ProfileSnapshot>) => void;\n  },\n) {\n  const ins = snapshot.insurance;\n  const [provider, setProvider] = useState(ins?.provider ?? \"\");\n  const [policy, setPolicy] = useState(ins?.policyNumber ?? \"\");\n  const [coverage, setCoverage] = useState(\n    ins?.coverageCents != null ? String(ins.coverageCents / 100) : \"\",\n  );\n  const [expires, setExpires] = useState(ins?.expiresAt ?? \"\");\n  const [busy, setBusy] = useState<string | null>(null);\n  const [err, setErr] = useState<string | null>(null);\n  const [saved, setSaved] = useState(false);\n  const fileRef = useRef<HTMLInputElement | null>(null);\n  const t = tr(langSignal.value === \"es\");\n\n  async function save(patch: Record<string, unknown>) {\n    setBusy(\"fields\");\n    setErr(null);\n    setSaved(false);\n    try {\n      const next = await profileClient.updateInsurance(patch);\n      onSaved({ insurance: next });\n      setSaved(true);\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.saveFailed);\n    } finally {\n      setBusy(null);\n    }\n  }\n  async function onFilePick(e: Event) {\n    const file = (e.target as HTMLInputElement).files?.[0];\n    if (!file) return;\n    setBusy(\"file\");\n    setErr(null);\n    try {\n      const rec = await filesClient.uploadBlob(file, file.name);\n      await save({ insuranceFileId: rec.id });\n    } catch (ex) {\n      setErr(uploadErrorMessage(ex, t.uploadTooLarge, t.uploadFailed));\n    } finally {\n      setBusy(null);\n    }\n  }\n\n  return (\n    <EditPanel\n      title={t.insurance}\n      busy={!!busy}\n      err={err}\n      saved={saved}\n      saving={t.saving}\n      savedLabel={t.saved}\n    >\n      <div style=\"display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px\">\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.provider}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={provider}\n            disabled={!!busy}\n            onInput={(e) => setProvider((e.target as HTMLInputElement).value)}\n            onBlur={() =>\n              provider.trim() !== (ins?.provider ?? \"\") &&\n              save({ provider: provider.trim() })}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.policyNumber}</span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={policy}\n            disabled={!!busy}\n            onInput={(e) => setPolicy((e.target as HTMLInputElement).value)}\n            onBlur={() =>\n              policy.trim() !== (ins?.policyNumber ?? \"\") &&\n              save({ policyNumber: policy.trim() })}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.coverage}</span>\n          <input\n            type=\"number\"\n            min=\"0\"\n            step=\"1000\"\n            style={inputStyle}\n            value={coverage}\n            disabled={!!busy}\n            placeholder={t.phCoverage}\n            onInput={(e) => setCoverage((e.target as HTMLInputElement).value)}\n            onBlur={() => {\n              const cents = coverage.trim() === \"\"\n                ? undefined\n                : Math.round(Number(coverage) * 100);\n              if (cents !== ins?.coverageCents && !Number.isNaN(cents)) {\n                save({\n                  coverageCents: cents,\n                });\n              }\n            }}\n          />\n        </label>\n        <label style=\"display:block\">\n          <span style={labelStyle}>{t.expires}</span>\n          <input\n            type=\"date\"\n            style={inputStyle}\n            value={expires ? expires.slice(0, 10) : \"\"}\n            disabled={!!busy}\n            onInput={(e) => setExpires((e.target as HTMLInputElement).value)}\n            onBlur={() =>\n              expires.slice(0, 10) !== (ins?.expiresAt ?? \"\").slice(0, 10) &&\n              save({ expiresAt: expires })}\n          />\n        </label>\n      </div>\n      <div style=\"margin-top:14px;display:flex;align-items:center;gap:12px\">\n        <button\n          type=\"button\"\n          disabled={busy === \"file\"}\n          onClick={() => fileRef.current?.click()}\n          style={btnSecondary}\n        >\n          {busy === \"file\"\n            ? t.uploading\n            : ins?.insuranceFileId\n            ? t.replaceCert\n            : t.uploadCert}\n        </button>\n        <input\n          ref={fileRef}\n          type=\"file\"\n          accept=\"application/pdf,image/*\"\n          aria-label={t.ariaInsuranceCertFile}\n          style=\"display:none\"\n          onChange={onFilePick}\n        />\n        <span style=\"font-size:12px;color:var(--fg-muted)\">\n          {ins?.insuranceFileId\n            ? `${t.certOnFile}${\n              ins.insuranceUploadedAt\n                ? ` · ${ins.insuranceUploadedAt.slice(0, 10)}`\n                : \"\"\n            }`\n            : t.certHint}\n        </span>\n      </div>\n    </EditPanel>\n  );\n}\n\n/** TaxEditCard — TIN entry + W-9 upload (roadmap p.2). The raw TIN never\n *  comes back from the server; we show the masked value it returns. */\nfunction TaxEditCard(\n  { snapshot, onSaved }: {\n    snapshot: ProfileSnapshot;\n    onSaved: (partial: Partial<ProfileSnapshot>) => void;\n  },\n) {\n  const tax = snapshot.tax;\n  const [tin, setTin] = useState(\"\");\n  const [busy, setBusy] = useState<string | null>(null);\n  const [err, setErr] = useState<string | null>(null);\n  const [saved, setSaved] = useState(false);\n  const fileRef = useRef<HTMLInputElement | null>(null);\n  const t = tr(langSignal.value === \"es\");\n\n  async function save(patch: Record<string, unknown>) {\n    setBusy(\"save\");\n    setErr(null);\n    setSaved(false);\n    try {\n      const next = await profileClient.updateTax(patch);\n      onSaved({ tax: next });\n      setSaved(true);\n      setTin(\"\");\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.saveFailed);\n    } finally {\n      setBusy(null);\n    }\n  }\n  async function onFilePick(e: Event) {\n    const file = (e.target as HTMLInputElement).files?.[0];\n    if (!file) return;\n    setBusy(\"file\");\n    setErr(null);\n    try {\n      const rec = await filesClient.uploadBlob(file, file.name);\n      await save({ w9FileId: rec.id });\n    } catch (ex) {\n      setErr(uploadErrorMessage(ex, t.uploadTooLarge, t.uploadFailed));\n    } finally {\n      setBusy(null);\n    }\n  }\n\n  return (\n    <EditPanel\n      title={t.tax}\n      busy={!!busy}\n      err={err}\n      saved={saved}\n      saving={t.saving}\n      savedLabel={t.saved}\n    >\n      <div style=\"display:grid;grid-template-columns:1fr;gap:12px\">\n        <label style=\"display:block\">\n          <span style={labelStyle}>\n            {t.tinLabel}{\" \"}\n            {tax?.tinMasked ? `(${t.onFile}: ${tax.tinMasked})` : \"\"}\n          </span>\n          <input\n            type=\"text\"\n            style={inputStyle}\n            value={tin}\n            disabled={!!busy}\n            placeholder={tax?.tinMasked ? t.enterToReplace : t.phTin}\n            onInput={(e) => setTin((e.target as HTMLInputElement).value)}\n            onBlur={() => tin.trim() && save({ tin: tin.trim() })}\n          />\n        </label>\n      </div>\n      <div style=\"margin-top:14px;display:flex;align-items:center;gap:12px\">\n        <button\n          type=\"button\"\n          disabled={busy === \"file\"}\n          onClick={() => fileRef.current?.click()}\n          style={btnSecondary}\n        >\n          {busy === \"file\"\n            ? t.uploading\n            : tax?.w9FileId\n            ? t.replaceW9\n            : t.uploadW9}\n        </button>\n        <input\n          ref={fileRef}\n          type=\"file\"\n          accept=\"application/pdf,image/*\"\n          aria-label={t.ariaW9File}\n          style=\"display:none\"\n          onChange={onFilePick}\n        />\n        <span style=\"font-size:12px;color:var(--fg-muted)\">\n          {tax?.w9UploadedAt\n            ? `${t.uploaded} ${tax.w9UploadedAt.slice(0, 10)}`\n            : t.w9Hint}\n        </span>\n      </div>\n    </EditPanel>\n  );\n}\n\ninterface PayRow {\n  key:\n    | \"venmo\"\n    | \"cashapp\"\n    | \"zelle\"\n    | \"paypal\"\n    | \"check\"\n    | \"cash\"\n    | \"ach\"\n    | \"card\";\n  label: string;\n  /** Backend field that stores the handle. null → no handle (cash). */\n  field: \"handle\" | \"cashtag\" | \"mailTo\" | \"instructions\" | null;\n  placeholder: string;\n  /** Money-routing handles (Venmo/Zelle/etc.) are required when the method is\n   *  enabled and get a retype-to-confirm guard — a typo sends funds to the\n   *  wrong place. A check's mailing address is optional (the contractor's\n   *  address already prints on the invoice) and needs no confirm field. */\n  optional?: boolean;\n}\n\n// `label` and `placeholder` hold i18n KEYS (resolved per-language at render).\nconst PAY_ROWS: PayRow[] = [\n  {\n    key: \"venmo\",\n    label: \"paymentMethod.venmo\",\n    field: \"handle\",\n    placeholder: \"settings.pay.venmoPh\",\n  },\n  {\n    key: \"cashapp\",\n    label: \"paymentMethod.cashApp\",\n    field: \"cashtag\",\n    placeholder: \"settings.pay.cashappPh\",\n  },\n  {\n    key: \"zelle\",\n    label: \"paymentMethod.zelle\",\n    field: \"handle\",\n    placeholder: \"settings.pay.zellePh\",\n  },\n  {\n    key: \"paypal\",\n    label: \"paymentMethod.paypal\",\n    field: \"handle\",\n    placeholder: \"settings.pay.paypalPh\",\n  },\n  {\n    key: \"check\",\n    label: \"paymentMethod.check\",\n    field: \"mailTo\",\n    placeholder: \"settings.pay.checkPh\",\n    optional: true,\n  },\n  {\n    key: \"ach\",\n    label: \"paymentMethod.ach\",\n    field: \"instructions\",\n    placeholder: \"settings.pay.achPh\",\n    optional: true,\n  },\n  {\n    key: \"card\",\n    label: \"paymentMethod.card\",\n    field: \"instructions\",\n    placeholder: \"settings.pay.cardPh\",\n    optional: true,\n  },\n  { key: \"cash\", label: \"paymentMethod.cash\", field: null, placeholder: \"\" },\n];\n\ninterface PayState {\n  enabled: boolean;\n  value: string;\n  confirm: string;\n}\n\n/** PaymentsEditCard — enable/disable each method + enter the handle with a\n *  retype-to-confirm guard (roadmap p.3). Saves the whole acceptedPaymentMethods\n *  object so the shallow-merge backend keeps untouched methods intact. */\nfunction PaymentsEditCard(\n  { snapshot, onSaved }: {\n    snapshot: ProfileSnapshot;\n    onSaved: (partial: Partial<ProfileSnapshot>) => void;\n  },\n) {\n  const apm = snapshot.identity?.acceptedPaymentMethods ?? {};\n  const init: Record<string, PayState> = {};\n  for (const r of PAY_ROWS) {\n    const cur = (apm as Record<\n      string,\n      {\n        enabled?: boolean;\n        handle?: string;\n        cashtag?: string;\n        mailTo?: string;\n        instructions?: string;\n      }\n    >)[r.key];\n    const val = r.field ? (cur?.[r.field] ?? \"\") : \"\";\n    init[r.key] = { enabled: !!cur?.enabled, value: val, confirm: val };\n  }\n  const [rows, setRows] = useState<Record<string, PayState>>(init);\n  const [busy, setBusy] = useState(false);\n  const [err, setErr] = useState<string | null>(null);\n  const [saved, setSaved] = useState(false);\n  const lang: Lang = langSignal.value === \"es\" ? \"es\" : \"en\";\n  const t = tr(langSignal.value === \"es\");\n\n  function patchRow(key: string, next: Partial<PayState>) {\n    setSaved(false);\n    setRows((cur) => ({ ...cur, [key]: { ...cur[key], ...next } }));\n  }\n\n  const mismatch = (r: PayRow) => {\n    const st = rows[r.key];\n    return st.enabled && r.field !== null && st.value.trim() !== \"\" &&\n      st.confirm.trim() !== \"\" && st.value.trim() !== st.confirm.trim();\n  };\n\n  async function save() {\n    // Validate every enabled handle-bearing method has a matching confirm.\n    for (const r of PAY_ROWS) {\n      const st = rows[r.key];\n      if (!st.enabled || r.field === null) continue;\n      // Optional fields (check's mailing address) may be left blank.\n      if (r.optional) continue;\n      const label = tFor(lang, r.label);\n      if (st.value.trim() === \"\") {\n        setErr(t.enterHandle(label));\n        return;\n      }\n      if (st.value.trim() !== st.confirm.trim()) {\n        setErr(t.handleMismatch(label));\n        return;\n      }\n    }\n    setBusy(true);\n    setErr(null);\n    setSaved(false);\n    // Preserve any methods we don't render here (e.g. `other`) via shallow spread.\n    const apmPatch: Record<string, Record<string, unknown>> = {\n      ...(apm as Record<string, Record<string, unknown>>),\n    };\n    for (const r of PAY_ROWS) {\n      const st = rows[r.key];\n      const entry: Record<string, unknown> = { enabled: st.enabled };\n      if (r.field) entry[r.field] = st.value.trim();\n      apmPatch[r.key] = entry;\n    }\n    try {\n      const id = await profileClient.updateIdentity({\n        acceptedPaymentMethods: apmPatch,\n      });\n      onSaved({\n        identity: {\n          ...(snapshot.identity ?? {} as never),\n          ...id,\n        } as typeof snapshot.identity,\n      });\n      setSaved(true);\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.saveFailed);\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  return (\n    <EditPanel\n      title={t.payments}\n      busy={busy}\n      err={err}\n      saved={saved}\n      saving={t.saving}\n      savedLabel={t.saved}\n    >\n      <p style=\"margin:0 0 14px;font-size:13px;color:var(--fg-muted, #6b7560)\">\n        {t.paymentsIntro}\n      </p>\n      <div style=\"display:flex;flex-direction:column;gap:12px\">\n        {PAY_ROWS.map((r) => {\n          const st = rows[r.key];\n          const bad = mismatch(r);\n          const label = tFor(lang, r.label);\n          const placeholder = r.placeholder ? tFor(lang, r.placeholder) : \"\";\n          return (\n            <div\n              key={r.key}\n              style={`border:1px solid ${\n                st.enabled\n                  ? \"var(--brand-green,#519843)\"\n                  : \"var(--border,#d8dcd5)\"\n              };border-radius:10px;padding:12px 14px;background:#fff`}\n            >\n              <label style=\"display:flex;align-items:center;gap:10px;cursor:pointer\">\n                <input\n                  type=\"checkbox\"\n                  checked={st.enabled}\n                  disabled={busy}\n                  onChange={(e) =>\n                    patchRow(r.key, {\n                      enabled: (e.target as HTMLInputElement).checked,\n                    })}\n                />\n                <span style=\"font-weight:700;font-size:14px;color:var(--fg)\">\n                  {label}\n                </span>\n              </label>\n              {st.enabled && r.field !== null && (\n                <div style=\"margin-top:10px;display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:10px\">\n                  <label style=\"display:block\">\n                    <span style={labelStyle}>\n                      {r.field === \"instructions\"\n                        ? t.instructionsOptional\n                        : r.optional\n                        ? t.mailingOptional\n                        : t.yourHandle}\n                    </span>\n                    <input\n                      type=\"text\"\n                      style={inputStyle}\n                      value={st.value}\n                      disabled={busy}\n                      placeholder={placeholder}\n                      onInput={(e) =>\n                        patchRow(r.key, {\n                          value: (e.target as HTMLInputElement).value,\n                        })}\n                    />\n                  </label>\n                  {!r.optional && (\n                    <label style=\"display:block\">\n                      <span style={labelStyle}>{t.retypeConfirm}</span>\n                      <input\n                        type=\"text\"\n                        style={`${inputStyle}${\n                          bad ? \";border-color:#a83b3b\" : \"\"\n                        }`}\n                        value={st.confirm}\n                        disabled={busy}\n                        placeholder={placeholder}\n                        onInput={(e) =>\n                          patchRow(r.key, {\n                            confirm: (e.target as HTMLInputElement).value,\n                          })}\n                      />\n                    </label>\n                  )}\n                  {bad && (\n                    <div style=\"grid-column:1 / -1;color:#a83b3b;font-size:12px\">\n                      {t.noMatch}\n                    </div>\n                  )}\n                </div>\n              )}\n            </div>\n          );\n        })}\n      </div>\n      <div style=\"margin-top:16px\">\n        <button type=\"button\" style={btnPrimary} disabled={busy} onClick={save}>\n          {busy ? t.saving : t.savePayments}\n        </button>\n      </div>\n    </EditPanel>\n  );\n}\n\n/** DangerZoneCard — irreversible account wipe. Type-to-confirm gate, then a\n *  single destructive button hits GET /me/wipe and bounces to the login page\n *  (the wipe drops every session, so the app is logged out anyway). */\nfunction DangerZoneCard(_props: { snapshot: ProfileSnapshot }) {\n  const [confirm, setConfirm] = useState(\"\");\n  const [busy, setBusy] = useState(false);\n  const [err, setErr] = useState<string | null>(null);\n  const t = tr(langSignal.value === \"es\");\n  const armed = confirm.trim().toUpperCase() === \"DELETE\";\n\n  async function wipe() {\n    if (!armed) return;\n    setBusy(true);\n    setErr(null);\n    try {\n      await profileClient.wipeAccount();\n      // Account + all sessions are gone — hard-redirect out of the app.\n      globalThis.location.href = \"/login\";\n    } catch (e) {\n      setErr(e instanceof Error ? e.message : t.wipeFailed);\n      setBusy(false);\n    }\n  }\n\n  return (\n    <div\n      class=\"panel\"\n      style=\"padding:18px 20px;border:1px solid #e3b4b4;background:#fdf6f6\"\n    >\n      <div class=\"panel__head\" style=\"margin-bottom:12px\">\n        <h3 class=\"panel__title\" style=\"color:#a83b3b\">{t.dangerZone}</h3>\n      </div>\n      <p style=\"margin:0 0 14px;font-size:13px;color:var(--fg-muted,#6b7560)\">\n        {t.wipeIntro}\n      </p>\n      <label style=\"display:block;max-width:320px\">\n        <span style={labelStyle}>{t.wipeConfirmLabel}</span>\n        <input\n          type=\"text\"\n          style={inputStyle}\n          value={confirm}\n          disabled={busy}\n          placeholder=\"DELETE\"\n          aria-label={t.ariaWipeConfirm}\n          onInput={(e) => setConfirm((e.target as HTMLInputElement).value)}\n        />\n      </label>\n      <div style=\"margin-top:16px\">\n        <button\n          type=\"button\"\n          disabled={!armed || busy}\n          onClick={wipe}\n          style={`padding:9px 16px;border-radius:8px;border:0;background:${\n            armed ? \"#a83b3b\" : \"#d8b4b4\"\n          };color:#fff;cursor:${\n            armed && !busy ? \"pointer\" : \"not-allowed\"\n          };font:inherit;font-weight:700`}\n        >\n          {busy ? t.wiping : t.wipeButton}\n        </button>\n      </div>\n      {err\n        ? (\n          <div style=\"margin-top:10px;color:#a83b3b;font-size:12.5px\">\n            {err}\n          </div>\n        )\n        : null}\n    </div>\n  );\n}\n\nexport default function SettingsPage() {\n  const [s, setS] = useState<State>(INITIAL);\n\n  useEffect(() => {\n    let alive = true;\n    profileClient.get()\n      .then((profile) => {\n        if (alive) setS({ loading: false, error: null, profile });\n      })\n      .catch((err: Error) => {\n        if (alive) setS({ ...INITIAL, loading: false, error: err.message });\n      });\n    return () => {\n      alive = false;\n    };\n  }, []);\n\n  if (s.loading) {\n    return (\n      <>\n        <ShimmerStyle />\n        <PageHeaderSkeleton />\n        <CardGridSkeleton rows={2} />\n      </>\n    );\n  }\n  if (s.error || !s.profile) {\n    return (\n      <div class=\"dashpage-error\">\n        {tFor(\"en\", \"settings.loadError\", {\n          error: s.error ?? tFor(\"en\", \"settings.unknownError\"),\n        })}\n      </div>\n    );\n  }\n\n  const p = s.profile;\n  const lang: Lang = p.user.language === \"es\" ? \"es\" : \"en\";\n  const t = tr(p.user.language === \"es\");\n  const onSaved = (next: Partial<ProfileSnapshot>) =>\n    setS((cur) =>\n      cur.profile ? { ...cur, profile: { ...cur.profile, ...next } } : cur\n    );\n\n  return (\n    <>\n      <section class=\"hero\" style=\"grid-template-columns:1fr\">\n        <div class=\"hero__copy\">\n          <h1 class=\"hero__title\">\n            {p.identity?.businessName ?? p.identity?.displayName ??\n              p.user.name ?? tFor(lang, \"settings.yourBusiness\")}\n          </h1>\n          <p class=\"hero__sub\">{t.heroSub}</p>\n        </div>\n      </section>\n\n      <div class=\"grid\">\n        <Card\n          title={t.account}\n          empty={t.nothingSet}\n          rows={[\n            [t.name, p.user.name],\n            [\n              t.phone,\n              p.user.phoneNumber ? fmtPhone(p.user.phoneNumber) : undefined,\n            ],\n            [t.email, p.user.email],\n            [t.language, p.user.language === \"es\" ? t.spanish : t.english],\n          ]}\n        />\n        <Card\n          title={t.businessIdentity}\n          empty={t.nothingSet}\n          rows={[\n            [\n              t.businessName,\n              p.identity?.businessName ?? p.identity?.displayName,\n            ],\n            [t.legalName, p.identity?.legalName],\n            [t.license, p.identity?.businessLicense],\n          ]}\n        />\n      </div>\n\n      <EditCard snapshot={p} onSaved={onSaved} />\n\n      <div class=\"grid\">\n        <AddressEditCard snapshot={p} onSaved={onSaved} />\n        <InsuranceEditCard snapshot={p} onSaved={onSaved} />\n      </div>\n\n      <div class=\"grid\">\n        <TaxEditCard snapshot={p} onSaved={onSaved} />\n        <Card\n          title={t.contractDefaults}\n          empty={t.nothingSet}\n          rows={[\n            [\n              t.paymentTerms,\n              p.contractDefaults?.paymentTermsDays != null\n                ? tFor(lang, \"settings.contractDefaults.net\", {\n                  n: p.contractDefaults.paymentTermsDays,\n                })\n                : null,\n            ],\n            [\n              t.deposit,\n              p.contractDefaults?.depositPct != null\n                ? `${p.contractDefaults.depositPct}%`\n                : null,\n            ],\n            [\n              t.warranty,\n              p.contractDefaults?.warrantyDays != null\n                ? `${p.contractDefaults.warrantyDays} ${t.days}`\n                : null,\n            ],\n          ]}\n        />\n      </div>\n\n      <PaymentsEditCard snapshot={p} onSaved={onSaved} />\n\n      <DangerZoneCard snapshot={p} />\n    </>\n  );\n}\n", "sourceName": "SettingsPage.tsx", "checklist": "# Capture checklist — SettingsPage\n\n**Theme:** light only (single theme; no dark mode exists).\n**Auth:** any logged-in contractor. Dev master OTP `000000` to log in at\n`http://localhost:5280` (frontend :5280, backend :4280). No super-admin needed.\n**No fabricated screenshots** — capture only against the running app.\n\n## Route / URL\n- `http://localhost:5280/settings`.\n\n## Viewports (component has no own @media; uses dashboard's 640px cutover)\n- **1280px** — `--container-product`; read-only pairs + Address/Insurance +\n  Tax/ContractDefaults render two-up via `.grid`.\n- **640px** — at/below this the `.hero` and every `.grid` collapse to ONE\n  column (dashboard.css `@media (max-width: 640px)`); shoot just above and just\n  below to capture the cutover.\n- **390px** — phone; confirm the editable field grids reflow (auto-fit\n  minmax(180px)) and the Danger Zone field clears the soft keyboard.\n\n## Element(s) to crop\n1. Hero (`.hero`, 1-col) — business-name title + sub.\n2. Read-only summary `.grid` (Account + Business identity cards), incl. an\n   honest-empty card (\"Nothing set yet\") if any sub-entity is blank.\n3. Edit details card — fields + logo button + app-language select + comms\n   toggle chips (show an `--on` green-bordered chip).\n4. Address + Insurance grid (incl. a coverage-in-dollars value, cert \"on file\").\n5. Tax + Contract-defaults grid (masked TIN \"(on file: **-***…)\", W-9 uploaded).\n6. Payments card — at least one enabled (green-bordered) row with handle +\n   retype-confirm fields.\n7. Danger Zone card (red `.settings-danger`).\n\n## Transient states to drive\n1. **loading** — hard-reload `/settings`; capture the Skeleton (shimmer +\n   page-header + 2-row card grid) before `/profile` resolves.\n2. **error** — block/fail `GET /profile` (offline or stall) → the\n   `.dashpage-error` message (NOTE: this class has no CSS rule — it renders as\n   unstyled text; capture as-is and flag).\n3. **saving / saved** — edit a field and blur; capture the \"Saving…\" then\n   \"Saved\" (green) status in the card head before it clears.\n4. **upload** — click Upload logo/cert/W-9; capture \"Uploading…\"; if possible\n   drive a 413 to capture the \"too large\" error copy.\n5. **payments-mismatch** — enable Venmo, type a handle, type a different\n   confirm → red-bordered confirm input + \"doesn't match\" line.\n6. **danger-armed** — type `DELETE` in the confirm field → wipe button turns\n   solid red `#a83b3b` and enables. **Do NOT click** (irreversible wipe).\n\n## Motion\n- Only the load-state Skeleton shimmer (see shared-components/skeletons for the\n  keyframes). Film it once; everything else is static. Reduced-motion: the\n  global token CSS clamps it to 0.01ms.\n"}]}, {"name": "verify", "pageMd": "# Page: `/verify`\n\n**Route:** `routes/verify.tsx` · **Source copied to:** `js/verify.tsx`, plus\nisland in `components/code-input/`.\n\n## Purpose\nStep 2 of auth: enter the 6-digit OTP texted to the phone from `/login`. Dev\nmaster OTP is **`000000`**.\n\n## Classification & data flow\n- **Page tier:** **static SSR** + one island.\n- **Required query (server):** reads `?phone=…`; **if absent → 302 → `/`**\n  (can't verify without a target number).\n- **Auth guard (server):** `loadUser` → if authenticated, **302 → `/dashboard`**.\n- **Language (server):** `langFromCookie` ?? `pickLangFromAcceptLanguage`;\n  uses `STRINGS[lang]` directly (the `s[...]` lookups) plus `tFor`.\n- **Phone masking (server):** `formatPhoneDisplay(e164)` → `(xxx) xxx-xxxx`\n  for the \"code sent to …\" line.\n- **Island:** `<CodeInput phoneNumber={phone} initialLang={lang} />` owns the\n  digit boxes, paste/auto-advance, the verify POST, error state, and the\n  redirect on success.\n\n## `<Head>`\n- `<title>{verify.h1} · {brand.name}</title>`\n- `<link rel=\"stylesheet\" href=\"/verify.css\">` (shared with `/login`).\n\n## Layout / composition order\n```\n.verify-shell\n  .verify-card\n    a.brand → href=\"/\"                       (logo-monster.png + wordmark)\n    ol.pm-steps  (aria verify.steps.aria)     ← 3-step progress indicator\n      li.pm-steps__item--done   ✓  phone\n      span.pm-steps__bar--done\n      li.pm-steps__item--active 2  code   (#pm-step-code)\n      span.pm-steps__bar (#pm-step-bar-2)\n      li.pm-steps__item         3  in     (#pm-step-in)\n    h1   verify.h1                            (32px)\n    p.muted   verify.lede + <strong>{masked phone}</strong>\n    <CodeInput phoneNumber initialLang />     ← ISLAND\n```\nThe `#pm-step-*` ids are advanced by the island as the user progresses\n(code accepted → step 3 lights up). Verify whether `static/verify-scripts.js`\nparticipates (the page does not `<script>` it directly; the island drives it).\n\n## Components on this page\n| Component | Where | Tier |\n|---|---|---|\n| `code-input` (`CodeInput`) | `pages/verify/components/code-input/` | island — OTP entry, auto-advance/paste, verify POST, success redirect |\n\n## Capture checklist (auth-free → capture-ready)\n- URL: `/verify?phone=%2B15551234567` (logged-out; needs the `phone` param or it\n  redirects to `/`). Logged-in 302s to dashboard.\n- Viewports: 390, 768, 1280 (single-column card; check `verify.css` `@media`).\n- States: empty, partially typed, full + verifying, invalid-code error, the\n  3-step progress at step 2 vs step 3. Master OTP `000000` advances it.\n\n## Build order\ndesign tokens → `verify.css` shell + `.pm-steps` → `code-input` island → this page.\n", "components": [{"name": "code-input", "component": "CodeInput", "spec": "# CodeInput\n\nThe 6-digit OTP entry on `/verify`. Six single-char boxes, paste/autofill aware,\nauto-submits when full, POSTs to verify, animates the SSR step-indicator to\n\"You're in\", then navigates to the dashboard (or onboarding for new users).\n\n## 1. Classification & behavior\n- **Bucket:** `island` (`islands/CodeInput.tsx`).\n- **Interaction tier:** `island` (client-only state) performing a **server\n  mutation via fetch + manual navigation** + **direct DOM mutation of the\n  SSR-rendered `.pm-steps` indicator** (it reaches outside its own tree by id).\n- **Client state owned (`useState`/`useRef`):**\n  - `refs: (HTMLInputElement|null)[]` — the six slot inputs (callback-ref array).\n  - `digits: string[6]` — the entered characters.\n  - `submitting: boolean` — disables CTA, swaps label to `verify.busy`.\n  - `errorKey: \"verify.errInvalid\"|\"verify.errExpired\"|\"verify.errRate\"|null`.\n  - `shake: boolean` — toggles `.shake` on `.code-input` for 380ms on failure.\n  - `cooldown: number` — resend countdown seconds (ticks via `setTimeout`,\n    decrement loop in an effect — see Liveness).\n  - `force` — a counter bumped on `langSignal` change to re-render.\n- **Props:** `phoneNumber` (E.164, from `?phone=`), `initialLang` (SSR-resolved).\n- **Mount effect:** reads `localStorage[\"pm:lang\"]` → sets `langSignal` to it ??\n  `initialLang` ?? `\"en\"`; focuses slot 0; subscribes to `langSignal`.\n- **Server mutation + feedback:**\n  - `submit(code?)` (auto-fires when 6th digit lands, on full paste/autofill, or\n    via the CTA) → `verifyClient.verifyOtp({ phoneNumber, code })` →\n    **POST `/api/auth/verify-otp`**.\n    - **Success (`result.ok`):** persist `localStorage[\"pm:last-phone\"]`; then\n      **mutate the SSR `.pm-steps` DOM directly** — flip `#pm-step-code` from\n      `--active`→`--done` (dot \"✓\"), add `--done` to `#pm-step-bar-2`, set\n      `#pm-step-in` `--active` (dot \"✓\"); after a **400ms** delay\n      `location.href = result.redirectTo` (`/dashboard?welcome=back` or\n      `/assistant?onboard=1` for new users — decided in `clients/verify.ts`).\n    - **Failure:** map backend error → `errorKey`; set `shake` true for 380ms;\n      clear all digits; refocus slot 0.\n  - `resend()` → guarded by `cooldown>0` → set `cooldown=30` → POST\n    `/api/auth/send-otp` (via `verifyClient.resendOtp`). Errors are swallowed\n    (\"keep cooldown\").\n- **Liveness:** request-response (verify + resend POSTs). PLUS a **client\n  countdown timer**: a `useEffect` on `cooldown` schedules a 1s `setTimeout`\n  that decrements until 0 — a self-driven tick, not pushed from a server. No\n  websocket, no polling of server state.\n- **ANTI-PATTERN flagged (cross-tree DOM mutation):** the success path imperatively\n  edits SSR-rendered nodes by `document.getElementById(\"pm-step-code\"/\"pm-step-in\"/\n  \"pm-step-bar-2\")` and rewrites `.pm-steps__dot` textContent. The step indicator\n  is owned by the route (`verify.tsx`), not the island, so this is an island\n  reaching across the boundary — fragile if the markup ids change. **Fix:** lift\n  the step state into the island (render the `.pm-steps` from CodeInput, or share\n  a signal) so the \"You're in\" transition is declarative, not `getElementById`\n  surgery. NOTE: this is NOT the `location.reload()` anti-pattern — it's a forward\n  `location.href` navigation, which is correct for a post-auth redirect.\n- **DEAD sibling (do not wire):** `static/verify-scripts.js` is a complete\n  plain-JS port of this island (posts to `/api/auth/verify-otp`, same shake/\n  resend logic). It is **referenced by nothing** (grep: zero `<script\n  src=\"/verify-scripts.js\">`); `verify.tsx` mounts the island instead. Confirmed\n  dead code — see verify.md.\n- **Data-shape hazards:**\n  - `setSlot` handles three input shapes: single char (normal typing),\n    multi-char (iOS SMS autofill / Android one-time-code / paste that bypasses\n    the paste handler — spreads across slots from index `i`), and the dedicated\n    `onPaste` handler (spreads a clipboard string). All three converge on\n    auto-submit when slot 6 fills.\n    - **Latent bug:** in the single-char branch, auto-submit at the last slot\n      builds the code as `[...digits.slice(0, i), v].join(\"\")` — `digits` is the\n      *previous* render's state (stale closure), and it drops slots `i+1..5`.\n      For sequential entry slots 0–4 are already set so this is fine; but it's a\n      stale-state pattern worth noting.\n  - `verifyClient.verifyOtp` translates raw backend responses into a discriminated\n    union; unknown errors collapse to `invalid_code`.\n\n## 2. Anatomy\n```\n<>\n  <div class=\"code-input [shake]\" onPaste=onPaste>\n    digits.map(i => <input ref type=text inputMode=numeric\n        autoComplete=one-time-code value={d}\n        onInput=setSlot onKeyDown=onKeyDown(Backspace→prev)\n        aria-label={t(\"verify.digitLabel\",{n:i+1})} />)   ← ×6\n  </div>\n  {errorKey && <p class=\"error\" role=\"alert\">{s[errorKey]}</p>}\n  <button class=\"btn btn-primary btn-lg\" type=button\n          disabled={submitting || code.length!==6} onClick={submit}>\n    {submitting ? t(\"verify.busy\") : s[\"verify.cta\"]}      ← \"Verify\"\n  </button>\n  <div class=\"meta\">\n    <a href=\"/\">{s[\"verify.editPhone\"]}</a>                ← \"Wrong number? Edit\"\n    <button onClick=resend disabled={cooldown>0}>\n      {cooldown>0 ? \"Resend in {n}s\" : \"Resend code\"}\n    </button>\n  </div>\n</>\n```\nMounted inside the route's `.verify-card`, below the `.pm-steps` indicator,\n`<h1>` (\"Check your phone\") and the lede with the masked phone.\n- **Slots/children:** none.\n\n## 3. Props\n| name | type | default | control | signal? |\n|---|---|---|---|---|\n| `phoneNumber` | string (E.164) | — (required) | text | no |\n| `initialLang` | `\"en\"\\|\"es\"` | `undefined` → `\"en\"` | select | seeds langSignal |\n\n## 4. States → cases\n| state | meaning | case |\n|---|---|---|\n| default | empty slots, slot 0 focused, CTA disabled | `cases/default/default.json` |\n| filled | all 6 digits entered, CTA enabled | `cases/filled/filled.json` |\n| submitting | verify POST in flight, CTA shows busy | `cases/submitting/submitting.json` |\n| error | wrong code → `.error` + `.shake`, digits cleared | `cases/error/error.json` |\n| resend-cooldown | resend disabled, \"Resend in 27s\" | `cases/resend-cooldown/resend-cooldown.json` |\n| es | Spanish strings (`langSignal=\"es\"`) | `cases/es/es.json` |\n\n## 5. Events\n- `ev.expect(e => e.source===\"input\" && e.type===\"input\")` → `setSlot(i,val)`:\n  single→fill+advance; multi→spread+autosubmit when full.\n- `ev.expect(e => e.source===\"input\" && e.type===\"keydown\" && e.key===\"Backspace\")`\n  → if empty, focus previous slot.\n- `ev.expect(e => e.source===\"div.code-input\" && e.type===\"paste\")` → `onPaste`:\n  spread clipboard digits, autosubmit if 6.\n- `ev.expect(e => e.source===\"button.btn-primary\" && e.type===\"click\")` →\n  `submit()` → POST `/api/auth/verify-otp`.\n- `ev.expect(e => e.source===\"div.meta button\" && e.type===\"click\")` →\n  `resend()` → POST `/api/auth/send-otp`, start 30s cooldown.\n- `ev.expect(e => e.source===\"div.meta a\" && e.type===\"click\")` → nav `/` (edit phone).\n- External (received): `langSignal` change → `force` re-render (new `s` strings).\n\n## 6. Motion (extracted)\n- **Shake — `@keyframes shake`** 360ms ease (added to `.code-input` for 380ms on\n  a failed verify, then removed): `0/100%{0} 20%{-8px} 40%{8px} 60%{-6px}\n  80%{6px}` on `translateX`.\n- **Input focus:** `transition: border-color 140ms, box-shadow 140ms`; focus =\n  green outline + `0 0 0 4px rgba(81,152,67,.20)` glow.\n- **CTA:** `.btn transition: transform 120ms, background 200ms, color 200ms`;\n  `:active` nudges `translateY(1px)`.\n- **Success step transition:** the `.pm-steps__dot` transitions\n  `background/color/box-shadow 280ms`; the newly-active \"You're in\" dot picks up\n  `pm-steps-pulse` (1.6s box-shadow halo). The island's 400ms pre-nav delay\n  exists to let this play. **Jank finding:** the 400ms hard timeout races the\n  navigation — on a slow redirect the pulse barely starts; fine, but the magic\n  number couples animation timing to navigation. Fix: drive nav off the\n  transition/animation end event, or keep the indicator inside the island.\n- **Reduced motion:** verify.css disables the active-dot pulse\n  (`@media (prefers-reduced-motion: reduce){.pm-steps__item--active .pm-steps__dot\n  {animation:none}}`); the global token clamp flattens shake + .btn transitions.\n\n## 7. Responsive (this component's own widths)\n- **No `@media` of its own.** Six 44×56px boxes with 8px gaps = ~328px, which\n  fits the `.verify-card` (max 440px, 32px padding → ~376px content) at every\n  width down to ~360px. On a 320px viewport the row can overflow the card\n  padding — verify.css does not narrow the slots, so confirm at 320px.\n- The shell (`.verify-shell`) re-centers above the soft keyboard via `--vvh`/\n  `--vvt` from the MobileViewport island.\n\n## 8. A11y\n- Each slot is a real `<input inputMode=\"numeric\" autoComplete=\"one-time-code\">`\n  with a unique `aria-label` (\"Digit 1\"..\"Digit 6\") — supports iOS/Android OTP\n  autofill. Error is `<p role=\"alert\">`. CTA + resend are real `<button>`;\n  edit-phone is a real `<a href=\"/\">`.\n- **Gaps:** no `aria-live` region narrates the auto-advance; the cross-tree\n  step-indicator update isn't announced; focus management on error (refocus slot\n  0) is good but not announced. Master dev OTP `000000` always verifies in dev.\n\n## 9. Used on\n- **`/verify`** only (`routes/verify.tsx` line 85). Page-local island.\n- Superseded `static/verify-scripts.js` (dead). Pairs with the SSR `.pm-steps`\n  indicator that `verify.tsx` renders and this island mutates on success.\n", "fixture": "{\n  \"category\": \"verify\",\n  \"folder\": \"code-input\",\n  \"controls\": {\n    \"phoneNumber\": { \"type\": \"text\", \"value\": \"+15125550142\" },\n    \"initialLang\": { \"type\": \"select\", \"value\": \"en\", \"options\": [\"en\", \"es\"], \"signal\": true }\n  }\n}\n", "cases": [{"name": "default.json", "content": "{\n  \"_name\": \"Default — empty slots, slot 0 focused\",\n  \"phoneNumber\": \"+15125550142\",\n  \"initialLang\": \"en\",\n  \"_signals\": { \"digits\": [\"\", \"\", \"\", \"\", \"\", \"\"], \"submitting\": false, \"errorKey\": null, \"shake\": false, \"cooldown\": 0, \"_langSignal\": \"en\" }\n}\n"}, {"name": "error.json", "content": "{\n  \"_name\": \"Error — wrong code (shake + cleared digits)\",\n  \"phoneNumber\": \"+15125550142\",\n  \"initialLang\": \"en\",\n  \"_signals\": { \"digits\": [\"\", \"\", \"\", \"\", \"\", \"\"], \"submitting\": false, \"errorKey\": \"verify.errInvalid\", \"shake\": true, \"cooldown\": 0, \"_langSignal\": \"en\" }\n}\n"}, {"name": "es.json", "content": "{\n  \"_name\": \"Spanish — errExpired shown (langSignal=es)\",\n  \"phoneNumber\": \"+15125550142\",\n  \"initialLang\": \"es\",\n  \"_signals\": { \"digits\": [\"\", \"\", \"\", \"\", \"\", \"\"], \"submitting\": false, \"errorKey\": \"verify.errExpired\", \"shake\": false, \"cooldown\": 0, \"_langSignal\": \"es\" }\n}\n"}, {"name": "filled.json", "content": "{\n  \"_name\": \"Filled — all six digits, CTA enabled\",\n  \"phoneNumber\": \"+15125550142\",\n  \"initialLang\": \"en\",\n  \"_signals\": { \"digits\": [\"4\", \"8\", \"2\", \"9\", \"1\", \"3\"], \"submitting\": false, \"errorKey\": null, \"shake\": false, \"cooldown\": 0, \"_langSignal\": \"en\" }\n}\n"}, {"name": "resend-cooldown.json", "content": "{\n  \"_name\": \"Resend cooldown — disabled, 'Resend in 27s'\",\n  \"phoneNumber\": \"+15125550142\",\n  \"initialLang\": \"en\",\n  \"_signals\": { \"digits\": [\"\", \"\", \"\", \"\", \"\", \"\"], \"submitting\": false, \"errorKey\": null, \"shake\": false, \"cooldown\": 27, \"_langSignal\": \"en\" }\n}\n"}, {"name": "submitting.json", "content": "{\n  \"_name\": \"Submitting — verify POST in flight\",\n  \"phoneNumber\": \"+15125550142\",\n  \"initialLang\": \"en\",\n  \"_signals\": { \"digits\": [\"4\", \"8\", \"2\", \"9\", \"1\", \"3\"], \"submitting\": true, \"errorKey\": null, \"shake\": false, \"cooldown\": 0, \"_langSignal\": \"en\" }\n}\n"}], "css": "/* ============================================================\n   CodeInput — 6-digit OTP entry island (/verify).\n   Extracted from static/verify.css (route <Head> links /verify.css).\n   Renders: .code-input > input × 6, an optional .error <p>, a\n   .btn.btn-primary.btn-lg verify button, and a .meta row with two\n   links/buttons. Shake animation toggled via the `.shake` class.\n   Tokens: --brand-green, --border-strong, --danger, --danger-bg,\n   --accent (pink), --radius-md/-sm/-pill, --font-heading, --shadow-sm.\n   ============================================================ */\n\n/* ---- the digit grid ---- */\n.code-input {\n  display: flex;\n  gap: 8px;\n  justify-content: center;\n}\n.code-input input {\n  width: 44px;\n  height: 56px;\n  text-align: center;\n  font-family: var(--font-heading);\n  font-weight: 800;\n  font-size: 24px;\n  border-radius: var(--radius-md);\n  border: 2px solid var(--border-strong);\n  background: #fff;\n  transition: border-color 140ms, box-shadow 140ms;\n}\n.code-input input:focus,\n.code-input input:focus-visible {\n  outline: 2px solid var(--brand-green);\n  outline-offset: 2px;\n  border-color: var(--brand-green);\n  box-shadow: 0 0 0 4px rgba(81, 152, 67, 0.20);\n}\n\n/* ---- error shake (added to .code-input for 380ms on a wrong code) ---- */\n.shake { animation: shake 360ms ease; }\n@keyframes shake {\n  0%, 100% { transform: translateX(0); }\n  20%      { transform: translateX(-8px); }\n  40%      { transform: translateX(8px); }\n  60%      { transform: translateX(-6px); }\n  80%      { transform: translateX(6px); }\n}\n\n/* ---- inline error message ---- */\n.error {\n  color: var(--danger);\n  background: var(--danger-bg);\n  padding: 10px 12px;\n  border-radius: var(--radius-sm);\n  font-size: 14px;\n}\n\n/* ---- verify button (shared .btn family, canonical verify.css) ---- */\n.btn {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  padding: 10px 18px;\n  border-radius: var(--radius-pill);\n  font-family: var(--font-heading);\n  font-weight: 700;\n  font-size: 15px;\n  transition: transform 120ms ease, background 200ms ease, color 200ms ease;\n}\n.btn:active { transform: translateY(1px); }\n.btn-primary {\n  background: var(--accent);          /* NOTE: verify.css maps .btn-primary to\n                                         --accent (pink), not --primary green */\n  color: #fff;\n  box-shadow: var(--shadow-sm);\n}\n.btn-primary:hover { background: var(--accent-hover); }\n.btn-lg { padding: 14px 24px; font-size: 16px; }\n.btn[disabled] { opacity: 0.55; cursor: not-allowed; }\n\n/* ---- the edit-phone / resend meta row (inside .verify-card) ---- */\n.verify-card .meta {\n  display: flex;\n  justify-content: space-between;\n  font-size: 14px;\n  color: var(--fg-muted);\n  margin-top: 4px;\n}\n\n/* Reduced motion: verify.css global token clamp neutralizes the shake +\n   .btn transitions; the .pm-steps active-dot pulse (driven by the SSR page,\n   not this island) is also disabled under prefers-reduced-motion. */\n", "cssName": "code-input.css", "source": "import { useEffect, useRef, useState } from \"preact/hooks\";\nimport { type Lang, langSignal, STRINGS } from \"../lib/lang.ts\";\nimport { t } from \"../lib/i18n.ts\";\nimport { verifyClient } from \"../clients/verify.ts\";\n\ninterface Props {\n  phoneNumber: string;\n  initialLang?: Lang;\n}\n\nconst SLOT_COUNT = 6;\n\nexport default function CodeInput({ phoneNumber, initialLang }: Props) {\n  // Single ref holding the slot inputs (a callback ref fills the array) —\n  // calling useRef inside a loop would violate the rules of hooks.\n  const refs = useRef<(HTMLInputElement | null)[]>([]);\n  const [digits, setDigits] = useState<string[]>(Array(SLOT_COUNT).fill(\"\"));\n  const [submitting, setSubmitting] = useState(false);\n  const [errorKey, setErrorKey] = useState<\n    \"verify.errInvalid\" | \"verify.errExpired\" | \"verify.errRate\" | null\n  >(null);\n  const [shake, setShake] = useState(false);\n  const [cooldown, setCooldown] = useState(0);\n  const [, force] = useState(0);\n\n  useEffect(() => {\n    const stored = globalThis.localStorage?.getItem(\"pm:lang\") as Lang | null;\n    langSignal.value = stored ?? initialLang ?? \"en\";\n    refs.current[0]?.focus();\n    const unsub = langSignal.subscribe(() => force((n) => n + 1));\n    return () => unsub();\n  }, []);\n\n  useEffect(() => {\n    if (cooldown <= 0) return;\n    const id = setTimeout(() => setCooldown((s) => s - 1), 1000);\n    return () => clearTimeout(id);\n  }, [cooldown]);\n\n  const lang = langSignal.value;\n  const s = STRINGS[lang];\n\n  function setSlot(i: number, val: string) {\n    const digitsOnly = val.replace(/\\D/g, \"\");\n    // Multi-digit input — iOS SMS autofill, Android one-time-code suggestion,\n    // or a plain paste that bypasses the paste handler. Spread the digits\n    // across the remaining slots starting at i, focus the last filled box,\n    // and auto-submit if we've completed the full code.\n    if (digitsOnly.length > 1) {\n      setDigits((prev) => {\n        const next = prev.slice();\n        for (let k = 0; k < digitsOnly.length && i + k < SLOT_COUNT; k++) {\n          next[i + k] = digitsOnly[k];\n        }\n        return next;\n      });\n      const lastIdx = Math.min(i + digitsOnly.length - 1, SLOT_COUNT - 1);\n      refs.current[lastIdx]?.focus();\n      if (i + digitsOnly.length >= SLOT_COUNT) {\n        submit(digitsOnly.slice(0, SLOT_COUNT));\n      }\n      return;\n    }\n    const v = digitsOnly.slice(-1);\n    setDigits((prev) => {\n      const next = prev.slice();\n      next[i] = v;\n      return next;\n    });\n    if (v && i < SLOT_COUNT - 1) refs.current[i + 1]?.focus();\n    if (v && i === SLOT_COUNT - 1) submit([...digits.slice(0, i), v].join(\"\"));\n  }\n\n  function onPaste(e: ClipboardEvent) {\n    const pasted = (e.clipboardData?.getData(\"text\") ?? \"\").replace(/\\D/g, \"\")\n      .slice(0, SLOT_COUNT);\n    if (pasted.length === 0) return;\n    e.preventDefault();\n    const next = pasted.padEnd(SLOT_COUNT, \"\").split(\"\").slice(0, SLOT_COUNT);\n    setDigits(next);\n    refs.current[Math.min(pasted.length, SLOT_COUNT - 1)]?.focus();\n    if (pasted.length === SLOT_COUNT) submit(pasted);\n  }\n\n  function onKeyDown(i: number, e: KeyboardEvent) {\n    if (e.key === \"Backspace\" && !digits[i] && i > 0) {\n      refs.current[i - 1]?.focus();\n    }\n  }\n\n  async function submit(code?: string) {\n    const finalCode = code ?? digits.join(\"\");\n    if (finalCode.length !== SLOT_COUNT) return;\n    setSubmitting(true);\n    setErrorKey(null);\n    try {\n      const result = await verifyClient.verifyOtp({\n        phoneNumber,\n        code: finalCode,\n      });\n      if (result.ok) {\n        // Persist the verified phone for next-visit one-tap login.\n        try {\n          globalThis.localStorage?.setItem(\"pm:last-phone\", phoneNumber);\n        } catch { /* SSR-safe */ }\n        // Animate Step 3 (\"You're in\") fill before navigating — visual\n        // continuity with the landing-page progress bar.\n        const codeStep = document.getElementById(\"pm-step-code\");\n        const inStep = document.getElementById(\"pm-step-in\");\n        const bar2 = document.getElementById(\"pm-step-bar-2\");\n        if (codeStep && inStep) {\n          codeStep.classList.remove(\"pm-steps__item--active\");\n          codeStep.classList.add(\"pm-steps__item--done\");\n          const codeDot = codeStep.querySelector(\".pm-steps__dot\");\n          if (codeDot) codeDot.textContent = \"✓\";\n          if (bar2) bar2.classList.add(\"pm-steps__bar--done\");\n          inStep.classList.add(\"pm-steps__item--active\");\n          const inDot = inStep.querySelector(\".pm-steps__dot\");\n          if (inDot) inDot.textContent = \"✓\";\n        }\n        setTimeout(() => {\n          globalThis.location.href = result.redirectTo;\n        }, 400);\n        return;\n      }\n      const map = {\n        invalid_code: \"verify.errInvalid\",\n        expired: \"verify.errExpired\",\n        rate_limited: \"verify.errRate\",\n      } as const;\n      setErrorKey(map[result.error]);\n      setShake(true);\n      setTimeout(() => setShake(false), 380);\n      setDigits(Array(SLOT_COUNT).fill(\"\"));\n      refs.current[0]?.focus();\n    } catch {\n      setErrorKey(\"verify.errInvalid\");\n    } finally {\n      setSubmitting(false);\n    }\n  }\n\n  async function resend() {\n    if (cooldown > 0) return;\n    setCooldown(30);\n    setErrorKey(null);\n    try {\n      await verifyClient.resendOtp({ phoneNumber, language: lang });\n    } catch { /* keep cooldown */ }\n  }\n\n  return (\n    <>\n      <div class={`code-input ${shake ? \"shake\" : \"\"}`} onPaste={onPaste}>\n        {digits.map((d, i) => (\n          <input\n            key={i}\n            ref={(el) => {\n              refs.current[i] = el;\n            }}\n            type=\"text\"\n            inputMode=\"numeric\"\n            autoComplete=\"one-time-code\"\n            value={d}\n            onInput={(e) => setSlot(i, (e.target as HTMLInputElement).value)}\n            onKeyDown={(e) => onKeyDown(i, e)}\n            aria-label={t(\"verify.digitLabel\", { n: i + 1 })}\n          />\n        ))}\n      </div>\n      {errorKey\n        ? <p class=\"error\" role=\"alert\">{s[errorKey] as string}</p>\n        : null}\n      <button\n        class=\"btn btn-primary btn-lg\"\n        type=\"button\"\n        disabled={submitting || digits.join(\"\").length !== SLOT_COUNT}\n        onClick={() => submit()}\n      >\n        {submitting ? t(\"verify.busy\") : s[\"verify.cta\"]}\n      </button>\n      <div class=\"meta\">\n        <a href=\"/\">{s[\"verify.editPhone\"]}</a>\n        <button type=\"button\" onClick={resend} disabled={cooldown > 0}>\n          {cooldown > 0\n            ? s[\"verify.resendIn\"].replace(\"{n}\", String(cooldown))\n            : s[\"verify.resend\"]}\n        </button>\n      </div>\n    </>\n  );\n}\n", "sourceName": "CodeInput.tsx", "checklist": "# Capture checklist — CodeInput\n\n**Theme:** light only.\n**Route/URL:** `http://localhost:5280/verify?phone=%2B15125550142` — the route\n**302s to `/` if `?phone=` is missing**, so always include it. Auth-free →\n**capture-ready** (302s to `/dashboard` if already logged in).\n\n## Auth\nNone to reach the page. Dev **master OTP `000000`** always verifies — use it to\ndrive the success → step-indicator-fill → redirect sequence.\n\n## Viewports (component has no own @media)\n- **Desktop 1280px** — card centered; the six 44×56 boxes sit comfortably.\n- **Mobile 390px** — verify-shell re-centers the card above the soft keyboard\n  (`--vvh`/`--vvt`); the OTP keypad should auto-open (`inputMode=numeric`).\n- **320px edge** — verify the slot row doesn't overflow the card padding.\n\n## Element(s) to crop\n- The whole `.verify-card` (brand + `.pm-steps` + h1 + lede + CodeInput).\n- The `.code-input` row alone (empty, filled, focused-slot, shake frame).\n- The `.pm-steps` indicator at step 2 (active \"Code\") and the post-success\n  \"You're in\" state (all three done/active).\n\n## Transient states to drive\n1. **default** — empty, slot 1 focused.\n2. **filled** — type `482913` (boxes fill, auto-advances, CTA enables).\n3. **submitting** — submit `000000`; CTA shows the busy label briefly.\n4. **success** — after `000000`, watch `.pm-steps` flip step 2→done and step 3→\n   active \"✓\" (400ms before the redirect to `/dashboard?welcome=back`). Stub or\n   throttle to hold this frame.\n5. **error** — type any wrong 6 digits → `.error` message + `.shake` (380ms),\n   digits clear, slot 1 refocuses. Capture each error string (invalid/expired/\n   rate) if drivable.\n6. **resend-cooldown** — tap \"Resend code\" → button disables, counts \"Resend in\n   30s\" → 0.\n7. **es** — `localStorage[\"pm:lang\"]=\"es\"` (or arrive with `&lang=es`) → Spanish.\n\n## Motion to film\n- `shake` (360ms) on a wrong code.\n- Input focus glow transition (140ms).\n- `.pm-steps__dot` 280ms recolor + `pm-steps-pulse` halo on the success step.\n- Reduced motion: re-run; shake + pulse should be neutralized (clamp + the\n  explicit `prefers-reduced-motion` rule on the active dot).\n"}]}], "stats": {"pages": 17, "shared": 11, "components": 56, "cases": 282}};
