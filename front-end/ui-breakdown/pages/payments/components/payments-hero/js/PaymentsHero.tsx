/* SOURCE POINTER — full verbatim source lives in:
 *   ../../payments-page/js/PaymentsPage.tsx
 *     · function PaymentsHero()  lines 463–589
 *     · METHOD_AV_BG             lines 80–90
 *     · METHOD_ICON / P2PIcon    lines 92–144
 *     · methodLabel              lines 77–78
 *
 * The `.pph` editorial header (own stylesheet /payments.css). Left = eyebrow
 * + amount headline ("$X showed up this month" or a "fresh" empty variant) +
 * sub + two assistant-seeded CTAs. Right = `.pph__stack` — up to three
 * rotated `.pph__stub` recent-payment cards (aria-hidden decoration). Pure
 * presentational; no client state, no fetch. See payments-hero.md. */
export {};
