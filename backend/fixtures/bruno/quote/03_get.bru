meta {
  name: 03 get quote
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/quotes/{{quoteId}}
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("id matches", function() {
    expect(res.getBody().id).to.equal(bru.getVar("quoteId"));
  });
}
