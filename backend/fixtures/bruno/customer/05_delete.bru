meta {
  name: 05 delete customer
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/customers/{{customerId}}
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("ok flag", function() {
    expect(res.getBody().ok).to.equal(true);
  });
}
