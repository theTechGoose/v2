meta {
  name: 05 delete invoice
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/invoices/{{invoiceId}}
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("ok flag", function() {
    expect(res.getBody().ok).to.equal(true);
  });
}
